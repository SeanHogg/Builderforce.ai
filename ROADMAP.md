# Builderforce.ai — Roadmap & Gap Register

> Single source of truth for **outstanding** work (TODO · In Progress · Partial) and deferred issues for the Builderforce.ai sub-project. The [README](./README.md) documents what exists today; completed/shipped/resolved items live in **[DONE.md](./DONE.md)**. This file tracks only what is next and what is broken. Deferred issues found during work are appended to the **Consolidated Gap Register** below (open items only — move an item to DONE.md when it is resolved).

> **Grouped by DOMAIN since 2026-08-22.** The register is sectioned by the platform’s own bounded contexts — the seats `DOMAIN_MANIFEST` declares in [api/src/application/kernel/DomainService.ts](./api/src/application/kernel/DomainService.ts) — rather than the ad-hoc numbered groups this file used to carry. One entry has one owning domain, and **placement is decided by evidence, not by topic**: an entry sits with the domain whose schema module declares the tables it cites (`api/src/infrastructure/database/schema/<domain>.ts`, which `check-domain-boundary.mjs` calls "that map"). Five declared domains (`revenue`, `investor`, `operations`, `people`, `support`) have no open entries and are therefore not sectioned here; that is a fact about the register, not about the roster.

> **Re-validated against the running code — 2026-08-20, with a migration-ledger and guard re-measure on 2026-08-22.** Every claim carrying a number was re-measured, every "X does not exist" was grepped, and everything that turned out to be shipped was moved to [DONE.md](./DONE.md). Bullets are technically compound, so the per-domain counts in the index are a guide, not a ticket count.

---

## How an entry is written

Each roadmap item must have one primary owner, one independently verifiable state, and an acceptance criterion tied to the declared method: the arc **Idea → Make → Run → Measure** for WHERE it sits, and the loop **Read → Prove → Build** for what it does — an item in Build names the proof form it serves and the kill condition Measure will grade. Split compound bullets when their subclaims differ in owner or completion state.

Authenticated UI work is never a standalone product page: it registers a destination or Canvas object and opens through the persistent shell. Every touched UI surface must satisfy PRD 21 — canonical primitives/tokens, all five locales, both themes, 360px, coarse-pointer and keyboard parity, visible focus, reduced motion, visible-disabled entitlements, and a bottom-centre Canvas composer.

An entry names its own blocker or it is not blocked. Scheduling around other work in flight is not a blocker and does not belong in this file — an entry records what the code does, what was measured, and what a person or a credential has to supply before it can move.

## Platform Roadmap (umbrella — Builderforce.ai · BuilderForce Agents · Evermind/SSM)

> **Vision**: Human as the manager — developers supervise, prioritize, approve, and steer AI agents rather than writing code themselves.

### 🌟 North Star — **Idea to Real**

> The method is not a slogan on this page; it is declared as spec data in two places and ratcheted against itself by `frontend/scripts/check-methodology.mjs`. **This roadmap uses that vocabulary and no other.**
>
> **THE ARC — `Idea → Make → Run → Measure` (→ Market → Expand).** Where you are. Owned by `STAGES` in [frontend/src/lib/navGroups.ts](./frontend/src/lib/navGroups.ts).
>
> **THE LOOP — `Read → Prove → Build`.** What you do to cross from Idea into Make. Owned by [api/src/application/realization/](./api/src/application/realization/), which declares **eight proof forms** — `demo-video`, `clickable-prototype`, `smoke-test`, `wizard-of-oz`, `poc`, `pilot`, `phone-line`, `live-system` — each carrying the question it answers, a fidelity and an effort. A ninth is a registry entry, never a branch.
>
> **Read and Prove spend nothing; only Build spends run budget.** The two acts that decide whether the expensive one is worth doing must never be why somebody skips them.
>
> **Every proof carries a kill condition**, and that number is graded in Measure, which closes the loop back to Idea. A proof with no condition that could fail is not a proof, it is a launch with extra steps.
>
> A roadmap item earns its place by moving a real user through that loop. Enabling work (runtime, gateway, schema) is legitimate and is marked as such — it does not outrank a broken Idea→Real journey.

## Consolidated Feature Register

| Feature | Area | Priority | Target | Status |
|---------|------|----------|--------|--------|
| **REVENUE** | | | | |
| Managed Agent hosting add-on ($49/mo/Agent) | Revenue | P0 | Q3 2026 | 🔄 In Progress |
| Onboarding funnel — first Agent in < 5 min | Revenue | P1 | Q3 2026 | 📋 Planned |
| Agent marketplace monetization (listing + tx fee, incl. Stripe checkout) | Revenue | P1 | Q3 2026 | 🔄 Partial — creation AND knowledge listings both settle through one verified-checkout primitive (2026-08-19); live end-to-end still awaits the Stripe secrets below |
| Enterprise license GA (on-prem Docker) | Revenue | P1 | Q4 2026 | 📋 Planned |
| SOC 2 Type I audit | Revenue | P1 | Q4 2026 | 📋 Planned |
| **MARKET PARITY (runtime)** | | | | |
| Live orchestration workspace (real-time DAG + task status) | Agents | P0 | Q3 2026 | 🔄 Partial |
| Inline diff / pair-programming mode (TUI accept/reject) | Agents | P1 | Q3 2026 | 🔄 Partial |
| Session auto-checkpoint (`/sessions`, `/undo`, `/fork`) | Agents | P1 | Q3 2026 | 🔄 Partial |
| Remote task result streaming (replaces fire-and-forget dispatch) | Agents | P2 | Q3 2026 | 📋 Planned |
| Multi-model role routing (per-step model assignment) | Agents | P2 | Q3 2026 | 📋 Planned |
| Spec import from GitHub Issues / Linear / Jira | Agents | P2 | Q3 2026 | 📋 Planned |
| Remaining Creation Canvas actions onto the correlated outcome contract (correlation coverage → 100%) | Analytics | P0 | Q3 2026 | 🔄 In Progress |
| Rendered presentation export of the value brief (copyable brief ships today) | Revenue | P1 | Q3 2026 | 🔄 Partial |
| **PHASE 6 — Dev Analytics & Team Intelligence** | | | Q3–Q4 2026 | 📋 Planned |
| Activity engine, integration engine (Jira/Bitbucket/GitHub/…), reporting/scheduling, PRD multi-agent orchestration, analytics UI | 6 | P0–P1 | Q3–Q4 2026 | 📋 Planned |
| **PHASE 7 — Agentic Security & Governance** ([PRD 08](./specs/builderforce/08-prd-agentic-governance.md)) | | | Q3 2026 | 📋 Planned |
| Policy packs + `governance-auditor` agent + `/api/governance` findings + BLOCKER-gates-merge + auto SOC2 evidence + governance portal | 7 | P1–P2 | Q3 2026 | 📋 Planned |
| **LATER PHASES** | | | | |
| Local LLM execution (llama.cpp/Ollama tool-calling, hybrid routing, SSM benchmarks, WebGPU CPU/WASM fallback) | 3 | P2–P3 | Q3 2026 | 📋 Planned |
| Multi-agent orchestration at scale (task DAG UI, cross-agent context/memory mesh, streaming aggregation) | 4 | P1–P2 | Q3–Q4 2026 | 📋 Planned |
| Production hardening (self-hosted Docker, fleet LB/failover, immutable audit + SIEM export, template gallery) | 5 | P1–P3 | Q4 2026 | 📋 Planned |

## ARR Milestones

| Period | Target ARR | Key driver |
|--------|-----------|------------|
| Q3 2026 | $50K | Teams plan + BuilderForce Agents LLM |
| Q4 2026 | $200K | Enterprise pilots + marketplace |
| Q1 2027 | $500K | Enterprise + marketplace |
| Q2 2027 | $1M | Scale + enterprise contracts |

## Key Metrics

**North-star metric:** percentage of ideas that reach a **graded proof** — a Build whose kill condition was measured, not merely a deliverable that was produced.

**Read → Prove:** time from first prompt to a chosen proof form · share of ideas that Read before they Build. **Build:** proofs reaching a real, reachable artifact · validation/pass rate · delivery success and retry rate. **Measure:** share of proofs whose kill condition was actually graded — the one metric that separates this method from a launch with extra steps. **Collaboration:** sessions inviting at least one human or agent · agent group-chat participation · successful synthesis rate. **Compounding value:** sessions resumed within 7 and 30 days · created outputs reused as inputs. **Efficiency:** human interventions per successful delivery · cost and latency per delivered outcome. **Measurement integrity:** percentage of meaningful actions with a correlated terminal outcome.

GitHub stars, package downloads, workflow counts, MCP connections, and active-agent counts remain operational or acquisition diagnostics. They are not evidence of customer value and do not lead roadmap prioritization.

Every metric is computed from the correlated outcome ledger and supports the same content-free aggregation dimensions: session, project, tenant, and platform. Platform aggregates must enforce minimum cohort sizes and never expose another tenant's session content. The session scorecard compares the current session with an aggregate tenant baseline; project and tenant rollups power value reviews, and privacy-safe platform rollups power the sales deck. Metric contract: **[Creation outcome metrics](./docs/design/creation-canvas/OUTCOME-METRICS.md)**.

## Claim-to-Proof Product Program

Public copy describes evidence available today; stronger promises become roadmap requirements until their acceptance criteria are continuously verified. This is the marketing surface of the same **Prove** act — a claim with no test behind it is the marketing version of a proof with no kill condition.

**Claim policy:** **Available** means shipped, reachable, and covered by repeatable verification. **Beta** means shipped with a named limitation, environment, or incomplete coverage. **Planned** is approved direction and must not be presented as current capability. Avoid `every`, `always`, `never`, `100%`, `zero`, `entire`, and `instant` unless a test or service objective proves the stated boundary. Every public capability must declare its execution/data boundary, prerequisites, status, and last verification date.

| Priority | Desired promise | Current honest position | System improvement and proof required | Status |
| --- | --- | --- | --- | --- |
| P0 | One creative canvas from idea to real | The canvas connects supported artifacts and hands approved work into supported delivery surfaces. | Release-gated journey tests for each marketed artifact: create, edit, review, hand off, reopen, recover. | 🔄 Partial |
| P0 | People remain in control of agent work | Policy gates cover configured execution paths; policy-resolution failure now blocks dispatch (shipped 2026-08-04). | Enumerate every mutating tool as blocked, approval-required, policy-auto-approved, or exempt; fail CI on an unclassified mutation. | 🔄 In Progress |
| P0 | Complete agent auditability | Supported workflows emit execution and decision records. | Common audit envelope for every mutation entry point with actor, tenant, input reference, decision, result, and correlation ID; reconcile sampled writes against audit records. | 🔄 Partial |
| P0 | Private, local AI work | Supported WebGPU computation is local; collaboration, remote evaluation, generation, and publishing can transmit data. Public capability and named-integration boundary matrices shipped 2026-08-04. | Extend the boundary manifest to every action, show it before execution, and retain it in provenance. | 🔄 In Progress |
| P1 | Evermind stays current | Keyed project-memory updates can replace superseded facts; source quality and retrieval coverage still matter. | Provenance, observed-at timestamps, contradiction detection, freshness budgets, stale-source alerts, and published correction-retention/stale-recall evaluations. | 📋 Planned |
| P1 | The same work is available in VS Code | VS Code is an engineering surface for relevant canvas and delivery context. | Web-to-extension parity matrix and contract tests for sessions, objects, comments, reviews, approvals, repository context, diagnostics, and delivery actions. | 🔄 Partial |
| P1 | Connected delivery without tool sprawl | Builderforce reduces context switching while connected services remain independent systems. | Integration health, sync direction, last-sync state, conflict handling, source-of-truth selection, and round-trip recovery tests per marketed connector. | 🔄 Partial |
| P1 | Costs connect to outcomes | Recorded model usage and human effort roll up where instrumentation exists. | Coverage indicators for unattributed spend/effort, immutable source references for priced usage, reconciliation reports, and traceable task-to-initiative rollups. | 🔄 Partial |
| P2 | Create "anything" | The platform supports a broad, named set of artifact types rather than literally every medium or runtime. A code-owned public registry with status, boundary, prerequisites, evidence, and verification date shipped 2026-08-04. | Add limits, exports, owner, and a working example to each registry entry; extend coverage from public surfaces to every artifact and action. | 🔄 In Progress |
| P2 | Model and provider freedom | Multiple supported cloud and local options exist; availability changes. | Generate public provider copy from the live catalog by plan, region, credential type, and runtime; remove manually maintained counts. | 📋 Planned |
| P2 | Fair competitive comparison | Criteria-first pages and five indexed comparison articles now avoid static price/provider scorecards (shipped 2026-08-04). | Any future vendor-specific fact requires a primary source, observation date, reviewer, and automatic expiry. | 🔄 In Progress |
| P2 | Clear value for CEOs and revenue leaders | Broad capability exists; outcome proof is still limited. | Measure Idea → chosen proof → Build → graded kill condition; publish customer evidence only with permission. | 🔄 In Progress |

**Marketing release gate:** assign status; link the owning capability and automated evidence; state prerequisites and data boundaries; constrain universal wording to a tested boundary; source and date comparisons; derive pricing and numeric claims from runtime sources of truth.

## Next Milestones (by stage of the arc)

**P0 — Idea → Make:** release-gate the canonical agent group-chat journey and every marketed delivery adapter; expand the shared deliverable manifest to image, voice, repository, mobile, model, and data outputs; make retry/recovery and validation visible on the Canvas.

**P1 — Make (collaborative):** directed human/agent mentions, moderator controls, parallel-run budget visibility, object-level review/approval, and cross-surface web/VS Code parity for conversation, artifacts, provenance, and delivery.

**P2 — Run → Measure:** publishable Creation templates, deliverable reuse as Canvas inputs, version comparison, portable export/import, and project/tenant value rollups sourced from correlated outcomes. A proof's kill condition already grades itself where a generated console posts one (`application/realization/realizationVerdict.ts`); what Measure still lacks is the **45 domain metric keys** that let each seat chart the number it manages by — fourteen of the seventeen seats still write none (see [Platform · PRD 20](#-platform--data-model-shell-i18n-vs-code-cicd--cost)).

**Supporting platform backlog (enabling work, not the arc):** remote task-result streaming, execution WebSockets, live runtime DAG, local gateway MCP provider, diff staging, fleet management, model-cost alerts, and cross-agent memory remain valid. They do not outrank a broken Idea→Real journey.

**Program — hired.video absorption.** Port the whole of hired.video into Builderforce.ai so its traffic can be redirected here. Plan: [PRD 18](./specs/builderforce/18-prd-hired-video-port.md); **100% deprecation** (operator decision 2026-08-07) — it ends as a redirect shell with nothing running behind it, so every surface lands here or is a stated product decision. **The 2026-08-22 mapping audit changed the shape of what is left, and the change is large:** the remaining tracks were scheduled as a SCHEMA port (~360 tables to create), and they are not one. `specs/builderforce/data-model/source-to-target.tsv` — the committed coverage map the build already guards — assigns **every remaining hired.video table to a primitive this schema owns**, and 362/363 targets are written. Re-verified row by row against `api/src/infrastructure/database/schema/`. **The rest of this port needs zero new tables**; it needs application code and surfaces over primitives that exist. Migration 1106 (two column additions) was the entire schema cost of the two largest remaining domains. Row-by-row mapping: [PRD 18 §5a](./specs/builderforce/18-prd-hired-video-port.md). **Three earlier claims here were stale and are corrected:** the Studio source IS checked out on the build machine (`C:\code\hired\hired.video`, `frontend/lib/studio` intact) so T4 is large but NOT blocked; payouts are NOT an env-gated stub (`PayoutAccountService` + `withdrawalMethods` + escrow are built and ledgered); and `points_fraud_flags` was mapped to `ledger_entry`, which was wrong and is now `alert_event`. **Landed 2026-08-22:** the whole points/gamification economy (10 source tables → 0 new tables) — see DONE.md. **Affiliates are the Associate program** (operator decision 2026-08-22): hired.video's six affiliate tables reconcile onto `sales_referrals`/`sales_associate_settings`/`sales_commission_rules`, they do not become a second referral economy. Remaining open items are registered under the domains that own them — [Delivery](#-delivery--workforce-boards-ceremonies--autonomous-lifecycle), [Canvas](#-canvas--creation-surface-documents-studio--qa), [Commerce](#-commerce--marketplace-talent--freelance), [Growth](#-growth--marketing-paid-media-social--public-surfaces) and [Hiring](#-hiring--postings-sourcing--applications).

**Program — burnrateos.com selective extraction.** Close BurnRateOS by extracting only capabilities that advance the **Idea → Real** journey into existing Canvas, domain and object owners. Not a 404-model/262-page port, and no parallel C-suite module. The 48 legacy C-suite use cases execute through the Creation Canvas menu with Idea/Make/Run/Measure ownership, live evidence, per-intent operation/output/completion contracts and failed outcomes when no permitted artifact changes. **The engineering is done: Builderforce runtime reads local tenant-scoped data and has no BurnRateOS HTTP or config dependency, and two CI guards (`check:no-burnrate-runtime`, `check:burnrate-policy`) hold that.** The affiliate, VoIP, content, push, pricing and provider decisions are settled in a CI-validated no-new-tables manifest; tenant/company ownership is a strict planner over existing segments, companies, objects and relations that fails rather than guesses. Plan and cutover evidence: [PRD 19](./specs/builderforce/19-prd-burnrateos-consolidation.md). **What remains is OPERATOR work, not code, and it is the only thing keeping the domain alive:** run the read-only production identity/data audits, transform and reconcile accepted rows, perform the provider customer actions, reconcile storage/billing, communicate the identity cutover and verify the redirects. Every one of those needs production credentials, a live environment and provider-account authority that no build of this repo can supply — so it cannot be closed from here and is not tracked as an engineering gap.

> ⚠️ **[PRD 19 §2](./specs/builderforce/19-prd-burnrateos-consolidation.md) governs BOTH programs.** hired.video and BurnRateOS collide with *each other* on twelve capabilities — affiliates, bookings, phone/VoIP, campaigns, OKRs, people/HR, payouts, content, support, poker/retros, kanban and SOC 2. BurnRateOS is now constrained to the existing owner named there: duplicate systems, pages, Durable Objects and tables are retired rather than ported. The historical combined inventory (**832 source tables, 328 route modules and 471 pages**) is an audit universe, not a destination count.

## Known Implementation Gaps (Builderforce.ai API)

*Both rows that stood here since 2026-08-18 were CLOSED 2026-08-19 — the agent-host channel
registry and paid knowledge-listing settlement both shipped; see [DONE.md](./DONE.md). What
follows is what closing them surfaced.*

| Gap | Location | Severity | Resolution |
|-----|----------|----------|------------|
| A knowledge listing can only be priced in **USD** | [knowledgeCommerce.ts](./api/src/application/knowledge/knowledgeCommerce.ts) `startKnowledgeCheckout` | LOW | `marketplace_knowledge` has no `currency` column — unlike `catalog_items`, which carries one — so the hosted checkout hardcodes `'USD'`. A seller outside the US prices in a currency they do not use. Fixing it is a column plus a currency choice on the PUBLISH surface, which is a pricing-UI change rather than part of settlement; deliberately not folded into the checkout work. Unblocks: a non-US seller pricing in their own currency |
| Adding a channel in the portal **records** it; it does not make the host connect it | [agentHostChannels.ts](./api/src/application/agentHost/agentHostChannels.ts) · [AgentHostRelayDO.ts](./api/src/infrastructure/relay/AgentHostRelayDO.ts) | MEDIUM | The registry now stores channels, seals their credentials and receives the host's own `channel.status` verdict over the relay — but config flows UP only. An agent host still configures its adapters from its local `channels/channel-config.ts`, so a channel added in the portal shows as configured and never connects until it is also configured on the host. Closing it means a `channel.configure` push and an agent-runtime consumer for it. **Blocked on a running on-prem host to validate against** — the push cannot be exercised from here, and shipping an unverified half-seam is what the register calls dead wiring. Unblocks: the portal being the source of truth for where a host speaks |
| The channel platform union is declared **twice**, in two packages | [agentHostChannels.ts](./api/src/application/agentHost/agentHostChannels.ts) `CHANNEL_PLATFORMS` · [builderforceApi.ts](./frontend/src/lib/builderforceApi.ts) `ChannelPlatform` | LOW | The server refuses an unknown platform, so the two cannot silently diverge into stored garbage — but a platform added server-side stays invisible to the panel until somebody edits the second copy. There is no shared contract package spanning `api` and `frontend` for non-canvas vocabulary (`creation-canvas-contract` is canvas-scoped), so closing it means choosing where that contract lives. Unblocks: one place to add Matrix or IRC |

## Risk Register

| Risk | Prob | Impact | Mitigation |
|------|------|--------|------------|
| Anthropic/OpenAI ships multi-agent IDE tool | High | High | Double down on self-hosting + open-source moat |
| Cursor/Windsurf adds MCP + multi-agent | Med | High | Ship live workflow DAG + agent mesh |
| Model API costs too high for free tier | Med | Med | Rate limiting + gateway margin (see cost-control gaps below) |
| Enterprise sales cycle too long | High | Low | SMB Teams plan first |

---

## Consolidated Gap Register — index by domain

| Domain | Seat | Section |
|---|---|---|
| `agents` | Platform | [🤖 Agents — runtime, gateway, Evermind & Brain](#-agents--runtime-gateway-evermind--brain) |
| `delivery` | Manager | [🚚 Delivery — workforce, boards, ceremonies & autonomous lifecycle](#-delivery--workforce-boards-ceremonies--autonomous-lifecycle) |
| `canvas` | Brain | [🎨 Canvas — creation surface, documents, studio & QA](#-canvas--creation-surface-documents-studio--qa) |
| `commerce` | Platform | [🛍️ Commerce — marketplace, talent & freelance](#-commerce--marketplace-talent--freelance) |
| `growth` | CMO | [📣 Growth — marketing, paid media, social & public surfaces](#-growth--marketing-paid-media-social--public-surfaces) |
| `integrations` | Platform | [🔌 Integrations — connectors, repos & the developer portal](#-integrations--connectors-repos--the-developer-portal) |
| `finance` | CFO | [💳 Finance — billing, payouts & entitlements](#-finance--billing-payouts--entitlements) |
| `hiring` | Recruiter | [🧲 Hiring — postings, sourcing & applications](#-hiring--postings-sourcing--applications) |
| `legal` | Counsel | [⚖️ Legal — contracts & obligations](#-legal--contracts--obligations) |
| `identity` | Platform | [👤 Identity — accounts, onboarding & consent](#-identity--accounts-onboarding--consent) |
| `governance` | Security | [🛡️ Governance — tenancy, embed & security automation](#-governance--tenancy-embed--security-automation) |
| `platform` | Platform | [🛠️ Platform — data model, shell, i18n, VS Code, CI/CD & cost](#-platform--data-model-shell-i18n-vs-code-cicd--cost) |

---

## 🤖 Agents — runtime, gateway, Evermind & Brain

> Domain `agents` (seat: Platform). Everything that RUNS a model: the cloud and on-prem agent runtimes, the PR loop, the LLM gateway and its routing, Evermind/SSM, and the Brain chat surface. The billing consequences of those runs are under [Finance](#-finance--billing-payouts--entitlements).

### Cloud agent execution & PR loop

> **Reconciled 2026-07-22.** A verification pass found much of this group was already shipped but stale in the roadmap — moved to [DONE.md](./DONE.md): the governance approval-gate (extracted, shared, called by the autonomous lane trigger) + MANAGER+ enforcement on `PATCH /approvals/:id`; the atomic `pr_opening_at` claim + `pr_opened` audit events on both finalize paths; `resolveTicketRepoContext` already preferring `tasks.gitBranch`; durable pause/resume + `ask_human`; the `findCanonicalBoard` `createdAt` tiebreak; and the lane-funnel wiring (QaFindingRouter/qualityRoutes/taskRoutes). **Shipped this pass:** symmetric host/cloud tool-audit `run.failed` read-repair + execution-scoping, LLM-trace threading for the `agent`/`dataset-gen` surfaces, and the flag-gated container-preview ingress scaffold. Remaining below is genuinely deploy/live-blocked or larger feature work.

- **Brain-created project has no repo → board-dispatched cloud coder degrades to reasoning-only** *(demo-readiness pass 2026-07-24)* — a fresh project created from the Brain has no connected git repo, so `runCodingDispatch` (`agent-runtime/src/infra/builderforce-coding-dispatch.ts:132-140`) hits the "No repo bound → reasoning-only" branch and the coder returns prose instead of writing code. The single-prompt→app demo path now sidesteps this by routing the dashboard build prompt into the IDE (R2 workspace + WebContainer, no git needed), but the *board-dispatch* path still degrades. Fix: auto-provision/connect a scratch repo at project-create (or route designer-modality projects through the IDE workspace) before any board dispatch.
- **Cloud agents wired but only happy-path validated — 50-gap PRD** ([09-prd-cloud-agent-validation.md](./specs/builderforce/09-prd-cloud-agent-validation.md)). P0: cloud V2 runs `bypassPermissions` + Bash on shared infra with no documented sandbox (G1); cloud-Worker fallback honors no steering/cancel (S5/S6); no test reconstructs a run from audit+snapshot+usage by `execution_id` (O1) nor asserts ledger==snapshot (O2); errored/cancelled runs leak the task workspace (W2); BYO-key-missing may silently fall to platform-billed key (B2). Execute 9a→9d. *(The `qa:cloud-agents` script now exists — `api/package.json` runs the reconstruction, loop-control, telemetry, ledger, engine and BYO-policy suites — so O1/O2 have a home; the P0 sandbox, steering and workspace-leak gaps are unchanged.)*
- **`cloud-container` executor demotes to durable — INFRA.** Real Cloudflare Container (`AgentContainerDO` + `[[containers]]` + `api/container/`) is built; needs a Containers-enabled Workers Paid account + a `wrangler deploy` that builds the image. Known bounds: `sleepAfter` 20m may stop a >20m single run (add keep-alive); short-lived clone token handed to the container shell (consider a scoped git-proxy); tokened clone URL is GitHub-only (GitLab/others get reasoning+commit-only). `CloudRunnerDO` (durable) built; verify on deploy that a durable V2 run ticks to completion + PR/merge (DO/alarm unverifiable locally). Container surface has no `ask_human`/pause-resume op (latent while it falls back to the DO).
- **Cloud loop can't run build/type-check/tests in-run (no shell).** `run_checks` is a real static validator (re-parses committed JSON/YAML) but full TS type-check/test/build needs the whole program + a shell — inherent to the Worker. Stays with CI-on-PR + human review + the webhook CI→auto-fix loop, or route to the Container/on-prem SDK sandbox. No pre-`finish` completeness/self-review loop (stub-detection has a static ceiling — a syntactically-valid comment-free stub is indistinguishable; folds into routing off the weak free-pool tail). V1 loop still auto-merges unverified code when `CLOUD_AUTOMERGE_REQUIRE_GREEN` is off (make green-gate the default once webhooks are provisioned everywhere).
- **Cloud execution doesn't clone/analyze the repo or write `PRD.md` into it** — `runCloudExecution` is PRD-first + rules-aware (persists a WIP PRD to `specs`) but has no FS/subprocess, so it can't pull the repo, run tools, or commit `PRD.md`. Requires a connected runtime (V2 SDK or self-hosted host). Hoist the PRD-first step into `dispatchAndQueue` for all engines so the flow is uniform. Downstream agents receive the PRD read-only (no append/section-edit tool); PRD commit-to-branch is GitHub-only (`commitFileToRepo`/`commitPrdToRepo`/`createPullRequest`). No agent-facing task-mutation tool / agent-user identity; assigned-agent ref reaches the card but isn't resolved to a name/avatar; Auto/default runs carry no `cloudAgentRef` so no identity to assign.
- **Confirm the rest of the `never_started` cohort drains now that runs are graded** *(manager diagnostics pass 2026-07-29; re-measured 2026-07-30)* — two structural causes are **fixed and the fix is now measured**: the candidate window is no longer a fixed set (0384) and a completed-but-unproductive run arms the autonomy breaker (0385). The cohort fell **370 → 200 (−46%)** across the 2026-07-30 capture, so the dispatch ceiling those runs were consuming has genuinely been freed. The remaining 200 are the independent hypothesis: the real evaluator may refuse them for a reason `classifyBulkAutoRunReason` does not model, which would mean the census reports a cause that is not the actual gate. **BLOCKED on post-deploy data**: query `auto_run_skip` for 5 of the cohort's task ids (e.g. 58, 59, 60, 61, 62 — `longest-idle=50d`) and read `reason`. Unblocks: knowing whether the census is telling the truth about why a ticket is not running.
- **Multi-repo spanning** — a run targets exactly ONE repo (one branch → one PR). A task that legitimately crosses repos needs a per-task repo *set* with a branch+PR per repo and a loop routing each write via matchHints/pathGlobs.
- **Multi-provider PR/repo loop needs live validation.** GitHub/GitLab/Bitbucket Cloud all cover create/merge/detail + read/write/PR; residual: run request-shapes against real GitLab/Bitbucket creds; Bitbucket detail is state/merged only (no diffstat/CI); GitLab lacks per-file additions/deletions; Bitbucket code search is workspace-scoped + Premium-gated (deferred); GitLab/Bitbucket `commitUrl` returned null.
- **Post-merge build validation + auto-fix is GitHub-Actions-only**, reuses the merged ticket branch (not a fresh `-fix-<n>`), only validates PRs with a recorded `merge_sha`, and gives the fix run step names not full log tails. *(The `buildStatus` badge half closed 2026-08-20 — `BuildStatusBadge` renders on the task card via `TaskBadges` and on the newest execution chip; see DONE.md.)*
- **Cloud observability is poll-only** (no live relay socket) — cloud log lines derive from polled tool-audit with a "Refresh" button; add a per-execution SSE/WS tail. Auto-run execution CREATION isn't pushed over the project socket (surfaces on the `useBoardLiveRuns` poll tick). Container hard-death self-heal can't push a live WS event (cross-isolate notifier needed). *(The relay log frame now takes an `executionId` — `AgentHostRelayDO.emitLog(level, message, executionId?)`, 2026-08-20 — so relay log streaming CAN be execution-scoped; what is still missing is the per-execution SSE/WS tail itself.)* **Backend for the execution-scoped host Logs/Timeline + terminal `run.failed` read-repair shipped 2026-07-22** (`toolAuditReadRepair.ts` shared by both reads; host read + `builderforceApi` client take `executionId`); **the last-mile UI wiring landed** — `ObservabilityContent.tsx` is localized and passes `executionId` into the host `toolAuditEvents` call (verified 2026-08-18).
- **LLM trace capture** — `surface` labels now cover gateway `chat`, `ide-chat`, and (2026-07-22) `agent` (workforce-inference `/agents/:id/chat`) + `dataset-gen`. The **Brain** service now threads it (`BrainService` mints a `traceId`, passes it to `service.complete()` and calls `logTrace` — 2026-08-20). Remaining caller: **`/v1/images`** (`ImageProxyService` has no `logTrace` at all and is not a `ProxyResult` — needs a trace-shape adapter). Streamed traces capture tokens but not the `response_body` (needs stream-tee); `ideAiRoutes.ts` streams aren't back-filled. Cloud runs don't deep-link to their `llm_trace` row / structured model+token fields.
- **Capability guardrail + cross-board data isolation.** (The governance approval-gate + MANAGER+ enforcement are shipped — see the group note.) The capability guardrail only enforces an explicitly-set `required_capabilities` (no capability-aware *router* that scores task-type vs agent skills; no sensible default for un-configured lanes); the board Triage control surfaces a capability mismatch on-demand only. `capabilities.load` telemetry conflates agent-pinned vs inherited artifacts (label each artifact's source scope). Moving a task across boards doesn't re-point `tasks.segment_id` to the target project's segment (data-isolation drift for segmented tenants).
- **Lane dispatch gaps.** (The lane-funnel wiring is done — QaFindingRouter/qualityRoutes/taskRoutes all route through `maybeAutoRunOnLaneEntry`/`onTaskLandedInLane`, and `findCanonicalBoard` has the `createdAt` tiebreak.) *(Two clauses closed 2026-08-20: `laneCoordinatorEntry.tryCoordinatorLaneEntry` routes a drag into the `SwimlaneCoordinator` whenever the lane declares something the simple executor cannot express — >1 staffed agent, a browser runtime, a non-default success policy, a lane action — and `laneAgentHost.resolveLaneAgentHostId` turns `(runtime, target)` into the `agentHostId` the dispatcher accepts, so a lane pinned to the operator's own machine no longer silently runs in the cloud. See DONE.md.)* Remaining: resident tickets (already in a lane before staffing) never auto-run — add a "run lane now"/backfill; the backlog lane seeds `gate:'auto'` — consider `gate:'human'` so staging work doesn't spend tokens; no integration test for assign→dispatch→run.
- **Cloud auto-merge / finalize:** Architect task is driven by `AnalysisRunnerDO`, not the runtime tool loop (no per-tool timeline/steering/approval); worker-served project list omits `hasArchitecturePrd`; "View Arch Analysis" doesn't auto-open the arch PRD drawer; no `POST /runs/:id/retry` for failed/skipped artifacts; auto-run only on manual button (no auto-run on first repo map + no commit-webhook incremental re-analysis); generation uses `json_object` not strict `json_schema`.

- **The manager pass still carries the mechanical PR loop, and the fleet pool is a stopgap for a per-project Durable Object** *(manager architecture, 2026-07-30)* — two residuals from the fan-out. (1) **The PR merge loop does not belong in a judgement pass.** It is mechanical, high-volume, provider-bound work whose cadence has nothing to do with ranking or triage, and it is what starved every stage behind it (measured: 93% of the pass). The merge queue now bounds it to ~4s so this is no longer urgent, but the structurally right shape is its own registry sweep alongside `runBoardSyncSweep`, with its own budget — then the manager pass cannot be starved by PR volume *by construction* rather than by a tuned depth. (2) **`MANAGER_SWEEP_BUDGET_MS = 60_000` is a guess at the Worker's eviction point**, not a measured value; the pool gives ~14 projects a tick at the measured 25s/pass, so a fleet larger than that still pages across ticks (fairly now, via the rotation, rather than never). If `notReached` on the cron log line stays near `projects`, the answer is a pass-per-project Durable Object with its own alarm — the `ProjectEvermindCoordinatorDO` pattern — not a bigger pool inside one invocation. Unblocks: a manager whose throughput scales with the fleet instead of with one Worker invocation. **Not blocked** — sequenced behind one capture of `notReached`, so the pool's real headroom is measured before it is replaced.
- **The 309-ticket unconfigured-lane cohort now has a name and a switch, but nobody has thrown the switch** *(shipped 2026-07-31 as `lane_unconfigured` + `allowAutoStaffLanes`, migration 0386 — see DONE.md)* — `backlog` (299) and `blocked` (10) authorise no role at all, and those tickets no longer masquerade as a staffing failure. What remains is the OPERATOR ACTION, and it is genuinely theirs: either declare a required role on those stages, staff an agent to them by hand, or grant `allowAutoStaffLanes` on the Manager policy panel and let the manager pin a producer. **Blocker: none technical — this is a deliberate governance decision. Its sequencing precondition is now MET: the 429 capacity/routing fix landed 2026-08-19 (escalating per-model backoff, availability-aware learned routing, and a manager that holds run-spending remedies while the pool is cooled — see DONE.md), so granting the switch no longer starts 309 tickets into a pool that would reject them. What remains is a person deciding.** Unblocks: 46% of the stalled board.
- **One ticket carries three `pull_requests` rows for the same PR number, and nothing reconciles them** *(manager diagnostics 2026-07-31; the rest of this entry closed 2026-08-19 — see DONE.md)* — the pile itself is now ACTIONABLE: `POST /api/manager/:projectId/blocked-prs/close` plus the `ManagerBlockedPrs` panel bulk-close every retired PR whose ticket already finished, and the `MERGE_QUEUE_DEPTH` question is decided (it stays at 1 — only the head can merge, so depth buys stale round-trips, not merges). What is NOT closed is the duplicate rows the panel exposed: **PR #21 and PR #27 each appear three times against a single ticket**. The uniqueness guarantee `uq_pull_requests_provider_number` covers `(tenant_id, repo_id, number)` only `where repo_id is not null and number is not null`, so triplicates are possible in exactly three shapes and they need opposite fixes: rows with a NULL `repo_id` (the index does not apply), rows in genuinely DIFFERENT repos that legitimately share a number (not a duplicate at all), or rows predating the index. Every insert path audited (`recordPullRequestRow`, `RepoService.dispatchPrCreation`, `prReconciliationService`, `repoBridge`) passes a real `repoId`, so no writer in the current code can produce shape one. **BLOCKED on one query against the live database** — `select id, repo_id, number, task_id, created_at from pull_requests where task_id = <the ticket> order by number` — which decides between backfilling + widening the partial index to cover `repo_id is null`, and closing this as a misread of a multi-repo project. Guessing between them would either add an index that cannot be created (duplicates still present) or delete rows that are not duplicates. Unblocks: knowing whether the PR inventory has a uniqueness hole or a reporting one.

### Cloud-agent run milestones → Brain chat

- **Milestone/dispatch messages render as plain assistant bubbles.** They carry `metadata.runMilestone` / `metadata.agentDispatch` but both chat renderers (web `BrainPanel`, VSIX webview) show them as ordinary assistant text (emoji-prefixed). Optional polish: style them as a distinct system/activity line (must localize in all 5 catalogs + both surfaces; milestone TEXT itself is server-generated English — localizing it needs a locale-aware template or client-side rendering off the structured `metadata`). Non-blocking — the content is already clear.

### 🚢 Replit-parity live container preview (phase 2)

> For reference: Replit's dev server runs in a REMOTE Linux container and `expo start --tunnel` publishes it on a temporary public URL, which the QR encodes; Expo Go then connects straight to the bundler for hot reload.
>
> **Code scaffold SHIPPED 2026-07-22 (flag-gated, inert until enabled).** The public-ingress vertical is built end-to-end: a signed, time-limited preview token (`api/src/application/runtime/previewToken.ts`), the `preview.builderforce.ai/<token>/*` ingress that proxies **HTTP + WebSocket** through `AGENT_CONTAINER` to the run's container (`previewIngress.ts`, wired in `index.ts` before site-hosting; `preview` is already a reserved subdomain), a mint endpoint (`GET /api/runtime/executions/:id/preview-url`, tenant-scoped), the `PREVIEW_INGRESS_ENABLED` flag (`env.ts`), and the container-side `/__preview__/*` HTTP+WS reverse-proxy to a dev server on `PREVIEW_PORT` (`container/server.mjs`). All of it 404s/503s while the flag is unset, so nothing changes until an operator turns it on. Remaining is deploy + live-validation + run behaviour:

- **Deploy + live-validation (INFRA).** Requires a Containers-enabled Workers **Paid** account, a `wrangler deploy` that builds the image, a proxied `preview` DNS record, and `wrangler secret put PREVIEW_INGRESS_ENABLED true`. None of the HTTP/WS proxy chain is verifiable locally (no container runtime); confirm on deploy that a WS upgrade survives `AGENT_CONTAINER` → `server.mjs` → dev server.
- **A run must actually START a dev server on `PREVIEW_PORT`, and the QR must encode the minted URL.** The passthrough targets `127.0.0.1:$PREVIEW_PORT`, but nothing yet makes an agent run `npm run dev` bound to that port, and `MobileDevicePanel` still encodes the published-site URL rather than calling `preview-url`. Wiring both (a run step that starts + health-checks the dev server; the panel consuming the mint endpoint) is the last mile — the panel change lands in the frontend/i18n pass. Also needs dev-server host tuning (Vite `server.allowedHosts` / `hmr.clientPort`, Metro host) so HMR connects back through the public origin.
- **Concurrency and cost are unbudgeted.** `api/wrangler.toml` caps `AgentContainerDO` at `max_instances = 5` with `sleepAfter = '20m'`; a per-project live preview is a long-lived instance per active editor, which that cap cannot serve beyond a handful of concurrent users. Decide the instance budget, an idle-eviction policy tighter than 20m, and whether live preview is a paid-plan feature.

### On-prem runtime, engine & tooling

- **pi-tui is the last `@mariozechner/pi-*` dependency.** The replacement is no longer a skeleton: `packages/tui` has a live Ink renderer and the runtime has an opt-in Ink session. Remaining work is feature-parity migration of the legacy `src/tui/*` consumers, making the Ink path the verified default, removing pi-tui, and then taking the explicit on-prem default-engine decision. *Terminal-verified only.*
- **Unified engine / tool-layer convergence.** On-prem now implements the shared `AgentEngine` contract; `resolveEngineById` shared; converged file tools (`write`/`edit`/`delete_file`/`list_files`) wired behind `tools.fs.convergedFileTools` (default OFF, non-sandboxed only). Remaining: cross-provider smoke-test then default-on flip; a sandbox-aware Node provider so sandboxed sessions converge; converge `read` (needs a shared read-capability **media affordance** — text-only today, would lose native image-reading) + `exec`/`process` (needs streaming `ctx.onUpdate?`); drive the SDK task-runner tools through the shared registry; collapse the relay `RelayTaskEngine` orchestration onto the shared engine. Confirm: converged on-prem `write`/`edit` narrow scope to workspace-root vs native's opt-in guard before the flip.
- **agent-runtime plugin-sdk DTS build broken** — `tsc -p tsconfig.plugin-sdk.dts.json` emits 9× TS6059 because `@builderforce/agent-tools` is source-only (no dist `.d.ts`) and its `src/*.ts` fall outside the dts `rootDir`. Fix = give agent-tools a real `.d.ts` build + split export conditions, or consume prebuilt declarations. Blocks agent-runtime full `pnpm run build` + publishable plugin-sdk types.
- **Brain embeddable = headless core only.** `brain-embedded` ships hooks/contexts/streaming; `BrainPanel`/`FloatingBrain` still live in the host. Extract the UI into `@seanhogg/builderforce-brain-embedded/ui` (inject `ChatInput`/`LinkComponent`/message-actions/projects adapter) for a one-component embed reused by web + VS Code. Also extract a shared `useToolConfirmationGate(isMutating)` hook (auto-approve gate duplicated web + VS Code) and a shared `buildComposerDirectives` package (currently ~10-line cross-package dup).
- **On-prem `.builderforce/` knowledge-loop for the VS Code agent** — the extension writes a `.builderforce/` grounding map but no per-run session notes (unlike on-prem `KnowledgeLoopService`); adding it compounds grounding over time.

### Surface-agnostic run-context (cross-surface)

- Context assembly is re-implemented 3× (cloud `prepareCloudRun`, on-prem `buildEmbeddedSystemPrompt`, VS Code `prompt.ts`). On-prem + VS-Code runs are starved of strategic + PRD + governance context (`TaskAssignMessage` carries only `{task, artifacts-slugs}`); no surface injects board settings/project vision/epic→OKR chain/`tasks.priority`; no delta/reconciliation routes context through `EvermindCognition` (cloud re-sends full PRD+governance+repo listing every run); no strategic write-back on any surface. Fix = ONE shared `ContextSource` (keyed blocks, version tokens, read-through cache) + `ContextReconciler` (commit to each surface's embedded Evermind, emit only augment/supersede deltas); the 3 builders become thin `assembleAgentContext` consumers. Also subsumes the capability-loader parity (`loadCapabilityContext` vs `artifacts.sync`). *Execution-critical rewrite; each surface RUN must validate.*
- Cloud affect still cannot run **GPU-trained** limbic/SSM models inside the Worker (inherent to the Worker runtime — heuristic regions only there); the trained models activate on other surfaces once `@seanhogg/builderforce-memory`(+`-engine`) is published (that publish step is the Evermind blocker tracked in the engine-telemetry / P0 items below).

### Duplicate local-disk path-escape resolver across the two on-prem providers

- **`resolveInside(root, relPath)` (agent-runtime `node-capability-provider.ts`) and `resolveInRoot(root, rel)` (VS Code `localCapabilities.ts`) are the same "resolve a relative path under a root, reject `..`/absolute escape" logic, hand-rolled twice** (one returns `null`, one throws). They can't move into the shared `@builderforce/agent-tools` as-is because that package is imported by the Cloudflare Worker and must stay node-builtin-free (`node:path` handles Windows-absolute/UNC that a pure-string reimplementation would miss). Fixing it — a small shared node-only path util both on-prem packages import (or a Worker-safe pure guard that both call for the escape check while keeping their own `path.resolve`) — removes the last cross-surface capability-provider duplicate. Non-blocking; both copies are currently correct.

### LLM gateway, routing & cost

- **`SUPERSEDED_MODEL_IDS` is hand-maintained — no drift check against live vendor `/models`** *(model-supersession pass 2026-07-26)*. `canonicalModelId` (`api/src/application/llm/LlmProxyService.ts`) now rewrites a stale pin (`claude-opus-4-8` → `claude-opus-5`) at every seam a stored id enters dispatch, which is what stops a months-old agent `base_model` from being silently filtered out of the BYO flagship seed. But the map is a hardcoded constant: the *next* Opus/Sonnet/GPT bump still needs a human to notice and add the row, and nothing fails CI when a vendor retires an id we still name. Fix = extend the existing live-`/models` reconciliation (item below) to also assert that every `SUPERSEDED_MODEL_IDS` **key** is either still served or already mapped, and that every **value** is present in the live payload. Unblocks: a vendor deprecation becomes a red build instead of a stalled ticket weeks later.
- **A known-broken BYO provider still LEADS the seed — it is retried (and fails over) on every request** *(BYO credential-health pass 2026-07-26)*. `providerAuthAlerts` now knows, per tenant + provider, that an account is rejected/unentitled/out-of-budget, and the cooldown store no longer benches a tenant's own vendor (correctly — the keyspace is global and cannot speak for one owner's account). Nothing feeds that tenant-scoped verdict back into `byoAutoSeedModels`, so a workspace with 2+ connected providers leads with the one we *know* returned 401, burns an upstream attempt, then fails over to the healthy one — every single request until the owner fixes it. `loadDemotedVendors` already does exactly this shape for 5xx streaks, but its store is keyed by vendor ALONE, so an auth alert cannot reuse it. Fix = resolve the alerted-vendor set where `tenantId` is in scope (the same seam that already threads `byoVendorPriority` from `resolveTenantLlmCredentials`) and pass it into the existing `demotedVendors` option — demote, never remove, since a `capacity` alert self-heals when the billing period rolls over and removal would silently change the funding source. Blocked on a dependency-graph call: the natural home (`resolveTenantLlmCredentials`) would import `providerAuthAlerts`, which already imports it back. Unblocks: a broken account costs one wasted attempt total instead of one per run.
- **Model-catalog ids are hand-maintained + drift from live vendor `/models`.** Verify OpenRouter / Cloudflare `@cf/*` / NVIDIA NIM (incl. `minimaxai/minimax-m2.7` default + `z-ai/glm-5.1`) / Google AI ids against each vendor's live models endpoint — wrong ids 404 and pollute the cascade. Derive/validate the FREE/PRO/CODING pools from the cached live `/models` payload (filter `supported_parameters: tools` + coding tag) so a stale hardcoded id can't silently route to a 404 backstop. NVIDIA NIM TTS/audio + FluxAPI async-poll (`taskId`) variants unimplemented.
- **Cost caps / overflow.** Free-plan run reached the funded Anthropic floor via the fallback chain (confirm the paid-overflow per-tenant cap + 60m cooldown gate free-plan cascades into `PAID_OVERFLOW_MODELS`, or fence the floor off entirely). No per-tenant cap on the always-on premium fallback for FREE quality-critical traffic (`useCase` is caller-supplied → uncapped abuse vector; overflow-meter the primary premium attempt). Pro plan free-tier capped uniformly at 2 attempts (no plan-aware carve-out). No per-user sub-ledger from `metadata.userId`. Cost attribution unattributed for gateway chat/image calls + on-prem host runs (thread ticket/project hints); no workspace(segment) rollup surface or project-page spend widget.
- **Prompt / response caching.** Gateway/agent-runtime prompt-cache TTL is fixed 5-min ephemeral (wire `_builderforce.cacheTtl:'1h'` through the agent-runtime direct-Anthropic path — gateway side shipped). Explicit prompt caching is Anthropic-only (extend to Gemini-via-OpenRouter once its explicit-cache behaviour is confirmed). builderforce-memory `CachingBridge`/`ResponseCache` (semantic response cache) still unused by gateway + agent-runtime (wire into the read path for idempotent/low-temp `useCase`s). Gateway semantic-cache L2 is KV brute-force (200-entry cap) — upgrade to Cloudflare Vectorize; provision + bind `SEMANTIC_CACHE_KV`. Web `SemanticCache` L1 shipped; L2 not wired (needs a gateway semantic-cache endpoint + tenant token) + possible double model-load. `getCachedOrGenerate` cortex wiring shipped; verify live.
- **Reliability / diagnostics.** `applyCooldowns` issues 2N parallel KV writes per failed cascade (collapse to one composite-key blob); `dispatchVendor` doesn't double-check cooldown before each attempt (defense-in-depth); streaming responses don't surface `failovers[]` (emit a final SSE `builderforce` chunk); streaming dispatch doesn't detect empty-but-200 (record a post-stream cooldown); Production gateway can drift stale (mixed npm/pnpm lockfiles in `api/`; verify a redeploy from `main`). Gateway strict-mode validation only fires when consumers send `response_format: json_schema strict` (consumer migration); per-vendor strict-schema capability is not advertised in `/v1/models`. `scoreWithLenientLLMOrFallback`/`LLM_UNAVAILABLE` is outside this repo (locate its owner). hired.video tailor still has its own client-side cascade and stale model pin. Per-vendor schema dialect metadata, vendor-prefixed image pools, and the image-health probe are shipped; see DONE.md.
- **Tenant-model sampling `params` (temperature/top_p)** applied at the gateway path only, NOT inside the V2 cloud agent loop (`AgentRunInput` doesn't carry gen params). No persona picker in the My-LLMs editor (`tenant_models.persona_id` server-supported). `tenant_model` presets feed the pickers but have no creation UI — **decision needed:** retire presets → make published LLM Studio models the source, or rename the group.
- **Gateway hardening:** no message-shape sanitizer for strict vendors (a non-user-leading history / empty tool-call `arguments` still trips Gemini `INVALID_ARGUMENT`); `llm` workflow nodes call the gateway with the host key (may not be gateway-authorized).

### LLM gateway — auth-alert / vendor-health follow-ups

- **Inheriting builds can still POST mutations to an Evermind they do not own.** The console now renders read-only for a build that inherits its container's Evermind (the real defect: the read path resolved the container's head so the UI showed `seeded: true` and stats, while every write posted to the raw project id, updated zero rows and returned OK — an affordance that silently did nothing). The affordance is hidden but the endpoints still accept those writes. Rejecting them server-side with a 409 would also change the agent/on-prem front-door contract, so it was left. Unblocks: a write path that refuses honestly rather than no-oping.
- **Codex 403 stands the VENDOR down, not the RUN.** A 401/403 raises a retryable error tagged `CODEX_AUTH_MARKER` so `classifyFailure`'s `auth` class applies a 30-minute single-strike vendor cooldown and the cascade advances — deliberately NOT `VendorFatalError`, which the dispatcher rethrows and which would kill an otherwise-servable request because one BYO account lost entitlement. If literal hard-fail semantics are wanted, that is a dispatcher change. Unblocks: an explicit decision on whether one dead BYO account should fail a request.
- **Vendor-health clear on the non-streaming success path is fire-and-forget.** `successJsonResult` is synchronous, so a recovered vendor can keep its seed-order demotion until the 30-minute TTL expires. It is still called, just not first. The streaming path awaits the clear. Unblocks: immediate promotion on recovery.

### `web_search` — surface + verification residuals

- **No search adapter has been exercised against its real live API.** All five (Tavily, Exa, Linkup, SearXNG, Wikipedia) are tested against a stubbed `fetch` — request shape, per-vendor result mapping, private-URL dropping, and the 401/403/429/timeout/non-JSON error contracts — because no vendor key and no SearXNG instance were available here. **Blocked on credentials/infrastructure, not on code:** verifying the keyed three needs a real account each, and SearXNG needs a running instance. `/api/integrations/:id/test` deliberately runs a real query through the *same* adapter, so an operator's first "Test connection" click is the genuine end-to-end proof for Tavily/Exa/Linkup; SearXNG and Wikipedia have no such button and are first proven by a real `web.search` call. Highest-risk assumption is the per-vendor response field names (Tavily `content`, Exa `text`, Linkup `name`/`content`), since a wrong guess yields empty snippets rather than an error.

### 🌙 Kimi Code — runtime-less operation *(2026-08-02)*

> **Kimi now WORKS for any tenant running a Builderforce runtime** — calls are performed on the user's own machine over the existing relay (see [DONE.md](./DONE.md)). Only the no-runtime case is outstanding, and it needs Kimi's answer to `docs/partnerships/kimi-code-hosted-integration-request.md`.

- **A tenant with NO connected runtime still cannot use Kimi Code from hosted cloud agents. ⚠️ BLOCKER: an approval decision from Kimi.** Kimi's edge refuses the Cloudflare Workers egress with an HTML 403 before the API reads the key (reproduced 2026-08-02: the byte-identical request returns a clean JSON `401 invalid_authentication` from an ordinary IP). Local egress solves this for anyone running a runtime; a tenant who wants purely hosted agents has no route, because the only remaining options are a static-egress relay — still a hosted reverse proxy serving a subscription key, with real ToS risk — or Kimi granting an approved delegated-auth path. Spoofing a first-party client identity stays ruled out (`openaiCompatibleVendors.ts:98`). Unblocks: Kimi Code on hosted cloud agents with no machine online.

### Evermind / SSM

> Remaining items are ALL blocked on a WebGPU runtime, GPU numerical validation, an operator credential/action, an external checkpoint, an upstream library, a product decision, or a live surface to validate an execution-critical change (⛔). The code-fixable follow-ups were closed 2026-07-22 — see [DONE.md](./DONE.md).

- ⛔ **P0 — real backprop in the trainer.** `MambaTrainer._trainStep` (`builderforce-memory/.../training/trainer.ts`) feeds only `dLogits` to AdamW for every param — no backward pass runs (autograd tape + scan/ssd backward kernels exist but are unused). `learn()` is SAFE (WSLA freezes backbone, trust region + NaN guard, rollback-on-regression) but ineffective. Fix = wire real per-param gradients. *Blocker: WebGPU runtime to validate numerically.*
- ⛔ **P1 — surface-agnostic run-context + embedded-Evermind delta wiring.** Context is assembled 3× (cloud `prepareCloudRun` / on-prem `buildEmbeddedSystemPrompt` / VS-Code `prompt.ts`); on-prem + VS-Code runs are starved of strategic/PRD/governance context; nothing routes context blocks through `EvermindCognition` for delta/reconciliation; no surface writes strategic state back up. Fix = one shared `ContextSource` + `ContextReconciler` all three consume. *Blocker: execution-critical rewrite of all three prompt-assembly surfaces — needs each surface RUN to validate.*
- ⛔ **P2 — grad-norm clip under-counts across >1 workgroup** (`weight_update.ts` non-atomic `norm_sq[0]` RMW race). Low impact today (trust region bounds every step); wrong for large-model training. *Blocker: GPU numerical validation.*
- ⛔ **P2 — foreign-checkpoint weight-port mapping.** Importer round-trips Evermind's own exports + warm-starts via a caller-supplied `rename` map, but real Codestral-Mamba/Falcon-Mamba ports need their tensor names mapped + scan-vs-depthwise-conv mixer reconciliation. Transformer coders (IQuest/NousCoder/Maincoder) can't be weight-ported at all — distill only. *Blocker: needs the real external checkpoints to build + validate the map.*
- ⛔ **P2 — learned-routing on-prem/IDE consumer.** Write-back door shipped in api (`POST /llm/v1/run-outcome` + `recordClientRunOutcome` + mig 0283). Open: on-prem Node host (`embedded-runner/model.ts`) should classify action type, read cached `routing:<scope>`, compute a local SSM bias, seed via `rankModelsForAction`, and call `/v1/run-outcome` at run completion (grep confirms zero of these in `agent-runtime`). *Blocker: the read-side re-ranking changes model selection on real runs — needs a running on-prem host to validate.*
- ⛔ **P2 — Evermind auto-routing eval gate.** Evermind is pin-only (`vendors/evermind.ts` `autoRoute:false`); nothing promotes a qualifying `evermind/<ref>` into the auto-route ranking. *Blocker: product decision on gate criteria (use-cases, benchmark threshold, opt-in scope) + live validation — routes billed traffic.*
- ⛔ **P3 — on-prem adaptation cost unbenchmarked** (measure/tune window/epoch/skip-threshold under load — needs a live host); **stdio MCP recall is lexical (Jaccard), not SSM-embedding** (headless binary doesn't stand up the GPU runtime).
- ⛔ **Blocked externally:** transformers.js `pipeline()` auto-registration for `model_type:"evermind"`; llama.cpp GGUF `architecture:"evermind"` execution; live Hugging Face push (needs write-scoped `HF_TOKEN` + optional `@huggingface/hub`). Evermind `.evermind` package doesn't embed its tokenizer (portability nicety; consumers load both together — not blocking).
- **P3 (code-fixable, not blocked) — Prompt API per-turn system-prompt fidelity.** `PromptApiModelProvider` (`frontend/src/lib/model-provider.ts`) fixes the system prompt at session creation, so the per-step `systemPrompt` assembled in `AgentRuntime.step` (with live project context) does not reach Chrome's built-in model — only its per-turn `memoryContext`/`fileContext` do. The cloud tier still receives the full system prompt. Fix = detect a changed system prompt and `session.clone()`/recreate, or fold the delta into the turn input. Unblocks full context fidelity when the Gemini-Nano tier is active. *Needs a Chrome build with the Prompt API to validate end-to-end.*

### Evermind enterprise layer — residuals *(layer shipped 2026-08-20, see [DONE.md](./DONE.md))*

> The enterprise capability gap customers named — ingestion pipelines over structured + unstructured data, a vector-store port with adapters, tenant/ACL-scoped hybrid RAG, multi-agent graph orchestration, LLM-native telemetry and an eval gate — shipped in `@seanhogg/builderforce-memory@2026.8.21` (593 tests green, coverage above every threshold). These are what that pass FOUND and deliberately did not close.

- ⛔ **The three vendor dialects and the Vertex bridge have never spoken to a live service.** `qdrant`, `pinecone` and `vertex-ai` in `builderforce-memory/packages/memory/src/vectorstore/dialects.ts`, plus `VertexAIBridge`/`VertexAIEmbedder`, are covered only by unit tests that assert the emitted request body and parse a recorded response shape. That catches a wrong field name but NOT a wrong endpoint version, a changed auth scheme, or a pagination rule nobody documented — the failure modes that only appear on the wire. The `evermind` dialect is exempt: this repo defines that contract, so it is correct by construction. *Blocker: no Qdrant/Pinecone/GCP credentials exist in this checkout; validating needs a live index per vendor plus a GCP project with Vertex AI enabled. Unblocks: quoting a customer's existing vector database as supported rather than as "adapter written, unproven".*
- **P2 (code-fixable, not blocked) — two lexical scorers with different tokenizers.** `api/src/domain/webSearch/textIndex.ts` implements its own stopword + stemming tokenizer and lexical scoring for web-search snippets, while `builderforce-memory`'s `bm25Search` uses the package's plain `tokenize`. They are not straight duplicates — the api's stemming is a genuine specialization the package lacks — but they are two answers to "how do we score lexical relevance", and they will drift. The consolidation is: give `Bm25Options` an optional `tokenize` hook, then have `textIndex` supply its stemmer and delegate scoring. NOT done in this pass because adding the hook with no consumer would ship a dead seam, and wiring the consumer is an api-side change whose ranking output needs a web-search regression run to prove unchanged. Unblocks: one lexical-relevance implementation across the platform.
- **P2 — the platform api consumes the retrieval primitives but not the new ports.** `agentKnowledge.ts` and `recallSops.ts` already import `hybridRetrieve`/`bm25Search` from `@seanhogg/builderforce-memory`, so the package is a live dependency — but nothing in `api/` yet uses `VectorStore`, `IngestionPipeline`, `EnterpriseRetriever`, `AgentGraph`, `Tracer`/`MetricsRegistry` or `EvalHarness`. Concretely: knowledge ingestion is still bespoke per surface (no content-hash incremental sync, so a re-index re-embeds everything), agent knowledge scoping is enforced in application code rather than compiled into the store query, and there is no per-request cost/TTFT attribution on the LLM gateway. Adoption is a distinct program — each of those is a migration with its own tenant-data blast radius — and is sequenced as roadmap work rather than treated as a defect of the package pass. Unblocks: the enterprise story being true of the PRODUCT, not only of the library.
- **P3 — `Tracer` sampling drops a trace whole, which is right, but there is no head-based propagation helper.** A trace continued across an HTTP hop must have its `traceId` passed in by the caller (`startSpan(name, { traceId })`); nothing parses or emits a W3C `traceparent` header. Fine for a single process, insufficient the moment portal → api → relay → runtime all want one trace. Fix = a `traceparent` parse/format pair plus the sampling flag. Unblocks: one span tree across the four hops of a cloud run, which is the shape cloud-agent observability actually needs.

### Evermind pre-diffed delta door has no producer

- ⛔ **`POST /api/projects/:id/evermind/learn` (the pre-diffed weight-delta door) has no caller anywhere in the repo** (confirmed: `dispatchProjectEvermindLearn` and the engine's `diffCheckpoints` producer both have zero application callers), so `kind: 'delta'` contributions never occur in practice — every real learning path (`brainEvermindLearning.ts`, `incidentLearning.ts`, teach-a-task) goes through `/evermind/learn-text`, and the fit runs server-side in `ProjectEvermindCoordinatorDO.drain()`. The route and dispatcher are kept as the documented ingress for an on-prem/IDE trainer that computes its own diff, but nothing exercises them and the stale-`baseVersion` guard is untested against a live producer. *Blocker: product decision (wire the on-prem runner to push diffs vs. retire the door) + a live coordinator DO to validate the guard.*

### Evermind teacher distillation — residuals

- ⛔ **Legacy ring entries carry no provenance.** Entries merged before the provenance fields existed have no `distilled`/`skipReason`, and Durable-Object ring rows are never rewritten. `evermindLearnedStatus` infers the one provable case (`text === prompt` → fault), so old teach-a-task echoes render correctly, but a legacy **refine-mode** entry whose teacher silently failed still renders as `self` because its text legitimately differs from the prompt. *Blocker: backfill needs a per-DO storage migration walking `RECENT_KEY` on a live runtime.*
- ⛔ **The teach action reports success before the teacher has run.** `POST /evermind/learn-text` enqueues and returns 200 immediately; the frontier call happens later in the debounced merge alarm (`DEBOUNCE_MS`). The console therefore toasts "Taught" even when distillation subsequently faults — discoverable only in the Learnings ring afterwards (a broken teacher now ALSO raises a `/quality` alert — see DONE.md). *Blocker: immediate point-of-teach feedback needs a post-merge notification / pollable per-contribution status channel.*

### Evermind "Import from builderforce-memory" — follow-ups

- ⛔ **Live-store clobber.** Compaction rewrites the snapshot file, but if the `builderforce-memory` MCP stdio server is *running* it holds the full store in memory and re-snapshots (overwriting the stubs) on its next `remember`/`forget`. So stubs reliably take effect only after the MCP server respawns (next Claude Code session). Fix: have the MCP server watch its snapshot for external compaction and re-hydrate, or expose a first-class `memory_compact` tool the server itself runs. *Blocker: adding the tool is self-contained in `memory-mcp`, but proving the race actually closes needs a live stdio server + MCP client — and wiring the extension to call it (vs. its current direct file write) risks a dead seam until then.*
- ⛔ **Markdown auto-memory not supported.** The importer targets the JSON snapshot only; the Claude Code markdown auto-memory (`MEMORY.md` + per-fact `*.md` with frontmatter) can't be imported/compacted through this path. A pure directory/frontmatter parser is trivial, but the compaction protocol assumes ONE file path — supporting a directory of per-fact files means threading per-file paths through `evermind.pickMemory` → `extract-memories` → `evermind.compactMemory` and per-file stub rewriting. *Blocker: the multi-file import/compact protocol change can only be verified against a live VS Code extension host; deferred rather than shipped half-wired.*

### Evermind console operate surface — follow-ups *(test bench + maintenance + analyzer shipped 2026-07-26; tabs + diagnostics export shipped 2026-07-26, see DONE.md)*

- **P3 — the diagnostics report body is deliberately English-only.** `packages/brain-ui/src/evermind/diagnosticsReport.ts` emits an unlocalized markdown document (the buttons around it ARE localized). That is the right call for a technical artifact read by whoever debugs the model — the same category as a stack trace — but it means a non-English operator pastes an English report. Revisit only if diagnostics start being read by the operator rather than sent onward. Unblocks: nothing today; recorded so the choice is not mistaken for an oversight.

### Evermind tool calling — follow-ups *(constrained decoding shipped 2026-07-26, see DONE.md)*

- **P2 — a tool-calling turn costs one forward pass per candidate and per scored argument, over the whole (capped) prompt.** `createEvermindToolDecoder` (`evermindRuntime.ts`) has no incremental/KV state to reuse — the published engine's `forward()` recomputes the full sequence — so an agent loop offering ~20 tools pays ~20 passes for the tool vote alone, plus one per enum/boolean/optional-property decision. Mitigated today only by capping the prompt at `MAX_TOOL_PROMPT_CHARS` (6000, left-truncated). Risk: CPU time in a Worker on a large tool catalogue. Fix: an engine-side incremental decode state (prefix cache) so candidates share the prompt pass, or score candidates in one batched pass. Unblocks: pinning Evermind on agent loops with a full tool catalogue rather than a small one.

### Evermind chat recall/learn/reconcile — follow-ups

- **P3 — reconcile is a lexical-overlap heuristic, not a true supersede check.** `countReconciledMemories` (brain-embedded) counts a recalled memory as reconciled when the answer restates ≥60% of its meaningful tokens; the real write-through supersede happens weight-side in the coordinator merge. Accepted as a display-only indicator (never gates learning); revisit only if it over/under-counts.

### Evermind training discipline — cookbook PEFT residuals (WGSL parity only)

> The CPU-reference training path now has the full efficient-training toolkit — LoRA, QLoRA, mixed precision, activation checkpointing, gradient accumulation, optimizer-state sharding — shipped + tested 2026-07-24 (see DONE.md). Residual is the **WebGPU/WGSL kernel parity** for these: the `MambaTrainer` WGSL path (blocked on the P0 real-backprop gap above + a live GPU) does not yet mirror the CPU LoRA/mixed-precision/sharding implementations. *Blocker: WebGPU runtime + GPU numerical validation — same blocker as the P0 backprop item; the CPU path (used by Node on-prem + tests) is complete.* A LoRA-per-persona head wired to the personality/limbic setpoints (replacing prompt-level persona injection with a weight-level adapter) is the natural follow-on now the adapter primitive exists — logged separately when the persona team picks it up.

### Evermind video/image generation — only real training remains

- ⛔ **P1 (compute-blocked) — codebooks + generator are untrained, so output isn't yet coherent.** The full train path exists and is exercised end-to-end (`video-train` Studio step: fit codec → build video sequences → train EvermindLM; the api `generate-media` endpoint loads the artifact and returns frames), but reaching watchable fidelity needs a real video/image corpus + GPU training, or teacher distillation from a frontier media model. All plumbing done and tested; large-scale training run pending. *Blocker: GPU training compute on a media corpus.*

### Memory-first "skip the LLM" — residual phases

- ⛔ **Phase E — collapse VSIX native `runAgent` onto `brain-embedded` `runLoop`:** inject host file tools, `persistence`, `approve()`, policy gates, code-change backstops, and the memory hooks, so the native `@builderforce` participant gets the memory-first short-circuit from the SHARED loop (per the operator's "collapse then add once" DRY decision — do NOT add memory to `runAgent` separately). Native surface left WORKING (unchanged `runAgent`) until it can be driven live. *Blocker: rewrites the entire native tool/approval/streaming/backstop path; the 7 checks in PRD §2.5 are only verifiable in a live extension host.* **→ Spec: [PRD-evermind-panel-and-native-loop-consolidation.md](PRD-evermind-panel-and-native-loop-consolidation.md) Feature 2.**
- ⛔ **Verify chat #55's actual `brainChats.projectId` vs the seeded project** (the panel "list Everminds" view shipped 2026-07-22 — see DONE.md; this residual data check remains). *Blocker: needs live data.*

### BYO usage attribution — residual gaps

- **Historical BYO rows are mis-attributed AND mis-billed.** Every row written via `completeForTenant` → `recordProxyUsage` before this fix landed has `byo = false` and a catalog-priced `cost_usd_millicents`, so tenants were charged for tokens their own key paid for, and those rows still render as platform-funded in `/insights/ai`. Fixing needs a backfill that re-infers BYO from `tenant_llm_provider_keys` + the row's model vendor and re-zeroes cost — inference is lossy (a key connected *after* the call would false-positive), so it needs an explicit operator decision on the cutoff. Unblocks: accurate historical spend + a truthful per-integration history.
- **Orphaned usage rows vanish from every tenant surface.** `llm_usage_log.tenant_id` is nullable with `ON DELETE SET NULL`, so rows survive tenant deletion but match no `tenant_id = ?` filter — they still count in the untenanted admin rollup (`adminRoutes.ts`), so admin and tenant totals silently disagree. Unblocks: admin/tenant reconciliation.
- **Image generation has no BYO path.** `imageProxyForPlan` never receives tenant vendor keys, so every image row is platform-funded even for a tenant with a connected provider. Unblocks: BYO-funded image generation + its attribution.

- **Workflow builder.** Branch nodes annotate but don't prune downstream edges (skip the untaken branch's dependents); transform nodes are `{{var}}` template-only (no JSONata); `mcp` nodes record intent — no transport wired (inject an `McpService` resolving a tenant MCP extension); no per-integration credential capture / OAuth connect; workflow *definitions* not project-scoped (add `workflow_definitions.project_id` + a project picker); the claim→execute→report loop is typecheck-verified not integration-tested; train/memory-write/KB-ingest are no-ops on GPU-less hosts. Wire the configurable workflow engine (`WorkflowConfig`/`StepTypeRegistry`, templates `AGENTIC_SEVEN_LAYER`/`TRAIN_LLM`) into the visual builder + a "Run Stack Check" execution-output timeline.

## 🚚 Delivery — workforce, boards, ceremonies & autonomous lifecycle

> Domain `delivery` (seat: Manager). The work item and everything that moves it: boards, swimlanes, role participation, ceremonies, and the measured autonomy of the ticket lifecycle.

### Coordinated Role Participation — minor refinements

> Full design in [PRD-coordinated-role-participation.md](./PRD-coordinated-role-participation.md). Phases 1–5 shipped (see DONE.md); only small refinements remain.

- **CI-green not yet a producer completion signal** — producer completion uses "a non-draft PR exists for the task" (`taskPrEvidence`); it does not additionally require `pullRequests.buildStatus='success'`. Add CI-green to `attributeRunToManifest` for stricter evidence.
- **`has_pr` condition predicate not wired** — `requirementApplies` supports `is_security`/`has_ui_change`/`is_data_change`; a `has_pr` condition would need the PR signal at evaluation time. Add if a PR-conditional requirement is needed.
- **PRD per-role section authorship isn't audit-verified** — every task PRD is scaffolded with anchored per-role sections (`scaffoldPrdSections`), but the audit doesn't yet verify a specific role's section is non-empty as evidence (it verifies via sign-offs + PR + child tasks). Add a section-authored check for the spec roles (BA/Architect) if section-level evidence is wanted.
- **Coordinator exterior-comms attribution is partial** — the Coordinator sequences roles, records hand-offs, owns stage advancement, and is the assignee; routing ALL outbound stakeholder notifications through the Coordinator identity (vs the acting agent) is not fully centralized. Wire remaining notification emitters through the Coordinator if single-voice attribution is required.
- **`evaluateTaskAutoRun` does not model the lane REQUIREMENT gate** — it resolves the board/lane/gate column, staffing, capability and re-run backpressure, but `enforceLaneRequirements` runs *after* it in `laneEntryTrigger` and can suppress the lane's normal agent. So the shared evaluator answers `canRunNow: true` / `will_run` for a ticket the trigger will then decline, and every consumer of that verdict (board triage chip, `/autorun-diagnostics`, the lifecycle gate snapshot, MCP `runNowCandidate`) overstates what will happen. Found on task 173 (held in `in_review` awaiting a `code-reviewer` sign-off while the gate block read `canRunNow: yes`). Mitigated 2026-07-25 — the block is now recorded as an `auto_run_skipped` / `lane_requirement_gate` event and the ledger keeps that recorded reason over a live `will_run` (`EVALUATED_AUTO_RUN_REASONS`) — but the evaluator itself still does not consult the requirement gate. Folding a read-only requirement-gate probe into `evaluateTaskAutoRun` would make one verdict true for all callers.
- **Lifecycle ledger cannot see sign-off / manifest state** — `buildTicketLifecycle` joins `activity_log`, `task_status_transitions`, `executions` and `tool_audit_events`. On a lifecycle-managed board the thing actually holding a ticket in a review lane is an outstanding `ticket_role_signoffs` verdict or an open `ticket_participants` slot, and neither is read — so the chain of custody shows a reviewer run completing and then nothing, with no way to see WHICH role still owes a sign-off. The new `lane_requirement_gate` skip names the dispatched reviewers but not the outstanding verdict per role. Add a fifth source (latest verdict per role + open manifest slots for the current stage) to the ledger and to the gate snapshot.
- **`decideTicketReadiness` may bounce non-code tickets** — the manager's CONDUCT pass returns a ticket to implementation when it `expectsCode` but has no branch. For an analysis/spec ticket mis-classified as code-bearing that is a return→re-run→review loop, bounded only by stall triage's 3-attempt escalation. Verify the `expectsCode` classification against `taskType`/`actionType` for spec-shaped tickets (e.g. task 173, "Gaps … are explicitly listed").

### Workforce, boards, kanban, ceremonies

- **hired.video's `people_strategic_objectives` would fork the OKR store** *(hired.video port, T3)*. hired.video carries its own strategic-objectives + outcomes + milestones tables (`api/src/routes/people-strategy.ts`, ~5 tables in `peopleOps.ts`), and Builderforce already owns `objectives` / `key_results` with `project_id` scope (0268). A literal port would stand up a second, unreconciled OKR store. The port must map onto the existing tables and invalidate `projectsList` on write. Unblocks: `hr.org_review` reading one objective set rather than picking a side.

- **Objective share is computed per direct task link only** — `loadObjectiveLineage` divides by the count of `objective_links` rows with a `task_id`, so a ticket that inherits its objective through a linked EPIC reports the Epic's share, not its own 1-of-N among the Epic's children. An objective linked at Epic granularity therefore shows every child claiming the same share. Fold Epic children into the delivery denominator (walk `tasks.parent_task_id` for `link_kind='epic'` rows) so the share reflects the real work set.
- **No way to link a ticket to an objective from the ticket** — the drawer's context strip reports "not linked to an objective" and points at the Planning board, because objective links are writable only via `POST /api/pmo/objectives/:id/links` (MANAGER). A ticket-side picker that posts that link would close the loop where the gap is actually noticed; today the operator has to leave the board, find the objective, and link from the other end.

- **Meeting RSVP has no UI, and a declined invite does not excuse ceremony attendance** — `POST /api/meetings/:id/rsvp` and `meeting_attendees.response` (`invited|accepted|declined|tentative`) have existed since 0292, but nothing in the product ever calls the endpoint, so `response` is `'invited'` on every row that was not auto-accepted by joining. Now that a ceremony's attendance is fed by its companion meeting (0366), a *declined* invite is exactly the signal that should resolve to `'excused'` rather than `'absent'` — the same treatment approved leave now gets. Needs an RSVP control on the meetings list/detail plus a `response==='declined'` branch in `resolveAttendanceVerdict`; until both land, someone who explicitly declined a standup is still recorded as a no-show.
- **Board columns are hardcoded `TASK_STATUSES`, not the board's swimlanes** — a template lane key beyond the 7 statuses materialises requirements but shows no column (drive board columns from `swimlanes`). No `swimlane_id` FK on `tasks` (string-convention coupling; deleting/renaming a lane orphans tasks to a fallback column). Custom-lane kanbans + automation-follows-user-lanes both need the FK + a lane-merge/reassign flow + per-lane automation flags.
- **Kanban follow-ups.** Template sales are free grants (no Stripe).
- **Teams / assignee scoping.** Board lane-agent picker isn't team-scoped (reconcile the `/api/agents` pool to `team_members` id-spaces); all-projects board can't scope the per-task picker to each task's project. Board agent-status chips are a load-time snapshot (poll `runtimeApi.listRecent()` + `boardsApi.dispatches()` while active, or subscribe SSE). Epic decomposition: no board parent/child tree UI, fan-out assignee routing is pass-through (no role resolution / no `SwimlaneCoordinator` dispatch). *(The `swimlane_agent_assignments` → `agent_assignments` merge landed 2026-08-20 as migration 1085, carrying the lane-only columns across scope-qualified; see DONE.md.)* Cron executor (`assignedAgentHostId`) is decoupled from `project_agents` attachments + per-agent `project_agent_id` — unify the three "assign an agent" notions.
- **Autonomous lane chaining** hardcodes `RUNNING→in_progress` / `COMPLETED→in_review` (single hop; doesn't walk `backlog→todo→ready→in_progress` by swimlane position) — unify with `SwimlaneCoordinator` for true BA→Dev→QA pipelines. Real-time board: bypassed status writers (`staleExecutionReaper` raw SQL, some DO direct `db.update`) only surface via the 8/30s backstop poll (funnel all status writes through one notifier / a DB-trigger outbox).

### Autonomous lifecycle — RE-MEASURED 2026-07-26 across all 767 tickets via `GET /api/tasks/:id/lifecycle`

> Every ticket was pulled through the platform's own lifecycle endpoint (not a re-derivation). Totals: **767 tickets, 755 stalled, 12 reached terminal, 6 fully autonomous, 409 never dispatched. 6,195 runs dispatched → 1,936 completed → 4,254 failed (69%).** The stall causes CONCENTRATE, which is the finding that reframes everything below: **313 `no_agent`** (0 have a staffed lane, 0 have a candidate, 307 never ran once), **149 `lane_requirement_gate`** (all staffed, avg 21.8 runs each), **116 `run_cap_exhausted`**, **100 `will_run`** (76 in `in_review`), **70 `no_board`/`no_lane`**. The manager's census + systemic-findings stage (0373, see DONE.md) now measures this continuously; the items below are what it cannot fix by itself.

### Autonomous lifecycle — MEASURED failure (audited 2026-07-25 via the new ticket-lifecycle ledger)

> The `GET /api/tasks/:id/lifecycle` + `GET /api/insights/autonomy` ledger (see DONE.md) was built to answer "do manager/human-created tickets actually complete their lifecycle autonomously?". Measured on tenant 1, 90-day window, 821 tickets created: **6 (0.7%) reached Done fully autonomously** (zero human lane moves), **12 (1.5%) reached Done at all**, **809 (98.5%) stalled**. The items below are the measured root causes, each still open.

- **The stall register makes the human-gate decision unavoidable rather than resolving it.** *(narrowed 2026-08-20 — the staffing half of this entry is closed: `kanbanTemplateService` now calls `seedLaneStaffingFromWorkforce` on board creation, and 1081 removed the duplicate boards whose lane config was dead. What remains is the GATE.)* The manager's stall triage (0367) diagnoses every non-moving ticket and escalates the ones it cannot fix — but `human_gate` and `capability_gap` are escalations BY DESIGN (the manager must not override a gate a human deliberately configured, nor conjure a capable agent). With `in_review.gate='human'` on all 10 boards, the register surfaces a standing escalation backlog. **Blocker: an operator decision on the review gate**, not an implementation — whether autonomous review is allowed to close a ticket, or a human signs every one.

- **Ceremonies.** Server-trusted presence (DO doesn't re-verify JWT per frame); server-authoritative `changed` broadcast (client-driven today); persistent session history + retro/poker integration; dnd-kit affordances (native HTML5 drag has no ghost/keyboard-a11y); auto-dispatch-on-Complete is client-driven (server-side from `POST /sessions/:id/complete`); turn timing has no pause/resume; power-meter cap falls back to a soft constant.
- **Personality** shows on `/settings` + Workforce card (human **and** agent, via the shared `PersonalitySummary`) + task-assignee hovercards. Not yet on a dedicated person profile page / brain @-mentions (drop the shared `AssigneeHovercard` / `AssigneePersonalityInline` in once those surfaces mount the `AssigneeProfilesProvider`).
- **Delta→ticket capture / Validator agent.** Marketplace-hire → tenant-local Validator unverified; chat deltas not on the agent PR-branch won't auto-complete.

### QA / Agentic Tester

- **Agentic Tester (schedulable):** QA runner container deploy pending a green build (image slimmed to chromium-only — verify <2000MB + a first live browser run under the CF sandbox); one-shot-container-vs-persistent-server lifecycle unproven; not a first-class `ide_agents` entry / not in the `workflow_triggers` builder; migrations 0206/0209 are applied, but the pipeline on top of them is verified by typecheck + schema drift only — no run has exercised it end to end.
- **QA pipeline gaps:** keep live container/Cloudflare lifecycle verification; add a runtime network sandbox beyond static source screening; upload screenshots to R2 and render them; derive project routes beyond `/`; support multi-actor scenarios and scoped test identities/tokens instead of recoverable site passwords; capture screenshots on findings; and make heat project-specific (`qa_journey_events` still has no `project_id`). **A cross-browser verdict is the one canvas QA check that still needs this container**: the page audit that shipped is a source audit (`lib/canvasPageAudit.ts`), so "is this broken in Safari" remains unanswerable until a real browser matrix runs somewhere. Generated-spec allow/deny validation, keyset aggregation with explicit truncation, navigation, exploration auto-dispatch, opt-in finding→staffed-fix-lane routing, and the canvas QA objects that publish into this library are shipped; see DONE.md.

- **Platform migration hub.** No custom-field/comment/attachment migration (Jira); webhook auto-registration still manual (Rally/GitLab/Bitbucket poll-only); Asana/ClickUp/ServiceNow/Freshservice have no real-time webhook normalizer (covered by the 5-min cron); outbound write-back designed but its ENQUEUE trigger is unwired (nothing inserts `board_sync_outbox` rows — needs a product decision on which task-mutation paths count as a local edit + conflict UX); `IntegrationCredentialsManager`/`BoardConnectionsManager` not localized; marketing `/integrations` SEO pages not added for the 8 new tools.

- **Repo-backed Designer.** `createRemoteRepo` GitHub-only (add GitLab/Bitbucket); commit-back has no concurrency/merge guard (compare vs `last_synced_sha`, added but never populated); migration 0211 is applied but its surface is verified by typecheck only. Repo→IDE import capped at 200 files/5MB, overlays without clearing stale files, one-way (no commit-back from the IDE), and `searchRepoCode`/`compare` remain GitHub-only. IDE file storage diverges worker (`{projectId}/`) vs api (`ide/projects/{projectId}/`). No server-side file-content guard (mirror `coerceFileContent`/`validateFileContentForPath` at the write route). WebContainer preview in a new tab fails ("must have an opener") — the isolated editor's `COOP:same-origin` severs `window.opener`. Hosting residual (custom domains + a site datastore + traffic in 0412; site-origin handlers, deep-link base and storage reclamation 2026-08-06 — see DONE.md): container-mode still serves no HTTP, so a project whose backend needs a real process (a long-running job, a framework's SSR) has nowhere to run it — the declarative ingress covers request/response work only.

---

## 🎨 Canvas — creation surface, documents, studio & QA

- **The frontend architecture ratchet is red on four pre-existing items that are not attributable to any one change** *(2026-08-22)* — `npm run check:architecture` fails on `'use client' files: 834 exceeds baseline 808`, `presentation -> infrastructure: components/repos/githubActionsSurface.tsx`, and three files over 800 lines (`components/board/BoardConfigPanel.tsx`, `components/ProjectDetailsPanel.tsx`, `lib/freelancerApi.ts`). None is in a file this pass touched, and the +26 client-directive drift spans ~50 files added across several sessions. A red ratchet hides the next real violation — which is exactly how the calendar's own `presentation -> infrastructure` breach would have gone unnoticed had the guard already been failing for other reasons. Fix = either argue the baseline up for the NAMED files (which is what the guard's own printout is for) or move the four violators; do not `--update` the whole tally blind. Unblocks: trusting this guard on the next change.

> Domain `canvas` (seat: Brain). The creation session and every object on it — documents, media, games, data-science and QA objects — plus the studio runtimes and the mobile modality that render them.

- **The instrumented `/api/creation-sessions/claim` failure has not been observed yet.** *(new 2026-08-18)* The catch now reports the PG code, the constraint, the statement index it maps to, the row count of that statement, and BOTH the case-sensitive and case-insensitive distinct counts of the object and connection ids — which is exactly what distinguishes the one cause already found and fixed (a case-only id collision) from a different one. **Blocker: a live occurrence.** Nothing further can be concluded until the instrumented path fires in production. Unblocks: closing the question of whether anonymous boards still fail to claim, with evidence instead of inference.

### 🎯 Realize (Idea → Real) — residuals

> The realization layer shipped 2026-08-15 (migration 0463): eight proof forms — demo video, clickable prototype, smoke test, wizard-of-oz, proof of concept, pilot, phone line, live system — each planned from a spec, built through the existing challenge materialiser, published to a real URL and wired to its own site collections (see [DONE.md](./DONE.md)). These are the parts deliberately left out of that pass.

- **The wizard-of-oz queue and the pilot report are public and carry participant contact details.** *(same pass)* `/queue` and `/pilot-report` are `verify: 'none'` so a proof works with zero setup on the day it is built, and each target seeds a BLOCKING first ticket saying so in as many words — but the platform still ships the open version by default, and a seeded ticket is a request, not a control. The clean fix is a per-realization read secret the console holds and the handler verifies (`shared-secret` already exists and needs no new vocabulary), generated at build time so zero-setup survives; what stops it today is that the console is a static page on the public site with nowhere to keep a secret. Options are a signed short-lived link, or moving the console off the published site and behind the app's own auth. **Blocker: that is a product decision about where an operator console lives**, not a missing implementation. Unblocks: pointing a wizard-of-oz at strangers without a ticket standing between the queue and the internet.

### 📖 Knowledge & canvas

- **`CreationCanvas.test.tsx` fails a DIFFERENT four tests on each full-file run, and passes every one of them in isolation.** *(found 2026-08-19 while wiring the palette entitlement gate)* Across four consecutive runs of the same unchanged tree the failing set was `{agent model, staff/task, evermind, palette collapse}`, then `{agent model, staff/task}`, then `{staff/task, workflow focus, palette collapse, spatial frame}` — and `collapses palette sections` passes on its own with an empty error log. That is not a product defect; it is a 4,000-line suite of 20-second full-canvas renders sharing one jsdom, one `localStorage` and one set of module mocks, where a slow machine reorders enough async work to break whichever assertion is closest to a `waitFor` boundary. The cost is real: a flaky suite is a suite people stop reading, and two GENUINE regressions in this file were found only because they failed deterministically while the flaky four moved around them. Fix = give each test its own `localStorage` (the palette-collapse state persists across cases today), replace the bare `fireEvent` + immediate `expect` pairs with `waitFor`, and split the file by surface so one render does not carry eighty-six cases. Unblocks: a canvas suite whose red means something.

- **Eight Canvas kinds are declared but have no editing runtime — they generate a manifest and stop** *(hired.video port, T4 — see [PRD 18](./specs/builderforce/18-prd-hired-video-port.md))*. `CREATION_OBJECT_KINDS` / `CREATIVE_CAPABILITIES` (`packages/creation-canvas-contract`) advertise `video, voice, podcast, image, animation, comic, cad, model3d` (plus `drawing`), and all of them resolve to `builtin_creative_compose`, which returns an artifact *manifest* — a description, not something a person can open and edit. **`game` is no longer one of them (2026-08-07):** it has a real in-canvas runtime (a sandboxed play frame) plus a five-adapter target port that ships it to a phone, an APK, an iOS build and Roblox — see DONE.md. The remaining eight are unchanged. hired.video has the runtimes for every one (`TimelineCanvas`, `DrawingStage`, `AnimationStage`, `InteractiveComicStage`, `GameStage`/`GameStage3D`, `CadStage`, `Cad3DStage`, `DocumentStage`, `MusicComposer`) plus the export pipelines those kinds promise — ~85K LOC across `frontend/lib/studio` + `frontend/components/studio`. This is also the root cause of the two entries below it about `comic`/`cad`/`resume` advertising formats the board cannot produce: the contract describes hired.video's capability, and the runtime that would deliver it lives in the other repo. Port one kind per slice, dynamically importing each runtime's heavy deps (`@react-three/fiber`, `ag-psd`, `jszip`, `mediapipe`) so the canvas entry chunk does not grow by their union. Unblocks: a capability list the board can actually honour, and Canvas being a creation surface rather than a brief generator. **NOT BLOCKED — the 2026-08-19 blocker was wrong and is retracted (re-verified 2026-08-22).** That check searched the Builderforce workspace root and concluded the runtimes did not exist anywhere. They do: `C:\code\hired\hired.video` is present on the build machine and `frontend/lib/studio` is intact, carrying `animation/`, `cad/`, `cad-3d/`, `comic/`, `drawing/`, `game/`, `image/`, `renderer/` and the rest. Nothing has to be restored. This is large — it is the biggest single piece of work left in the port — but it is ordinary engineering, one kind per slice, dynamically importing each runtime's heavy deps. **Two constraints on the way in:** `CreationCanvas.tsx` is a 13,246-line god component under active decomposition (see the Platform entry) and may NOT be grown — each runtime arrives as a self-contained component reached through `creationObjectRegistry` DATA, never as another branch inside it; and each runtime must land with its import/edit/preview/export loop intact rather than as a viewer. The `CREATIVE_CAPABILITIES` half of this cluster IS closed — every advertised format now names the machine that produces it, so the contract no longer promises a studio that is not here; see DONE.md.
- **DOCX conversion keeps text, headings, lists and tables — and drops everything else.** `docxXmlToMarkdown` does not read inline images (`w:drawing` → `word/media/*`), footnotes, endnotes, headers or footers, so a dropped Word file loses its figures. The trade-off is deliberate for now: inlining media as base64 data URLs would put megabytes into node data that the local-session snapshot serialises on every viewport change. Fix = upload extracted media through `workspaceStore` and reference it by URL. Unblocks: an imported report showing its own charts.
- **The document editor's format vocabulary stops where markdown stops — no underline, no alignment, no font, no size, no colour.** *(logged 2026-08-20 while giving the page runtime a Word-sized editor; the SIZE and FILL half of that complaint is closed — see [DONE.md](./DONE.md).)* `DocumentEditor` offers exactly what `lib/richText.ts` can round-trip, and that is deliberate: a canvas `document` is stored as markdown and nothing else, so a control producing a style the save drops would be a lie told once per keystroke. But the request behind "let me edit like Word" includes the four Word controls markdown has no syntax for, and the ones people reach for first are underline and centre. The obvious workaround is the wrong one: emitting raw `<u>`/`<p align>` into the stored markdown would leak literal tags into the .docx and PDF writers (`api/src/application/office/markdownBlocks.ts` parses a documented SUBSET and degrades anything else to a paragraph) and into Brain's context. **BLOCKED on an explicit product decision:** whether a canvas document may store a richer format than markdown at all — and if so, whether the second format is a sidecar the exporters learn to read, or a replacement, because five readers (card, page editor, print sheet, .docx writer, Brain) depend on there being exactly one. Unblocks: the last four toolbar controls a person coming from Word looks for, and the .docx round-trip entry above, which is the same question asked about an IMPORTED file.
- **An imported .docx round-trips through markdown, so the original styling is lost.** A dropped Word file becomes a canvas `document` (markdown), and exporting it re-renders through `/api/exports` — the returned .docx carries Builderforce's own styling, not the source document's theme, numbering, or section layout. Fix = keep the source container and write edits back into its `word/document.xml` rather than regenerating. Unblocks: "update this Word doc with new information" returning the same document, edited.
- **Sell paid knowledge — hosted-card settlement.** Paid knowledge listings purchase + install end-to-end on the default self-hosted config (`PAYMENT_PROVIDER=manual`). For hosted providers (`stripe`/`helcim`) the checkout returns `{ requiresConfig: true }` — the one-off (`mode:payment`) Stripe Checkout session + webhook settlement branch (distinct from the subscription flow) is not yet wired, and needs a live Stripe account + 2-account smoke. This is the same missing branch as the `knowledgeRoutes.ts` `/listings/:id/checkout` row in *Known Implementation Gaps*, and it is the ONE door every Wave-1 paid path goes through — settle it once, and the webhook must land on the `ledger_entries` unique reference `listingCommerce` already uses so a replay collides in the database rather than in a check somebody remembered to write.
- **Canvas richness + block-CRDT multi-cursor.** Canvas realtime is still doc-level (whole-model serialise over the single Yjs `Y.Text('content')` channel — no per-block CRDT, so no true block-level multi-cursor); richer canvas blocks remain thin (image/video/file are URL-only — no upload/drawing/freehand). Unblocks: per-block CRDT map + asset upload pipeline.
- **Real-time co-editing — the DEPLOY.** *(narrowed 2026-08-19 — the code half shipped; see DONE.md)* The `worker/` `CollaborationRoom` is now SERVER-AUTHORITATIVE: `yjs`+`y-protocols`+`lib0` are dependencies of that package, the DO holds the `Y.Doc`, answers the y-websocket sync handshake so a late joiner adopts the current document, persists to `state.storage`, and holds awareness so a joiner sees who is here. A hibernation bug went with it — sessions lived in an instance `Map` the runtime is free to discard between messages, so every message on an idle connection was silently dropped; identity now rides on the socket. 63 worker tests pass, including 19 new ones against the real yjs packages. **Blocked on one operator action this repo cannot perform:** deploy `worker/` (it has never been deployed), set `NEXT_PUBLIC_COLLAB_WS_URL` on the frontend, and run a live 2-client smoke — nothing about a Durable Object's hibernation, eviction or WebSocket lifecycle can be verified without a real deployment. Until then the client hook stays inert (the env var is unset) and every editor falls back to local editing with autosave. Unblocks: two people in one document, and the peer-avatar entry below, which needs the same transport.
- **Two people on one canvas cannot see each other INSIDE the game they are playing.** *(playable-game pass 2026-08-17)* Inviting somebody to the canvas and playing the same game now works end to end — the play surface is open to every visitor, names who is here and opens the canvas's own invite door — but each player walks a private copy of the world. There are no peer avatars and no shared score, so a Roblox place with a goal is a race nobody can watch. **Blocked on a transport this repo cannot deploy:** canvas presence is an HTTP poll (`creationSessionsApi.presence`) carrying viewport, cursor and selection at a cadence built for cursors, not for a walker at 60fps, and the low-latency channel that would carry positions is the `worker/` `CollaborationRoom` DO — the same undeployed unit the "Real-time co-editing" row above is blocked on. Fix = once that DO is deployed, broadcast `{ position, yaw }` per walker on it and render peers with the `PlayerAvatar` figure the third-person camera already draws (hired.video's original `GameStage3D` had exactly this; it was deliberately trimmed on the way in — see `world.ts`'s header). Unblocks: "invite your friend and play together" meaning one game rather than two.
- **The generated `.rbxlx` place has never been opened in Roblox Studio.** *(game targets pass 2026-08-07; narrowed 2026-08-17)* **Narrowed, not closed:** the place is now read back into a walkable world (`robloxWorld.ts`) and `game.test.ts` round-trips a real `rbxlxFromSpec` output through that reader — positions, sizes, packed colours, anchoring, the `bf_role` child markers, the baseplate-as-ground and the CDATA-split Luau all come back as authored. That proves the file says what it was meant to say; it still does not prove Studio agrees. `robloxPlace.ts` emits version-4 XML and is tested hard on the properties that are known to fail silently — lowercase `<Vector3 name="size">`, the opaque-alpha `Color3uint8` pack, unique referents, CDATA that survives a Luau `]]>`, escaped part names, service presence. But every one of those is a test asserting against the format as implemented here, not against the parser that matters. **Blocked on a live environment this repo cannot reach:** Roblox Studio is a desktop application, there is no headless validator and no published XSD that a CI step could check the file against. The failure mode if a property IS wrong is narrow but nasty — Studio rejects the file with a byte-offset error, or worse opens it with parts silently defaulted. One human opening one generated place and pressing Play closes this permanently; until someone does, treat the Rojo project (`default.project.json` + `src/**.luau`, which goes through Rojo's own parser rather than ours) as the safer of the two artifacts. Unblocks: confidence that "create a Roblox game" ends in a playable place rather than a download that will not open.

### Data-scientist canvas review — Idea → Real still has no compute and no monitor *(2026-08-13; re-validated 2026-08-18 — the model / runComparison / labelSet / prompt kinds have since shipped and were removed)*

- **Three data-science capabilities are BUILT server-side and unreachable from the board.** The canvas is strong at IDEA and DATA (`dataset`/`datasource` snapshot-vs-live, `dataContract` → `checksFromContract()`, `erd`, `metric`, `lineage` + `staleDerivatives`, and a genuinely relational client-side query engine: filter → derive → timeGrain → groupBy → aggregate → having → sort → window → limit). It is thin at ANALYSIS and **absent** for MODEL, SHIP and MONITOR — not because the capability is missing but because the front door has no tool for it: **Two of the three closed 2026-08-20** — `canvas_read_training_run` reaches `ide_training_jobs`/`ide_training_logs` and `canvas_forecast_series` reaches `/api/insights/forecast`. The third is unchanged: `prompt_library_entries`/`prompt_library_versions` have **no** canvas tool (grep: no `canvas_*prompt*` tool exists), so prompt iteration still lives behind its own tab — the same pattern the QA-tester and CMO reviews found. Fix = one canvas tool over the surface that already exists. **Not blocked.** Unblocks: prompt iteration happening on the board rather than beside it.

### Canvas — competitive gaps against Miro *(logged 2026-08-15 during a Miro market-research pass)*

> Every bullet below is a **product prioritisation decision**, not a defect: the canvas works as designed and
> nothing here is broken. They are recorded because a buyer comparing the two surfaces will ask about each one.
> **DECIDED 2026-08-19.** The operator made the prioritisation call this section was blocked on, so these are
> no longer undecided — they are scheduled or declined:
>
> **CHASE (three, and only three):** the ordered presentation sequence, board-level version history + restore,
> and the `shape` renderer that draws an imported Miro ellipse as an ellipse. All three are buildable over
> machinery that already exists — the presence channel that carries `viewport`, `canvasActionJournal`, and the
> `stickyShape` field the importer already writes — which is why they were chosen over the larger items.
>
> **ALL THREE SHIPPED on 2026-08-19** — see DONE.md. The `shape` renderer draws eight geometries from
> `data-sticky-shape`; the presentation sequence walks the board's frames with next/previous, writing the
> presenter's `viewport` through the presence channel that already carries it, so every follower moves for
> free; and version history now covers the board that had none — a guest board keeps named checkpoints in the
> browser and restores them through the SAME panel a saved board uses.
>
> Two of the three descriptions in this entry were wrong when it was written, which is worth recording because
> the corrections are what made the work small: a saved board ALREADY had server-side revisions, a named
> checkpoint and a restore, and a whole-board JSON archive export already shipped as `exportSession` — so
> "there is no board-level version history or restore" was true only for the guest surface, which is where the
> work actually went. `canvasActionJournal` was named as the thing to fold over and could not be: it records
> LABELLED ACTIONS with durations, not state, so there is nothing in it to reconstruct a board from.
>
> **CEDE (deliberately, not by neglect):** content-level governance at Enterprise-Guard scale, and Spaces as a
> cross-board container. Recorded as declined so they are not re-opened as though merely unbuilt; a buyer
> comparison will still raise them, and the honest answer is that we do not chase them today.
>
> **The third CEDE item was built anyway, on 2026-08-20**, and the decision is corrected rather than quietly
> dropped: the public canvas API + webhooks + third-party widget runtime now exist. Migration 1100 generalised
> the EXISTING `webhook_subscriptions` / `webhook_deliveries` primitive (one HMAC scheme, one backoff curve,
> one dead-letter) to serve canvas events rather than standing up a second delivery loop, and 1101 added the
> widget registry whose `entry_origin` is derived server-side from `entry_url` — the only origin the sandboxed
> frame will accept a message from. See [DONE.md](./DONE.md).
>
> **The facilitation layer, the stencil registry and the knowledge-board merge were built on 2026-08-20**
> and have moved to [DONE.md](./DONE.md). The knowledge board FOLDED into the Creation Canvas rather than
> staying a simpler surface — the operator decision this section had been waiting on — so
> `components/canvas/canvasModel.ts`, `<CanvasBoard>` and the Brain's canvas slide-out are deleted, and the
> two primitives that board had and the front door did not (a running clock and a live transclusion) are
> now canvas kinds.
>
> The remaining entries in this section (Talktrack-style narrated walkthrough, the Miroverse licensing
> question, the Miro OAuth app) keep their own blockers, which are stated per item.

- **A Miroverse-scale template import is not legally available, and should not be attempted.**
  *(researched 2026-08-15)* The 3,000–7,000 community templates are third-party creators' work published under
  Miro's Online Community Terms, which license reuse *within Miro boards*; there is no bulk-export API, and
  scraping the gallery into a competing canvas is an IP exposure rather than a feature. The path that IS open and
  is now built: a person copies a Miroverse template into their own Miro account — which Miro's terms explicitly
  permit — and imports THAT board (`CanvasMiroPanel`). The panel says so, in five languages. Recorded here so the
  question is not re-opened as if it were merely unbuilt. **Blocker: none — this is a decision, not a gap.**
- **The Miro connector authenticates with a personal token, not OAuth.** `defaults/whiteboard.ts` ships
  `auth.kind: 'bearer'` because an OAuth connector cannot be connected at all until someone registers a
  Builderforce app with Miro and puts its client id and secret in this deployment's secrets — shipping OAuth-first
  would have shipped a connector nobody could use. A token takes a person about a minute to mint, so the migration
  works today; a one-click "Connect Miro" does not. **Blocker: a registered Miro developer app plus
  `MIRO_CLIENT_ID`/`MIRO_CLIENT_SECRET` in the deployment secrets, neither of which can be created from here.**
  Unblocks: connect-in-one-click, and `providerOAuthConnect` covering the whiteboard port like every other.
- **Nothing records a narrated walkthrough of the board.** We have a video EDITOR (`CanvasVideoEditor`) and a
  render pipeline; Miro's Talktrack records across any browser tab, transcribes, surfaces key moments and posts to
  Slack. Fix = capture over the existing media store + the transcript object we already have (`canvasTranscript`).
- **There is no content-level governance on boards.** `activity_log`, RBAC, the security agent and the privacy
  diagnostic cover ACTORS; Miro Enterprise Guard covers CONTENT — sensitive-data discovery across boards, board
  classification and sensitivity labels, classification-driven guardrails, retention/lifecycle automation, and
  eDiscovery with legal holds and bulk board export. Unblocks: regulated-buyer procurement.
- **There is no cross-board container.** Miro Spaces holds many boards plus external documents as one initiative
  hub; our scope is project-level (`ProjectScopeContext`) and board-centric, so "everything about this launch"
  has no home. Fix = a Space entity over the PRD-20 kernel, not a new per-feature table.

### Studio (video / voice)

- **Voice.** Consent/provenance gate gates GA; no in-IDE marketplace browse UI / buy-checkout backend; on-device embeddings persist localStorage-only (per-device); trained weights + marketplace-checkout remain; broader IDE shell still English (FileExplorer/AITrainingPanel/AgentPublishPanel/top-bar/terminal/`lib/modality.ts`); Voice has no quality-gate equivalent; Studio video binaries are IndexedDB-only (no cross-device MP4 sync — mirror to R2).
- **Video engine.** `mamba-coherence.ts advanceState()` is a CPU placeholder, not the WGSL selective-scan kernel; no learned RIFE/FILM interpolation backend; no real motion module; img2img recursion drifts on long clips; anchor-walk gives scene drift rather than per-object motion; ControlNet conditioning and inpainting remain absent; the two-pass Refined chroma path still needs a real GPU-browser verification; ORT-web exposes no per-input dtype metadata; Safari without WebCodecs needs a WebM/VP9 fallback; WebGPU exposes no VRAM. Brain-created dataset files still bypass dataset/training registration. Model-count growth and a tenant-configurable Refined default are not outcome gaps unless they block a named deliverable. `mp4-muxer`, the Advanced refinement override, and IDE modality-render tests are shipped; see DONE.md.

### 📱 Mobile modality — residuals

- **The device simulator previews react-native-web, not a native runtime.** A Mobile project's centre pane renders the app through `react-native-web` inside an iframe, so anything depending on a genuinely native module (camera, push, native navigation transitions) will not behave as on a device. Fixing means running Metro (out of reach in a WebContainer today) or delegating builds to an Expo service.
- **The scan-to-phone QR points at a built deploy, not a live dev server.** WebContainer dev-server URLs resolve only inside the browser tab that owns the container, so a phone cannot load one; `MobileDevicePanel` encodes the published site URL. With the GitHub pipeline a push refreshes what the QR serves, but that is a build cycle, not hot reload.
- **The original 0-byte-scaffold writer is still unidentified.** A Mobile project was observed with all five scaffold paths present in R2 at size 0 — so something wrote empty objects at exactly those paths, and neither the creation seed nor the file-list self-heal repaired them. `templateForProject` no longer bails on an unknown `template` id (the most likely gate that swallowed the heal), and `assembleMountContents` now persists any scaffold file it has to substitute, so the state self-corrects on the next Run either way. The *writer* was never found: audit every `saveFile`/R2 `put` under `ide/projects/` that can be reached with an empty body (editor mount-time onChange, collaboration join, file-create) and reject a 0-byte write to a known scaffold path at the route.
- **The Builder run terminal is hardcoded English.** Every line the Run/Check pipeline writes (`▶ Run started`, `[1/3] Preparing project files…`, the scaffold-restore warnings, check pass/fail) is a literal in `BuilderWorkspace.tsx` (formerly `IDENew.tsx`, renamed when the standalone IDE destination was folded into the canvas), unlike the component's dialogs which go through `useTranslations`. Localizing one line in a stream interleaved with untranslated npm/vite output would read worse than either extreme, so this needs doing as one pass over the whole run log.

### The canvas component itself

- **`CreationCanvas.tsx` is still an 11,181-line god component — every layer BELOW presentation is out, and presentation itself has one module of an estimated five** *(PRD 22 §3.4; the context map, the domain layer, the layering ratchet, `MaterializeDataset`, `ShareCanvasSession`, `PresenceRelay`, `PersistCanvas`, `ImportCanvasFile`, `AdoptRemoteBoard`, three gateways, `localCanvasStore`, `SyncSocialCampaign`, the ten card acts and `useSharedCanvasRoom` all CLOSED, see [DONE.md](./DONE.md); re-measured 2026-08-22)*. **What now exists:** `domains/canvas/{domain,application,infrastructure,presentation}/` plus `domains/{finance,hiring,marketing,teaching}/` for the work those contexts own — 23 layered modules, 180 tests over the tree in 11 s, layering ratchet green on an empty baseline. **What is still open:** the rest of `presentation/`. `CanvasInner` is **11,181 lines in one function** — 95 `useState`, ~171 `useCallback`, ~52 `useRef`, ~49 `useEffect`, ~44 `useMemo` — before a 923-line JSX body. The JSX is NOT the problem: it already composes about forty child components. The mass is the hook body, and the only move that shrinks it is into CUSTOM HOOKS that OWN cohesive state. `useSharedCanvasRoom` is the first and the pattern is proven — two `useState`s, three `useRef`s, two effects and four callbacks left, and the contract that crossed the line mentions no nodes, edges or React setters, which is the test for whether a second surface could mount it unchanged. **Remaining clusters, largest first:** `useCanvasBrain` (the turn machinery — the run ref, thinking, streaming, model selection, pending actions and ~40 callbacks; by far the biggest single win), `useCanvasSession` (load, save, revision, roster, the relay socket and the presence poll), `useCanvasSelection` (selection, inspector focus, scope mode), `useCanvasHistory` (checkpoints, branches, the history panel), `useCanvasDrawing`. Budget ~20 lines net per cluster after wiring: this is a long series of small correct moves, not one large one. **And a caution that cost a pass to learn:** extracting a USE CASE does not shrink this file — `AdoptRemoteBoard` removed a duplicated block AND a real defect and the component still grew 25 lines, because the wiring plus its documentation costs more than the duplication it replaced. Use cases buy correctness and testability; hooks buy size. **Smaller residues:** `seatTeammate` is still inline (canvas-native drag placement, not another context's work), and three board invariants remain unenforceable from the board alone — `checkpointNamesRealState` and `singleLineOfHistory` need the `history` module that owns `Checkpoint`/`Branch`, and `derivationNamesItsSource` needs the registry's view of which kinds derive. Unblocks: a canvas test file whose slowest case is a render rather than a whole application layer.
- **A canvas kind can reach `CREATION_OBJECT_KINDS` with no label in any catalog, and only one test notices** *(found 2026-08-20)*. `brandKit` and `audience` shipped into the contract with no `creationCanvas.object.*` entry in any of the five files — the second time this exact omission is recorded here (thirteen unlabelled kinds were the 2026-08-19 instance). The `messages.test.ts` assertion that catches it runs in the FRONTEND suite, so a contract-package change lands green in its own package and reds a different one. Fix = move the label-coverage assertion next to the contract that declares the kinds, or fail the contract package's own build on a kind with no catalog entry. Unblocks: the vocabulary and its words shipping together instead of one commit apart.

### Brain & chat

- **Composer parity is done for the `/` menu and the plan chip, but the RECIPIENT and PERSONA controls still differ per surface** *(deferred 2026-08-07 — needs an explicit scope decision)*. After this pass both composers render the same `/` menu (mode · memory · effort · thinking · model · consolidate/fork · settings), the same Auto-mode pill and the same plan chip in the same position. What still diverges: the VS Code webview routes the next message with a `PopoverMenu` "To" chip carrying avatars (`clients/vscode/webview/src/App.tsx`), while the web renders a native `<select>` inside `contextControls` (`frontend/src/components/brain/BrainPanel.tsx`); and the web additionally has an "Acting as" persona/agent `<select>` that the extension has no equivalent for at all. Fixing = a shared recipient control in `packages/brain-ui` plus a decision on whether the IDE composer should gain persona selection (it has no persona surface today). Unblocks the last of "one composer, every surface". **BLOCKER: how far parity should go here is a product call — the persona half is a new IDE feature, not a re-render of an existing one.**

- **On-prem agent loop cannot fail over to another model when one won't emit tool calls** *(deferred by decision, 2026-07-25; hosted half corrected 2026-07-31)* — the Brain run loop and `BrainService` both swap models themselves once a model burns its stall budget, through the one shared `chooseStallFailover`. (This entry previously credited `BrainService` with doing it "via the gateway cascade"; it did not — it dropped a model pin that the default BYO path never sets, so its failover branch was unreachable. See DONE.md 2026-07-31.) `agent-runtime`'s `agent-loop.ts` has no equivalent: its `Model` is an object injected by `AgentSession` from the operator's own config, with no catalog to pick a successor from and no gateway cascade in front of it, so it ends on the loud `stallExhaustedNotice` instead. Left deliberately — silently replacing an operator's explicitly-pinned self-hosted model is a different product decision from re-routing a gateway request, and doing it would mean plumbing a model catalog through `CreateAgentSessionOptions` to every on-prem caller. Fixing it (a `pickFallbackModel` seam on `AgentLoopConfig` + a catalog source in `agent-session.ts`) would give on-prem runs the same self-healing the hosted surfaces just got.
- **On-device Mamba inference not in the shared Brain** (deferred by decision) — the shared `brain-embedded` Brain uses server-backed Evermind memory (recall + write-through learn). The in-browser Mamba SSM path lives only in the agent-runtime/agent-publish stack (`frontend/src/lib/mamba-engine.ts`, `AgentPublishPanel`). Re-adding on-device SSM *memory or inference* into the Brain chat behind gating is a distinct feature — kept tracked rather than built, because it largely duplicates the server Evermind and the in-browser SSM isn't a capable chat model. Revisit if a genuinely private, no-server memory mode is wanted.
- **DNS-rebinding pre-fetch check is best-effort (TOCTOU)** — `resolveAndAssertPublic` runs a Cloudflare-DoH A/AAAA lookup and blocks private resolutions before `POST /api/brain/fetch-url` (failing open on DoH failure), but Workers still can't pin the *resolved* IP to the actual fetch connection, so a rebind between the DoH check and the fetch remains theoretically uncaught. Same residual class as the MCP guard, now narrowed.
- **Landing-prompt → Brain still uses the in-app action registry, not real MCP** (deferred by decision) — no external MCP client/OAuth/tool-discovery. A real external-MCP client stands up a new security surface (storing third-party credentials, server-side fetch to arbitrary MCP URLs, feeding externally-authored tool defs + results into the LLM context — a prompt-injection vector). Should be built deliberately (SSRF guard, credential vault, per-tool consent, 3-legged OAuth), not folded into a close-out.

### Brain chat traceability + artifact links — residual polish

- **~290-tool catalog advertised in full to the Brain every turn.** `GET /v1/mcp/tools` ships the entire `CATALOG` (each with full description + JSON-Schema params) to the web/VSIX Brain unfiltered — a large fixed per-turn context cost. A per-surface tool subset (like `CLOUD_AGENT_PLATFORM_TOOLS` for cloud agents) or lazy tool discovery would cut it. Unblocks longer multi-step Brain sessions before compaction.
- **Native VS Code chat participant doesn't get the auto-link.** `clients/vscode/src/agent.ts runAgent` (the `@builderforce` GitHub-Copilot participant loop) does NOT run `brainRunStore.runLoop`, so its MCP-created items are not auto-linked to a chat. Low priority (participant only renders where Copilot Chat is installed).

### Brain chat — duplicate gap/analysis creation (needs reproduction)

- **A Brain run can create near-duplicate gap tickets / re-run the same analysis instead of fixing the work.** Reported from a VSIX transcript: the agent, asked to fix two bug tickets, "duplicated the analysis twice and didn't fix them". The idempotent guard in `builtin_tasks_create` dedups only on an EXACT normalized-title match (`normTitle`), so a retry that rephrases the gap title slightly slips through as a second ticket; and there is no cross-turn guard against re-deriving the same analysis. Fixing = (a) fuzzy/semantic title-dedup (or dedup a new gap against the chat's already-linked tickets via `builtin_chats_list_tickets` before creating), and (b) a stronger directive/guard against re-running a completed analysis. Needs a captured repro to pin the exact path. Unblocks the agent converging on the FIX rather than re-analyzing.

### Ask-the-Manager — open verification after the persisted-persona repair (0379)

- **Verify a live manager turn now emits tool calls on project 11 / chat 86.** The zero-tool-call capture (api 2026.7.172, `xai-oauth/grok-4.3`, 7 turns / 102 tools / 0 calls) is explained by the persisted persona reciting dead catalog ids — `ide_agents.bio` written by migration 0376, unreachable by the code fix — and repaired by `0379` + the tool-free persona (see DONE.md 2026-07-28). What that capture CANNOT distinguish is whether the vendor path also contributes: the same trace is produced by "the prompt named nothing callable" and by "this model/vendor will not tool-call here". The confirming evidence is a fresh Ask-the-Manager capture showing `tool calls: > 0`, taken at least five minutes after the deploy (the resolved persona is read-through cached, 300s KV / 60s L1). If it is STILL zero with a persona that names no tool, the remaining suspect is the xAI Responses path (`vendors/xaiOAuth.ts` → `responsesApi.ts`) — instrument `buildResponsesBody` against a real 102-tool payload rather than re-reading the prompt. **BLOCKER: needs a live turn.**
- **ANSWERED on the prompt half, 2026-07-31 — the remaining suspect is now the live one.** A fresh capture (chat 86, api 2026.7.172+) came back at zero tool calls again, but its newest reply transcribed **`builtin_manager_digest`** — the ADVERTISED name — where the pre-0379 replies had recited `manager.digest`. That is positive evidence the persona repair took and the prompt names nothing dead, and it moves the whole remaining suspicion onto the vendor/model path. The report no longer conflates the two (`narratedToolNaming`; see DONE.md 2026-07-31). So the open item is narrowed: **instrument `buildResponsesBody` (`vendors/xaiOAuth.ts` → `responsesApi.ts`) against a real 102-tool payload** and confirm whether the tool array survives the Responses translation at that size — the model may never be seeing the definitions it is being asked to call. Note the same chat has since resolved onto `direct/meta/muse-spark-1.1`, so a repeat may not even be on xAI. **BLOCKER: needs a live turn on a known vendor + the request body as sent upstream.**

### Brain data tools — open question on chat #71

- **Verify the 308→64 tool selection actually restores tool calling on chat #71.** The capture proved the catalog loads fine (`Tools available to the model: 308`), so the zero-tool-call behaviour is the oversized catalog, now trimmed per turn. A fresh turn should show a `tools.selected` trace step and an actual tool call. If it STILL makes none with 64 relevant tools advertised, the remaining cause is the free-pool model itself and the fix moves to model routing (pin a BYO model for capability chats). BLOCKER: needs a live turn.

- **P2 — Mesh WebRTC caps at ~4-5 simultaneous cameras.** Meeting/ceremony video ([useMediaRoom.ts](./frontend/src/lib/useMediaRoom.ts)) is full-mesh P2P (bandwidth ~N²). Fine for a standup, degrades for a "whole team on camera" broadcast. The hook + signaling relay are provider-shaped so an SFU can be swapped in behind the same interface. **Blocked on infra:** needs a real Cloudflare Calls (Realtime) app — an SFU implementation behind the `UseMediaRoom` interface + server-side track push/pull.

- **P2 — TURN relay not provisioned (managed code path now wired).** `GET /api/meetings/ice` mints Cloudflare-managed short-lived TURN credentials when `CLOUDFLARE_TURN_KEY_ID` + `CLOUDFLARE_TURN_API_TOKEN` are set, in addition to the static `TURN_URL`/`TURN_USERNAME`/`TURN_CREDENTIAL` path. **Remaining = an ops step (credentials):** create a TURN key in the Cloudflare Realtime dashboard and `wrangler secret put` the two values. Until then ~10-15% of symmetric-NAT peer pairs can't connect.

- **P3 — Meeting agent voice is browser `speechSynthesis`, not a cloned voice track.** Agent attendees speak via a caption/transcript bridge voiced client-side by the Web Speech API. This uses the OS default TTS voice, not the tenant's cloned voice, and is silent on browsers lacking `speechSynthesis`. Fix = optionally synthesize server audio via the existing `ttsProvider` + an enrolled voice clone, store in R2, carry an `audioUrl` on the `agent-say` frame. **Blocked on product:** a decision on which enrolled voice each agent uses.

- **`FeedbackTriage.load` is memoized on `refreshKey` only (eslint-disabled exhaustive-deps).** Callers recreate `load` per render, so depending on it would loop; the current guard is correct but fragile if a caller ever needs `load` to change without `refreshKey` changing.

---

## 🛍️ Commerce — marketplace, talent & freelance

> Domain `commerce` (seat: Platform). Listings, orders, gigs and bookings — the marketplace and the freelance economy on top of it. The rails that move the money are under [Finance](#-finance--billing-payouts--entitlements).

- **A buyer cannot drive a listing before they buy it — the Stage *sandbox* is parked.** Installing a listing's
  snapshot into a throwaway tenant and driving it needs a disposable tenant with a lifecycle and a cost
  per stage-press. **Blocker: an infrastructure decision** on what that tenant is and who pays for it.
  Until then the platform states the limit honestly as `stage.sandboxLimit`, a standing `warn` that
  `declaredLimits` carries onto the buyer's page. Unblocks: a buyer driving the thing before they buy it.

- **Two of the five "no Builderforce equivalent" domains are still open; three are closed** *(hired.video port, T6; re-audited 2026-08-22 against `schema/` — the original list of six is now two)*. **Closed since it was written:** (d) WebAuthn/passkeys (0988), (c) whitelabel (`whitelabel_tenants`), (e) courses/LMS core (`courses`, `course_modules`, `course_lessons`, `course_enrollments`, `course_certificates`, `lms_connectors`, `lms_course_publishes`, `course_checkouts`, `learning_cohorts`), (f) bookings (`booking_services`, `booking_hosts`, `booking_reservations`), and (a) gamification — the whole points economy shipped 2026-08-22, see DONE.md. **Still open, and both are application work over tables that exist:** **(b) affiliates**, which per the operator decision of 2026-08-22 is NOT a new economy but the existing **Associate** program — hired.video's `affiliate_profiles`/`_referrals`/`_conversions`/`_payouts`/`_tiers`/`_link_clicks` reconcile onto `sales_associate_settings`, `sales_referrals` and `sales_commission_rules` with commissions as `ledger_entries`, and `growth.affiliate_referrals` is a duplicate of that shape that should be retired rather than populated; and **the LMS remainder** — learning PATHS (ordered `relations` edges over `courses`, not a join table) and the xAPI LRS (statements are actor-verb-object, which is `activity_log`'s shape; `external_lrs_targets`/`lrs_credentials` are `connections`+`credentials`). Unblocks: an Associate program that pays through the same ledger every other payout uses, and a learning surface that can sequence courses and report completion to an external LRS.
- **Only the TAX half of hired.video's payout stack is still unported** *(hired.video port, T6; rewritten 2026-08-22 — the "payouts are an env-gated stub" claim this entry rested on was false)*. Payouts, onboarding, escrow release and the earnings report ARE built: `application/payouts/PayoutAccountService.ts` (destinations as `connections` capability=`payout`, credential sealed by `credentialCrypto`, 0459's partial unique indexes enforcing one default), `finance/withdrawalMethods.ts`, `marketplace/milestones.ts` escrow and `finance/earningsLedger.ts` all move real money through `ledger_entries`. What is genuinely missing is **tax reporting**: hired.video's `TaxReportingService` and its AES-GCM tax-ID vault have no equivalent, so a platform that pays people cannot produce a 1099. The vault is the part that needs care — a tax ID is the worst possible fact to store twice, so it belongs in the kernel `credentials` seal that already holds every other secret, not in a column. Unblocks: paying US contractors above the reporting threshold without a manual year-end scramble.

- **The unified menu's data halves are open: no company directory, no seat-workbench index, and two entitlement helpers.** *(unified menu, after the 2026-08-09 build — see [PRD 21 §11.11](./specs/builderforce/21-prd-unified-experience.md#1111-what-shipped-and-what-did-not--2026-08-09))* The IA shipped (one registry, the arc, four marketplace families, reference pages as panels); three data-shaped pieces did not. **(a) The company family is visible and inert.** `companies` exists (PRD 20, investor domain) but is tenant-scoped with no nullable-owner column and no verification, so browse → claim → verify has nothing to write against; it needs the company graph PRD 19 B0 brings **and** the answer to PRD 21 §7 decision 4 (does the directory list businesses that never opted in — a data-protection call, not a schema one). The family renders with an honest line rather than hidden, per §2.6 rule 7. **(b) Level 2 is still each destination's existing tab set** — `filter(parent === seat)` waits on the PRD 19 tracks that bring leaves to parent, so `REFERENCE_DESTINATIONS` currently ends at the explainer rather than continuing into a seat's feature tree. **(c) Two entitlement helpers now coexist**: `earnedRung()` (three rungs — public / signed-in / workspace) and `RosterNav`'s `earned()` (PRD 20's manifest rungs, still used by the kernel surface). Reconciling them is a decision about the manifest's rung scale, i.e. PRD 20 data, not UI. Related: the fourth rung ("claimed a company") cannot be enforced until a company can be claimed, so RUN rows sit at `WORKSPACE`. **Blocked on:** PRD 19 B0's company graph for (a) and (b), and an operator answer to §7 decision 4 for (a). Unblocks: the second front door — browse a directory, claim your business, and the whole Run stack scopes to it.
- **Marketplace = one surface (talent/models/workforce/skills/personas/content).** **Jobs + Projects as categories (vision):** add `jobs` next (reuse `listJobs()`, low-risk); `projects` needs a product decision on what's browsable to whom.
- **Monetization not wired (partial).** Publishing an agent stores `published`+price but `'agent'` isn't a `artifactTypeEnum` value → no purchase/payout path; "Hire" only increments `hire_count` (no provisioning/runtime bind); kanban-template/skill/persona installs are still free grants (Stripe checkout unbuilt). Knowledge listings work end-to-end on `PAYMENT_PROVIDER=manual`; hosted-card one-off settlement is the remaining follow-up (see Knowledge & canvas below).
- **Gig Marketplace — deferred follow-ups.** Payments/payouts still ride the existing stub (`isPayoutsConfigured`) — a fixed-bid gig has no escrow/milestone flow. The hired-worker engagement-board grant is intentionally NARROW; no full board mutation / comments / web-IDE for the freelancer yet. Two publish paths exist (`marketplace.publish_ticket`; `POST /api/jobs` with `sourceTicketId`) — could converge. Eval scores (`proposal_evaluations`) aren't yet surfaced on an insights/analytics lens — only inline on the row.
- **Freelance / talent.** Avatars are served full-size from R2 (add on-the-fly resize). Payout backend needs `PAYOUT_WEBHOOK_URL`; notify email needs `NOTIFY_EMAIL_URL` (both degrade to manual/in-app). Register page body + roster `RolesTab` drift + `available_for_hire` schema drift (add a migration or allowlist — gates `npm test`). The hired.video clauses of this entry (env-gating, binary résumé parsing, the embed viewer) are closed by 0471 — see DONE.md.
- **Upwork-parity gap audit (freelance marketplace).** In-platform messaging + two-sided reputation + JSS/Top-Rated badges are done. Remaining vs Upwork, ranked by leverage:
  1. **Fixed-price contracts + milestones + escrow (P0) — the money movement still rides the env-gated
     payout stub.** *(narrowed 2026-08-19 — everything else in this clause is closed; see
     [DONE.md](./DONE.md) for the data model, the state machine, the funded-before-work gate, both UI
     halves and the bidder's counter-proposed schedule.)* `ledger_entries` records every escrow move —
     `hold` on fund, `payout` on release, `refund` on cancel — idempotent on the unique
     `escrow:<milestoneId>:<action>` reference, so the platform's own books are correct. What is not
     wired is the leg that moves real money out: `createPayout` is behind `PAYOUT_WEBHOOK_URL`, and a
     release deliberately still succeeds without it, because refusing would strand every self-hosted
     install. **Partial by nature**, and the honest fix is the hired.video payout stack this register
     already names as the port direction (`PayoutLedgerService`, `PayoutOnboardingService`,
     `ReleaseWorker`, plus Tremendous and Helcim-ACH adapters). Unblocks: a released milestone reaching
     a freelancer's bank rather than only the ledger.
  2. **Financial completeness (P2) — narrowed to tax forms.** *(2026-08-20)* The platform/service-fee model is stated out loud (`finance/platformFees.ts`), withdrawal methods are managed as kernel `connections` with `capability='payout'` rather than a second table (`finance/withdrawalMethods.ts`), and the earnings/transaction-history surface exists (`finance/earningsLedger.ts`). What is left is **tax forms — 1099 / W-8BEN / W-9** — for which nothing in `api/src` exists, and which is the reporting half of the same payout stack item 1 names as the port direction.
  3. **Trust & governance (P2) — narrowed to the bid economy.** *(2026-08-20)* The dispute/mediation flow shipped: migration 0986 gave `gig_disputes` (0425, which had no writer at all) an escrow-milestone subject, so `disputed` finally has the human exit `application/marketplace/escrow.ts` said it was waiting for. Still absent: a client payment-method verification badge, a Connects/bid-economy or membership tier, productized fixed-price packages, and agency-as-freelancer accounts.

---

## 📣 Growth — marketing, paid media, social & public surfaces

> Domain `growth` (seat: CMO). Campaigns, paid media, organic social, landing pages, SEO and every public marketing surface.

### 💸 Paid advertising & measurement — residuals

> The connect → launch → steer → measure slice shipped 2026-08-15 (migration **0470**,
> `/api/ads` + `/api/measurement`, the `AdsProvider` and `AnalyticsProvider` ports, eight
> ad networks, four measurement platforms, six new organic social networks and the
> `ad-insights` daily sweep; see [DONE.md](./DONE.md)). These are the parts deliberately
> left out of that pass.

- **No adapter has ever run against a live ad account, so no spend path is proven.** *(identified 2026-08-15 while building the ports)* All eight adapters are asserted against recorded provider payloads in `adsProviders.test.ts` — the unit conversions, the objective refusals, TikTok's 200-with-an-error envelope, Google's budget-before-campaign ordering — but a payload that parses is not a campaign that runs. The failure modes that remain are exactly the ones a unit test cannot see: a Google developer token without Basic access, a Meta app whose `ads_management` was never reviewed, an X account with no funding instrument, a LinkedIn tenant without approved Advertising API access. **Blocker: one sandbox or live ad account per network, plus the developer app behind it — neither is available in this environment.** Until one full create → launch → pause → sync cycle is recorded per network, the honest description is "written and structurally verified", not "proven". Unblocks: telling a customer we run their paid media and meaning it.
- **All nine ad networks now satisfy the ad-set/ad port, but NOTHING CONSUMES IT — there is no service, route or UI beneath a campaign.** *(logged 2026-08-20 while migrating the six adapters the 2026-08-19 commit left behind)* `AdsProvider` gained `listAdSets`/`createAdSet`/`updateAdSet`/`listAds`/`createAd`/`updateAd` plus the targeting vocabulary, and every network implements them (see [DONE.md](./DONE.md)) — but a grep for those methods outside `networks/` returns only `adsService.createAdCampaign`'s default-ad-set composition. The `ad_sets` and `ads` tables exist and are written by nobody; `adUtm.ts` (the UTM tagger the port's own docs say stamps a destination before it reaches an adapter) has **zero callers**. So a campaign can be created and targeted only through the one default ad set, and the audience a caller names in `AdSetDraft.targeting` has no surface that can name it. Fix = an `adSetService` over the three levels, `/api/ads/:account/adsets` + `/ads`, the UTM stamp wired into `createAd`, and the canvas panel that edits them. Unblocks: targeting a paid campaign at all from outside the port, and `adUtm.ts` ceasing to be dead code.
- **LinkedIn's stored external ids were re-pointed by migration 1102, and the rows written under the old mapping are RETIRED rather than re-linked.** *(logged 2026-08-20)* The adapter used to map our campaign onto a `sponsoredCampaign`, which is LinkedIn's ad-set level; it now maps onto `sponsoredCampaignGroup` (see [DONE.md](./DONE.md)). A migration cannot repair the old rows — an `ad_campaigns` row holds a campaign id and its parent GROUP id exists only on LinkedIn — and deleting them would cascade every day of LinkedIn spend in `ad_insights`, which the trailing sync window cannot rebuild. So 1102 prefixes their `external_id` with `retired:li-*` and stamps the name: history survives, the sweep re-populates at group grain, and the retired rows sit beside the new ones. Fix = a one-off reconciler that reads each retired campaign's parent group from LinkedIn and re-points the row, or an operator decision to drop them. Unblocks: LinkedIn spend history joining up across the remap instead of reading as two campaigns.
- **Connecting an ad account is a PASTED TOKEN, not a consent flow** — the same gap the social accounts have, for the same reason. `application/shared/providerOAuthConnect.ts` already generalises the round trip and each network satisfies `OAuthProviderConfig` structurally, so the code side is a registry entry per network plus `/api/ads/callback/:network`. **Blocked on operator action, not code:** each network needs its own developer app with `/api/ads/callback/*` registered, and every one of them gates SPEND permissions behind its own review. Until those exist a consent flow would dead-end after the user granted it. Unblocks: a CMO connecting their own ad accounts without visiting eight developer portals.
- **YouTube publishes from the CANVAS but not from a social campaign, and the durable upload table has no writer.** *(narrowed 2026-08-20)* `application/youtube/youtubePublishing.publishCanvasVideoToYouTube` now streams an R2 object straight into Google's resumable session and returns a video id, mounted at `/api/youtube` — so a canvas video CAN be published. Two things did not follow it: `socialProviders.youtube` still declares `publishMode: 'none'`, so `socialCampaignService` refuses the network and "post the launch video everywhere" still omits the one where video is the point; and migration 1095's `youtube_uploads` table — the chunked, resumable, Worker-eviction-surviving upload the migration argues for at length — is registered in the entity layer and **written by nothing** (grep: three hits, all schema/registry). The single-shot PUT works only while the whole object fits one request's budget. Fix = a chunk sweep over `youtube_uploads`, then flip `publishMode` to `'media'`. Unblocks: publishing a long video, and publishing to YouTube from a campaign.

### 📣 Connected social accounts & social campaigns — residuals

> The connect → read → compose → publish slice shipped 2026-08-12 (migration 0458, `/api/social`, canvas `socialFeed` / `socialPost` / `socialCampaign`; see [DONE.md](./DONE.md)). These are the parts deliberately left out of that pass.

- **Connecting a social account is a PASTED TOKEN, not a consent flow.** A social account is a `connector_connections` row on the built-in X / LinkedIn / Facebook / Instagram / TikTok manifests, so connecting means pasting an access token (plus a Page id, author URN or IG account id) rather than clicking through the network's own consent screen. `application/shared/providerOAuthConnect.ts` already generalises the round trip for mailbox / drive / calendar and each social network satisfies `OAuthProviderConfig` structurally, so the code side is a registry entry per network plus `/api/social/callback/:network`. **Blocked on operator action, not code:** each network needs its own developer app (client id + secret in the deployment's secrets, `/api/social/callback/*` as a registered redirect URI) and each one gates PUBLISHING permissions behind its own app review — X needs an elevated user-context token, LinkedIn needs approved Community Management access, Meta needs `pages_manage_posts` / `instagram_content_publish` reviewed, and TikTok needs Content Posting API approval. Until those exist a consent flow would dead-end after the user grants it. Unblocks: a CMO connecting their own accounts without visiting five developer portals.

### CMO canvas review — where Idea → Real still stops *(2026-08-13; re-validated 2026-08-18 — the ad-connector and web-analytics halves have since shipped and were removed)*

> The bullet below shares its root cause with the three that closed on 2026-08-20: `api/src/infrastructure/database/schema/growth.ts` already declares the marketing data model (`brand_kits`, `ad_campaigns`/`ad_sets`/`ads`, `ab_tests`(+`_variants`/`_segments`), `landing_pages`, `marketing_seo_pages`, `customer_journeys`/`journey_touchpoints`, `marketing_leads`, `marketing_audiences`/`_members`, `marketing_suppressions`, `marketing_assets`, `nurture_flows`/`follow_up_enrollments`, `site_traffic_daily`, `marketing_heatmap_pages`, `marketing_content_items`) and `CREATION_OBJECT_REGISTRY` exposes **none of it**. A grep of `frontend/src` for `brandKits|adCampaigns|abTests|landingPages|marketingLeads|customerJourneys|nurtureFlows|siteTrafficDaily|marketingAudiences|marketingAssets` returns exactly one unrelated file (`SurveysManager.tsx`). The canvas can compose and publish a campaign; it cannot budget one, keep it on brand, or prove it worked.

> **Three of the four landed 2026-08-20.** `MARKETING_OBJECT_KINDS` in `packages/creation-canvas-contract/src/marketing.ts` declares `brandKit` and `audience`, with `BRAND_BINDING_FIELD` (`brandKitRef`) added to exactly the generative kinds and a board-holds-exactly-one fallback; `CanvasCalendarSurface.tsx` draws the time axis over every object already carrying `scheduledAt`, which is why no `campaignCalendar` kind was added. See [DONE.md](./DONE.md). One bullet remains, below.
>
> Two defects rode in with it and were fixed the same day (see [DONE.md](./DONE.md)): the contract's `MarketingObjectKind` was missing from `creationObjectRegistry.ts`'s exhaustiveness `Exclude`, and neither kind was labelled in any of the five catalogs. The label-coverage half of that lesson is still open and is recorded under [Canvas](#-canvas--creation-surface-documents-studio--qa).

- **No A/B test object, so `experiment` records results nothing produced.** `growth.ts` has `ab_tests`, `ab_test_variants`, `ab_test_segments` and `experiments`; the founder `experiment` kind stores `variants` as an **authored** table (`{variant, exposure, conversion, lift}`) with no binding to a live split, no `website`/landing-page variant, and no traffic allocation. Consequence: the object that is supposed to make marketing empirical is a place to type numbers you got somewhere else — the empty-shell defect `emptyShellProblem()` was written to stop, reappearing one level up as a *credibility* shell. Fix = bind `experiment` to `ab_tests` + a variant field on `website`/`prototype`. Unblocks: "we tested it" meaning something.

### Marketing surfaces & copy

- **Sales deck has zero customer proof** (CEO-review finding, 2026-07-23): no logos, quotes, case studies, or measured outcome numbers anywhere in marketing — the deck's ROI slide is explicitly *illustrative*. Needs GTM work, not code: land ≥1 design partner, capture one measured result (e.g. PR cycle-time delta, token-spend delta from the consumption meters we already ship), and swap it into the deck's Unit-Economics slide + /customers surface. Unblocks moving buyers from "pilot" to "org-wide."
- **The marketing surface has 4 primitive importers out of 478 files, and 818 raw controls re-derive the accessibility contract by hand** *(public-page audit 2026-08-09, partially closed same day)*. Repo-wide `@/components/ui` adoption is 12 files; on the public pages it is `HomePatterns.tsx`, `NewsletterSignupSection.tsx`, `ToolsHubClient` and one freelance profile card — plus `RouteMarketing.tsx`, migrated in the audit pass (→ DONE.md), which moved the shared teaser surface of **86 routes** onto `<ButtonLink>`/`<Surface>` and deleted its 95-line parallel vocabulary. What remains: **2,652 inline `style={{…}}` sites, 633 hand-rolled `1px solid` borders, 581 raw `<button>`s and 237 raw `<input>/<textarea>/<select>`s** in marketing-owned files. §2.7 already rules the inline-style and hand-rolled-border rows a *verbosity* measure with no ratchet, and that judgement stands — but the raw `<button>` and `<input>` counts are **not** verbosity: §2.5 says `loading` must supply the spinner *and* `aria-busy`, and "a bare `<input>` in a form is a defect", so those 818 sites are 818 places where the a11y contract is hand-rolled. `surfaceClassName()` now exists (`components/ui/Surface.tsx`) precisely so a `<Link>`, `<details>` or `<figure>` can take the surface look without re-inlining a border — the seam that blocked this sweep. Fix = continue the migration outward from the shared chrome, then ratchet raw `<button>`/`<input>` shrink-only. **Not blocked.**
- **1,136 English marketing copy literals live in TypeScript, outside the i18n catalogs** *(public-page audit 2026-08-09)*. `lib/content.ts` (977 long literals) and `lib/routeMarketing.ts` (159) are the single source for the product sections, competitor/integration SEO copy, and per-route hero + highlights + FAQ — plain English string constants, so `RouteMarketing` shows English BODY copy on 86 public routes in all five locales even though its chrome is now translated (`routeMarketing.*`, five catalogs → DONE.md). Plus `blogData.ts`. **Narrowed 2026-08-15:** the 25 hand-authored surfaces are unchanged, but the OTHER 61 authed routes no longer take English from this file at all — a route with no entry now renders its DESTINATION (localized `nav.group.*` name + a `routeMarketing.destination.*` pitch shipped in all five catalogs) or the method, so the English-literal surface shrank instead of growing with the route count (→ DONE.md). **The /legal half of this entry is CLOSED** (→ DONE.md): those seven pages are binding instruments, and the resolution there was the correct one for a contract — chrome fully translated, body English, and a translated notice stating the English text governs. Marketing copy is the opposite case and should genuinely be translated. **Blocked on an operator decision, not on engineering:** ≈1,136 strings × 4 locales is a commissioning decision (professional translation, MT-plus-review, or English-only with `hreflang`), and shipping unreviewed machine translation of positioning copy is worse than the gap. Name the choice and the code follows in one pass — the catalog wiring is already proven by the legal pass.
- **7 marketing routes still have zero localized files** *(public-page audit 2026-08-09, was 23, then 13)*. Routes whose own components never call `useTranslations`, so even labels, filters and empty states are hardcoded. **The seven `/legal/*` pages and the shared `CompliancePage.tsx` are CLOSED** (→ DONE.md), taking route-local hardcoded strings from **362 → 249** and zero-i18n routes from 23 → 16 (three of which — `/models`, `/talent`, `/diagnostics` — are bare redirects with no strings at all, so 13 real routes remain). Outstanding: all seven `/agents/*` sub-pages — `acknowledgements`, `contact`, `integrations`, `shoutouts`, `showcase`, `skills` (+ `skills/[slug]`) and `workflow-builder`, none of which calls `useTranslations` anywhere. *(`/sell-builderforce`, `/integrations`, `/integrations/[tool]`, `/marketplace/[slug]` and `/blog/[slug]` all localized 2026-08-20; re-measured this pass.)* Distinct from the copy-corpus entry above: this is UI chrome, which should simply be translated, not commissioned. **Not blocked** — *(the `.claude/hooks/localize-guard.mjs` this entry relied on does not exist in the repo; the standing rule is enforced by review, not by a hook)*. It would block the next author who touches any of them anyway.
- **Docs proxy** — verify post-deploy that next-on-pages *proxies* `/docs/*` (strip `/docs`) rather than 301-redirecting cross-origin; fall back to a Workers Route or bind `docs.builderforce.ai`. Marketing/public pages still center on `.page-inner` (decide per-page migration to left-aligned `PageContainer`). Per-route feature teasers still set `<title>`/`<meta>` from the CLIENT (convert to the server-wrapper pattern for indexable heads) — the `Disallow` half of this is CLOSED (→ DONE.md: `robots.txt` is now generated from the same registry as the sitemap). `/compile` not in `PUBLIC_SHELL_PREFIXES` (it is now a `noindex` generic teaser, which is the honest answer for an internal route, so this is a note rather than a defect). Per-article OG card text lost (generate per-article PNGs at build time). 23/25 blog posts have only the auto-cover. Feature copy in three unconnected lists (one `Feature`-shaped source). Marketing header dropdowns are CSS-only (no JS `aria-expanded`/touch toggle). Hero backdrop pivoted to a procedural solar-system fly-through (pacing/light-mode tuning remains). Stale `nav.tab.{finops,devfinops,allocation}` keys (remove in the insights tab-key cleanup).

- **`/compare` has six arenas but leaf pages for only one.** `COMPARE_ARENAS` (frontend/src/lib/content.ts) now carries 6 markets and 30 vendor columns, and `arenaForCompetitor()` already resolves a leaf page to its arena — but `COMPETITOR_SEO` still has entries for the 7 AI-coding-agent vendors only, so `/compare/{slug}` exists for those and 404s for Jira, Figma, Zapier, OpenRouter, Upwork and the other 16. The plumbing is done; what is missing is CONTENT: each leaf needs a curated tagline, summary and verdict (`compare.competitors.<key>.*`) in all five catalogs, and 23 auto-generated ones would be exactly the thin-page bloat `SEO_INTEGRATIONS` is curated to avoid. **Blocked on an operator decision** — which of the 23 markets are worth an indexed "vs" page, since that is a positioning call and not an engineering one. Unblocks: long-tail "{vendor} alternative" capture beyond coding agents.

- **Distribution: three channels still need a credential or a live environment** *(narrowed 2026-08-07 — the engineering half shipped, see [DONE.md](./DONE.md) and [distribution/README.md](./distribution/README.md))* — the remote MCP server, the Claude Code plugin, the GitHub Action, the generated Docker MCP payload and the CI publish jobs all exist. What is left is **not code**: (a) **Hugging Face** — `hf-export/Evermind` has its card and all four artifact formats ready, blocked on an HF account + write token; (b) **a reviewer test tenant** — a seeded tenant with realistic projects/tickets/agents plus a shareable credential, blocked on a live environment; it is a hard gate for Anthropic, Slack, Atlassian, AWS and Microsoft review; (c) **cloud marketplaces (AWS / Microsoft / Google)** — blocked on legal-entity + partner-program enrolment before a listing can be created at all. Also unset and therefore skipped at release: `VSCE_PAT`, `OVSX_PAT`, `ACTION_MIRROR_TOKEN`/`ACTION_MIRROR_REPO`. Unblocks: the ~10 remaining Tier-1/Tier-2 listings.

- **A published site cannot run ARBITRARY server code** *(narrowed 2026-08-06; was "cannot run server code")*. The site now has a server half: its project's canvas handlers answer on its own origin at `https://<site>/api/<route>` through the shared ingress executor, a handler can respond `text/html` (server-rendered pages) and read the site's own collections back via a `data` step. What is still out of reach is **customer-authored JavaScript**: no framework SSR, no arbitrary compute, because Workers has no `eval`/`new Function` and the alternatives are Workers-for-Platforms (a paid dispatch namespace) or a container per project. `project_sites.mode` still only ever writes `'static'`; `'container'` needs the flag-gated preview ingress (§1) promoted to a durable web-serving surface, which is the same work as the container-mode residual above. Unblocks: a backend whose logic exceeds the declarative vocabulary.

- **Custom-domain certificates are code-complete but not live** *(residual after 0412, 2026-08-06)*. Claim, DNS-over-HTTPS ownership proof, the Cloudflare for SaaS client, the lifecycle sweep, host routing and the UI all ship and are unit-tested against a fake Cloudflare. **Single concrete blocker: the Cloudflare for SaaS entitlement on the builderforce.ai zone plus `wrangler secret put CLOUDFLARE_ZONE_ID` / `CLOUDFLARE_SAAS_API_TOKEN`.** Until then a verified domain parks at `pending_certificate` and says so — ownership verification itself needs no credential and works today.

---

## 🔌 Integrations — connectors, repos & the developer portal

> Domain `integrations` (seat: Platform). The connection as an entity: third-party providers, git hosts, OAuth round trips, templates, and the publishers who extend the platform.

### ⌗ Templates — residuals

> The templates framework landed 2026-08-16: a guided-setup contract with a step-kind registry, a template manifest with an output-kind registry, a three-source catalogue merged like the connector one, and the four parallel "starting point" catalogues consolidated onto one entry contract (see [DONE.md](./DONE.md)). These are the parts deliberately left out of that pass.

- **A publisher can list an installable template but not a canvas pack or a prompt.** `POST /api/templates` accepts a manifest whose outputs are `workflow` / `tasks`; the `pack` and `prompt` entries in the merged catalogue are CLIENT sources (`lib/templates/catalog.ts`) with no server manifest behind them, so a third party cannot publish either. Closing it means two more registered output kinds (`canvas`, `prompt`) whose materialisers write a creation session rather than workspace rows — which needs a product decision first about whether a purchased pack lands as a session, an object group, or a board. Until then the marketplace's Templates chip lists automations only, and the packs/prompts beside them are ours.

### 📥 Google connectors — storage backend + inbound trigger

- **Google Drive as a project's PRIMARY file storage backend is not wired.** The Drive integration is connectable/testable, but the IDE's working store is still R2-only: `ideRoutes.ts` reads/writes/lists/deletes files inline against `env.UPLOADS`. Using Drive AS the backend needs (a) a `StorageProvider` port with R2 + Drive adapters, (b) refactoring those 4 handlers behind it while preserving the seeding/repo-import parity, (c) a per-project `storage_backend` column + selector, and (d) Drive path↔fileId mapping (Drive is name/parent-addressed, not path-addressed). Genuinely blocked on a live Google OAuth app to verify end-to-end. Unblocks: "connect Google Drive and use it as the backend storage for a project/account."
- **Google Drive and OneDrive are built but cannot complete a consent round trip until the app registrations grant file scopes.** *(cloud files pass 2026-08-07)* The whole slice ships — `drive_connections` (migration 0415), a `DriveProvider` port with Google Drive and Microsoft Graph adapters, `/api/drive` (`connect`/`callback`/`files`/`content`), the browsable panel on the canvas, and the import that hands a fetched file to the same engine a drag-and-drop uses. It reuses the login clients (`GOOGLE_CLIENT_ID`/`MICROSOFT_CLIENT_ID`), which is why no new secret is needed — but those apps were registered for IDENTITY only. **Blocked on three operator actions that cannot be done from this repo:** (1) in Google Cloud Console, enable the Drive API and add `https://www.googleapis.com/auth/drive.readonly` to the OAuth consent screen (a scope the consent screen does not declare is rejected at the authorize step, before any of our code runs); (2) in the Azure app registration, add the delegated `Files.Read` permission; (3) register `https://<api-host>/api/drive/callback/google` and `.../callback/microsoft` as authorized redirect URIs on both. Until then `/connect/:provider` returns a consent URL the provider refuses, which is why the panel surfaces each provider's `configured` flag rather than pretending. Unblocks: browsing your own Drive on the canvas and opening a Google Doc as an editable document without downloading it first.

### 🪣 Bitbucket Server parity — the rest of the surface

- **`buildGitApiBaseUrl` takes an opt-in `{ allowBitbucketServer }` flag, and only the branch-lifecycle / PR callers pass it.** The Server 1.0 base is NOT path-compatible with Cloud's `/2.0/repositories/...`, so `getPullRequestDetail`, `readRepoContents`, `commitFileToRepo`, `repoBridge` and `fetchBuildError` keep throwing their typed `unsupported` refusal. A Bitbucket Server tenant can have branches swept and PRs opened/merged/reverted, but still cannot use the IDE repo bridge, PR detail, or CI build-error context. Fixing means giving each of those callers a Server path builder. Unblocks: full self-hosted Bitbucket support.
- **Bitbucket (both editions) cannot revert a merged PR.** Neither Cloud nor Server exposes a server-side revert, and synthesizing the revert commits would need the Cloud `src` API, which has no Server equivalent. `revertMergedPullRequest` returns the structured `unsupported` refusal. Unblocks: post-merge undo on Bitbucket.

### 🐙 GitHub offload — residuals

- **The GitHub App does not exist yet — only the code that uses it.** `githubApp.ts` + `resolveRepoAuth` are live and tested, but with `GITHUB_APP_ID` / `GITHUB_APP_PRIVATE_KEY` unset every repo call authenticates with the tenant's user PAT. The visible consequence: Check Runs silently degrade to commit statuses (the Checks API is App-only). Fix = register the App on the GitHub org, grant `checks:write` + `contents:write` + `pull_requests:write` + `workflows:write` + Code-scanning/Dependabot read, then `wrangler secret put` both values. Unblocks: rich checks, least-privilege repo access, token rotation, and deleting the `pollPrCiStatus` fallback.
- **QA and security findings still have no per-file anchor.** The acceptance-review path produces `{path, line}` findings end-to-end, but the other two producers do not. `QaFindingLike` carries `route`/`selector` (no source location); `SecurityAuditService.recordFinding` takes `location` as free text; `githubAlerts.ts` stringifies a real line number into `"path:line"` at the mapper. Neither can post inline PR comments or check annotations. Fixing means giving those two the same `path`/`line` fields and parsing the alert location.
- **CodeQL `query-filters` is deliberately empty and untuned.** `.github/codeql-config.yml` mutes nothing. The likely noise source is the shell-command-injection family firing across `api/container/server.mjs` and `agent-runtime/`. After the first scan, add `exclude:` entries citing actual alert IDs and confirm the sandbox boundary is genuinely test-covered before muting.
- **`agent-runtime` unit tests are an advisory CI job, not a gate.** `test:fast` has 7 failing files locally, all of which look Windows-specific (temp-dir `ENOENT`/`ENOTEMPTY`, a "keeps lock on linux" test, a collision with a real running gateway) rather than genuine regressions — but that was never verified on Linux. The job runs `continue-on-error: true`. Resolve it either way: drop `continue-on-error` once green, or fix/remove the job.
- **`agent-runtime` formatting, lint and LOC checks are ungated pre-existing debt.** Re-measured: `format:check` reports **3,449** unformatted files, `lint` **588 errors** across 3,654 files, and `check:loc` **12 files** over the 500-line limit (all 501–516 lines). Deliberately NOT auto-fixed — `prettier --write` across 3,449 files is a single mechanical commit that destroys `git blame`, a decision the operator should take on its own merits. The LOC leg is the cheap one — 12 files each needing ~20 lines extracted.
- **The Actions never-scheduled reconcile only correlates precisely for repos re-enabled after 2026-07-19.** Correlation matches an execution id embedded in the workflow's `run-name`. A repo still running the pre-`run-name` workflow produces anonymous runs, and the classifier returns `wait` rather than risk failing a live run — those fall back to the 20-minute reaper. Fix = an operator clicking "Enable agent runs" again, or a migration re-committing the workflow to every enabled repo.
- **The cloud-agent surface picker warns rather than hard-disabling GitHub Actions on a repo that can't run it.** A hard disable needs the readiness boolean inside the picker, which would mean a second readiness hook call or prop-drilling a `canX` the DRY rule forbids. The warning names the exact fix, but a user can still submit the form and get the durable-executor fallback.
- **`qa-e2e` cannot be a PR gate.** Its Playwright suite runs against a live deployed environment with credentials and pulls its test list from the API first, so it is excluded from `ci.yml`. Covering it would need an ephemeral deploy per PR.

### 🔌 OpenAI Codex BYO — one residual

- **The Codex request contract is verified by unit test, not against the live ChatGPT backend.** No live Codex subscription credential is available, so the corrected contract (and now the real passthrough stream) is asserted against a mocked upstream only. If "Test connection" still fails after deploy, the error string names the real upstream status per attempt. **BLOCKED: needs a live ChatGPT/Codex subscription credential — no code change substitutes for one.** Unblocks: confirmed end-to-end BYO Codex routing.
- **The OAuth redirect still races a locally-running Codex CLI.** `OPENAI_CODEX_REDIRECT_URI` is `http://localhost:1455/auth/callback` because that is what OpenAI has registered for this client id; the user copy-pastes the redirect URL into `oauth/complete`. A spent code is now diagnosed by name (`OPENAI_CODEX_CODE_CONSUMED`, a 400 that says to quit `codex login` and retry) and the connect copy warns before the attempt — but the RACE itself needs a redirect URI on a domain we control, registered against a client id we own. **BLOCKED: an OpenAI-side client registration, not a code change.** Unblocks: a non-racy, non-copy-paste connect flow.

### 🔑 Google OAuth — verification review for the connector scopes

- **Gmail/Calendar/Drive/YouTube connectors work only for Google test users until the app passes verification** *(2026-08-18)* — the redirect URIs are registered and the consent screen now renders (see [DONE.md](./DONE.md)), and `/legal/privacy` + `/legal/terms` now exist as the public URLs the consent screen and the verification form require. What remains is the submission itself: Gmail's `gmail.modify`/`gmail.send` are RESTRICTED scopes and Calendar/Drive/YouTube are sensitive, so any account that is not on the Google Cloud Console test-user list cannot complete a connector consent. **BLOCKED: Google's verification submission is an operator action in the Cloud Console (app must be published, policy links set, and a demo video of the consent flow recorded) — no code change substitutes for it.** Unblocks: Connect inbox for every tenant rather than only listed test users.

### 🧩 Developer Portal — the money half and the review stages

- **The paid path and the programs are not built** *(PRD 24 Phase 1 shipped 2026-08-15; the DNS-verification sweep and the Phase 3 catalog projection landed 2026-08-19 — see [DONE.md](./DONE.md); [PRD 24](./specs/builderforce/24-prd-developer-portal.md) §7)*. A publisher can register, verify their domain self-serve, ship a reviewed version, have any tenant install it under a scope grant, and be found on the public `/integrations` page. *(Phase 3's remainder — directory search/categories/ranking, publisher install analytics, and the dynamic sandbox-tenant and agentic governance-agent review stages — landed 2026-08-20 with migration 1094; see DONE.md.)* What is NOT built: **Phase 2** — install-scoped OAuth for a vendor acting on a tenant's behalf, paid plans against `catalog_items`, vendor webhooks (`installation.created`), metered usage onto the tenant invoice, and the payout leg; **Phase 4** — the Technology/Solutions partner tracks, the rev-share threshold and hyperscaler listings. **BLOCKED on operator decisions for the money half** — PRD 24 §9 asks for the rev-share threshold and confirmation that identity verification gates charging (the review gate already enforces the latter as a recommended default, and a different answer changes `packageReview`'s `paid_requires_identity` check). Unblocks: a vendor being paid for an extension.

---

## 💳 Finance — billing, payouts & entitlements

> Domain `finance` (seat: CFO). Money in and money out: subscription checkout, the card lifecycle, entitlement errors, and the attribution of what a run actually cost.

### 💎 Premium (any-paid-OpenRouter) model tier — residual gaps

- **No end-to-end card-validation run.** The SetupIntent flow is unit-tested at the evaluator/classifier level but no test card has been driven through `POST /api/tenants/:id/card-validation` → `checkout.session.completed(mode=setup)` → `card_validated_at`. Blocked on the same Stripe config as the billing section below. Unblocks: confidence before live mode.

### 💳 Stripe card processing — Stripe-only, awaiting secrets

- **Webhook endpoint not registered in Stripe.** Needs `https://api.builderforce.ai/api/webhooks/payment` subscribed to `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.payment_failed`, `setup_intent.setup_failed` → yields `STRIPE_WEBHOOK_SECRET`. Unblocks: activation-on-payment.
- **Worker secrets not set.** `STRIPE_SECRET_KEY` and `STRIPE_WEBHOOK_SECRET` need `wrangler secret put` against the account owning the `api.builderforce.ai` Worker. The four Price IDs are optional. **Until the two required secrets exist no tenant can upgrade** — the previous free-activation path is intentionally gone. Unblocks: the end-to-end test-card run.
- **No end-to-end card verification yet.** The webhook parsing/mapping is unit-tested, but no test card has been driven through `POST /api/tenants/:id/subscription/checkout` → webhook → tenant activation. Unblocks: confidence before live mode.

### 💳 Actionable entitlement errors — live verification

- **⚠️ NEEDS AN OPERATOR DECISION — live end-to-end verification.** Every path is covered by tests against the real handlers (`llmRoutes.test.ts` for the 402/free-pool gate order, `tenantRoutes.cardLifecycle.test.ts` + `webhookRoutes.cardSwap.test.ts` for validate→replace→remove→cancel), but nothing has run against a live gateway. **The blocker is no longer missing credentials:** `api/.env` holds a working `OPENROUTER_API_KEY` and `NEON_DATABASE_URL` — but that database is **production** (`*.us-east-1.aws.neon.tech`, no staging URL anywhere in the repo), so a live run would create a test tenant + API key in production data and spend real gateway credit. To clear it, either (a) point a run at a staging Neon branch, or (b) explicitly authorise the production exercise. Separately, the card lifecycle can't be exercised without `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`, so the one assumption still unverified is that a real SetupIntent's expanded `payment_method` carries the `pm_…` id that 0346/0347 persist (code degrades to the customer-wide detach if not).

---

## 🧲 Hiring — postings, sourcing & applications

- **Phone is still a connector, and the operator asked for the product** *(hired.video port, T6; decision taken 2026-08-22 — port the full phone product)*. `business_phone_numbers` exists and `tenant/businessPhoneSubscription.ts` is its only feature path; hired.video's `call_logs`, `sms_messages`, `phone_balances` and `phone_balance_transactions` are unported. **No new tables are needed** and the schema already says so: `business_phone_numbers`' own docstring reads "the calls are `deliveries`; the balance is a `ledger_entries` denomination", `deliveries.channel` already admits `'sms'`, and `comm_credits` is declared in `application/kernel/denominations.ts` for exactly this. What is missing is provisioning against a real carrier, the call/SMS write paths, and a metered balance that debits per message and per minute. Unblocks: a phone number a tenant can actually buy, and outbound SMS that is billed rather than free.

- **hired.video's job SOURCING half is unported: scraper, JD refresh and job analytics** *(hired.video port, T2; scope confirmed 2026-08-22 — the operator elected to port the scraper, accepting the third-party ToS exposure)*. `services/job/scraper.ts`, `routes/jobscraper.ts`, `routes/job-analytics.ts`, `services/job/salary-popularity.ts` and `time-to-hire.ts` ingest postings from external boards and report on them. 0471 ported the job-SEEKER half. **Targets, per the coverage map:** a board is a `connections` row (vendor = the board), its sync cursor is `sync_states` — the kernel pair that already runs every other ingestion — and a scraped posting is a `catalog_items` row, so again no new tables. Unblocks: a jobs surface fed by more than what this platform's own employers post.

> Domain `hiring` (seat: Recruiter). The job posting and everything downstream of it — applications, interviews, placements — plus the public employer surfaces that feed it.

- **FO-B3 — `jobPosting` applications is the one consumer that never routed through the signature
  primitives, and it is blocked.** *(narrowed 2026-08-19 — the other four landed 2026-08-16; see
  [DONE.md](./DONE.md).)* `applicantCount`/`distribution` are documented as "read from the hiring
  domain", but no canvas `jobPosting` object carries the numeric `job_postings.id` a real
  `job_applications` count would join on — there is no `canvas_sync_job_posting` equivalent of FO-A1's
  `canvas_sync_account` that creates or resolves that backend row in the first place. Building the
  handler without it would mean matching by title, which is exactly the string-matching defect
  FO-A1/FO-A2 exist to remove. **Blocker: a decision** on whether a canvas `jobPosting` should sync
  to/from a real `job_postings` row (its own foundation, FO-A1-shaped) or whether applications stay
  off-canvas and are read through a different surface. Unblocks: the offer→employee handover
  `people.ts` names as the seam between the Recruiter and HR seats.
- **`/companies` and `/reviews` are the last two ported calls to action still 404ing** *(hired.video article port; narrowed 2026-08-16 when /salary, /references and /interview landed)*. Three posts link to them — `how-to-leave-a-company-review-that-helps`, `hired-video-vs-glassdoor-…` and `research-employers-with-reviews-and-salary-data` — and they describe a multi-axis employer review (culture / leadership / work-life balance / compensation / career growth / diversity & inclusion) on a browsable company directory. Nothing here models either: `companies` in `investor.ts` is a PORTFOLIO company, `employer_branding_pages` is a tenant's own page, and `freelancer_reviews` rates a marketplace worker. So it needs two tables (`employer_companies`, `employer_reviews` with the six axes as smallint columns — six stable named axes are attributes, not a repeating group), a public browse + aggregate read, and an authenticated submit. **Blocker: an explicit operator decision on moderation posture, which is not an engineering call.** This surface publishes user-written claims about NAMED third-party employers, so someone has to choose the default (recommended: rows land `pending` and stay invisible until approved, making exposure opt-in), who approves, and what the takedown path is. Everything else is about a day once that is answered. Unblocks: the last three ported articles and the Glassdoor-comparison positioning they were written for.

## ⚖️ Legal — contracts & obligations

> Domain `legal` (seat: Counsel). The contract as a record with live obligations, and the documents behind it.

- **FO-G2 — A contract repository with live obligations.** `contract.obligations` is
  `{obligation, owner, due}` prose, and nothing binds a contract to the invoices it should generate or a
  `bill` to the contract it should be checked against. *(Unblocked 2026-08-16 — FO-A2 gave
  `contract.counterparty` a real `account` join via `counterpartyAccountField`, so a contract and its
  invoices now resolve to the same account instead of matching by name.)* FO-C1 landed 2026-08-15. The
  renewal-warning half is already done: `contract.renewsAt` is a declared deadline and the `due-within`
  comparator shipped 2026-08-15.

## 👤 Identity — accounts, onboarding & consent

> Domain `identity` (seat: Platform). Who a caller is: accounts, onboarding, and the consent surfaces a third party verifies us through.

- **OAuth consent screen still calls the app "Builder Force"** *(Google branding verification, 2026-08-18)*. Google's reviewer flagged that the consent-screen application name does not match the name on the home page. The home-page half is fixed — `/` now server-renders real content, names itself **Builderforce.ai** in the hero badge above the `<h1>`, and carries an `AboutAppSection` band stating the purpose and the Google scopes it requests (see DONE.md 2026-08-18). **Blocker: the remaining half is a Google Cloud Console field, not a repository value.** `"Builder Force"` appears nowhere in this codebase — it was typed into the consent screen — so it cannot be changed by a commit. Fix = set the OAuth consent screen's application name to exactly `Builderforce.ai` (matching `applicationName` in `frontend/src/app/layout.tsx` and the `og:site_name`), confirm the home-page URL is `https://builderforce.ai/`, then resubmit for verification. Unblocks: verified Gmail/Calendar/Drive/Sheets scopes, and therefore every connected-account feature that depends on them for users outside the test list.

- **Segmented-mode not usable yet.** Write-threading cutover not done (~35 insert sites must pass `c.get('segmentId')`; add owner-only `PATCH /api/tenants/:id` to flip `isolation_mode`); read isolation still app-level `eq(tenantId)` (add Postgres RLS on Neon as the enforced floor — needs `SET LOCAL` in an explicit txn given the stateless HTTP driver); no `segment_members`/per-segment roles/`seg` token claim; tenant-wide aggregation silently merges all segments; uniqueness namespaces (`projects.key`) are tenant-scoped not segment-scoped. No DB-backed test locks the no-bleed invariants.

- **Spec/impl alignment (docs 01/04/05).** Identity model contradictory (spec assumes BuilderForce federates an external OIDC IdP; the platform IS its own IdP — add an inbound-OIDC verifier keyed by `tenants.idp_issuer` + `kind EMBEDDED|DIRECT`, `resolveSegment(jwt)` accepting both shapes, `IdentityCache`); spec's agentic layer duplicates the existing stack (map onto `executions`/`workflows`/`sourceControlIntegrations`, extend with `segment_id`); spec's PM spine overlaps `projects`/`tasks`/`specs` (**product decision needed**: unify vs partition); ID-type collision (spec uuid PKs vs platform `serial`); embed = align to the `studio-embedded` mount pattern + add scoped/rotatable S2S tokens + a `tenant_webhooks` table (`workitem.released` has no emit wiring); no portal UI for MCP extensions / embed-session tokens / host BI config / GitHub-credential + swimlane assignment; ingested feedback has no triage UI + no `GET /v1/validation/engagements` proxy; MCP relay SSRF DNS-rebinding residual; embed-session tokens have no dedicated mint rate limit.

---

## 🛡️ Governance — tenancy, embed & security automation

> Domain `governance` (seat: Security). Controls, findings and policy — plus tenant isolation, the embed boundary, and the audits that prove them.

- **P3 — Live agent audit run unverified in-repo (needs creds + runtime).** The board ticket → `maybeAutoRunOnLaneEntry` → `dispatchCloudRunForTask` wiring typechecks, but a full end-to-end live audit run needs a connected repo credential + an available cloud runtime.
- **SOC 2 Type I is the governance-pitch gate** (CEO-review finding): the deck sells approval gates + immutable audit hard, but compliance posture is one "in progress" line. Already a Revenue-register item (Q4 2026) — flagging the dependency: enterprise deals sourced by the deck will stall at security review until it lands.
- **Embed rail.** The wired surfaces still need runtime validation in-frame; poker/retro WS relay is push-invalidate-then-refetch rather than state deltas/presence/hibernatable sockets; and `SessionRoomDO` needs a live deploy. `/embed/[view]` remains a legitimate third-party boundary, but every `available:true` view must resolve to a real shared surface—`sprints` and `velocity` currently render blank. Theme propagation, VS Code hash/query project deep links, and the pre-built `builderforce-embedded/dist` are shipped; see DONE.md.
- **Governance / security automation remains incomplete.** The schema, policy packs/gates, CRUD, and effective-gate routes are shipped. Remaining: automated scanners and repo-access population; a common immutable mutation-audit envelope; governance-auditor runs/findings; automated evidence; merge gating; per-segment DSR coexistence; and linking Product/Agile trackers to the work-item spine. Reconcile PRD 08's still-Planned checklist with this shipped foundation before scheduling the residual phases.

---

## 🛠️ Platform — data model, shell, i18n, VS Code, CI/CD & cost

- **Two frontend ratchets are RED on the working tree, and neither was made red by the work that found them** *(found 2026-08-22 while landing the points surfaces; verified by grepping the failure output — no `components/points/*` file appears in either)*. `check:architecture` reports `'use client'` files at 842 against a baseline of 808, plus three new >800-line production files (`components/board/BoardConfigPanel.tsx`, `components/ProjectDetailsPanel.tsx`, `lib/freelancerApi.ts`) and one presentation→infrastructure violation (`components/repos/githubActionsSurface.tsx`). `check:design-scale` reports `offScaleFontSizes` at 3799 against 3767, concentrated in `components/settings/MyLlmsContent.tsx` (+16), `components/qa/QaQualitySections.tsx` (+13), `components/security/PasskeysPanel.tsx` (+13) and eight others. All of those files are from earlier in-flight work sitting uncommitted in this tree. **This matters more than the numbers do:** a ratchet that is already red cannot report the NEXT regression, which is exactly the "one red guard hides the rest" failure the build-guard convention exists to prevent — every pass from here lands blind to its own drift. **Not blocked.** Closing it is two mechanical passes (name the type role on ~32 declarations; extract the three oversized components) plus a decision on whether the `'use client'` baseline should absorb the six genuinely-new points cards, which are interactive and self-gating and therefore cannot be server components. Unblocks: a frontend build that fails on new drift instead of on old drift.

- **Three of the source product's five points-fraud heuristics read signals this platform does not collect** *(points economy, 2026-08-22 — a deliberate scope decision, recorded rather than silently dropped)*. `application/points/fraudSignals.ts` ships two rules (bulk-close burst, sustained closure spike) against hired.video's five. The three omitted are **sybil signup-IP clustering** (needs a `signup_ip` on the account, which `users` does not carry), **device-fingerprint clustering** (needs a device id from an extension or app, which this platform has no ingest for), and **refund-then-redeem** (needs refunds correlated to a first earning event, which means the Stripe refund path writing into the points ledger's view of the account). Shipping them as dead code would have read as coverage while never firing once. Unblocks: detection of ring farming rather than only of single-account bursts — worth doing when redemption volume makes it worth doing, and not before.

> Domain `platform` (seat: Platform). The floor everything else stands on: the consolidated data model, the frontend shell and its localization, the VS Code surface, the deploy pipeline, the guard ratchets, and the infrastructure bill.

### PRD 20 · Consolidated data model — what is still open

> **Re-measured 2026-08-18 by running the guards, not by reading the last entry.** Steps 1, 2, 3 and the
> kernel halves of 6 and 7 shipped ([DONE.md](./DONE.md)). The target schema is **362 / 362 written
> (100%)**, all **24 api guards are green**, the kernel API and the domain surfaces are live (the manifest now declares **17** domains — `operations` and `legal` became seats after this note was written).
> Everything below is a **data or behaviour move against a running system**, not a schema decision —
> exactly the boundary PRD 20 §5 draws between steps 2–3 and steps 4–5.

- **Six data-model ratchets hold 362 open violations to pay down.** Each guard passes on a shrink-only
  baseline in `api/scripts/.<name>-baseline.txt`, so green means "no worse", not "clean". Open balances:
  **187** consolidated tables reachable only through the generic entity layer (`check-table-adoption`) ·
  **71** tables with no tenant-scoping column (global catalogues are fine here; customer-data tables are
  not, and are invisible to `check-tenant-scope.mjs` by construction) ·
  **65** table names matching a kernel shape, each needing a verdict — legitimate noun, or duplication to
  migrate · **28** cross-module schema imports ·
  **8** duplicate-shape clusters (`drive_connections`=`mailbox_connections` · `portfolios`=`initiatives` ·
  `tenant_custom_roles`=`platform_modules` · `tool_runs`=`marketing_tool_runs` ·
  `tenant_manager_defaults`=`project_manager_configs` · `tenant_skill_assignments`=`agent_host_skill_assignments` ·
  `import_type_mappings`=`board_type_mappings` · `kanban_template_lane_requirements`=`swimlane_requirements`) ·
  **3** polymorphic `(kind, id)` references with nothing to point at (`activity_log`,
  `creation_session_objects`, `proposal_evaluations` — clears when the `objects` registry lands).
  A decided entry is no longer counted here: verdicts live in `api/scripts/adjudications/<guard>.mjs`.
  **Blocked on an operator decision:** which of the 25 kernel primitives are accepted (PRD 20 §6).
  That gates the 8 clusters and **21** of the 65 shape-lint entries — the ones shaped `annotation` (8),
  `run` (8) and `revision` (5), whose merge targets have zero feature consumers today (`runs` and
  `work_items` are reachable only from `domains/kernel/entities.ts`). The remaining **44** shape-lint
  verdicts and the 28 import edges need no decision and can start now.
  Unblocks: `npm test` becomes a real gate on the data model instead of a shrinking allowance.

- **`chat_messages` and `brain_chat_messages` are two message tables neither guard catches.**
  `check-signature-duplication` scores them below its 0.55 threshold (one carries tenant/segment/
  agent_host/session, the other chat/event_key); strip the parentage and the payload is identical —
  `role`, `content`, `metadata`, `seq`, `created_at`. **Blocked on the same §6 decision:** the merge
  target is the kernel `messages` primitive, whose only feature consumer is `messaging/kernelThreads.ts`.

- **The 1,206 → 387 consolidation is specified, mapped and measured; the only thing left in it is an
  operator decision.** *(analysis 2026-08-07, spec current)* The full four-pass analysis — the 25
  primitives, the IDF-weighted signature method and its stated ceiling, the arithmetic corrections, the
  BCNF-not-4NF normalisation argument and the six CI checks — is **published in
  [PRD 20](./specs/builderforce/20-prd-consolidated-data-model.md)** with the coverage map committed as
  [`specs/builderforce/data-model/source-to-target.tsv`](./specs/builderforce/data-model/source-to-target.tsv).
  `check-model-coverage.mjs` re-verifies it on every run: **1,130 distinct source tables, 0 unaccounted**
  (567 absorbed by a kernel primitive · 168 merged into a sibling · 24 into the canvas · 9 flattened ·
  **362 distinct `keep` targets**, reconciling row-for-row with the domain roster; 25 kernel + 362 = 387).
  *The analysis does not need re-stating in this file, and the previous ~1,400-word retelling of it has
  been removed — the spec and the guard are the source of truth.*
  **Blocked on an operator decision**, unchanged: which of the 25 primitives are accepted, and which
  product wins each of the nine contested areas. Each primitive still needs one engineer with the domain
  in their head before its migration is written. Unblocks: a merge that does not triple the schema, and
  the standing rule ("a feature may add domain tables, it may not add another instance of an existing
  shape") that stops the count climbing again.

- **213 of the 323 created consolidated tables are reachable ONLY through the generic entity layer.**
  *(`check-table-adoption.mjs`, re-measured 2026-08-18; baseline retrimmed this pass — `saved_searches`
  and `site_subscriptions` had been migrated onto feature paths and were still listed)* The meter reports
  two tiers, because one number was misleading: **323 created · 323 registered via `entities.ts` · 110
  reached by a feature path (101 import, 26 raw SQL) · 213 registry-only · 0 unreachable.** Registration
  is a hard floor (0 unreachable) and the 213 is shrink-only. **Not blocked** — it closes as steps 6–7
  migrate features across, and it is the meter for them.

- **The entity layer covers the tables; the PRODUCT BEHAVIOUR of the two merged products does not exist
  here yet.** Every consolidated table has a typed, tenant-scoped, cached read/write path
  (`api/src/application/domains/`) and a surface (`EntityBrowser` on every seat), which is what the
  adoption meter measures. What that does NOT give is hired.video's product behaviour or the selected
  BurnRateOS Idea→Real workflows on top of those rows: the ATS funnel screens and the production-backed
  finance, validation, investor, growth and delivery transformations remain per-feature work. BurnRateOS's
  other 404 models and 262 pages are explicitly not backlog. The generic surface is the floor, not the
  ceiling, but a registry-only count is not a reason to invent a workflow or a table. **Not blocked** —
  PRD 18 remains a port; PRD 19 requires production-use evidence before a BurnRateOS capability is
  extracted into its existing owner.

- **`marketing_sessions` is mapped to `metric_fact`, and it cannot be** *(found 2026-08-08; re-verified
  2026-08-18 — `source-to-target.tsv` line 219 still files `BF marketing_sessions` under Growth &
  marketing with move `primitive` → `metric_fact`)*. Two things make that target impossible rather than
  merely awkward: `metric_facts` declares `tenant_id NOT NULL`, and a marketing session is written
  **before an account exists**, so there is no tenant to scope it to; and a lead row is an *entity*
  (opaque visitor id, first-touch attribution, a conversion pointer to `users`, a per-UTC-day allowance
  counter that is authoritative rather than derived), while `metric_facts` is by its own docstring "a
  derived number that was given its own DDL". What *can* legitimately become `metric_facts` is the daily
  aggregate over these rows, not the rows. The same argument applies to `marketing_session_prompts`
  (0434), which hangs off the same pre-tenant key. **Blocked on an operator decision**, and named as
  such: correcting the row means changing `move` to `keep`, which takes the distinct-`keep` count from
  **362 → 363** and requires the matching bump to `EXPECTED.keeps` in `scripts/check-model-coverage.mjs`
  — i.e. it edits the headline number PRD 20 §3 rests on. Unblocks: a coverage map that survives its own
  guard when the growth family is migrated, instead of failing the moment somebody executes this row.

- **`marketing_sessions` lives in Identity's schema module but belongs to Growth & marketing**
  *(re-verified 2026-08-18: the `pgTable` is at `schema/identity.ts:1066`)*. `marketing_session_prompts`
  (0434) was placed in `growth.ts` — its correct module — so the two halves of one concept are split
  across modules, joined on `visitor_id` rather than by a foreign key (which is why the split costs no
  schema import in either direction and no `check-domain-boundary` edge). Moving the lead row to
  `growth.ts` is what closes it, and the cost is real: the row references `users` for
  `converted_user_id`, so the move creates a **counted `growth → identity` edge**. That makes it a PRD 20
  §5 step 2 module move, not something a feature should smuggle in. **Not blocked** — it needs the
  `users` reference routed through the kernel `object` registry, or the edge accepted deliberately.
  Unblocks: "one table, one domain" holding for the growth family.

- **Step 5 — the 95 legacy tables `check-shape-lint.mjs` still names hold LIVE rows.** The kernel
  primitives exist and are empty; the per-feature comment/member/share/event/balance tables are still the
  ones being written to. Each family needs a backfill + a read cutover + a drop, in the order §5 sets out
  (tokens/share-links/invitations/revisions first — small and self-contained — then the 59-table ledger,
  then events and connectors following the 0295 and 0410 precedent). **Blocked on nothing but the data
  move**: it cannot be done in the same pass as writing the target schema, because every one of those
  tables is read by live traffic and each cutover needs its own verification. Unblocks: shape-lint
  95 → 0, and the eight signature-duplication clusters, five of which are legacy pairs that only
  disappear when one side is dropped.

- **Steps 6 and 7 are moving the WRONG WAY, and that is the finding.** *(re-counted 2026-08-18)* The exit
  criteria are unchanged — application folders → 16, route files → 16, `page.tsx` → 15 surfaces + the
  canvas, layering baseline → 0. The counts are: **129 application folders** (was recorded 101),
  **237 route files** (was 197), **166 `page.tsx` routes** (was 134). Only the layering ratchet improved,
  to **126** presentation-imports-infrastructure files (the entry claiming it was "frozen at 144, not
  falling" is false; it fell by 18). New features are still landing on the old boundary faster than the
  migrations move them off it, which is what the numbers say and what the exit criteria will keep saying
  until a lane owns the migration rather than each feature deciding for itself.

- **The domain-specific metric keys still have no rollup — 14 of 17 domains produce nothing.**
  *(re-counted 2026-08-19: `DOMAIN_MANIFEST` in `api/src/application/kernel/DomainService.ts` declares
  **45 keys across 17 domains**, not the 40 across 15 recorded here before; `operations` and `legal`
  were added as seats since.)* The registry projection writes `<domain>.items` and `<domain>.events` for
  every seat (see DONE.md), so no surface ships an empty Trends panel, and **three domains now write real
  numbers** — `kernel/rollups/{finance,operations,legal}.ts` all insert into
  `metric_facts`. The other fourteen do not, as do the five derived-number rollups §3.1 names
  (`channel_performance`, `site_traffic_daily`, `arr_projections`, `quota_attainment`,
  `rd_financials_quarterly`). Each is a scheduled aggregate over a table the Step 4/5 data move has not
  populated yet, which is why it follows rather than blocks. **This is the open half of Measure** — a
  proof's own kill condition already grades itself via `realization/realizationVerdict.ts`; the number a
  *seat* manages by does not. Unblocks: each seat charting its own metric.

- **Registration is write-driven for the entity layer and sweep-driven for everything else.** A row
  created through `/api/<scope>/entities/...` registers into `objects` in the same call, so it is
  addressable immediately. `projectRegistry()` still converges rows written by the legacy routes, which do
  not call `registerObject()` — a ticket created by `taskRoutes` at 10:00 is addressable at the next
  tick. Closes per family as the Step 5/6 cutovers move those routes onto the entity layer.

- **`domain-boundary` is at 34, not 0.** *(guard re-run 2026-08-22 after the root-table moves; the entry said 35,
  and 38 before that)* The move that closed the root-table entry took `growth → platform` with it — see
  [DONE.md](./DONE.md). The 34 that remain are genuine cross-domain reads (`delivery → agents`,
  `growth → canvas`, `platform → finance`) that need a kernel route or a named view rather than a direct
  table import. Each is a small refactor; together they are the last of §3's holding rule.
- **54 table-name families straddle two or more schema modules, and no guard counts them.**
  *(measured 2026-08-22 while closing the root-table entry)* `check-domain-root-table.mjs` now pins the
  seventeen ROOT tables and `check-domain-boundary.mjs` counts the import edges, but neither notices that
  `agent_*` spans `agents.ts` + `delivery.ts` (23 tables), `marketing_*` spans `agents.ts` + `growth.ts`
  (17), or `tenant_*` spans three modules (13). That is the general form of the six defects just fixed.
  **Deliberately not guarded rather than blocked**: the only mechanical test is a shared leading token, and
  it reports `saved_*`, `on_*`, `data_*`, `ai_*` and `report_*` as families when they are unrelated tables
  that share a word — a 54-entry baseline of mostly noise is how a guard gets `|| true`’d into decoration,
  which is the argument `lib/ratchet.mjs` already makes. It needs either a real ownership manifest per
  family or a per-family decision, not a heuristic. Unblocks: the same "review a seat on its own" property
  the root-table guard now protects, extended past the root to the family beneath it.

### ☁️ Self-hosted backends (AWS / Google Cloud / Azure) — residuals

> The three cloud hosting adapters shipped 2026-08-15 alongside the realization layer: one shared generated engine (`handlerEngineSource.ts`) embedded byte-identically by all four self-hosted targets, plus per-cloud entrypoints, IaC and deploy workflows (see [DONE.md](./DONE.md)). These are the parts that pass structurally and have not been exercised against a live account.

- **No generated cloud deploy has ever run end to end against a real AWS, Google Cloud or Azure account.** *(identified 2026-08-15 while building the adapters)* `selfHosted.test.ts` asserts the generated artefacts structurally — the SAM template exposes a Function URL with `AuthType: NONE`, the Cloud Run workflow federates rather than storing a service-account key, `host.json` empties Azure's `/api` route prefix — but a template that parses is not a stack that deploys. The failure modes that remain are exactly the ones a unit test cannot see: an IAM policy narrower than `sam deploy` needs, a Cloud Build API not enabled on a fresh project, an Azure Function App on a Windows plan where the Node v4 model does not load. **Blocker: three cloud accounts and their credentials, which are not available in this environment.** Until one full run per cloud is recorded, the honest description of these three strategies is "generated and structurally verified", not "proven". Unblocks: telling a customer with a data-residency clause that their system runs in their account, and meaning it.

### VS Code extension

- **In-editor voice cloning needs one frontend deploy before its runtime resolves** *(canvas parity pass 2026-08-06)* — the editor canvas is the web canvas, so it inherits on-device voice cloning, but the ONNX runtime it needs (~21 MB of `.wasm`) is deliberately NOT in the VSIX: `vite.canvas.config.ts`'s `bf-drop-wasm` plugin discards it and `CanvasHost.wasmBaseUrl` points the runtime at `https://builderforce.ai/ort/` instead, fetched on first use (`configureOnnxRuntime` in `voiceEngine.ts`). The serving half is code-complete — `frontend/scripts/copy-ort-runtime.mjs` runs on `prebuild`, publishes both variants into `public/ort/`, and fails the build if a file exceeds Cloudflare's 25 MiB asset cap — but **those files only exist on the CDN after the next frontend `cf-deploy`**, so until then a voice-clone enrolment in the editor 404s on the runtime and falls back to the metered server path (the documented degrade, not a crash). **BLOCKED on a deploy, nothing else.** Verify after deploy with `curl -I https://builderforce.ai/ort/ort-wasm-simd-threaded.jsep.wasm` (expect 200 + `content-type: application/wasm`). Also note `onnxruntime-web` is now pinned to `1.22.0-dev.20250409-89f8206ba4` in BOTH `frontend` and `clients/vscode` — the exact build `@huggingface/transformers@3.8.1` depends on — because 1.26/1.27's `jsep` variant is 25.02/25.58 MiB and cannot be served as a Cloudflare static asset at all; unpinning re-breaks the hosting.

- **"Never leave VS Code" embed** — extension opens `/embed/<view>` webview panels (auth bridged; `vscode-webview:` trusted in `embedTrust.ts` + CSP). Residuals: needs frontend `cf-deploy`; views self-gate on tenant embed capabilities. Native pages already pivoted to bundled-React webview screens; residual: a future in-editor realtime session screen (Retros/Poker are read-only lists). `Sunburst` geometry untested (brain-ui has no test runner).
- **Native chat / sessions.** `@builderforce` participant only surfaces inside GitHub Copilot Chat; a dedicated sessions tab needs the **proposed** `chatSessionsProvider` API (Insiders-only). Decision: keep the webview sidebar (recommended, works everywhere) vs a sideload-only proposed-API build. Sessions persist transcript only (no per-session model/tool history); no Local/Web toggle. Agent tool-calling assumes OpenAI-style `tools`. Composer dictation depends on the runtime exposing `SpeechRecognition` (absent in many Electron builds). Effort/Thinking/"Browse the web" toggles ride prompt directives. `read_file` can't page BEYOND a provider's byte cap.
- **The extension-host suite covers activation, commands, views and the webview bridge — but NOT the auth/device-code flow** *(residual after the host-test pass 2026-08-19)*. `clients/vscode/test-integration/` now launches a real VS Code and gates the release publish on it (see DONE.md), which closed the activation gap it immediately exposed. What it still cannot exercise is sign-in: `BuilderForceAuthProvider` opens `<web app>/activate`, waits for a `bfk_…` editor key and stores it in the OS keychain, so a test of it needs a live gateway, a real tenant and a browser round-trip. **Blocker: credentials — a dedicated test tenant plus a `BF_EDITOR_KEY` secret the release workflow can read**; every other half (the key→JWT exchange, the SecretStorage write, the `builderforce.signedIn` context key) is assertable once that exists. Until then a broken sign-in still reaches a user before it reaches CI. Unblocks: the last surface that can only be validated by installing a `.vsix` and clicking.

### 🗂️ VSIX Sessions multi-tab — follow-ups

> Built per [PRD-vsix-session-tabs.md](./specs/builderforce/PRD-vsix-session-tabs.md).

- **Proposed chat-sessions API convergence.** `clients/vscode/src/chatSessions.ts` offers a native per-tab chat surface under `--enable-proposed-api` (`chatSessionsProvider`), parallel to the per-session tabs now shipped on the stable `BrainWebview` path. If VS Code promotes that API to stable, the two per-tab implementations should converge onto one. Fixing it unblocks retiring one of the two code paths.

### Frontend architecture & bundle

- **The frontend is a client-side SPA wearing an App Router — 66 of 166 pages are (re-measured 2026-08-18) `'use client'`** *(PRD 22 re-audit 2026-08-08)*. 764 of 1,228 files under `frontend/src` open with `'use client'` (counted by first-line directive; strict and loose counts are identical, so no comment inflates it — the ratchet moved 755 → 764 on 2026-08-09, six of those the product-update/beta surfaces, which are genuinely interactive), including 68 of 137 `app/**/page.tsx` route entries, and `app/layout.tsx:170-197` wraps every route in nine nested client context providers (`Auth`, `Cart`, `Emulation`, `RolePreview`, `PermissionDebugger`, `Confirm`, `Toast`, `DemoMode`, `Locale`). A client-rooted page forces its whole import subtree into the **client bundle** regardless of interactivity — this is the *mechanism* behind PRD 22 §3.1's eager `CanvasStage` import, not an isolated mistake, and dynamic-importing `CanvasStage` moves no bytes for the other 67 client pages. **Precisely: this does NOT delay first paint** — Next server-renders client components and `<Suspense>` still streams inside a client tree; the costs are transferred/parsed JS, hydration (TTI/INP), re-render breadth from nine root providers, and the inability to `await` data during render (which is what couples this to the caching entry above). Fix = classify the 68 client pages (genuinely interactive at root vs client-by-accident), push `'use client'` to leaves, split route-specific providers out of the root layout, and ratchet both counts downward; must be validated per-route against the post-hydration `LocaleProvider` locale swap. Specified as PRD 22 §3.14 / §6.1 / H-18. Unblocks: real bundle reduction rather than symptom-level splitting.
- **COMPONENT-level code splitting is rare — `next/dynamic` in 9 files, zero `React.lazy`** *(PRD 22 re-audit 2026-08-08)*. Note the qualifier: **library-level splitting is already established and is not a gap** — Mermaid (`MermaidDiagram.tsx:48`), xterm + 2 addons (`Terminal.tsx:22-24`), WebContainer (`useWebContainer.ts:70`, `browserRuntime/factory.ts:85`, both `webcontainer/connect` pages), onnxruntime-web (`voiceEngine.ts:54`) and y-monaco (`CodeEditor.tsx:75`) are all dynamically imported, 57 `await import()` sites in total. The gap is that runtime `import()` inside an already-loaded client module defers a *library*, whereas `next/dynamic`/`React.lazy` defers a *component subtree* including everything it statically imports — which is what PRD 22 §3.1 requires, and that exists in only 4 files (`app/ide/[id]/page.tsx`, `CodeEditor.tsx`, `agent/FileChangeViewer.tsx`, `creation-canvas/CanvasBuildPanel.tsx`) with zero `React.lazy`. Fix = scope PRD 22 Phase 1 as *introduction of the component-level pattern* with a per-route owner list (training, 3D, voice, game, Studio panels), sequenced after the RSC conversion so the gain is attributable. Unblocks: correctly-sized Phase 1 estimates.
- **A catalog key was written as a per-locale MAP inside every catalog, and next-intl cannot resolve it** *(found 2026-08-20 running `messages.test.ts`)*. `widgets.title.autoVerdictMissRate` and `widgets.title.autoVerdictByAgent` were each stored as `{en, zh, es, fr, de}` — one object, copied identically into all five files — so `t()` returned an object where a string was expected and both widget titles rendered as the raw key in every language. It is the ad-hoc per-locale map the standing rule names explicitly, and it survived because nothing type-checks a catalog leaf. Flattened this pass and the suite is green; what is NOT closed is the guard: `messages.test.ts` catches a MISSING key and a key that merely copies English, and had no assertion for a leaf that is not a string at all. Fix = assert every leaf's type when walking the catalogs. Unblocks: the next one of these failing at authoring time rather than in front of a user.

### Frontend / Admin

- **P3 — Demo-tenant agents may surface in the PUBLIC marketplace/workforce registry** *(demo-accounts pass 2026-07-24)*. The admin "paid pro" rollups no longer count `is_demo` tenants (fixed 2026-07-24 — see DONE.md), but a separate question remains: the Talent persona demo tenant publishes `coder`/`copywriter` agents to the public marketplace. Confirm that demo-tenant published agents are *intended* to appear in the public workforce registry — if not, exclude `is_demo`-tenant agents from the public marketplace read. Product decision + a marketplace-read filter. Unblocks: a public registry free of demo fixtures.
- **P3 — Onboarding no longer captures a builder's "intent" (scope-cut).** Zero-setup onboarding auto-provisions a "Default" workspace + "Default" project and removed the manual workspace/project steps from `OnboardingStepper.tsx`; the build/custom-agent/monetize/automate/learn intent multi-select lived in the old project step, so `users.userIntent` is no longer recorded for new builders (`completeOnboarding` is now called without intent). Fixing it (re-add intent as an optional lightweight first step, or an in-app prompt) restores intent-based personalization/analytics. The stale `onboarding.intent.*` + `steps.workspace/project.*` i18n keys are now unused.
- **P4 — Demo product-tour events aren't surfaced as funnel columns** *(demo-tour pass 2026-07-24)*. `DemoTour` emits `tour_started`/`tour_step`/`tour_completed`/`tour_skipped` to `demo_events` and they're captured + visible in the admin Demo Funnel panel's "recent events" list, but the funnel matrix renders a fixed `STAGES` array that doesn't include the tour kinds, so tour engagement/completion isn't a first-class funnel column. Fix = add the tour stages (or a small "tour completion rate" tile) to `DemoFunnelPanel` so you can see which persona's tour actually gets finished. Low priority — the data is already recorded, this is a visualization enhancement only.
- **P3 — Auth-page marketing copy sourced from `content.ts` is English-only.** The Register/Login form bodies are localized, but their marketing panels still pull static English data from `frontend/src/lib/content.ts` (the login intro sentence, `LOGIN_FAQ`, every `REGISTER_MARKETING` variant). This is a shared 1591-line marketing data module consumed across the whole public site. Fix = migrate `content.ts` marketing strings to next-intl catalogs (or a per-locale content map).
- **P3 — Team chats can't be renamed/archived through the owner-admin path.** Team Chat (mig 0294) reuses the Brain chat stack but `verifyChatOwnership` still filters `origin='brainstorm'`, so a PATCH/DELETE on an `origin='team'` chat returns "Chat not found". Intentional today, but if a surface ever exposes rename/archive on the active team chat it will 404 silently. Fix = either gate those controls off for `isTeamChat`, or add a team-chat-aware admin path.
- **P3 — No sweep for pre-existing fake/unverified accounts.** Migration 0285 backfills `email_verified_at = created_at` for EVERY existing account, so fake accounts created before the OTP gate remain `verified`. The gate only stops NEW fake signups. Fix (optional ops task) = a one-time admin sweep to flag never-signed-in / disposable-domain accounts.
- **P3 — Ceremony drag-and-drop is non-functional on touch (mobile is view-only).** The stage's assign/group/schedule/return-to-backlog interactions all rely on HTML5 drag-and-drop, which does not fire on touch devices. The mobile-responsive pass makes the stage READABLE on a phone but a mobile user still can't assign work by dragging. Fix = add a tap-to-assign fallback or adopt a pointer-based DnD lib.

### 🔐 Login "CORS error for everyone" — investigate the Cloudflare-level trigger

> Reported 2026-07-09: `net::ERR_FAILED` + "No 'Access-Control-Allow-Origin' header" on **every** endpoint. From outside the worker is healthy (curl gets the CORS header), so this is NOT a worker CORS-config bug — a headerless response reaching the browser means a **Cloudflare-level failure** (Worker exception 1101, CPU/mem 1102, or a WAF/Bot-Fight/rate-limit challenge). Mitigated (api 2026.7.67): the top-level `fetch` handler now wraps `buildApp(env).fetch(...)` in try/catch and returns a CORS'd JSON 500.

- **Still open — find the actual trigger:** (a) check Cloudflare dashboard → Workers logs / Logpush for `1101/1102` exceptions around the incident window; (b) check `api_error_log` table + the Product Quality pillar; (c) confirm no Bot Fight Mode / WAF / rate-limit rule is challenging browser traffic while letting datacenter curl through; (d) rule out a transient Neon connection-construction failure in `buildDatabase`/`buildApp` under cold-start pressure.

### 🌍 i18n / localization

- Only ~a handful of surfaces are migrated (`/settings` is the reference); ~330 components still hold hardcoded English. Biggest un-migrated: marketing copy in `content.ts` + `/evermind` + `PricingPageClient` + blog markdown; `QaContent.tsx`; `AgentPublishPanel`; `TaskMgmtContent`; `dashboard/page.tsx` + `ProjectsContent`; `VoiceClonePanel`; whole Sidebar/nav chrome; `OnboardingStepper`; `CloudAgentFormFields`; `IntegrationCredentialsManager`/`BoardConnectionsManager`. VS Code runtime l10n bundles (de/es/fr/zh) for `projectsTree`/page labels. No CJK font coverage (add a `system-ui` CJK fallback stack); public/marketing pages render dynamically (split a `/[locale]/` segment for the ~7 public pages if SEO/cache matters); client payload not namespace-scoped; no machine-translation pipeline.
- **Standing rule:** any page/component ADDED or MODIFIED must be localized (all 5 catalogs en/zh/es/fr/de), dark+light, and mobile-friendly in the same pass — see [[i18n-localization]], [[theme-and-responsive-ui]].
- **The message catalogs are partitioned by LOCALE, which is a presentation axis, so no bounded
  context owns its own ubiquitous language.** *(registered 2026-08-15; **diagnosis corrected the same
  day** — the first version of this entry called five files "monolithic" and proposed
  `messages/<locale>/<namespace>.json`. That is the same wrong axis sharded finer: it would have fixed
  the merge conflict and left the ownership defect in place. Withdrawn.)* `frontend/src/i18n/messages/{en,zh,es,fr,de}.json`
  is five files of ~892 KB holding **243 top-level namespaces**, so the FIRST partition of the
  translation store is *language*. Language is not a domain boundary — it is a rendering concern, like
  theme — and partitioning by it first is why the architecture rule's own words do not hold here:
  "ubiquitous language shared by schema/API/UI" means the words a context uses **are part of that
  context**, and today sixteen domains' vocabulary sits mixed in one bucket keyed by locale with no
  owner per key. The merge conflict every parallel agent hits is the *symptom*; the missing ownership is
  the defect, and the symptom is why it went unnoticed.
  **The runtime is not the reason, and that is what settles it.** `frontend/scripts/publish-message-catalogs.mjs`
  (prebuild) already emits a per-locale asset into `public/i18n/` and `i18n/catalog.ts` already fetches
  exactly the one a request needs — so the per-locale bundle is **already a projection**, built from
  source, precisely because importing 4.7 MB broke the 4 MB Edge ceiling ([[i18n-catalogs-are-assets]]).
  A projection is shaped for its reader; the SOURCE must be shaped for its owner. The two were conflated.
  Fix = partition the source by bounded context, one directory per domain, **isomorphic to
  `api/src/infrastructure/database/schema/<domain>.ts`** — `messages/<domain>/{en,zh,es,fr,de}.json`,
  ~16 directories rather than 243 loose keys — with the existing namespaces re-keyed under their owning
  domain (`commerce.stage.*`, `growth.site.*`, `canvas.app.*`), the publish script folding the domain
  files into the same per-locale asset it emits today (**no runtime change, no new fetch, no second
  answer to "where are the messages"**), and `messages.test.ts` walking directories instead of importing
  five files. Namespaces whose owner is genuinely contested go to `common/` under a **shrink-only
  ratchet** — the instrument `.table-adoption-baseline.txt` already uses — never a bucket that quietly
  stops shrinking. **Not blocked** — it is a single mechanical rewrite of 4.7 MB of JSON plus a publish-script
  and test change, and it must land as one commit because a half-partitioned store has two answers to
  "where are the messages". Unblocks: one owner per vocabulary, and a domain's words shipping with the
  domain that speaks them.

### Frontend guard ratchets — the balances still to pay down

> Every number below was re-measured by running the guard on 2026-08-22, not carried forward. Three of
> the seven items this subsection once held are closed and live in [DONE.md](./DONE.md) —
> `check:canvas-tools`, the five `frontend tsgo` errors, and the design-scale colour and radius legs.
> What is left is shrink-only debt with a named owner per file: the ratchets hold the line, and the
> balance only moves when somebody touching a surface moves it.

- **`check:design-scale` has one leg over its baseline: `offScaleFontSizes` is 3,807 against 3,767 (+40).** *(re-measured 2026-08-22)* The colour and radius legs are clean — the one literal hex the previous reading recorded (`components/creation-canvas/CanvasFacilitateSurface.module.css`, `var(--surface, #fff)`) is now a bare token, so `literalHexFiles` is back to 0. What is left is the type ramp, and it is a different KIND of work: 40 off-scale sizes (down from 59 as the surfaces that landed with the last commit are worked through), worst offenders `components/settings/MyLlmsContent.tsx` (16), `components/qa/QaQualitySections.tsx` (13) and `components/security/PasskeysPanel.tsx` (13). Fix = move each size onto a role in the ramp, per file — a visual change on surfaces somebody has to look at, not a codemod. **Not blocked.** Unblocks: `frontend npm run check` going green, which today hides whatever lands next.
- **`check:architecture` is over its baseline on four counts.** *(re-measured 2026-08-22)* `'use client'` files are **833 against a baseline of 808** (+25 — `CareerAiClient`, `app/disputes`, `app/freelancer/{disputes,earnings}`, `IncidentTopologySection`, `WhyLadderSection`, `DeepLinkPickerClient`, `LtiLaunchClient`, `PublishGigClient`, `PromptUseActions`, `ReferencesClient`, `ShortlistClient`, `app/workforce/hire`, `SuspectAccountsPanel`, `TenantPremiumCapEditor`, `BlogResumeTemplates`, `BuildStatusBadge` among them); one new `presentation → infrastructure` import (`components/repos/githubActionsSurface.tsx`); and three new files over 800 lines (`components/board/BoardConfigPanel.tsx`, `components/ProjectDetailsPanel.tsx`, `lib/freelancerApi.ts`). All arrived with the 2026-08-20 commit. **Not blocked, but it is a JUDGEMENT rather than a bump**: PRD 22's whole case (the frontend-architecture entries above) is that a client-rooted page drags its import subtree into the client bundle, so raising the baseline by 25 concedes the very thing the ratchet exists to measure. Each of the 25 needs classifying as genuinely-interactive-at-root or client-by-accident before the number moves in either direction.
- **A whole feature is checked in at a path that can never build: `Builderforce.ai/Builderforce.ai/frontend/src/dashboard/cross-project-health/`** (three files, 2026-07-28, ~20 KB) — a nested duplicate of the repo folder name, so it sits outside the real frontend's `tsconfig` include and outside every import graph. Nothing in `frontend/src` references `CrossProjectHealthDashboard`; its design doc `CROSS-PROJECT-HEALTH-DASHBOARD.md` sits at the repo root pointing at it. So the dashboard has been written, documented and unreachable for three weeks. Fix = decide with its author whether to move it under `frontend/src/` and wire a route, or delete it and the doc together. *Not deleted here: it is somebody's unshipped feature, not stray build output, and the call is theirs.* **BLOCKED: needs the author's decision — move or drop.**

### 🛡️ Cloudflare serves a managed challenge to every datacenter caller of the whole `builderforce.ai` zone *(found 2026-08-08)*

- **Any request from an automated/datacenter IP gets a `403` + `cf-mitigated: challenge` ("Just a moment…" HTML) instead of reaching the worker, on EVERY hostname in the zone.** Measured from a GitHub-hosted runner (egress `68.154.118.243`, colo IAD) against `POST api.builderforce.ai/mcp`, `GET api.builderforce.ai/health`, `POST builderforce.ai/gateway/mcp`, `GET builderforce.ai/`, `GET www.builderforce.ai/` — all five returned `403 cf-mitigated: challenge`, steady across 5 consecutive attempts, unchanged by a browser `User-Agent` and identical over IPv4. The same requests answer correctly from a residential IP (`POST /mcp` → `401` + `WWW-Authenticate: Bearer realm="builderforce"`), so the worker is healthy and nothing in `api/src` produces this — the mitigation is at the edge, in front of the Worker. **This is not only a CI problem: the audience of the MCP Registry listing (Anthropic connectors, AWS, Gemini Enterprise, hosted agents) is exactly the class of caller being challenged, and `builderforce.ai/gateway/*` — documented in `api/wrangler.toml` as "the ONE whitelisted host all clients should target" — is challenged too.** The `publish-mcp-registry` release job correctly refuses to publish under this condition (`.github/workflows/release.yml`); the `Refresh demo accounts` curl in `deploy-api` would fail the same way once `DEMO_RESEED_SECRET` is set. **Since 2026-08-14 that job WARNS rather than failing the release on this specific `cf-mitigated` path** — it had been painting every push to main red since 2026-08-08 for a condition the workflow cannot clear, and a permanently-red release is how a genuine regression in `deploy-api`/`deploy-frontend` goes unnoticed. The publish is gated on the probe's `publish` output so "warn instead of fail" cannot degrade into "publish anyway", every run writes the remedy table to the job summary, and any OTHER unexpected status (404/500/timeout — broken rather than blocked) still fails hard. **BLOCKER: the fix is a Cloudflare zone setting, which needs dashboard/API access this repo does not carry (`CF_API_TOKEN` is a Workers-deploy token, not zone WAF).** Re-confirmed 2026-08-14 on a fresh release run: `POST api.builderforce.ai/mcp` still answers `401` from a residential IP and `403 cf-mitigated` from the runner, so nothing has drifted in the Worker and the condition is unchanged. **FOUR different settings emit this same header and 403, with different remedies, and the earlier "or add a WAF skip rule" phrasing was wrong for one of them:** Bot Fight Mode (free plan) runs outside the Ruleset Engine and accepts **no** exceptions — Cloudflare's docs state plainly that Skip/Bypass/Allow "have no effect" on it — so it can only be turned OFF; Super Bot Fight Mode (Pro+) *is* skippable, but only by a rule naming the `http_request_sbfm` phase, which a generic "skip remaining custom rules" rule does not do; Security Level `high`/`under_attack` challenges by IP reputation, which is what datacenter egress scores as; and Browser Integrity Check challenges anything whose headers aren't a browser, i.e. every correct API client. The last two are skippable as the `securityLevel` and `bic` products. Everything that can be done without the token now is: `scripts/cloudflare-bot-exception.mjs` reads which mode is on and applies the matching remedy under `--apply` (idempotent), and the `publish-mcp-registry` error names it. **To clear this, someone with the Cloudflare dashboard needs to either flip the toggle or mint a token scoped to the `builderforce.ai` zone with Zone Settings:Edit + WAF:Edit and run that script.** Unblocks: the MCP Registry listing, every server-to-server MCP/API client, and any CI step that touches the deployed API.

### 🎲 `api` full-suite vitest run is FLAKY — a different file fails each run

- **The full `pnpm --filter api test` run fails 1–2 tests, and it is a DIFFERENT file every time; each one passes in isolation.** Observed 2026-07-31 across three consecutive full runs of 4,283 tests: run 1 failed `manager/staffUnfilledLanes.test.ts` (3 assertions) + `manager/stallTriage.test.ts`; run 2 failed `infrastructure/database/orderByNullPlacement.test.ts`; the failing files then passed cleanly on their own (`staffUnfilledLanes` 33/33, the whole `manager` dir 422/422, `orderByNullPlacement` 2/2). The failures are not assertion drift — the same assertion passes seconds later — so a green isolated run proves nothing about CI, and a red CI run cannot currently be trusted to mean a real regression. Both suspects are cheap to check first: the run reports ~820s of *import* time against 108s wall-clock, so worker-pool contention is plausible; and several of the affected files read SOURCE off disk (`orderByNullPlacement` scans schema files, the manager tests build fixtures), which under that I/O load is the classic partial-read. Fix: run with `--pool=threads --poolOptions.threads.singleThread` and with `--sequence.shuffle=false` to see whether it is concurrency or ordering, then isolate whichever module-global the setup file is not resetting. Unblocks: trusting CI on this package at all. **BLOCKER: not reproducible on demand — it needs several full runs to catch, and the failing file is different each time, so there is no single case to bisect yet.** The cheapest next step is the one experiment that separates the two hypotheses: run the suite single-threaded and unshuffled, and compare against a normal run over the same tree.

### 💤 Neon compute cost — KV cron work-gate residuals

- **Phase 0 is a manual console action.** Autosuspend/scale-to-zero + the 0.25 CU minimum must be enabled in the Neon console for the gate to actually save money. **BLOCKED: needs the Neon console — it is a control-plane toggle, not code.** Everything on the code side of this section is now closed (stuck-schedule detection, the daily `db-vacuum` and weekly `db-reclaim` sweeps, and per-table autovacuum tuning — see [DONE.md](./DONE.md)), so this toggle is the only thing left between the gate and the saving it was built for: without autosuspend the endpoint never scales to zero and the idle ticks the gate skips buy nothing. Unblocks: the compute half of the Neon Free tier.

### CI/CD, migrations, schema drift & tech-debt

- **The IDE starter templates exist twice, guarded only by a test.** The scaffold the API seeds into R2 (`api/src/application/project/projectTemplate.ts`) and the one the frontend mounts as the Run fallback (`frontend/src/lib/vanillaDefaults.ts`) are byte-identical copies, because the Worker and the Next app share no linked package — the two drifted once (`webmobile` known to the frontend, absent from the API's registry) and shipped Web + Mobile projects with no files. `api/src/application/project/templateParity.test.ts` now fails the build on divergence, but the real fix is a workspace package linked into both. Deferred because wiring a new package through `wrangler` + `next-on-pages` risks the deploy path for what is currently a green test.

- **CI safety.** No ephemeral-DB `db:migrate` dry-run before deploy (needs a provisioned throwaway Neon/Postgres). Deployed Cloudflare Pages output can drift stale (post-build assert `_headers` carries COOP `same-origin` + COEP `credentialless`; service worker `/sw.js` intermittent fetch-fail needs prod repro). *Closed 2026-08-19 (see [DONE.md](./DONE.md)):* `deploy-api` and `deploy-frontend` are now bound to the `production` GitHub Environment, so a required-reviewer rule is a settings change rather than a workflow edit — **the protection rule itself is still unset and is an operator action**; the brittle `sed` worker-patch is replaced by `scripts/patch-worker-fetch.mjs`, which FAILS the build on a missing anchor (the `sed` exited 0 either way, so an unpatched worker could reach production unnoticed). *Stale, verified 2026-08-19:* `brain-embedded` already installs with pnpm — `release.yml` branches on the presence of `pnpm-lock.yaml`; there were no injected `console.log`s in the deploy path.
- **Migration numbering and the schema-drift allowlist are the residual, not application.** *(ledger queried 2026-08-22 — every migration is applied; the entry that listed twenty-odd "authored but not applied" files was stale and is recorded as resolved in [DONE.md](./DONE.md))*. What remains: historical migration-prefix collisions are grandfathered in `.migration-collisions-allowlist.txt` (0009/0014/0018/0060/0064/0068/0075/0076 + the 0245 pair), so two files can share a prefix and the ledger keys on the full filename — a coordinated renumber plus a ledger update is still owed, and until it lands the prefix is not an identifier. The ledger also holds **five rows whose file no longer exists** (`0109_execution_messages.sql`, `0111_agent_feedback_and_perf.sql`, `0114_workforce_teams.sql`, `0120_engine_v2_retire_v1.sql`, `0121_project_sites.sql`) — applied under names that were later reused for different migrations at the same prefix, which is the same collision seen from the database side. Separately, the schema-drift allowlist still grandfathers ~75 push-only tables/columns with no `CREATE TABLE` migration; backfill idempotent creates, then shrink the allowlist toward zero. Unblocks: a migration number that means one thing.
- **Lint / test debt.** ~72 React-hooks ergonomics warnings demoted to `warn` (re-enable one at a time + burn down `exhaustive-deps`); `eslint-config-next@16` is a major ahead of `next@15` (pin to 15.x or budget the `next` 16 upgrade); root `npx vitest run` has ~28 pre-existing failures; `requireTenantAccess` test uses a hand-rolled Drizzle chain mock; cloud/on-prem end-to-end telemetry emission + relay `persistToolAudit` remain integration-only. `TenantTokenLimitOverrideEditor`/`TenantPremiumOverrideEditor` share ~80% scaffold (extract a `modes[]`-driven primitive). `docker compose up` requires an external `NEON_DATABASE_URL` (add a local-neon-http-proxy behind `LOCAL_NEON=1`). README documents stale pre-rename artifacts (`/api/claws`, `ClawRelayDO`, `.coderClaw/`, `X-Claw-Signature`). *Stale, verified 2026-08-19:* the frontend vitest runner works (`messages.test.ts` runs green at 74/74 — it was RED on three unlabelled canvas object kinds, now fixed; see [DONE.md](./DONE.md)), and `__clearL1CacheForTests()` is exported and wired through `api/test/setup.ts` `setupFiles`.
- **Brand / distribution de-brand (release-level, not a copy sweep).** Schema + distribution `claw`/`coderclaw` occurrences are load-bearing: applied migration filenames/DDL (`coderclaw_instances`, `assigned_claw`, `claw_*` columns), live API/SDK field names, the published npm package `coderclaw` + CLI binary + `coderclaw.ai` domain + `CODERCLAW_*` env surface, and Cloudflare DO migration history (`ClawRelayDO` — never rename, orphans DO storage). Full de-brand = new rename migrations + coordinated column/field renames + an npm/binary/domain/env rename with aliasing + a deploy. API-key prefix `clk_`→`bfa_` dual-accept live; rotate legacy keys then drop the acceptance branches. Native apps in `apps/` are raw source: ~445 files carry `coderclaw` branding (Android `ai.coderclaw.android`, iOS/macos bundle ids) + no CI wiring; macOS `Package.swift` `.copy("Resources/CoderClaw.icns")` points at a missing file; `CoderClawKit` Swift-package rename needs an Xcode build.

### 🏛️ Architecture pass — residuals (opened 2026-07-26)

> The eight DDD/SOLID/DRY items shipped in full — see [DONE.md](./DONE.md). These are what that pass FOUND and deliberately did not fix.

- **The layering ratchet is at 126 files and IS falling** *(guard re-run 2026-08-19)*. `npm run check:layering` stops NEW `presentation → infrastructure` imports; **110** non-test presentation modules still import the schema directly and hold **286 selects / 108 inserts / 84 updates** inline in the route layer *(re-measured 2026-08-19 — the previous figures of 144 modules and 843/210/251 were stale by a wide margin; the direction is down)*. The baseline only shrinks when someone touching a route moves its queries into an application service. Worst offenders by count: `adminRoutes`, `agentHostRoutes`, `qaRoutes`, `knowledgeRoutes`. Unblocks: a domain layer that is load-bearing rather than an island — **541** files import the Drizzle schema directly while **44** import a domain entity class (up from 24, so the layer is being adopted rather than abandoned).
- **271 unscoped tenant queries remain baselined, across 107 files** *(2026-08-19, after the conversion pass).* Down from 524/168; the pre-0944 series was 522/152 → 505/149 → 410/135, then 524/168 when `tasks` gained a tenant column and 118 statements became visible for the first time. **What is left is NOT the same kind of work as what was just done.** The 253 that converted did so because a tenant id was already in scope; the transformation was mechanical and `tsgo` proved each one. The compiler REJECTED 219 more, and every rejection means the same thing: **that function does not know its tenant.** Those are the documented "safe via an owner-checked parent" population, and making them self-scoping is not an edit — it is threading a tenant id down a call chain, which changes signatures across a slice of the codebase per chain. So the remaining 271 need per-chain design decisions (thread `tenantId`, or accept the parent check and record why), not another codemod. Unblocks: retiring the baseline entirely, at which point the guard becomes a wall instead of a ratchet.
- **23 of 38 permissions are enforced; the remaining 15 are advisory for STRUCTURAL reasons, not for want of a middleware line** *(2026-08-19).* The delivery surfaces operators actually want overrides on are now real gates: `project:read/write/delete`, `task:read/write/delete/assign`, `workflow:read/write`, `member:read`, `report:read/export`. What is left divides into three kinds, and only the first is ordinary work. **(a) No surface exists** — `project:archive` has no archive route; there is no ad-hoc report-export endpoint beyond the scheduled deliveries now gated by `report:export`. Either build the route or drop the permission (dropping it must consider existing `user_permission_overrides` rows). **(b) The caller is not a tenant member** — `marketplace:read/purchase/publish` sit behind `requireMarketplaceAuth`, a separate identity with no `tenantId`/`role` on the request, and `requirePermission` reads exactly those; gating that router would 403 every marketplace user. Making these real means unifying the two identities first — a design change, not a gate. **(c) The caller is a machine** — `agentHost:*` spans a router where about half the endpoints authenticate with a host API key and establish no member session, `workflow:execute` is the claim/host-result pair on that seam, and `approval:read` shares the mix. A blanket gate takes the agent fleet offline; the tenant-JWT half of `workflowRoutes` is gated and the machine half deliberately is not. `system:*` needs the superadmin path audited the same way. The reasoning is recorded beside `ENFORCED_PERMISSIONS` so it is not re-derived, and `permissionEnforcement.test.ts` keeps the set and the gates in lockstep both ways.
