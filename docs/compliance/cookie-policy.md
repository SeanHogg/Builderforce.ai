# Cookie policy control evidence

The public policy is `/legal/cookies`. `CookieConsentManager.tsx` defaults optional tags off, presents Reject and Accept at the same interaction level, persists the choice locally, prevents the GTM request before opt-in, and allows reopening from every footer. Marketing/targeted-advertising consent remains false because BuilderForce does not enable that purpose.
