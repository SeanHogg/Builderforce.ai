# Blog capability gap register

Last code audit: 2026-08-05. Scope: all 59 files in `frontend/src/content/blog`.

The articles remain published. This register treats their product statements as acceptance criteria. A claim is **evidenced** only when a production path and a regression test exist; UI copy or a mock is not evidence.

## Implemented in this slice

- `webgpu-lora-explained`, `local-first-ai-webgpu-in-the-browser`: LoRA rank/alpha now drive a real frozen-base adapter; local-only mode performs no training API calls; output is PEFT-shaped safetensors; the API validates LoRA A/B tensors and bounds before R2 persistence. On capable devices the adapter's merged forward, exact A/B gradient projection, gradient accumulation, and AdamW update run in WGSL; base-model loss/autograd remains on the CPU and is disclosed as a hybrid path.
- `build-and-train-evermind-on-the-creation-canvas`, `brain-operates-the-creation-canvas`: an Evermind object's `train` action now opens the real adapter studio, links a completed workspace job/artifact to the object, and `evaluate` invokes the canonical evaluation endpoint instead of creating an unevaluated placeholder.
- `real-time-collaboration-humans-and-agents`, `video-meetings-standups-and-shared-calendars`: meetings now expose a direct-media-only control. That mode requests STUN-only ICE configuration and tests prove every TURN URL is removed; relay-fallback explicitly discloses that encrypted media may traverse TURN.
- `multiplayer-creation-canvas-web-vscode`, `vs-code-command-center-for-your-agentic-workforce`: the VS Code Canvas already uses the canonical object/command contract and durable Session endpoints; it now catches up from the same revisioned event log every two seconds so web edits arrive without manual refresh or a separate document format.
- `build-and-train-evermind-on-the-creation-canvas`, `inside-evermind-architecture`: browser training now packages the merged base, adapter, and matching tokenizer into a validated `.evermind`; Canvas can publish it as a tenant model and invoke the same live runtime used by the gateway. Publication requires held-out text, runs the immutable package through an automatic quality gate before registration, and removes rejected uploads. The studio can re-point a stable callable slug to a prior tenant-owned package and returns a reversal token.
- `creation-canvas-beyond-chat`, `forty-eight-live-objects-one-creation-canvas`: Image, Animation, Podcast, Comic, Game, CAD, 3D Model, Resume, and Template actions now generate, preview, and download real browser artifacts. Each action writes a transport-neutral deliverable manifest with MIME type, filename, provider, and validation evidence; formats without a renderer are not falsely reported as produced.
- `multiplayer-creation-canvas-web-vscode`, `creation-canvas-templates-frames-branches-checkpoints`: VS Code now exposes native checkpoints, restore-as-new-revision, branches, per-object reviewed merges, and modal destructive deletion over the canonical graph.
- `migrate-and-integrate-jira-monday-rally-gitlab-bitbucket`, `single-pane-board-connectors`: every committed migration stamps projects, tasks, and sync connections with its import-run lineage. Manager API, built-in MCP, and the migration wizard can atomically roll back only that run; imported projects containing later work are preserved.
- `real-time-collaboration-humans-and-agents`, `video-meetings-standups-and-shared-calendars`: meeting and ceremony controls now report the selected runtime ICE candidate path. The UI distinguishes a verified direct path from an encrypted TURN-relay path instead of inferring privacy from configuration alone.
- `semantic-response-cache`: the browser cache now connects its on-device L1 to the shared gateway L2. Server partitions include tenant, role, and authenticated principal, writes invalidate read-through state, and an explicit scoped purge path is tested.

## Open implementation gaps

| Gap | Articles affected | Acceptance evidence still required |
| --- | --- | --- |
| Full-GPU base-model autograd and large named models | `webgpu-lora-explained`, `local-first-ai-webgpu-in-the-browser`, `ai-dataset-generation-best-practices` | The LoRA adapter forward/backward/update path is WGSL with CPU fallback, and compatible Evermind checkpoints/tokenizers can be loaded. The base LM loss/autograd is still CPU. Full-GPU end-to-end training and the historic ~2B/device-speed table require measured hardware fixtures before those exact limits are considered evidenced. |
| Every Canvas action is live | `forty-eight-live-objects-one-creation-canvas`, `creation-canvas-beyond-chat`, `design-build-debug-one-spatial-workspace`, `customer-feedback-to-ten-mockups`, `live-data-stories-on-the-creation-canvas`, `creation-canvas-templates-frames-branches-checkpoints`, `create-before-you-sign-up`, `product-ideation-with-builderforce` | Core workflow, site, video, dataset, project, mockup, stand-up, Evermind, and nine creative adapters now deliver artifacts. Remaining declared registry actions (notably Sales, MCP, Voice, Chart, Dashboard, Task, and Agent operations) need the same deliverable-manifest integration tests. The registry currently has more kinds than the historic “48” snapshot. |
| Cross-client destructive/branch parity | `multiplayer-creation-canvas-web-vscode`, `vs-code-command-center-for-your-agentic-workforce`, `in-browser-ide-and-collaboration`, `builderforce-vs-cursor-windsurf`, `builderforce-vs-claude-code` | Web and VS Code now share edits, presence, comments, reconnect catch-up, checkpoints, restore, branch, reviewed merge, and destructive confirmation. A two-process permission/conflict fixture remains to exercise the deployed transport end to end. |
| Media-path evidence across every surface | `real-time-collaboration-humans-and-agents`, `video-meetings-standups-and-shared-calendars`, `visual-team-rituals-humans-and-agents`, `multi-party-team-chat-humans-and-agents` | Direct-only configuration and selected-candidate reporting are implemented for meeting and ceremony web surfaces. The VS Code meeting surface still needs the same selected-pair disclosure. |
| Connector parity and migrations | `single-pane-board-connectors`, `migrate-and-integrate-jira-monday-rally-gitlab-bitbucket`, `builderforce-agents-and-agent-integration`, `skills-assignment-and-the-marketplace` | Named adapters, discovery, retries, webhook ingestion, reconciliation, and lineage rollback exist. Attachment/comment/history round-trip fixtures remain vendor-dependent and should be added for each API that supports them. |
| Autonomous delivery proof | `autonomous-swimlane-execution`, `multi-agent-orchestration`, `fleet-management-and-agent-routing`, `define-a-need-the-agentic-system-solves-it`, `everything-an-agentic-employee-can-do`, `getting-started-with-ai-agents` | End-to-end execution fixtures that cover dispatch through artifact/PR delivery, cancellation, stale-host recovery, budget enforcement, approval interruption, and audit replay. |
| QA and observability closure | `agentic-tester-autonomous-qa`, `quality-error-observability-one-click-fix`, `task-execution-and-observability` | Browser/device test runner evidence, issue deduplication, reproduction artifacts, error-to-fix-to-PR trace, deployment verification, and failure/rollback tests. |
| Governance, tenancy, and compliance assurance | `security-and-multi-tenant-architecture`, `approval-gates-and-human-oversight`, `cobit-governance-readiness-for-agentic-it`, `knowledge-management-sops-and-compliance`, `role-gated-accountability-proof-of-participation`, `system-of-record-for-agentic-work` | Tenant-isolation adversarial suite, authorization matrix, immutable audit retention/verification, approval-bypass tests, evidence exports, restore drills, and externally scoped compliance evidence. Architecture alone is not certification. |
| Planning and executive evidence | `planning-spine-cost-bearing-delivery`, `compare-projects-and-build-an-executive-roadmap`, `specs-and-planning-with-ai`, `automated-rfp-response-from-your-codebase`, `every-role-operating-picture`, `ai-development-maturity-diagnostic` | Source freshness indicators, provenance to canonical records, cost/date rollups, permission-filtered role views, deterministic regeneration, and export reconciliation tests. |
| Agent/persona effectiveness | `ai-agent-personality-psychometric-personas`, `skills-assignment-and-the-marketplace` | Versioned persona/skill manifests in execution traces, before/after task evaluation, conflict resolution, and marketplace package integrity tests. |
| Incident learning loop | `incident-management-on-call-and-war-rooms` | Monitor breach → incident → escalation → war room → RCA → Knowledge → Evermind contribution integration test, including notification failures and retry evidence. |
| Cache correctness and savings | `semantic-response-cache` | L1→L2 wiring, tenant/principal/role partitioning, invalidation, threshold misses, and cross-partition tests exist. Remaining evidence is a shared embedding-version key across web/agent releases and measured token/cost deltas from production telemetry. |
| Competitive and platform-wide assertions | `agent-tech-stack-all-seven-layers`, `best-ai-coding-agents-compared`, `builderforce-vs-devin`, `builderforce-vs-github-copilot`, `introduction-and-overview` | A dated evidence snapshot for Builderforce and each compared product; all current feature assertions must point to the relevant capability tests above. |

## CI policy to add

Each material blog claim should receive a stable capability ID in frontmatter. CI should fail when an ID has no passing evidence test, when its test is skipped, or when evidence is older than the product/version named in a comparison. The gap table above is the migration backlog; new articles should not merge without IDs from day one.
