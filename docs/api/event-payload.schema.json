{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://builderforce.ai/api/event-payload.schema.json",
  "title": "Builderforce Progress Event Payload",
  "description": "Standardized event payload describing task/job progress for progress-stream consumers.",
  "type": "object",
  "properties": {
    "progressPct": {
      "description": "A 0–100 value indicating the current progress of the task or job. The value 100 MUST be emitted ONCE, after all processing steps are confirmed complete and no further progress updates will follow. This is the authoritative signal of task/job completion for progress-stream consumers. Nebular values (e.g., 99) do not indicate final completion.",
      "type": "number",
      "minimum": 0.0,
      "maximum": 100.0
    }
  },
  "required": ["progressPct"],
  "example": {
    "progressPct": 100
  }
}