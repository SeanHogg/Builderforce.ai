{
  "name": "@builderforce/agent-tools",
  "version": "2026.9.5",
  "private": true,
  "type": "module",
  "description": "Runtime-agnostic agent tool contract shared by the cloud (Worker) and on-prem (Node) engines. The package ROOT is node-builtin-free (the Worker imports it); node-only helpers live behind the `./node-path` export condition, which the Worker never imports.",
  "exports": {
    ".": "./src/index.ts",
    "./symbols": "./src/symbols.ts",
    "./node-path": "./src/node-path.ts",
    "./node-symbols": "./src/node-symbols.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
