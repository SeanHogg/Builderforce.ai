{
  "name": "@builderforce/ide-templates",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The IDE starter scaffolds (vanilla Vite + React Native/react-native-web) and the modality→scaffold registry, shared by the API Worker that seeds them into R2, the legacy worker's file router, and the Next app that mounts them as the Run fallback. Source-only: every consumer aliases the bare specifier straight at src/index.ts.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts"
}
