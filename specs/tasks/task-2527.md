# Task 2527 — P0 Advisor profiles + marketplace category

Status: **blocked on a readable checkout**. This file is the ticket’s implementation note, not the feature.

## Constraint

`write_file` can commit to `seanhogg/builderforce.ai` on `builderforce/task-2527`, but `list_files` / `read_file` / `git_*` return “no repository bound”. Blind overwrites of talent, marketplace, contract, or i18n files are refused.

## Design (do not regress)

- Advisor is a **mode** on the existing talent/freelancer listing.
- **Do not** add advisory to `PEOPLE_OBJECT_KINDS`.
- **Do not** add `advisor` to `MARKETPLACE_LISTING_KINDS`.
- Marketplace discovery is category slug `advisors` (`?category=advisors`), logged-out browse allowed.
- Fields when advisor mode is on: expertise, industries, languages, methods (`video|phone|email|in-person`), stages (`start|grow|exit`), session length, price (`0` = volunteer), location (required iff in-person), confidentiality default.
- Reuse talent profile renderer. Primary CTA **Book**; **Hire** only if available-for-hire.
- Authenticated setup is a PRD 21 Canvas destination, not a new app chrome.
- Five locales, light and dark. Turning advisor mode off does not clear hire fields.

A0 Book bind (already on main, do not redo): `booking_hosts.host_ref` = talent userId; `GET /api/freelancers/:id/booking-services`; `POST /api/freelancers/:id/reservations`; TalentDetailClient Book is `!isOwner && bound`, not gated on `canHire`.

## Next run

Once `list_files`/`read_file` work: sync `main`, locate freelancer profile schema + TalentDetailClient + marketplace category list + canvas destination registry + five-locale message files, then make the smallest additive change that satisfies the ticket PRD acceptance criteria.
