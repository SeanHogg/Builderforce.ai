> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2539
> _Each agent that updates this PRD signs its change below._

# PRD: Meeting Notes Marketplace Agent

## Problem & Goal

Advisory meetings currently rely on ad-hoc note-taking. Notes are inconsistent, often incomplete, and rarely available to the rest of the workspace without extra human effort. We already have `runAgentTurn` as the execution surface for agent turns, but Meeting Notes is not a first-class marketplace `agent` that advisors can discover, enable, or reason about like other agents.

**Goal:** Publish Meeting Notes as a marketplace `agent` listing that wraps `runAgentTurn` with a silent notetaker persona, scoped to `kind=advisory`. Enable it by default for dogfood advisors so internal advisory sessions produce structured notes without a setup step.

## Target users / ICP roles

- **Dogfood advisors** — internal advisors running `kind=advisory` sessions; primary launch cohort; default-on.
- **Advisor operators / workspace admins** — need the listing to be discoverable, toggleable, and clearly scoped so it does not leak into non-advisory kinds.
- **Downstream marketplace / agent-runtime agents** — consume this PRD to implement listing metadata, persona wrapping, default-on policy, and turn invocation.

End clients in advisory sessions are *not* the operator of this agent; they should experience notes as a silent byproduct, not as a participant.

## Scope

In scope for this WIP:

- A marketplace `agent` listing for Meeting Notes.
- A silent notetaker persona that wraps `runAgentTurn` (no spoken/visible participation in the live advisory turn stream unless notes are explicitly requested by product surfaces).
- Restriction to sessions with `kind=advisory`.
- Default-on enablement for dogfood advisors only.
- Listing metadata sufficient for marketplace discovery, enable/disable, and runtime binding to `runAgentTurn`.
- Persistence and attachment of notes to the originating advisory session so downstream surfaces can read them.

## Functional requirements

1. **Listing.** Meeting Notes is published as a marketplace `agent` with stable identity, display name “Meeting Notes”, short description that it silently captures advisory session notes, and capability tags that identify it as a notetaker (not a conversational advisor).
2. **Runtime wrap.** Each eligible turn is executed by wrapping `runAgentTurn` with the silent notetaker persona. The wrapper must pass through session, kind, and turn context required by `runAgentTurn`; it must not introduce a second execution path.
3. **Persona.** The persona is a silent notetaker: it does not address participants, does not ask questions, does not offer advice, and does not emit user-visible chat in the advisory transcript. It only produces notes artifacts.
4. **Kind gate.** The agent runs only when `kind=advisory`. For any other kind it must not be invoked, even if the listing is enabled on the workspace.
5. **Default-on (dogfood).** For dogfood advisors, the listing is enabled by default on advisory sessions. Advisors may disable it per session or per workspace. Non-dogfood advisors do not receive default-on; listing may exist but remains opt-in until a later rollout.
6. **Notes output.** On session completion (and on durable mid-session checkpoints if `runAgentTurn` already supports them), the agent writes structured meeting notes bound to the session: summary, decisions, action items (owner + due date when stated), and open questions. Notes are attributable to the Meeting Notes agent, not to a human participant.
7. **Idempotency.** Re-running a turn or retrying `runAgentTurn` must not duplicate notes; updates replace or version the existing notes artifact for that session.
8. **Disable path.** When disabled, `runAgentTurn` is not called for this agent, and no notes artifact is created or updated.
9. **Marketplace contract.** Listing fields, enablement flags, and persona binding are expressed in the same agent-listing schema used by other marketplace agents so catalog, policy, and runtime agents do not need a special case beyond `kind=advisory` and default-on dogfood.

## Acceptance criteria

- Marketplace catalog shows a Meeting Notes `agent` listing that binds to `runAgentTurn` with the silent notetaker persona.
- In a `kind=advisory` session for a dogfood advisor, the agent is enabled with no manual install or toggle; notes appear on the session after the session completes.
- The advisory live transcript contains no notetaker utterances, questions, or advice.
- In a non-`advisory` session, the agent does not run even if the listing is enabled.
- A dogfood advisor can disable Meeting Notes; subsequent advisory sessions produce no notes until re-enabled.
- A non-dogfood advisor does not get the agent on by default.
- Retrying the same turn does not create duplicate notes for the session.
- Notes include summary, decisions, action items, and open questions when those are present in the session; missing sections are omitted rather than hallucinated.
- Downstream agents can resolve the listing, its enablement state, and the session-bound notes artifact without a one-off API.

## Out of scope

- General availability default-on for non-dogfood advisors.
- Notetaking for kinds other than `advisory`.
- Visible / speaking meeting-participant personas, live captions, or in-call coaching.
- Client-facing notes editing UX, sharing, export, or email recap flows (consume the artifact later).
- Changing the `runAgentTurn` contract, billing, or quota model.
- Multi-language notes localization, custom note templates, CRM writeback, or compliance redaction beyond what `runAgentTurn` already applies.
- Auto-assigning action-item owners that were not stated in the session.

## Requirements

**Owner:** business-analyst (Ada) · ticket **#2539** (child of epic **#2538** P0/P1 Meeting Notes; grandparent **#2522**). This ticket is **P0 listing + wrap only**. Sibling **#2540** owns the `summarizeMeeting` / `parseMeetingMinutes` three-section Markdown contract. P1 siblings **#2541** (`pullStandupMinutesAct`), **#2542** (follow-up propose), **#2543** (guest project + claim) are out of this ticket.

### Definitions

| Term | Meaning for this ticket |
|---|---|
| **Marketplace `agent` listing** | A catalog listing in the existing Agents family (same schema as other marketplace agents: identity, display name, short description, capability tags, enablement flags, persona binding). **Not** a canvas listing kind. **Do not** add `advisor` (or `meeting_notes`) to `MARKETPLACE_LISTING_KINDS`. **Do not** add `/advisors`. `category=advisors` remains a **talent filter**, not a listing kind. |
| **Silent notetaker persona** | A `runAgentTurn` persona that produces notes artifacts only. It does not address participants, ask questions, offer advice, or emit user-visible chat in the advisory live transcript. |
| **Eligible session** | A meeting whose `kind` is exactly `advisory` (registered in `api/src/presentation/routes/meetingRoutes.ts` `KINDS` by **#2536**). `advisory` is **not** in `TEAM_CEREMONY_KINDS` (no default team-chat link). Unknown kinds continue to coerce to `adhoc` and are ineligible. |
| **Dogfood advisor** | The **host** of an eligible session who is on the internal dogfood allowlist. The allowlist store is the architect’s call (workspace/tenant flag or equivalent — not a new listing kind). Host identity reuses the talent person bind (`booking_hosts.host_ref` = talent `userId`) when that bind exists; this ticket does **not** invent an advisor listing kind or a Book CTA. |
| **Notes artifact** | The session-bound minutes document attributable to the Meeting Notes agent (not to a human participant). Restricted by default with the rest of advisory session work product. |

### Seams (implement against these; do not fork)

- **Runtime:** wrap existing `runAgentTurn` in `api/src/application/meetings/meetingIntelligence.ts` (or the current owner of that export). Pass through session, `kind`, and turn context. **No second execution path.**
- **End-of-meeting minutes:** `summarizeMeeting` already runs on meeting end. Sibling **#2540** keeps the three-section Markdown (`Summary` / `Decisions` / `Action items`) so `parseMeetingMinutes` does not fork. This ticket’s artifact **must remain parseable** by that contract for those three sections.
- **Catalog / enablement:** the same agent-listing schema used by other marketplace agents. Catalog, policy, and runtime need **no special-case API** beyond (a) `kind=advisory` gate and (b) dogfood default-on.
- **Disable:** when the listing is disabled for the host/workspace/session, `runAgentTurn` is **not** invoked for this agent and the notes artifact is **not** created or updated.

### R-1 — Listing identity (FR-1, FR-9)

**Given** the marketplace Agents catalog  
**When** a consumer resolves the Meeting Notes listing  
**Then** they get a stable identity with:

- display name **“Meeting Notes”**
- short description that it **silently** captures notes for **advisory** sessions
- capability tags that identify it as a **notetaker**, not a conversational advisor
- persona binding to the silent notetaker that wraps `runAgentTurn`
- enablement flags readable through the existing listing/enablement surface (no one-off API)

The listing is discoverable and toggleable. It is **not** a talent person, not a Book bind, and not a new `MARKETPLACE_LISTING_KINDS` value.

### R-2 — Runtime wrap (FR-2)

**Given** an eligible session whose Meeting Notes listing is enabled  
**When** a turn is due  
**Then** the platform invokes **one** path: `runAgentTurn` wrapped with the silent notetaker persona, passing session, `kind=advisory`, and turn context already required by `runAgentTurn`.

**Must not:** introduce a parallel minutes LLM, a second agent loop, or a change to the `runAgentTurn` contract, billing, or quota model.

### R-3 — Silent persona (FR-3)

**Given** the notetaker is running in an advisory session  
**When** participants speak or the turn completes  
**Then** the advisory **live transcript contains no** notetaker utterances, questions, or advice. The only output is the notes artifact (R-6). End clients experience notes as a silent byproduct, not as a participant.

### R-4 — Kind gate (FR-4)

**Given** any session whose `kind` is not `advisory` (including `standup`, `planning`, `retrospective`, `review`, `adhoc`, and unknown→`adhoc`)  
**When** the Meeting Notes listing is enabled on the workspace  
**Then** the agent **is not invoked**. `runAgentTurn` is not called for this persona. No notes artifact is created or updated.

The kind gate is evaluated **before** dogfood default-on and **before** the wrap.

### R-5 — Dogfood default-on, opt-in otherwise, disable path (FR-5, FR-8)

**Given** an eligible (`kind=advisory`) session whose **host** is a dogfood advisor  
**When** the session starts with no prior Meeting Notes preference  
**Then** the listing is **enabled** with no install or toggle step. Notes appear on the session after it completes (R-6).

**Given** a dogfood advisor disables Meeting Notes (per session or per workspace)  
**When** a subsequent advisory session runs  
**Then** `runAgentTurn` is not called for this agent and no notes artifact is created or updated until re-enabled.

**Given** a **non-dogfood** advisor  
**When** they run an advisory session  
**Then** Meeting Notes is **not** on by default. The listing may exist in the catalog; enablement is opt-in until a later GA ticket (out of scope here).

Default-on **never** overrides R-4 (non-advisory kinds stay dark).

### R-6 — Notes artifact (FR-6) and parser compatibility (#2540)

**Given** an enabled eligible session completes (and on durable mid-session checkpoints **only if** `runAgentTurn` already supports them — this ticket does not invent a checkpoint protocol)  
**When** the wrap finishes  
**Then** one notes artifact is bound to the originating session and is attributable to the Meeting Notes agent.

Content rules:

- Include **summary**, **decisions**, **action items** (owner + due date **only when stated** in the session), and **open questions** when those are present.
- **Omit** missing sections rather than hallucinate them.
- **Do not** auto-assign action-item owners that were not stated.
- The durable three-section Markdown (`Summary` / `Decisions` / `Action items`) remains the contract `parseMeetingMinutes` already consumes. **Do not fork that parser on this ticket** — format/parser work is **#2540**. Open questions, if persisted, must not break those three sections.
- Notes and transcript stay **restricted by default** with other advisory session work product (explicit founder/host action required to share more widely). Downstream read uses the existing session-bound artifact surface, not a one-off API.

### R-7 — Idempotency (FR-7)

**Given** a notes artifact already exists for a session  
**When** the same turn is retried or `runAgentTurn` is re-invoked for that session  
**Then** notes are **not duplicated**. The existing artifact is replaced or versioned in place (one current notes document per session). Title/session identity is the idempotency key for this ticket; generalising `pullStandupMinutesAct` (idempotent-by-title board placement) is **#2541**, not here.

### R-8 — Marketplace / downstream contract (FR-9)

**Given** catalog, policy, and runtime agents  
**When** they resolve Meeting Notes  
**Then** they use the **same** agent-listing schema as other marketplace agents: listing fields, enablement flags, persona binding, and the session-bound notes artifact.

The **only** product special cases allowed on this ticket are:

1. invoke only when `kind=advisory` (R-4)
2. default-on for dogfood hosts (R-5)

**Must not:**

- add `advisor` / Meeting Notes to `MARKETPLACE_LISTING_KINDS`
- add an `/advisors` route
- treat `category=advisors` as a listing kind
- dump talent/booking/minutes source onto epic **#2522** or **#2527**
- ship P1 minutes→board, follow-up booking, or guest claim (those are **#2541–#2543**)
- keep meeting intelligence as a private platform-only path for dogfood (parent **#2538**): the listing must be a real marketplace `agent` the catalog can resolve

### Traceability

| Requirement | Functional req | Acceptance criteria |
|---|---|---|
| R-1 | FR-1, FR-9 | Catalog shows Meeting Notes `agent` bound to `runAgentTurn` + silent persona |
| R-2 | FR-2 | Wrap is the only execution path |
| R-3 | FR-3 | Live transcript has no notetaker utterances |
| R-4 | FR-4 | Non-`advisory` sessions do not run the agent even if enabled |
| R-5 | FR-5, FR-8 | Dogfood default-on; disable stops notes; non-dogfood is opt-in |
| R-6 | FR-6 | Notes on session after complete; sections omitted if absent; restricted by default |
| R-7 | FR-7 | Retry does not duplicate notes |
| R-8 | FR-9 | Downstream resolve listing, enablement, and artifact without a one-off API |

### Explicitly not this ticket

- GA default-on for non-dogfood advisors
- Notetaking for kinds other than `advisory`
- Visible / speaking participant, live captions, in-call coaching
- Client-facing notes editing, sharing, export, email recap
- Changing `runAgentTurn` contract, billing, or quota
- Multi-language notes, custom templates, CRM writeback, extra redaction
- Auto-assigning unstated action-item owners
- `summarizeMeeting` / `parseMeetingMinutes` format changes (**#2540**)
- Board placement via `pullStandupMinutesAct` (**#2541**)
- Follow-up slot propose/email (**#2542**)
- Guest project + claim link (**#2543**)
- Marketplace Book bind / A0–A5 (**#2545** / **#2527**)

### Implementation ownership

BA deliverable is this Requirements section on the ticket PRD. **No product-repo patch on #2539** for this role. Architect authors **Design**; developer on **#2539** (not #2522 / #2527 / #2538) implements against R-1–R-8. Branch file `specs/tasks/task-2539.md` (if present) is a docs-only dump and is **not** the apply.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:51:13.464Z_

---

### R-1 — Listing type and family

1. Publish one marketplace listing named **Meeting Notes**.
2. Listing type is **`agent`**. It is not a skill, workflow, tool, persona, playbook, or Talent `person`/`gig`.
3. Storefront family is **Agents** (`family=agent`). Chips already in `marketplaceFamilies.ts` are `builtin` | `community`. Meeting Notes is a **platform-owned** agent → file it under the **`builtin`** chip (not the canvas `agent` kind that rides Community). Architect may move the chip only if the same hire contract is preserved.
4. The row is a published `ide_agents` record that satisfies `publicAgentScope` (`status = 'active'`, `published = true`, **not** a `tenants.is_demo` fixture). Demo-tenant fixtures are excluded from the public catalogue today; do not re-introduce them.
5. **Do not** add `advisor` (or `meeting-notes`) to `MARKETPLACE_LISTING_KINDS`. **Do not** add `/advisors`. **Do not** add a fifth family.
6. CTA is **Hire** (roster write), the same verb as every other Agents-family listing. It is never Book (Talent) or Install-as-asset.

**Acceptance:** `/marketplace?family=agent` shows a Meeting Notes listing whose type/family is `agent`. Downstream callers resolve it through the shared public-agent predicate with no one-off flag.

---

### R-2 — Runtime wrap (`runAgentTurn` only)

1. Every eligible advisory turn the notetaker handles goes through the existing **`runAgentTurn`** function. No second model gateway, prompt pipeline, or meeting-turn runtime.
2. Today `runAgentTurn` is a **speaking** participant: it persists a caption as `speakerKind: 'agent'`, broadcasts `agent-say`, and `postChatMessage`s into the meeting chat. Silent capture is a **mode of that function** (flag / persona), not a parallel path.
3. The listing’s enablement is what selects the silent-notetaker persona. Callers do not pass a private “meeting notes” feature flag.

**Acceptance:** Enabling the listing is sufficient for the wrap to run. Grep of the change set shows notetaker turns calling `runAgentTurn`; no new `callModel` / gateway helper is introduced for this ticket.

---

### R-3 — Silent notetaker persona

1. The persona **produces notes from the turn**. It does **not** speak, interrupt, or send participant-visible messages.
2. Forbidden side effects while the silent persona is active: `agent-say` broadcast, meeting-chat post, caption rows that appear in the participant caption feed, any “notes bot” utterance in the room.
3. Allowed side effects: persist notes to the meeting’s notes/summary artifact; structured logging; the existing end-of-meeting `summarizeMeeting` path (R-9).
4. Advisees are not a control-surface user. They must not be asked to enable, dismiss, or reply to the notetaker.

**Acceptance:** On a live `kind=advisory` session with the listing enabled, participants see no notetaker message in chat or captions, and notes still exist on the meeting after the turn.

---

### R-4 — Kind gate (`advisory` only)

1. The notetaker **runs only** when the meeting/session `kind === 'advisory'`.
2. **Constraint (code, 2026-09-16):** `MEETING_KINDS` is currently `standup | planning | retrospective | adhoc | direct | interview | review`. **`advisory` does not exist.** This ticket **adds** `advisory` to that closed set (schema check, API create/patch enum, `defaultTitle`, frontend kind union). It does **not** invent a parallel kind field.
3. Any other kind is a **no-op**: do not call `runAgentTurn` for the notetaker, do not write notes, do not hire-check-fail the session.
4. Who **writes** `kind=advisory` on create: advisory 1:1s composed from Talent + `booking_services` (PRD 26 A0/A1, tickets #2527 / #2532 / #2545). Until those writers land, the agent is correctly idle. This ticket still owns the kind + the gate so those writers have somewhere to point.
5. Non-advisory kinds named in the parent epic (coaching, support, internal standups, ceremonies) stay out of scope. Existing ceremony kinds keep today’s speaking `runAgentTurn` behaviour.

**Acceptance:** A meeting with `kind=advisory` and the listing enabled produces notes. The same listing enabled on `standup` / `direct` / `review` / etc. produces **zero** notes and **zero** `runAgentTurn` notetaker invocations.

---

### R-5 — Dogfood default-on

1. **Dogfood advisors** receive Meeting Notes **enabled without opt-in** at publish and when a new dogfood advisor is created afterwards.
2. **Dogfood ≠ demo.** `tenants.is_demo` is the sales-fixture tenant that `publicAgentScope` already hides. Dogfood is the **operator/internal tenant** where real advisory sessions are run. Architect names the exact predicate (tenant flag, allowlist, or `host_ref` on `booking_hosts`); BA requires it to be a single testable function, not a scattered `if`.
3. **Dogfood advisor** (until Architect tightens): a user in that dogfood tenant who hosts advisory sessions — a Talent `person` listing and/or a `booking_hosts.host_ref` bind (PRD 26 A0). If A0 is not yet measurable, default-on applies to every user in the dogfood tenant who can organize a meeting; it must still **not** apply to non-dogfood tenants.
4. Non-dogfood users **do not** get default-on. They hire from the marketplace like any other agent.
5. A dogfood advisor **may turn it off** through the same marketplace hire/unhire (or listing-enable) control as other agents. That off state **sticks across sessions** until they turn it back on. Re-running default-on **must not** re-enable an explicit unhire.
6. Default-on is a **roster hire** of the published Meeting Notes agent, not a process-env flag and not a per-meeting checkbox.

**Acceptance:** New and existing dogfood advisors have the listing hired after publish. A dogfood advisor who unhires stays unhired across a process restart and a new advisory session. A non-dogfood tenant’s advisors are not hired unless they hired it themselves.

---

### R-6 — Discoverability and shared contract

1. Listing metadata (name, description, type `agent`) is consistent with other Agents-family rows so the existing storefront card renders it without a one-off component.
2. Downstream agents resolve and invoke it from the **shared marketplace contract** (`ide_agents` + hire/roster). No `MEETING_NOTES_ENABLED` env, no special-case MCP tool, no hardcoded agent id outside the listing row.
3. Stable public identity for the minutes poster already in use: `summarizeMeeting` posts as `{ kind: 'agent', ref: 'meeting-notes', name: 'Meeting Notes' }`. The listing’s `ref` **must be** `meeting-notes` so chat attribution and the listing are the same agent. Do not mint a second ref.

**Acceptance:** A caller that already knows how to hire a published agent can hire Meeting Notes with no new API. Chat posts from minutes continue to attribute `meeting-notes`.

---

### R-7 — Idempotent enablement and idempotent notes

1. Re-hiring, re-publishing, or re-applying default-on **does not** double-subscribe the roster (one hire row per workspace × agent).
2. The same advisory turn processed twice **does not** duplicate notes (key by meeting id + turn/caption id, or equivalent).
3. `summarizeMeeting` on end remains best-effort and must not append a second copy of the same minutes if it already wrote `meetings.summary` for that meeting.

**Acceptance:** Default-on applied twice leaves a single hire. Replaying a turn leaves a single notes artifact. Ending a meeting twice does not duplicate the three-section minutes.

---

### R-8 — Failure isolation

1. Notetaker or `runAgentTurn` failure **must not** fail the advisory session for participants: no 502 on the participant turn, no blocked start/end, no rolled-back reservation.
2. **Constraint (code, 2026-09-16):** `POST /:id/agent-turn` today returns **502** when the speaking agent’s gateway fails. That path stays for **speaking** agents. The silent notetaker must **not** share that failure mode with the room.
3. Meeting **end** already auto-calls `summarizeMeeting` in a try/catch and never blocks ending. Keep that isolation. A notetaker miss is logged (`reportCaughtError` or equivalent) and is otherwise a no-op.
4. Participants are not shown a notetaker error toast.

**Acceptance:** Forcing a notetaker gateway failure still starts, runs, and ends the advisory meeting; the reservation and participant chat succeed; the failure is recorded for operators.

---

### R-9 — Minutes contract (`summarizeMeeting` — do not fork)

Parent #2538 P0: on meeting end, `summarizeMeeting` already runs. **Keep** the three-section Markdown so `parseMeetingMinutes` does not fork.

Canonical sections (already in code):

1. `## Decisions`
2. `## Action items` (or `## Actions`)
3. `## Open questions` (or `## Questions`)

Rules:

1. This ticket **does not** replace `summarizeMeeting` with the silent persona. Silent capture (R-2/R-3) may **feed** the same summary; the published minutes remain this contract.
2. Do not add sections, rename headings, or change list shape in a way that breaks `parseMeetingMinutes`.
3. P1 placement of action items onto the board (`pullStandupMinutesAct`, idempotent by title) is **out of scope** here — tracked on #2538 / follow-up, not this listing ticket.

**Acceptance:** After an advisory meeting ends, `meetings.summary` is still parseable by the existing minutes parser. Existing standup/planning/retro summaries keep working.

---

### R-10 — Confidentiality

PRD 26 FR-7: mentoring notes, transcripts, and the Brain thread default **`restricted`**.

1. Notes produced by this agent inherit that ceiling. They must not cross `guest` / `share` / `publicMedia` / `aiContext` without an explicit founder act.
2. The **listing** (name, description, hire CTA) stays public. Session work product does not.
3. No new confidentiality level.

**Acceptance:** An advisory session’s notes are not visible on a shared board or in public marketplace content.

---

### R-11 — Enablement model (hire = on)

| Audience | How it turns on | How it turns off |
|---|---|---|
| Dogfood advisor | Default hire at publish + on advisor create (R-5) | Same unhire control as any agent; sticks |
| Non-dogfood advisor | Manual Hire on `/marketplace?family=agent` | Unhire |
| Advisee | N/A (not a control-surface user) | N/A |
| Non-advisory meeting | N/A (kind gate no-op even if hired) | N/A |

There is no per-meeting “enable notes” checkbox in this ticket.

---

### R-12 — Traceability (PM FR / AC → requirement)

| PM item | Requirement |
|---|---|
| FR-1 Listing type `agent` | R-1 |
| FR-2 Runtime wrap `runAgentTurn` | R-2 |
| FR-3 Silent notetaker | R-3 |
| FR-4 Kind gate `advisory` | R-4 |
| FR-5 Dogfood default-on | R-5, R-11 |
| FR-6 Discoverability | R-1, R-6 |
| FR-7 Idempotent enablement | R-7 |
| FR-8 Failure isolation | R-8 |
| AC: listing visible as `agent` | R-1, R-6 |
| AC: enable binds silent wrap | R-2, R-3 |
| AC: notes on `advisory` only | R-4 |
| AC: other kinds no-op | R-4 |
| AC: dogfood on by default | R-5 |
| AC: dogfood can turn off and it stays off | R-5, R-11 |
| AC: non-dogfood not default-on | R-5 |
| AC: session success independent of notetaker | R-8 |
| AC: downstream shared contract | R-6 |
| Parent #2538: keep three-section Markdown | R-9 |
| PRD 26 FR-7 restricted notes | R-10 |
| PRD 26: no `advisor` listing kind, no `/advisors` | R-1 |

---

### Constraints discovered in the bound repo (do not re-derive)

1. **`advisory` is not a meeting kind yet.** Adding it is in scope for the developer on this ticket (R-4).
2. **`runAgentTurn` currently speaks.** Silent mode is a wrap/flag of that function, not a new runtime (R-2, R-3).
3. **`summarizeMeeting` already posts as `ref: 'meeting-notes'`** and already isolates failure on meeting end. Reuse both (R-6, R-8, R-9).
4. **Public agents are `ide_agents` + `publicAgentScope`**, not `catalog_items` and not a new canvas kind (R-1, R-6).
5. **Agents family chips are `builtin` \| `community`.** Canvas listing kind `agent` already maps to the community chip — do not confuse that with this builtin (R-1).
6. **`tenants.is_demo` is fixtures, not dogfood.** Default-on must not target demo tenants (R-5).
7. **Speaking `POST /:id/agent-turn` 502s the caller.** Notetaker must not (R-8).

---

### Assumptions (Architect may tighten; do not silently invert)

- A1. Builtin chip, not community/canvas-published agent.
- A2. Listing `ref` = `meeting-notes` to match the existing chat poster.
- A3. Per-turn silent capture **feeds** `meetings.summary`; `summarizeMeeting` on end remains the canonical minutes. Per-turn notes are **not** posted to chat.
- A4. Default-on is a roster hire with a durable “explicitly unhired” mark so re-application cannot resurrect an opt-out.
- A5. A0/A1 writers of `kind=advisory` may land on #2527 / #2532 / #2545. This ticket still ships kind + gate + listing. Idle-until-A1 is acceptable.
- A6. Price / public GA packaging are out of scope; dogfood listing may be free (`price = 0`) without inventing a second catalogue.

---

### Out of scope (this ticket — do not implement here)

- P1 of #2538: generalise `pullStandupMinutesAct` (idempotent-by-title board placement). Follow-up: file under #2538 if missing.
- P1 of #2538: propose (do not silently book) a follow-up slot + email both parties — **#2542**.
- P1 of #2538: guest project + claim link — **#2543**.
- Non-advisory kinds; participant-facing recap/chat; new note storage / CRM sync; custom model pipeline; default-on for non-dogfood; multi-agent orchestration; human editor UX; note-quality scoring; pricing / public marketplace launch.
- Dumping talent/booking/minutes source onto epic #2522. P0 code ships on **this** ticket’s branch (`builderforce/task-2539`) after Architect/Developer lanes, not as a BA docs file under `specs/tasks/`.

**BA deliverable is this Requirements section on the ticket PRD.** No product-repo patch from this lane. Prior docs-only `specs/tasks/task-2539.md` dumps (if present on the branch) are **not** the apply and must not ship.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:50:13.006Z_

## Design

_Owned by the architect (CTO) · ticket **#2539** · 2026-09-16 · execution **31914**. Implements PO-locked contract **R-1–R-12** (exec 31792). This section is the apply for the developer. Do **not** treat `specs/tasks/task-2539.md` as design (dump; must not ship).

### D-0 — Non-goals and ownership

This ticket is **P0 listing + wrap only**. Architect lane is **PRD Design**, not product code. Developer implements on `builderforce/task-2539` against D-1–D-12. Do not dump work onto epic **#2522**, **#2527**, or parent **#2538**.

**This ticket does not:**

- Add `advisory` to `MEETING_KINDS` / create-patch enum / `defaultTitle` / frontend kind union — **#2536** owns that. This ticket **gates** on `kind === 'advisory'` only. Idle-until-#2536 is acceptable (PO lock #1, A5).
- Fork `summarizeMeeting` / `parseMeetingMinutes` or change published headings — **#2540**.
- Board placement (`pullStandupMinutesAct`) — **#2541 / #2613**.
- Follow-up propose — **#2542**. Guest claim — **#2543**.
- Add `advisor` / `meeting-notes` / `meeting_notes` to `MARKETPLACE_LISTING_KINDS`, add `/advisors`, or treat `category=advisors` as a listing kind.
- Change `runAgentTurn` billing/quota, add a second model gateway, or 502 the room for a notetaker miss.

### D-1 — Listing identity (R-1, R-6, R-11)

**One** published `ide_agents` row, resolved through the existing public-agent predicate (`publicAgentScope`: `status = 'active'`, `published = true`, **not** a `tenants.is_demo` fixture). Not `catalog_items`. Not a canvas listing kind.

| Field | Value |
|---|---|
| `id` (stable public ref) | **`meeting-notes`** — same string `summarizeMeeting` already posts as `{ kind: 'agent', ref: 'meeting-notes', name: 'Meeting Notes' }`. Do not mint a second ref. |
| `name` | `Meeting Notes` |
| short description / `bio` | Silently captures notes for **advisory** sessions. Not a conversational advisor. |
| `skills` / capability tags | Include **`notetaker`**. Do not tag it as an advisor/coach. |
| `builtinKind` | **`meeting_notes`** (varchar 32; files the row under the Agents-family **`builtin`** chip, not canvas/`community`) |
| `published` | `true` |
| `status` | `active` |
| `priceCents` | `0` (A6; no GA packaging on this ticket) |
| family / CTA | `family=agent`; CTA **Hire** (roster write). Never Book, never Install-as-asset. |

**Chip:** Agents family chips are already `builtin` \| `community` (`frontend/src/lib/marketplaceFamilies.ts`). Canvas `agent` kind maps to **community**. Meeting Notes is platform-owned → **builtin**. Reuse the existing storefront card. No one-off component, no fifth family, no `/agents` route.

**Seed:** `ensurePublishedMeetingNotesListing(db)` upserts that **single** row. Publisher `tenant_id` is a **non-demo**, non-dogfood-consumer catalog owner (first-party Agents publisher). Resolve in order: (1) tenant that already publishes other first-party `ide_agents` listings; (2) else a dedicated non-demo publisher tenant created by the seed; (3) **never** a `tenants.is_demo` fixture; **never** clone per-tenant via `provisionBuiltinAgents`.

**Do not** add `meeting_notes` to the per-tenant `provisionBuiltinAgents` seed list. Those rows are owned by the consumer tenant; `POST /agents/:id/hire` **409s** “you already own this”, which would break Hire/unhire (R-5, R-11).

Downstream callers hire through the existing `POST /agents/:id/hire` / unhire / `agent_purchases` path. No `MEETING_NOTES_ENABLED` env, no special-case MCP tool, no hardcoded id **outside** this listing row and the `MEETING_NOTES_AGENT_ID = 'meeting-notes'` constant that points at it.

### D-2 — Publisher tenant ≠ dogfood consumer (hire contract)

`POST /agents/:id/hire` refuses when `ide_agents.tenant_id === caller tenant` (own-agent 409). Unhire is a soft-delete on `agent_purchases.unhired_at`. Therefore:

- The **listing owner** (publisher) is not the dogfood tenant that receives default-on.
- Dogfood enablement is a **roster hire** (`agent_purchases` row + `provisionHiredAgent`), not ownership and not a process-env flag.
- If an environment has no publisher tenant yet, the seed creates one; it must stay `is_demo = false` and `is_dogfood = false`.

### D-3 — Runtime wrap: silent mode of `runAgentTurn` (R-2, R-3)

**One execution path.** Extend existing `runAgentTurn` in `api/src/application/meetings/meetingIntelligence.ts`. No new `callModel` / gateway helper, no parallel minutes LLM, no second agent loop.

Today `runAgentTurn` is a **speaking** participant: caption `speakerKind: 'agent'`, broadcast `agent-say`, `postChatMessage` into meeting chat. Silent capture is a **mode of that function**.

```ts
// additive; default preserves ceremony speaking agents
runAgentTurn(args, options?: { silent?: boolean })
```

When `silent === true` (Meeting Notes persona only):

| Side effect | Speaking (default) | Silent notetaker |
|---|---|---|
| LLM via existing `completeJson` / gateway | yes | yes (same gateway) |
| Participant caption row / caption feed | yes | **no** |
| `agent-say` broadcast | yes | **no** |
| Meeting-chat `postChatMessage` | yes | **no** |
| Persist notes into `meetings.summary` (replace, not append) | no | **yes** (feeds canonical minutes; see D-7) |
| Structured log / `reportCaughtError` | as today | yes |

Persona instructions (system prompt branch, not a new pipeline): produce notes from the turn; do not address participants; do not ask questions; do not offer advice; omit missing sections; never invent action-item owners. Advisees are not a control surface.

Listing enablement (active hire) is what selects this persona. Callers do not pass a private “meeting notes” feature flag.

### D-4 — Kind gate (R-4) — evaluate first

```
maybeRunMeetingNotesTurn(...)
  1. if meeting.kind !== 'advisory' → return   // R-4, before hire, before wrap
  2. if !isMeetingNotesHired(tenant) → return  // R-5 / R-11
  3. runAgentTurn(..., { silent: true })       // R-2 / R-3
```

Unknown kinds continue to coerce to `adhoc` elsewhere and are **ineligible**. Ceremony kinds (`standup`, `planning`, `retrospective`, `review`, `adhoc`, `direct`, `interview`, …) keep today’s **speaking** `runAgentTurn` behaviour for other agents. Meeting Notes is a no-op on those kinds even if hired.

**Do not** add `advisory` on this ticket. Writers of `kind=advisory` are #2536 (kind enum) and #2527 / #2532 / #2545 (Talent + `booking_services`). Until they land, the agent is correctly idle.

### D-5 — Dogfood predicate and default-on (R-5, R-11)

**No `dogfood` string exists in `api/` today.** Architect names **one** testable function, not a scattered `if`.

**Schema:** add `tenants.is_dogfood boolean NOT NULL DEFAULT false`. Check: `NOT (is_dogfood AND is_demo)` — demo fixtures and dogfood are disjoint. `tenants.is_demo` remains the sales-fixture flag `publicAgentScope` already hides. Do **not** default-on demo tenants.

```ts
export function isDogfoodTenant(t: { isDogfood: boolean; isDemo: boolean }): boolean {
  return t.isDogfood === true && t.isDemo !== true;
}
```

Until A0 (`booking_hosts.host_ref` = talent `userId`) is measurable, default-on applies to **the dogfood tenant** (every workspace user there who can organize a meeting), not a per-person allowlist. Non-dogfood tenants never get a hire from this path.

**Default-on write** (`ensureDogfoodMeetingNotesHire`):

- `INSERT INTO agent_purchases (tenant_id, agent_id) VALUES (dogfoodTenant, 'meeting-notes') ON CONFLICT (tenant_id, agent_id) DO NOTHING`.
- **Must not** clear `unhired_at`. The existing hire route’s `onConflictDoUpdate { set: { unhiredAt: null }, setWhere: isNotNull(unhiredAt) }` is **manual re-hire only**. Re-applying default-on must not resurrect an opt-out (A4, R-5).
- On a **fresh** insert only: `provisionHiredAgent` (same as hire route) so the roster identity exists.
- Idempotent: two calls → one row.

**When to run default-on:** listing seed / migration backfill of current dogfood tenants; tenant **create** with `is_dogfood`; tenant **patch** `is_dogfood` false→true. **Not** on every meeting start (would be the wrong layer and risks coupling to the revive path).

**Off path:** existing unhire (`unhired_at = now()`, `revokeHiredAgent`). Sticks across process restart and new advisory sessions. Non-dogfood: catalog may show the listing; enablement is manual Hire.

No per-meeting “enable notes” checkbox.

### D-6 — Invocation seam and failure isolation (R-8)

**Do not** send notetaker turns through `POST /:id/agent-turn`. That route **502s** when the speaking agent’s gateway fails; that contract stays for speaking agents.

**Hook:** after a **durable user caption** is persisted for the meeting (existing caption writer in meeting routes / caption service), call `maybeRunMeetingNotesTurn` in a `try/catch` that **cannot** fail the caption request, start, or end. Fire-and-forget is acceptable; if awaited, catch everything.

On notetaker / gateway failure:

- `reportCaughtError` (or equivalent) for operators
- no 502, no blocked start/end, no rolled-back reservation
- no participant error toast
- session continues without notes for that turn

Meeting **end** already auto-calls `summarizeMeeting` in try/catch and never blocks ending — keep that isolation (R-8, R-9).

### D-7 — Notes artifact and minutes contract (R-6, R-9, R-10)

- **One** current notes document per meeting: `meetings.summary` (replace in place; do not append).
- Per-turn silent capture **feeds** that column. **End-of-meeting `summarizeMeeting` remains canonical.**
- Per-turn notes are **not** posted to chat (A3). End-of-meeting chat attribution stays `{ kind: 'agent', ref: 'meeting-notes', name: 'Meeting Notes' }`.
- Published minutes headings **must remain** what code already writes and `parseMeetingMinutes` already consumes: `## Summary` / `## Decisions` / `## Action items`. Do **not** add `## Open questions` on this ticket (PO lock #2; parser work is #2540). Open questions, if captured in the silent prompt, stay inside Summary prose or are dropped — they must not break those three sections.
- Omit missing sections; do not hallucinate; do not auto-assign unstated owners.
- Confidentiality (R-10 / PRD 26 FR-7): notes inherit the advisory session **restricted** ceiling. No new confidentiality level. Listing (name, description, Hire) stays public; session work product does not leak to `guest` / `share` / `publicMedia` / `aiContext` without an explicit founder act.

### D-8 — Idempotency (R-7)

| Event | Rule |
|---|---|
| Default-on twice | One `agent_purchases` row (`ON CONFLICT DO NOTHING`) |
| Manual re-hire of an already-active hire | Existing hire route no-op (hire_count does not bump) |
| Same advisory turn processed twice | `meetings.summary` replaced, not duplicated (key = meeting id; one current document) |
| Meeting ended twice | `summarizeMeeting` must not append a second copy if `meetings.summary` was already written for that meeting (keep/extend existing best-effort guard; do not add a second writer) |

Title/session identity is the idempotency key here. Generalising `pullStandupMinutesAct` is **#2541 / #2613**, not this ticket.

### D-9 — Enablement matrix (R-11)

| Audience | On | Off |
|---|---|---|
| Dogfood tenant (host workspace) | Default hire at publish + when tenant becomes dogfood (D-5) | Same unhire as any agent; `unhired_at` sticks; default-on will not revive |
| Non-dogfood | Manual Hire on `/marketplace?family=agent` | Unhire |
| Advisee | N/A | N/A |
| Non-advisory meeting | Kind gate no-op even if hired | N/A |
| Demo tenant | Never default-on; listing excluded from public catalogue via `publicAgentScope` | N/A |

### D-10 — Module map (developer)

| Area | Path | Change |
|---|---|---|
| Silent wrap | `api/src/application/meetings/meetingIntelligence.ts` | `runAgentTurn(..., { silent?: boolean })`; skip speak side effects; replace-write `meetings.summary` when silent |
| Gate + hire + seed | `api/src/application/meetings/meetingNotesListing.ts` (**new**) | `MEETING_NOTES_AGENT_ID`, `ensurePublishedMeetingNotesListing`, `isMeetingNotesHired`, `ensureDogfoodMeetingNotesHire`, `maybeRunMeetingNotesTurn` |
| Dogfood predicate | same module or `api/src/application/tenants/isDogfoodTenant.ts` (**new**) | **One** function; used by default-on only |
| Schema | `tenants.is_dogfood` + CHECK vs `is_demo`; migration | No new listing kind; no new notes table |
| Caption hook | existing durable caption persist (meeting routes / caption service) | `try/catch` → `maybeRunMeetingNotesTurn`; do not use `POST /:id/agent-turn` |
| Hire/unhire | `workforceRoutes.ts` / `hiredAgents.ts` | **Reuse as-is**. Default-on must not call the revive `unhiredAt: null` path |
| Catalog UI | existing Agents-family public-agent card | No new route/component unless builtin chip filter needs `builtin_kind = 'meeting_notes'` mapped to `builtin` (match current filter; do not invent a card) |
| `provisionBuiltinAgents.ts` | **do not add** `meeting_notes` to per-tenant seeds | |
| `MARKETPLACE_LISTING_KINDS` / `/advisors` | **do not touch** | |

### D-11 — Test plan (developer; QA owns evidence later)

1. **Kind gate:** hired + `kind=standup|direct|review|adhoc|planning|retrospective` → zero `runAgentTurn` notetaker calls, `meetings.summary` unchanged.
2. **Advisory + hired:** silent `runAgentTurn` called; no `agent-say`, no chat post, no participant caption; notes on `meetings.summary`.
3. **Advisory + unhired:** no wrap, no notes.
4. **Dogfood default-on:** new dogfood tenant gets one hire; demo tenant does not; non-dogfood does not.
5. **Sticky unhire:** unhire then re-run `ensureDogfoodMeetingNotesHire` → still unhired (`unhired_at` intact).
6. **Idempotent seed/hire:** seed twice, default-on twice → one listing row, one purchase row.
7. **Failure isolation:** forced gateway failure in silent path → caption persist 2xx, meeting end 2xx, `reportCaughtError` called, no 502.
8. **Minutes parse:** after end, `parseMeetingMinutes` still accepts `## Summary` / `## Decisions` / `## Action items`. Ceremony summaries unchanged.
9. **Catalog:** `/marketplace?family=agent` (public agent list) includes Meeting Notes with type/family `agent`, ref `meeting-notes`, Hire CTA. Grep of the change set: notetaker turns call `runAgentTurn`; no new gateway helper.

### D-12 — Assumptions locked (do not silently invert)

- A1. Builtin chip, not community/canvas-published agent.
- A2. Listing `id` / chat `ref` = `meeting-notes`.
- A3. Per-turn silent capture feeds `meetings.summary`; `summarizeMeeting` on end is canonical; per-turn notes are not posted to chat.
- A4. Default-on = `INSERT … ON CONFLICT DO NOTHING` (never clears `unhired_at`).
- A5. Idle until #2536 + A0/A1 writers of `kind=advisory` is acceptable.
- A6. `priceCents = 0`; no public GA packaging.
- A7. Publisher tenant ≠ dogfood consumer tenant ≠ demo tenant.
- A8. Open questions are **not** a published heading on this ticket.
- A9. `advisory` kind membership is **#2536**, not this ticket.

### Out of scope (repeat, so the branch cannot “helpfully” grow)

P1 of #2538: `pullStandupMinutesAct` (#2541/#2613), follow-up slot (#2542), guest project + claim (#2543). Visible/speaking participant, live captions, in-call coaching, client notes UX, CRM, custom model pipeline, default-on for non-dogfood, multi-agent orchestration, note-quality scoring.

_— rewritten by CTO · 2026-09-16T16:35:43.393Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:50:16.297Z · execution #31694

**BA (Ada) · 2026-09-16:** Requirements R-1–R-12 authored on this ticket PRD (no product-repo patch from the BA lane). Grounded in: `MEETING_KINDS` has no `advisory` yet; `runAgentTurn` currently speaks (caption + `agent-say` + chat); `summarizeMeeting` already posts as `ref: 'meeting-notes'` with three-section Markdown and isolates end-of-meeting failure; public agents are `ide_agents` + `publicAgentScope` (demo tenant ≠ dogfood); Agents family chips are `builtin` | `community`; PRD 26 forbids adding `advisor` to `MARKETPLACE_LISTING_KINDS` and forbids `/advisors`. P1 follow-ups stay on #2538 children (#2542 follow-up slot, #2543 guest project). Architect owns Design next; developer implements on `builderforce/task-2539`, not on epic #2522.

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:51:36.141Z · execution #31693

**BA (Ada) · 2026-09-16:** Authored Requirements R-1–R-8 on this ticket PRD. Grounded in parent #2538 P0 (marketplace `agent` listing wrapping `runAgentTurn`, silent notetaker, `kind=advisory`, dogfood default-on), sibling split (#2540 parser contract, #2541–#2543 P1), #2536 `advisory` in `KINDS` only, and the composition rule: do not add `advisor` to `MARKETPLACE_LISTING_KINDS` or `/advisors`. BA lane needs **no product-repo patch**; `specs/tasks/task-2539.md` on the branch is not the apply. Architect owns Design next; developer implements on #2539 only.

---

### Update — Product Manager · 2026-09-16T16:10:40.475Z · execution #31792

**PO ready-lane lock (2026-09-16, exec this run).** Approved for Architect Design. Contract = **R-1–R-12** (exec 31694). The earlier **R-1–R-8** block (exec 31693) is superseded except as cited below.

**Corrections (do not bounce BA; implementers follow this lock):**
1. **R-4 kind ownership is VOID as “this ticket adds `advisory`.”** #2536 owns adding `advisory` to `MEETING_KINDS` / create-patch enum / `defaultTitle` / frontend kind union. #2539 owns the **gate only** (`kind === 'advisory'` → wrap; else no-op). Idle-until-#2536 is acceptable (same as A5 idle-until-A1). Do not invent a parallel kind field.
2. **R-9 headings vs code:** Canonical published minutes remain what `summarizeMeeting` already writes: `## Summary` / `## Decisions` / `## Action items`. R-9’s “Decisions / Action items / Open questions” list is **wrong vs `meetingIntelligence.ts`**. Do not fork `parseMeetingMinutes` (#2540). Open questions, if persisted, must not break those three sections. Per-turn silent capture **feeds** `meetings.summary`; end-of-meeting `summarizeMeeting` stays canonical. Per-turn notes are **not** posted to chat (A3).
3. **Listing (R-1, R-6, R-11):** marketplace `agent` on `ide_agents` + `publicAgentScope`; family=`agent`; chip=`builtin` (not canvas/community); CTA=**Hire**; `ref`=`meeting-notes` (matches existing `summarizeMeeting` poster). **Do not** add `advisor`/`meeting-notes` to `MARKETPLACE_LISTING_KINDS`, **do not** add `/advisors`, **do not** treat `category=advisors` as a listing kind.
4. **Runtime (R-2, R-3, R-8):** silent mode of existing `runAgentTurn` (no second gateway). Forbidden: `agent-say`, meeting-chat post, participant caption feed. Speaking `POST /:id/agent-turn` 502 stays for speaking agents; silent notetaker **must not** 502 the room. Failures: `reportCaughtError` (or equivalent), session continues.
5. **Default-on (R-5):** roster hire for dogfood tenant hosts (not `tenants.is_demo`). Explicit unhire sticks; re-applying default-on must not resurrect opt-out. Non-dogfood = opt-in Hire. No per-meeting checkbox.
6. **P1 out:** #2541 / #2613 `pullStandupMinutesAct`; #2542 follow-up propose; #2543 guest claim. Do not re-file.
7. **No product-repo patch from PO.** `specs/tasks/task-2539.md` is not the apply and must not ship (deleted if present). Developer implements on `builderforce/task-2539` after Architect Design — not on #2522 / #2527 / #2538.

Grounded: `runAgentTurn` currently speaks (caption + `agent-say` + chat); `summarizeMeeting` already posts `{ kind: 'agent', ref: 'meeting-notes', name: 'Meeting Notes' }` with Summary/Decisions/Action items and isolates end failure; Agents chips are `builtin` | `community`; public agents are `ide_agents` + `publicAgentScope`.

---

### Update — CTO · 2026-09-16T16:36:12.498Z · execution #31914

**Architect (CTO) · 2026-09-16T16:35Z · execution #31914.** Design D-0–D-12 authored on this ticket PRD (section replace). Contract remains PO-locked R-1–R-12. Key locks for the developer: listing = single published `ide_agents` id/`ref`=`meeting-notes`, `builtinKind=meeting_notes`, family=agent, chip=builtin, CTA=Hire, `priceCents=0`; publisher tenant ≠ dogfood ≠ demo; do **not** seed via `provisionBuiltinAgents`; silent mode of existing `runAgentTurn` (no second gateway); kind gate `=== 'advisory'` **before** hire (**#2536** adds the kind, this ticket does not); dogfood = `tenants.is_dogfood AND NOT is_demo` via one function; default-on = `INSERT … ON CONFLICT DO NOTHING` (never clears `unhired_at`); invoke from caption persist try/catch, **not** `POST /:id/agent-turn` (502 stays speaking-only); minutes headings stay `## Summary` / `## Decisions` / `## Action items`. No product-repo patch from this lane. GitHub 403 blocked re-delete of `specs/tasks/task-2539.md`; PO already removed it — if it reappears it must not merge. Developer implements on `builderforce/task-2539` next.

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T17:07:55.141Z · execution #32072

### Developer lane BLOCKED — no repo bind (execution 32072, 2026-09-16)

**Verdict: `changes_requested`. No code was written and no check was run.**

Every repository and shell tool refused this run:

| Tool | Result |
|---|---|
| `list_files` (incl. `glob: specs/tasks/task-2539.md`) | `no repository bound to this task` |
| `git_status` / `git_sync_latest` | `no repository checked out — git tools need a bound repo` |
| `run_command` | `no repository checked out — run_command needs a bound repo` |
| `project_files_list` | `404 Not found` |

Verified at the record level this run: project 11 has `githubRepoUrl`, `githubRepoOwner`, `githubRepoName`, `sourceControlIntegrationId`, `sourceControlProvider`, `sourceControlRepoFullName` and `sourceControlRepoUrl` **all `null`**. `projects.update` exposes only name/description/status/modality — **no agent run can set the bind.** This is an operator-only GitHub connect, tracked on **#2600**.

**Nothing from D-10 exists.** Not started: `api/src/application/meetings/meetingNotesListing.ts` (`MEETING_NOTES_AGENT_ID`, `ensurePublishedMeetingNotesListing`, `isMeetingNotesHired`, `ensureDogfoodMeetingNotesHire`, `maybeRunMeetingNotesTurn`); the `runAgentTurn(..., { silent?: boolean })` mode in `meetingIntelligence.ts`; the `tenants.is_dogfood` column + `NOT (is_dogfood AND is_demo)` CHECK + migration; the caption-persist `try/catch` hook. No build, type-check or test was executed — **do not read any exit-0 into this run.**

**Deliberately not done, per the locked contract:**

- No `specs/tasks/task-2539.md` stand-in. PO lock #7 and D-0 both state that docs dump is *not* the apply and must not ship; writing one to appear productive would put a file on the PR that the contract forbids. (It also cannot be deleted from here — the delete needs the same bind.)
- No Implementation Notes and no Test Evidence section. Authoring either would assert work and verification that did not happen.

**Design is not at fault.** D-0–D-12 is implementable as written; only the bind is missing.

**Next run:** do not re-dispatch a developer until #2600 lands an operator bind *and* a shell is available. This is the same failure class already recorded on #2556, #2557, #2558, #2618 and #2622 — repeated dispatches burn runs and produce nothing.

**Downstream lanes:** code-reviewer, team-lead, QA and validator have no artifact to assess. They should not sign off on absent code.

_— Developer · execution 32072_