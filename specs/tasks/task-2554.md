> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2554
> _Each agent that updates this PRD signs its change below._

# PRD 27 — Idea-validation survey loop (wiring only)

WIP product requirements for downstream agents. Implement PRD 27 idea-validation surveys as **wiring** of existing form, canvas, campaign, CRM, and collect surfaces. Do not invent a parallel survey product.

## Problem & Goal

Founders already author forms, publish them, send campaigns, keep CRM contacts, and collect responses. Idea validation still cannot close that loop: an Idea cannot host a form surface, draft test questions onto a linked form, send via the existing campaign path, pull owned CRM contacts into an audience, or fold response counts back onto the Idea card.

**Goal:** Wire B0–B5 so an Idea can run Idea → Make (form) → Run (campaign send) → Measure (collect on the card) without a new survey kind, second answer store, fifth contact store, `/surveys` destination, or second send path. Founders set `validated` / `dropped` themselves. Never auto-status.

## Target users / ICP roles

- **Founder / builder** on the Ideas canvas: drafts questions, publishes a form, sends to people, reads counts, marks the idea validated or dropped.
- **Respondent:** opens `/f/<slug>` on a phone (~360px) in either theme and submits.
- **Not in this slice:** sales reps as a second owner of contacts; marketers inventing a new campaign type.

## Scope

Wiring only against existing objects and APIs.

| Slice | Intent |
| --- | --- |
| **B0** | Form actions + `CONNECTED_CANVAS_ACTIONS`; object-scoped form surface (registry unique order, `AUTHORING_SURFACE`, host mount); `formObject` publish body + `/f/` URL; `summarizeForm` client; `CanvasFormSurface`; `peopleObjects` add close; i18n five catalogs. |
| **B1** | FR-3/5: Ask people on Ideas (disabled-not-hidden); `IDEA_EVIDENCE_KINDS` includes `form`; `idea.tested` catalog. |
| **B2** | FR-4: connect `idea.test` (draft questions from problem / segment / `riskiestAssumptions` onto the linked form). Fix `canvas_publish_form` hint. Drop or connect `idea.explore`. |
| **B3** | FR-6: Send via existing `growthApi.createCampaign` + send. `CAMPAIGN_BLOCKERS` first. No second send path. |
| **B4** | FR-7: CRM → `marketing_audience_members` with `source='crm'` only; de-dupe `(audience_id, email)`; own `sales_contacts` only (`owner_user_id`). Dedicated from-crm route. |
| **B5** | FR-8: collect folds `summarizeForm` counts onto the card. Founder sets validated/dropped manually. |
| **FR-10** | next-intl 5 locales; theme tokens; `/f/<slug>` both themes; ~360px. |
| **FR-11** | `category=new` release note + blog (Idea→Make→Run→Measure) **only if** the existing content pipeline is a small add. |
| **House** | `pnpm run check` in frontend. Isolate from the unrelated dirty tree. |

§11.7 Ideas visibility is already shipped — **leave `PHASE_SURFACES` alone.** Do not implement §11.1–11.6 / §11.8 unless a B0–B5 wire cannot land without it. Do not pick §11.4 / `sales_contacts` as user-owned canonical.

## Functional requirements

### B0 — Form surface on the canvas

1. Register form actions on `CONNECTED_CANVAS_ACTIONS`.
2. Object-scoped form surface: unique registry order, `AUTHORING_SURFACE`, mounted on the host object (Idea), not a global fifth surface.
3. Publish uses existing `formObject` publish body and existing `/f/<slug>` URL. Do not add `/surveys`.
4. Client can call `summarizeForm` for counts used later by B5.
5. `CanvasFormSurface` renders the linked form in-canvas.
6. `peopleObjects` add/close works for the people picker used by Ask people / send.
7. Copy lives in the five next-intl catalogs (same five locales as FR-10).

**Kill conditions:** `/f/<slug>` unopenable, **or** form surface not in the registry.

### B1 — Ask people on Ideas (FR-3 / FR-5)

1. Ideas expose **Ask people**. If send is blocked, the control is **disabled, not hidden**.
2. `IDEA_EVIDENCE_KINDS` includes `form`.
3. `idea.tested` catalog strings exist for the tested/evidence state.

### B2 — Draft test questions (FR-4)

1. Connect `idea.test`: draft questions onto the linked form from Idea `problem`, `segment`, and `riskiestAssumptions`.
2. Fix the `canvas_publish_form` hint so publish/send copy matches the wired form, not a missing survey dest.
3. `idea.explore`: **drop or connect** — do not leave a half-wired explore path that implies a second survey kind.

### B3 — Send (FR-6)

1. Send uses existing `growthApi.createCampaign` + existing send.
2. Evaluate `CAMPAIGN_BLOCKERS` first; blocked send stays on the disabled Ask people control (B1).
3. No second send path, no survey-specific campaign type.

### B4 — CRM into audience (FR-7)

1. Dedicated from-crm route. **Do not** reuse `POST` members (hardcoded `source: 'import'`).
2. Inserts `marketing_audience_members` with `source='crm'` only.
3. De-dupe on `(audience_id, email)`.
4. Read **own** `sales_contacts` only (`owner_user_id` = current user). `sales_contacts` is not the user-owned canonical store.

### B5 — Measure on the card (FR-8)

1. Collect folds `summarizeForm` counts onto the Idea card.
2. Founder sets `validated` / `dropped` **manually**. Never auto-advance from response counts.

### FR-10 — i18n, theme, public form

1. next-intl: five locale catalogs for all new strings (B0 catalogs included).
2. Theme tokens only; `/f/<slug>` works in both themes.
3. Public form usable at ~360px width.

### FR-11 — Launch content (conditional)

If the existing content pipeline is a small add: one `category=new` release note and one blog that narrates Idea → Make → Run → Measure. Skip if the pipeline is not a small add.

### House

- Frontend: `pnpm run check`.
- Isolate this work from the unrelated dirty tree.

## Acceptance criteria

- Form surface is in the canvas registry (unique order, `AUTHORING_SURFACE`, host-mounted). `CONNECTED_CANVAS_ACTIONS` includes form actions.
- Publishing a linked form yields a working `/f/<slug>` (both themes, ~360px). Kill if unopenable or surface missing from registry.
- Ideas show Ask people; blocked send is disabled-not-hidden. `IDEA_EVIDENCE_KINDS` includes `form`. `idea.tested` catalog present in five locales.
- `idea.test` drafts questions from problem / segment / `riskiestAssumptions` onto the linked form. `canvas_publish_form` hint is correct. `idea.explore` is either dropped or fully connected.
- Send goes through `growthApi.createCampaign` + send after `CAMPAIGN_BLOCKERS`. No alternate send API or `/surveys` dest.
- From-crm route writes `marketing_audience_members` with `source='crm'`, de-dupes `(audience_id, email)`, and only copies contacts where `owner_user_id` is the current user. Members `POST` (`source: 'import'`) is unused.
- Idea card shows `summarizeForm` counts. Status `validated` / `dropped` changes only from an explicit founder action.
- `PHASE_SURFACES` unchanged. No new survey kind, answer store, contact store, or send path.
- `pnpm run check` passes in frontend on this change set.
- FR-11 ships only when it is a small add to the existing pipeline; otherwise omitted.

## Out of scope

- New survey kind, second answer store, fifth contact store, `/surveys` destination, second send path.
- Treating §11.4 / `sales_contacts` as user-owned canonical.
- Implementing §11.1–11.6 / §11.8 unless a listed wire cannot land without it.
- Changing `PHASE_SURFACES` or redoing §11.7 Ideas visibility.
- Auto-setting idea `validated` / `dropped` from form counts or any heuristic.
- Reusing members `POST` (`source: 'import'`) for CRM sync.
- Broad CRM / campaign / form refactors unrelated to B0–B5.
- Unrelated dirty-tree files; do not mix them into this change set.
- FR-11 if the content pipeline is not a small add.

## Requirements

_Owned by the business-analyst — to be authored._

## Design

_Owned by the architect — to be authored._

## Implementation Notes

**Blocked — no readable checkout. B0–B5 not implemented. Do not merge this branch.**

Signed: Bob Developer (V2 Container) · 2026-09-16 · ticket #2554

### What this run found

- Ticket `gitBranch=builderforce/task-2554` but the developer container has **no bound repo**: `git_sync_latest`, `git_status`, `git_undo`, and `run_command` fail (`no repository bound` / `no repository checked out` / `credential delegation absent`). `list_files` / `read_file` worked briefly against a sparse overlay, then also failed (`no repository bound to this task`).
- `write_file` still commits onto the GitHub branch. That is how production-shaped files get destroyed without a way to `git show origin/main`.
- Platform `pendingChanges`: branch is **1 commit ahead of main, no PR**. `codeDelivered=true` is **false signal** from the probe, not B0–B5.
- Diff vs main (platform): `specs/tasks/task-2554.md` created; `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` **created** (not modified) — the bound main looks like a sparse/wrong tree, so a merge would *add* a stub canvas engine, not patch the real one.

### Damage to revert before any merge

- Commit **0267ffd** (`SeanHogg/Builderforce.ai@0267ffd`) wrote a 9-line probe over `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts`. Original was ~128 lines (`CanvasChangeKind`, `applyObjectChange`, create/update/delete/move). `git_undo` refused (no checkout). Follow-up tickets filed: bind-checkout gap + revert-0267ffd bug (children of #2554).

### What B0–B5 actually needs (not done)

Wiring only, against existing objects — no new survey kind, answer store, contact store, `/surveys` dest, or second send path. Leave `PHASE_SURFACES` alone.

| Slice | Touch (must READ first) |
| --- | --- |
| B0 | `CONNECTED_CANVAS_ACTIONS`; object-scoped form surface (`AUTHORING_SURFACE`, unique registry order, host-mounted on Idea); `formObject` publish + `/f/<slug>`; `summarizeForm`; `CanvasFormSurface`; `peopleObjects` add/close; five next-intl catalogs |
| B1 | Ask people on Ideas (disabled-not-hidden); `IDEA_EVIDENCE_KINDS` includes `form`; `idea.tested` catalogs |
| B2 | Connect `idea.test` (draft from `problem` / `segment` / `riskiestAssumptions`); fix `canvas_publish_form` hint; drop or fully connect `idea.explore` |
| B3 | `growthApi.createCampaign` + existing send after `CAMPAIGN_BLOCKERS` |
| B4 | Dedicated from-crm route → `marketing_audience_members` `source='crm'`, de-dupe `(audience_id, email)`, own `sales_contacts` only (`owner_user_id`). Do not reuse members POST (`source: 'import'`) |
| B5 | Fold `summarizeForm` counts onto the Idea card. Never auto-set `validated` / `dropped` |
| House | `pnpm run check` in frontend. Isolate from unrelated dirty tree. FR-11 only if content pipeline is a small add |

### Do not

- Dump conceptual survey wiring onto this branch.
- Sign off developer/`in_progress` as approved until B0–B5 is in a real checkout and `pnpm run check` passes.
- Merge `builderforce/task-2554` while 0267ffd’s stub `canvasChange.ts` is on it.

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:38:21.808Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:38:26.781Z · execution #31656

2026-09-16 Bob Developer: BLOCKED. No readable checkout of SeanHogg/Builderforce.ai for builderforce/task-2554. write_file still commits — probe commit 0267ffd destroyed canvasChange.ts (recorded as created vs sparse main). Filed child gap (bind checkout) + child bug (revert 0267ffd). B0–B5 not implemented. Do not merge. Do not dump conceptual wiring. Next run needs a full bind before any edit.

---

### Update — Cloud agent d02ff7ee-9cf2-4c44-8558-c89104f6278f · 2026-09-16T16:51:01.236Z · execution #31656

2026-09-16 Bob Developer (V2 Container) · execution #31656: STILL BLOCKED. GitHub API rate-limited on the bound repo (seanhogg/builderforce.ai) — read_file, list_files, search_code, and delete_file all return 403. CDN-side inspection (jsDelivr mirror of main) suggests B0, B1, B2, B4, and B5 are already wired — CanvasFormSurface.tsx has publish/close/summarizeForm (B0/B5), addFromCrm (B4), idea.tested catalogs exist, CONNECTED_CANVAS_ACTIONS includes form. B3 (Send via growthApi.createCampaign + send, gated on campaignBlockers) appears to be the ONLY unimplemented slice — the surface has no send button or campaignCreate call. However this is based on a CDN mirror, not the actual bound checkout, so it cannot be treated as verified. Commit 0267ffd stub of canvasChange.ts is still on the branch and must be reverted before any merge. Developer signoff: changes_requested — cannot produce a safe deliverable without a readable repo. Next run needs: (1) a working checkout, (2) revert 0267ffd, (3) verify CDN findings against real files, (4) implement B3 send path.

---

### Update — Product Manager · 2026-09-16T17:10:55.242Z · execution #32073

2026-09-16 Product Owner (Cloud agent, PO signoff e84fcbe7): VERDICT: **changes_requested**. The branch `builderforce/task-2554` (4 commits ahead of main, no PR) delivers only `specs/tasks/task-2554.md` (PRD edits). The one code artifact — `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` — was a stub at a **wrong path** (repo root is `frontend/`, not `Builderforce.ai/frontend/`). The real file at `frontend/src/domains/canvas/domain/canvasChange.ts` (138 lines) is intact on main and already contains `form: ['publish','collect','close']` and `idea: ['explore','test','reveal']` — these predate this branch. `'form'` is already in `IDEA_EVIDENCE_KINDS` on main.

The stub commit 0267ffd is now **reverted** (delete commit bb1eef6). None of B0–B5 is implemented. `codeDelivered=true` on the ticket is a false signal.

Fixes required before approval:
- **B0:** Implement `CanvasFormSurface`, wire `formObject` publish → `/f/<slug>`, `peopleObjects` add/close, i18n in 5 catalogs, register in creationObjectRegistry with `AUTHORING_SURFACE`.
- **B1:** `idea.tested` catalog strings in 5 locales. Verify `IDEA_EVIDENCE_KINDS` consumer reads the constant (not hardcoded).
- **B2:** Wire `idea.test` → draft questions from problem/segment/riskiestAssumptions onto linked form. Fix `canvas_publish_form` hint in `peopleObjects.ts`. Drop or fully connect `idea.explore`.
- **B3:** Wire send through `growthApi.createCampaign` + send after `CAMPAIGN_BLOCKERS` evaluation. No second send path.
- **B4:** Dedicated from-crm route → `marketing_audience_members` with `source='crm'`, de-dupe `(audience_id, email)`, own `sales_contacts` only.
- **B5:** Fold `summarizeForm` counts onto the Idea card. Never auto-set validated/dropped.
- **House:** `pnpm run check` must pass on the change set. Isolate from unrelated dirty tree.
- **FR-10:** next-intl 5 locales, theme tokens, `/f/<slug>` both themes at ~360px.
- **FR-11:** Skip unless content pipeline is a small add.