> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2586
> _Each agent that updates this PRD signs its change below._

# PRD: Start / Grow / Exit listing filters for Advisors

**Status:** WIP  
**Priority:** P0 (depends on #2527 stage fields)  
**Surface:** `?category=advisors` listing

## Problem & Goal

Advisor listings already store canonical stage values (`Start`, `Grow`, `Exit`) from P0 #2527, but the advisors category listing does not expose those values as filters. Seekers must scan cards or open profiles to learn whether an advisor is relevant to their stage, which hides supply, increases bounce, and under-uses the data we already persist.

**Goal:** Treat Start / Grow / Exit as first-class listing filters on `?category=advisors`, backed by the same stage fields stored in P0 #2527, so seekers can narrow the advisor inventory to the stages they care about without leaving the listing.

## Target users / ICP roles

- **Seekers** browsing advisors (founders and operators deciding who to talk to next).
- **Advisors / listing owners** whose profiles already declare one or more stages; they should not need a second data model for discovery.
- **Internal ops / growth** who need stage-qualified advisor inventory to be queryable from the public listing URL.

## Scope

- Advisors category listing only (`?category=advisors` and equivalent category routing).
- Read-only filter UI + query behavior driven by existing listing stage fields from #2527.
- URL, empty, and multi-select behavior consistent with other first-class listing filters on this surface.
- Analytics events for filter apply / clear so stage demand can be measured.

## Functional requirements

1. **Canonical values.** Filter options are exactly `Start`, `Grow`, and `Exit`, matching the stage enum stored on listings in #2527. Do not introduce aliases, extra stages, or a free-text stage filter.
2. **First-class placement.** On `?category=advisors`, Start / Grow / Exit appear as a dedicated listing filter control (same visual and interaction rank as other primary listing filters), not buried in “more filters,” chips-only, or profile-only UI.
3. **Matching.** A listing is included when **any** of its stored stage values intersects the selected filter set (OR across selected stages). Listings with no stage data are excluded when any stage filter is active.
4. **Multi-select.** Seekers may select one, two, or all three stages. Selecting all three is equivalent to “any staged advisor” and still excludes unstaged listings.
5. **URL as source of truth.** Selected stages are encoded on the listing URL (stable, shareable, back-button friendly) and restore the control on load. Deep links such as `?category=advisors` plus the stage param(s) must produce the same result set as in-UI selection.
6. **Compose with existing listing behavior.** Stage filters compose with current advisors listing sort, search, pagination, and other filters via AND across filter types. Changing stage selection resets to page 1 and refetches the listing.
7. **Empty and zero-result.** If no advisors match, show the standard listing empty state. If #2527 data is missing for a listing, it simply does not match an active stage filter; do not error the page.
8. **Category gating.** The stage filter renders only for advisors. Other categories must not show it or honor leftover stage query params as a visible control.
9. **Labels and counts (if the listing already shows facet counts).** Use the same labels as stored stages. Counts, if present on this listing, must reflect the #2527 fields and current composed query.

## Acceptance criteria

- Given an advisors listing with mixed `Start` / `Grow` / `Exit` / unstaged rows, selecting **Start** shows only listings whose #2527 stage set includes `Start`.
- Selecting **Start** and **Exit** shows listings that include Start, Exit, or both; Grow-only and unstaged listings are hidden.
- Clearing all stage selections returns the unfiltered advisors listing (other filters unchanged).
- Reloading or sharing the filtered URL restores the same selections and result set.
- Stage filter is visible and usable on `?category=advisors` at listing-filter rank; it does not appear on non-advisor category listings.
- Filter interaction does not require a profile page visit and does not write or migrate stage data.
- QA can map each visible result to the listing’s stored #2527 stage field(s) with no client-side-only matching.

## Out of scope

- Changing, adding, or backfilling stage fields (owned by #2527).
- Advisor onboarding / edit-profile UX for stages.
- New stages, custom stages, or seeker-side “I am in Start/Grow/Exit” profiling.
- Ranking, scoring, or sort changes based on stage (filter only).
- Stage filters on non-advisor categories, search global, maps, or saved-search products beyond this listing URL.
- Personalization, recommendations, or paywalling of stage-qualified supply.

## Requirements

This ticket is **compose-only, read-only filtering**. It does not write or migrate stage data (owned by P0 #2527 / #2528), does not add a listing kind, `/advisors` route, `MARKETPLACE_LISTING_KINDS` value, fifth marketplace family, SCORE/SBA affiliation, or a second stage axis.

`?category=advisors` is a **talent filter** (P0 #2529): Talent family + advisor-mode person listings. Bind the existing marketplace listing (`frontend/src/app/marketplace/MarketplacePageClient.tsx`, `CatalogToolbar`) and the existing public talent list (`GET /api/public/freelancers` / `TalentFilters` in `frontend/src/lib/freelance/talentProfile.ts`). Do not add a new list endpoint or search index.

**Sequencing.** BA/architect may complete this contract now. **Code must not start** until P2 (#2579 / #2580 — expertise, language, method, stage) is in flight **or** the P0+P1 kill condition on epic #2522 has passed, **and** P0 has persisted `freelancer_profiles.stages`. Sibling P3 tickets (#2584 badge, #2585 location) compose with this filter; they do not implement it.

Cited spec `specs/builderforce/26-implementation-notes-advisor-platform.md` may be absent on `main`. Do **not** invent a parallel contract file or `specs/tasks/task-2586.md` on this branch. Architect authors Design against the IDs below.

---

### Glossary

| Term | Meaning |
|---|---|
| **Advisor listing** | Talent `person` rows in advisor-mode, browsed at `/marketplace?category=advisors` (legacy `category` maps onto Talent; not a `FamilyId`). |
| **P0 stage field** | `freelancer_profiles.stages` — JSON `string[]`, empty `[]`. Canonical tokens **`start` \| `grow` \| `exit`** only (#2528 locked decision 4). |
| **Unstaged** | `stages` is `[]`, null, missing, or contains none of the three canonical tokens. |
| **First-class filter** | A control on the advisor listing toolbar at the same visual and interaction rank as other primary listing filters; **not** buried in “more filters,” chips-only after a drawer, or profile-only UI. |
| **Active stage filter** | One or more canonical tokens are selected (including all three). |

---

### R0 — Bind; do not fork

1. Reuse the advisors-category listing P0/P2 already own. No new package, table, CMS, or marketplace family.
2. Do **not** add `advisors` to `MARKETPLACE_LISTING_KINDS` or `FamilyId`. Do **not** add `advisory` to `PEOPLE_OBJECT_KINDS`.
3. Do **not** map this filter onto company `BUSINESS_STAGES` (`idea` · `mvp` · `early_revenue` · `growth` · `scale` in `startupListing.ts`) or `FUNDING_STAGES`. Those describe the founder company. Advisor stage is the P0 trio.
4. P2 (#2579 R1) currently binds query key `stage` to `BUSINESS_STAGES`. **This ticket overrides that row** for `?category=advisors`: `stage` filters `freelancer_profiles.stages` with tokens `start` \| `grow` \| `exit` only. P3 does **not** add a second stage widget or a second query key. If P2 ships a `stage` `<Select>` of BUSINESS_STAGES first, this ticket **replaces** that control with the first-class Start / Grow / Exit chips below — it does not sit beside it.
5. GitHub API 403 on this run: do not dump a docs-only `specs/tasks/task-2586.md` as a stand-in.

---

### R1 — Canonical values and labels

| Storage token (query + JSON) | Visible label (en) |
|---|---|
| `start` | Start |
| `grow` | Grow |
| `exit` | Exit |

1. Options are **exactly** those three. No aliases (`pre-seed`, `seed`, `series-a`, `idea`, `mvp`, `growth`, `scale`, `startup`, `scale-up`), no free-text stage box, no SCORE-branded synonyms.
2. P0 writes lowercase tokens. Filter matching is **case-insensitive after trim**. Unknown stored strings are ignored (they do not make a listing “staged”).
3. Unknown or empty query tokens are **ignored** (treated as unset). Do not 400 the listing page.
4. i18n: chip labels are catalog keys in the existing `marketplace` / `talent` namespace, all **five locales**, light and dark. Do not hardcode English in the toolbar beyond existing talent patterns.

---

### R2 — First-class placement and category gate

1. On `?category=advisors` (and equivalent P0 routing, e.g. `family=talent` + that category), render **Start**, **Grow**, and **Exit** as three first-class toolbar chips/toggles — same rank as other primary listing filters (P2 expertise / language / method, existing `discipline` / `sort`). Logged-out browse required.
2. Interaction: each chip is a toggle button with accessible pressed state. Keyboard operable. Do not rely on colour alone.
3. This is a **closed 3-value multi-select**, not a new generic taxonomy multi-select widget. P2’s “single-select `<Select>`” rule does **not** apply to this control; the PM contract for this ticket is multi-select.
4. When `category` is absent or any value other than `advisors`:
   - Do **not** render the three chips.
   - Do **not** apply leftover `stage` query params to talent, agents, skills, knowledge, companies, or publish grids (ignore; no chips; no empty/wrong grids; do not 400).
5. The control does not appear on the advisor **profile** as the way to filter the listing. Filter interaction must not require a profile visit.

---

### R3 — Matching (server-authoritative)

1. A listing is included when **any** of its stored P0 `stages` tokens intersects the selected filter set (**OR** across selected stages).
2. **Unstaged listings are excluded** whenever the stage filter is active (one, two, or all three chips on).
3. Selecting **all three** is “any staged advisor”: listings whose `stages` contain at least one of `start`/`grow`/`exit`. It is **not** equivalent to clearing the filter. Clearing all chips (R5) returns the unfiltered-by-stage set, **including unstaged**.
4. Examples (given mixed inventory):
   - `stage=start` → only rows whose `stages` include `start` (may also include grow/exit). Grow-only, exit-only, and unstaged are hidden.
   - `stage=start` + `stage=exit` (or `stage=start,exit`) → rows that include Start, Exit, or both. Grow-only and unstaged are hidden.
   - no `stage` param → unfiltered-by-stage (unstaged included).
5. Matching runs **on the server** as part of the existing public talent list, **only when** `category=advisors`. Client-side AND over the current page is insufficient (breaks counts, pagination, and shared URLs). QA must be able to map each visible card to that listing’s stored `#2527` `stages` field with no client-only matching.
6. If the P0 column is missing for a row (migration not applied, null, `[]`), that row simply does not match an active stage filter. **Do not error the page.**

---

### R4 — URL as source of truth

1. Query key: **`stage`** (singular), shared with P2’s stage axis — not a new `stages` / `journey` / `fit` key.
2. Values: `start` \| `grow` \| `exit`. Architect picks **one** serialization and uses it everywhere: repeatable `stage=start&stage=exit` **or** comma-separated `stage=start,exit`. Order-independent. Duplicate tokens collapse.
3. Reload, share, and back/forward restore the same selections and result set. Deep links `?category=advisors` plus the stage param(s) produce the same result set as in-UI selection.
4. Changing stage selection **resets `page` to 1** and refetches (same as other marketplace filters).
5. Clearing all stage chips **removes** `stage` from the URL (no `stage=` empty residue). Keep `category=advisors` and every other filter key.
6. Invalid tokens are dropped from the effective set; if none remain valid, treat as unset (R3.4 third bullet).

---

### R5 — Composition with existing listing behavior

1. Stage **AND**s with other filter types: P2 expertise / language / method, P3 location (#2585) when present, existing talent `q`, `discipline`, `sort`, pagination.
2. Stage does **not** change ranking or sort. Filter only.
3. Result count and zero-result copy follow the **standard listing empty state** already used by marketplace/talent (`CatalogToolbar` `resultCount`, existing empty). Controls stay visible when the intersection is empty. No SCORE or “become a mentor” empty marketing.
4. **Facet counts:** if this listing **already** shows per-option counts, those counts must reflect P0 `stages` under the current composed query (other filters applied; the stage facet’s own selection follows existing marketplace facet-count semantics). If the listing does **not** already show facet counts, do not add a count product in this ticket.
5. Auth: filters and results work **logged-out**. Do not newly require `auth: 'web'` / tenant JWT.

---

### R6 — Analytics (events only)

1. On apply (chip on) and clear (chip off / clear-all of this facet), emit the **same marketplace filter analytics events** already used by other listing filters, with facet id `stage` and the selected token set.
2. If the listing has **no** existing filter-apply/clear events, do **not** invent a new analytics pipeline, dashboard, or SEO landing page (P3 R9.6). The filter still ships.
3. No SCORE/SBA properties on the event.

---

### R7 — Cross-cutting

- **Theme:** existing tokens only. No SCORE palette.
- **a11y:** chips = toggle buttons with pressed state; do not use colour as the only selected affordance.
- **NF:** no new tables; no write path on `stages`; no booking/payment/availability changes; no N+1 per card to evaluate stage.
- **Regression:** non-advisor categories remain query-compatible.

---

### R8 — Out of scope / anti-goals

1. Changing, adding, or backfilling `freelancer_profiles.stages` (P0 #2527 / #2528).
2. Advisor onboarding / edit-profile UX for stages.
3. New stages, custom stages, or seeker-side “I am in Start/Grow/Exit” profiling.
4. Ranking, scoring, or sort changes based on stage.
5. Stage filters on non-advisor categories, global search, maps, or saved-search products beyond this listing URL.
6. Personalization, recommendations, or paywalling of stage-qualified supply.
7. A second stage control bound to `BUSINESS_STAGES` / `FUNDING_STAGES`.
8. Pro-bono / unpaid **facet** (badge is #2584; not a filter here).
9. Location filter (sibling #2585).
10. Docs-only `specs/tasks/task-2586.md` as the deliverable.

---

### Acceptance traceability

| PM criterion | Req |
|---|---|
| Select **Start** → only listings whose P0 `stages` include `start` | R3.1, R3.4 |
| Select **Start** and **Exit** → Start, Exit, or both; Grow-only and unstaged hidden | R3.1–R3.4 |
| Clear all stage selections → unfiltered-by-stage (other filters unchanged) | R4.5, R3.4 |
| Reload / share filtered URL restores selections and result set | R4.3 |
| Visible and usable on `?category=advisors` at listing-filter rank; absent on other categories | R2 |
| No profile visit; no write/migrate of stage data | R0, R2.5, R8.1 |
| QA maps each visible result to stored #2527 `stages` (server-side) | R3.5 |
| Missing P0 data → no match, no page error | R3.6 |
| All three selected still excludes unstaged | R3.2–R3.3 |
| Compose AND; page resets to 1 | R4.4, R5.1 |

---

### Decisions locked by BA (do not re-open in Design without a PRD change)

1. **Tokens = P0 `start` \| `grow` \| `exit`.** Not `BUSINESS_STAGES`, not `FUNDING_STAGES`, not SCORE branding.
2. **One axis, query key `stage`.** This ticket **promotes** P2’s stage facet to first-class multi-select chips; it does not add `stages` / `journey`.
3. **OR across selected chips; AND across filter types.** Unstaged excluded iff any chip is on. All-three ≠ cleared.
4. **Server-side** on the existing public talent list, advisors category only.
5. **No write path.** Display + filter of #2527 data only.
6. **P2 R1 stage row is wrong against #2528 / #2583 R5.** Follow-up gap filed so #2580 does not ship BUSINESS_STAGES against `freelancer_profiles.stages`.

_— authored by Ada (business-analyst) · 2026-09-16 · execution #31735_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:56:42.227Z_

## Design

## Design

_Authored by Architect · 2026-09-16 · execution TBD · lane `ready`_

### D-0 — Data dependency (read-only)

`freelancer_profiles.stages` **does not exist** on `main` (0 repo-wide matches as of 2026-09-16). This Design assumes it lands as a `jsonb` column defaulting `'[]'` with canonical tokens `start` / `grow` / `exit` per #2528 R-4. Until that column ships, the server predicate is a no-op (no rows match an active filter) and the frontend filter is gated behind a feature flag or absent.

**Schema addition** (owned by #2528, repeated here for completeness):
```sql
ALTER TABLE freelancer_profiles
  ADD COLUMN IF NOT EXISTS stages jsonb NOT NULL DEFAULT '[]';
```

### D-1 — URL serialization: comma-separated

**Choice:** `stage=start,grow` (comma-separated), not repeated `stage=start&stage=grow`.

**Rationale:** The existing talent `listFreelancers` call site already uses `URLSearchParams` with `.set(key, value)` — comma-separated values are one `.set()` call; repeated keys require `.append()` + array-from-`.getAll()`. One `.set()` is the simpler diff against `TalentFilters`. Tokens are order-independent; duplicates collapse to a `Set` before encoding.

- `?category=advisors&stage=start` → one chip pressed
- `?category=advisors&stage=start,exit` → two chips pressed
- No `stage` param → unfiltered (unstaged included)
- `?category=advisors&stage=` (empty value) → treated as unset
- `?category=advisors&stage=idea,scale` (all invalid) → treated as unset
- `?category=talent&stage=start` → ignored (category gate; no chips rendered, no predicate applied)

### D-2 — Frontend bindings

| Concern | Binding | Change |
|---------|---------|--------|
| Chips component | `frontend/src/components/FilterChips.tsx` (`FilterChip` interface: `id`, `label`, optional `count`) | Reuse; no new chip widget |
| Mount point | `frontend/src/app/marketplace/MarketplacePageClient.tsx` → `CatalogToolbar` slot | Add `<FilterChips>` row for `?category=advisors` only |
| Chip data | `frontend/src/lib/freelance/talentProfile.ts` → `ADVISOR_STAGE_CHIPS` constant | New constant: `[{id:'start',label:t('start')},{id:'grow',label:t('grow')},{id:'exit',label:t('exit')}]` |
| Query interface | `frontend/src/lib/freelance/talentProfile.ts` → `TalentFilters` | Add `stage?: string` (comma-separated or undefined) |
| List call | `frontend/src/lib/freelance/talentProfile.ts` → `listFreelancers()` | Append `stage` to `URLSearchParams` when truthy |
| Category gate | `MarketplacePageClient.tsx` → existing `useSearchParams()` category read | Only render chips when `category === 'advisors'` |
| URL sync | `MarketplacePageClient.tsx` → existing `useRouter().replace()` pattern | `stage` param set/removed; `page` reset to 1 on change |
| Analytics | Existing marketplace filter event (if present) | Emit with `facetId: 'stage'`, `tokens: string[]` on apply/clear |

### D-3 — Server predicate (API)

| Concern | Binding | Change |
|---------|---------|--------|
| Query interface | `api/src/presentation/routes/freelancerRoutes.ts` → `TalentFilters` | Add `stage?: string` |
| Route parse | `GET /api/public/freelancers` handler in `freelancerRoutes.ts` | Parse `stage` query param, split on `,`, trim, lowercase, filter to canonical set `{start,grow,exit}` |
| Predicate | Within the existing `listFreelancers` DB query (same file or its service) | **Only when `category` is `advisors` AND parsed stage set is non-empty:** add a WHERE clause using Postgres `jsonb` overlap operator `freelancer_profiles.stages ?| ARRAY[$1, $2, $3]` on the parameterized canonical tokens. Unstaged rows (`[]`) naturally fail the overlap check. |
| Category guard | Server must know the listing is advisors. Pass `category` query param through (already available) or derive from existing talent-category routing. Stage predicate is **not** applied for non-advisor categories. |
| All-three = any staged | The overlap operator naturally handles this: `stages ?| ARRAY['start','grow','exit']` matches any row with at least one canonical token. |
| Missing column safety | If `freelancer_profiles.stages` does not exist at query time, the predicate is skipped (no error). This Design cannot add the column — #2528 owns it. |

### D-4 — i18n keys

Namespace: existing `marketplace` / `talent` (five locales: en, es, fr, de, ja). Add:

| Key | en default |
|-----|------------|
| `advisors.filter.stage.start` | `Start` |
| `advisors.filter.stage.grow` | `Grow` |
| `advisors.filter.stage.exit` | `Exit` |

Chip labels read from these keys in the existing `useTranslations()` hook pattern already used throughout `MarketplacePageClient`. No hardcoded English strings in components.

### D-5 — Category gate (frontend + server)

**Frontend:** In `MarketplacePageClient.tsx`, the stage chips render only when `category === 'advisors'`. When category is absent or any other value, the chips do not render and the `stage` query param is not read from or written to the URL by this control. Leftover `stage` in the URL on non-advisor categories is simply ignored (no chips, no empty grid, no 400).

**Server:** The stage predicate (`?|` overlap check) is only appended to the freelancer query when the request is scoped to advisors. Implementation: check the existing `category` parameter on the request, or pass a `category` field through `TalentFilters`. If `category !== 'advisors'` or category is absent, skip the stage predicate entirely.

### D-6 — Accessibility

Each chip is a `<button>` with `role="button"` and `aria-pressed={selected}`. The `FilterChips` component already supports `pressed`/`selected` state — verify its `aria-pressed` binding. Selected state is communicated through both color AND a visual indicator (existing `FilterChips` pattern: filled background + checkmark or border style). No color-only affordance.

### D-7 — Composition with sibling filters

Stage ANDs with all other filter types (expertise, language, method, discipline, search `q`, location when #2585 ships, sort). Stage does NOT affect ranking or sort order. Changing stage selection resets `page` to 1 via the existing pagination reset pattern in `MarketplacePageClient`.

### D-8 — Zero-result and empty state

When the stage filter + other filters produce zero results, the standard marketplace/talent empty state renders (existing `CatalogToolbar` result count + existing empty component). All controls — including the stage chips — remain visible so the seeker can clear the filter. No SCORE-branded or "become a mentor" marketing copy.

### D-9 — What this Design does NOT do

- Does NOT add `freelancer_profiles.stages` column (#2528 owns it)
- Does NOT add `advisors` to `MARKETPLACE_LISTING_KINDS` or `FamilyId`
- Does NOT create a new `/advisors` route
- Does NOT create a new API endpoint (binds to existing `GET /api/public/freelancers`)
- Does NOT add a second stage widget or `BUSINESS_STAGES` control
- Does NOT write or migrate stage data
- Does NOT add facet counts (unless the listing already shows them)
- Does NOT create a new analytics pipeline (reuses existing filter events if present)
- Does NOT add a `specs/tasks/task-2586.md` file

### D-10 — Developer implementation sequence (informational)

1. Wait for #2528 merge (column exists) → unblock.
2. Add `stage?: string` to `TalentFilters` (frontend + API).
3. Add server predicate: parse `stage`, validate against canonical set, append `?|` overlap WHERE clause **only** for advisors category.
4. Add `ADVISOR_STAGE_CHIPS` constant + i18n keys.
5. In `MarketplacePageClient.tsx`: when `category === 'advisors'`, read `stage` from searchParams, render `<FilterChips>` with three toggle chips.
6. Wire chip toggle → URL update (set/remove `stage`, reset page to 1).
7. Wire analytics events if filter-event pipeline exists.
8. Verify: non-advisor categories ignore `stage` param; clearing all chips removes `stage` from URL; all-three vs cleared produces correct results; missing P0 data → no match, no error.

### D-11 — URL serialization example (informational)

```
# One chip selected
/marketplace?category=advisors&stage=start

# Two chips selected
/marketplace?category=advisors&stage=start,exit

# All three (any staged advisor)
/marketplace?category=advisors&stage=start,grow,exit

# Cleared (no stage param — unstaged included)
/marketplace?category=advisors

# Non-advisor (stage param ignored; chips not rendered)
/marketplace?category=talent&stage=start
```

_— rewritten by CTO · 2026-09-16T17:01:23.394Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:57:57.058Z · execution #31735

**BA (Ada), 2026-09-16 — Requirements authored for #2586 (execution #31735).** Locked: P0 `freelancer_profiles.stages` tokens `start|grow|exit` (not `BUSINESS_STAGES` / `FUNDING_STAGES`); query key `stage` (shared with P2 — this ticket **promotes** that axis to first-class multi-select chips, does not add a second widget); OR across selected chips; unstaged excluded when any chip is on; selecting all three ≠ clearing; server-side match on existing public talent list, `?category=advisors` only; no write/migrate. P2 #2579 R1 stage row is wrong against #2528 — gap **#2637** filed to remap #2580. No product-repo patch; GitHub 403; do not ship `specs/tasks/task-2586.md`. Code still gated on P2 in flight or P0+P1 kill + P0 `stages` persisted.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:22:35.167Z · execution #31874

## Product lock — Kevin (Product Manager) · 2026-09-16 · execution #31875 · lane todo

**Status: Product-locked.** BA Requirements **R0–R8**, glossary, locked decisions 1–6, and the acceptance-traceability table are adopted **unchanged** as the contract. This PM run does not reopen them. Architect owns Design. No product-repo patch this lane. No `specs/tasks/task-2586.md`.

This is an **independent** product-lock of the same BA contract (sibling PM run holds exclusive `prd` for a parallel freeze). It does **not** rewrite R0–R8.

### What this slice is

Compose-only, read-only **Start / Grow / Exit** listing filters on `?category=advisors`. First-class toolbar chips (closed 3-value multi-select) bound to P0 `freelancer_profiles.stages` tokens `start | grow | exit`. Query key **`stage`** (singular) — this ticket **promotes** P2’s stage axis to chips; it does **not** add `stages` / `journey` / `fit`, a second widget, or `BUSINESS_STAGES`. Server-authoritative OR-across-chips, AND-across-filter-types, on the existing public talent list. No write/migrate of stage data.

### P0 — Bind; do not fork (R0, R8)

- Reuse the advisors-category listing P0/P2 already own (`frontend/src/app/marketplace/MarketplacePageClient.tsx`, `CatalogToolbar`; `GET /api/public/freelancers` / `TalentFilters` in `frontend/src/lib/freelance/talentProfile.ts`). No new package, table, CMS, list endpoint, search index, or marketplace family.
- Do **not** add `advisors` to `MARKETPLACE_LISTING_KINDS` or `FamilyId`. Do **not** add `advisory` to `PEOPLE_OBJECT_KINDS`. Do **not** ship `/advisors`.
- Do **not** map this filter onto company `BUSINESS_STAGES` (`idea` · `mvp` · `early_revenue` · `growth` · `scale`) or `FUNDING_STAGES`. Those describe the founder company. Advisor stage is the P0 trio.
- **#2580 already locked the same axis.** PM product-lock exec **#31858** on #2580: stage = `start | grow | exit`, not `BUSINESS_STAGES`. Gap **#2637**’s remap is **satisfied on the #2580 PRD**; #2637 does not implement; #2586 does not sit beside a BUSINESS_STAGES `<Select>`. If a P2 build still ships BUSINESS_STAGES against `freelancer_profiles.stages`, that is a #2580/#2637 defect — not a second stage control here.
- Bound repo `seanhogg/builderforce.ai` is the right product repo. This PM slot does not implement. Project 11 `githubRepoUrl` is still null; GitHub list/search this run is 403-class (same observational bind as #2584/#2580/#2528). `task-2586.md` is **absent on main** — do not dump it.
- Parent: epic **#2583** (P3). This ticket owns **only** the Start/Grow/Exit **listing filter**. Sibling **#2584** owns the pro-bono card badge (not a facet here). Sibling **#2585** owns location.

### P1 — Canonical values and labels (R1)

| Storage token (query + JSON) | Visible label (en) |
|---|---|
| `start` | Start |
| `grow` | Grow |
| `exit` | Exit |

- Options are **exactly** those three. No aliases (`pre-seed`, `seed`, `series-a`, `idea`, `mvp`, `growth`, `scale`, `startup`, `scale-up`), no free-text, no SCORE-branded synonyms.
- P0 writes lowercase. Matching is **case-insensitive after trim**. Unknown stored strings are ignored (they do not make a listing “staged”).
- Unknown or empty query tokens are **ignored** (treated as unset). Do not 400 the listing page.
- i18n: chip labels are catalog keys in the existing `marketplace` / `talent` namespace, all **five locales**, light and dark. Do not hardcode English in the toolbar beyond existing talent patterns.

### P2 — First-class placement and category gate (R2)

- On `?category=advisors` (and equivalent P0 routing, e.g. `family=talent` + that category), render **Start**, **Grow**, and **Exit** as three first-class toolbar chips/toggles — same rank as other primary listing filters (P2 expertise / language / method, existing `discipline` / `sort`). Logged-out browse required.
- Each chip is a toggle button with accessible pressed state. Keyboard operable. Do not rely on colour alone.
- This is a **closed 3-value multi-select**. P2’s “single-select `<Select>`” rule does **not** apply to this control.
- When `category` is absent or any value other than `advisors`: do **not** render the chips; do **not** apply leftover `stage` query params to talent, agents, skills, knowledge, companies, or publish grids (ignore; no chips; no empty/wrong grids; do not 400).
- The control does not appear on the advisor **profile** as the way to filter the listing. Filter interaction must not require a profile visit.

### P3 — Matching, server-authoritative (R3)

- A listing is included when **any** of its stored P0 `stages` tokens intersects the selected filter set (**OR** across selected stages).
- **Unstaged listings are excluded** whenever the stage filter is active (one, two, or all three chips on). Unstaged = `stages` is `[]`, null, missing, or contains none of the three canonical tokens.
- Selecting **all three** is “any staged advisor”: listings whose `stages` contain at least one of `start`/`grow`/`exit`. It is **not** equivalent to clearing the filter. Clearing all chips returns the unfiltered-by-stage set, **including unstaged**.
- Examples: `stage=start` → only rows whose `stages` include `start` (may also include grow/exit). `stage=start` + `stage=exit` (or `stage=start,exit`) → Start, Exit, or both; Grow-only and unstaged hidden. No `stage` param → unfiltered-by-stage (unstaged included).
- Matching runs **on the server** as part of the existing public talent list, **only when** `category=advisors`. Client-side AND over the current page is insufficient. QA maps each visible card to that listing’s stored `#2527`/`#2528` `stages` field with no client-only matching.
- If the P0 column is missing for a row (migration not applied, null, `[]`), that row simply does not match an active stage filter. **Do not error the page.**

### P4 — URL as source of truth (R4)

- Query key: **`stage`** (singular). Values: `start` \| `grow` \| `exit`.
- Architect picks **one** serialization and uses it everywhere: repeatable `stage=start&stage=exit` **or** comma-separated `stage=start,exit`. Order-independent. Duplicate tokens collapse. (Align with #2580’s existing marketplace convention if one is already chosen in Design.)
- Reload, share, and back/forward restore the same selections and result set.
- Changing stage selection **resets `page` to 1** and refetches.
- Clearing all stage chips **removes** `stage` from the URL (no `stage=` empty residue). Keep `category=advisors` and every other filter key.
- Invalid tokens are dropped from the effective set; if none remain valid, treat as unset.

### P5 — Composition, empty, a11y (R5, R7)

- Stage **AND**s with other filter types: P2 expertise / language / method, P3 location (#2585) when present, existing talent `q`, `discipline`, `sort`, pagination.
- Stage does **not** change ranking or sort. Filter only.
- Result count and zero-result copy follow the **standard listing empty state** (`CatalogToolbar` `resultCount`, existing empty). Controls stay visible when the intersection is empty. No SCORE or “become a mentor” empty marketing.
- **Facet counts:** if this listing **already** shows per-option counts, those counts must reflect P0 `stages` under the current composed query. If it does **not**, do not add a count product in this ticket.
- Auth: filters and results work **logged-out**. Do not newly require `auth: 'web'` / tenant JWT.
- Theme: existing tokens only. No SCORE palette. Chips = toggle buttons with pressed state; colour is not the only selected affordance.
- NF: no new tables; no write path on `stages`; no booking/payment/availability changes; no N+1 per card to evaluate stage. Non-advisor categories remain query-compatible.

### P6 — Analytics (R6)

- On apply (chip on) and clear (chip off / clear-all of this facet), emit the **same marketplace filter analytics events** already used by other listing filters, with facet id `stage` and the selected token set.
- If the listing has **no** existing filter-apply/clear events, do **not** invent a new analytics pipeline, dashboard, or SEO landing page. The filter still ships.
- No SCORE/SBA properties on the event.

### Sequencing vs #2528 / P2 / P3 build gate (not a Design start-gate)

| Ticket | Role vs this freeze |
|--------|---------------------|
| **#2528** | Hard **data owner** of `freelancer_profiles.stages` (`start \| grow \| exit`; empty `[]`). Observed 2026-09-16: still `todo`, branch `builderforce/task-2528` (unmerged, **no PR**). |
| **#2527** | Advisors category listing surface (P0). Observed: `blocked`. Do **not** re-dispatch from this ticket. |
| **#2579** P2 | Parent #2583 Build gate: P3 **code** must not start until P2 is in flight (open PR) **or** the P0+P1 kill on epic #2522 has passed (#2527, #2532, #2538). #2579 is `ready`, 3 commits, **no PR** — **not** “in flight”. |
| **#2580** | P2 filters including the `stage` axis. Product-locked exec **#31858**; stage already `start \| grow \| exit`. This ticket **promotes** that axis to first-class chips. |

- This PM freeze and **Architect Design are not blocked** on #2528 merge or on P2.
- **Developer implementation is blocked** until (a) `freelancer_profiles.stages` exists on the public talent list payload and (b) the parent P3 Build gate is green. Then report blocked; do **not** substitute `BUSINESS_STAGES`, client-only matching, or a new list endpoint.
- `builderforce/task-2528`, `builderforce/task-2579`, and `builderforce/task-2583` are **not** this ticket’s implementation branch. Implement on this ticket’s own branch cut from default/`main`.
- No finish-to-start edge added from this PM lane (OR kill-path on #2579; data FTS on #2528 is already implied by #2580/#2583). Live write/migrate of `stages` is **forbidden** here.

### Non-goals (must not ship here)

| Item | Owner / reason |
|------|----------------|
| Creating / persisting / backfilling `freelancer_profiles.stages` | #2527 / #2528 |
| Advisor onboarding / edit-profile UX for stages | #2528 |
| New stages, custom stages, seeker-side “I am in Start/Grow/Exit” | out of scope |
| Ranking / scoring / sort by stage | filter only |
| Stage filters on non-advisor categories, global search, maps, saved-search | out of scope |
| A second stage control bound to `BUSINESS_STAGES` / `FUNDING_STAGES` | forbidden; #2580 R-6 + #2637 |
| Pro-bono / unpaid **facet** | badge is #2584; not a filter here |
| Location filter | sibling #2585 |
| `/advisors`, `MARKETPLACE_LISTING_KINDS` `"advisor"`, `PEOPLE_OBJECT_KINDS` advisory | forbidden |
| `specs/tasks/task-2586.md` | do not ship |
| Product-repo patch from this PM lane | do not ship |

Priority stays **medium** on the board. ICP = seekers browsing advisors who need to narrow by Start / Grow / Exit without opening profiles.

### Handoff from this lane

- **Architect (Design):** three first-class chips on `CatalogToolbar` for `?category=advisors` only; URL serialization for `stage`; server predicate on existing `GET /api/public/freelancers` (OR-across tokens; unstaged excluded iff active; all-three ≠ clear); category gate that ignores leftover `stage` on other categories; i18n keys; a11y pressed state. Do not invent a new list endpoint, family, or widget library.
- **PO (ready):** accept R0–R8 + P0–P6.
- **Developer:** this ticket’s own branch; read-only filter; no write path; no client-only page filter. Blocked until sequencing (a)+(b) above.
- **QA:** AC matrix — Start only; Start+Exit; clear-all restores unstaged; URL round-trip; chips absent off-advisors; leftover `stage` ignored off-advisors; all-three still excludes unstaged; missing P0 data → no match, no page error; server-side map to stored `stages`.

_— Kevin (Product Manager) · 2026-09-16 · execution #31875 · lane `todo`_

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:24:32.247Z · execution #31875

**Product lock.** BA R0–R8 adopted unchanged as the implementation contract.

**P0 — Bind; do not fork.** Compose-only, read-only filtering on the existing advisors-category listing. Bind `frontend/src/app/marketplace/MarketplacePageClient.tsx` + `CatalogToolbar` and `GET /api/public/freelancers` / `TalentFilters` in `frontend/src/lib/freelance/talentProfile.ts`. No new list endpoint, search index, package, table, CMS, `/advisors` route, `MARKETPLACE_LISTING_KINDS` value, `FamilyId`, or `PEOPLE_OBJECT_KINDS` `advisory`.

**P1 — One stage axis, P0 tokens.** Query key `stage` (singular). Storage + query tokens `start` \| `grow` \| `exit` only (labels Start / Grow / Exit). Not `BUSINESS_STAGES`, not `FUNDING_STAGES`, not SCORE/SBA synonyms. This ticket **promotes** P2 #2580’s stage facet to first-class multi-select chips; it does **not** add a second widget or a second query key (`stages` / `journey` / `fit`). If P2 still ships a `stage` `<Select>` of BUSINESS_STAGES, this ticket **replaces** that control — it does not sit beside it. #2580 PM lock (exec 31858) already remapped stage to `start|grow|exit`; #2637 remains the vehicle to correct parent #2579 Decision 1. Do not re-open #2579 from this child.

**P2 — First-class chips, advisors only.** On `?category=advisors` (and equivalent P0 routing), render Start / Grow / Exit as three toolbar toggle chips at the same rank as other primary listing filters. Closed 3-value multi-select (P2’s single-select `<Select>` rule does not apply). Logged-out browse required. When `category` is absent or not `advisors`: no chips; leftover `stage` params are ignored (no empty/wrong grids, no 400). Filter interaction must not require a profile visit.

**P3 — Server-authoritative match.** OR across selected chips; AND across filter types. Unstaged listings (`[]` / null / missing / no canonical token) are excluded whenever any chip is on. Selecting all three = “any staged advisor”, **not** equivalent to clearing. Matching runs on the existing public talent list, advisors category only. Client-side AND over the current page is insufficient. Missing P0 data → no match, no page error. No write/migrate of `freelancer_profiles.stages` (owned by #2527 / #2528).

**P4 — URL + composition.** URL is source of truth. Architect picks **one** serialization (repeatable `stage=start&stage=exit` **or** comma-separated `stage=start,exit`) and uses it everywhere. Order-independent; duplicates collapse; unknown tokens dropped. Changing stage resets `page` to 1. Clearing all chips **removes** `stage` from the URL. Stage does not change ranking or sort. Facet counts only if this listing already shows them. Empty state = standard listing empty; controls stay visible.

**P5 — Analytics events only.** Reuse existing marketplace filter apply/clear events with facet id `stage`. If none exist, do not invent a pipeline. No SCORE/SBA properties.

**P6 — Gate and ownership.** Architect owns Design. **No product-repo patch this lane.** Do not dump `specs/tasks/task-2586.md`. `githubRepoUrl` is still null on project 11; bound checkout `seanhogg/builderforce.ai` has 0 matches for `task-2586` / `MarketplacePageClient.tsx`. **Code must not start** until P2 (#2579 / #2580) is in flight **or** the P0+P1 kill on epic #2522 (#2527 / #2532 / #2538) has passed, **and** P0 has persisted `freelancer_profiles.stages`. Observed 2026-09-16: #2579/#2580 `ready` (not in flight); #2527/#2532/#2538 `blocked`; kill not green.

**Siblings (compose, do not implement here):** #2584 pro-bono badge; #2585 location filter; #2580 expertise / language / method (+ stage query binding). Data dep: #2528.

**Status:** product-locked. BA decisions 1–6 remain closed.

_— Kevin (Product Manager) · 2026-09-16 · execution #31875_