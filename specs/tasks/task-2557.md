> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2557
> _Each agent that updates this PRD signs its change below._

# PRD: packages/brain-ui — Test then Dist

## Problem & Goal

`packages/brain-ui` must be proven green and then emitted as a consumable build artifact. Downstream agents and packages cannot rely on this UI package until unit/integration tests pass and the `dist` output is produced from a clean, successful test run.

**Goal:** Run `pnpm test` in `packages/brain-ui`, require a passing result, then produce `dist` (package build / publishable output) from that same tree. Stop on test failure; do not ship or leave a stale `dist` as success.

## Target users / ICP roles

- Monorepo maintainers and CI operators who gate merges on package health
- Downstream package/app consumers that import `@brain-ui` (or equivalent) from `dist`
- Agent runners executing this WIP task without product-owner reinterpretation

## Scope

- Working directory: `packages/brain-ui`
- Package manager: `pnpm`
- Sequence: **test first, dist second**
- Success means tests pass and `dist` is generated (or refreshed) afterward
- Fail-fast: any test failure aborts; do not run or claim dist success

## Functional requirements

1. Resolve and use the package’s existing `pnpm` scripts and lockfile; do not invent a parallel toolchain.
2. Execute `pnpm test` inside `packages/brain-ui` (or the repo-equivalent filter that targets only this package).
3. Treat a non-zero test exit code as task failure. Do not continue to dist.
4. After a passing test run, execute the package dist/build path that writes `dist` (e.g. `pnpm dist` or the package’s documented build script that emits `dist`).
5. Dist must complete with exit code 0 and leave a present, up-to-date `dist` directory (or equivalent emit path defined by the package).
6. Do not skip tests, `--passWithNoTests` around real failures, ignore failing suites, or reuse an old `dist` to satisfy the dist step.
7. Do not change product behavior except as required to make tests pass and dist succeed (fix breakages in this package only if they block the sequence).

## Acceptance criteria

- `pnpm test` for `packages/brain-ui` exits 0.
- Dist/build is run only after that passing test run.
- Dist/build exits 0.
- `packages/brain-ui/dist` exists and reflects the post-test sources (not an untouched pre-test artifact).
- CI/agent logs show the ordered sequence: test → dist.
- No unpublished test failures, skipped blocking suites, or empty/missing dist presented as done.

## Out of scope

- Other packages’ tests, builds, or publishes
- App-level E2E, visual regression, or manual QA unless already wired into this package’s `pnpm test`
- Version bumps, changelog, npm publish, or registry release
- New product features, redesign, or dependency upgrades unrelated to unblocking test/dist
- Changing monorepo tooling, pnpm version policy, or CI graph beyond what this package needs to test then dist

## Requirements

This ticket is **verify-then-emit** for one package. It does not add product features. Requirements are grounded in `packages/brain-ui/package.json` as of 2026-09-16 (package `@seanhogg/builderforce-brain-ui` **2026.9.12**). Downstream roles (architect, developer, QA, validator) must treat these as the contract; do not invent a parallel toolchain.

### R-1 — Working directory and existing toolchain

- Work **only** in `packages/brain-ui` (or the repo-root equivalent `pnpm --filter @seanhogg/builderforce-brain-ui <script>`).
- Use the package’s existing **pnpm** scripts and lockfile. Do **not** add npm/yarn, a new test runner, or a new bundler.
- Existing scripts (do not rename or wrap unless a mechanical breakage in this package requires it):
  - `test` → `vitest run --passWithNoTests`
  - `build` → `tsup --config tsup.config.ts`  ← **this is the dist path**
  - `typecheck` → `tsc --noEmit` (optional extra; **not** a substitute for `test` or `build`)
- There is **no** `dist` script. Saying `pnpm dist` is a misread of this package. Dist is `pnpm build`.
- `prepare` / `prepublishOnly` also run `build`. This ticket must **not** npm-publish or bump `version`.

### R-2 — Hard prerequisite: brain-embedded dist (#2556)

- `package.json` depends on `@seanhogg/builderforce-brain-embedded` via `link:../../brain-embedded`.
- `tsup.config.ts` externalizes that package; consumers resolve types through **brain-embedded** `dist/index.d.ts`.
- `src/LiveActivity.test.ts` imports `BrainRunActivity` from that package.
- Parent PRD 29 build order: **#2556 (brain-embedded test → dist) before this ticket**.
- If `brain-embedded/dist/index.d.ts` (and matching cjs/mjs) is missing or stale relative to the sources under test, **stop**. Report blocked on #2556. Do not fake a brain-ui dist against a missing core.

### R-3 — Test first: `pnpm test` must actually run the suite

- CWD `packages/brain-ui`: run **`pnpm test`** (the existing script).
- `vitest.config.ts` is already: environment **`jsdom`**, `include: ['src/**/*.test.{ts,tsx}']`, `exclude: ['**/node_modules/**', 'dist/**']`, `testTimeout: 10_000`.
- The include set is **not empty**. At least these files must execute (not skip):
  - **Change A (run-visibility):** `src/LiveActivity.test.ts` — `phaseLine` for `composing` / `composed`, byte size, per-phase distinct lines, caller-bundle localization.
  - **Change B (ticket-parent):** `src/chatTickets/TicketParentLine.test.tsx` — parent is named (`in <label>` + `↳`), no parent renders empty string (not a placeholder row), tooltip carries the full title, button only when `onOpen` is provided, whole phrase localizes.
  - Other existing files under the include glob, including `BrainTimeline.test.tsx`, `ToolStep.test.tsx`, `timelineModel.test.ts`, `toolStepView.test.ts`, `promptOptions/PromptOptionsMenu.test.tsx`, `chatTickets/aggregateTicketHealth.test.ts`, `chatTickets/chatSwitcherLabel.test.ts`, `chatTickets/runGate.test.ts`, `evermind/*.test.ts`, `project360/sunburstGeometry.test.ts`.
- `--passWithNoTests` is already on the script. It must **not** be used to accept a 0-test run: if vitest reports zero files/tests, this ticket **fails** even if the process exits 0. The flag does not skip failing assertions; do not add a second `--passWithNoTests`, `--bail` wrap, or `--exclude` that drops Change A/B files.

### R-4 — Fail-fast: non-zero test exit aborts dist

- Treat a non-zero `pnpm test` exit as **task failure**.
- Do **not** run `pnpm build`, do **not** refresh `dist`, and do **not** claim success from a pre-existing `dist/` tree.
- Fix policy (inherited from parent #2555 / PRD 29): **mechanical breakage only** inside `packages/brain-ui` (wrong import path, exhaustive `Record` missing `'composing'`, stale test literal, missing catalog key). Report real logic defects; do not patch them here.
- Do not skip, `.skip`, or `--exclude` a blocking suite to go green.

### R-5 — Dist second: `pnpm build` emits the public artifact

- Only after R-3 exits 0, run **`pnpm build`** in `packages/brain-ui`.
- tsup entry is `src/index.ts`. Emit path is `packages/brain-ui/dist/`.
- `package.json` public contract that must exist after a successful build:
  - `main` → `dist/index.cjs`
  - `module` → `dist/index.mjs`
  - `types` → `dist/index.d.ts`
  - `exports["."]` maps types/import/require to those three files
- `styles.css` is shipped beside dist (not bundled). Do not omit it from `files`.
- Externals stay external (React, `react-markdown`, `remark-gfm`, `@seanhogg/builderforce-brain-embedded`). Do not bundle the host’s React or the brain core into this UI package.

### R-6 — Dist must be post-test and current

- `pnpm build` must exit 0.
- `packages/brain-ui/dist` must be **present and newer than the test run** (mtime of `dist/index.d.ts` / `index.mjs` / `index.cjs` ≥ test completion). Reusing an untouched pre-test artifact is failure.
- Do not copy a cached dist from another branch or CI job.

### R-7 — Scope, version, and commit policy

- In scope: this package’s test run + dist emit, plus mechanical fixes **in this package** that unblock that sequence.
- Out of scope (already owned elsewhere): other packages’ tests/builds, api/frontend/vscode steps (#2558–#2561), VSIX 2026.9.72 packaging, npm publish, version bump (package is already 2026.9.12; parent epic packages VSIX **2026.9.72**), product features, dependency upgrades, concurrent dirt (benchmarking, investor, identity, creation-canvas, ROADMAP).
- **Do not commit** unless the operator explicitly asks (parent PRD 29). Report results first.
- Do not dump a `specs/tasks/task-2557.md` conceptual stand-in. Requirements live on **this ticket PRD**.

### R-8 — Evidence the later lanes must produce

QA / developer logs must show this **ordered** sequence and nothing else presented as done:

1. `packages/brain-ui` → `pnpm test` → exit 0, with Change A (`LiveActivity` composing) and Change B (`TicketParentLine`) included in the run.
2. `packages/brain-ui` → `pnpm build` → exit 0.
3. `packages/brain-ui/dist/index.cjs`, `dist/index.mjs`, `dist/index.d.ts` exist and are post-test.

Name every failing test with its file and assertion. A missing/empty dist, a skipped Change A/B file, or dist-before-test is not done.

This BA lane does **not** execute `pnpm` (no shell on this executor). Architect designs the sequence; developer/QA run it and fill Implementation Notes / Test Evidence.

### Traceability

| ID | Parent PM criterion |
|----|---------------------|
| R-1, R-3 | Functional req 1–2 (existing pnpm scripts; `pnpm test` in this package) |
| R-4 | Functional req 3, 6 (fail-fast; no skip / no fake `--passWithNoTests`) |
| R-5, R-6 | Functional req 4–5 (dist after green tests; exit 0; current dist) |
| R-2 | PRD 29 build order (brain-embedded dist before brain-ui) |
| R-7 | Out of scope + parent fix/commit policy |
| R-8 | Acceptance criteria (ordered test → dist evidence) |

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:48:33.820Z_

## Design

This ticket is a **verify-then-emit gate**, not a new UI design. Change A (`LiveActivity` composing-phase copy) and Change B (`TicketParentLine` parent naming) are already written and covered by in-tree tests. Architect lock: prove the existing suite green, then regenerate the committed `packages/brain-ui/dist/` that hosts resolve. Do not redesign the package, add a `dist` script, rename scripts, or change the public API.

Parent: **#2555**. Hard prerequisite: **#2556** (`brain-embedded` test → dist). Planning edge `2556 → 2557` (finish-to-start, dep id 6) is already recorded. Downstream VSIX packaging is **#2560 / #2561** (not this ticket).

This architect lane has **no shell**. Design is the deliverable. Developer then QA execute §7 and fill Implementation Notes / Test Evidence.

### 1. Package identity and install topology

| Fact | Value |
|---|---|
| npm name | `@seanhogg/builderforce-brain-ui` |
| Path / cwd | `packages/brain-ui/` (directory that contains its `package.json`) |
| Version on tree | `2026.9.12` — **do not bump** (VSIX 2026.9.72 is **#2561 / #2562**; a package bump is out of scope) |
| Workspace | **None.** There is no `pnpm-workspace.yaml`. The repo is a set of separately installed packages, each with its own `package.json` + `pnpm-lock.yaml`, joined by `link:` specifiers (`scripts/ensure-linked-deps.mjs`). |
| Canonical command form | `cd packages/brain-ui && pnpm <script>`. Do **not** use `pnpm --filter @seanhogg/builderforce-brain-ui` — that flag requires a workspace file that does not exist. R-1’s “or `--filter`” clause is a misread of this tree. |
| Package manager | Existing **pnpm** + this package’s `pnpm-lock.yaml`. Do not add npm/yarn, a new test runner, or a new bundler. |

`packages/brain-ui` `dependencies` / `devDependencies` that are `link:`:

- `@seanhogg/builderforce-brain-embedded` → `link:../../brain-embedded` (**runtime + types**; R-2)
- `@builderforce/agent-loop` → `link:../agent-loop` (devDependency)

`postinstall` runs `node ../../scripts/ensure-linked-deps.mjs`. pnpm’s `link:` only symlinks the target directory and does **not** install the linked package’s own deps. Those links must resolve before tests run. If `node_modules` is missing or incomplete, `pnpm install` from `packages/brain-ui/` **without rewriting lockfile intent**. Failures in `ensure-linked-deps.mjs` are warnings by design; they must not be “fixed” by converting the repo into a workspace.

`prepare` / `prepublishOnly` both run `npm run build`. They are install/publish hooks, **not** the dist command for this ticket (R-5). Do not npm-publish.

`typecheck` (`tsc --noEmit`) is an optional extra. It is **not** a substitute for `pnpm test` or `pnpm build` and is not in the required sequence.

### 2. Hard prerequisite — #2556 `brain-embedded` dist (fail-closed)

`tsup.config.ts` **externalizes** `@seanhogg/builderforce-brain-embedded`. Consumers (and this package’s own tests/types) resolve that package through **`brain-embedded/dist/index.d.ts`** (plus matching `index.mjs` / `index.cjs`). `src/LiveActivity.test.ts` imports `BrainRunActivity` from it; `src/index.ts` re-exports model-identity helpers from it.

**Before `pnpm test`:**

1. Confirm `brain-embedded/dist/index.d.ts`, `brain-embedded/dist/index.mjs`, and `brain-embedded/dist/index.cjs` exist.
2. Confirm `BrainRunActivity['phase']` in those types includes `'composing'` (Change A). A pre-#2556 dist that lacks the phase is **stale**.
3. If either check fails: **STOP**. Report blocked on **#2556**. Do **not** fake a brain-ui dist against a missing or stale core. Do **not** vendor, stub, or inline the brain-embedded types to unblock.

Main currently carries a `brain-embedded/dist/` tree; that is **not** proof #2556 completed. Developer execution of R-3→R-6 waits on #2556’s regenerated, post-test dist. Architect Design may be authored now; the test→build sequence may not.

### 3. Single-entry library + sibling stylesheet

`tsup.config.ts` uses `libraryConfig` from `scripts/tsup.base.mjs` with **one** entry:

```
entry: { index: 'src/index.ts' }
```

Shared tsup options (`libraryConfig`):

- `format: ['esm', 'cjs']` → `.mjs` / `.cjs`
- `dts` on, `sourcemap: true`, `clean: true`, `outDir: 'dist'`

Package-specific externals (do not bundle; do not drop):

```
...REACT_EXTERNALS,          // react, react-dom, react/jsx-runtime
'react-markdown',
'remark-gfm',
'@seanhogg/builderforce-brain-embedded'
```

React is a **peer** (`>=18`). The markdown libs and brain-embedded are package dependencies but stay external so the host app / webview dedupes them. Bundling the host’s React or the brain core into this UI package is a defect.

`styles.css` is **not** an entry and is **not** emitted into `dist/`. It is shipped **beside** dist:

- `exports["./styles.css"]` → `./styles.css`
- `files`: `["dist", "styles.css"]`

Do not omit `styles.css` from `files`. Do not invent a CSS bundling step.

`packages/brain-ui/dist/` **is committed** (root `.gitignore` does not ignore it; same frozen-install pattern as `brain-embedded/dist`). A successful `pnpm build` **replaces** the directory (`clean: true`). The developer’s only allowed tree change is that regenerated `dist/` (plus install side-effects). Do **not** hand-author files under `dist/`. Reusing an untouched pre-test artifact is failure (R-6).

`TicketParentLine` is **internal** (not re-exported from `src/index.ts`). It still **must run** as a test file (Change B). Dist does not need a new export for it.

### 4. Test architecture (gate)

| Item | Value |
|---|---|
| Script | `"test": "vitest run --passWithNoTests"` |
| Config | `vitest.config.ts`: `environment: 'jsdom'`, `include: ['src/**/*.test.{ts,tsx}']`, `exclude: ['**/node_modules/**', 'dist/**']`, `testTimeout: 10_000` |
| Why jsdom | Component tests (`BrainTimeline.test.tsx`, `ToolStep.test.tsx`, `PromptOptionsMenu.test.tsx`, `TicketParentLine.test.tsx`) need a DOM / `renderToStaticMarkup`. Do not switch to `node`. |

`--passWithNoTests` is already on the script. It is a **trap**. Vitest exit 0 with **zero executed files is a hard fail** (R-3). Do not add a second `--passWithNoTests`, `--bail` wrap, or `--exclude` that drops Change A/B. The developer must record the executed-file count; it must be **> 0**.

Include glob is **not empty**. All 15 in-tree files matching `src/**/*.test.{ts,tsx}` must execute (not skip):

**Change A (run-visibility) — must run:**

- `src/LiveActivity.test.ts` — `phaseLine` for `composing` / `composed`, byte size (`21.1 KB` / omitted until measured), per-phase distinct lines (`starting` / `thinking` / `writing` / `composing` / `tool` / `awaiting` / `finishing`), caller-bundle localization (`composing` / `composed` keys, never English defaults)

**Change B (ticket-parent) — must run:**

- `src/chatTickets/TicketParentLine.test.tsx` — parent is named (`in <label>` + `↳`), no parent renders `''` (not a placeholder row), tooltip carries the full title, `<button>` only when `onOpen` is provided, whole phrase localizes

**Remainder of the include set — must also run:**

- `src/BrainTimeline.test.tsx`
- `src/ToolStep.test.tsx`
- `src/timelineModel.test.ts`
- `src/toolStepView.test.ts`
- `src/promptOptions/PromptOptionsMenu.test.tsx`
- `src/chatTickets/aggregateTicketHealth.test.ts`
- `src/chatTickets/chatSwitcherLabel.test.ts`
- `src/chatTickets/runGate.test.ts`
- `src/evermind/CodingGateNote.test.ts`
- `src/evermind/actionGuide.test.ts`
- `src/evermind/diagnosticsReport.test.ts`
- `src/evermind/learnedStatus.test.ts`
- `src/project360/sunburstGeometry.test.ts`

Do not skip, `.skip`, shard, or retarget. Do not run the monorepo-wide matrix, frontend E2E, or `clients/vscode` tests.

**Hard stop (R-4):** any non-zero `pnpm test` exit, skipped-as-failed, incomplete, or zero-files-executed run → **do not start `pnpm build`**, do not refresh `dist/`, do not claim success from a pre-existing `dist/` tree. Record cwd, command, exit code, and failing `file` / assertion names.

Fix policy (parent #2555 / PRD 29): **mechanical breakage only** inside `packages/brain-ui` (wrong import path, exhaustive `Record` missing `'composing'`, stale test literal, missing catalog key). Real logic defects are **reported, not patched**. Do not patch brain-embedded, frontend, api, or vscode here.

### 5. Dist command and artifact contract

There is **no** `dist` script. Saying `pnpm dist` is a misread of this package. After tests exit 0 with a non-zero file count **and** Change A + Change B included:

```
cd packages/brain-ui
pnpm build    # tsup --config tsup.config.ts
```

Do not invoke `prepare` / `prepublishOnly` as the packaging step.

After a successful build, `packages/brain-ui/dist/` must contain at least:

| Role | Files that must exist |
|---|---|
| Root export | `index.mjs`, `index.cjs`, `index.d.ts` |
| tsup extras (allowed) | `index.mjs.map`, `index.cjs.map`, `index.d.cts` |

These satisfy `package.json`:

- `main` → `dist/index.cjs`
- `module` → `dist/index.mjs`
- `types` → `dist/index.d.ts`
- `exports["."]` `types` / `import` / `require` → those three files
- `exports["./styles.css"]` → `./styles.css` (repo root of the package, **not** under `dist/`)
- `files`: `["dist", "styles.css"]`

Every path named above must exist on disk after the build. Extra tsup chunks are allowed; missing entry files are not.

**R-6 currency:** mtime of `dist/index.d.ts` / `index.mjs` / `index.cjs` must be **≥ test completion**. Copying a cached dist from another branch or CI job is failure.

### 6. Public API freeze

`src/index.ts` is the public surface. Dist must continue to export (among others) `LiveActivity`, `DEFAULT_LIVE_ACTIVITY_LABELS`, `formatElapsed`, `SLOW_AFTER_MS`, `BrainTimeline`, `ToolStep`, `ChatTicketsPanel`, and the brain-embedded model-identity re-exports. Do not add, rename, or remove public exports to make the build pass.

`LiveActivityLabels` must keep `composing` / `composed` keys (Change A). Do not collapse composing into `writing` or `tool` to silence a test.

Do not bundle externals. Do not change `exports` / `files` / `peerDependencies`.

### 7. Developer execution sequence (this executor has no shell)

Working directory: `packages/brain-ui/`.

0. **R-2 gate.** If `brain-embedded/dist/index.{d.ts,mjs,cjs}` is missing or its `BrainRunActivity` phase union lacks `'composing'`: stop and report blocked on #2556. Do not continue.
1. If `node_modules` is missing or incomplete: `pnpm install` without lockfile rewrite; confirm the `link:` targets resolve.
2. `pnpm test`. Record: cwd, command, exit code, **executed test-file count** (must be > 0), and that Change A (`src/LiveActivity.test.ts`) and Change B (`src/chatTickets/TicketParentLine.test.tsx`) ran. Name every failing test with file + assertion.
3. **If and only if** step 2 exits 0 with a non-zero file count and A+B included: `pnpm build`. Record exit code.
4. Confirm on disk: `dist/index.cjs`, `dist/index.mjs`, `dist/index.d.ts` exist, are non-empty, and are **newer than the test run**. Confirm `styles.css` is still beside the package (not deleted).
5. Working tree: regenerated `packages/brain-ui/dist/**` only. No source, CI, lockfile, version, or `specs/tasks/` edits unless a mechanical breakage inside this package blocked step 2 or 3.

QA owns the Test Evidence section of this PRD; the developer records the same facts in Implementation Notes (R-8). Logs must show the **ordered** sequence `pnpm test` → `pnpm build` and nothing else presented as done.

### 8. File set and freeze

| In this ticket | Out |
|---|---|
| Regenerated `packages/brain-ui/dist/**` after a green test run | Source under `packages/brain-ui/src/` except mechanical breakage that blocks test/build |
| Install side-effects (`node_modules`, not committed) | `package.json` scripts / `files` / `exports` / version (`2026.9.12` stays) |
| Architect Design on **this ticket PRD** (no product-repo spec dump) | `specs/tasks/task-2557.md` (**forbidden**, R-7; deleted if present) |
| | `pnpm-workspace.yaml` (does not exist — do not add one) |
| | vitest / tsup / pnpm version changes |
| | `brain-embedded` source or dist (owned by **#2556**) |
| | `api`, `frontend`, `clients/vscode`, VSIX 2026.9.72 (**#2558–#2562**) |
| | npm publish, git tags, changelog, version bump |
| | `typecheck` as a substitute for test or build |

Do **not** commit unless the operator explicitly asks (parent PRD 29) — report results first. If the executor must open a PR, it may contain only generated `dist/` (and install side-effects / a mechanical fix in this package), never a conceptual spec dump.

Concurrent unrelated dirt (benchmarking, investor, identity, creation-canvas, ROADMAP) is left alone.

### 9. R-8 evidence matrix (QA / developer must fill)

| # | Check | Pass iff |
|---|---|---|
| E1 | cwd | `packages/brain-ui` |
| E2 | `pnpm test` | exit 0, executed files > 0 |
| E3 | Change A | `src/LiveActivity.test.ts` in the run (composing / composed / bytes / distinct phases / localization) |
| E4 | Change B | `src/chatTickets/TicketParentLine.test.tsx` in the run (named parent, empty when none, tooltip, button-if-onOpen, localized phrase) |
| E5 | Fail-fast | no `pnpm build` if E2–E4 fail |
| E6 | `pnpm build` | exit 0, only after E2 |
| E7 | Artifacts | `dist/index.cjs`, `dist/index.mjs`, `dist/index.d.ts` present; `styles.css` still at package root |
| E8 | Currency | those dist files mtime ≥ test completion |
| E9 | Prereq | #2556 dist present and `BrainRunActivity` includes `'composing'` |

A missing/empty dist, a skipped Change A/B file, dist-before-test, or a green claim against a missing brain-embedded dist is **not done**.

_— Architect (CTO) · 2026-09-16 · execution #31867. Design lock for R-1–R-8. No product-behavior patch._

_— rewritten by CTO · 2026-09-16T16:20:37.628Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:47:43.234Z · execution #31707

**BA #2557 (Ada, 2026-09-16):** Authored Requirements R-1–R-8 on this ticket PRD. Grounded in `packages/brain-ui` scripts (vitest `jsdom` + tsup `libraryConfig` dist), Change A (`LiveActivity` composing) and Change B (`TicketParentLine` + panel `parentLink`). No product-repo patch from the BA lane. `specs/tasks/task-2557.md` must not ship — a concurrent BA holds that path to drop it. Hard prerequisite: #2556 brain-embedded dist. Out of scope: VSIX (#2561 / #2562 at 2026.9.72), publish, version bump, commit of `dist`. This executor cannot run `pnpm test` / `pnpm build`; those are developer then QA.

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:48:57.699Z · execution #31706

2026-09-16 · Business Analyst (execution #31707): Requirements R-1–R-8 authored on this ticket PRD from the live `packages/brain-ui` tree. Facts used: package `@seanhogg/builderforce-brain-ui` 2026.9.12; `test` = `vitest run --passWithNoTests` (jsdom, include `src/**/*.test.{ts,tsx}`); **no `dist` script** — emit is `pnpm build` / tsup → `dist/index.{cjs,mjs,d.ts}`; Change A locked by `LiveActivity.test.ts`, Change B by `TicketParentLine.test.tsx`; blocked on #2556 brain-embedded dist. Deleted branch dump `specs/tasks/task-2557.md` (docs-only, not the BA deliverable). This lane did not run `pnpm` (no shell). Developer/QA execute R-3→R-6 and fill Test Evidence. Do not re-dump a conceptual spec.

---

### Update — Product Manager · 2026-09-16T15:58:56.768Z · execution #31768

**PO ready-lane (product-owner, 2026-09-16):** Approved scope + acceptance. R-1–R-8 are the contract: packages/brain-ui only; `pnpm test` then `pnpm build` (there is no `pnpm dist` script); Change A `LiveActivity.test.ts` (composing) and Change B `TicketParentLine.test.tsx` must actually run; fail-fast; dist `index.{cjs,mjs,d.ts}` post-test; hard prereq #2556; no publish/version-bump/VSIX/commit of dist. Architect still owns Design. Developer/QA execute R-3→R-6 and fill Test Evidence. Signoff id 8003659c-d1ad-46a7-85c5-4df8115997d9 · execution 31707.