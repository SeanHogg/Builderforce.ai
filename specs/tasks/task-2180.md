# Task 2180 — Disposition of Dependabot PR 820

Board ticket 2180. GitHub PR 820 is **closed** (2026-09-10, Dependabot: these dependencies are no longer a dependency). The Dependabot head branch was **deleted** the same day. Do **not** merge, rebase, recreate, or reopen PR 820. Do **not** use PR 821 / ticket 2181. Remaining vitest / transitive-vite work is board ticket **2603**.

This file is the in-repo audit trail required by FR-D2 / FR-D5. Ticket 2180 ships **no** converter package.json, lockfile, config, or source change.

## BA Requirements (normative)

**FR-D1. Closed PR is not a merge vehicle.** GitHub PR 820 is closed (2026-09-10) and its head branch is deleted. Nobody may merge, rebase, recreate, or reopen it as the vehicle for this ticket.

**FR-D2. No converter code change.** Do not modify converter package.json, pnpm-lock.yaml, vitest.config.ts, tsconfig.json, tests, or src. The only in-repo deliverable is this file plus the ticket PRD.

**FR-D3. No retarget of this ticket.** Do not rewrite ticket 2180 into a vitest-only bump that keeps PR 820’s title, GitHub URL, or branch.

**FR-D4. No invented Vite 8 landing.** Do not open a new Dependabot PR, a manual upgrade PR, or a pin of vite 8.2.2 in the converter package as part of this ticket.

**FR-D5. Audit trail contents (this file).** Names: PR 820 closed 2026-09-10 and branch deleted; Dependabot close reason above; current versions vitest 2.1.9, vite 5.4.21 (transitive), vite-node 2.1.9; converter shape (Node CLI, build = tsc); security conclusion (FR-D8); follow-up pointer (ticket 2603).

**FR-D6. Follow-up, not silent drop.** Remaining work is board ticket **2603** (gap; window 2026-09-16 to 2026-10-07): planned vitest bump in the converter package so the test runner and transitive vite 5.4.21 leave the 2.1.x / 5.4.x line. Ticket 2181 / GitHub PR 821 was also closed by Dependabot on 2026-09-10 with the same message; do not use PR 821 as the merge vehicle.

**FR-D7. Board close path.** After required ready-lane sign-offs on the rewritten PRD, cancel/close board ticket 2180 with pointers to this file and the ticket PRD. Do not leave it in a “merge the Dependabot PR” workflow.

**FR-D8. Security conclusion.** Direct vite on converter: none. Closing PR 820 does not leave an unpatched direct Vite 5.4.21 product dependency. Residual risk is transitive vite 5.4.21 via vitest 2.1.9. Exploitability is low (Node CLI plus environment node tests, not a Vite preview/dev server). Residual is owned by ticket 2603. This ticket’s delta introduces no new high/critical advisory.

Non-requirements: install against the deleted Dependabot lockfile; vite build / Vite dev-server smoke; snapshot updates; pinning Node in CI for Vite 8; AppSec sign-off of a Vite 8 merge that will not happen.

## Inventory (main, re-verified 2026-09-16)

- Package: webdit/converter. Node CLI. build = tsc. typecheck = tsc --noEmit. convert = tsx. test = vitest run.
- Direct vite: none. No vite.config.
- Direct vitest: specifier caret 2.1.0, lockfile 2.1.9.
- Transitive vite 5.4.21 via vitest 2.x and vite-node 2.1.9.
- vitest.config.ts: include test/**/*.test.ts, environment node only. No server, optimizeDeps, preview, plugins, or CSS.
- tsconfig.json: ES2022, module ESNext, Node types, outDir dist, include src.
- Repo .nvmrc is 24 (Architect correction; earlier drafts said 22). Vite 8 Node floor is N/A because Vite 8 is not landing.
- GitHub PR 820 intended vite 5.4.21 to 8.2.2 (indirect) and vitest 2.1.9 to 4.1.11 (indirect). Nothing landed.
- Branch builderforce/task-2180 versus main: audit-trail markdown only. No converter package, lockfile, config, or src change.

## Architect Design (locked)

AD-1. Converter is a tsc Node CLI, not a Vite app. Product path is tsc; tests are vitest in node; convert uses tsx.

AD-2. vite is not a direct dependency. PR 820 intended vite 5.4.21 to 8.2.2 and vitest 2.1.9 to 4.1.11. Nothing landed.

AD-3. This ticket must not change package.json, pnpm-lock.yaml, vitest.config.ts, tsconfig.json, src, or tests. No vite 8.2.2 pin.

AD-4. Vite 6/7/8 and Vitest 3/4 migration items are N/A. No Vite plugins. Peer check does not apply.

AD-5. Runtime floor is Node 24. Do not pin CI Node for Vite 8.

AD-6. Follow-up is ticket 2603. That ticket must not invent a standalone vite 8 pin or reopen PRs 820/821.

AD-7. Security: no direct vite to patch. Residual transitive vite 5.4.21 is low exploitability on a Node CLI test runner. Owned by 2603. This ticket adds no new advisory.

AD-8. Cloudflare Pages on PR 820 is not a converter gate. Do not self-merge PR 820.

AD-9. Developer writes Implementation Notes only. Reviewer checks audit-trail markdown only. QA does not run vite build. Validator accepts FR-D1 through FR-D8 then the board ticket is cancelled.

## Close path

After required ready-lane sign-offs on this Design and the rewritten PRD: cancel/close board ticket 2180 with pointers to the ticket PRD and this file. Do not leave it in a “merge the Dependabot PR” workflow.

Authored by Ada (Sr. Product Mgr) / Business Analyst, 2026-09-16.
Architect Design locked by CTO, 2026-09-16.
Re-verified after Architect lock (FR-D6 = ticket 2603; .nvmrc 24; lockfile vite 5.4.21 / vite-node 2.1.9 / vitest 2.1.9) by Ada, 2026-09-16.
