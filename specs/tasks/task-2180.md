# Task 2180 — Disposition Audit Trail

Ticket: 2180 — Dependency PR 820: Bump vite and vitest in /webdit/converter

Disposition: Cancel/close this board ticket. Do not merge, rebase, recreate, or reopen GitHub PR 820.

## 1. PR 820 Status

State: Closed (Dependabot, 2026-09-10).
Close reason: "Looks like these dependencies are no longer a dependency, so this is no longer needed."
Head branch: Deleted.
Intended change: vite 5.4.21 to 8.2.2 + vitest 2.1.9 to 4.1.11.
Outcome: Nothing landed.

## 2. Current Dependencies (main, verified 2026-09-16)

vitest: direct devDependency, specifier caret 2.1.0, resolved 2.1.9
vite: transitive only (via vitest 2.x / vite-node), resolved 5.4.21
vite-node: transitive only (via vitest 2.x), resolved 2.1.9

vite is not a direct dependency — absent from package.json.
vitest.config.ts uses environment:node only; no vite.config exists.
tsconfig.json targets ES2022, module ESNext, types node, outDir dist.

## 3. Converter Shape

/webdit/converter is a Node CLI. Build is tsc, not Vite.
Scripts: build=tsc, typecheck=tsc --noEmit, convert=tsx, test=vitest run.
No Vite dev server, preview, plugins, CSS pipeline, or browser targets.

## 4. Version Review (N/A)

All Vite 6/7/8 and Vitest 3/4 migration items are N/A because no merge occurs:
- No Vite server, no CSS pipeline, no optimizeDeps, no preview
- Zero Vite plugins, no browser targets
- Converter bundler is tsc, not Vite/Rolldown
- No test-runner bump on this ticket
- Vite 8 Node floor is irrelevant

## 5. Node Floor

.nvmrc is 24 (verified 2026-09-16). Vite 8 Node floor is N/A.

## 6. Security

Direct vite: none.
Residual transitive vite 5.4.21 via vitest 2.1.9: low exploitability (Node CLI, environment:node tests).
This ticket introduces no new advisory.
Residual owned by follow-up ticket 2603.

## 7. Follow-up

Ticket 2603: Planned vitest bump in /webdit/converter (2026-09-16 to 2026-10-07).

## 8. Forbidden

No merge/reopen PR 820. No package/lockfile/config/src changes. No retargeting. No Vite 8 pin.

## 9. Disposition

Cancel/close board ticket 2180. PR 820 is closed, branch deleted, converter has no direct vite.
Audit trail: this doc + ticket PRD. Remaining work: ticket 2603.

Authored by Business Analyst, ready-lane, 2026-09-16.