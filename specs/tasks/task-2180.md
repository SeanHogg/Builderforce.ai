# Task 2180 — Disposition of Dependabot PR 820

Board ticket 2180. GitHub PR 820 is closed (2026-09-10, Dependabot: these dependencies are no longer a dependency). Head branch deleted. Do not merge, rebase, recreate, or reopen PR 820. Do not use PR 821 / ticket 2181. Remaining vitest work is board ticket 2603.

## Inventory (main)

- Package: webdit/converter (@webdit/convert). Node CLI. build = tsc -p tsconfig.json.
- Direct vite: none. No vite.config.
- Direct vitest: specifier caret 2.1.0, lockfile 2.1.9.
- Transitive vite 5.4.21 via vitest 2.x. vite-node 2.1.9.
- vitest.config.ts: environment node only.
- .nvmrc is 24 (not 22).

## Architect Design (locked, execution 31776)

AD-1. Converter is a tsc Node CLI, not a Vite app. Product path is tsc; tests are vitest in node; convert uses tsx.

AD-2. vite is not a direct dependency. PR 820 intended vite 5.4.21 to 8.2.2 and vitest 2.1.9 to 4.1.11. Nothing landed.

AD-3. This ticket must not change package.json, pnpm-lock.yaml, vitest.config.ts, tsconfig.json, src, or tests. No vite 8.2.2 pin.

AD-4. Vite 6/7/8 and Vitest 3/4 migration items are N/A. No Vite plugins. Peer check does not apply.

AD-5. Runtime floor is Node 24. Do not pin CI Node for Vite 8.

AD-6. Follow-up is ticket 2603. That ticket must not invent a standalone vite 8 pin or reopen PRs 820/821.

AD-7. Security: no direct vite to patch. Residual transitive vite 5.4.21 is low exploitability on a Node CLI test runner. Owned by 2603. This ticket adds no new advisory.

AD-8. Cloudflare Pages on PR 820 is not a converter gate. Do not self-merge PR 820.

AD-9. Developer writes Implementation Notes only. Reviewer checks audit-trail markdown only. QA does not run vite build. Validator accepts FR-D1 through FR-D8 then the board ticket is cancelled.

Close path: after ready-lane sign-offs, cancel/close ticket 2180. Pointers: ticket PRD Design section and this file.

Authored by CTO (Architect), 2026-09-16.
