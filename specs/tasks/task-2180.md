> **PRD** — drafted by Product Manager · task #2180
> _Each agent that updates this PRD signs its change below._

# PRD: Review and Disposition of Dependabot PR #820 (vite + vitest bump)

**Status:** WIP  
**Repo:** SeanHogg/Builderforce.ai  
**PR:** [#820](https://github.com/SeanHogg/Builderforce.ai/pull/820)  
**Branch:** `dependabot/npm_and_yarn/webdit/converter/multi-0272144093`  
**Package path:** `/webdit/converter`  
**Change:** Coordinated bump of `vite` (5.4.21 → 8.2.2) and `vitest` (paired update)  
**Decision required:** Merge after validation and sign-off, or close with a recorded reason

---

## Problem & Goal

GitHub Dependabot pull request 820 proposed a coordinated bump of vite 5.4.21 to 8.2.2 and a paired vitest update under `/webdit/converter`. That GitHub PR is **already closed** (Dependabot, 2026-09-10: these dependencies are no longer a dependency) and its head branch is **deleted**. Current `main` `/webdit/converter` is a Node CLI (`build` = `tsc -p tsconfig.json`), with **no vite direct dependency**. vitest remains a direct devDependency (caret 2.1.0, lockfile 2.1.9) and still pulls **transitive vite 5.4.21** plus vite-node 2.1.9. Cloudflare Pages failure on PR 820 is not a converter product signal.

The original “merge Vite 8 after validation” goal is therefore invalid. Treating this ticket as a live toolchain bump would reopen a deleted branch, invent a Vite 8 landing that the converter does not use as a direct dependency, or duplicate later Dependabot work.

**Goal:** Dispose ticket 2180 against already-closed GitHub PR 820. Record versions, inventory, security conclusion, and the close rationale in this PRD and in `specs/tasks/task-2180.md`. Do **not** merge or reopen PR 820. Do **not** retarget this ticket into a vitest-only bump. Remaining vitest / transitive-vite work is a follow-up, not this ticket.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:41:30.876Z_

---

## Target users / ICP roles

| Role | Interest |
| --- | --- |
| Web converter maintainers (`/webdit/converter`) | App still builds, serves, and tests after the bump |
| Frontend / platform engineers | Vite 8 + Vitest pairing, plugin and config compatibility |
| Security / AppSec | CVE closure vs. new advisory surface; lockfile integrity |
| CI owners | Pipeline still green on the Dependabot branch |
| Release / merge approvers | Merge vs. close decision with recorded sign-off |

End users of the converter product are not in scope for this PRD; they are affected only if a bad merge ships a broken build.

---

## Scope

In scope:

- PR #820 only: `vite` 5.4.21 → 8.2.2 and the paired `vitest` bump under `/webdit/converter`, including lockfile updates on the Dependabot branch.
- Compatibility review against current converter config (`vite.config.*`, `vitest.config.*` / `vite.config` test block), plugins, scripts, Node engine, and CI.
- Security and supply-chain review of the new versions (advisories addressed, new advisories introduced, npm provenance / lockfile diff).
- Running the normal required checks (install, typecheck if present, unit tests, production build, lint if present).
- Documented breaking-change gap analysis for Vite 6, 7, and 8 and the matching Vitest major(s).
- Merge with required reviews **or** close with a written reason (e.g. blocked on a follow-up upgrade PR, Node floor, or plugin incompatibility).
- Minimal in-PR config/script fixes that are required to make the bump land (rename deprecated options, align Vitest to Vite 8, pin Node in CI if the new floor demands it).

---

## Functional requirements

1. **Inventory the diff.** Confirm files touched are limited to converter package manifests and lockfiles (`package.json`, `package-lock.json` / `yarn.lock` / `pnpm-lock.yaml` under `/webdit/converter`). Flag any unexpected source or config edits.
2. **Resolve the exact new versions.** Record landed `vite` (8.2.2) and `vitest` versions, plus any transitive Vite-family packages Dependabot moved (`@vitejs/plugin-*`, `vite-node`, etc.).
3. **Breaking-change review.** Walk Vite 6/7/8 and corresponding Vitest migration notes for items that apply to this app: `server.fs`, CSS, `optimizeDeps`, `preview`, plugin hook changes, default browser targets, `test.environment`, pool/workers, coverage provider, and snapshot defaults.
4. **Peer and plugin compatibility.** Verify every Vite plugin and Vitest-related package used by the converter declares support for Vite 8 / the new Vitest major. Do not merge if a required plugin is Vite-5-only unless it is upgraded in the same PR or a blocking follow-up is opened and this PR is closed.
5. **Runtime floor.** Confirm local and CI Node (and npm/yarn/pnpm) versions meet Vite 8 / Vitest requirements. If CI is below the floor, either bump the CI Node version as part of landing this change or close the PR as blocked.
6. **Install and lockfile integrity.** Clean install from the PR lockfile must succeed with the repo’s package manager; no unexplained duplicate Vite copies or mixed major versions in the converter tree.
7. **Functional verification in `/webdit/converter`.**
   - Install dependencies.
   - Production build succeeds.
   - Dev server starts and serves the converter entry without transform errors (smoke).
   - Vitest suite runs to completion with no new failures; update snapshots only when the change is an intended Vitest default shift and the new snapshots are reviewed.
   - Existing lint/typecheck scripts still pass.
8. **Security impact.** Compare GitHub Dependabot/advisory data for `vite` 5.4.21 vs 8.2.2 and the old vs new `vitest`. Record CVEs closed, CVEs remaining, and any new high/critical issues. Reject merge if the new versions introduce unmitigated high/critical advisories that the previous versions did not have, unless AppSec explicitly accepts the risk.
9. **Required checks and sign-offs.** GitHub required status checks must be green. Obtain the normal code-owner / maintainer review plus security sign-off when advisories are in play. Do not self-merge past failing or skipped required checks.
10. **Disposition.** If validation and sign-offs pass, merge with the repo’s standard merge method (squash unless policy says otherwise) and delete the head branch if that is repo default. If blocked, close #820, comment the blocker, and file or link the follow-up (plugin upgrade, Node bump, or staged 5→6→7→8 path).
11. **Audit trail.** PR comment or merge message must state versions, checks run, security conclusion, and merge/close reason.

---

## Acceptance criteria

- [ ] PR #820 is limited to the converter Vite/Vitest bump and lockfile; stray files are explained or the PR is closed.
- [ ] Exact old → new versions for `vite` and `vitest` are recorded on the PR.
- [ ] Vite 6/7/8 and Vitest migration items applicable to `/webdit/converter` are listed as handled, N/A, or blocking.
- [ ] All converter Vite plugins and Vitest integrations support the new majors, or the PR is closed with a follow-up.
- [ ] CI and documented engines satisfy the new Node/package-manager floor, or the PR is closed as blocked on that bump.
- [ ] Clean install, production build, Vitest, and existing typecheck/lint scripts succeed on the PR branch.
- [ ] No new test failures; any snapshot updates are reviewed and justified.
- [ ] Security comparison is written on the PR; no unaccepted high/critical regression.
- [ ] Required GitHub checks are green; required reviews and security sign-off (when applicable) are present.
- [ ] PR is merged **or** closed with a public comment stating the blocker and follow-up. No silent ignore.
- [ ] Merge/close comment names versions, verification commands/checks, and the security conclusion.

---

## Out of scope

- Unrelated Dependabot PRs or other workspace packages outside `/webdit/converter`.
- Feature work, UI changes, or converter product behavior changes not required to compile and test on Vite 8 / new Vitest.
- Optional migrations (new Vite 8-only features, Vitest UI, coverage-provider swaps) unless required for compatibility.
- Rewriting CI beyond the Node/package-manager floor needed to run the new toolchain.
- Publishing a converter release or changing user-facing versioning.
- Pinning or vendoring Vite/Vitest forks.
- Manual major-by-major upgrade PRs (5→6, 6→7, 7→8) unless #820 is closed as too large and those PRs are the follow-up path.
- Changing org-wide merge policy, required reviewers, or Dependabot config except a follow-up ticket if grouping majors is deemed too aggressive.

## Requirements

_Owned by the business-analyst — to be authored._

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Product Manager · 2026-09-16T15:23:17.106Z · execution #31640

**PO ready-lane review (2026-09-16):** GitHub PR #820 is already **closed** (Dependabot, 2026-09-10: “these dependencies are no longer a dependency”); head branch deleted. Current `/webdit/converter` is a Node CLI (`build` = `tsc`), with **no `vite` dependency** and **`vitest` still a direct devDependency** (`^2.1.0`, lockfile `2.1.9`) plus `vitest.config.ts`. Cloudflare Pages failure on #820 is not a converter product signal. This ticket is **not ready** as written: do not merge/reopen #820; PM/BA must either (A) dispose this ticket as already-closed with an audit trail, or (B) retarget to a *new* vitest-only bump. Verdict: `changes_requested`.