> **PRD** — drafted by Product Manager · task #2521
> _Each agent that updates this PRD signs its change below._

# Advisor Platform — WIP PRD

## Problem & Goal

Builderforce users currently piece together project, estimate, schedule, and customer context by hand, then go outside the product for advice. That context-switching produces inconsistent recommendations, lost history, and advice that ignores live job data.

**Goal:** Ship an in-product Advisor Platform that answers questions, proposes next actions, and explains tradeoffs using the user’s Builderforce workspace (jobs, estimates, schedule, CRM, and docs). Advice must be attributable, reversible, and safe to act on—never a black-box mutation of production data.

Success looks like: a user can start an advisor thread from any job or estimate, receive grounded guidance with citations to workspace records, accept or reject suggested actions, and resume the same thread later with full history.

## Target users / ICP roles

| Role | Why they use Advisor |
| --- | --- |
| Owner / GM (home-services & light commercial contractors, ~5–75 field staff) | Margin, mix, capacity, and “what should we do this week” decisions |
| Estimator / sales lead | Bid strategy, scope gaps, comparable jobs, pricing risk |
| Project / operations manager | Schedule conflicts, crew loading, change-order impact |
| Office admin / CSR | Customer comms drafts, follow-up cadences, missing paperwork |
| Builderforce internal (CS, onboarding, solutions) | Reproducing a customer’s workspace question and coaching them in-product |

Primary ICP: US residential and light-commercial contractors already live on Builderforce jobs + estimates. Secondary: multi-location operators and franchise GMs who need portfolio-level advice.

## Scope

In scope for this WIP:

- Advisor surface (global + contextual entry from job, estimate, schedule, and customer records)
- Threaded conversations with persistence, resume, and share-within-org
- Retrieval over the signed-in user’s permitted Builderforce records
- Grounded answers with citations (record type, id, and field/snippet)
- Suggested actions the user can preview, edit, accept, or dismiss
- Advisor personas (Operations, Estimating, Customer, Financial pulse) as selectable lenses—not separate products
- Usage, quality, and safety telemetry sufficient to operate the feature
- Spec file `specs/builderforce/26-prd-advisor-platform.md` as the shared source of truth for downstream implementation

## Functional requirements

1. **Entry points**
   - Global “Ask Advisor” from the app shell.
   - Contextual “Ask about this” on job, estimate, schedule, and customer detail views, preloading that record as the primary context.
   - Deep link to an existing thread.

2. **Conversation model**
   - Threads belong to the org; visible to the author and roles the author shares with.
   - Messages are user, advisor, system (context/citation), or action-proposal.
   - User can rename, archive, and search their threads.
   - Switching persona mid-thread is allowed; the switch is recorded in-thread.

3. **Grounding & permissions**
   - Retrieval may only include records the current user can already read in the UI.
   - Answers that use workspace data must attach citations. If retrieval is empty or low-confidence, the advisor must say so and ask a clarifying question instead of inventing numbers.
   - No cross-org retrieval. No use of other customers’ data for few-shot examples in production.

4. **Personas (lenses)**
   - **Operations:** crew, schedule, job status, blockers.
   - **Estimating:** price, scope completeness, comps, risk flags.
   - **Customer:** comms tone, follow-ups, outstanding items.
   - **Financial pulse:** backlog, margin, aging, cash-relevant job health (read-only; no GL posting).
   - Default persona is inferred from the entry point; user can override.

5. **Suggested actions**
   - Advisor may propose discrete, previewable actions (e.g., draft a change order note, create a follow-up task, suggest a schedule move, draft a customer SMS/email).
   - Every proposal shows the target record, fields to change, and before/after.
   - Accept writes through existing product APIs with the user’s auth; dismiss discards; edit opens the native form with values prefilled.
   - Advisor never writes production data without an explicit accept.

6. **Safety & non-goals baked into behavior**
   - No legal, tax, or licensed-professional advice presented as definitive; those answers must include a limitation line and a recommend-human-review cue.
   - No instructions that bypass permissions, export another user’s private data, or fabricate bid numbers as if they were calculated.
   - Rate-limit and cost-cap per org; degrade to “Advisor busy / try again” rather than ungrounded completion.

7. **Observability**
   - Log thread id, persona, retrieval hit/miss, citation count, action proposed/accepted/dismissed, latency, and token/cost.
   - PII in logs follows existing Builderforce redaction rules.

## Acceptance criteria

- From a job detail page, a user can open Advisor and see that job already attached as context within one click.
- An answer that references a job, estimate, or customer includes at least one citation that navigates to the live record.
- When the user lacks access to a record, Advisor neither mentions it nor leaks its existence beyond what the UI already allows.
- A proposed action shows a diff/preview; accepting it persists via the same path as the equivalent UI action; dismissing it leaves data unchanged.
- Refreshing the browser restores the thread, persona, citations, and pending proposals.
- Empty-retrieval questions produce a clarification prompt, not a numeric guess.
- Org admin can disable Advisor; when disabled, all entry points hide and API calls return a stable disabled error.
- Telemetry events fire for thread start, message, citation, and action accept/dismiss, and are queryable in the existing ops store.
- Spec at `specs/builderforce/26-prd-advisor-platform.md` matches this document (problem, users, scope, requirements, acceptance, out of scope) so implementation agents do not diverge.

## Out of scope

- Autonomous agents that mutate jobs, invoices, payroll, or accounting without an explicit user accept
- Voice / phone advisor, SMS inbound advisor, or field-tech mobile-offline advisor
- Training or fine-tuning on customer workspace data
- Human marketplace of live advisors; video consults
- Custom org-authored personas or bring-your-own model in this WIP
- Public unauthenticated advisor, SEO chat widget, or embeddable third-party script
- Tax, legal, insurance, or licensed engineering sign-off
- Multi-org / franchise rollup advisor (portfolio across legal entities)
- Replacing existing reports, dashboards, or estimate math engines
- End-to-end UI pixel spec, model vendor selection, or pricing packaging (tracked separately)

## Requirements

_Owned by the business-analyst. Authored 2026-09-16 on ticket **#2521** (lane `ready`). Signed off as business-analyst / ready._

This section is the ready-lane contract for auto-captured Brain chat **#113** (ticket title: “Code change (1 file) from Brain chat #113”). It specifies *what* this ticket is. It does **not** implement a product change. Architect Design and Developer Implementation, if this ticket proceeds, must follow this section — not the Problem & Goal / Functional requirements / Acceptance criteria above.

Related tickets (do not absorb their work here): research **#2523** (DONE); sibling auto-capture **#2544** (DONE, chat #112, same file); parent epic **#2522**; deferred product epic **#2545**; gap **#2604** (rewrite PRD 26 to the #2522 composition); P0/P1 children **#2527**, **#2532**, **#2538**; meeting slices **#2533**, **#2534**, **#2535**, **#2536**.

### Product-identity override (read first)

The Problem & Goal, Target users, Scope, Functional requirements (1–7), and Acceptance criteria on this ticket PRD describe an **in-product AI Advisor** for home-services contractors (jobs, estimates, schedule, CRM; personas; suggested actions that mutate via existing APIs; org-admin disable). That product is:

- **not** what Brain chat #113 wrote,
- **not** what `specs/builderforce/26-prd-advisor-platform.md` contains on `main`,
- **not** epic **#2522** (SCORE-class advising, marketplace-dogfooded),
- **not** epic **#2545** (marketplace Book bind + A0–A5).

Implementers of #2521 MUST treat those PM sections as **superseded**. The product of record for PRD 26 is epic **#2522**, with the file rewrite owned by gap **#2604**. This ticket must not become a third product.

### R0 — Ticket identity (P0)

**Given** this ticket was minted as an auto-capture of Brain chat #113 changing one file,
**when** any role on this ticket produces a deliverable,
**then** that deliverable records the already-captured spec path — it is not a new product.

- Named file (repo-relative): `specs/builderforce/26-prd-advisor-platform.md`. The auto-capture prefix `Builderforce.ai/` is the workspace folder, not a second path.
- This BA slot’s deliverable is **this Requirements section** on the ticket PRD.
- Must not: product/API/UI code; `specs/tasks/task-2521.md`; a conceptual stand-in because a glob or empty checkout missed the file.

### R1 — File already on main (verify-first / no-op) (P0)

The named file **already exists on `main`** (268 lines; title “Advisor platform as marketplace composition (not a SCORE clone)”; **Board:** epic #2545). Chat #113 is not an uncommitted delta waiting for `builderforce/task-2521`.

- Developer work on #2521, if any, is **verify-first**: confirm the file is on `main` and close without a new commit.
- Must not rewrite the file on this branch to the contractor AI advisor (PM WIP above).
- Must not rewrite the file on this branch to the #2522 composition — that rewrite is **#2604**.
- Must not treat a failed glob, GitHub 403, or empty checkout as “file missing, so write it here”. `read_file` on this run returned the live spec.

### R2 — Product of record is epic #2522 (P0)

PRD 26’s product (once #2604 lands) is **marketplace-dogfooded advising**. SCORE.org is a **capability checklist**, not a clone.

In-scope for the epic (not for this ticket to build):

1. Talent listing/filter for Advisors (`category=advisors`) — not an entity, not a listing kind, not `/advisors`.
2. First-class meeting kind `advisory` (create, list, complete). Standup remains distinct. Not `TEAM_CEREMONY_KINDS`.
3. Meeting Notes agent scoped to advisory meetings.
4. Minutes → board via a generalised pull of today’s `pullStandupMinutesAct`.
5. Public Book on the advisor profile (generalise ATS `/book/[token]`; slices #2532–#2536).

Epic kill condition (not this ticket): a logged-out visitor discovers an advisor, books a slot, joins the meeting, and after it ends sees at least one board task and one proposed follow-up — no staff click.

Non-goals (epic): SCORE trademark / SBA affiliation, new video stack, new notes format, new CMS/LMS, new payout rail.

### R3 — In-repo file currently boards #2545; rewrite is #2604 (P0)

On `main` today the file specifies epic **#2545** tracks A0–A5 (Talent ↔ `booking_services` Book bind, restricted Brain thread, workshops, Academy, toolkit packs, mentoring engagement). That is a known fork vs #2522.

- **#2604** already has signed BA R0–R9 for the rewrite: PRD 26 describes only #2522; A2–A5 / `/marketplace?family=talent` Book bind / restricted Brain are deferred to **#2545** (explicit, not “later in 26”).
- This ticket must not perform that rewrite and must not leave children to pick either product.
- Shared non-goals already in the file that **stay** (do not regress): do not add `advisor` to `MARKETPLACE_LISTING_KINDS`; no `/advisors` destination; no volunteer/chapter table; SCORE is a checklist not a clone.

### R4 — Contractor AI advisor is out (P0)

Do **not** implement, spec, or scaffold any of:

- Global “Ask Advisor” / contextual “Ask about this” on job, estimate, schedule, customer
- Advisor personas (Operations, Estimating, Customer, Financial pulse) as in-product LLM lenses
- Suggested actions that preview/accept/dismiss mutations of jobs, estimates, or CRM
- Retrieval over contractor workspace records with citations
- Org-admin disable flag for that AI surface
- The PM acceptance criterion that the spec “matches this document (problem, users, scope…) so implementation agents do not diverge” — that would **fork** PRD 26 into a third product

Those items are out of scope for #2521, #2522, and #2545 unless a future epic explicitly takes them.

### R5 — Hard fail conditions (P0)

A change on this ticket fails review if it:

1. Adds `/advisors` as a destination or marketplace family route.
2. Adds `advisor` / `advisory` to `MARKETPLACE_LISTING_KINDS`.
3. Adds a volunteer / pro-bono **table** (P3 is a badge + listing filters, owned elsewhere).
4. Clones SCORE (trademark, SBA affiliation, chapter CMS, separate video stack).
5. Punches `authMiddleware` off the meetings router (join API is #2535).
6. Writes meeting `kind` other than existing kinds or `advisory` (allowlist is #2536).
7. Dumps `specs/tasks/task-2521.md`, `DONE.md`, or a stand-in spec because checkout/glob failed.
8. Uses PR **#829** as this ticket’s vehicle (that PR is #2522; do not merge as-is — includes `task-2522.md` + unshipped-route claims).
9. Rewrites `26-prd-advisor-platform.md` to the contractor AI advisor **or** performs #2604’s rewrite here.

### R6 — Bound repository (P0)

The bound repo is **seanhogg/builderforce.ai**. This BA slot does **not** require a repository write.

- Success of this BA slot = Requirements signed on the ticket PRD.
- Architect / Developer: if checkout is empty or GitHub 403, do not invent files; bind/unblock is not this ticket.
- This run could `read_file` the live spec; do not reverse that finding.

### R7 — Spec path (P0)

- Epic #2522 Spec remains `specs/builderforce/26-prd-advisor-platform.md`.
- Auto-capture path `Builderforce.ai/specs/builderforce/26-prd-advisor-platform.md` is the same file.
- `specs/builderforce/README.md` and `ROADMAP.md` already point at that path as SCORE-class advising, specified not built.
- Implementation notes (`26-implementation-notes-advisor-platform.md`) compose on the PRD; they are not this ticket and must not become a second PRD (#2593 / #2604).

### R8 — Lane roles on this ticket (P0)

| Role | This ticket |
|---|---|
| Business analyst (this slot) | Author R0–R9 on the ticket PRD; sign off ready |
| Architect | Design only if needed to confirm no-op / point at #2604; must not design the contractor AI advisor |
| Developer | Verify file on main; no product patch; no `specs/tasks/task-2521.md` |
| Reviewer / QA / Validator | Confirm no unexpected product diff; the PM WIP was not implemented |

### R9 — Non-goals (P0)

- Shipping P0–P3 of epic #2522.
- Shipping A0–A5 of epic #2545.
- Re-researching SCORE (#2523 DONE).
- Re-parenting children #2527 / #2532 / #2538.
- Absorbing #2604, #2605, #2606.
- Voice/phone advisor, SMS inbound, field-tech offline, BYO model, public unauthenticated SEO chat widget.

### Traceability

| Requirement | Source |
|---|---|
| R0, R1, R8 | Auto-capture of chat #113; file already on main; BA = PRD Requirements |
| R2 | Epic #2522 |
| R3 | Gap #2604; in-repo file still boards #2545 A0–A5 |
| R4 | Correction of this ticket’s PM WIP |
| R5 | Epic non-goals + Advisor tickets (#2527, #2580, #2585, #2586) |
| R6 | Bound repo / no BA repo write |
| R7 | #2522 Spec path; ROADMAP + specs README |
| R9 | Out of scope |

### Done when (this BA slot)

R0–R9 are recorded on this ticket PRD. No repository file change. File rewrite of `26-prd-advisor-platform.md` remains **#2604**. Product implementation remains **#2522** children.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T16:03:17.678Z_

## Design

_Owned by the architect. Authored 2026-09-16 on ticket **#2521** (lane `ready`). Signed off as architect / ready. Execution **#31886**._

This section is the ready-lane Design contract. It specifies *how* this ticket is delivered. It does **not** implement a product change and it does **not** design the superseded PM contractor-AI-advisor WIP.

Related tickets (do not absorb their work here): research **#2523** (DONE); sibling auto-capture **#2544** (DONE); parent epic **#2522**; deferred product epic **#2545**; gap **#2604** (rewrite PRD 26; PR **#832** already open); P0/P1 children **#2527**, **#2532**, **#2538**; meeting slices **#2533**–**#2536**.

### Verdict (R0, R1, R8)

**Verify-first no-op.** Brain chat #113 named `specs/builderforce/26-prd-advisor-platform.md`. This run `read_file`’d that path on bound repo **seanhogg/builderforce.ai** (`main`): **268 lines**, title “Advisor platform as marketplace composition (not a SCORE clone)”, **Board: epic #2545**. Chat #113 is not an uncommitted delta waiting on `builderforce/task-2521`.

- Architect deliverable = **this Design section** on the ticket PRD.
- Developer (next producer): re-confirm the file is on `main`; **no product patch**; **no** `specs/tasks/task-2521.md`.
- Must not: rewrite the file to the contractor AI advisor (PM WIP above); rewrite the file to the #2522 composition (**#2604**); treat GitHub 403 / failed glob / empty `search_code` as “file missing, so write it here”. `list_files` on `specs/builderforce/` returned the live path; `read_file` returned 268 lines.

### Layer A — Identity and bound tree (R0, R1, R6, R7)

| Fact | Evidence this run |
| --- | --- |
| Named file | `specs/builderforce/26-prd-advisor-platform.md` (auto-capture prefix `Builderforce.ai/` is the workspace folder, not a second path) |
| On `main` | 268 lines; SCORE-as-checklist; currently boards **#2545** A0–A5 |
| Spec path for epic #2522 | Same file. `specs/builderforce/README.md` item 26 already points at it as “SCORE-class advising … **Not a SCORE clone.**” |
| Implementation notes | `26-implementation-notes-advisor-platform.md` compose on the PRD; not this ticket; must not become a second PRD (#2593 / #2604) |
| Bound repo | seanhogg/builderforce.ai — checkout is populated. Search 403 is not absence (R5.7 / R6). |
| `specs/tasks/` on `main` | empty. A prior pass on `builderforce/task-2521` added `specs/tasks/task-2521.md` — **dead code / R5.7 fail**. Must not ship. Peer architect holds that path for delete; this slot does not re-add it. |

No new modules, routes, tables, listing kinds, or UI surfaces belong on this ticket.

### Layer B — Ownership split (R2, R3, R9)

| Concern | Owner | This ticket |
| --- | --- | --- |
| Product of record: marketplace-dogfooded advising; SCORE = capability checklist not a clone | epic **#2522** children (P0–P3) | do not ship |
| Rewrite PRD 26 so it describes only #2522; defer A2–A5 / `/marketplace?family=talent` Book bind / restricted Brain to **#2545** (explicit) | gap **#2604** (PR **#832** open, 5 commits ahead, in review) | do not rewrite |
| A0–A5 as the live 268-line file currently specifies | epic **#2545** | do not ship |
| Ticket PRD Requirements R0–R9 | BA, exec **31762**, signed | consume as contract |
| Ticket PRD Design | Architect, this slot | this section |
| Product / API / UI code | none on #2521 | verify-first |

In-scope for the **epic** (not for this ticket to design or build):

1. Talent listing/filter for Advisors (`category=advisors`) — not an entity, not a listing kind, not `/advisors`.
2. First-class meeting kind `advisory` (create, list, complete). Standup remains distinct. Not `TEAM_CEREMONY_KINDS`. Allowlist is **#2536**.
3. Meeting Notes agent scoped to advisory meetings.
4. Minutes → board via a generalised pull of today’s `pullStandupMinutesAct`.
5. Public Book on the advisor profile (generalise ATS `/book/[token]`; slices **#2532**–**#2536**).

Epic kill condition (not this ticket): a logged-out visitor discovers an advisor, books a slot, joins the meeting, and after it ends sees at least one board task and one proposed follow-up — no staff click.

### Layer C — Fail-closed (R4, R5)

If an implementer is unsure, they **write nothing** to the product tree. This Design does not specify APIs, schemas, or UX for the contractor AI advisor, and it does not specify the #2604 rewrite.

Do **not** implement, spec, or scaffold any of:

- Global “Ask Advisor” / contextual “Ask about this” on job, estimate, schedule, customer
- Advisor personas (Operations, Estimating, Customer, Financial pulse) as in-product LLM lenses
- Suggested actions that preview/accept/dismiss mutations of jobs, estimates, or CRM
- Retrieval over contractor workspace records with citations
- Org-admin disable flag for that AI surface
- The PM acceptance criterion that the spec “matches this document … so implementation agents do not diverge” — that would **fork** PRD 26 into a third product

#### R5 proof matrix (this ticket)

| # | Hard fail | Proof this slot does not trip it |
| --- | --- | --- |
| 1 | `/advisors` destination or marketplace family route | No frontend / `navGroups` change |
| 2 | `advisor` / `advisory` added to `MARKETPLACE_LISTING_KINDS` | No contract / enum change |
| 3 | Volunteer / pro-bono **table** | No schema. P3 badge + listing filters owned elsewhere |
| 4 | SCORE clone (trademark, SBA, chapter CMS, separate video) | No product design. File already says checklist-not-clone; do not regress |
| 5 | Punch `authMiddleware` off the meetings router | No API change. Join API is **#2535** |
| 6 | Meeting `kind` other than existing kinds or `advisory` | No meetings change. Allowlist is **#2536** |
| 7 | Dump `specs/tasks/task-2521.md`, `DONE.md`, or a stand-in spec | Architect does not create these. Prior-pass dump is forbidden; delete-only if present on the branch; do not re-add |
| 8 | Use PR **#829** as this ticket’s vehicle | #829 is **#2522**. Do not merge as-is (includes `task-2522.md` + unshipped-route claims) |
| 9 | Rewrite `26-prd-advisor-platform.md` to the contractor AI advisor **or** perform #2604’s rewrite here | Design forbids both. Rewrite stays **#2604** / PR **#832** |

### R3 inventory (live file — do not mutate on #2521)

The 268-line file on `main` currently specifies epic **#2545** tracks. This is the known fork vs #2522 that **#2604** exists to close.

| Track | What the live file says | Who owns the split |
| --- | --- | --- |
| **A0 Bind** | Talent listing ↔ `booking_services`; Book CTA on `/marketplace?family=talent` | #2604 defers Book bind to **#2545** |
| **A1 Session** | Reservation → meeting + calendar + restricted Brain thread | **#2545** |
| **A2 Group** | `one_to_many` join from the same listing | #2604 defers to **#2545** |
| **A3 Academy** | Stage-tagged `course` listings + enrolment | #2604 defers to **#2545** |
| **A4 Toolkit** | One pack per `BUSINESS_STAGES` | #2604 defers to **#2545** |
| **A5 Engagement** | Continue-with-this-advisor | #2604 defers to **#2545** |

Shared non-goals already in the file that **stay** on every ticket (do not regress):

- Do not add `advisor` to `MARKETPLACE_LISTING_KINDS`
- No `/advisors` destination
- No volunteer / chapter table
- SCORE is a checklist, not a clone
- No sixth canvas `LISTING_LAUNCH_MODE`
- Mentoring confidentiality defaults `restricted`; do not invent a sixth confidentiality level

### Components / data flow

**None on this ticket.** No new services, no new DTOs, no new UI.

Downstream (other tickets, after their own Design):

```
#2522 composition (file text owned by #2604)
  Talent filter category=advisors  →  existing Talent family (person|gig)
  meeting kind=advisory            →  existing meetings router (authMiddleware stays)
  Meeting Notes agent              →  advisory meetings only
  minutes → board                  →  generalised pullStandupMinutesAct
  public Book                      →  generalise ATS /book/[token] (#2532–#2536)

#2545 (live file until #2604 splits)
  A0 Book bind → A1 restricted thread → A2–A5 workshops / Academy / packs / engagement
```

### Developer contract

1. Verify `specs/builderforce/26-prd-advisor-platform.md` is on `main` (268 lines, boards #2545). That is the whole product check.
2. No product-repo patch. No rewrite of the named spec.
3. If `specs/tasks/task-2521.md` exists on `builderforce/task-2521`, **delete it** (R5.7). Do not add it. `main` already lacks it.
4. Do not use PR #829 as this ticket’s vehicle.
5. Implementation Notes on this ticket PRD: record the verify-first result only — not a second PRD.

### Done when (this architect slot)

This Design is recorded on the ticket PRD. No product file change is required from Architect. File rewrite remains **#2604**. Product implementation remains **#2522** children. Reviewer / QA / Validator confirm no unexpected product diff and that the PM contractor-AI-advisor WIP was not implemented.

_— authored by CTO (architect) · 2026-09-16 · execution #31886_

_— rewritten by CTO · 2026-09-16T16:23:13.900Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T16:03:48.931Z · execution #31762

BA 2026-09-16 (lane ready, exec 31762): Requirements R0–R9 signed on this ticket PRD. Ticket identity = auto-capture of Brain chat #113 against `specs/builderforce/26-prd-advisor-platform.md`, which already exists on `main` (268 lines, boards epic #2545 A0–A5). This BA slot is docs-only / verify-first no-op — no product-repo patch, no `specs/tasks/task-2521.md`. The PM WIP on this PRD (in-product AI Advisor for home-services contractors) is **superseded**; product of record is epic #2522; file rewrite is gap #2604. Architect/Developer must not implement the contractor AI advisor and must not absorb #2604.

---

### Update — CTO · 2026-09-16T16:22:47.106Z · execution #31885

Architect 2026-09-16 (lane ready, exec 31886): Design D0–D6 signed on this ticket PRD. Verdict = verify-first no-op. Named spec `specs/builderforce/26-prd-advisor-platform.md` re-read on main (268 lines, boards #2545). Forbidden dump `specs/tasks/task-2521.md` deleted from `builderforce/task-2521` (R5.7). Do not rewrite PRD 26 here (#2604 / PR #832). Do not use PR #829. Do not design the contractor AI advisor. Developer: confirm file on main; no product patch; do not recreate the dump.

---

### Update — CTO · 2026-09-16T16:23:52.434Z · execution #31886

Architect 2026-09-16 (lane ready, exec 31886): Design authored on this ticket PRD. Verdict = verify-first no-op. Named spec `specs/builderforce/26-prd-advisor-platform.md` already on main (268 lines, boards #2545). Do not rewrite (#2604 / PR #832). Do not implement contractor AI advisor. Do not dump or re-add `specs/tasks/task-2521.md` (R5.7). No product-repo patch from Architect. Developer next: confirm file on main; delete-only of any leftover task dump; Implementation Notes = verify-first result only.