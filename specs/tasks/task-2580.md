# PRD: Advisors Marketplace Category Filters

**Status:** WIP  
**Surface:** Marketplace listing, `?category=advisors`  
**Constraint:** Category-specific filters must not change behavior for any other marketplace category.

---

## Problem & Goal

**Problem.** Marketplace search for advisors is under-specified. Buyers landing on `?category=advisors` cannot narrow listings by the dimensions that actually decide a match: what the advisor knows, what language they work in, how they deliver, and which company stage they typically serve. Existing marketplace filters are either generic (price, rating, availability) or owned by other categories. Applying advisor dimensions globally would pollute other category UIs and query contracts. Omitting them leaves advisor discovery as a long, unfiltered scroll.

**Goal.** On `category=advisors` only, expose four first-class filters—**expertise**, **language**, **method**, **stage**—that compose with existing marketplace query, sort, and pagination. All other categories keep their current filter sets, URLs, empty states, and result counts. No shared filter schema, facet API, or listing card should change for non-advisor categories.

**Success (directional).** Advisor-category sessions use at least one of the four filters; filtered result sets match listing metadata; non-advisor category traffic, bounce, and conversion are unchanged.

---

## Target users / ICP roles

| Role | Need |
|---|---|
| **Founder / operator (buyer)** | Find an advisor who has done this domain, speaks this language, works this way, at this stage—without reading every profile. |
| **Operator / hiring manager (buyer)** | Share a filtered advisors URL with stakeholders; expect the same set on reload. |
| **Advisor (seller)** | Be discoverable when their expertise, languages, methods, and stages are filled in; not appear under the wrong facets. |
| **Marketplace ops / catalog** | Maintain advisor facet taxonomies without forking the whole marketplace filter stack. |

Primary ICP for this change: buyers on the advisors category. Sellers are affected only via listing metadata and facet coverage.

---

## Scope

**In scope**

- Advisors category listing (`/marketplace` or equivalent with `category=advisors`).
- Four filters: expertise, language, method, stage.
- URL query params that round-trip those filters (shareable, reload-safe, back-button-safe).
- Facet values sourced from advisor listing metadata / taxonomy, not free text on the listing page.
- Composition with existing advisors-applicable controls (search query, sort, price/rate if already present, pagination).
- Empty, zero-result, and "clear filters" states for this category only.
- Isolation: other `category=*` routes, default (no category) marketplace, and any category switcher must not gain, lose, or remap filters.

**Assumptions**

- Advisor listings already (or will, via a separate catalog task) store expertise, language, method, and stage as structured fields.
- Marketplace already keys listings and filters by `category`.
- Multi-select is the default interaction unless a taxonomy is strictly single-value (stage may be single- or multi-select; both must be defined in implementation, not mixed per session).

---

## Functional requirements

### 1. Category gating

- The four filters render **only** when the active category is `advisors` (query `category=advisors` on `/marketplace`). This param is additive; it does not replace `family` / `kind` and does not create a `/advisors` route or a `MARKETPLACE_LISTING_KINDS` value.
- Switching away from advisors removes those controls and **drops** advisor-only params (`expertise`, `language`, `method`, `stage`, `category=advisors`) from the URL. Other-category params are not rewritten except to remove advisor-only keys.
- Switching **to** advisors does not inherit inapplicable filters from the previous family (e.g. asset kind, product condition, job type). Inapplicable params are stripped; applicable shared params (`q`, `sort`) may persist; `page` resets to 1.
- Default marketplace (no category) and every non-advisor / non-talent family keep their current filter chrome and query contract. `category=advisors` pasted onto a non-talent family is ignored for chrome and predicates (keys stripped).

### 2. Filter: expertise

- Multi-select from `EXPERTISE_AREAS` (`@builderforce/creation-canvas-contract` / `startupListing.ts`: `product`, `marketing`, `operations`, `finance`, `legal`, `people`, `technology`, and any further members of that exported array).
- Match: listing `expertise` includes **at least one** selected value (OR within the filter).
- Facet counts reflect the current result set after other applied filters (AND across filter groups).
- Do not ship a parallel GTM / fundraising / "technical architecture" chip set unless those slugs already exist in `EXPERTISE_AREAS`.

### 3. Filter: language

- Multi-select from the #2528 closed ISO 639-1 list: `en, es, fr, de, pt, zh, ja, ko, hi, ar, it, nl, pl, ru, tr, vi, id, th, sv, uk`.
- Codes in data and URL; localized labels in UI.
- Match: listing `languages` offers **at least one** selected language (OR within the filter).
- Do not invent languages outside that list. Unused codes may show count `0` and follow the existing marketplace facet pattern.

### 4. Filter: method

- Multi-select from listing `methods`: `video`, `phone`, `email`, `in-person` only (#2528).
- Match: listing includes **at least one** selected method (OR within the filter).
- Do not ship 1:1 / group / retained / project / workshop / async as method values.

### 5. Filter: stage

- Multi-select from listing `stages`: `start`, `grow`, `exit` only (#2528). OR within the filter. Do not mix single-select in the same UI.
- Match against stored `stages` only — not inferred from buyer profile, and **not** `BUSINESS_STAGES` (pre-seed / seed / series A / …).

### 6. Composition and query

- Cross-filter logic: **AND** between the four groups; **OR** within a multi-select group.
- Combine with existing search `q` (AND): text search still applies to advisor name, bio, and other currently searched fields.
- Combine with existing sort and page size; applying or changing a filter resets to page 1.
- Result count and empty state must match the filtered set.
- Deep link example (normative slugs):
  `?category=advisors&expertise=product,finance&language=en,es&method=video&stage=start`
- Unknown or malformed advisor filter values are ignored (not 500); remaining valid filters still apply.
- Clearing all advisor filters returns the unfiltered advisors category set.

### 7. Listing eligibility

- Only **published** talent listings in the advisors view (`category=advisors` → Talent family) are facetable and returnable. Company `SEEKING_TYPES` `'advisors'` rows are not this population.
- Listings missing a given field do not match a positive filter on that field; they remain visible when that filter is unused.
- Filter application is server-authoritative (or equivalent API). Client-only hiding of cards is not sufficient.
- Fields are consumed from #2528 (`freelancer_profiles`); this ticket does not create them.

### 8. Isolation / non-regression

- Non-advisor category pages must not request advisor facet endpoints as a hard dependency of first paint.
- Shared listing/filter components must branch on category; advisor taxonomies are not merged into global facet enums.
- Analytics, ads, SEO canonicals, and sitemap behavior for other categories unchanged.
- API consumers of non-advisor listing endpoints see no new required params and no changed default result sets.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:56:10.446Z_

---

## Acceptance criteria

1. **Advisors-only chrome.** Given `category=advisors`, the listing UI shows expertise, language, method, and stage. Given any other category or no category, those four controls are absent.

2. **Correct AND/OR.** Selecting two expertises and one language returns listings that have at least one of those expertises **and** that language. Counts and cards agree.

3. **URL round-trip.** Applying filters updates the query string. Opening that URL in a new session restores the same filters, order-independent param values, and the same result set (stable sort).

4. **Category switch.** From advisors with filters applied, switching to another category removes advisor params and does not show advisor filters. Switching back to advisors does not resurrect the previous advisor filters unless they are still in the URL.

5. **Pagination / sort.** Changing any of the four filters resets to page 1 and keeps the current sort. Changing page or sort does not clear advisor filters.

6. **Empty and clear.** A combination with zero listings shows the existing marketplace empty/zero-result pattern (advisors copy is allowed). "Clear filters" removes the four params and restores the full advisors set.

7. **Bad values.** `?category=advisors&expertise=not-a-real-slug` does not error; invalid values are dropped; valid siblings still filter.

8. **Metadata fidelity.** A listing is returned for a filter only if its stored expertise / language / method / stage fields justify the match. No client-side-only filtering that disagrees with the API.

9. **Other categories unchanged.** For each existing non-advisor category: same filter set, same default results for the same URL, same result count for representative queries, no new query params required, no visual regression on filter chrome. Smoke at least one category that already has its own custom filters and one that does not.

10. **Performance envelope.** Facet load and filtered listing response stay within the current marketplace listing SLO for comparable result sizes; no N+1 per facet on the listing request.

11. **A11y.** Filters are keyboard-operable, labelled, and announced consistently with existing marketplace filter controls (including selected-count and clear).

---

## Out of scope

- Creating, editing, or backfilling advisor listing fields (expertise, language, method, stage) in the seller onboarding or profile editor—catalog/taxonomy work is a dependency, not this PRD.
- New marketplace categories, or renaming `advisors`.
- Recommendation / ranking model changes (e.g. "best match" using these fields).
- Buyer-profile auto-application of language or stage.
- Geo, price, rating, availability, or calendar filters (keep as they are if already on advisors; do not add here).
- Free-text tag clouds or user-generated facet values on the listing page.
- Saved searches, alerts, or email digests for these filters.
- Localization of the marketplace chrome beyond using existing i18n patterns for new labels.
- Mobile-specific redesign of the filter drawer (follow existing marketplace filter IA).
- Admin taxonomy CMS, unless required to ship the four closed lists.
- Breaking API changes for non-advisor clients; versioned additive advisor query params only.
- Ads, featured placements, or paid boost interacting with these facets.

## Requirements

_Owned by the business-analyst. Normative for implementation. Functional-requirement examples above (GTM / 1:1 / pre-seed, etc.) are **non-normative** — they must not be shipped as taxonomies._

### Locked decisions

1. **No new listing kind, no `/advisors` route.** Do not add `advisor` (or `advisors`) to `MARKETPLACE_LISTING_KINDS`. Do not ship a parallel Advisor storefront. Advisors remain talent. This matches PRD 26 FR-1.4, #2528, and epic #2579 ("compose only").
2. **Honor `?category=advisors` as an additive marketplace query param** on the existing `/marketplace` listing. It is **not** in the repo today (0 matches). It does **not** replace `family` / `kind`. It selects the advisors talent view and is the **only** gate for the four filters.
3. **Do not confuse company `SEEKING_TYPES` `'advisors'`** (`companies.seeking` in `startupListing.ts` — startups looking *for* advisors) with this buyer view. This ticket filters **published talent listings**, not company directory rows.
4. **Vocab is #2528 + contract constants — not the PM examples.** Single source of truth; do not fork parallel enums in the marketplace UI or API.
5. **Stage is multi-select.** #2528 stores `stages` as `string[]` (`start | grow | exit`). OR within the group. Do **not** use `BUSINESS_STAGES` (pre-seed / seed / series A / …). Do not mix single-select and multi-select in one session.
6. **This ticket does not create, edit, or backfill profile fields.** Persistence is #2528 (`freelancer_profiles`: `expertise`, `languages`, `methods`, `stages`). If a column is empty/`[]`, a positive filter on that field does not match; the listing still appears when that filter is unused.
7. **Server-authoritative.** Client-only hiding of cards is not sufficient. Additive optional query params only; non-advisor listing endpoints gain **no** new required params and **no** changed defaults.
8. **Siblings stay out.** Visitor chat = #2581. Resources tab = #2582. Volunteer badge / price `0` = #2584. Location filter = #2585. Industries, session length, session price, confidentiality, geo, rating, availability are **not** filters here.

### Population and chrome gate

- **Population:** published talent listings on `/marketplace` (Talent family: `family=talent`). Default marketplace and every non-talent family are unchanged.
- **Chrome gate:** the four filters render **if and only if** the active query has `category=advisors`.
- If `category=advisors` is set and `family` is omitted, treat the view as Talent (`family=talent` implied for results + chrome). Do not switch the user into Assets.
- If `category=advisors` is pasted onto a **non-talent** family (`family=asset` or any non-talent value), **do not** render the four filters, **do not** apply advisor field predicates, and **do not** fail first paint. Strip the advisor-only keys (see R-9) rather than merging taxonomies.
- Switching **to** `category=advisors` does not inherit inapplicable other-family params (asset kind chips, product condition, job type, etc.). Shared params `q`, `sort` may persist; `page` resets to 1.

### Closed vocabularies (normative)

Import; do not copy-paste a second list into marketplace code.

| Filter | Query key | Stored field (#2528) | Allowed values | Match |
|---|---|---|---|---|
| Expertise | `expertise` | `expertise` `string[]` | `EXPERTISE_AREAS` from `@builderforce/creation-canvas-contract` (`startupListing.ts`: `product`, `marketing`, `operations`, `finance`, `legal`, `people`, `technology`, plus any further members of that array such as fundraising / international / BD — **the exported array wins**) | listing includes **at least one** selected value (OR) |
| Language | `language` | `languages` `string[]` | ISO 639-1 closed list from #2528: `en, es, fr, de, pt, zh, ja, ko, hi, ar, it, nl, pl, ru, tr, vi, id, th, sv, uk` | at least one selected (OR) |
| Method | `method` | `methods` `string[]` | `video \| phone \| email \| in-person` only | at least one selected (OR) |
| Stage | `stage` | `stages` `string[]` | `start \| grow \| exit` only | at least one selected (OR) |

- Codes in the URL and API; localized labels in the UI via existing i18n / `startups.vocab.*` patterns. Do not invent labels that imply a different taxonomy (no "Series A", no "1:1", no "GTM" unless that slug already exists in `EXPERTISE_AREAS`).
- Facet values come from these closed lists, not free text on the listing page. Unused values may show count `0` and follow the existing marketplace facet pattern (disable vs allow empty apply). Do not invent languages outside the ISO list.

**Rejected (do not implement):** method = 1:1 / group / retained / project / workshop / async; stage = pre-seed / seed / series A / series B+ / growth / late / any; expertise = a hand-written GTM/fundraising chip set that is not `EXPERTISE_AREAS`.

### URL contract

Deep-link shape (order of keys and of comma-separated values is insignificant):

```
/marketplace?category=advisors&expertise=product,finance&language=en,es&method=video&stage=start
```

- Values are the slugs above (e.g. `method=in-person`, `stage=grow`). Comma-separated multi-select. Repeat-key vs comma form: pick the existing marketplace convention if one exists; otherwise comma-separated.
- Compose with existing `family`, `kind`, `q`, sort, and page params; do not rename them.
- Unknown or malformed advisor values are **ignored** (not 500); remaining valid advisor filters still apply. Example: `expertise=not-a-real-slug&language=en` → drop the bad expertise, still filter language `en`.
- Opening the URL in a new session restores the same filters and the same result set under the current stable sort.
- Clearing all four advisor params (or "Clear filters") returns the unfiltered `category=advisors` set.

### Cross-filter logic

- **AND** across the four groups; **AND** with existing `q` (name, bio, and any fields already searched).
- **OR** within a multi-select group.
- Facet counts reflect the current result set **after** the other applied groups (AND across groups).
- Changing any of the four filters resets to page 1 and keeps the current sort. Changing page or sort does not clear advisor filters.
- Result count and empty/zero-result chrome must match the filtered set (existing marketplace empty pattern; advisors copy allowed).

### Isolation / non-regression

- Non-advisor (and no-category) pages must not request advisor facet endpoints as a **hard dependency of first paint**.
- Shared listing/filter components branch on `category=advisors`; advisor taxonomies are not merged into global facet enums.
- Analytics, ads, SEO canonicals, and sitemap behavior for other families/kinds unchanged.
- Smoke at least one family that already has custom filters (e.g. Talent `kind=gig`) and one that does not (e.g. an asset kind). Same filter set, same default results for the same URL, same result count for representative queries.

### User stories

- **US-1** As a founder on `/marketplace?category=advisors`, I see expertise, language, method, and stage, and I do not see them on any other family or on marketplace with no category.
- **US-2** As a hiring manager, I select two expertises and one language and get listings that have at least one of those expertises **and** that language; the count matches the cards.
- **US-3** As a stakeholder, I open a shared filtered URL and see the same filters and the same result set (order-independent params, stable sort).
- **US-4** As a buyer, I switch from advisors (with filters) to another family: advisor chrome and advisor params are gone; going back to advisors does not resurrect filters unless they are still in the URL.
- **US-5** As a buyer, changing a filter jumps to page 1; paging or sorting does not clear filters; zero results show the empty pattern; Clear filters restores the full advisors set.
- **US-6** As an advisor, I am returned for a filter only when my stored `expertise` / `languages` / `methods` / `stages` justify the match. Unpublished listings never appear.

### Numbered requirements

- **R-1 Category gate.** Four filters render only when `category=advisors` is the active marketplace category. Absent or any other category: those controls are not in the DOM and advisor predicates are not applied.
- **R-2 Additive `category` param.** Implement `category=advisors` on `/marketplace` without adding a `MARKETPLACE_LISTING_KINDS` value and without a `/advisors` route. Non-talent `family` values ignore the advisor chrome (see Population).
- **R-3 Expertise.** Multi-select from `EXPERTISE_AREAS`. OR within. AND with other groups. Query key `expertise`.
- **R-4 Language.** Multi-select from the #2528 ISO list. Codes in data; localized labels in UI. OR within. Query key `language`. Do not invent codes.
- **R-5 Method.** Multi-select `video | phone | email | in-person`. OR within. Query key `method`.
- **R-6 Stage.** Multi-select `start | grow | exit`. OR within. Query key `stage`. Not `BUSINESS_STAGES`.
- **R-7 Composition.** AND across groups and with `q`. Filter change → page 1, keep sort. Page/sort change does not clear the four params.
- **R-8 URL round-trip.** Applying filters writes the query string. Reload / new session / back button restore filters and results. Bad slugs dropped; siblings still apply.
- **R-9 Category switch.** Leaving advisors **drops** `expertise`, `language`, `method`, `stage`, and `category=advisors`. Other-family params are not rewritten except to remove those keys. Entering advisors strips inapplicable other-family params; does not resurrect prior advisor filters unless they remain in the URL.
- **R-10 Eligibility.** Only **published** talent listings are facetable and returnable. Missing field ⇒ no match on a positive filter for that field; still visible when that filter is unused. Filter application is server-authoritative.
- **R-11 Empty and clear.** Zero listings → existing marketplace empty/zero-result pattern. Clear filters removes the four params and restores the unfiltered advisors set.
- **R-12 Isolation.** Other families/kinds: same chrome, same default results, no new required params, no advisor facet on first paint, no global enum merge. No SCORE branding, no new CMS/LMS.
- **R-13 A11y.** Filters are keyboard-operable, labelled, and announced consistently with existing marketplace filter controls (selected-count and clear included).
- **R-14 Performance.** Facet load and filtered listing stay within the current marketplace listing SLO for comparable sizes; no N+1 per facet on the listing request.
- **R-15 Single vocab source.** Marketplace UI and API import `EXPERTISE_AREAS` and the #2528 method/stage/language lists (lockstep with `listing.ts` / `talentFields.ts` as #2528). No duplicated KINDS-style chip arrays that can drift (the ScheduleMeetingPanel / `meetingRoutes.ts` failure mode).

### Traceability (Acceptance criteria)

| AC | Requirements |
|---|---|
| 1 Advisors-only chrome | R-1, R-2 |
| 2 Correct AND/OR | R-3–R-7 |
| 3 URL round-trip | R-8 |
| 4 Category switch | R-9 |
| 5 Pagination / sort | R-7 |
| 6 Empty and clear | R-11 |
| 7 Bad values | R-8 |
| 8 Metadata fidelity | R-10, Locked #6–#7 |
| 9 Other categories unchanged | R-12 |
| 10 Performance envelope | R-14 |
| 11 A11y | R-13 |

### Dependencies (not this ticket)

- **#2528** — profile columns and editor for expertise / languages / methods / stages. This ticket consumes them.
- Epic **#2579** sequencing: do not start implementation until the P0+P1 kill condition on **#2522** passes (#2527, #2532, #2538) as the epic states.
- **#2581 / #2582 / #2584 / #2585** — sibling P2/P3 surfaces; do not implement here.

### Out of scope (this slice)

Creating or backfilling advisor fields; new marketplace categories or renaming `advisors`; `MARKETPLACE_LISTING_KINDS` / `/advisors`; recommendation/ranking model; buyer-profile auto-application of language or stage; geo / price / rating / availability / calendar filters; industries filter; free-text tag clouds; saved searches / alerts; admin taxonomy CMS; ads / featured / paid boost; SCORE branding; new CMS/LMS; visitor chat; Resources tab.

_— authored by Ada (Business Analyst) · 2026-09-16_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:55:34.257Z_

## Design

_Owned by the architect — authored by Architect · 2026-09-16 · execution #32014_

### Design Overview

This Design is the filter-specific architecture for ticket #2580. It inherits the layered compose model from the parent epic #2579 Design (D-1–D-6, CTO exec #31990) and provides the granular component, API, URL, and data-path decisions for the four advisor filters only. Sibling concerns (#2581 visitor chat, #2582 Resources tab) are out of scope for this Design.

**Principle.** The query string is the single source of truth for filter state. Each multi-select control reads its value from `router.query`, displays selected chips, and writes back via `router.replace`. This follows the existing marketplace pattern for `discipline` and `sort`. The four new controls are rendered inside the existing `CatalogToolbar` slot, gated on `category=advisors`.

---

### D-2580.1 Component Architecture

```
MarketplacePageClient (existing)
└── CatalogToolbar (existing slot)
    ├── discipline <Select>         (existing, all categories)
    ├── sort <Select>               (existing, all categories)
    ├── <AdvisorFilters />          (NEW, advisors only)
    │   ├── ExpertiseMultiSelect    → EXPERTISE_AREAS
    │   ├── LanguageMultiSelect     → P0 ISO 639-1 option list
    │   ├── MethodMultiSelect       → video | phone | email | in-person
    │   └── StageMultiSelect        → start | grow | exit
    └── resultCount + listing grid  (existing)
```

#### D-2580.1.1 New files

| File | Purpose |
|---|---|
| `frontend/src/app/marketplace/AdvisorFilters.tsx` | Container: renders the four multi-select controls, gates on `category=advisors`, manages URL read/write |
| `frontend/src/app/marketplace/AdvisorFilters.module.css` | Scoped styles (if needed; prefer existing tokens) |

#### D-2580.1.2 Modified files

| File | Change |
|---|---|
| `frontend/src/app/marketplace/MarketplacePageClient.tsx` | Import and render `<AdvisorFilters />` inside `CatalogToolbar` when `category=advisors` |
| `frontend/src/lib/marketplaceApi.ts` (or equivalent API client) | Forward the four query keys to the freelancers endpoint when `category=advisors` |
| API route: `GET /api/public/freelancers` | Accept and apply optional query params `expertise`, `language`, `method`, `stage` (server-side) |

#### D-2580.1.3 No new files outside the marketplace filter scope

- No new routes. No new API endpoints.
- No changes to `MARKETPLACE_LISTING_KINDS`, `PEOPLE_OBJECT_KINDS`, or any shared enum.
- No changes to profile editor (#2528 owns fields), chat (#2581), Resources tab (#2582), or listing cards.

---

### D-2580.2 Multi-Select Widget Pattern

**Decision: Multi-select for all four filters.** #2580 Locked Decision #5 and the PO amendment to #2579 (exec #31891) both confirm multi-select. Each control allows selecting zero or more values; selected values render as chips/tags inside the control. The parent #2579 R2.2's "single-select" hedge is superseded by #2580's Locked #5 and the PO amendment.

**Widget implementation approach (choose one during implementation, based on existing codebase patterns):**

1. **Preferred: Reuse the existing marketplace multi-select pattern if one exists.** Search the codebase for a multi-select chip component already used in marketplace filters. If found, use it.
2. **Fallback: A `<MultiSelect>` component composed from the existing design system primitives.** A dropdown with checkboxes + selected-value chips outside the dropdown. Match the existing `filterControlStyle` token for sizing, border, and radius.

**Multi-select behavior:**
- Opening the dropdown shows all allowed values with checkmarks for selected items.
- Selected values render as removable chips outside (or inside) the control.
- Clicking a chip's × removes that value and updates the URL.
- "Clear all" link/button in the `AdvisorFilters` container removes all four keys from the URL.
- Each control has an `aria-label` matching the existing `discipline`/`sort` pattern.

---

### D-2580.3 Visibility Gate

```ts
// MarketplacePageClient.tsx (conceptual)
const isAdvisorsCategory = router.query.category === 'advisors';

// Also gate on family: advisors are talent; non-talent families ignore the category param
const isTalentFamily = !router.query.family || router.query.family === 'talent';

const showAdvisorFilters = isAdvisorsCategory && isTalentFamily;
```

**Gate truth table:**

| `category` | `family` | Show advisor filters? | Apply advisor predicates? |
|---|---|---|---|
| `advisors` | (absent) | ✅ Yes — implied Talent | ✅ Yes |
| `advisors` | `talent` | ✅ Yes | ✅ Yes |
| `advisors` | `asset` or any non-talent | ❌ No | ❌ No — strip keys |
| (absent) | (any) | ❌ No | ❌ No |
| `freelancers` or any non-advisor | (any) | ❌ No | ❌ No |

When `showAdvisorFilters` is `false`:
- `<AdvisorFilters />` is not rendered.
- The four query keys (`expertise`, `language`, `method`, `stage`) are never read or applied.
- No advisor facet API calls are made on first paint.

---

### D-2580.4 URL Contract

**Query key format: comma-separated values.**

```
/marketplace?category=advisors&expertise=product,finance&language=en,es&method=video&stage=start
```

- Multi-select values are comma-joined in a single key occurrence. If the existing marketplace convention uses repeat-key (`&key=a&key=b`), defer to that convention. Otherwise comma-separated.
- Compose with existing params: `family`, `kind`, `q`, `sort`, `page`, `limit`.
- Do not rename existing params. Do not add `advisor` to param names.

**Parsing (conceptual):**

```ts
function parseAdvisorFilters(query: ParsedUrlQuery): AdvisorFilters {
  const parse = (key: string, valid: Set<string>): string[] => {
    const raw = query[key];
    if (!raw) return [];
    const values = typeof raw === 'string' ? raw.split(',') : raw;
    return values.filter(v => valid.has(v));
  };
  return {
    expertise: parse('expertise', EXPERTISE_SLUGS),
    language: parse('language', LANGUAGE_CODES),
    method: parse('method', METHOD_SLUGS),
    stage: parse('stage', STAGE_SLUGS),
  };
}
```

**Writing (conceptual):**

```ts
function writeAdvisorFilter(
  currentQuery: ParsedUrlQuery,
  key: string,
  values: string[]
): void {
  const next = { ...currentQuery, page: '1' };
  if (values.length > 0) {
    next[key] = values.join(',');
  } else {
    delete next[key];
  }
  router.replace({ query: next }, undefined, { shallow: true });
}
```

**Key lifecycle:**

| Event | Action |
|---|---|
| User selects a value | Append to array, write comma-joined to query, reset page to 1 |
| User deselects a value | Remove from array; if array becomes empty, delete key from query; reset page to 1 |
| User clicks "Clear all" | Delete all four keys; keep `category=advisors`, `family`, `kind` if present |
| Page load / back / forward / shared link | Hydrate controls from `router.query` using `parseAdvisorFilters` |
| Invalid query value (e.g. `?stage=bogus`) | Drop from parsed result; control shows no selection for that key; no error |
| Category switch away from advisors | Delete all four advisor keys + `category` from query |
| Category switch to advisors | Strip inapplicable other-family params; do not resurrect prior advisor filters |

---

### D-2580.5 API Integration

#### D-2580.5.1 Client-side

The marketplace API client (e.g. `frontend/src/lib/marketplaceApi.ts` or the hook that calls `GET /api/public/freelancers`) forwards the four query keys **only when** `category=advisors`:

```ts
// Conceptual — in the freelancers fetch hook
const params: Record<string, string> = {
  q: query.q ?? '',
  page: String(query.page ?? 1),
  limit: String(query.limit ?? 20),
  sort: query.sort ?? defaultSort,
};

// Only forward advisor filter keys when the gate is open
if (query.category === 'advisors') {
  if (query.expertise) params.expertise = query.expertise;
  if (query.language)  params.language  = query.language;
  if (query.method)    params.method    = query.method;
  if (query.stage)     params.stage     = query.stage;
}

// Existing params (discipline, etc.) pass through unchanged
```

#### D-2580.5.2 Server-side

`GET /api/public/freelancers` gains four **optional** query params. They are ignored when the request is not in the advisors category (server-side guard).

| Query param | DB column (from #2528 `freelancer_profiles`) | Filter SQL (conceptual) |
|---|---|---|
| `expertise` | `expertise` (`text[]` or JSONB array) | `WHERE expertise && ARRAY[$1, $2, ...]` (overlap) |
| `language` | `languages` (`text[]` or JSONB array) | `WHERE languages && ARRAY[$1, $2, ...]` (overlap) |
| `method` | `methods` (`text[]` or JSONB array) | `WHERE methods && ARRAY[$1, $2, ...]` (overlap) |
| `stage` | `stages` (`text[]` or JSONB array) | `WHERE stages && ARRAY[$1, $2, ...]` (overlap) |

- **Array overlap** (`&&`) implements OR-within-group: the listing matches if its array shares at least one element with the query values.
- **AND across groups:** each non-empty filter adds an additional `WHERE` clause, combined with `AND`.
- **AND with `q`:** the existing text search (`q`) is an additional `AND` condition.
- **Invalid values:** parsed server-side against the closed vocabularies; unknown slugs are silently dropped before building the SQL query.
- **Server-side category guard:** the four params are only applied when the request context indicates `category=advisors`. If the guard fails (e.g. a non-advisor caller passes them), they are ignored.
- **No new required params:** the endpoint works identically for non-advisor callers.
- **Published only:** the existing talent listing filter (`visibility = published`) already applies; no change needed.

#### D-2580.5.3 Facet counts

Facet counts (the number shown next to each option, e.g. "Product (12)") are **optional for the initial implementation**. If the existing marketplace pattern computes facet counts server-side and returns them alongside results, add the four advisor facets to that response shape. If counts are not yet a pattern, omit them rather than introducing N+1 queries — the zero-result empty state (D-2580.7) covers discoverability.

**If facet counts are implemented:** they must reflect the current result set after other applied filters (AND across groups). Each count query is scoped to the currently-applied filters from the other three groups.

**R-14 compliance:** do not issue one query per facet option. Batch counts into a single query (e.g. a `GROUP BY` with conditional counts, or a single facet-count endpoint that returns counts for all four groups in one response).

---

### D-2580.6 Sort, Pagination, and Composition

**Sort:** No new sort control. Advisors use the existing marketplace/talent `sort` query param (same values). Applying or changing any filter resets `page` to 1 but preserves the current `sort`.

**Pagination:** Existing pagination (`page`, `limit`). Changing page or limit does not clear the four advisor filter keys.

**Composition matrix:**

| Action | `page` | `sort` | advisor filters | `q` |
|---|---|---|---|---|
| Select/deselect a filter value | → 1 | kept | updated | kept |
| Change sort | → 1 | updated | kept | kept |
| Change page | kept | kept | kept | kept |
| Change `q` (search) | → 1 | kept | kept | updated |
| Clear all filters | → 1 | kept | deleted | kept |
| Switch to advisors | → 1 | kept | empty (unless in URL) | kept |
| Switch away from advisors | → 1 | kept | deleted | kept |

---

### D-2580.7 Empty and Zero-Result States

**Zero results (filters applied, no matches):** Use the existing marketplace empty/zero-result pattern. Copy: "No advisors match these filters." (i18n key: `marketplace.advisors.emptyFiltered`). Same layout, same component as other marketplace empty states. No SCORE or "become a mentor" copy.

**Unfiltered advisors with zero listings:** Use the existing empty state for the Talent family (no change).

**Clear filters button:** An inline "Clear filters" control inside `<AdvisorFilters />`. Removes all four keys; keeps `category=advisors`. After clearing, the unfiltered advisors set loads.

---

### D-2580.8 Category Switch Isolation

**Leaving advisors:**
```ts
function onCategoryChange(newCategory: string) {
  if (newCategory !== 'advisors') {
    const next = { ...router.query };
    delete next.expertise;
    delete next.language;
    delete next.method;
    delete next.stage;
    delete next.category; // or update to newCategory
    next.page = '1';
    router.replace({ query: next });
  }
}
```

**Entering advisors:**
```ts
function onCategoryChange(newCategory: string) {
  if (newCategory === 'advisors') {
    const next = { ...router.query, category: 'advisors', page: '1' };
    // Strip inapplicable other-family params
    const advisorParamKeys = new Set([
      'category', 'expertise', 'language', 'method', 'stage',
      'q', 'sort', 'page', 'limit', 'family', 'kind'
    ]);
    for (const key of Object.keys(next)) {
      if (!advisorParamKeys.has(key)) delete next[key];
    }
    // Do not resurrect prior advisor filters — only keep if already in URL
    router.replace({ query: next });
  }
}
```

---

### D-2580.9 i18n

New strings in the `marketplace` namespace (all five locales):

| Key | English default |
|---|---|
| `marketplace.advisorFilters.expertise` | "Expertise" |
| `marketplace.advisorFilters.language` | "Language" |
| `marketplace.advisorFilters.method` | "Method" |
| `marketplace.advisorFilters.stage` | "Stage" |
| `marketplace.advisorFilters.clearAll` | "Clear filters" |
| `marketplace.advisorFilters.expertisePlaceholder` | "Any expertise" |
| `marketplace.advisorFilters.languagePlaceholder` | "Any language" |
| `marketplace.advisorFilters.methodPlaceholder` | "Any method" |
| `marketplace.advisorFilters.stagePlaceholder` | "Any stage" |
| `marketplace.advisors.emptyFiltered` | "No advisors match these filters." |
| `marketplace.advisors.emptyFilteredDetail` | "Try adjusting or clearing your filters." |

Language labels (localized names for each ISO 639-1 code) may reuse existing locale data if available (e.g. `Intl.DisplayNames`). If not, add static label maps to the `marketplace` namespace.

Method and stage labels (e.g. "Video call", "Phone call", "Email", "In person"; "Start", "Grow", "Exit") go in the `marketplace` namespace.

---

### D-2580.10 Accessibility

- Each multi-select control has an `aria-label` matching the existing `discipline`/`sort` pattern.
- Selected-value count is announced (e.g. "Expertise, 2 selected").
- Dropdown is keyboard-operable: `Enter`/`Space` to open, arrow keys to navigate options, `Enter`/`Space` to toggle, `Escape` to close.
- Chip remove buttons have accessible labels (e.g. "Remove Product from expertise filter").
- "Clear filters" is a keyboard-focusable button.
- Focus order follows DOM order: expertise → language → method → stage → clear filters.
- No new ARIA roles beyond what the existing filter controls use.

---

### D-2580.11 Theme and Styling

Existing design tokens only:
- `--bg-elevated` for control background
- `--border` for control border
- `--muted` for placeholder text, count badges
- `--radius-*` for border radius
- `filterControlStyle` (existing token) for control sizing

No SCORE palette, no new tokens, no new CSS variables. Reuse the existing `CatalogToolbar` layout and spacing.

---

### D-2580.12 Vocab Single Source (R-15)

All four vocabularies must be imported from a single source — no duplicated arrays:

| Vocabulary | Import from |
|---|---|
| Expertise | `import { EXPERTISE_AREAS } from '@builderforce/creation-canvas-contract'` |
| Language codes | Import from #2528's language list (or the shared constant it exports) |
| Method slugs | `['video', 'phone', 'email', 'in-person']` — define once, shared between UI parse/validate and API validation |
| Stage slugs | `['start', 'grow', 'exit']` — define once, shared between UI and API |

**Anti-pattern (do not implement):** Copy-pasting the array literals into both the API route and the frontend component. Define a shared constant module or import from the contract package.

**If #2528 exports constants for language/method/stage** (e.g. `ADVISOR_METHODS`, `ADVISOR_STAGES`, `ADVISOR_LANGUAGES`), import those directly. If not, the developer should define a single shared constant file (e.g. `packages/creation-canvas-contract/src/advisorFilters.ts`) and have both frontend and API import from it — this is a #2528 concern; #2580 consumes whatever #2528 exports.

---

### D-2580.13 Implementation Gate

**AC-GATE is not green as of 2026-09-16.** #2527, #2532, and #2538 are all `blocked` with unmerged branches and no PRs. This Design is the Architect deliverable for the `ready` lane. **No product code may be committed to #2580 until the three kill conditions pass or epic #2579 is in flight.** The developer may read the codebase, plan the implementation, and prepare a branch, but must not open a PR or merge until the gate clears.

---

### D-2580.14 File Map (Implementation Reference)

This section is a guide for the developer — not a substitute for reading the codebase.

| Concern | Likely touchpoint |
|---|---|
| Marketplace page | `frontend/src/app/marketplace/MarketplacePageClient.tsx` |
| Catalog toolbar | `frontend/src/app/marketplace/` (CatalogToolbar or equivalent) |
| Advisor filters component | New: `frontend/src/app/marketplace/AdvisorFilters.tsx` |
| Marketplace API client | `frontend/src/lib/marketplaceApi.ts` (or equivalent hook) |
| Public freelancers API | `GET /api/public/freelancers` — add optional query params |
| Contract expertise enum | `@builderforce/creation-canvas-contract` → `startupListing.ts` → `EXPERTISE_AREAS` |
| Profile fields (#2528) | `freelancer_profiles.expertise`, `.languages`, `.methods`, `.stages` |
| i18n | `locales/*/marketplace.json` (or equivalent namespace files) |

---

### D-2580.15 Design-to-Requirements Traceability

| Design section | Requirements covered |
|---|---|
| D-2580.2 Multi-select | R-3, R-4, R-5, R-6 (multi-select from closed lists) |
| D-2580.3 Visibility gate | R-1 (category gate), R-2 (additive param), R-12 (isolation) |
| D-2580.4 URL contract | R-8 (URL round-trip), R-9 (category switch) |
| D-2580.5 API integration | R-10 (server-authoritative eligibility), R-14 (no N+1), R-15 (single vocab) |
| D-2580.6 Sort/Pagination | R-7 (composition: filter→page 1, page/sort doesn't clear) |
| D-2580.7 Empty state | R-11 (zero results, clear filters) |
| D-2580.8 Category switch | R-9 (drop keys, strip inapplicable params) |
| D-2580.9 i18n | R-4 (localized labels), R-13 (a11y labels) |
| D-2580.10 Accessibility | R-13 (keyboard, labels, announcements) |
| D-2580.11 Theme | Existing token constraints (no new tokens) |
| D-2580.12 Vocab source | R-15 (single vocab source, no drift) |
| D-2580.13 Gate | AC-GATE (no code until kill conditions pass) |

_— authored by Architect · 2026-09-16 · execution #32014_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:56:13.818Z · execution #31727

BA 2026-09-16: Requirements R-1–R-15 authored and signed. FR §2–§5 examples rewritten to #2528 / `EXPERTISE_AREAS` vocabs (method = video|phone|email|in-person; stage = start|grow|exit; not BUSINESS_STAGES; not 1:1 / pre-seed). `?category=advisors` is an additive `/marketplace` param — not in the repo today — and must not add `advisor` to `MARKETPLACE_LISTING_KINDS` or ship `/advisors`. Company `SEEKING_TYPES` `'advisors'` is a different population. Data dependency #2528; siblings #2581/#2582/#2584/#2585 out of scope. No product-repo patch on this BA slot.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:18:41.103Z · execution #31858

## Product lock — Ada (Sr. Product Mgr) · 2026-09-16 · execution #31858

**Status: product-locked.** Requirements **R-1–R-15 adopted unchanged**. Architect owns Design. This PM slot does not implement and does not patch the product repo.

Locked for implementation (do not re-derive):

1. Additive `/marketplace?category=advisors` only. Do not add `advisor`/`advisors` to `MARKETPLACE_LISTING_KINDS`. Do not ship `/advisors`. Confirmed 0 matches for `category=advisors` on `main`.
2. Vocab is #2528 + contract `EXPERTISE_AREAS`. Stage = `start | grow | exit` (not `BUSINESS_STAGES`). Gap #2637's required remap is already satisfied on this PRD; #2580 is the code home after Architect Design; #2637 does not implement. #2586 later promotes the same stage axis — not a parallel widget.
3. Population = **published talent listings**. Company `SEEKING_TYPES` `'advisors'` is a different population (startups seeking advisors).
4. Server-authoritative AND-across / OR-within. Bad slugs dropped; siblings still apply. Client-only card hiding is not sufficient.
5. Siblings stay out of this ticket: #2581 visitor chat, #2582 Resources tab, #2584 volunteer/price-0 badge, #2585 location filter.
6. Data dependency #2528 (`freelancer_profiles` columns). This ticket consumes them; it does not create/edit/backfill fields.
7. **Gate:** no product code until P2 epic #2579 is in flight **or** #2527 / #2532 / #2538 kill-green. Snapshot at this freeze: #2527 / #2532 / #2538 still `blocked`; #2579 is `ready` with unmerged `builderforce/task-2579` (no PR); this ticket `gitBranch` is null. Do not dump onto the epic branch.
8. No `specs/tasks/task-2580.md` dump (none on `main`; do not invent one).

_— Ada (Sr. Product Mgr) · 2026-09-16T16:16Z · execution #31858_

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:19:19.266Z · execution #31857

**PM product-lock · Ada / Kevin (Sr. Product Mgr) · 2026-09-16T16:18Z · execution #31858**

R-1–R-15 adopted **unchanged**. No rewrite of vocabs, URL contract, isolation, or AND/OR.

**Locked for this lane**
- Status: product-locked. Architect owns Design. Developer owns implementation after the gate. This PM slot does **not** ship marketplace UI/API.
- Additive `/marketplace?category=advisors` only. Do **not** add `advisor`/`advisors` to `MARKETPLACE_LISTING_KINDS`. Do **not** ship `/advisors`. Company `SEEKING_TYPES` `'advisors'` is a different population.
- Vocab remains #2528 + `EXPERTISE_AREAS`: language ISO list; method `video|phone|email|in-person`; stage `start|grow|exit` (R-6). **Do not revert R-6 to `BUSINESS_STAGES`** — gap #2637 is closed on this ticket's PRD; P3 #2586 promotes the same `stage` axis to chips (no second widget/key).
- Persistence is #2528 (`freelancer_profiles`). This ticket does not create, edit, or backfill columns.
- Siblings stay out: #2581 chat, #2582 Resources, #2584 volunteer badge, #2585 location.

**Gate (no product code this lane)**
Epic #2579: do not implement until the P0+P1 kill on #2522 passes (#2527, #2532, #2538) **or** #2579 is in flight. Observed 2026-09-16: #2527/#2532/#2538 still `blocked` (unmerged branches, no PRs); #2579 `ready` not in flight; #2580 `gitBranch` is null. Do **not** re-dispatch #2527 from this ticket. Do **not** dump `specs/tasks/task-2580.md` or patch seanhogg/builderforce.ai from this PM run.

Comma vs repeat-key query form, facet-count SQL, and shared-filter component branching are Design/implementation — not this freeze.

---

### Update — Product Manager · 2026-09-16T16:39:19.871Z · execution #31958

**PO ready approved · Product Owner · 2026-09-16 · execution #31958**

Scope + acceptance criteria **approved unchanged**. BA R-1–R-15 and ACs 1–11 (traceability table) are the contract. PM product-lock (exec #31858) stands.

Inspected: ticket PRD (Problem/Goal, FR §1–§8, ACs 1–11, Locked 1–8, R-1–R-15, isolation, URL contract, vocabs); BA exec #31727; PM exec #31858; parent epic #2579 (ready, not in flight); data dep #2528 (ready); P0/P1 kill #2527/#2532/#2538 (still `blocked`); siblings #2581/#2582/#2584/#2585 remain out. Bound repo seanhogg/builderforce.ai returned empty / GitHub 403 — expected; this lane does not patch product code.

**Accepted (do not re-derive):**
1. Additive `/marketplace?category=advisors` only. No `MARKETPLACE_LISTING_KINDS` advisor/advisors. No `/advisors` route. Company `SEEKING_TYPES` `'advisors'` is a different population.
2. Four filters iff `category=advisors` on Talent: expertise (`EXPERTISE_AREAS`), language (#2528 ISO 639-1), method (`video|phone|email|in-person`), stage (`start|grow|exit`). **Not** `BUSINESS_STAGES`. Stage is **multi-select** (Locked #5 / R-6 wins over the earlier Assumptions hedge).
3. AND across groups, OR within; server-authoritative; bad slugs dropped; URL round-trip; category switch drops advisor keys.
4. Isolation R-12 / AC 9: other families unchanged. Siblings #2581/#2582/#2584/#2585 out. Persistence is #2528; this ticket consumes, does not create fields. #2586 later promotes the same `stage` axis (no second widget).
5. Gate: no product implementation until P2 #2579 is in flight **or** #2527/#2532/#2538 kill-green. Architect owns Design next. PO sign-off ≠ Design ≠ code shipped.

Comma vs repeat-key query form, facet-count SQL, and shared-filter component branching remain Design — not this freeze.

_— Product Owner · 2026-09-16T16:38Z · execution #31958_

---

### Update — CTO · 2026-09-16T17:06:55.577Z · execution #32027

### Update — Architect · 2026-09-16 · execution #32014

**Architect ready signed off. Design D-2580.1–D-2580.15 authored and committed to the PRD Design section.**

Deliverable: complete filter-specific architecture for #2580, inheriting #2579's layered compose model (D-1–D-6). Covers:

- **D-2580.1** Component tree: `<AdvisorFilters />` inside `CatalogToolbar`, gated on `category=advisors`
- **D-2580.2** Multi-select widget pattern (all four filters multi-select per Locked #5)
- **D-2580.3** Visibility gate truth table (category × family → show/apply decision)
- **D-2580.4** URL contract: comma-separated values, parse/validate/write lifecycle
- **D-2580.5** API integration: client-side forwarding (gate-guarded), server-side array-overlap SQL (`&&`), optional facet counts
- **D-2580.6** Sort/pagination composition matrix (filter change → page 1, keep sort; page/sort change → keep filters)
- **D-2580.7** Empty/zero-result states and Clear filters control
- **D-2580.8** Category switch isolation (drop advisor keys on leave; strip inapplicable params on enter; never resurrect)
- **D-2580.9** i18n keys (11 new `marketplace.*` keys)
- **D-2580.10** Accessibility (keyboard, labels, announcements, focus order)
- **D-2580.11** Theme/styling (existing tokens only, no SCORE palette)
- **D-2580.12** Single vocab source (R-15 — import from contract, no duplicated arrays)
- **D-2580.13** Implementation gate (no code until P0/P1 kill passes)
- **D-2580.14** File map for developer reference
- **D-2580.15** Design-to-requirements traceability table

No product code committed — gate not green. Developer owns implementation after gate clears.