> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2591
> _Each agent that updates this PRD signs its change below._

# PRD: Unblock ticket 2555 — bind a readable checkout for the developer container

**Status:** WIP  
**Ticket:** #2555  
**Related:** PRD 29 verify-and-package; PRD 27 (#2562) VSIX package `2026.9.72`; same class of blocker as #2588  
**Repo / branch:** `SeanHogg/Builderforce.ai` on `builderforce/task-2555` (reported 1 commit ahead of `main`, no PR)

## Problem & Goal

The developer container reports `seanhogg/builderforce.ai` as bound on `builderforce/task-2555`, but `list_files`, `read_file`, `git_*`, and `run_command` all fail with `no repository bound to this task`. Without a readable, executable checkout, PRD 29 cannot run the Change A/B suites, apply mechanical-only fixes, or confirm the VSIX.

**Goal:** Bind `SeanHogg/Builderforce.ai` as `builderforce/task-2555` (or restore git credential delegation) so the **next developer run on #2555** can search the tree, execute the §3 suites in order, package, and report. Do not dump conceptual run-visibility code. **This ticket does not package a VSIX** (exclusive pin `builderforce.builderforce-ai@2026.9.79` is owned by #2614 / #2560 / #2561).

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:07:47.413Z_

## Target users / ICP roles

- Downstream developer-container agents executing PRD 29 verify-and-package
- Product / platform operators unblocking checkout binding (same class as #2588)
- Maintainers of `SeanHogg/Builderforce.ai` who need a working workspace on `builderforce/task-2555`

## Scope

In scope:

- Make the reported bind real: a readable, writable git checkout of `SeanHogg/Builderforce.ai` at `builderforce/task-2555` available to **ticket #2555**
- Restore git credential delegation if that is the missing piece
- Confirm `list_files`, `read_file`, `git_*`, and `run_command` succeed against that checkout on a **#2555** developer run
- Leave the workspace ready for the existing Change A/B suite order (#2556 → #2560). Packaging one VSIX at the exclusive pin **2026.9.79** is parent #2555 / #2560 work after bind — not this ticket.

Out of scope items are listed below. Binding is the unblock; suite execution and packaging are the *next* run’s job once the checkout is readable. A readable checkout on `builderforce/task-2591` is **not** the unblock.

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:07:51.874Z_

## Functional requirements

1. **Effective bind.** The task workspace is bound to `SeanHogg/Builderforce.ai` on branch `builderforce/task-2555`. The bind is not merely reported; file and git tools operate on that tree.
2. **Credential / delegation path.** If the reported bind is a ghost (status says bound, tools say unbound), restore git credential delegation or re-bind so the same identity can fetch, status, and (if needed) commit on that branch.
3. **Tool surface.** After bind:
   - `list_files` enumerates the repo root and nested packages
   - `read_file` returns file contents
   - `git_*` (status, log, diff, branch) run against the bound repo
   - `run_command` executes in the checkout working directory
4. **Branch fidelity.** Working tree is `builderforce/task-2555`, not a detached or default-branch checkout that would hide the existing +1 commit vs `main`.
5. **Handoff readiness.** After bind, a subsequent developer run can:
   - search the tree
   - run Change A/B suites in order: `brain-embedded` → `brain-ui` → `api` → `frontend` → `clients/vscode`
   - package one VSIX at version `2026.9.72` per PRD 27 (#2562)
   - report results
6. **Mechanical-only posture.** This ticket does not introduce conceptual run-visibility code, new product features, or non-mechanical refactors. Fixes, if any, are limited to making the checkout readable/bound.
7. **Parity with #2588.** Treat this as the same class of blocker: reported bind vs actual unbound tools. Prefer the known restore/bind path over a new binding subsystem.

## Acceptance criteria

- Developer-container status and tools agree: the task has a bound repository, and that repository is `SeanHogg/Builderforce.ai` at `builderforce/task-2555`.
- `list_files` on the repo root succeeds and shows the expected package layout (`brain-embedded`, `brain-ui`, `api`, `frontend`, `clients/vscode`, and related paths).
- `read_file` on a known file in the checkout returns content (not a bind error).
- `git status` (or equivalent `git_*`) succeeds, shows branch `builderforce/task-2555`, and is consistent with “1 commit ahead of `main`, no PR.”
- `run_command` (e.g. `pwd` / `git rev-parse --show-toplevel`) runs in the checkout and does not return `no repository bound to this task`.
- No conceptual run-visibility code is added or dumped as part of this unblock.
- The next developer run can start the PRD 29 §3 suite sequence and the `2026.9.72` VSIX package without first re-fighting bind/credential errors.

## Out of scope

- Implementing or dumping conceptual run-visibility code
- Opening a PR for `builderforce/task-2555` (none exists; not required to unblock)
- Running the Change A/B suites or producing the VSIX on *this* ticket except as an optional smoke that the bind is real
- Changing VSIX version away from `2026.9.72` or altering PRD 27 (#2562) packaging rules
- Reordering or rewriting the suite sequence (`brain-embedded` → `brain-ui` → `api` → `frontend` → `clients/vscode`)
- Non-mechanical product changes, new binding architecture, or unrelated repo work
- Fixing unrelated failures inside the suites once the checkout is readable (those belong to the PRD 29 verify-and-package run)

## Requirements

**Owner:** Business Analyst (Ada) · 2026-09-16 · ticket #2591 (gap unblocking #2555 / PRD 29)  
**PM lock:** Product Manager · 2026-09-16 · execution 31791 — R-1–R-13 adopted; R-9 clarified to exclusive pin 2026.9.79 (#2614). This ticket remains an **environment unblock**, not a product feature. **No product-repo patch**, no `specs/tasks/task-2591.md`, and **no conceptual run-visibility dump**.

### R-1 Effective bind target
Bind **SeanHogg/Builderforce.ai** (canonical product repo) as the checkout for **ticket #2555**, on branch **`builderforce/task-2555`**. The bind must be the one the developer-container tools (`list_files`, `read_file`, `git_*`, `run_command`) actually use — not a status-only / ghost bind.

### R-2 Ghost bind is the defect
Today the container *reports* `seanhogg/builderforce.ai` bound on `builderforce/task-2555` while `list_files` / `read_file` / `git_*` / `run_command` fail with `no repository bound to this task`. Treat that disagreement as the bug. Restore git credential delegation or re-bind so status and tools agree.

### R-3 Success is a #2555 run, not a #2591 checkout
`#2591.gitBranch = builderforce/task-2591` is **not** success (same class as `#2588.gitBranch = builderforce/task-2588` not unblocking #2527, and `#2600` not unblocking #2554 until that ticket’s container reads). Pass criterion: a subsequent **developer run on #2555** can enumerate the repo root, read known files, and `git status` shows `builderforce/task-2555`.

### R-4 Project 11 has no GitHub URL — operator bind, not product code
Project 11 (`BuilderForce.AI`) has `githubRepoUrl=null`, `githubRepoOwner=null`, `sourceControlRepoUrl=null` (same constraint as #2588 / #2600 / #2575 / #2545 / #2532). Agents on this ticket **cannot** create the bind by committing to `seanhogg/builderforce.ai`. The producer is the platform operator / credential-delegation restore. Do not invent a binding subsystem in-repo.

### R-5 Tool surface after bind
Once bound, all of the following must succeed against the checkout working directory **on a #2555 developer run**:

- `list_files` at repo root enumerates the expected layout, including `brain-embedded` / `packages/brain-embedded`, `packages/brain-ui`, `api`, `frontend`, `clients/vscode`
- `read_file` returns contents of a known file (not a bind error)
- `git status` / `git log` / `git diff` / `git branch` run against that tree
- `run_command` (e.g. `pwd`, `git rev-parse --show-toplevel`) executes in the checkout and does **not** return `no repository bound to this task`

### R-6 Branch fidelity
Working tree is **`builderforce/task-2555`**, not `main`, not detached HEAD, and not #2591’s own branch. Preserve existing commits vs `main` on that branch. Do **not** open a PR for `builderforce/task-2555` as part of this unblock (none is required to bind).

### R-7 Mechanical-only / no run-visibility dump
This ticket does not introduce conceptual run-visibility code, new product features, or non-mechanical refactors. After bind, the next #2555 developer may apply **mechanical-only** fixes required to make the existing Change A/B suites pass. Do not dump survey wiring, `PHASE_SURFACES` changes, advisor kinds, or any #2554 / #2601 `canvasChange` probe. #2601 (revert `0267ffd` on `builderforce/task-2554`) is a **different** parent.

### R-8 Handoff — suite order (next run, not this ticket)
After bind, PRD 29 §3 runs **in this order** (children of #2555). This ticket does **not** execute the suites except as an optional smoke that the bind is real:

| Order | Child | Package | Command contract |
| --- | --- | --- | --- |
| 1 | #2556 | brain-embedded | `pnpm test`, then dist |
| 2 | #2557 | packages/brain-ui | `pnpm test`, then dist (`pnpm build` / tsup) |
| 3 | #2558 | api | `pnpm vitest run src/application/brain`, then typecheck from `api/` |
| 4 | #2559 | frontend | typecheck + i18n catalog guard; vitest brain components if present |
| 5 | #2560 | clients/vscode | `tsc --noEmit`, `pnpm test`, `vsce package` |

Do not reorder.

### R-9 VSIX pin is out of scope (exclusive pin 2026.9.79 via #2614)
This ticket does **not** package, bump, downgrade, or retarget the VSIX. Parent **#2555** step 6 / **#2560** / **#2561** own packaging. Exclusive pin is **`builderforce.builderforce-ai@2026.9.79`** (#2614 path a). **2026.9.72 is retired.** Live `clients/vscode/package.json` is 2026.9.79. Do **not** produce a 72 VSIX and do **not** rewrite CHANGELOG. R-9’s original “do not retarget on this ticket” still holds — the 72-vs-79 decision lives on #2614 / parent #2555, not here.

### R-10 Parity with #2588 / #2600
Same class of blocker: reported bind vs unbound tools; project 11 `githubRepoUrl=null`. Prefer the known restore/bind path over a new binding architecture. Do not treat a readable checkout on **this** ticket’s branch as the unblock.

### R-11 No `specs/tasks` stub; PRD is source of truth
Do not ship `specs/tasks/task-2591.md` (or a conceptual stand-in against an empty or unrelated tree). A prior-pass dump of that path is **dead code** and must not merge. Requirements live on this ticket PRD.

### R-12 Out of scope (explicit)
- Implementing or dumping conceptual run-visibility code
- Opening a PR for `builderforce/task-2555`
- Running Change A/B suites or producing the VSIX on *this* ticket (optional bind smoke only)
- Packaging, bumping, or downgrading the VSIX (exclusive pin 2026.9.79 is #2614 / #2560 / #2561)
- Reordering the suite sequence
- Non-mechanical product changes; new binding architecture
- Fixing unrelated suite failures once the checkout is readable (those belong to #2556–#2560)
- Mixing in #2554 survey / #2601 `0267ffd` canvasChange restore
- Claiming this PM or BA sign-off **is** the bind
- Treating `builderforce/task-2591` as the success checkout

### R-13 Acceptance for *this* PM / BA lane
The backlog BA lane and this todo PM lane are **done** when R-1–R-13 are recorded on the ticket PRD and signed. They are **not** done when git tools still fail. Architect / developer lanes on #2591 must not write product code to satisfy R-1; they specify / operate the bind. The bind is verified when a **#2555** developer run’s tools agree with R-5 and R-6.

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:07:12.469Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:58:01.572Z · execution #31748

**BA (Ada) 2026-09-16:** Requirements R-1–R-13 signed on this ticket PRD. #2591 is the env-unblock for #2555 (PRD 29 verify-and-package): bind SeanHogg/Builderforce.ai as `builderforce/task-2555`. Success is a #2555 developer run whose tools agree, **not** a readable #2591 checkout (`gitBranch=builderforce/task-2591` is not success). Project 11 `githubRepoUrl=null` — operator / credential-delegation restore; no product-repo patch; no `specs/tasks/task-2591.md`; no run-visibility dump. VSIX stays `2026.9.72` (72-vs-79 is #2560/#2561). Suite order unchanged: #2556 → #2557 → #2558 → #2559 → #2560. This BA workspace listed 0 files + GitHub 403; empty tree was not written. BA sign-off is not the bind.