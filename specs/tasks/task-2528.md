> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2528
> _Each agent that updates this PRD signs its change below._

# PRD: Talent Profile Structured Fields & Toggles

**Status:** WIP  
**Owner:** Product  
**Surface:** Freelancer / talent profile (edit + public)

## Problem & Goal

Talent profiles today lack structured signals for *how* someone works (channel, stage, session length, price, confidentiality, location, languages) and *what* they cover (expertise, industries). Matching, search, and booking therefore rely on unstructured bio text, which is inconsistent and hard to filter.

**Goal:** Let talent declare a complete, structured working profile via toggles and fields, persist it as first-class profile data, and expose it on the public profile so clients can evaluate fit before booking. Price `0` means volunteer / complimentary.

## Target users / ICP roles

- **Talent (freelancer, advisor, expert):** Creates and maintains the profile; needs fast, unambiguous controls.
- **Client / buyer:** Reads the public profile to decide channel, budget, stage fit, and confidentiality.
- **Internal ops / matching:** Uses structured fields for search, filters, and recommendations (read path only in this slice).

## Scope

In scope for this WIP:

- Profile **edit** experience: toggles + fields listed below, with validation and save.
- Profile **public** experience: display of saved values (including volunteer pricing and confidentiality default).
- Persistence on the talent record; empty/off fields do not appear as required for publish unless noted.
- Price semantics: `0` = volunteer (complimentary). Positive amounts are paid session rates in the platform currency already used for bookings.

Out of scope items are listed at the end.

## Functional requirements

### Profile fields (edit)

Talent can set the following on their profile. Each group is independently editable and savable with the rest of the profile.

1. **Expertise**  
   Multi-select (or tag) list of expertise areas. At least one recommended for a complete profile; not a hard block to save.

2. **Industries**  
   Multi-select list of industries served.

3. **Languages**  
   Multi-select of languages the talent can work in.

4. **Methods**  
   Multi-select of delivery methods: `video`, `phone`, `email`, `in-person`.  
   Unselected methods are not offered. At least one method required to be bookable.

5. **Stages**  
   Multi-select of company/engagement stages: `start`, `grow`, `exit`.  
   Unselected stages mean the talent does not position for that stage.

6. **Session length**  
   Talent sets offered session duration(s) (e.g. minutes). Must be a positive duration. If multiple lengths are supported by the existing booking model, allow multiple; otherwise a single default length.

7. **Price**  
   Numeric rate for a session. `0` is valid and means **volunteer** (no charge). Negative values are rejected. Display unit matches existing marketplace currency.

8. **Location**  
   Location used for in-person work and/or timezone context (city/region or equivalent existing location control). Required if `in-person` is selected; optional otherwise.

9. **Confidentiality default**  
   Toggle/default for whether sessions are treated as confidential by default (on/off). Public profile shows the default so clients know the baseline; it does not replace a booking-time agreement.

### Toggles and empty state

- Methods, stages, and confidentiality default are explicit on/off (or multi-select equivalent) controls, not free text.
- Off / empty optional fields are omitted from the public profile rather than shown as “None.”
- Talent can clear optional fields and save.

### Public profile

- Show expertise, industries, languages, methods, stages, session length, price (with volunteer treatment when `0`), location (when present), and confidentiality default.
- Volunteer price is labeled as volunteer / complimentary, not as `$0` alone.
- In-person is only presented as available when method `in-person` is on and location is set.

### Validation

- Price ≥ 0; non-numeric rejected.
- Session length > 0.
- If method includes `in-person`, location must be set.
- At least one method required before the profile can be marked bookable / complete (draft save without methods is allowed).
- Unknown enum values for methods/stages are rejected.

## Acceptance criteria

- Talent can open profile edit and set all nine field groups, then save and reload without data loss.
- Methods are limited to video, phone, email, in-person; stages to start, grow, exit.
- Saving price `0` stores volunteer; public profile shows volunteer / complimentary, not a paid `$0` rate.
- Saving price `> 0` shows the paid rate in the existing currency format.
- Negative price and non-positive session length are blocked with inline errors; profile is not saved in an invalid state.
- Selecting in-person without location is blocked; clearing in-person allows location to be optional.
- Confidentiality default persists and is visible on the public profile as the stated default.
- Public profile hides unset optional fields (expertise, industries, languages, stages, location) instead of empty placeholders.
- A profile with no methods can be saved as draft but cannot be bookable until ≥ 1 method is set.
- Existing profile fields (name, bio, photo, etc.) are unchanged by this work.

## Out of scope

- Search / filter / matching UI that consumes these fields (data is stored for later use).
- Booking-flow changes, checkout, tax, or payouts (including charging `0`).
- Per-session override of price, length, method, or confidentiality at booking time (defaults only).
- New expertise/industry/language taxonomies beyond wiring to existing or a simple selectable list.
- Multi-currency pricing, sliding-scale, or package pricing.
- NDA document generation, legal e-sign, or compliance workflows.
- Admin bulk-edit, CSV import, or migration of unstructured bio text into these fields.
- Mobile-specific redesign beyond using existing profile edit patterns.
- Notifications, completeness scoring, or mandatory profile-completion gating beyond bookable vs draft as specified.

## Requirements

**Product-locked** by Product (2026-09-16). BA locked decisions 1–9 are adopted unchanged as **P0–P8**. Architect owns Design. This slot writes no product code and no `specs/tasks` dump.

Persist the nine working-profile groups on the existing `freelancer_profiles` row (edit + public). Advisor remains a talent mode — no `/advisors`, no new `MARKETPLACE_LISTING_KINDS` value.

### P0 — Reuse the talent listing

Columns on `freelancer_profiles` (`api/src/infrastructure/database/schema/commerce.ts`). Editor `/freelancer/profile` (`frontend/src/app/freelancer/profile/page.tsx`, existing one-Save form). Public `TalentProfileView` (`frontend/src/components/freelance/TalentProfileView.tsx`) + `/talent/[id]`. Same lockstep vocab pattern as career intent (`api/src/application/career/listing.ts` + `frontend/src/components/freelance/talentFields.ts`). Transport remains `GET/PATCH /api/freelancers/me` and public `GET /api/freelancers/:id` (`frontend/src/lib/freelance/talentProfile.ts`). Do not fork a second profile, advisor listing, or canvas listing kind.

### P1 — Session price is a new column

`session_price_cents` integer nullable. **Do not overload** `hourlyRateCents` (gig / hourly; identity/rate row stays as-is). Semantics:

- `0` = volunteer / complimentary on the public profile (never a paid `$0`).
- `null` omits the session-price row.
- `> 0` is a paid session rate in the existing `currency` (default USD).
- Negative values are rejected.

Hourly-rate display is unchanged.

### P2 — Session length is a single default

`session_length_minutes` integer nullable, allowed range **1–480** or `null`. Multiple durations already live on `booking_services.durationMin`; this ticket does **not** edit `booking_services`. Empty/`null` omits the row on the public profile.

### P3 — Vocab reuse, not new taxonomies

Closed lists only. Unknown values → `400` and no write.

| Group | Source | Values |
|---|---|---|
| Expertise | `EXPERTISE_AREAS` (`packages/creation-canvas-contract/src/startupListing.ts`) | `go_to_market`, `fundraising`, `product`, `engineering`, `design`, `growth`, `operations`, `finance`, `legal`, `people` |
| Industries | `STARTUP_SECTORS` (same module) | `saas`, `marketplace`, `fintech`, `healthtech`, `edtech`, `climate`, `consumer`, `hardware`, `ai_ml`, `other` |
| Methods | this ticket | `video` \| `phone` \| `email` \| `in-person` |
| Stages | this ticket | `start` \| `grow` \| `exit` — **do not** substitute `BUSINESS_STAGES` (`idea` / `pre_seed` / `seed` / …) |
| Languages | closed ISO 639-1 | `en, es, fr, de, pt, zh, ja, ko, hi, ar, it, nl, pl, ru, tr, vi, id, th, sv, uk` |

Add the methods / stages / languages constants to `talentFields.ts` **and** the API listing module in lockstep (same pattern as `TALENT_SEEKING_MODES`).

### P4 — Location reuses the existing column

`freelancer_profiles.location` (varchar 120) already sits on the identity/rate row. Required **only** when `methods` includes `in-person`. Optional otherwise; omitted on the public profile when empty. In-person is presented as available only when the method is on **and** location is set.

### P5 — Confidentiality default is a boolean

`confidentiality_default` boolean, default `false`. Public profile shows stated-default copy **only when on**. This is not an NDA, e-sign, or booking-time agreement.

### P6 — Bookable gate (additive, with backfill)

Today `FreelancerProfile.bookable` is true when the person hosts an active `booking_service` (`booking_hosts` in `commerce.ts`). Keep that bind and **add** `methods.length >= 1`.

- Migration **backfills `['video']`** onto currently-bookable rows so deploy does not drop Book CTAs.
- Draft save with empty `methods` remains allowed.
- A profile is not bookable until ≥ 1 method **and** the existing host bind.

### P7 — Empty / off optional groups are omitted

Public profile (and editor Preview, which is the same `TalentProfileView`) hides unset optional groups — expertise, industries, languages, stages, location, session length, session price (`null`), confidentiality when `false`. No “None” placeholders.

### P8 — Validation is server-authoritative

Invalid PATCH writes **nothing**. `400` with one of:

`SESSION_PRICE_INVALID` · `SESSION_LENGTH_INVALID` · `LOCATION_REQUIRED` · `METHOD_INVALID` · `STAGE_INVALID` · `EXPERTISE_INVALID` · `INDUSTRY_INVALID` · `LANGUAGE_INVALID`

Client may mirror for inline errors; the server is the source of truth.

### Field catalogue (API names)

| Group | Storage | Empty | Notes |
|---|---|---|---|
| Expertise | `expertise` JSON `string[]` | `[]` | `EXPERTISE_AREAS` |
| Industries | `industries` JSON `string[]` | `[]` | `STARTUP_SECTORS` |
| Languages | `languages` JSON `string[]` | `[]` | ISO list in P3 |
| Methods | `methods` JSON `string[]` | `[]` | chips; ≥1 to be bookable |
| Stages | `stages` JSON `string[]` | `[]` | `start/grow/exit` |
| Session length | `session_length_minutes` int null | omit | 1…480 |
| Session price | `session_price_cents` int null | omit | `0` = volunteer |
| Location | existing `location` | omit | required if in-person |
| Confidentiality | `confidentiality_default` bool | omit when false | stated default only |

Live `freelancer_profiles` does **not** yet have the new columns (`expertise`, `industries`, `languages`, `methods`, `stages`, `session_length_minutes`, `session_price_cents`, `confidentiality_default`). Architect Design names the migration; this PM slot does not add it.

### User stories

- **US-1** Talent sets methods, stages, session length, session price, location, confidentiality; save + reload without data loss.
- **US-2** Talent multi-selects expertise, industries, languages from closed lists; can clear.
- **US-3** Client sees only set fields; volunteer labelled; in-person only with location; confidentiality only when on.
- **US-4** Negative price, non-positive length, unknown enums, in-person without location → inline error, not saved.
- **US-5** Draft with no methods can save; not bookable until ≥1 method + existing host bind.

### Editor

Two new fieldsets on the existing one-Save form: **What you cover** (expertise, industries, languages) and **How you work** (methods, stages, session length, session price with “0 = volunteer”, confidentiality). Location stays on the identity/rate row. Preview uses the same public treatment.

Existing fields (name, bio, photo, skills, hourly rate, career intent, résumé, visibility, publish) are unchanged.

### Process lock (this PM slot)

- **No product-repo patch** on `seanhogg/builderforce.ai` from Product. Architect owns `## Design`.
- **No `specs/tasks/task-2528.md`** and no `specs/builderforce/task-2528.md` dump. Contract is this ticket PRD.
- **Production UI waits** on parent/sibling kill-switch tickets **#2527 / #2532 / #2538** (and sibling **#2579** P2 compose-only). Do not ship profile columns while those gates are not green.
- Search/filter UI, booking/checkout/payouts, editing `booking_services`, per-session overrides, new taxonomies, multi-currency/packages, NDA/e-sign, bio migration, completeness scoring, and `/advisors` stay out of scope.

### Out of scope (this slice)

Search/filter UI, booking/checkout/payouts, editing `booking_services`, per-session overrides, new taxonomies, multi-currency/packages, NDA/e-sign, bio migration, completeness scoring, `/advisors`.

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:18:43.250Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:51:35.861Z · execution #31675

BA (2026-09-16): Requirements section authored and locked (sessionPriceCents as a new column, methods/stages closed enums, bookable backfill `['video']` on currently-bookable rows, vocab reuse of EXPERTISE_AREAS + STARTUP_SECTORS). Repo copy at `specs/builderforce/task-2528.md` could not be rewritten this run — GitHub API rate limit 403 on writes. Next run: replace Ada’s PM dump in that file with the Requirements section; do not re-derive. Architect may proceed from the ticket PRD.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:12:46.590Z · execution #31805

## Product lock — Product Manager · 2026-09-16 · execution #31805

**Status: Product-locked.** Architect owns Design next. Developer implements after Design. This PM slot does not patch product code.

**Contract:** BA locked decisions 1–9 and the field catalogue / user stories / editor / out-of-scope in § Requirements are adopted **unchanged**. Do not re-derive. The sentence “Full implementable spec: `specs/tasks/task-2528.md`” in that section is **superseded** — the implementable contract is this ticket PRD. `specs/tasks/task-2528.md` is a dead dump and must not ship.

### P0 Binding
Reuse the talent listing. Columns on `freelancer_profiles`; editor `/freelancer/profile`; public `TalentProfileView`. Advisor remains a talent **mode** — no `/advisors`, no new `MARKETPLACE_LISTING_KINDS` value. Same lockstep vocab pattern as career intent (`listing.ts` + `talentFields.ts`). Bound repo `seanhogg/builderforce.ai` is the right product repo; this PM slot does not implement.

### P1 Session price
New column `session_price_cents`. Do **not** overload `hourlyRateCents` (gig / hourly). `0` = volunteer / complimentary on the public profile (never a paid `$0`). `null` omits the session-price row. Hourly-rate display is unchanged. Currency = existing marketplace currency.

### P2 Session length + location + confidentiality
- Length: single default `session_length_minutes` (1–480 or null). Multiple durations already live on `booking_services`; this ticket does **not** edit services.
- Location: reuse existing city/region field. Required only when method `in-person` is selected; optional otherwise. In-person is only presented as available when method is on **and** location is set.
- Confidentiality default: boolean, default `false`. Public shows copy **only when on**. Not an NDA / booking-time agreement.

### P3 Vocab (reuse, not new taxonomies)
- Expertise = `EXPERTISE_AREAS`. Industries = `STARTUP_SECTORS`.
- Methods = `video | phone | email | in-person` (chips). Unknown values rejected (`METHOD_INVALID`).
- Stages = `start | grow | exit`. Do **not** substitute `BUSINESS_STAGES`.
- Languages = closed ISO 639-1: `en, es, fr, de, pt, zh, ja, ko, hi, ar, it, nl, pl, ru, tr, vi, id, th, sv, uk`.

### P4 Bookable gate (additive, with backfill)
`bookable = existing booking_hosts bind AND methods.length >= 1`. Migration backfills `['video']` onto currently-bookable rows so deploy does not drop Book CTAs. Draft save with empty methods remains allowed. A profile with no methods is not bookable.

### P5 Editor IA + public omit
Two new fieldsets on the existing **one-Save** form:
- **What you cover:** expertise, industries, languages.
- **How you work:** methods, stages, session length, session price with helper “0 = volunteer”, confidentiality default.
Location stays on the identity/rate row. Preview uses the same public treatment. Empty / off optional groups are omitted on the public profile (no “None”). Existing identity fields (name, bio, photo, hourly rate) are unchanged.

**Validation** is server-authoritative (`400` with `SESSION_PRICE_INVALID`, `SESSION_LENGTH_INVALID`, `LOCATION_REQUIRED`, `METHOD_INVALID`, `STAGE_INVALID`, `EXPERTISE_INVALID`, `INDUSTRY_INVALID`, `LANGUAGE_INVALID`). Invalid requests write nothing. Client shows inline errors.

### Sequencing (PRD 26 / epic #2527)
This is child 1 of epic #2527. Ships structured fields so #2529 (marketplace category `advisors`, logged-out browse) can reuse `TalentProfileView` without a second schema. #2530 is authenticated setup-as-destination (existing `/freelancer/profile`, not `/advisors/setup`). #2531 is five locales + both themes. Search/filter/matching UI that consumes these fields is **out of scope** (later). Gate #2527 is blocked; this child product-locks independently and does not wait on #2529/#2530/#2531.

### Explicitly not this slot
- No product-code patch, no migration, no editor/public UI, no booking/checkout/payouts, no `booking_services` edits, no per-session overrides, no new taxonomies, no `/advisors`, no specs/tasks dump, no `specs/builderforce/` rewrite.
- GitHub writes 403 this run (same constraint BA recorded). Project 11 `githubRepoUrl` is null. Architect proceeds from this ticket PRD.

_— Product Manager · execution #31805 · 2026-09-16_