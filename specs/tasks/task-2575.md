> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2575
> _Each agent that updates this PRD signs its change below._

# PRD: R7 — DONE.md close-out, Gap Register cleanup, and gated release notes

## Problem & Goal

R6 work is complete only when the gap is closed in the shared records and a release note draft exists. Today the Gap Register still lists the open item at `ROADMAP.md` L142, `DONE.md` has no dated `RESOLVED` entry for this gap, and release notes have not been categorized. Publishing notes without an operator check risks shipping the wrong categories (especially the 403 chain, which must stay silent).

**Goal:** After R6 is done, write a dated `RESOLVED` into `DONE.md`, delete **only** Gap Register line `ROADMAP.md` L142, draft release notes with the mandated categories, and **stop** until the operator confirms publish.

## Target users / ICP roles

- **Operator** — sole authority to approve publishing release notes.
- **Downstream agents** — consume this PRD; they must not publish, invent extra Gap Register edits, or add a 403-chain note.
- **Release-notes consumer** (internal changelog / users of persona delegation and diagnostics) — sees only the two approved items.

## Scope

In scope, **strictly after R6** (R-1):

1. Append a dated `RESOLVED` record to `DONE.md` for this gap (R-2).
2. Delete the Gap Register row that names **🟠 PRD 27 (staffed work + coordination gate + chat diagnostics as data) is CODE COMPLETE but entirely UNVERIFIED**, matching **by that text** — not by a frozen `ROADMAP.md` L142 number (R-3). No other ROADMAP lines.
3. Draft (do not publish) release notes (R-4, R-5, R-6):
   - `category=new` — persona delegation (`spawn_agent` `as_agent`)
   - `category=improvement` — persisted diagnostics
   - **omit** any note for the 403 chain
4. Ask the operator to confirm before any publish step (R-7).

_— rewritten by Product Manager · 2026-09-16T16:20:01.425Z_

## Functional requirements

1. **R6 gate.** Do not write `DONE.md`, delete the Gap Register row, or draft/publish notes until R6 is complete (#2574 + remainder #2597, all six work-mode steps). If R6 is not done, stop and report blocked. R5 #2573 (migrations 1177 then 1178 on core) is a predecessor of R6.
2. **DONE.md.** Add one dated `RESOLVED` entry that names the closed gap (persona delegation + persisted diagnostics close-out). Date must be the resolution date (the day R6 passed), not a placeholder. Cite #2562 / #2575.
3. **Gap Register.** Delete exactly the matching **PRD 27** Gap Register bullet. Originally `ROADMAP.md` L142; **match by PRD 27 text**, not by line number (a newer React-contexts bullet found 2026-09-16 now precedes it). Leave surrounding ROADMAP content unchanged. Do not delete if R6 is incomplete. If L142 is a different row, stop.
4. **Release notes draft** (unpublished):
   - **New:** persona delegation via `spawn_agent` with `as_agent`.
   - **Improvement:** persisted diagnostics.
   - **None:** 403 chain — no title, no body, no “fixed 403” mention.
5. **Publish gate.** Present the `DONE.md` entry, the Gap Register deletion, and the draft notes to the operator in-band. Publish only after explicit confirmation. If the operator declines or is silent, leave notes unpublished.
6. **Idempotency.** If the PRD 27 row is already gone or `DONE.md` already has this `RESOLVED`, do not duplicate; still require operator confirm before any publish.
7. **Duplicate.** Do not double-apply with #2568.
8. **No specs dump / no product-code.** Do not ship `specs/tasks/task-2575.md`. Do not implement persona delegation, diagnostics, or the 403 chain on this ticket.

_— rewritten by Product Manager · 2026-09-16T16:18:36.555Z_

## Acceptance criteria

- R6 (#2574 live re-test **and** remainder #2597, all six work-mode steps pass) completed **before** any R7 mutation of `DONE.md` or `ROADMAP.md`. R5 #2573 is a predecessor of R6.
- `DONE.md` contains a single new dated `RESOLVED` entry for this gap (persona delegation + persisted diagnostics), dated the day R6 passed.
- The Gap Register row that names **🟠 PRD 27 (staffed work + coordination gate + chat diagnostics as data) is CODE COMPLETE but entirely UNVERIFIED** is deleted, matched **by that text** — not by a frozen `ROADMAP.md` L142 number (a newer React-contexts bullet now sits above it on `main`). No other ROADMAP edits.
- Draft notes include `category=new` for `spawn_agent` / `as_agent` persona delegation and `category=improvement` for persisted diagnostics.
- Draft notes contain **zero** 403-chain coverage.
- No release notes are published until the operator explicitly confirms.
- Operator is asked in-band before publish; a no/wait leaves artifacts local/draft-only.
- Duplicate R7 #2568 does not double-apply.
- `specs/tasks/task-2575.md` (or equivalent dump) is not shipped.

_— rewritten by Product Manager · 2026-09-16T16:19:18.232Z_

## Out of scope

- Implementing or regressing persona delegation, diagnostics persistence, or the 403 chain.
- Editing any Gap Register line other than the PRD 27 bullet (match by text, not a frozen L142).
- Publishing release notes, tagging a release, or notifying customers without operator confirmation.
- Adding extra changelog entries, rewriting historical `DONE.md` resolutions, or backfilling notes for unrelated gaps.
- Treating this PRD as permission to skip the R6-before-Gap-Register-delete rule.
- Double-applying via duplicate R7 #2568.
- Shipping `specs/tasks/task-2575.md` or any docs-only stub PR.
- This ready-lane Product Owner lock mutating `DONE.md` / `ROADMAP.md` or publishing notes.

_— rewritten by Product Manager · 2026-09-16T16:18:45.004Z_

## Requirements

R-1 through R-12 are the locked product contract for #2575. Functional requirements 1–6 are adopted unchanged and restated here so later lanes do not re-derive them. Architect owns Design. This is not an implementation pass.

**R-1 — R6 gate.** Do not write `DONE.md`, delete any Gap Register row, or draft/publish release notes until R6 is complete. R6 = #2574 (live re-test: file and staff two tasks on project 11) **and** remainder #2597 (all six VS Code work-mode Copy + diagnostics steps recorded as pass). R5 #2573 (core ledger lists migration 1177 then 1178) is a predecessor of R6; R7 stays blocked while R5 is open. If R6 is not done, stop and report blocked. This gate is an **implementation** constraint, not a ready-lane product defect.

**R-2 — DONE.md.** After R6, append **one** dated `RESOLVED` entry matching existing `DONE.md` style. Date = the calendar day R6 passed, not a placeholder. The entry names the closed gap: persona delegation (`spawn_agent` `as_agent`) + persisted diagnostics close-out. Cite tickets #2562 / #2575. Do not rewrite historical resolutions.

**R-3 — Gap Register (text match, not L142).** Delete the single Gap Register row whose heading is **🟠 PRD 27 (staffed work + coordination gate + chat diagnostics as data) is CODE COMPLETE but entirely UNVERIFIED**. Match **by that text**. A newer first Gap Register bullet (React contexts, found 2026-09-16) now sits above it on `main`, so a frozen `ROADMAP.md` L142 number is not a safe identifier and must not be used to pick the line. Delete that one row only. No other ROADMAP edits. If the matching row is already gone or the line at a given number is a different row, **stop** (idempotency / do not delete the wrong line).

**R-4 — Release notes draft, `category=new`.** Unpublished note for persona delegation via `spawn_agent` `as_agent`. Body: A run can delegate a self-contained sub-task to execute **as** a named workspace agent (`spawn_agent` `as_agent`). The child inherits that agent’s compiled persona (role, bio, skills, personality) instead of the caller’s. This **is** the product decision that persona delegation clears the new-FEATURE bar named on the Gap Register row.

**R-5 — Release notes draft, `category=improvement`.** Unpublished note for persisted brain-chat diagnostics. Body: Chat diagnostics persist (`brain_chat_diagnostics`) and are retrievable via `builtin_chats_diagnostics` after Copy, so a copied report can be correlated to a stored row.

**R-6 — Release notes omit the 403 chain.** Zero coverage. No title, no body, no “fixed 403” mention in draft or published notes.

**R-7 — Publish gate.** Present the `DONE.md` line, the Gap Register deletion, and the two draft notes to the operator **in-band**. Publish **only** after explicit confirmation. Operator silent or decline → notes stay unpublished. This ticket does not tag a release, bump versions, or notify customers.

**R-8 — Idempotency.** If the PRD 27 Gap Register row is already gone or `DONE.md` already has this `RESOLVED`, do not duplicate. Still require operator confirm before any publish.

**R-9 — Duplicate R7.** Ticket **#2568** is the same close-out. Do not apply twice. #2575 is the applying ticket; #2568 is a no-op if #2575 already closed the records.

**R-10 — No product code.** Out of scope: implementing or regressing persona delegation, diagnostics persistence, or the 403 chain. No `vsce` / version bump / migration (those are R3–R5 on epic #2562).

**R-11 — No specs dump.** Do not add or keep `specs/tasks/task-2575.md` (or equivalent). A prior pass left that stub on `builderforce/task-2575`; the applying developer must delete it rather than ship a docs-only PR. No conceptual stand-in if the repo bind fails.

**R-12 — Design owner.** Architect owns Design. This PO lock is the product contract only.

_— rewritten by Product Manager · 2026-09-16T16:19:13.649Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

**Author:** Bob Developer (V2 Container) · #2575 · 2026-09-16 · execution 31722

### Apply status — NOT applied this run

R7 mutations of `DONE.md` / `ROADMAP.md` were **not** written. Two hard gates failed:

1. **R6 gate (PRD FR-1).** Do not write `DONE.md`, delete L142, or draft/publish notes until R6 is complete.
   - Epic #2562 still lists R6 as remaining.
   - #2574 “Live re-test…” is `ready` (not done). Duplicate R6 child #2567 is `backlog`.
   - Operator remainder #2597 (VS Code work-mode Copy + diagnostics) is `todo`. Pass only if all six steps on that ticket hold.
   - Predecessor R5 #2573 (migrations 1177/1178 on core) is still `in_progress`. R6 cannot pass while R5 is open.
2. **Repo bind.** This developer container has **no repository bound**. `git_sync_latest`, `list_files`, `run_command`, and `project_files_list` all fail. Project 11 `githubRepoUrl=null`. Cannot read `DONE.md` / `ROADMAP.md` L142, so cannot apply or verify. Same bind-blocker as #2573/#2555/#2545/#2532. Bind **SeanHogg/Builderforce.ai** as `builderforce/task-2575`. Do **not** dump a conceptual stand-in.

Branch `builderforce/task-2575` is recorded 1 commit ahead of `main` with no PR. Treat that commit as unverified (likely docs-only stub). After bind: inspect it; if it is dead `specs/tasks/task-2575.md` (or equivalent), delete it — do not ship a docs-only PR. Reconcile; do not append a second stub.

Duplicate R7 ticket **#2568** (`backlog`, also 1 unmerged commit) is the same close-out. Do not apply twice.

### Planned mutations (only after R6 pass + bind)

Do these in order, then **stop** for operator confirm. No publish.

1. **Read** `DONE.md` (tail) and `ROADMAP.md` around L142. Confirm L142 is the matching Gap Register row for this PRD 27 gap (persona delegation + persisted diagnostics). If L142 is already gone or is a different row, **stop** (idempotency / do not delete the wrong line).
2. **`DONE.md`:** append **one** dated `RESOLVED` entry matching existing file style. Date = actual resolution date (the day R6 passed), not a placeholder. Names the closed gap: persona delegation (`spawn_agent` `as_agent`) + persisted diagnostics close-out. Tickets #2562 / #2575. Do not rewrite historical resolutions.
3. **`ROADMAP.md`:** delete **exactly** the matching Gap Register line (specified as L142). No other ROADMAP edits.
4. **Release notes — draft only, unpublished.** Do not tag a release, do not notify customers.

### Unpublished release-notes draft (present to operator; do not publish)

| category | title | body |
|---|---|---|
| `new` | Persona delegation via `spawn_agent` `as_agent` | A run can delegate a self-contained sub-task to execute **as** a named workspace agent (`spawn_agent` `as_agent`). The child inherits that agent’s compiled persona (role, bio, skills, personality) instead of the caller’s. |
| `improvement` | Persisted brain-chat diagnostics | Chat diagnostics persist (`brain_chat_diagnostics`) and are retrievable via `builtin_chats_diagnostics` after Copy, so a copied report can be correlated to a stored row. |

**Omit entirely:** the 403 chain — no title, no body, no “fixed 403” mention.

### Publish gate

Present the `DONE.md` line, the L142 deletion, and the two draft notes to the operator **in-band**. Publish **only** after explicit confirmation. If the operator declines or is silent, leave notes unpublished. This run did not publish.

### Out of this apply

- No code for persona delegation, diagnostics persistence, or the 403 chain.
- No extra Gap Register lines.
- No changelog besides the two draft categories above.
- No `vsce` / version bump / migration (R3–R5).

### Unblock checklist for the next developer run

1. Operator binds SeanHogg/Builderforce.ai at `builderforce/task-2575` (project 11 `githubRepoUrl` today null).
2. R5 #2573: core ledger lists 1177 then 1178.
3. R6 #2574 + remainder #2597: all six work-mode steps recorded as pass.
4. Then apply the three artifacts above and **ask again** before any publish.

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:49:32.545Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:49:35.901Z · execution #31722

**2026-09-16 · Bob Developer (exec 31722):** R7 apply blocked. R6 is not complete (#2574 `ready`, remainder #2597 `todo`, R5 #2573 still `in_progress`) so PRD FR-1 forbids writing `DONE.md` or deleting ROADMAP L142. Second blocker: no repo bound on this container (project 11 `githubRepoUrl=null`; git/list_files/run_command fail). No product-tree mutation this run; no release notes published. Planned apply + unpublished draft notes are in Implementation Notes. Operator must bind SeanHogg/Builderforce.ai as `builderforce/task-2575`, finish R5 then R6, then confirm publish. Duplicate R7 #2568 must not double-apply.

---

### Update — Cloud agent d02ff7ee-9cf2-4c44-8558-c89104f6278f · 2026-09-16T15:57:24.780Z · execution #31722

**2026-09-16 · Bob Developer (exec 31722 requeue):** R7 still not applied. FR-1 holds: R5 #2573 `in_progress`; R6 #2574 `ready`; remainder #2597 `ready` (was todo) — none done. GitHub API 403 (user 15883045) blocks list/read/write including delete of `specs/tasks/task-2575.md`. Context now shows seanhogg/builderforce.ai bound, but writes still fail. Read-only via jsdelivr/raw: `DONE.md` has no PRD 27 RESOLVED; Gap Register still has the 🟠 PRD 27 unverified row. A newer first Gap Register bullet (React contexts, found 2026-09-16) sits above it, so **do not delete by L142 number** — match the PRD 27 row by text. Unpublished draft unchanged (new=`spawn_agent` `as_agent`; improvement=persisted diagnostics; omit 403). No publish. Duplicate R7=#2568.

---

### Update — Product Manager · 2026-09-16T16:19:20.729Z · execution #31855

**2026-09-16 · Product Owner ready-lane lock (exec 31855):** Approved. Contract = **R-1–R-10** (R6 gate; one dated DONE.md RESOLVED; Gap Register delete **by PRD 27 text not frozen L142**; unpublished notes `new`=`spawn_agent` `as_agent` + `improvement`=persisted diagnostics; **omit 403**; operator confirm before publish; idempotent; duplicate #2568 must not double-apply; no `specs/tasks/task-2575.md`; no product-code; bind required). Architect owns Design. This lane did **not** mutate `DONE.md`/`ROADMAP.md` and did **not** publish notes. Inspected: ROADMAP Gap Register still has the 🟠 PRD 27 row after a newer React-contexts bullet; R5 #2573 `in_progress`; R6 #2574 `ready`; remainder #2597 `ready` (six steps unrecorded); developer exec 31722 `changes_requested` (correctly blocked on FR-1); branch had dead `specs/tasks/task-2575.md` — deleted (commit bdc7f20c). Project 11 `githubRepoUrl` still null.

---

### Update — Product Manager · 2026-09-16T16:19:22.969Z · execution #31854

**2026-09-16 · Product Owner ready-lane lock (exec 31854):** Contract = **R-1–R-12** (Requirements section). FR-1–FR-6 adopted. Architect owns Design. No product-repo patch this lane. No specs/tasks dump (R-11: applying developer deletes `specs/tasks/task-2575.md` rather than shipping it).

Inspected against `main` (raw): `DONE.md` has **no** PRD 27 `RESOLVED`; Gap Register still carries the 🟠 PRD 27 unverified row as the **second** bullet under the index (first bullet is React contexts, found 2026-09-16). **Do not delete by L142** — R-3 / AC match by text.

R5 #2573 `in_progress` and R6 #2574 / #2597 not done remain **implementation** gates (R-1), not a reason to reject this ready-lane contract. Developer exec 31722 correctly did not apply. Duplicate R7 = #2568 (R-9). Unpublished draft locked as R-4 / R-5; omit 403 (R-6); operator confirm before publish (R-7). Persona delegation **does** clear the new-FEATURE bar (R-4). Do not use PRs 820/821.