> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2440
> _Each agent that updates this PRD signs its change below._

# PRD: Render `%` in brain-ui chat switcher/dropdown

**Status:** WIP  
**Type:** Bugfix / display correctness  
**Surface:** brain-ui chat switcher and chat dropdown

## Problem & Goal

**Problem.** The brain-ui chat switcher and chat dropdown do not surface linked-ticket completion. Epic #2436 (“Add % complete to VSIX chat lists”) requires every chat-list surface to show the same batched `progressPct` already computed by `ChatTicketService` (the same number the in-chat header widget uses). Without it, users scanning chats in brain-ui cannot tell which conversations are furthest along, and the switcher will diverge from the VS Code sessions tree (#2439).

A prior draft of this PRD misread the ticket title “Render `%`” as a display bug for the U+0025 character in chat *titles*. That is not the defect. Literal `%` in titles must still render as `%` (non-regression), but the work is to **render the progress percentage**, not to fix title encoding.

**Goal.** When a listed chat carries `progressPct` (0–100), the switcher trigger and every dropdown row show that value as ` · {n}%` next to the chat title, matching epic AC and the sessions-tree format. When `progressPct` is omitted or null (no linked tickets), no indicator is shown. Titles that themselves contain `%` remain character-for-character identical.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:17.582Z_

## Target users / ICP roles

- End users who switch chats from the brain-ui switcher/dropdown and need ticket-linked progress without opening the chat.
- VS Code / webview users who already see (or will see) the same indicator in the sessions tree and expect parity.
- Power users scanning many chats to pick the one furthest along.
- QA / frontend engineers verifying list-row progress rendering and title non-regression for `%`.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:21.991Z_

## Scope

In scope: visual rendering of **ticket-linked `progressPct`** on brain-ui **chat switcher** (collapsed trigger) and **chat dropdown** (open list) only.

Includes:

- Primary chat title plus optional ` · {n}%` suffix when `progressPct` is a number
- Selected, hover, focus, disabled, and overflow/ellipsis states of both trigger and rows
- Keyboard and pointer open/close of the switcher/dropdown
- Empty, short, and long titles, including titles that themselves contain one or more `%` characters
- Accessible name / `aria-label` / tooltip including the percent when present
- Consuming `progressPct` from the existing chat-list payload (`BfBrainChat`); no per-row ticket fetch
- Non-regression: U+0025 in titles, previews, and labels still paints as the character `%`

Sibling tickets (do **not** re-implement here):

- #2437 — API `GET /api/brain/chats` batches `progressPct` via `ChatTicketService`
- #2438 — `progressPct?: number` on `BfBrainChat` in `bfApi.ts`
- #2439 — same suffix on VS Code `sessionsTree.ts` `item.description`
- #2441 — live refresh when ticket health changes after the list was fetched

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:26.106Z_

## Functional requirements

1. **Progress indicator.** If `chat.progressPct` is a finite number in `[0, 100]`, append ` · {n}%` (space, U+00B7 MIDDLE DOT, space, integer, percent sign) to the visible title on both the collapsed trigger and each dropdown row. `{n}` is `Math.round(progressPct)` clamped to 0–100.
2. **Absence vs zero.** If `progressPct` is `undefined`, `null`, or non-finite, omit the indicator entirely. Do **not** show ` · 0%`, ` · —`, or a placeholder. A numeric `0` **does** show ` · 0%` — that means linked tickets exist and none are done.
3. **Canonical format.** The suffix must match epic #2436 AC and sibling #2439: ` · 85%`, not `(85%)`, `85 percent`, a progress bar, or a colored chip.
4. **Literal `%` in titles (non-regression).** Chat titles, previews, and secondary labels that contain U+0025 must still render as `%`. Titles must not be passed through printf-style, i18n, or template formatters that consume `%`. A title `Q4 12% growth` with `progressPct: 40` shows `Q4 12% growth · 40%`.
5. **No interpolation.** The suffix is concatenated after the canonical title. Do not format the title with `%s` / `%d` / i18n `%{n}`.
6. **Overflow.** Long titles plus the suffix ellipsize with the same rules as titles without progress. Prefer clipping the title, not dropping the `%` glyph of the suffix or of a title that contains `%`.
7. **Selection & filter.** If the dropdown filters or highlights, matching is against the **title** (and existing search fields). `progressPct` is not a search wildcard. A query `%` matches titles containing `%`, not “any progress”.
8. **Accessibility.** Accessible name includes the title and, when progress is shown, a spoken form such as “, 85% complete”. Do not rely on the MIDDLE DOT alone for AT.
9. **Stability.** Presence or absence of `progressPct`, or of `%` in the title, must not crash the menu, blank the trigger, or skip the row.
10. **Parity.** Trigger and open list both show the same title + suffix for the current chat. The number shown is the list payload’s `progressPct`, not a second client-side computation.
11. **No extra I/O.** The switcher must not call ticket-health or `ChatTicketService` per row. It reads `progressPct` already on the chat DTO.
12. **Unchanged chats.** Chats without linked tickets (no `progressPct`) look unchanged versus today.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:30.093Z_

## Acceptance criteria

- A chat with title `Sprint board` and `progressPct: 85` shows `Sprint board · 85%` on the switcher trigger and in every dropdown row state (default, hover, selected, truncated).
- A chat with no `progressPct` (or `null`) shows only its title — no ` · 0%`, no dash, no extra separator.
- A chat with `progressPct: 0` shows ` · 0%`; a chat with `progressPct: 100` shows ` · 100%`.
- Titles `%`, `%%`, `%s`, `100%`, `foo%bar`, `%20`, `complete%`, and `Q4 12% growth` still render character-for-character; when `progressPct` is also set, the suffix is appended after the full title.
- Switching to a chat that has `progressPct` updates the trigger to that title + ` · {n}%` (modulo the same ellipsis rules as other titles).
- Filtering the dropdown by a substring of the title (including a substring that is `%`) still returns and labels the matching chats; the progress suffix is not required to be part of the query.
- Screen reader name for the row matches the visible title and includes the percent when present (e.g. “Sprint board, 85% complete”).
- No JS exception, blank row, extra network request per row, or layout jump unique to `progressPct` or to `%` in the title.
- Visual snapshot or equivalent covers at least: `progressPct: 85`, `progressPct: 0`, omitted `progressPct`, title `100%` with `progressPct: 50`, title `%s` with no progress, and a long title with `%` near the ellipsis boundary.
- Chats without `%` in the title and without `progressPct` are visually unchanged.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:33.982Z_

## Out of scope

- Computing or batching `progressPct` (API `GET /api/brain/chats` + `ChatTicketService`) — #2437
- Adding `progressPct?: number` to `BfBrainChat` in `bfApi.ts` — #2438
- Rendering the same suffix on the VS Code sessions tree (`sessionsTree.ts` `item.description`) — #2439
- Live refresh when ticket health changes after the list was fetched — #2441
- Renaming chats, persistence, or backend title validation
- Chat transcript / message body rendering
- The in-chat header progress widget (already ships; this ticket **reads** the same number, it does not restyle that widget)
- Other brain-ui surfaces (sidebars, search results, share dialogs, tabs) unless they **are** this switcher/dropdown
- Changing search wildcard semantics product-wide
- Localization of the `%` character or locale-specific percent formatting (`85 %` vs `85%`)
- New visual design (no progress bar, badge, or color-coded chip unless already used for this suffix)
- Encoding fixes for other special characters except insofar as a shared escaping path is required so `%` in titles does not regress

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:39:38.250Z_

## Requirements

### User stories

1. **As a** brain-ui user scanning the chat switcher, **I want** each listed chat that is linked to tickets to show how complete those tickets are, **so that** I can pick the conversation that is furthest along without opening it.
2. **As a** user who already sees `%` complete on the VS Code sessions tree, **I want** the brain-ui switcher and dropdown to use the same number and the same ` · {n}%` format, **so that** chat identity is consistent across surfaces.
3. **As a** user whose chat title contains `%` (metrics, URLs, `%s`), **I want** that title to still display character-for-character, **so that** adding progress does not break names.

### Numbered requirements (PM lock · todo)

These IDs are the implementer contract. They refine, not replace, the BA display rules below.

- **R-1 Progress indicator.** If the list DTO progress field is a finite number in `[0, 100]`, append ` · {n}%` (space, U+00B7 MIDDLE DOT, space, integer, percent sign) to the visible title on both the collapsed switcher trigger and each open-list row. `{n}` is `Math.round(value)` then clamped to 0–100.
- **R-2 Absence vs zero.** If the field is `undefined`, `null`, or non-finite, omit the indicator entirely. Do **not** show ` · 0%`, ` · —`, or a placeholder. A numeric `0` **does** show ` · 0%` — linked tickets exist and none are done. Never client `?? 0`.
- **R-3 Canonical format.** Suffix matches epic #2436 AC and sibling #2439: ` · 85%`. Not `(85%)`, `85 percent`, a progress bar, a chip, or `{n}% · {title}` (that order is retired for this surface). Always `85%` with no locale gap.
- **R-4 Literal `%` in titles (non-regression).** Chat titles, previews, and secondary labels that contain U+0025 still render as `%`. Titles must not pass through printf-style, i18n, or template formatters that consume `%`. A title `Q4 12% growth` with progress `40` shows `Q4 12% growth · 40%`.
- **R-5 No interpolation.** Concatenate the suffix after the canonical title. Do not format the title with `%s` / `%d` / i18n `%{n}`.
- **R-6 Overflow.** Long titles plus the suffix ellipsize with the same rules as titles without progress. Prefer clipping the title, not dropping the `%` glyph of the suffix or of a title that contains `%`.
- **R-7 Selection & filter.** Matching is against the **title** (and existing search fields). Progress is not a search wildcard. A query `%` matches titles containing `%`, not “any progress”.
- **R-8 Accessibility.** Accessible name is `{title}` when no progress; `{title}, {n}% complete` when progress is shown. Tooltip, if any, includes the same title and percent. Do not rely on the MIDDLE DOT alone for AT.
- **R-9 Stability.** Presence or absence of the progress field, or of `%` in the title, must not crash the menu, blank the trigger, or skip the row.
- **R-10 Parity.** Trigger and open list both show the same title + suffix for the current chat. The number shown is the list payload’s progress field, not a second client-side computation.
- **R-11 No extra I/O.** The switcher must not call ticket-health or `ChatTicketService` per row. Zero extra HTTP from this surface.
- **R-12 Unchanged chats.** Chats without linked tickets (no progress field) look unchanged versus today.
- **R-13 Shared helper.** Implement via existing `packages/brain-ui/src/chatTickets/chatSwitcherLabel.ts` (already exported from `packages/brain-ui/src/index.ts`). Do **not** fork a second formatter. Align its visible suffix to R-1. Merge-dialog callers of this helper keep the same grammar (one helper, one string). Architect may extract a tiny `formatChatProgressSuffix` only if #2439 needs the same suffix off the title; default is reuse `chatSwitcherLabel`.
- **R-14 Display gate is the progress field.** Consume `ticketProgressPct` on the chat list DTO (already shipping on `BfBrainChat` / brain-ui chat types). If #2438 lands `progressPct` as well, Architect maps **one** input into the helper — do not read both and diverge. Do not require `ticketCount > 0` as the *display* gate: omit when progress is null/non-finite; show when progress is a finite `[0, 100]`. Producers omit the field when no tickets are linked.
- **R-15 Sibling split.** #2443 owns “each dropdown row binds **that row’s own** list field” (do not copy the selected chat’s percent onto every row; Architect: shared UI value vs shared list payload). #2440 still paints trigger + rows with the formatter and must not introduce a single shared UI value for all rows. #2439 / #2442 own the VSIX tree (`item.description` `{relativeTime} · {n}%`; title stays title-only). #2441 owns live refresh after fetch. #2437 / #2438 produce the field (soft-dep: absent field = today’s UI, no crash). #2444 owns N+1.
- **R-16 Spec home.** This ticket’s spec lives on **this ticket PRD**. Do not add `specs/tasks/task-2440.md` or any docs-only stub to the product repo. Developer implementation is `packages/brain-ui` only (plus tests on `chatSwitcherLabel`). No version bump / publish on this ticket.

### Data contract (consumed, not produced)

This ticket **consumes** the list DTO; it does not define the API.

| Field | Type | Meaning |
| --- | --- | --- |
| `title` (canonical chat title) | `string` | Shown as-is. May contain U+0025. |
| `ticketProgressPct` (keep existing name) | `number \| null \| undefined` | Ticket-linked completion 0–100 from `ChatTicketService`, batched on `GET /api/brain/chats` (#2437). #2438 may also type `progressPct`; Architect maps one field into the helper (R-14). |

Rules:

- Treat only a finite number in `[0, 100]` as “has progress”. Round to nearest integer (`Math.round`) then clamp.
- `undefined` / `null` / `NaN` / non-number → no suffix.
- Numeric `0` is progress (show ` · 0%`); it is not “missing”.
- Do not recompute from tickets, messages, or the header widget. Do not fetch per row.
- If #2437/#2438 have not landed, the field is absent and the switcher must behave as today (no suffix, no crash). Feature is additive.

### Display rules

Visible label:

```
{title}                // no progress field
{title} · {n}%         // progress field present
```

- Separator is space + U+00B7 MIDDLE DOT + space (epic AC: “` · 85%` next to the title/time”).
- `{n}` is the rounded integer; always a `%` suffix with no locale gap (`85%` not `85 %`).
- Apply on **both** the collapsed trigger (current chat) and every open-list row.
- Secondary line (preview, timestamp) is unchanged unless it already displays the title; do not duplicate the suffix on the preview.
- Ellipsis: same overflow policy as today; keep the progress suffix visible when space allows; never use a printf/`%s` formatter on the title.

### Accessibility

- `aria-label` / accessible name: `{title}` when no progress; `{title}, {n}% complete` when progress is shown.
- Tooltip, if any, must include the same title and percent.
- MIDDLE DOT is visual only; AT must hear “percent complete”, not an unlabeled “85”.

### Interaction

- Open/close (pointer and keyboard), highlight, select, and filter behave as today.
- Filter/highlight match the **title** (and existing fields). `%` in the query is literal. Progress is not a filter facet in this ticket.
- Selecting a chat with progress updates the trigger to that title + suffix.

### Non-functional

- **No N+1:** zero extra HTTP from the switcher/dropdown for progress.
- **Perf:** suffix concatenation is O(rows); no layout thrash unique to `%`.
- **i18n:** do not pass titles through formatters that treat `%` as a specifier. If a string is translated, use a placeholder that is not `%s` on the title itself (e.g. concatenate in code).
- **Resilience:** unknown extra fields on the chat DTO are ignored. Out-of-range numbers clamp; they do not throw.

### Dependencies and sequence

| Ticket | Role vs #2440 |
| --- | --- |
| #2436 (epic) | Parent. Shared format ` · {n}%`. Source of truth = in-chat header widget / `ChatTicketService`. |
| #2437 | Producer of the list progress field. Soft dependency: UI is a no-op until the field exists. |
| #2438 | Producer of `BfBrainChat.progressPct` (may alias `ticketProgressPct`). Soft dependency for TypeScript consumers. |
| #2439 / #2442 | Parallel VSIX tree. **Must use the same suffix grammar** so VSIX tree and brain-ui switcher do not drift. Tree puts suffix on `description`, not title. |
| #2441 | Live refresh of the number after ticket health changes. **Out of scope** here: #2440 paints whatever the last list payload had. |
| #2443 | Dropdown **per-row binding** (each row’s own field; not a copied selected-chat value). Shares this ticket’s formatter. |
| #2444 | No N+1. |

Architect (#2440 Design) should confirm `chatSwitcherLabel` as the single helper (R-13) and name the exact switcher/dropdown component that consumes it.

### Traceability

| AC | Requirement |
| --- | --- |
| `Sprint board` + 85 → `Sprint board · 85%` | R-1, R-3, Display rules |
| Missing/`null` → title only | R-2 |
| `0` → ` · 0%`; `100` → ` · 100%` | R-2 |
| Titles containing `%` unchanged; suffix appended | R-4, R-5 |
| Trigger updates on switch | R-10 |
| Filter by title / by `%` | R-7 |
| AT name includes percent | R-8 |
| No extra I/O, no crash | R-9, R-11 |
| Snapshots for 85, 0, omitted, `100%`+50, `%s`, long title | Test Evidence (QA) |
| Unchanged chats stay unchanged | R-12 |
| One helper, no fork | R-13 |
| One DTO field, no `?? 0` | R-14 |
| Rows not a copied selected % | R-15 / #2443 |
| No specs/tasks dump | R-16 |

### Explicit non-goals (BA + PM)

- Do not invent a second progress calculation on the client.
- Do not wait on #2441 to ship the first paint.
- Do not treat this ticket as a “percent character encoding” bugfix; that reading is retired.
- Do not dump this PRD into `specs/tasks/`.
- Do not restyle the in-chat header progress widget.
- Do not change VSIX `sessionsTree.ts` here (#2439 / #2442).

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T15:56:37.196Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:40:48.164Z · execution #31665

**BA decision (ticket #2440).** The ticket title “Render `%` in brain-ui chat switcher/dropdown” is a child of epic #2436 (Add % complete to VSIX chat lists), not a U+0025 encoding bug. Ada’s first PRD drafted the wrong problem. Corrected Problem & Goal, Scope, Functional requirements, Acceptance criteria, and Out of scope; authored Requirements. Display contract: consume list `progressPct` (0–100 from ChatTicketService / #2437+#2438); paint ` · {n}%` (U+00B7) on trigger and dropdown rows; omit when null/undefined; numeric 0 shows ` · 0%`. Literal `%` in titles is a non-regression, not the feature. Soft-dep on #2437/#2438; format parity with #2439; live refresh is #2441 (out of scope). No repo code in this BA pass — Requirements are the deliverable.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T15:57:05.907Z · execution #31742

## Product requirements (R-1–R-15) — PM lock

Signed by Kevin (Product Manager) · execution #31743 · 2026-09-16.

**This lane:** todo product lock only. Architect owns **Design** next. Developer implements after Design. **No product-repo patch this pass** (bound `seanhogg/builderforce.ai` is empty / project 11 `githubRepoUrl=null`). **Do not ship `specs/tasks/task-2440.md`** — ticket PRD is source of truth; a prior BA dump on the branch is dead and must not be the apply path.

### Metric (locked)

The switcher/dropdown paints **linked-ticket rollup** for the tickets attached to **that** chat — the same number the in-chat header widget / `ChatTicketService` already computes (epic #2436). It is **not** context-window fill (#2608), **not** streaming/generation progress, **not** billing/cost/quality.

| Layer | Field | Role |
| --- | --- | --- |
| Client chat record | `ticketProgressPct: number \| null \| undefined` | What the helper reads. `null` / absent = no linked tickets. |
| List API (`GET /api/brain/chats`, #2437) | `progressPct` | Same rollup on the wire. Client maps it onto `ticketProgressPct` (or the list object after that map). |
| `BfBrainChat.progressPct?` (#2438) | optional number | **Do not bind the switcher to this field if #2438 keeps it as distinct chat-run/streaming progress.** Collapsing the two names is a sibling defect; this surface always paints ticket rollup. |

### Formatter (this ticket owns; #2443 must not fork)

Named location for Architect to confirm in Design: `packages/brain-ui/src/chatTickets/chatSwitcherLabel.ts` (+ existing unit test). One helper; do not fork in `BrainPanel` / the VS Code webview.

**Visible grammar (locked — #2440 wins over #2443’s quoted prefix):**

```
{title}                  // no computable rollup
{title} · {n}%           // finite rollup 0–100
```

- Separator is space + U+00B7 MIDDLE DOT + space.
- `{n}` = `Math.round(value)` then clamp to 0–100; no locale gap (`85%` not `85 %`).
- **Not** `{n}% · {title}`. That prefix is what #2442 retires on the VSIX tree. #2443’s PRD still quotes the old prefix — #2443 owns **per-row binding** only; format lives here.
- Concatenate after the canonical title. Do **not** pass the title through printf / i18n `%s` / `%d` / `%{n}` (literal `%` in titles is a non-regression, not the feature).
- Secondary line (preview, timestamp) is unchanged; do not duplicate the suffix there.
- VSIX Sessions tree (#2439 / #2442) uses the same ` · {n}%` grammar in a **different slot** (`TreeItem.description`; title stays title-only). Do not re-implement that tree here.

### Presence vs zero (do not invent a third policy)

| Value | Render |
| --- | --- |
| finite number in `[0, 100]`, including `0` | `{title} · {n}%` (`0` means linked tickets exist and none are done) |
| `null` / `undefined` / non-finite / field absent | title only — no ` · 0%`, no dash, no placeholder |

Never `?? 0` inside the helper. **#2437 constraint:** if the list API emits never-null `summary?.pct ?? 0` for chats with no linked tickets, the map onto `ticketProgressPct` **must restore `null`** when no tickets are linked. Painting ` · 0%` on every unlinked chat fails R-3 / FR12 (unchanged chats). This ticket does not change the API; it refuses to treat a coerced `0` as “has tickets.”

### Numbered requirements

- **R-1.** Consume ticket-rollup only (`ticketProgressPct` after the list map). No per-row ticket-health fetch. No client recompute. No N+1.
- **R-2.** If the value is a finite number in `[0, 100]`, the collapsed trigger and every dropdown row show `{title} · {n}%`.
- **R-3.** If the value is `undefined`, `null`, or non-finite, show the title only.
- **R-4.** Numeric `0` shows ` · 0%`; numeric `100` shows ` · 100%`.
- **R-5.** Trigger and open list show the same title + suffix for the current chat. The number is the list payload’s rollup, not a second client-side computation.
- **R-6.** Titles, previews, and labels that contain U+0025 still render as `%`. Example: title `Q4 12% growth` with rollup `40` → `Q4 12% growth · 40%`.
- **R-7.** Filter / highlight match the **title** (and existing search fields). A query `%` matches titles containing `%`, not “any progress.” Progress is not a filter facet.
- **R-8.** Accessible name: `{title}` when no rollup; `{title}, {n}% complete` when shown. Tooltip, if any, matches. MIDDLE DOT is visual only — AT must hear “percent complete.”
- **R-9.** Overflow uses today’s ellipsis rules. Prefer clipping the title, not dropping the suffix `%` or a `%` that is part of the title.
- **R-10.** Presence or absence of the field, or of `%` in the title, must not crash the menu, blank the trigger, skip a row, or fire extra HTTP.
- **R-11.** Unknown extra DTO fields are ignored. Out-of-range numbers clamp; they do not throw.
- **R-12.** Chats with no linked tickets (field omitted / null) look unchanged versus today.
- **R-13.** Soft-dep on #2437 / #2438: absent field = today’s UI. Live refresh after ticket health changes is **#2441 (out of scope)**. Per-row binding (every row showing one leaked number) is **#2443 (out of scope)**. VSIX tree is **#2439 / #2442 (out of scope)**.
- **R-14.** Single helper; Architect names the exact brain-ui switcher/dropdown component in Design and confirms the helper path. Do not restyle the in-chat header widget.
- **R-15.** Tests / snapshots cover at least: rollup `85`, `0`, omitted; title `100%` with rollup `50`; title `%s` with no rollup; a long title with `%` near the ellipsis boundary. Helper must be pure per chat object (given chat A’s fields, it never reads chat B).

### Explicit non-goals (PM)

- Do not author **Design** (architect) or **Implementation Notes** (developer).
- Do not invent a second progress calculation on the client.
- Do not wait on #2441 to ship first paint.
- Do not treat this ticket as a percent-character encoding bug (that reading is retired).
- Do not retarget to context-window usage.
- Do not add a progress bar, badge, or color-coded chip.
- Do not localize the `%` glyph or insert a locale gap (`85 %`).

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T15:57:10.696Z · execution #31743

**PM lock (todo · Kevin · execution 31743).** Product slice is locked as numbered **R-1–R-16** on Requirements. This lane does **not** implement code: Design is Architect; implementation is Developer on `packages/brain-ui` (`chatSwitcherLabel` + switcher trigger/rows). Bound GitHub is rate-limited (403) this run; `list_files`/`read_file`/`delete_file` failed. Branch `builderforce/task-2440` is 9 commits ahead with Ada’s `specs/tasks/task-2440.md` dump — that file **must not** be opened as the implementation PR (R-16). Do not invent a stand-in against an empty checkout. **Success:** a linked chat shows `{title} · {n}%` on trigger and rows; unlinked chats unchanged; titles with `%` character-identical; AT `{title}, {n}% complete`; zero extra HTTP. Soft-dep #2437/#2438; format parity #2439; row-own-field binding #2443; refresh #2441. Field: `ticketProgressPct` (keep); never `?? 0`.

---

### Update — Product Manager · 2026-09-16T16:19:41.612Z · execution #31869

## Product Owner ready-lane lock — 2026-09-16 · execution #31870

Signed by Product Owner (product-manager-t1) reviewing scope + acceptance criteria at lane `ready`.

**Verdict: approved.** BA Requirements + PM lock (exec 31665 / 31743) are sufficient to implement. Architect owns **Design** next (still unauthored; that is a parallel ready-lane slot, not a PO change request). Developer implements after Design. This PO pass does **not** author product code.

### Canonical implementer contract

Use the **Requirements** section IDs **R-1–R-16** (Progress indicator … Spec home). The later PM-append block that re-numbers overlapping rules as R-1–R-15 is commentary, **not** a second ID space. Do not invent a third numbering.

### P0–P6 (operational, unchanged substance)

- **P0 Field.** Consume `ticketProgressPct` (`number | null | undefined`). Finite `[0, 100]` (incl. `0`) paints; `null` / absent / non-finite omits. Never client `?? 0`. Do **not** bind streaming/run `BfBrainChat.progressPct` if #2438 keeps that name distinct. If list API #2437 emits never-null `summary?.pct ?? 0`, the map onto `ticketProgressPct` **must restore `null`** when no tickets are linked — otherwise every unlinked chat would show ` · 0%` and fail R-12.
- **P1 Grammar.** Visible `{title} · {n}%` suffix (space + U+00B7 + space + rounded/clamped integer + `%`, no locale gap). Not `{n}% · {title}`, not `(85%)`, not a bar/chip. Trigger and every dropdown row use the same formatter.
- **P2 One helper.** `packages/brain-ui/src/chatTickets/chatSwitcherLabel.ts` only. #2443 owns per-row **own-field** binding (do not copy the selected chat’s percent onto every row). VSIX tree is #2439/#2442. Refresh is #2441. API is #2437. DTO is #2438.
- **P3 No extra I/O.** Zero HTTP from this surface. Soft-dep: absent field = today’s UI, no crash.
- **P4 Literal `%`.** Titles/previews containing U+0025 stay character-identical. Concatenate; no printf / i18n `%s` on the title. AT name: `{title}` or `{title}, {n}% complete`.
- **P5 Spec home.** Ticket PRD only. **Do not ship `specs/tasks/task-2440.md`.** Prior BA dump on `builderforce/task-2440` is dead. No version bump.
- **P6 Success.** Linked chat: `{title} · {n}%` on collapsed trigger and open-list rows. Unlinked: title only. `0` → ` · 0%`; `100` → ` · 100%`. Title `Q4 12% growth` + 40 → `Q4 12% growth · 40%`.

### AC vs R-1–R-16

Acceptance criteria in this PRD are testable and map 1:1 to R-1–R-16 / P0–P6. Snapshots (R-15 in Requirements): `85`, `0`, omitted, title `100%`+50, title `%s` with no rollup, long title with `%` near ellipsis. Out of scope is honored: no header-widget restyle, no VSIX tree, no live refresh, no second client rollup.

### Branch / repo note

Diff on `builderforce/task-2440` is **docs-only** (`specs/tasks/task-2440.md`). That file violates R-16/P5 and must not be the implementation PR. Project 11 `githubRepoUrl` is still null; product apply is `packages/brain-ui` on SeanHogg/Builderforce.ai after Architect Design. No new board tickets from this review.

---

### Update — Product Manager · 2026-09-16T16:22:45.458Z · execution #31870

## Product Owner ready-lane lock — Kevin · 2026-09-16

**Verdict: approved** (scope + AC). Architect owns Design next. No product-code in this pass.

**Canonical implementer IDs** are the **R-1–R-16** list under Requirements → "Numbered requirements (PM lock · todo)" (Progress indicator … Spec home). The later PM restatement numbered R-1–R-15 is the **same slice**; do **not** implement against those restated IDs. Constraints from the restatement that are not a separate R-id still bind: never client `?? 0`; list-map restores `null` on `ticketProgressPct` when no tickets are linked (so a #2437 never-null `progressPct: 0` cannot paint ` · 0%` on unlinked chats); do not bind `BfBrainChat.progressPct` if #2438 keeps it as streaming/run progress; visible grammar is suffix `{title} · {n}%` (space + U+00B7 + space + integer + `%`), **not** the current helper prefix `{n}% · {title}`.

**Feasibility (inspected on seanhogg/builderforce.ai `main`):** `packages/brain-ui/src/chatTickets/chatSwitcherLabel.ts` already exists and is exported from `packages/brain-ui/src/index.ts`. Today it (1) prefixes `{n}% · {title}` and (2) gates on `ticketCount > 0`. This ticket **retires both**: suffix per R-1/R-3; display gate is finite `ticketProgressPct` in `[0, 100]` only (R-14) — `ticketCount` is not a render gate. Sole in-package caller today is the merge-dialog list in `ChatTicketsPanel.tsx`; merge-dialog keeps the same helper (R-13). Architect **must name** the exact collapsed-trigger + open-dropdown components that consume it (R-14). Default file set is `packages/brain-ui` + helper tests (R-16). If the trigger/dropdown live in a host (web BrainPanel / VSIX `<select>`), Design may add that host file — do not silently skip paint on the named surface.

**Sibling split (unchanged):** formatter + trigger/row paint = #2440; per-row own-field (no copied selected %) = #2443; VSIX tree `item.description` = #2439/#2442; list API = #2437; DTO = #2438; live refresh = #2441.

**Must not ship:** `specs/tasks/task-2440.md` (R-16). PO could not delete it this run (held by Product Manager). Delete before any product PR; ticket PRD is source of truth.

**Success:** linked chat shows `{title} · {n}%` on trigger and rows; unlinked/null/non-finite = title only; numeric `0` → ` · 0%`; titles with U+0025 character-identical; AT `{title}, {n}% complete`; zero extra HTTP.