> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2588
> _Each agent that updates this PRD signs its change below._

# PRD: Bind a readable checkout for the developer container (unblock #2527)

## Problem & Goal

**Problem.** Ticket #2527 (P0 Advisor profiles) is blocked by an environment gap, not by product design. The developer container can `write_file` into `seanhogg/builderforce.ai` on branch `builderforce/task-2527`, but `list_files`, `read_file`, and all `git_*` operations fail with `no repository bound` / `credential delegation absent`. Implementing Advisor mode requires reading existing talent listing, marketplace category, `PEOPLE_OBJECT_KINDS`, `TalentDetailClient`, Canvas destinations, and i18n. Blind overwrites would destroy production code.

**Goal.** Bind a readable checkout (or restore git credential delegation) for branch `builderforce/task-2527` so the next developer run can search the tree, edit in place, and open a real PR. Success is unblocking #2527 without changing Advisor product behavior in this ticket.

## Target users / ICP roles

- Downstream developer agents assigned to #2527 (primary)
- Platform / container operators who bind repos and credential delegation
- Reviewers who must receive a real PR against `builderforce/task-2527`

## Scope

In scope: bind (or re-bind) a readable working copy of `seanhogg/builderforce.ai` at `builderforce/task-2527` inside the developer container; restore credential delegation so `list_files`, `read_file`, and `git_*` succeed on that checkout; confirm write path still works so in-place edits and a PR are possible.

Out of this ticket’s product work: Advisor profile UX, talent listing changes, marketplace category, `PEOPLE_OBJECT_KINDS`, `TalentDetailClient`, Canvas destinations, and i18n. Those remain #2527 after this unblock.

## Functional requirements

1. The developer container has a bound working tree for `seanhogg/builderforce.ai` on branch `builderforce/task-2527`.
2. `list_files` enumerates the bound tree (at least paths needed for Advisor: talent listing, marketplace category, `PEOPLE_OBJECT_KINDS`, `TalentDetailClient`, Canvas destinations, i18n).
3. `read_file` returns file contents from that tree without “no repository bound”.
4. `git_*` operations (status, diff, log, branch, commit, push as already authorized for this task) succeed; they must not fail with “credential delegation absent”.
5. `write_file` continues to work on the same bound branch so edits are in-place, not disconnected blobs.
6. Binding is durable for the next developer run on this task (same repo, same branch) without a second manual bind.
7. No production files are truncated or replaced as a side effect of binding.

## Acceptance criteria

- A developer-container run on this task can `list_files` and `read_file` existing Advisor-related sources listed above.
- The same run can `git_*` against `builderforce/task-2527` without “no repository bound” or “credential delegation absent”.
- An in-place edit via `write_file` is visible in `git status` / `git diff` on that branch.
- The next agent can open a real PR from `builderforce/task-2527` using the bound checkout.
- Binding does not rewrite, empty, or force-push production code.

## Out of scope

- Implementing Advisor mode, profile UI, or any #2527 product behavior
- Changing talent listing, marketplace category, `PEOPLE_OBJECT_KINDS`, `TalentDetailClient`, Canvas destinations, or i18n
- Broad git / credential platform redesign beyond this container and branch
- Access to repos or branches other than `seanhogg/builderforce.ai` @ `builderforce/task-2527`
- Blind file creation as a substitute for a readable checkout

## Requirements

This ticket is an **environment unblock**, not a product change. Requirements are the bind contract the architect and developer must satisfy so ticket **#2527** (P0 Advisor profiles) can search, edit in place, and open a real PR. No Advisor UX, talent listing, marketplace category, canvas kind, booking bind, or i18n change is in scope here.

### R-1 — Target identity

- **Repository:** `SeanHogg/Builderforce.ai` (same origin as this workspace’s `seanhogg/builderforce.ai`).
- **Branch the next #2527 developer run must be checked out on:** `builderforce/task-2527`.
- **Ticket whose developer container must be readable:** #2527 (not #2588, not epic #2522).
- **Do not** bind a different repo, a different branch, or dump Advisor source onto this ticket’s PR.

Ticket #2527 already records `gitBranch = builderforce/task-2527`. Ticket **#2588** now records `gitBranch = builderforce/task-2588` (corrected 2026-09-16: earlier drafts said `null`). A run on #2588 may therefore see a readable working tree on **this ticket’s branch** — enough to inventory main and author this contract, **not** the success condition. Success is a developer-container run **on #2527**.

Project 11 (`BuilderForce.AI`) still has `githubRepoUrl = null`, `githubRepoOwner = null`, `sourceControlRepoUrl = null`. That is the platform-level reason container tools fail with `no repository bound` / `credential delegation absent`. Binding for #2527 is an operator/container action against that ticket’s `gitBranch`; it is not a product-repo commit on #2588.

### R-2 — Bound working tree

The #2527 developer container must have a bound working copy of that repo at that branch such that:

1. `list_files` enumerates the tree (not `no repository bound`).
2. `read_file` returns existing file contents (not `no repository bound`).
3. `search_code` can locate symbols in that tree.
4. `git_*` (status, diff, log, branch, commit, push as already authorized for the task) succeed (not `credential delegation absent`).
5. `write_file` edits the **same** bound tree in place; writes must appear in `git status` / `git diff` on `builderforce/task-2527`.

### R-3 — Advisor-related sources that must be readable (do not overwrite)

These paths exist on **main** as of the PM checkout on #2588 (`list_files` / `read_file` succeeded). A #2527 run must be able to **read** them before any edit. Blind `write_file` of these paths is forbidden on this ticket and on #2527 until the file has been read.

| Concern | Path (repo-relative) | PM inventory 2026-09-16 |
|---|---|---|
| Talent detail (already imports Book + Message CTA) | `frontend/src/app/talent/[id]/TalentDetailClient.tsx` | Present; imports `BookAdvisorPanel`, `listTalentBookingServices`, `talentPrimaryAction` |
| Talent listing | `frontend/src/app/talent/page.tsx` | Present |
| Talent profile view | `frontend/src/components/freelance/TalentProfileView.tsx` | Present |
| Book panel (A0-shaped; already on main) | `frontend/src/components/freelance/BookAdvisorPanel.tsx` | Present; uses `reserveTalentSession` |
| Booking client (ATS book-by-token) | `frontend/src/app/book/[token]/BookingClient.tsx` | Present |
| Talent ↔ booking_services bind | `frontend/src/lib/freelance/booking.ts` | Present; `talentPrimaryAction`, `listTalentBookingServices`, `reserveTalentSession`; bind is `booking_hosts.host_ref` = talent user id |
| Marketplace storefront | `frontend/src/app/marketplace/MarketplacePageClient.tsx` | Present |
| Marketplace families (`talent` is a family, not a listing kind) | `frontend/src/lib/marketplaceFamilies.ts` | Present; `FamilyId = 'talent' \| 'company' \| 'agent' \| 'asset'`; talent kinds = `person`, `gig`. `LEGACY_CATEGORY` has **no** `advisors` row yet |
| Listing kinds (do **not** add `advisor`) | `packages/creation-canvas-contract/src/marketplaceListings.ts` | Present; five launch verbs only (`play` · `open` · `run` · `preview` · `install`) |
| `PEOPLE_OBJECT_KINDS` | `packages/creation-canvas-contract/src/people.ts` | Present; HR kinds only. Do **not** add `advisory` / `advisor` |
| Object vocabulary (imports people) | `packages/creation-canvas-contract/src/objectKinds.ts` | Present |
| Advisor product spec | `specs/builderforce/26-prd-advisor-platform.md` | Present; forbids `/advisors`, fifth family, and `advisor` listing kind |
| i18n | `frontend/src/i18n/messages/en.json` (and sibling locales) | Locales on main: `en`, `de`, `es`, `fr`, `zh` |

**Constraint discovered on this checkout:** A0-shaped product code is already on main (`talentPrimaryAction`, `listTalentBookingServices`, `BookAdvisorPanel`, Talent detail Book CTA). #2588 must not truncate, replace, or “re-implement” those files as a side effect of binding. After bind, **#2527 must not re-implement A0**; remaining P0 is advisor **mode/fields** on the existing talent listing plus a **talent-family filter** for `category=advisors` (see R-9).

**Constraint:** `specs/builderforce/26-implementation-notes-advisor-platform.md` is **not** on main (only `26-prd-advisor-platform.md` is). Do not invent that file on this branch. Composition-contract text lives in project memory / epic #2522; landing it on main is out of scope for #2588.

### R-4 — Durability

The bind must survive the next developer-container run on **#2527** without a second manual bind: same repo, same branch, credential delegation still present.

### R-5 — Non-destructive

Binding must not rewrite, empty, force-push, or truncate production files. No force-push to `main`. No commit on this ticket that changes talent, marketplace, canvas kinds, booking, or i18n.

This ticket’s product-repo footprint must stay **empty of Advisor source**. The only allowed change on `builderforce/task-2588` is removing dead environment-ops docs (`specs/tasks/task-2588.md` — deleted). Do not open a product PR from #2588.

### R-6 — Scope firewall (this ticket)

| In | Out |
|---|---|
| Bind / re-bind checkout + restore git credential delegation for **#2527** | Advisor profile UX, toggle, fields |
| Confirm `list_files` / `read_file` / `git_*` / in-place `write_file` on `builderforce/task-2527` | New listing kind, `/advisors` route, `MARKETPLACE_LISTING_KINDS` value `advisor`, `PEOPLE_OBJECT_KINDS` value `advisory` |
| Operator notes in **this ticket PRD** only | Product PRs, blind file creation as a substitute for a readable tree |
| Reconcile / drop conceptual dumps **on #2527’s branch after bind** (not here) | Re-implementing A0 Book CTA / `booking.ts` already on main |

### R-7 — Acceptance (testable)

A developer-container run **assigned to #2527** can:

1. `list_files` at `frontend/src/app/talent` and see `TalentDetailClient.tsx`.
2. `read_file` `frontend/src/app/talent/[id]/TalentDetailClient.tsx` and receive the existing source (BookAdvisorPanel import present on main).
3. `search_code` for `PEOPLE_OBJECT_KINDS` and hit `packages/creation-canvas-contract/src/people.ts`.
4. Run `git_*` on `builderforce/task-2527` without “no repository bound” or “credential delegation absent”.
5. Make a trivial in-place edit via `write_file` that shows in `git diff` on that branch, then revert it — proving write and git share one tree.
6. Open a real PR from `builderforce/task-2527` for **#2527 product work** (that PR is #2527’s, not this ticket’s).

This ticket is done when those six are true for #2527’s container, **or** when an operator records that #2527’s container already has a readable checkout (a readable #2588 checkout of main / `builderforce/task-2588` is necessary but not sufficient proof).

### R-8 — Roles

- **Product Manager (#2588, this section):** Scope firewall, R-3 inventory, post-bind product constraints for #2527. No product files.
- **Architect (#2588):** Design how the container bind + credential delegation is restored (ticket `gitBranch`, repo binding, secret delegation, project 11 `githubRepoUrl=null`). No product design.
- **Developer (#2588):** Execute the bind / restore delegation. Do not author Advisor product files. Do not commit to `builderforce/task-2588` except to keep it empty of dumps.
- **QA / Validator:** Prove R-7 against a **#2527** developer run, not against this ticket’s checkout alone.

### R-9 — Post-bind product constraints (for #2527, not this ticket)

Recorded here so the first readable #2527 run does not dump a second product. **Do not implement these on #2588.**

1. **Advisor is a mode on Talent, not a type.** `FamilyId` stays four values. Talent kinds stay `person` · `gig`. Do not add `advisor` to `MARKETPLACE_LISTING_KINDS`. Do not add `advisory` to `PEOPLE_OBJECT_KINDS`. Do not add `/advisors`.
2. **`category=advisors` is a talent filter.** `LEGACY_CATEGORY` on main has `talent` / `gigs` / `all` / … and **no** `advisors` row. After bind, #2527 may add `advisors → { family: 'talent', kind: 'person' }` (or equivalent query filter) so logged-out browse of `/marketplace?category=advisors` resolves to the Talent family. That is a row in the existing table, not a fifth family.
3. **A0 is already on main.** Book CTA, `BookAdvisorPanel`, `talentPrimaryAction`, overlap-refusing `reserveTalentSession` must be **read and reused**, not rewritten.
4. **#2527 branch already has conceptual dumps** (8 commits ahead of main, no PR): `specs/tasks/task-2527.md` and `packages/creation-canvas-contract/src/advisorMode.ts` (created under a `Builderforce.ai/` prefix). First action after a readable checkout: **delete those dumps**, then read the R-3 files, then implement remaining P0 (profile toggle/fields, locales, category filter) against the real tree.
5. Remaining #2527 P0 (after A0): expertise, industries, languages, methods, stages, session length, price (0 = volunteer), location, confidentiality default — on the existing talent/freelancer profile; five locales (`en` `de` `es` `fr` `zh`); both themes. Authenticated setup is a Canvas destination, not new chrome.

### R-10 — Operator bind recipe (architect/developer execute; PM does not)

1. Bind project 11 (or the #2527 container) to `SeanHogg/Builderforce.ai`.
2. Check out **#2527** on `builderforce/task-2527` (not `builderforce/task-2588`, not `builderforce/task-2522`).
3. Restore git credential delegation so `list_files` / `read_file` / `search_code` / `git_*` work in that container.
4. Do not force-push. Do not merge #2527’s 8 dump commits. Do not copy Advisor source onto #2588.
5. Prove R-7 on a **#2527** run, then leave this ticket.

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T15:48:55.363Z_

## Design

This ticket is an **environment unblock**, not a product design. The architect deliverable is this bind + credential-delegation contract so ticket **#2527** (P0 Advisor profiles) can search, edit in place, and open a real PR. No Advisor UX, talent listing, marketplace family, canvas kind, booking, or i18n change is designed or shipped here.

### D-0 — Success identity (do not retarget)

| Item | Value |
|---|---|
| Repo to bind | `SeanHogg/Builderforce.ai` (`https://github.com/SeanHogg/Builderforce.ai`) |
| Branch the next **#2527** developer run must be checked out on | `builderforce/task-2527` |
| Ticket whose **developer container** must become readable | **#2527** (not #2588, not epic #2522) |
| This ticket’s own `gitBranch` | `builderforce/task-2588` — a readable tree **here** is necessary to author this contract, **not** R-7 |
| Project 11 source-control row (measured 2026-09-16, exec 31808) | `githubRepoUrl = null`, `githubRepoOwner = null`, `githubRepoName = null`, `sourceControlRepoUrl = null` |

Agent `projects.update` **cannot** set those GitHub fields. Layer A is an operator / project-settings action, not a product-repo commit.

### D-1 — Topology: three surfaces, not one

Measured this run (exec 31808) against live project/ticket/execution state:

| Surface | Who | What tools do today |
|---|---|---|
| **S1 — Durable executor** (this run, BA, PM) | Cloud/durable agent on `seanhogg/builderforce.ai` | `list_files` / `read_file` succeed on **main** (R-3 inventory reconfirmed below). `search_code` can still 403 on GitHub API rate-limit **independently of bind**. A readable S1 is **not** R-7. |
| **S2 — #2527 developer container** (Bob, executor=`container`) | Ticket #2527, `gitBranch=builderforce/task-2527` | `write_file` succeeds as **disconnected blobs**. `list_files` / `read_file` / `git_*` fail with `no repository bound` / `credential delegation absent`. Two paused container runs (exec 31643, 31644) already dumped product-shaped files without a working tree. |
| **S3 — Project 11 source-control connection** | BuilderForce.AI project record | All GitHub URL/owner/name fields **null**. This is the platform-level reason container tools fail. |

**Ghost bind:** ticket metadata can name `seanhogg/builderforce.ai` @ `builderforce/task-2527` while S2 tools still disagree. Treat tool results as truth, not the ticket’s `gitRepo` / `gitBranch` labels.

**Write-without-read:** `write_file` talks to a blob/PR path that does **not** share a working tree with `list_files` / `read_file` / `git_*`. Proven by #2527 dumps (see D-9). Binding is restoring **one** working tree, not enabling more blind writes.

### D-2 — Root-cause layers (restore in order)

| Layer | Failure string | What must be true |
|---|---|---|
| **A — Project GitHub connection** | `no repository bound` at project scope | Project 11 connected to `SeanHogg/Builderforce.ai`. After connect, `githubRepoUrl` / `githubRepoOwner` / `githubRepoName` are non-null and agree. |
| **B — Container working-tree checkout** | `no repository bound` inside the **#2527** container | That container has a local working copy of the repo **on `builderforce/task-2527`**. `list_files` enumerates it; `read_file` returns contents; `search_code` hits the same tree; `write_file` edits **that** tree. |
| **C — Git credential delegation** | `credential delegation absent` | `git_*` (status, diff, log, branch, commit, push as already authorized for the task) succeed **in the same container** against the same checkout. |

Layer A is **shared** with sibling unblocks (#2590 for #2545, #2591 for #2555, #2600 for #2554, #2639 for #2562). Connecting project 11 once is the shared step. This ticket’s success remains specifically **#2527 Layers B+C**. Do not retarget #2588 to bind every sibling.

### D-3 — Fail-closed constraints

1. **No product files on `builderforce/task-2588`.** Do not add Advisor source, `advisorMode.ts`, `/advisors`, `MARKETPLACE_LISTING_KINDS` value `advisor`, `PEOPLE_OBJECT_KINDS` value `advisory`, i18n keys, or a substitute tree. Operator notes live **only** in this PRD.
2. **Do not force-push.** No force-push to `main`. No merge of #2527’s dump commits from this ticket.
3. **Do not re-dispatch #2527** until R-7 is proven on S2. Premature dispatch burns the producer and adds more dumps (already happened: exec 31643/31644).
4. **Do not invent an in-repo bind subsystem** (scripts, `.builderforce` config, dummy git remotes). Binding is a platform/container action.
5. **Do not bind a different repo or branch.** Not `builderforce/task-2588`, not `builderforce/task-2522`, not another GitHub origin.
6. **Do not truncate or “re-implement” R-3 files** as a side effect of binding. A0 Book path is already on main.
7. If Layer A cannot be set by the assigned developer (no project-settings permission), **escalate to a human operator**. Do not substitute dumps. Fail closed.

### D-4 — Operator bind sequence (developer executes; architect does not)

1. **Layer A.** In project 11 settings, connect GitHub to `SeanHogg/Builderforce.ai` (`https://github.com/SeanHogg/Builderforce.ai`). Confirm owner/name agree. This also unblocks Layer A for the sibling class; it does **not** by itself satisfy R-7.
2. **Layer B.** On ticket **#2527** (not this ticket), ensure the developer container checks out `builderforce/task-2527` as a **local working copy** of that origin. Re-bind if the container reports the repo bound but `list_files` still fails (ghost bind).
3. **Layer C.** Restore git credential delegation into that same container so `git status` / `git diff` / `git log` / `git branch` (and commit/push as already authorized) do not fail with `credential delegation absent`.
4. **Durability (R-4).** The bind must survive the **next** #2527 developer-container run without a second manual bind: same repo, same branch, credentials still present.
5. **Prove R-7 on a #2527 run** (D-5). Then leave this ticket. Do not open a product PR from #2588.

### D-5 — Proof matrix (R-7) — must run on S2

A developer-container run **assigned to #2527** can:

| # | Check | Pass |
|---|---|---|
| 1 | `list_files` at `frontend/src/app/talent` | Sees `TalentDetailClient.tsx` (not `no repository bound`) |
| 2 | `read_file` `frontend/src/app/talent/[id]/TalentDetailClient.tsx` | Existing source; `BookAdvisorPanel` import present (do not overwrite) |
| 3 | `search_code` for `PEOPLE_OBJECT_KINDS` | Hits `packages/creation-canvas-contract/src/people.ts` |
| 4 | `git_*` on `builderforce/task-2527` | No `no repository bound`, no `credential delegation absent` |
| 5 | Trivial in-place `write_file` then revert | Change appears in `git diff` on **that** branch — write and git share one tree |
| 6 | Open a real PR from `builderforce/task-2527` | That PR is **#2527 product work**, not this ticket’s |

This ticket is done when those six are true for **#2527’s container**, **or** when an operator records that #2527’s container already has a readable checkout. A readable #2588 / main checkout (S1) is **not** sufficient.

**QA / Validator:** prove R-7 against a **#2527** developer run, not against this ticket’s checkout alone. Distinguish GitHub **403 rate-limit** (API throttled; local `list_files`/`read_file` may still work — observed this run on S1) from unbound / missing delegation.

### D-6 — Failure-mode dictionary

| Observation | Layer | Action |
|---|---|---|
| `no repository bound` on `list_files` / `read_file` in #2527 container | A and/or B | Connect project 11; checkout `builderforce/task-2527` in that container |
| `credential delegation absent` on `git_*` | C | Restore git credentials into the same container; do not “fix” by skipping git |
| `write_file` works, `list_files` fails | Ghost bind / write-without-read | Re-bind working tree; **stop writing** until read works |
| `search_code` 403 / GitHub rate limit while `list_files` works | Not unbound | Wait / retry; do not treat as bind failure; do not dump files |
| Ticket `gitRepo` set but tools fail | Ghost bind | Tool results win |
| #2588 checkout readable | S1 only | Irrelevant to R-7 |

### D-7 — Role split (this ticket)

| Role | Owns | Does not |
|---|---|---|
| **Architect (this section)** | Bind topology, layers A/B/C, fail-closed, proof matrix, post-bind first actions for #2527 | Product design; executing the bind; a product PR |
| **Developer (#2588)** | Execute D-4 if they have operator/container access; otherwise escalate; keep `builderforce/task-2588` **empty of dumps** | Advisor product files; committing to this branch except to delete dead ops docs |
| **QA / Validator** | Prove D-5 / R-7 on a **#2527** run | Treating S1 as proof |
| **Product Manager / BA** | Scope firewall R-1–R-10 (already locked) | Bind mechanics |

### D-8 — R-3 inventory reconfirmed on S1 (exec 31808, `ref: main`)

These paths exist on main and **must be readable on S2 before any #2527 edit**. Blind `write_file` of these paths is forbidden until the file has been read. This ticket must not truncate them.

| Concern | Path | This run |
|---|---|---|
| Talent detail (Book + Message CTA) | `frontend/src/app/talent/[id]/TalentDetailClient.tsx` | Present; imports `BookAdvisorPanel`, `listTalentBookingServices`, `talentPrimaryAction` |
| Talent listing | `frontend/src/app/talent/page.tsx` | Present (listed under `frontend/src/app/talent/`) |
| Talent profile view | `frontend/src/components/freelance/TalentProfileView.tsx` | Present |
| Book panel (A0, already on main) | `frontend/src/components/freelance/BookAdvisorPanel.tsx` | Present |
| Booking client | `frontend/src/app/book/[token]/BookingClient.tsx` | Present (do not re-implement) |
| Talent ↔ booking_services | `frontend/src/lib/freelance/booking.ts` | Present (`booking.test.ts` sibling exists) |
| Marketplace storefront | `frontend/src/app/marketplace/MarketplacePageClient.tsx` | Present (PM inventory; do not overwrite) |
| Marketplace families | `frontend/src/lib/marketplaceFamilies.ts` | Present; `FamilyId = 'talent' \| 'company' \| 'agent' \| 'asset'` — **do not add a fifth family** |
| Listing kinds | `packages/creation-canvas-contract/src/marketplaceListings.ts` | Present — **do not add `advisor`** |
| `PEOPLE_OBJECT_KINDS` | `packages/creation-canvas-contract/src/people.ts` | Present; HR kinds only (`employee`, `orgChart`, `headcountPlan`, `compBand`, …) — **do not add `advisory` / `advisor`** |
| Object vocabulary | `packages/creation-canvas-contract/src/objectKinds.ts` | Present |
| Advisor product spec | `specs/builderforce/26-prd-advisor-platform.md` | Present on main |
| Implementation notes | `specs/builderforce/26-implementation-notes-advisor-platform.md` | **Absent on main** (listing of `specs/builderforce/` has the PRD only). Do **not** invent it on this branch. #2593 owns landing notes if they land at all. |
| i18n | `frontend/src/i18n/messages/{en,de,es,fr,zh}.json` | All five locales present |

**A0 is already on main.** After bind, **#2527 must not re-implement** `BookAdvisorPanel`, `talentPrimaryAction`, `listTalentBookingServices`, or `reserveTalentSession`. Remaining P0 is advisor **mode/fields** on the existing talent listing plus a **talent-family filter** for `category=advisors` (R-9 — **do not implement on #2588**).

### D-9 — First actions on #2527 after S2 is readable (not this ticket)

#2527 is **8 commits ahead of main, no PR**. Container file-change log (not to be copied here):

- `specs/tasks/task-2527.md` (created/modified under write-without-read)
- `Builderforce.ai/packages/creation-canvas-contract/src/advisorMode.ts` (**nested `Builderforce.ai/` prefix** — a dump, not a real package path)

**First action after bind (developer of #2527):**

1. **Delete those dumps.** Do not merge them. Do not keep `advisorMode.ts`.
2. **Read** every R-3 path (D-8) before editing.
3. Implement remaining P0 against the **real** tree: expertise / industries / languages / methods / stages / session length / price (0 = volunteer) / location / confidentiality default on the existing talent/freelancer profile; five locales; both themes; `LEGACY_CATEGORY` row `advisors → { family: 'talent', kind: 'person' }` (or equivalent query filter). Authenticated setup is a Canvas destination, not new chrome.
4. **Do not** add `/advisors`, a fifth `FamilyId`, `advisor` listing kind, or `advisory` people kind.

### D-10 — This ticket’s product-repo footprint

#2588 file-change log is **only** `specs/tasks/task-2588.md` (created then **deleted** by PM). Keep it that way. Do not open a product PR from #2588. The only allowed git change on `builderforce/task-2588` is removing dead environment-ops docs so the branch stays empty of Advisor source.

### D-11 — What this architect run cannot do (and must not fake)

- Cannot set project 11 `githubRepoUrl` via agent tools.
- Cannot operate the #2527 developer container from this durable executor.
- Cannot satisfy R-7 from S1.
- Will **not** write product files, bind scripts, or conceptual dumps onto `builderforce/task-2588` as a stand-in.

The architect record for lane `ready` is **this Design**. Developer executes D-4. QA proves D-5 on **#2527**.

_— authored by Architect / CTO · 2026-09-16 · execution #31808_

_— rewritten by CTO · 2026-09-16T16:13:21.777Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:34:03.655Z · execution #31657

BA checkout on #2588 is `ref: main` and **is readable** (`list_files` / `read_file` succeed). That does **not** satisfy R-7: success is a #2527 developer-container run on `builderforce/task-2527`. #2588 has `gitBranch=null`. A0-shaped Advisor code is already on main (`BookAdvisorPanel`, `frontend/src/lib/freelance/booking.ts`, TalentDetailClient Book CTA) — do not overwrite. `specs/builderforce/26-implementation-notes-advisor-platform.md` is absent on main; only `26-prd-advisor-platform.md` is present. Removed stray `specs/tasks/task-2588.md` so this ticket does not ship product-repo dead code; Requirements live in this PRD.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T15:48:58.433Z · execution #31712

**Product Manager (todo lane) — 2026-09-16.** Scope locked as an environment unblock for **#2527**, not a product change.

This run’s checkout of `seanhogg/builderforce.ai` is readable (`list_files` / `read_file` succeed). R-3 paths all exist on main; A0 Book path is already shipped (`BookAdvisorPanel`, `frontend/src/lib/freelance/booking.ts`, TalentDetailClient Book CTA). `FamilyId` is `talent | company | agent | asset`; talent kinds `person`, `gig`; `LEGACY_CATEGORY` has no `advisors` row; i18n locales `en de es fr zh`. `specs/builderforce/26-implementation-notes-advisor-platform.md` is still absent on main.

**gitBranch correction:** #2588 is `builderforce/task-2588` (not `null`). Readable #2588 ≠ R-7. Success remains a #2527 container on `builderforce/task-2527`. Project 11 `githubRepoUrl` is still `null`.

**This ticket’s repo footprint:** deleted dead `specs/tasks/task-2588.md` (R-6: operator notes live only on this PRD). Do not ship Advisor source, `advisorMode.ts`, or a substitute tree on `builderforce/task-2588`.

**#2527 dumps to drop after bind (not here):** branch is 8 commits ahead of main with no PR — `specs/tasks/task-2527.md` and `advisorMode.ts`. Reconcile by deleting, then read R-3, then implement remaining P0 against the real files. Do not re-implement A0.

Architect owns Design (bind + credential delegation). Developer executes the bind. QA proves R-7 on a **#2527** run.

---

### Update — CTO · 2026-09-16T16:13:59.406Z · execution #31808

**Architect (ready lane) — 2026-09-16 · execution #31808.** Design authored on this ticket PRD (section `Design`). Contract: three surfaces (S1 durable checkout ≠ R-7; S2 #2527 container is the success target; S3 project 11 `githubRepoUrl=null`). Restore Layers A (project GitHub connect — agent tools cannot set it), B (local checkout of `builderforce/task-2527` in the #2527 container), C (git credential delegation). Fail closed: no product files on `builderforce/task-2588`; do not re-dispatch #2527 until R-7; do not merge dump commits; first post-bind action on #2527 is delete `specs/tasks/task-2527.md` and nested `Builderforce.ai/.../advisorMode.ts`, then read R-3. R-3 reconfirmed on S1 this run (TalentDetailClient Book CTA, `FamilyId` four values, `PEOPLE_OBJECT_KINDS` HR-only, `26-prd-advisor-platform.md` present, implementation-notes absent). No product-repo patch from this architect slot. Developer executes D-4; QA proves D-5 on a **#2527** run.