> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2618
> _Each agent that updates this PRD signs its change below._

# PRD: Drop docs-only `specs/tasks/task-2566.md` (not R5)

## Problem & Goal

Branch `builderforce/task-2566` contains a single docs-only commit that adds `specs/tasks/task-2566.md`. That file is the same anti-pattern as `task-2573.md`: a markdown spec is not applying migrations **1177** / **1178** on core.

A BA run on PR **#2566** (execution **31719**) attempted to remove the file and failed with GitHub **403** (rate limit). The file must be deleted **before any merge of #2566**.

**Goal:** Remove `specs/tasks/task-2566.md` from the branch/PR so #2566 cannot merge a docs-only stand-in for R5. R5 complete stays defined as: core `_migrations` listing `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql`, plus the column/table probes already covered by **#2573**.

## Target users / ICP roles

- Downstream agents sharing this WIP PRD for #2566
- Core maintainers reviewing `builderforce/task-2566`
- Release / R5 gate owners who must not treat a markdown task file as migration application

## Scope

In scope:

- Delete `specs/tasks/task-2566.md` on `builderforce/task-2566` (or equivalent PR #2566 working tree) before merge
- Confirm the PR no longer adds that path
- Leave R5 completion criteria unchanged: migrations **1177** and **1178** present in core `_migrations`, plus #2573 column/table probes
- Retry the delete if a prior automated attempt hit GitHub 403 rate limit

Out of scope is listed below.

## Functional requirements

1. **Delete the docs-only file.** `specs/tasks/task-2566.md` must not exist on the branch that would merge as #2566.
2. **No substitute docs-only R5.** Do not replace the file with another markdown task/spec that claims to apply 1177/1178.
3. **Do not implement 1177/1178 in this change.** Applying those migrations is not this task; they are already the R5 bar on core (and probes live in #2573).
4. **Merge gate.** #2566 must not be merged while `specs/tasks/task-2566.md` is still present.
5. **Rate-limit recovery.** If GitHub returns 403/rate limit, wait and retry the delete; do not leave the file in place as a workaround.
6. **Preserve R5 definition.** After the delete, R5 complete remains: core `_migrations` lists `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql`, plus column/table probes from #2573.

## Acceptance criteria

- [ ] `specs/tasks/task-2566.md` is absent from `builderforce/task-2566` / PR #2566
- [ ] `git log` / PR diff for #2566 does not introduce that path (docs-only add is gone)
- [ ] No new `specs/tasks/*.md` is added as a stand-in for applying 1177/1178
- [ ] Core `_migrations` still expected to list `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql` for R5 (not delivered by this docs delete)
- [ ] Column/table probes remain owned by #2573; this work does not reopen or duplicate them
- [ ] #2566 is not merged until the file is deleted
- [ ] A 403/rate-limit on delete is treated as transient; the file is still removed before merge

## Out of scope

- Authoring or applying `1177_coordination_requires_manager.sql` or `1178_brain_chat_diagnostics.sql`
- Column/table probes and related work on #2573
- Deleting or rewriting `task-2573.md` except as historical anti-pattern reference
- Changing R5 completion criteria
- Unrelated files, migrations, or product behavior on the branch
- Permanent workarounds that leave the markdown file merged because of GitHub rate limits

## Requirements

This section is the backlog contract for ticket **#2618** (gap child of **#2566**, epic **#2562** / PRD 27 **R5**). It binds the **docs-only stand-in delete**. It does **not** apply migrations, change the R5 complete definition, or reopen **#2573** probes.

**This BA slot is a requirements contract, not a repo change.** Authoring this section, signing the backlog BA slot, or writing another `specs/tasks/*.md` does **not** delete the file and does **not** mean R5 landed.

### Relationship to sibling tickets (do not collapse)

| Ticket | Role |
| --- | --- |
| **#2566** (parent) | PRD 27 R5 **acceptance contract**: 1177 then 1178 on **core** primary (`NEON_DATABASE_URL`), ledger `_migrations`, already-applied = no-op success. Parent **R11** already forbids shipping `specs/tasks/task-2566.md`. |
| **#2573** | PRD 27 R5 **execution gap**: the human operator applies the same two files and runs column/table probes. Physical apply happens **once**. |
| **#2618** (this ticket) | **Delete-only gap:** remove `specs/tasks/task-2566.md` from branch `builderforce/task-2566` (and any PR that would merge that branch) **before merge**. Same anti-pattern as `task-2573.md`: markdown is not applying 1177/1178. |
| **#2567 / #2574 / #2597** | R6 live re-test. Still blocked until the R5 complete signal (ledger + probes), **not** until this file is gone. |

A successful delete on **#2618** does **not** record R5 complete. A successful apply on **#2566 / #2573** does **not** excuse leaving the markdown file in a mergeable PR.

### User stories

1. As a **core maintainer** reviewing `builderforce/task-2566`, I must not be asked to merge a docs-only `specs/tasks/task-2566.md` as if it applied 1177/1178.
2. As the **R5 gate owner**, I need R5 complete to stay: core `_migrations` lists `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql`, plus **#2573** column/table probes — never “a markdown task file exists.”
3. As a **downstream agent** on #2566, I must treat GitHub **403 / rate limit** as transient and still delete the file before merge, not leave it as a workaround.
4. As a **BA / cloud executor** without GitHub write quota or a populated checkout, I must **not** mark the **developer** slot complete, invent a substitute markdown file, or claim R5 applied.

### Current behaviour (baseline)

- Branch `builderforce/task-2566` is recorded on parent **#2566**. Ticket **#2566** currently has **no** `githubPrNumber` / `githubPrUrl`. The PM draft’s “PR #2566” means **that branch / whatever PR would merge it**, not a confirmed GitHub pull-request number. Do not invent a PR number.
- That branch has (or had) **one docs-only commit** adding `specs/tasks/task-2566.md`. That path is the defect.
- BA run on **#2566** (execution **31719**) attempted the delete and failed with GitHub **403** (rate limit). The file must still be removed before any merge of that branch.
- This **#2618** BA executor bound **seanhogg/builderforce.ai** with **0 files** (`list_files` → GitHub 403; project 11 `githubRepoUrl` is null, same class as **#2588 / #2545 / #2532 / #2600**). Physical delete is **not** possible on this BA pass.
- Parent **#2566** Requirements (R11 / AC-9) already: forbidden as R5 = `specs/tasks/task-2566.md` (or `task-2573.md`), runbook-only PRs, and BA/architect prose without ledger proof.
- R5 artefacts on `main` (`a0eef9439` baseline used by #2566): `api/migrations/1177_coordination_requires_manager.sql`, `api/migrations/1178_brain_chat_diagnostics.sql`, runner `api/scripts/migrate.mjs` primary track. **Unchanged by this ticket.**

### Functional requirements

**R-1 — Delete target.** `specs/tasks/task-2566.md` must **not exist** on branch `builderforce/task-2566`, nor in the tree of any GitHub PR that would merge that branch to `main`. Absence is the only in-scope success for this ticket.

**R-2 — Single-path change.** The developer change is **delete that one path** (or close/abandon the docs-only PR unmerged so `main` never gains it). No other product files, migrations, runner edits, probes, or version bumps.

**R-3 — No substitute docs-only R5.** Do **not** replace the file with `specs/tasks/task-2566.md`, `specs/tasks/task-2618.md`, `specs/tasks/task-2573.md`, or any other markdown/spec that claims 1177/1178 were applied. Ticket PRDs (this section, parent #2566 Requirements) remain the source of truth. Do not dump a `specs/tasks/` file from this BA pass.

**R-4 — Not an apply.** This ticket does **not** author, edit, squash, reverse, or run `1177_coordination_requires_manager.sql` or `1178_brain_chat_diagnostics.sql`. Ad-hoc SQL, `migrate.mjs`, and Neon writes are **out of scope**.

**R-5 — R5 definition unchanged.** After the delete, R5 complete remains exactly:

1. Core `_migrations` lists `1177_coordination_requires_manager.sql` **and** `1178_brain_chat_diagnostics.sql`;
2. Column/table probes owned by **#2573** (`project_manager_configs.coordination_requires_manager` boolean NOT NULL DEFAULT false; relation `brain_chat_diagnostics` plus 1178 indexes; read-only probes do not error).

This work does **not** deliver (1) or (2) and must not be reported as R5 complete.

**R-6 — Merge gate.** Branch `builderforce/task-2566` / any PR from it **must not merge** while `specs/tasks/task-2566.md` is still present. Acceptable resolutions, in order:

1. Delete the file on that branch and push (preferred if the branch will still merge for a non-docs reason — there should be **no** remaining docs-only reason to merge).
2. Close / do not merge a PR whose only commit is the docs-only add, so `main` never receives the path.

Merging then reverting is **not** the happy path.

**R-7 — Rate-limit recovery.** GitHub **403 / API rate limit** on delete is **transient**. Wait and retry. Do **not** leave the file in place as a workaround, do **not** “complete” the developer slot on 403, and do **not** treat a failed `delete_file` / empty checkout as success.

**R-8 — Who deletes.** A **developer** (or operator) with GitHub write on `builderforce/task-2566` when quota allows. BA, architect, and this no-shell executor **cannot** apply the delete on an empty/403 checkout. BA sign-off of **this** slot means the contract is written.

**R-9 — Idempotency.** If `specs/tasks/task-2566.md` is already absent from the branch and from `main`, this ticket is **no-op success**. Do **not** recreate the file to have something to delete.

**R-10 — Success signal (this ticket only).** All of:

- Path `specs/tasks/task-2566.md` is absent from `builderforce/task-2566` and from any open PR that would merge it;
- Diff / `git log` for that PR no longer introduces that path (docs-only add is gone or the PR is closed unmerged);
- No new `specs/tasks/*.md` was added as a stand-in.

**Not** part of this signal: `_migrations` rows, probe SQL, worker 2026.9.37 deploy, or R6 live re-test.

**R-11 — Anti-pattern named.** `task-2566.md` is the same class as `task-2573.md`: a markdown task file is **not** applying 1177/1178 on core. Do not score presence of the file as R5 progress. Do not rewrite `task-2573.md` except as this historical reference.

**R-12 — Bound checkout constraint (this run).** Physical delete is **blocked on this BA pass**: bound repo **seanhogg/builderforce.ai** listed 0 files; `list_files` returned GitHub 403 rate limit (user ID 15883045). Developer on **#2618** (or parent **#2566**) retries the delete when GitHub writes succeed. Do not invent a conceptual file in an unrelated tree.

**R-13 — Do not reopen #2573.** Column/table probes, operator runbook, `NEON_DATABASE_URL`, and abort/idempotency rules stay on **#2566 / #2573**. This gap does not duplicate them.

**R-14 — Slot meaning.** BA sign-off on lane `backlog` = **this contract is written**. It does **not** mean the file is gone and does **not** mean migrations landed. Developer sign-off still requires R-10 proof (path absent). Operator/developer sign-off on **#2566 / #2573** still requires ledger + probes.

### Data contract (verification)

| Check | Source of truth |
| --- | --- |
| File gone | `specs/tasks/task-2566.md` absent on `builderforce/task-2566` and on `main` |
| No stand-in | No new `specs/tasks/*.md` claiming 1177/1178 applied |
| Merge blocked while present | Branch/PR must not merge with that path |
| R5 complete (unchanged; **not** this ticket) | `_migrations` + #2573 probes per parent #2566 R7/R8 |
| 403 on delete | Retry; not success |

### Non-goals (this ticket)

- Authoring or applying 1177/1178; any other migration ID; transactional/apps tracks.
- Column/table probes and operator apply (**#2573**).
- Changing R5 completion criteria or parent #2566 R1–R11.
- Deleting or rewriting `task-2573.md` except as anti-pattern reference.
- Product behaviour, worker 2026.9.37 deploy, R6 re-test, R7 DONE.md / release notes.
- Unrelated files on the branch.
- Permanent workarounds that leave the markdown file merged because of GitHub rate limits.
- Shipping `specs/tasks/task-2618.md` from this BA lane.

### Acceptance criteria (testable)

1. `specs/tasks/task-2566.md` is absent from `builderforce/task-2566` / any PR that would merge it.
2. That PR’s diff does not introduce the path, **or** the docs-only PR is closed unmerged so `main` never gains it.
3. No new `specs/tasks/*.md` is added as a stand-in for applying 1177/1178 (including `task-2618.md`).
4. Core `_migrations` is still **expected** (by #2566/#2573, not by this delete) to list `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql`.
5. Column/table probes remain owned by **#2573**; this work does not reopen or duplicate them.
6. The branch is not merged while the file is still present.
7. A 403/rate-limit on delete is treated as transient; the file is still removed before merge.
8. BA sign-off on this backlog slot means **this contract is written**. It does **not** mean the file is deleted and does **not** mean R5 applied.

### Open items for Design / Developer (not BA)

- Exact GitHub write path once rate limit clears: `delete_file` / git rm on `builderforce/task-2566` vs close-unmerged.
- Confirm whether a GitHub PR number exists (parent #2566 currently has none recorded).

_— authored by Ada (business-analyst) · #2618 backlog · 2026-09-16_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:57:42.339Z_

## Design

This Design is the **ready-lane architect contract** for gap **#2618**. It binds **how** `specs/tasks/task-2566.md` is removed from parent branch `builderforce/task-2566` so that path never reaches `main`. It does **not** apply migrations **1177** / **1178**, does **not** change the R5 complete definition, and does **not** reopen **#2573** probes.

Frozen product contract remains BA **R-1–R-14** (PM lock exec **31798**, PO ready exec **31853**). Architect sign-off of this section ≠ file gone ≠ R5 complete.

### D-0 — Nature of the change

| Field | Value |
| --- | --- |
| Kind | **Delete-only** (docs anti-pattern removal) |
| Product-repo patch on this checkout | **None** |
| Migrations / Neon / `migrate.mjs` | **Out of scope** (R-4; owned by **#2566 / #2573**) |
| New `specs/tasks/*.md` | **Forbidden** (R-3) |
| Architect deliverable | **This Design on the ticket PRD** |
| Developer deliverable | Physical delete on **parent** branch `builderforce/task-2566` (or leave that branch unmerged / no PR) |

A markdown task file is **not** applying `1177_coordination_requires_manager.sql` or `1178_brain_chat_diagnostics.sql` on core. Same class as `task-2573.md` (R-11). Do not score presence of the file as R5 progress.

### D-1 — Delete target (only in-scope path)

| Field | Value |
| --- | --- |
| Path | `specs/tasks/task-2566.md` |
| Tree | Branch **`builderforce/task-2566`** (parent ticket **#2566** `gitBranch`) |
| Not the target | `main`; this ticket branch `builderforce/task-2618`; any other path |

Success for **#2618** is **absence** of that one path on the parent branch **and** on `main` (R-1, R-10). Nothing else.

### D-2 — This checkout is not the delete target

| Observation (this architect run, exec **31927**) | Consequence |
| --- | --- |
| Bound repo **seanhogg/builderforce.ai**; project 11 `githubRepoUrl` still **null** | Do not invent a second remote or a conceptual file in an unrelated tree |
| Ticket `gitBranch` = `builderforce/task-2618` | Developer operates on **parent** `builderforce/task-2566`, not here |
| `list_files` glob `specs/tasks/**` and `**/task-2566.md` on **main** → **0 matches**; `specs/` holds `specs/builderforce/` only | Recreating `task-2566.md` here to delete it would violate **R-3 / R-9** |
| Forbidden dump `specs/tasks/task-2618.md` was still on `builderforce/task-2618` (prompt “Files already on this branch”) | Deleted this run (commit `f90762c358c2a285968dab9d7f35fdd2d54b0386`). Keep it gone. Do not replace it. |

### D-3 — Resolution order (locked)

Execute **in this order**. Merging then reverting is **forbidden**.

1. **Preferred.** Developer with GitHub write quota on `builderforce/task-2566` runs `delete_file` / `git rm specs/tasks/task-2566.md` for **that one path** and pushes. **Do not open a GitHub PR** unless a non-docs change remains (none should for this gap).
2. **Fallback (403 persists).** **Do not merge** the branch and **do not open a PR** from it. Unmerged + no PR = `main` never gains the path (R-6.2 / AC-2).
3. **Forbidden.** Merge then revert; “complete” the developer slot on GitHub **403**; leave the file as a rate-limit workaround.

### D-4 — No GitHub PR number

Parent **#2566** has `githubPrNumber` / `githubPrUrl` = **null**. Platform reason recorded by PM: `builderforce/task-2566` is ahead of `main` and **no pull request was ever opened**. Do **not** invent a PR number. Do **not** open a PR from `builderforce/task-2566` while the docs-only add is the reason to merge. Do **not** open a product feature PR from `builderforce/task-2618` for this architect slot.

This run observed parent **#2566** `pendingChanges.aheadCount` = **5** (originally described as 1 docs-only commit). That growth is **not** a license to merge, apply 1177/1178, or expand #2618. Developer still deletes **only** `specs/tasks/task-2566.md` on that branch (R-2). Other commits on the parent, if any, belong to **#2566 / #2573**, not this gap.

### D-5 — Single-path change (R-2)

In scope on `builderforce/task-2566`:

- Delete `specs/tasks/task-2566.md` **or** leave the branch unmerged with no PR.

Out of scope on every tree (this checkout, parent branch, `main`):

- Author, edit, squash, reverse, or run `1177_coordination_requires_manager.sql` / `1178_brain_chat_diagnostics.sql`
- `api/scripts/migrate.mjs`, ad-hoc SQL, Neon writes, `NEON_DATABASE_URL`
- Column/table probes (`project_manager_configs.coordination_requires_manager`, relation `brain_chat_diagnostics`, 1178 indexes) — **#2573**
- Version bumps, worker 2026.9.37, R6 live re-test, R7 DONE.md / release notes
- Any other product file

### D-6 — Forbidden artifacts (R-3)

Do **not** add or restore:

- `specs/tasks/task-2566.md`
- `specs/tasks/task-2618.md`
- `specs/tasks/task-2573.md`
- Any other `specs/tasks/*.md` that claims 1177/1178 were applied

Source of truth remains the **ticket PRD** (this Design + parent **#2566** Requirements). Ticket PRDs are not git dumps under `specs/tasks/`.

### D-7 — R5 definition unchanged (R-5)

After the delete, R5 complete remains **exactly**:

1. Core `_migrations` lists `1177_coordination_requires_manager.sql` **and** `1178_brain_chat_diagnostics.sql`;
2. Column/table probes owned by **#2573** (`project_manager_configs.coordination_requires_manager` boolean NOT NULL DEFAULT false; relation `brain_chat_diagnostics` plus 1178 indexes; read-only probes do not error).

This Design does **not** deliver (1) or (2). A successful delete on **#2618** does **not** record R5 complete. A successful apply on **#2566 / #2573** does **not** excuse leaving the markdown file in a mergeable branch.

### D-8 — Rate-limit recovery (R-7)

GitHub **403 / API rate limit** on delete or search is **transient**. Wait and retry. Do **not** leave the file in place, do **not** mark the developer slot complete on 403, and do **not** treat a failed `delete_file` / empty checkout as success.

This architect run: `search_code` for `task-2566.md` / `task-2618.md` returned GitHub **403** (user ID 15883045). `list_files` on `specs/` succeeded and showed **no** `specs/tasks/` tree on `main`. 403 is recorded as transient, not as the parent-branch file having been removed.

### D-9 — Idempotency (R-9)

If `specs/tasks/task-2566.md` is already absent from `builderforce/task-2566` **and** from `main`, this ticket is **no-op success**. Do **not** recreate the file to have something to delete.

`main` is already a no-op for the target path. Parent branch is **not** proven absent until the developer (or operator) inspects `builderforce/task-2566` when GitHub reads succeed.

### D-10 — Who does what

| Role | Action |
| --- | --- |
| Architect (this slot) | Author this Design. No product-src. Keep `specs/tasks/task-2618.md` absent on `builderforce/task-2618`. |
| Developer (**#2618** in_progress) | `git rm` / `delete_file` **only** `specs/tasks/task-2566.md` on **`builderforce/task-2566`** when writes succeed; **or** leave that branch unmerged / no PR. Sign-off requires R-10 proof (path absent, no stand-in). |
| Operator / developer on **#2566 / #2573** | Ledger + probes. Unchanged. Not this ticket. |
| This no-shell executor | **Cannot** apply the parent-branch delete (wrong branch + 403). Design sign-off ≠ file gone. |

### D-11 — Verification (developer / QA — not this slot)

| Check | Pass |
| --- | --- |
| Target gone | `specs/tasks/task-2566.md` absent on `builderforce/task-2566` |
| `main` clean | Path absent on `main` (already true this run) |
| No stand-in | No new `specs/tasks/*.md` claiming 1177/1178 applied (including `task-2618.md`) |
| Merge gate | Parent branch **not** merged; **no** GitHub PR opened for the docs-only add |
| Not R5 | Do **not** assert `_migrations` rows or probe SQL as this ticket’s proof |

### D-12 — Slot meaning

Architect sign-off on lane `ready` = **this Design is written**. It does **not** mean `specs/tasks/task-2566.md` is gone from `builderforce/task-2566` and does **not** mean R5 applied.

_— authored by CTO (architect) · #2618 ready · 2026-09-16 · execution 31927_

_— rewritten by CTO · 2026-09-16T16:31:29.961Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:58:10.857Z · execution #31760

**BA decision (Ada, #2618 backlog):** Requirements R-1–R-14 signed. This ticket is delete-only of `specs/tasks/task-2566.md` from `builderforce/task-2566` before merge — not R5 apply. Physical apply + probes stay #2573; R5 complete definition unchanged (parent #2566 R7/R8). This BA pass cannot delete: bound seanhogg/builderforce.ai is empty and GitHub 403 rate-limited (same class as #2600). Do not ship `specs/tasks/task-2618.md`. BA sign-off ≠ file gone. Developer retries delete when GitHub writes succeed; close-unmerged is acceptable if that is the only commit.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:07:31.911Z · execution #31798

**Product lock (Kevin BA/PM/PO · #2618 todo · execution 31798 · 2026-09-16):** BA R-1–R-14 are accepted as-is. This ticket is delete-only of `specs/tasks/task-2566.md` — not R5 apply, not #2573 probes, not a product-repo patch.

**Closed open items:**
- **No GitHub PR exists.** Parent #2566 has `githubPrNumber`/`githubPrUrl` = null. Platform reason: `builderforce/task-2566` is 1 commit ahead of `main` and no pull request was ever opened. Diff is docs-only (`specs/tasks/task-2566.md` created then modified on execution 31719). Do not invent a PR number.
- **Resolution order (product):** (1) Developer `git rm` / `delete_file` that one path on `builderforce/task-2566` when GitHub writes succeed, and **do not open a PR** unless a non-docs change remains (none should). (2) If 403 persists: **do not merge** the branch and **do not open a PR** from it. Unmerged + no PR = `main` never gains the path (AC-2 / R-6.2). Merging then reverting is forbidden.
- **This #2618 checkout is not the delete target.** Ticket `gitBranch` is null. Bound `seanhogg/builderforce.ai` **main** already has no `specs/tasks/task-2566.md` (`search_code` 0 matches; `specs/tasks/` empty). Recreating it here to delete it would violate R-3/R-9. Developer operates on parent branch `builderforce/task-2566`.

**Out for this PM slot:** no `specs/tasks/task-2618.md`, no 1177/1178 authoring, no Design (architect: single-path delete or abandon-unmerged). PM sign-off ≠ file gone ≠ R5 complete.

---

### Update — Product Manager · 2026-09-16T16:17:05.793Z · execution #31853

**PO ready (Kevin BA/PM/PO · #2618 ready · execution 31853 · 2026-09-16):** Scope + AC **approved**. Frozen contract = BA **R-1–R-14** (PM lock exec 31798). This ticket is **delete-only** of `specs/tasks/task-2566.md` on `builderforce/task-2566` before merge — **not** R5 apply, **not** #2573 probes, **not** a product-repo patch on this checkout.

**Inspected:** Bound `seanhogg/builderforce.ai` `main` — `search_code` 0 for `task-2566.md` and `task-2618.md`; `specs/tasks/` empty (R-9 no-op here). Parent #2566 still has `githubPrNumber`/`githubPrUrl` = null, branch `builderforce/task-2566` unmerged (ahead of main). Recreating either dump on this branch would violate R-3. Architect owns Design (single-path `git rm` or abandon-unmerged). Developer owns the physical delete on the parent branch. PO sign-off ≠ file gone ≠ R5 complete.

---

### Update — CTO · 2026-09-16T16:31:53.401Z · execution #31927

**Architect Design (CTO · #2618 ready · execution 31927 · 2026-09-16):** Design D-0–D-12 locked. Delete-only of `specs/tasks/task-2566.md` on parent `builderforce/task-2566` (preferred: `git rm` / `delete_file` that one path; fallback: leave unmerged / no PR). This checkout `builderforce/task-2618` is not the delete target — `main` already lacks the path (R-9). Recreating it here is forbidden (R-3). Forbidden dump `specs/tasks/task-2618.md` deleted this run (`f90762c`). No 1177/1178, no #2573 probes, no product-src. Parent #2566 still has no GitHub PR (`githubPrNumber` null; aheadCount observed 5 — still do not merge). GitHub 403 on `search_code` treated as transient. Architect sign-off ≠ file gone ≠ R5 complete. Developer owns the parent-branch delete.

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T17:01:59.546Z · execution #32039

## Implementation Notes — Developer run #2 (exec 32039)

**Observed (this run):**
- No repository bound to this execution — `git_status`, `list_files`, `run_command` all return "no repository bound / no repository checked out."
- Project 11 `githubRepoUrl` = null (operator-only bind gap; confirmed by `builtin_projects_get(11)`).
- GitHub 403 for user ID 15883045 persists (parent #2566 `pendingChanges.reason`).
- Preferred action (`git rm` / `delete_file` on `builderforce/task-2566`) is impossible from this no-shell environment.

**Fallback status (R-6.2 / AC-2 / D-3.2):**
- Parent #2566 has `githubPrNumber` = null, `githubPrUrl` = null.
- Branch state: `unmerged`, `aheadCount: 5`.
- **Result:** No PR exists. `main` cannot gain `specs/tasks/task-2566.md` through a merge of this branch.

**Constraints respected (this run):**
- R-3: No `specs/tasks/task-2618.md` or other stand-in created.
- R-4: No 1177/1178 migrations authored or applied.
- R-5: R5 definition unchanged (ledger + #2573 probes).
- R-9: Did not recreate the file to have something to delete.

**What remains:**
- Preferred: A developer/operator with a bound repository and GitHub write quota performs `git rm specs/tasks/task-2566.md` on branch `builderforce/task-2566`.
- Fallback (already satisfied): Branch remains unmerged with no PR; `main` never receives the path.

_— recorded by Developer (Bob) · exec 32039 · 2026-09-16T16:59_