> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2537
> _Each agent that updates this PRD signs its change below._

# PRD: Mirror to Advisor's Connected Calendar When Present

**Status:** WIP  
**Feature:** Mirror platform events to an advisor’s connected external calendar when a connection exists  
**Priority:** P1

## Problem & Goal

Advisors schedule and manage client work inside the product, but they live in Google Calendar, Outlook, or similar. When a platform event is created, updated, or cancelled, it often never appears on the advisor’s real calendar unless they copy it by hand. That causes missed meetings, double-booking, and extra admin.

**Goal:** When an advisor has a connected calendar, automatically mirror eligible platform events onto that calendar (create / update / cancel). When no calendar is connected, the product flow must still succeed and must not require a calendar. Mirroring is best-effort relative to the source of truth in the product.

## Target users / ICP roles

- **Advisor** — primary beneficiary; owns the connected calendar and needs events visible in their day-to-day calendar app.
- **Client / advisee** — books or participates in events that should land on the advisor’s calendar; does not need to know about mirroring.
- **Ops / admin** — may create or reassign events on behalf of advisors; needs mirroring to follow the assigned advisor.
- **Internal platform / integrations** — calendar connection, token refresh, and sync workers.

## Scope

In scope for this WIP:

- One-way mirror: **product event → advisor’s connected calendar** (not full two-way sync).
- Trigger on create, update, cancel/delete of eligible events assigned to an advisor.
- Honor “when present”: if the advisor has no connected calendar, skip mirroring and complete the product operation.
- Support already-connected Google Calendar and Microsoft 365 / Outlook calendars (existing OAuth connections).
- Map core event fields, keep a stable external event id, and keep product as source of truth.
- Idempotent writes; retries with backoff; non-blocking failure (product mutation succeeds even if mirror fails).
- Re-mirror or clean up when the assigned advisor changes, when a calendar is connected after events already exist, and when a calendar is disconnected.

## Functional requirements

### Eligibility

1. An event is eligible if it is an advisor-facing scheduled item (meeting, appointment, or blocked working session) with a start time, end time, and assigned advisor.
2. All-day and timed events are both eligible. Internal-only notes, audit logs, and client-private records that the advisor should not put on an external calendar are not eligible.
3. Mirroring is per assigned advisor. If an event has no assigned advisor, do not mirror.

### Connected calendar detection

4. “Connected calendar present” means the assigned advisor has a valid, non-revoked calendar integration with a writable target calendar selected (or a documented default, e.g. primary).
5. If no connection, expired/revoked tokens that cannot be refreshed, or no writable calendar, treat as **not present** and skip mirroring without failing the user-facing action.
6. Calendar presence is evaluated at mirror time, not only at event creation.

### Create / update / cancel

7. **Create:** After the product event is persisted, upsert a corresponding event on the advisor’s connected calendar.
8. **Update:** When title, description/notes (sanitized), start, end, timezone, location/conference link, or attendee-visible status changes, patch the remote event. Do not create duplicates.
9. **Cancel / delete:** When the product event is cancelled or deleted, delete or cancel the remote event if it exists. Missing remote events are not errors.
10. **Reassignment:** If the assigned advisor changes, remove (or cancel) the remote event from the previous advisor’s calendar if still connected, and create it on the new advisor’s calendar if connected.
11. **Late connect:** When an advisor connects a calendar, enqueue mirroring of that advisor’s upcoming eligible events (not historical events older than a defined lookback, default 1 day in the past).
12. **Disconnect / revoke:** Stop new mirrors. Best-effort delete previously mirrored events that the product created, or leave them with a documented policy; do not block disconnect.

### Field mapping and privacy

13. Remote event title and times must match the product event. Timezone must be preserved; do not silently convert to UTC in the advisor’s UI.
14. Description may include meeting purpose, join link, and client display name **only if** the advisor’s org settings allow client identifiers on external calendars. Default: include join link and generic title; omit sensitive account/financial detail.
15. Do not write secrets, internal IDs in the title, or data the client marked private.
16. Conference/join URL from the product, if any, is copied onto the remote event.
17. The product stores `external_calendar_id`, `external_event_id`, provider, and last successful sync timestamp on the event (or a mirror mapping table).

### Reliability and conflict behavior

18. Product CRUD is the source of truth. Calendar API failure must not roll back the product event. Surface a non-blocking warning to the advisor when a mirror fails persistently.
19. Writes are idempotent on `(event_id, advisor_id, provider, calendar_id)`.
20. Concurrent updates: last product write wins; use etag/if-match where the provider supports it; on 409/412, re-fetch and retry once.
21. If the remote event was deleted or edited by the advisor, the next product update recreates or overwrites the mirrored fields. Do not attempt to merge arbitrary advisor-side edits in this WIP.
22. Respect provider rate limits; queue per-advisor; retry with exponential backoff and a dead-letter after N failures.
23. Do not invite the client as a calendar attendee from the advisor’s personal calendar unless that is already an explicit product behavior. Default: advisor-only event (no extra invites that spam the client).

### Permissions and audit

24. Only use the advisor’s own OAuth grants. Never write to another user’s calendar.
25. Log mirror attempts (success/skip/fail) with event id, advisor id, provider, and reason. No payload secrets in logs.

## Acceptance criteria

1. Given an advisor with a valid connected writable calendar, creating an eligible event results in a matching event on that calendar within the sync SLA (target: &lt; 60 seconds under normal load).
2. Given no connected calendar (or revoked/unwritable), creating/updating/cancelling the same event succeeds in the product with no error requiring calendar setup; no remote write is attempted beyond a cheap presence check.
3. Updating time, title, location, or join link on the product event updates the existing remote event; a second remote event is not created.
4. Cancelling or deleting the product event removes or cancels the remote event when a mapping exists; repeating the cancel is a no-op.
5. Reassigning the event from Advisor A to Advisor B removes/cancels A’s remote copy if A is connected and creates B’s remote copy if B is connected.
6. Connecting a calendar backfills upcoming eligible events for that advisor and does not duplicate events that already have a mapping.
7. Disconnecting a calendar stops further mirrors; subsequent product actions still succeed.
8. Client-sensitive fields configured as excluded never appear in the remote description/title.
9. Calendar API 5xx / rate limit / transient auth-refresh failure leaves the product event intact, records a failed mirror, and retries; after exhausting retries the advisor can see a non-blocking sync warning.
10. Token refresh succeeds for a still-valid connection and mirroring continues without the advisor re-OAuthing.
11. All-day events and timed events in non-UTC timezones round-trip without date or hour shift on Google and Microsoft calendars.
12. Automated tests cover: connected create/update/cancel, skip-when-absent, reassignment, late connect backfill, idempotent retry, and mapping persistence.

## Out of scope

- Two-way sync (importing advisor-created calendar events into the product, or treating the external calendar as source of truth).
- Free/busy availability reads, booking-slot generation, or conflict blocking based on the external calendar (separate feature).
- New calendar providers beyond already-supported Google and Microsoft 365/Outlook.
- Mirroring to a client’s calendar, team shared mailboxes, or an org-wide resource calendar.
- Recurrence expansion edge cases beyond what the product already stores (no new RRULE engine).
- Full merge of advisor-side edits, attachments, or guest-list management on the provider event.
- Historical backfill of past events beyond the small lookback used for late connect.
- SMS/email notification redesign, video-meeting provider changes, or calendar-connect onboarding UX beyond a failure/warning state.
- Compliance-specific redaction packs per jurisdiction (use the single org setting in this WIP).

## Requirements

_Owned by the business-analyst. Grounded in repo state as of 2026-09-16. This ticket is the #2532 P0 bullet **“Mirror to advisor's connected calendar when present.”** Sibling write paths: #2534 (visitor → `meetings` row, advisor organizer) and #2536 (`kind = advisory`). Do not rebuild calendar OAuth, providers, or a mapping table._

### 0. Current-state inventory (reuse)

| Asset | Location | Already does |
|---|---|---|
| Connections | `calendar_connections` (`api/src/infrastructure/db/schema.ts`, migration 0292) | Per-user Google / Microsoft OAuth; `calendar_id` default `'primary'`; unique `(tenant_id, user_id, provider)` |
| Token refresh | `freshAccessToken` in `api/src/application/calendar/calendarService.ts` | Refresh-token grant; persists rotated tokens; returns `null` on failure |
| Presence | `getUserConnection(db, tenantId, userId)` | First matching connection; optional provider filter |
| Create mirror | `pushMeetingEvent(...)` | Returns `null` when no connection / no token / provider throw. Never fails the caller |
| Delete mirror | `deleteMeetingEvent(...)` | Best-effort; missing remote is not an error |
| Mapping | `meetings.calendar_provider`, `calendar_event_id`, `calendar_html_link` | Written after a successful push on `POST /meetings` |
| Providers | `api/src/application/calendar/calendarProviders.ts` | `createEvent`, `listUpcoming`, `deleteEvent`, `freeBusy`. **No `updateEvent`.** |
| OAuth | `api/src/presentation/routes/calendarRoutes.ts` | Connect / callback / disconnect. Disconnect deletes the connection row only |
| Today's caller | `meetingRoutes.ts` `POST /` | Pushes with `organizerId = userId` (authenticated creator) and other attendees' emails (`sendUpdates=all` on Google) |

A0 bind remains `booking_hosts.host_ref = talent userId`. Do not add `advisor` to `MARKETPLACE_LISTING_KINDS` or a `/advisors` route.

### 1. P0 scope (this ticket)

**In P0**

- One-way product → **assigned advisor's** connected Google or Microsoft calendar, on **create** and **cancel/delete** of an eligible **advisory** meeting.
- Skip when the advisor has no valid writable connection. Product CRUD always succeeds.
- Reuse `pushMeetingEvent` / `deleteMeetingEvent` / `meetings.calendar_*`. No second HTTP client, no new mapping table.
- Remote organizer is the **advisor**, never the visitor.

**Not this ticket** (follow-ups; see R-11)

- Provider `updateEvent` and remirror on title/time/location patch; host reassignment.
- Late-connect backfill; disconnect remote-event cleanup.
- Durable retry queue, dead-letter, advisor-visible sync-warning UI.
- Org-setting redaction packs; two-way sync; client calendar; new providers; all-day / RRULE.

---

### R-1 Eligibility

An event is eligible to mirror iff all of:

1. A `meetings` row exists with `kind = 'advisory'` (#2536 registers the kind in `meetingRoutes.ts` `KINDS` only; unknown kinds today coerce to `adhoc` — do **not** treat `adhoc` as advisory).
2. `scheduled_at` is non-null. End = `scheduled_at + duration_minutes` (existing create path).
3. An assigned advisor user id is present: `booking_hosts.host_ref` of the bound service, or the meeting attendee with `role = 'host'` — **not** `created_by` when `created_by` is the visitor.
4. Status is `scheduled` or `live` for create; `cancelled` (or row deleted) for delete.

Transcripts, minutes, internal notes, and client-private records are not eligible. Product meetings are timed via `duration_minutes`; all-day is out of P0.

### R-2 “Connected calendar present”

Evaluated at **mirror time** (not only at meeting create):

1. A `calendar_connections` row exists for `(tenant_id, advisor_user_id)`.
2. `freshAccessToken` returns a non-null access token.
3. `getCalendarProvider(conn.provider)` is non-null (`google` \| `microsoft` only).
4. Target calendar is `conn.calendar_id`, or `'primary'` if null.

If any check fails: treat as **not present**, skip the remote write, do not fail the product action, do not require calendar setup from the visitor or the advisor.

### R-3 Create — advisor calendar, not visitor

After the advisory `meetings` row and attendees are persisted (visitor-book path #2534 **and** any authenticated `reserveForHost` path that also writes a meeting):

1. Resolve `advisorUserId` per R-1.3.
2. Call existing `pushMeetingEvent(db, env, advisorTenantId, advisorUserId, input)`.
3. `advisorTenantId` is the meeting's `tenant_id` (advisor tenant used by `reserve(..., hosted.tenantId)`), never a visitor tenant if different.
4. On a non-null result, persist `calendar_provider`, `calendar_event_id`, `calendar_html_link` on that meeting (same update as `POST /meetings`).
5. On `null`, leave those columns null. Booking/meeting create remains **201**.

**Do not** pass the visitor's user id into `pushMeetingEvent`. Today's `POST /meetings` uses caller `userId`; visitor book must not copy that blindly.

**Do not** add a second Google/Microsoft client. If `CalendarEventInput` needs an explicit timezone later, extend the existing helper; do not fork it.

### R-4 Field mapping (P0)

| Remote field | Value |
|---|---|
| Title | Product meeting title. No internal ids, reservation ids, or secrets |
| Start / end | `scheduled_at` and `scheduled_at + duration_minutes` as ISO-8601 |
| Join URL | Existing `${APP_URL}/meetings?join=${meeting.id}` (or the #2535 visitor join-token URL once it exists), via current `joinUrl` on `createEvent` |
| Description | Join link + meeting title/purpose. **Omit** client legal name, email, account/financial detail (no org setting in P0). Prefer `Advisory session — {title}` over putting the visitor email on the remote event |
| Attendees | **Advisor-only.** Do **not** pass the visitor email as `attendeeEmails`. `POST /meetings` currently passes other attendees and Google uses `sendUpdates=all`, which would mail the client from the advisor's personal calendar — forbidden for advisory visitor bookings (PRD FR-23). If the helper is shared with team meetings, make `attendeeEmails` opt-in and default empty for advisory; do not change authenticated team-meeting invite behavior |
| Calendar | Advisor's connection only (R-2). Never another user's calendar |

### R-5 Skip-when-absent

Given no connection, a revoked/expired token that cannot refresh, an unknown provider, or `createEvent` throw:

- Product create/update/cancel of the meeting/reservation still succeeds.
- No remote write beyond `getUserConnection` + optional `freshAccessToken`.
- Do not surface a calendar-setup error to the visitor. P0 does not require an advisor-facing warning UI.

### R-6 Idempotent create

Idempotent on `(meeting.id, advisor_user_id, provider, calendar_id)`:

- If `meetings.calendar_event_id` is already set, do **not** call `createEvent` again.
- A retried visitor-book that reuses the same meeting row must no-op the mirror.
- `pushMeetingEvent` today always `createEvent`s; the caller (or a thin wrapper) must gate on existing mapping. No new mapping table in P0.

### R-7 Cancel / delete

When the product meeting is cancelled or deleted and `calendar_event_id` + `calendar_provider` are set:

- Call existing `deleteMeetingEvent` with the **advisor** user id and the stored provider/event id.
- Missing remote event is not an error (already true). Repeating cancel is a no-op.
- If no mapping, skip.
- Wire this on whichever cancel path #2534 / bookings use, not only `meetingRoutes` cancel.

### R-8 Source of truth and failure

Product CRUD is source of truth. Calendar API 5xx / 429 / auth-refresh failure must not roll back the meeting or reservation.

P0: catch, return `null`, leave mapping unset (create) or unchanged (delete) — same as today's `pushMeetingEvent`. Durable retry / dead-letter / persistent warning are **not** P0.

P0 **does** require a skip/fail log with meeting id, advisor id, provider, and reason (no tokens, no description payload). Use existing `reportCaughtError` / structured log.

### R-9 Permissions

Only the advisor's own `calendar_connections` grant. The only user id passed into `getUserConnection` / `freshAccessToken` for this ticket is `host_ref` / host attendee. Never the visitor, a teammate, or a shared mailbox.

### R-10 Timezone (P0 minimum)

Do not silently show the wrong local hour.

- Today Microsoft `createEvent` hardcodes `timeZone: 'UTC'` and Google `createEvent` sends `dateTime` without a `timeZone` field — a known defect for non-UTC advisors.
- P0: pass UTC ISO instants (already `scheduledAt.toISOString()`). Document the Microsoft UTC hardcode as a **known gap**; follow-up threads `timeZone` through `CalendarEventInput`.
- No RRULE / all-day engine in this ticket.

### R-11 Deferred (not P0)

- **Late connect:** connecting a calendar does not backfill in P0. Unmapped upcoming events stay unmapped until a later product write or a follow-up job.
- **Disconnect:** deleting `calendar_connections` already stops further mirrors (R-2 fails). P0 does not delete previously pushed remote events.
- **Reassignment:** advisory visitor bookings are 1:1 with `host_ref`; changing host is out of P0.
- **Patch remirror:** no `updateEvent` on `CalendarProvider`. Title/time/location changes do not patch the remote event in P0; last product write does not recreate in P0 either (R-6 forbids a second `createEvent` while a mapping exists).

### R-12 Tests (this ticket)

Automated tests at the helper / route boundary (stub providers; no live OAuth):

1. Advisor with a valid connection: creating an eligible advisory meeting calls `createEvent` **once** and persists `calendar_event_id` / `calendar_provider`.
2. No connection / `freshAccessToken` null / `createEvent` throw: meeting/reservation create still 201; no mapping written; visitor does not receive a calendar error.
3. Organizer id passed to the helper is the advisor `host_ref`, not the visitor user id or email.
4. Advisory create does not send the visitor email in `attendeeEmails`.
5. Existing mapping: second create/retry does not call `createEvent` again.
6. Cancel with mapping calls `deleteEvent`; cancel without mapping does not throw.
7. Both `google` and `microsoft` names resolve through `getCalendarProvider`.

Do not require tests for backfill, reassignment, or retry queues in this ticket.

### Traceability (P0)

| Req | PM FR / AC |
|---|---|
| R-1 | FR 1–3 |
| R-2 | FR 4–6; AC 2 |
| R-3 | FR 7; AC 1 |
| R-4 | FR 13–16, 23; AC 8 |
| R-5 | FR 5, 18; AC 2 |
| R-6 | FR 19; AC 3 (create half) |
| R-7 | FR 9; AC 4 |
| R-8 | FR 18, 25; AC 9 (P0 subset) |
| R-9 | FR 24 |
| R-10 | FR 13; AC 11 (documented gap) |
| R-11 | FR 10–12; AC 5–7 deferred |
| R-12 | AC 12 (P0 subset) |

### Hook for architect / developer

Preferred: one wrapper (e.g. `mirrorAdvisoryMeeting(db, env, meeting, advisorUserId)`) called from the visitor-book writer (#2534) and any `reserveForHost` → meeting writer. Non-advisory `POST /meetings` stays as-is.

`#2536` must land `advisory` in `KINDS` or R-1 never matches. Calendar-connect onboarding UX is out of scope. Do not dump a parallel calendar stack onto this branch.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:51:48.883Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:51:23.247Z · execution #31692

**BA (ticket #2537):** Authored Requirements R-1–R-16 from live `calendarService.ts`, `calendarProviders.ts`, `meetingRoutes.ts`, and `bookings.ts`. Load-bearing: calendar writes use `meetings.createdBy` (advisor organizer per parent #2532), **not** the HTTP caller; no `updateEvent` exists today so PATCH currently drifts; cancel deletes remote but does not clear stored ids; `reserve`/`reserveForHost` must **not** dual-write. Do not add `advisory` to `KINDS` (#2536). Do not add `advisor` to `MARKETPLACE_LISTING_KINDS` or `/advisors`. No product-repo patch in this BA pass. `specs/tasks/task-2537.md` is a docs-only PM stub (same class as #2536) — GitHub 403 blocked delete in this run; developer MUST NOT ship it as the apply.

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:52:21.948Z · execution #31691

BA 2026-09-16: P0 is create/cancel one-way mirror onto the assigned advisor's existing Google/Microsoft connection via pushMeetingEvent/deleteMeetingEvent. Organizer = booking_hosts.host_ref, never the visitor. Skip-when-absent; product 201 always. No new mapping table, no updateEvent, no backfill, no retry UI in this ticket. Advisory create must not pass visitor emails as calendar attendees (Google sendUpdates=all would spam the client). Microsoft UTC hardcode + missing Google timeZone documented as known gap. Attempted to file two P1 children under #2532 (patch remirror/timezone; late-connect/disconnect/retry) — create failed on tasks_assigned_user_id_fkey; retrying with an explicit agent ref. Do not dump calendar source on this BA run; Requirements live on this ticket PRD (same pattern as #2557). GitHub repo reads were 403 rate-limited after calendarService.ts, calendarProviders.ts, meetingRoutes.ts POST, calendarRoutes.ts, schema calendar_connections/meetings, and migration 0292.

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:54:43.150Z · execution #31691

BA 2026-09-16: P1 follow-ups filed under epic #2532, blocked on #2537: #2621 remirror-on-patch + timezone-preserving CalendarEventInput; #2623 late-connect backfill, disconnect cleanup, retry/warning. Default assignee Ada (project-staffed). Architect should treat R-1–R-12 as the P0 contract; do not expand this ticket to cover #2621/#2623.

---

### Update — Product Manager · 2026-09-16T16:18:13.566Z · execution #31842

**PO ready freeze (2026-09-16, exec 31842):** Contract = **R-1–R-12** adopted unchanged. Early BA note “R-1–R-16 / writes use `meetings.createdBy`” is superseded: organizer id is `booking_hosts.host_ref` (or attendee `role=host`), never the visitor, never the HTTP caller. P0 shrink of full-PRD AC 3 (patch half), 5–7, 9 (durable retry/UI), 11 (timezone round-trip) is accepted; those live on #2621 / #2623. Grounded in `calendarService.pushMeetingEvent`/`deleteMeetingEvent` (skip-when-absent, no rollback), `CalendarProvider` with **no `updateEvent`**, Google `sendUpdates=all` (so advisory must omit `attendeeEmails`), Microsoft `timeZone: 'UTC'` hardcode (R-10 known gap), `meetingRoutes` `KINDS` without `advisory` (#2536). No product calendar patch from this PO run. Deleted branch-noise `specs/tasks/task-2537.md` (commit 8c257cfd) — do not re-add. Architect owns Design next; developer implements the wrapper + R-12 tests. Status left `ready`.