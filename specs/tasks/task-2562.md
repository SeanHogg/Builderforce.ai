> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2562
> _Each agent that updates this PRD signs its change below._

# PRD 27 — Verify, version, and ship staffed work + chat diagnostics

**Status:** WIP (implementation complete; remaining work is verify / version / ship)  
**Spec:** `Builderforce.ai/specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md`  
**Constraint:** Code for §2 is written. Do not re-implement §2. Do not touch unrelated dirty tree (canvas, 1176, composingActivity). Never `git stash`. Stage by path only.

## Problem & Goal

Staffed-work persona delegation and persisted chat diagnostics are implemented across api, agent-tools, agent-loop, brain-embedded, VS Code, and touched frontend files, but they are not yet type-checked, version-bumped, packaged, recorded, or operator-verified. Until that closes, the features cannot ship.

**Goal:** Verify the existing implementation, bump package versions, rebuild brain-embedded dist, produce the VSIX, update ship records, and hand off operator-only steps (install, migrations 1177/1178, live re-test, release-note publish decision). Leave the tree otherwise untouched.

## Target users / ICP roles

- **Staff / operators** using Builderforce in VS Code who delegate work to staffed personas and inspect chat diagnostics.
- **Downstream agents** executing R1–R4 and R7 on this tree.
- **Human operator** for install, migrations 1177/1178, live re-test (R5–R6), and the release-note publish ask.

## Scope

In scope for this finish pass (parent **#2562** + children **#2563–#2567**, **#2575**, **#2639**):

- **Do not re-implement §2.** Persona delegation, persisted diagnostics, and the 403/coordination gate stay as already written on `main`.
- **Verify (R1/R2):** type-check `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files. Run **touched** vitest only. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full. Use `NODE_OPTIONS=--max-old-space-size=8192`. Fix real failures only. Child **#2563**.
- **Versions (R3) — verify-first, never downgrade.** Original targets: api `2026.9.37`, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. Live `main` (PO ready pass): api **`2026.9.40`** (already past 37), brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. Do **not** re-bump packages at or past target. Do **not** rewrite api 40→37. Rebuild `brain-embedded` dist only if a bump lands or dist is stale vs `2026.9.31`. Child **#2564**. Do not bump `brain-ui`.
- **VSIX:** exclusive pin is **#2614** — live `clients/vscode/package.json` **`2026.9.79`**. Ticket-PRD `2026.9.72` and product-spec `2026.9.75` are stale. Do not bump, downgrade, or re-pin VSIX on #2562.
- **Package (R4):** `vsce package` without install (operator). Child **#2565**.
- **Operator (R5/R6):** apply migrations **1177** and **1178** (#2566); live re-test (#2567; remainder #2597 Sean). Agents do not apply them.
- **Records (R7):** `DONE.md` resolved entry + release-notes **ask** (#2575). Notes policy: persona delegation = **new**; persisted diagnostics = **improvement**; 403 chain = **no note**.
- Stage **by explicit path** only; unrelated dirty paths (canvas, 1176, composingActivity) stay unstaged. Never `git stash`. No `specs/tasks/task-2562.md` dump.

Out of this agent’s hands (operator): apply migrations 1177/1178; live re-test; VSIX install. Env-unblock for a shell checkout is **#2639**.

_— rewritten by Product Manager · 2026-09-16T16:28:01.088Z_

## Functional requirements

Implementation already exists on `main`. Do not rewrite §2 behavior. Remaining requirements, as locked by PO R-1–R-10:

**R1 / R2 — Verify** (child **#2563**)

- Type-check `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files.
- Run only tests that this change actually touches. Exclude a full run of `clients/vscode/harness/scenarios.test.ts`.
- Set `NODE_OPTIONS=--max-old-space-size=8192` for type-check and vitest.
- Fix real type or test failures introduced by this work. Do not drive-by-fix unrelated dirty tree.

**R3 — Version policy** (child **#2564**)

- Original targets: api `2026.9.37`, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`.
- Live `main`: api **`2026.9.40`**, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. **Verify-first.** Do not re-bump. Do not downgrade api 40→37.
- Leave VSIX at live **`2026.9.79`** (#2614). Do not bump or downgrade it.
- Rebuild `brain-embedded` dist only if a bump lands or dist is stale vs `2026.9.31`.

**R4 — Package** (child **#2565**)

- Produce the VSIX with `vsce package`.
- Do not install; install is operator.
- Do not change the VSIX version.

**R5 — Migrations** (operator, child **#2566**)

- Operator applies migrations **1177** and **1178**. Agents do not apply them.

**R6 — Live re-test** (operator, child **#2567**)

- Operator re-tests staffed-work delegation and persisted chat diagnostics against a live stack after install + migrations.
- Pass contract lives on #2567: work-mode VS Code chat on project 11 against #2594 (Ada) and #2595 (John Coder). Remainder #2597 (Sean).

**R7 — Records and notes** (child **#2575**)

- Add a resolved entry to `DONE.md` (do not invent one on a no-shell run; `DONE.md` on `main` is empty).
- Ask before publishing release notes.
- If notes are approved: persona delegation as **new**; persisted diagnostics as **improvement**; omit the 403 chain entirely.

**Git hygiene**

- Stage by explicit path.
- Do not stage canvas, 1176, or composingActivity work.
- Never `git stash`.
- No `specs/tasks/task-2562.md` dump.

_— rewritten by Product Manager · 2026-09-16T16:28:05.379Z_

## Acceptance criteria

**AC1.** Staffed-work persona delegation behaves as specified in §2 of the existing product spec; no §2 re-implementation was performed.

**AC2.** Chat diagnostics persist and surface as specified in §2; no §2 re-implementation was performed.

**AC3.** Type-check is green for `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files, run with `NODE_OPTIONS=--max-old-space-size=8192`. Child **#2563**.

**AC4.** Touched vitest is green under the same `NODE_OPTIONS`. `clients/vscode/harness/scenarios.test.ts` was not run in full. Child **#2563**.

**AC5.** Packages are **at or past** the original targets and were **not downgraded**: api ≥ `2026.9.37` (live **`2026.9.40`**), brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. VSIX remains live **`2026.9.79`** (exclusive pin **#2614** — not 72, not 75). `brain-embedded` dist matches `2026.9.31` unless a later legitimate bump supersedes it. Child **#2564**.

**AC6.** `vsce package` produced the VSIX without changing its version. Install, migrations 1177/1178, and live re-test are documented as operator steps (children **#2565 / #2566 / #2567**), not executed by the coding agent.

**AC7.** `DONE.md` has a resolved entry for this PRD (child **#2575**). Release notes were not published without an explicit ask; note classification is persona delegation = **new**, persisted diagnostics = **improvement**, 403 chain = **no note**.

**AC8.** Git staging is path-scoped to this work. canvas, 1176, and composingActivity remain unstaged. `git stash` was not used. No `specs/tasks/task-2562.md` dump ships.

_— rewritten by Product Manager · 2026-09-16T16:28:09.877Z_

## Out of scope

- Re-implementing or redesigning §2 (staffed work, chat diagnostics, 403 chain).
- Full `clients/vscode/harness/scenarios.test.ts` run.
- Bumping the VSIX version (already `2026.9.72`).
- Agent-applied DB migrations or live environment install / re-test.
- Publishing release notes without asking.
- Any mention of the 403 chain in release notes.
- Staging, stashing, or otherwise disturbing unrelated dirty tree: canvas, 1176, composingActivity.
- Unrelated refactors, drive-by lint, or dependency upgrades.

## Requirements

**PO-locked 2026-09-16 (ready, exec 31881).** BA slot unstaffed; Product Owner adopts the existing Functional requirements + product spec §2/§3 as the implementer contract. Architect owns Design. Do not re-derive.

**P0 / R-1 — Do not re-implement §2.** Staffed-work persona delegation (`spawn_agent as_agent` → persona brief), persisted chat diagnostics (`ChatDiagnosticsReport` / mig 1178), and the coordination-gate 403 chain (`coordinationGate` / mig 1177) already exist on `main`. This ticket is verify / version-policy / package / records / operator-handoff only. No redesign, no second implementation, no drive-by on canvas / 1176 / composingActivity.

**P1 / R-2 — Verify-first versions. Never downgrade.** Original bump targets were api `2026.9.37`, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. Live `main` on this PO pass: api **`2026.9.40`** (past target), brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. A bound shell run **verifies**; it does not apply a second bump and must not rewrite api 40→37. Rebuild `brain-embedded` dist only if a bump actually lands or dist is stale vs `2026.9.31`. Child **#2564**. Do not bump `brain-ui`.

**P2 / R-3 — VSIX exclusive pin is #2614.** Live `clients/vscode/package.json` is **`2026.9.79`**. Ticket-PRD `2026.9.72` and product-spec `2026.9.75` are stale/observational. Do **not** bump, downgrade, or re-pin the VSIX on #2562. `#2614` (a) is not operator confirmation on other pins.

**P3 / R-4 — Type-check + touched vitest.** `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files. `NODE_OPTIONS=--max-old-space-size=8192`. Run only tests this change touches. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full. Fix real failures only. Child **#2563**. Product-spec claim “R1–R4 verified locally (2026-09-16)” is the record to confirm, not a licence to skip the shell pass when a checkout exists.

**P4 / R-5 — Package without install.** `vsce package` produces the VSIX. Install is operator. Do not change the VSIX version. Child **#2565**.

**P5 / R-6 — Operator-only R5/R6.** Agents do not apply DB migrations or live-install. Migrations **1177** and **1178** = child **#2566**. Live re-test = child **#2567** (pass contract on that ticket: work-mode VS Code chat on project 11 against **#2594** Ada and **#2595** John Coder; Copy contains both literal `Staffing:` lines; `brain_chat_diagnostics` row after Copy; `builtin_chats_diagnostics` returns it; remainder **#2597** Sean). Work-mode identity `auth:bfk:v2:` is not itself a pass.

**P6 / R-7 — Records and notes.** Dated RESOLVED `DONE.md` entry + release-notes **ask** is child **#2575**. Do not invent a `DONE.md` row on a no-shell run (`DONE.md` on `main` is empty). Do not publish notes without an explicit operator ask. Taxonomy: persona delegation (`spawn_agent as_agent`) = **new**; persisted diagnostics = **improvement**; 403 chain = **no note**.

**P7 / R-8 — Git hygiene and dumps.** Stage by **explicit path** only. Never `git stash`. Leave canvas, 1176, and composingActivity unstaged. **No** `specs/tasks/task-2562.md` dump (forbidden; drop if present on the branch). No product-repo patch from this ready-lane PO run.

**P8 / R-9 — Bind and Design.** Shell-capable R1/R2/R4 need a readable checkout of `SeanHogg/Builderforce.ai` as `builderforce/task-2562`. Env-unblock is child **#2639** (operator bind, empty product-repo footprint). Architect owns Design. Production UI is not this ticket.

**P9 / R-10 — Children own remaining work.** Parent #2562 is the locked contract + coordination. Do not reopen §2 on the parent. Children: **#2563** verify, **#2564** versions (verify-first), **#2565** package, **#2566** migrations (operator), **#2567** live re-test (operator), **#2575** DONE.md + notes ask, **#2639** bind.

_— rewritten by Product Manager · 2026-09-16T16:27:22.117Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

**Run:** execution 31758 (developer container, 2026-09-16). **Checkout:** none. Project 11 `githubRepoUrl` is null. Bound surface is `seanhogg/builderforce.ai` with **0 files**. `list_files`, `read_file`, `git_*`, and `run_command` all fail (`no repository bound` / `no repository checked out`). Same class of blocker as #2588 (#2527) and #2600 (#2554). GitHub API is also 403 rate-limited (user 15883045) on branch inspection.

**Did not re-implement §2.** No product files were written. No conceptual stand-in (no invented `DONE.md`, no invented version bumps). Never `git stash`. Canvas / 1176 / composingActivity were not touched because nothing was staged.

**R1/R2 (verify):** Not executed. Cannot type-check or run vitest without a checkout. Child **#2563** remains todo.

**R3 (versions):** Not re-applied on this run. Child **#2564** BA already recorded that the target versions (`api 2026.9.37`, `brain-embedded 2026.9.31` via tsup, `agent-loop 2026.9.17`, `agent-tools 2026.9.5`) are claimed **already on main `a0eef9439`** — next bound run is **verify-first**, not a second bump. Do not bump `brain-ui`. VSIX pin in this PRD (`2026.9.72`) is stale vs live `clients/vscode/package.json` **`2026.9.79`** — tracked by **#2614** (parent #2555). Do **not** bump or downgrade VSIX on #2562.

**R4 (`vsce package`):** Not executed (no checkout, no `vsce`). Child **#2565** remains todo. Install remains operator.

**R5 / R6:** Operator-only (migrations **1177/1178**, live re-test). Children **#2566 / #2567**. Not attempted.

**R7:** `DONE.md` not written (no checkout). Release notes **not** published. The ask remains: persona delegation = **new**; persisted diagnostics = **improvement**; 403 chain = **no note**. Child **#2575** owns the DONE.md + notes ask.

**Next developer run** (once SeanHogg/Builderforce.ai is bound as `builderforce/task-2562` with readable git + shell):

1. `git_sync_latest` from `main`.
2. Verify (do not re-bump if already at target) the four package versions; rebuild `brain-embedded` dist only if a bump landed or dist is stale vs `2026.9.31`.
3. Type-check `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files with `NODE_OPTIONS=--max-old-space-size=8192`.
4. Run **touched** vitest only. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full.
5. `vsce package` without install. Do not change the VSIX version (defer to #2614).
6. Stage **by explicit path only**. Leave canvas, 1176, composingActivity unstaged. Never `git stash`.
7. Add a `DONE.md` resolved entry; do **not** publish release notes without the operator ask.

**Out of this run:** any product-repo patch, `specs/tasks/task-2562.md` dump, VSIX version bump, DB migrations, live install / re-test.

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:57:18.428Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:57:21.704Z · execution #31758

**2026-09-16 developer (exec 31758):** Blocked — no repository checkout (project 11 `githubRepoUrl` null; bound `seanhogg/builderforce.ai` is empty). Did not invent product files. R3 targets already claimed on main `a0eef9439` (#2564 verify-first). VSIX pin 2026.9.72 vs tree 2026.9.79 is #2614, not this ticket. Filed a bind-gap child. Operator still owns R5/R6 and the R7 notes ask (persona delegation=new, diagnostics=improvement, 403 chain=no note).

---

### Update — Cloud agent d02ff7ee-9cf2-4c44-8558-c89104f6278f · 2026-09-16T16:12:27.809Z · execution #31758

**2026-09-16 developer (exec 31758, durable requeue):** Checkout is now readable on `seanhogg/builderforce.ai` (main). Did not re-implement §2. Deleted forbidden `specs/tasks/task-2562.md` (prior dump; PRD 27 forbids it). Inventory against main:

- Product spec `specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md` already records **R1–R4 verified locally (2026-09-16)**.
- R3 already on main: api `2026.9.37`, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. No second bump. `brain-embedded/dist/` present.
- VSIX `clients/vscode/package.json` is `2026.9.79` — do not bump or downgrade (#2614 / parent #2555).
- §2 present: `coordinationGate.ts`, `agentPersonaBrief.ts`, `staffingSummary.ts`, `chatDiagnosticsStore.ts`, `as_agent` on spawn_agent, MCP catalog spreads, migrations **1177** and **1178**.
- This executor has **no shell** — did not re-run tsc/vitest/`vsce package`. Children **#2563** (verify) and **#2565** (package) still own a shell-capable re-run if wanted.
- R5/R6 remain operator (#2566 / #2567). R7 DONE.md + notes ask is **#2575**; `DONE.md` on main is empty; did not invent an entry. Notes policy unchanged: persona delegation = new; persisted diagnostics = improvement; 403 chain = no note.
- Never `git stash`. Did not touch canvas / 1176 / composingActivity.

---

### Update — Product Manager · 2026-09-16T16:28:38.255Z · execution #31890

**PO ready lock (exec 31881, 2026-09-16).** Approved. Contract **R-1–R-10 / P0–P9** on ticket PRD. Grounded on `main`: api **2026.9.40**, brain-embedded **2026.9.31**, agent-loop **2026.9.17**, agent-tools **2026.9.5**, VSIX **2026.9.79** (`clients/vscode/package.json`). Product spec `specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md` records R1–R4 verified locally; remaining is operator R5–R7 + records. Do not re-implement §2. Verify-first versions — never downgrade api 40→37. VSIX exclusive pin **#2614** (not 72, not 75). No product-repo patch this lane. No `specs/tasks/task-2562.md` dump. Architect owns Design. Children: #2563 verify, #2564 versions, #2565 package, #2566/#2567 operator, #2575 records, #2639 bind. Notes: persona delegation = new; persisted diagnostics = improvement; 403 chain = no note.