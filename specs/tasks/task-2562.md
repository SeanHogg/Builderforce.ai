> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2562
> _Each agent that updates this PRD signs its change below._

# PRD 27 — Verify, version, and ship staffed work + chat diagnostics

**Status:** WIP (implementation complete; remaining work is verify / version / ship)  
**Spec:** `Builderforce.ai/specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md`  
**Constraint:** Code for §2 is written. Do not re-implement §2. Do not touch unrelated dirty tree (canvas, 1176, composingActivity). Never `git stash`. Stage by path only.

## Problem & Goal

Staffed-work persona delegation and persisted chat diagnostics are implemented across api, agent-tools, agent-loop, brain-embedded, VS Code, and touched frontend files, but they are not yet type-checked, version-bumped, packaged, recorded, or operator-verified. Until that closes, the features cannot ship.

**Goal:** Verify the existing implementation, bump package versions, rebuild brain-embedded dist, produce the VSIX, update ship records, and hand off operator-only steps (install, migrations 1177/1178, live re-test, release-note publish decision). Leave the tree otherwise untouched.

## Target users / ICP roles

- **Staff / operators** using Builderforce in VS Code who delegate work to staffed personas and inspect chat diagnostics.
- **Downstream agents** executing R1–R4 and R7 on this tree.
- **Human operator** for install, migrations 1177/1178, live re-test (R5–R6), and the release-note publish ask.

## Scope

In scope for this finish pass:

- Type-check: `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files.
- Run touched vitest only. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full. Use `NODE_OPTIONS=--max-old-space-size=8192`. Fix real failures only.
- Version bumps:
  - api `2026.9.36` → `2026.9.37`
  - brain-embedded `2026.9.30` → `2026.9.31`
  - agent-loop `2026.9.16` → `2026.9.17`
  - agent-tools `2026.9.4` → `2026.9.5`
  - VSIX is already `2026.9.72` — do not bump it.
- Rebuild brain-embedded dist after the version bump.
- `vsce package` (install is operator).
- `DONE.md` resolved entry.
- Stage **by path** only; unrelated dirty paths stay unstaged.
- Ask before publishing release notes. Notes policy: persona delegation = **new**; persisted diagnostics = **improvement**; 403 chain = **no note**.

Out of this agent’s hands (operator): apply migrations 1177/1178; live re-test; VSIX install.

## Functional requirements

Implementation already exists. Do not rewrite §2 behavior. Remaining requirements:

**R1 / R2 — Verify**

- Type-check the packages listed above plus touched frontend files.
- Run only tests that this change actually touches. Exclude a full run of `clients/vscode/harness/scenarios.test.ts`.
- Set `NODE_OPTIONS=--max-old-space-size=8192` for type-check and vitest.
- Fix real type or test failures introduced by this work. Do not drive-by-fix unrelated dirty tree.

**R3 — Version and rebuild**

- Apply the four package bumps listed in Scope.
- Leave VSIX at `2026.9.72`.
- Rebuild `brain-embedded` dist so the shipped artifact matches `2026.9.31`.

**R4 — Package**

- Produce the VSIX with `vsce package`.
- Do not install; install is operator.

**R5 — Migrations (operator)**

- Operator applies migrations **1177** and **1178**. Agents do not apply them.

**R6 — Live re-test (operator)**

- Operator re-tests staffed-work delegation and persisted chat diagnostics against a live stack after install + migrations.

**R7 — Records and notes**

- Add a resolved entry to `DONE.md`.
- Ask before publishing release notes.
- If notes are approved: persona delegation as new; persisted diagnostics as improvement; omit the 403 chain entirely.

**Git hygiene**

- Stage by explicit path.
- Do not stage canvas, 1176, or composingActivity work.
- Never `git stash`.

## Acceptance criteria

**AC1.** Staffed-work persona delegation behaves as specified in §2 of the existing PRD; no §2 re-implementation was performed.

**AC2.** Chat diagnostics persist and surface as specified in §2; no §2 re-implementation was performed.

**AC3.** Type-check is green for `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files, run with `NODE_OPTIONS=--max-old-space-size=8192`.

**AC4.** Touched vitest is green under the same `NODE_OPTIONS`. `clients/vscode/harness/scenarios.test.ts` was not run in full.

**AC5.** Versions are `api 2026.9.37`, `brain-embedded 2026.9.31`, `agent-loop 2026.9.17`, `agent-tools 2026.9.5`; VSIX remains `2026.9.72`; brain-embedded dist was rebuilt.

**AC6.** `vsce package` produced the VSIX. Install, migrations 1177/1178, and live re-test are documented as operator steps, not executed by the coding agent.

**AC7.** `DONE.md` has a resolved entry for this PRD. Release notes were not published without an explicit ask; note classification is persona delegation = new, persisted diagnostics = improvement, 403 chain = no note.

**AC8.** Git staging is path-scoped to this work. canvas, 1176, and composingActivity remain unstaged. `git stash` was not used.

## Out of scope

- Re-implementing or redesigning §2 (staffed work, chat diagnostics, 403 chain).
- Full `clients/vscode/harness/scenarios.test.ts` run.
- Bumping the VSIX version (already `2026.9.72`).
- Agent-applied DB migrations or live environment install / re-test.
- Publishing release notes without asking.
- Any mention of the 403 chain in release notes.
- Staging, stashing, or otherwise disturbing unrelated dirty tree: canvas, 1176, composingActivity.
- Unrelated refactors, drive-by lint, or dependency upgrades.

## Requirements

_Owned by the business-analyst — to be authored._

## Design

_Owned by the architect — to be authored._

## Implementation Notes

**Run:** execution 31758 (developer container, 2026-09-16). **Checkout:** none. Project 11 `githubRepoUrl` is null. Bound surface is `seanhogg/builderforce.ai` with **0 files**. `list_files`, `read_file`, `git_*`, and `run_command` all fail (`no repository bound` / `no repository checked out`). Same class of blocker as #2588 (#2527) and #2600 (#2554). GitHub API is also 403 rate-limited (user 15883045) on branch inspection.

**Did not re-implement §2.** No product files were written. No conceptual stand-in (no invented `DONE.md`, no invented version bumps). Never `git stash`. Canvas / 1176 / composingActivity were not touched because nothing was staged.

**R1/R2 (verify):** Not executed. Cannot type-check or run vitest without a checkout. Child **#2563** remains todo.

**R3 (versions):** Not re-applied on this run. Child **#2564** BA already recorded that the target versions (`api 2026.9.37`, `brain-embedded 2026.9.31` via tsup, `agent-loop 2026.9.17`, `agent-tools 2026.9.5`) are claimed **already on main `a0eef9439`** — next bound run is **verify-first**, not a second bump. Do not bump `brain-ui`. VSIX pin in this PRD (`2026.9.72`) is stale vs live `clients/vscode/package.json` **`2026.9.79`** — tracked by **#2614** (parent #2555). Do **not** bump or downgrade VSIX on #2562.

**R4 (`vsce package`):** Not executed (no checkout, no `vsce`). Child **#2565** remains todo. Install remains operator.

**R5 / R6:** Operator-only (migrations **1177/1178**, live re-test). Children **#2566 / #2567**. Not attempted.

**R7:** `DONE.md` not written (no checkout). Release notes **not** published. The ask remains: persona delegation = **new**; persisted diagnostics = **improvement**; 403 chain = **no note**. Child **#2575** owns the DONE.md + notes ask.

**Next developer run** (once SeanHogg/Builderforce.ai is bound as `builderforce/task-2562` with readable git + shell):

1. `git_sync_latest` from `main`.
2. Verify (do not re-bump if already at target) the four package versions; rebuild `brain-embedded` dist only if a bump landed or dist is stale vs `2026.9.31`.
3. Type-check `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files with `NODE_OPTIONS=--max-old-space-size=8192`.
4. Run **touched** vitest only. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full.
5. `vsce package` without install. Do not change the VSIX version (defer to #2614).
6. Stage **by explicit path only**. Leave canvas, 1176, composingActivity unstaged. Never `git stash`.
7. Add a `DONE.md` resolved entry; do **not** publish release notes without the operator ask.

**Out of this run:** any product-repo patch, `specs/tasks/task-2562.md` dump, VSIX version bump, DB migrations, live install / re-test.

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:57:18.428Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:57:21.704Z · execution #31758

**2026-09-16 developer (exec 31758):** Blocked — no repository checkout (project 11 `githubRepoUrl` null; bound `seanhogg/builderforce.ai` is empty). Did not invent product files. R3 targets already claimed on main `a0eef9439` (#2564 verify-first). VSIX pin 2026.9.72 vs tree 2026.9.79 is #2614, not this ticket. Filed a bind-gap child. Operator still owns R5/R6 and the R7 notes ask (persona delegation=new, diagnostics=improvement, 403 chain=no note).

---

### Update — Cloud agent d02ff7ee-9cf2-4c44-8558-c89104f6278f · 2026-09-16T16:12:27.809Z · execution #31758

**2026-09-16 developer (exec 31758, durable requeue):** Checkout is now readable on `seanhogg/builderforce.ai` (main). Did not re-implement §2. Deleted forbidden `specs/tasks/task-2562.md` (prior dump; PRD 27 forbids it). Inventory against main:

- Product spec `specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md` already records **R1–R4 verified locally (2026-09-16)**.
- R3 already on main: api `2026.9.37`, brain-embedded `2026.9.31`, agent-loop `2026.9.17`, agent-tools `2026.9.5`. No second bump. `brain-embedded/dist/` present.
- VSIX `clients/vscode/package.json` is `2026.9.79` — do not bump or downgrade (#2614 / parent #2555).
- §2 present: `coordinationGate.ts`, `agentPersonaBrief.ts`, `staffingSummary.ts`, `chatDiagnosticsStore.ts`, `as_agent` on spawn_agent, MCP catalog spreads, migrations **1177** and **1178**.
- This executor has **no shell** — did not re-run tsc/vitest/`vsce package`. Children **#2563** (verify) and **#2565** (package) still own a shell-capable re-run if wanted.
- R5/R6 remain operator (#2566 / #2567). R7 DONE.md + notes ask is **#2575**; `DONE.md` on main is empty; did not invent an entry. Notes policy unchanged: persona delegation = new; persisted diagnostics = improvement; 403 chain = no note.
- Never `git stash`. Did not touch canvas / 1176 / composingActivity.