> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2567
> _Each agent that updates this PRD signs its change below._

# PRD: R6 — Operator Live Re-Test

## Problem & Goal

Work-mode VS Code chat on **project 11** can still refuse with `manager role required`, omit `Staffing:/as <agent>` from the Copy report, fail to persist a `brain_chat_diagnostics` row after Copy, or resolve the session as `developer` because cache `auth:bfk:v2:` did not ship. PRD 27 cannot close until an operator proves those paths **live**.

This ticket is **not** a new operator-console “Live re-test” product. There is no live-safe check catalog, confirmation dialog, rate-limit service, production probe runner, or operator-UI action to build. The PM draft that described those is **rejected**.

**Goal:** Pass PRD 27 **R6** on project 11: two staffed subject tickets, work-mode VS Code chat with no manager-role refusal, a Copy payload that contains the literal Staffing lines, a `brain_chat_diagnostics` row written after Copy and returned by `builtin_chats_diagnostics`, and work-mode identity (a developer-resolved key is **not** a pass).

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:53:20.575Z_

## Target users / ICP roles

- **Operator** (Sean Hogg, `assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) — sole actor who can **pass** R6. Drives work-mode VS Code chat, Copy, diagnostics read, and optional `auth:bfk:v2:` inspect. Evidence lands on #2597 and is referenced from this ticket.
- **Ada** (`assignedAgentRef=fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6`) — staffing subject **#2594** (`Staffing:/as Ada`).
- **John Coder** (`assignedAgentRef=658608ba-59ab-4ec3-873d-211a89ea000f`) — staffing subject **#2595** (`Staffing:/as John Coder`).
- **Developer container** — already filed and staffed the two subjects on #2574. It cannot drive VS Code work-mode chat, Copy, or auth inspect. It is **not** a pass actor.
- End customers, unprivileged viewers, and a generic on-call / SRE “operator console” persona are **not** users of this ticket.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:53:23.914Z_

## Scope

In scope for **project 11** only, as the PRD 27 **R6 contract** (this ticket #2567, child of epic #2562):

1. Two filed, staffed subject tickets — already **#2594** (Ada) and **#2595** (John Coder) under #2574. Do not re-file.
2. Operator live checks in **work-mode VS Code chat** (not manager UI): no `manager role required`; `/as Ada` and `/as John Coder`; Copy payload with both literal Staffing lines; `brain_chat_diagnostics` row after Copy; `builtin_chats_diagnostics` returns that row; work-mode identity (`auth:bfk:v2:` inspect if the session still resolves as `developer`).
3. Evidence recorded on **#2597** (operator remainder) and referenced from this ticket.
4. Explicit rejection of any product implementation of the discarded PM “Live re-test” console feature on `builderforce/task-2567`.

Sibling mapping (do not duplicate work):

| Ticket | Role in R6 |
|---|---|
| **#2567** (this) | R6 **contract** (BA Requirements). Not a product patch. |
| **#2574** | File + staff the two subjects (done). |
| **#2594** / **#2595** | Staffing fixtures. Dispatched agents confirm staffing and **stop**. |
| **#2597** | Operator remainder — the only run that can pass R6. |
| **#2573** / **#2566** | R5 migrations `1177` then `1178` on core (precondition). |

Out of this ticket’s producer work: API / VSIX / frontend patches, applying migrations, version bumps (R3), VSIX package (R4), `DONE.md` (R7).

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:53:26.944Z_

## Functional requirements

BA Requirements **R-1–R-15** are adopted **unchanged** as the product contract. Priority mapping:

**P0 — Contract only (R-1, R-13, R-14).** This ticket is the PRD 27 R6 contract, not a product change. No API, VSIX, brain-embedded, or frontend patch on `builderforce/task-2567`. A docs-only `specs/tasks/task-2567.md` is not evidence — drop it; do not merge it. The discarded operator-console Live re-test product (`live_safe` catalog, confirmation UX, rate limits, production probe runner) stays **rejected**.

**P1 — Two staffed subjects, already done (R-4, R-5).** #2594 (`Staffing:/as Ada`, Ada `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6`) and #2595 (`Staffing:/as John Coder`, John Coder `658608ba-59ab-4ec3-873d-211a89ea000f`), filed and staffed by #2574 execution 31660. Re-filing is a scope fail, not a substitute.

**P2 — R5 precondition, not this ticket (R-2).** Core migrations `1177_coordination_requires_manager.sql` then `1178_brain_chat_diagnostics.sql` must be on core before api `2026.9.37` (#2573 / #2566). At PM lock, #2573 is still `in_progress`. If 1178 is absent, R-9/R-10 500 and R6 fails closed. Do not apply migrations from this ticket.

**P3 — Six operator live checks (R-6–R-11).** Sole pass actor is the operator on **#2597** in work-mode VS Code Brain chat on **project 11**:

1. No `manager role required` (or HTTP 403/502 wrapping it).
2. `/as Ada` against #2594 and `/as John Coder` against #2595.
3. Copy payload contains both literal lines `Staffing:/as Ada` and `Staffing:/as John Coder`.
4. `brain_chat_diagnostics` row with timestamp **strictly after** Copy.
5. `builtin_chats_diagnostics` returns that same row (id + timestamp).
6. Session is work-mode / owner, not `developer`. A developer-resolved `auth:bfk:v2:` is a **fail** (record `{ present | stale | missing }` + role only; no key material).

**P4 — Evidence on #2597 (R-12).** Copy excerpt with both Staffing lines; diagnostics row id + timestamp equal across store and MCP; session role (and auth prefix state if developer); explicit statement that work-mode chat did not return manager-role required. Pointer from this ticket. Absence of any item = R6 not passed.

**P5 — Pass producer is the operator (R-3, R-14, R-15).** A BA run, this PM lock, a developer-container run, or a manager-UI action **cannot** satisfy P3. They may only record that they could not. PM sign-off ≠ R6 complete.

**P6 — Developer no-op on this branch (R-8, R-13).** Developers must not implement Copy, diagnostics, staffing, or the rejected console feature as a substitute for the live checks. Do not merge fixture-noise branches `builderforce/task-2594` or `builderforce/task-2595` as R6 evidence.

_— product-locked by Kevin (product-manager) · 2026-09-16 · execution 31852_

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:17:39.519Z_

## Acceptance criteria

R6 is **product-locked** to BA R-1–R-15 / P0–P6. It **passes only** when **all** of the following hold on a live **work-mode VS Code** session against project 11, evidenced on **#2597**:

- #2594 and #2595 exist and remain staffed as Ada and John Coder respectively (P1; already true at lock).
- Work-mode chat does **not** return `manager role required` (or equivalent) (P3.1).
- The copied report contains both literal lines `Staffing:/as Ada` and `Staffing:/as John Coder` (P3.2–P3.3).
- A `brain_chat_diagnostics` row exists with timestamp **after** that Copy (P3.4).
- `builtin_chats_diagnostics` returns that same row (id + timestamp) (P3.5).
- Session identity is work-mode, not `developer` (P3.6). A developer-resolved key is a **fail** even if the other checks look green.

This PM lock, a BA run, a developer-container run, or a manager-UI action **cannot** satisfy these criteria. **#2597** is the operator evidence ticket. **This PM sign-off is the contract, not an R6 pass.**

Near-misses that **fail** (unchanged from BA): paraphrased Staffing lines (`Staffing: as Ada`, `as Ada` without the `Staffing:/as` prefix, wrong case); only one of the two lines; a historical diagnostics row from an earlier chat; 500 because migration 1178 is not on core; treating this container’s `developer` identity as the auth check.

**All six live checks must hold** on top of the two staffed subjects. Partial green is a fail.

_— product-locked by Kevin (product-manager) · 2026-09-16 · execution 31852_

_— rewritten by Kevin BA/PM/PO (Durable) · 2026-09-16T16:17:43.550Z_

## Out of scope

- Authoring a new “Live re-test” operator-console action, live-safe check catalog, confirmation UX, concurrency/rate limits, production probe runner, or audit-event product (the rejected PM draft).
- Sandbox, staging, or local re-test as a substitute for work-mode VS Code.
- Load / soak / chaos testing, traffic replay, or multi-target fan-out.
- Automated live re-test on every deploy or alert.
- Customer self-serve “test my account” flows.
- Remediation, rollback, or config change from a re-test result.
- Re-filing or re-staffing #2594 / #2595.
- Applying migrations `1177` / `1178` (that is R5 / #2573 / #2566).
- Version bumps, VSIX package, or `DONE.md` (R3 / R4 / R7).
- Product code, API, VSIX, brain-embedded, or frontend patches on `builderforce/task-2567`.
- Shipping `specs/tasks/task-2567.md` or any conceptual Copy / diagnostics stand-in.
- Pasting or logging `auth:bfk:v2:` key material, hashes, or the full cache key.
- Driving R6 from manager UI or a developer container and calling that a pass.
- Merging fixture-noise branches `builderforce/task-2594` or `builderforce/task-2595` as R6 evidence.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:53:37.046Z_

## Requirements

This ticket is the PRD 27 **R6 contract**, not a product change. R6 is an **operator live re-test** of already-shipped staffed-work + chat-diagnostics on **project 11**. No API, VSIX, brain-embedded, or frontend patch belongs on `builderforce/task-2567`. A docs-only `specs/tasks/task-2567.md` is **not** evidence — drop it; do not merge it.

The discarded PM draft (operator-console Live re-test action, `live_safe` catalog, confirmation UX, rate limits, production probe runner) is **out of scope**. Developers must not implement it.

**Pass producer:** operator on **#2597**. This BA run cannot execute R-6–R-11.

### R-1 — Ticket identity and siblings

| Ticket | What it is |
|---|---|
| **#2562** | Epic: PRD 27 — verify, version, and ship staffed work + chat diagnostics |
| **#2567** (this) | Original R6 child. **Contract only.** |
| **#2574** | Gap: file and staff two tasks on project 11. **Done** (execution 31660). |
| **#2594** | Subject A — `Staffing:/as Ada` |
| **#2595** | Subject B — `Staffing:/as John Coder` |
| **#2597** | Operator remainder — the only run that can **pass** R6 |
| **#2573** / **#2566** | R5 — apply `1177` then `1178` on core (precondition) |

Do not re-file the subjects. Do not treat a green BA/developer-container run on this ticket as R6 passed.

### R-2 — Precondition (not this ticket)

PRD 27 **R5** (#2573) must have applied core migrations `1177_coordination_requires_manager.sql` then `1178_brain_chat_diagnostics.sql` before api `2026.9.37` is on the worker. If **1178** is absent, R-9/R-10 will 500 and R6 fails closed. Do not apply migrations from this ticket. At BA authoring time #2573 is still `in_progress` — do not assume the ledger lists both.

### R-3 — Actor and surface

| Item | Value |
|---|---|
| Sole pass actor | Operator (Sean Hogg, `assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) |
| Surface | VS Code **work-mode** Brain chat (installed PRD 27 work-mode VSIX) |
| Project | **11** only |
| Not a valid surface | Manager UI; this cloud/developer-container executor; Web Brain; a session that still resolves as `developer` |

This BA run, any developer-container run, and any manager-UI action **cannot** satisfy R-6–R-11. They may only record that they could not.

### R-4 — Subjects (fixtures; no product work)

| Ticket | Staffing line (literal) | assignedAgentRef | Agent |
|---|---|---|---|
| **#2594** | `Staffing:/as Ada` | `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6` | Ada (Sr. Product Mgr) |
| **#2595** | `Staffing:/as John Coder` | `658608ba-59ab-4ec3-873d-211a89ea000f` | John Coder (Durable) |

Both tickets instruct dispatched agents: confirm staffing and **stop**. Branches `builderforce/task-2594` and `builderforce/task-2595` (fixture-noise BA commits) are **not** R6 evidence. Do not merge them as this ticket.

### R-5 — File + staff already done (not this ticket)

#2574 execution 31660 filed and staffed #2594 and #2595. Re-filing from this ticket, from manager UI, or from a developer container is a **scope fail**, not a substitute for the operator checks.

What that container **could not** do (still open on #2597): work-mode chat, Copy, `brain_chat_diagnostics`, `builtin_chats_diagnostics`, `auth:bfk:v2:` inspect.

### R-6 — Work-mode chat is admitted (check 1)

**Given** the operator opens a **new** work-mode VS Code chat bound to project 11  
**When** they send any turn (including the `/as` invocations in R-7)  
**Then** the response is **not** `manager role required`, not HTTP 403/502 wrapping that error, and not an equivalent coordination-gate refusal.

Fail: any such refusal in work-mode chat. A developer-container 403 on kanban routes is **out of band** and does not pass or fail this check.

### R-7 — Slash-staff both subjects (check 2)

**Given** R-6 holds  
**When** the operator invokes, in that same work-mode chat:

1. `/as Ada` against **#2594**
2. `/as John Coder` against **#2595**

**Then** each invocation targets the named ticket and the named agent. Order is not significant.

Fail: invocations from manager UI; `/as` against any other ticket; staffing a different agent.

### R-8 — Copy payload contains both literal Staffing lines (check 3)

**Given** R-7 completed in that chat  
**When** the operator uses the chat **Copy** control (the product Copy that POSTs diagnostics — not OS clipboard of a selection that skips the report)  
**Then** the copied payload contains **both** of these **literal** lines, byte-for-byte as a line (leading/trailing whitespace ignored; spelling and slash required):

```
Staffing:/as Ada
Staffing:/as John Coder
```

Near-misses that **fail**: `Staffing: as Ada`, `as Ada` without the `Staffing:/as` prefix, `Staffing:/as ada`, only one of the two lines, a staffing summary JSON field without the lines, or a copied fragment that is not the Copy report.

The Copy report may also carry a trailing `ChatDiagnosticsReport` fenced JSON block with `schemaVersion: 1` (PRD 27 §6.5). That block is supporting evidence; it does **not** replace the two Staffing lines.

### R-9 — Diagnostics row written after Copy (check 4)

**Given** the Copy in R-8  
**When** the operator inspects `brain_chat_diagnostics` for that chat  
**Then** a row exists whose `created_at` (or equivalent timestamp) is **strictly after** the Copy action time.

Record: **row id** and **timestamp**.

Fail: no row; row timestamp **before** Copy; a historical row from an earlier chat reused as evidence; 500 because 1178 is not on core (#2573 still open).

### R-10 — MCP diagnostics returns the same row (check 5)

**Given** the row from R-9  
**When** the operator calls `builtin_chats_diagnostics` for that chat  
**Then** the tool returns a row whose **id** and **timestamp** match R-9 (same chat, same Copy). Identifier or timestamp mismatch is a fail.

This BA executor **does not** have `builtin_chats_diagnostics` in catalog — only the operator’s work-mode session can run this check.

### R-11 — Work-mode identity; developer key is not a pass (check 6)

**Given** the work-mode session used for R-6–R-10  
**When** the operator resolves the session key’s role

- If the role is the expected **work-mode / owner** identity (not `developer`): record the **role name only**. Auth-store inspect is optional.
- If the role is still `developer`: inspect VS Code secret `auth:bfk:v2:` and record **only** `{ present | stale | missing }` plus the resolved **role**. **Do not paste, log, or screenshot key material, hashes, or the full cache key.**

**Fail-closed:** a developer-resolved work-mode key is **not** a pass, even if R-6–R-10 look green. Documenting the inspect result is required and still a **fail** until identity is work-mode. Cache schema is `KEY_CACHE_SCHEMA.bfk = 2` (`auth:bfk:v2:<hash>`); a v1 leftover is `stale`.

This container resolving as `developer` is expected and is **not** the R6 auth check.

### R-12 — Evidence that must be written

Pass requires **all** of the following on **#2597** (and a pointer from this ticket), not only in chat:

1. Copy excerpt that visibly includes the two literal lines from R-8.
2. Diagnostics **row id** + **timestamp**, shown equal between `brain_chat_diagnostics` and `builtin_chats_diagnostics`.
3. Session **role** (and `auth:bfk:v2:` present/stale/missing if the role was `developer`).
4. Explicit statement that work-mode chat did **not** return manager-role required.

Absence of any item = R6 not passed. This BA Requirements section is **not** that evidence.

### R-13 — Scope firewall

| In | Out |
|---|---|
| R6 contract on this ticket PRD | Operator-console Live re-test product (rejected PM draft) |
| Pointer to #2574 / #2594 / #2595 / #2597 | Re-filing / re-staffing the subjects |
| | Driving R6 from manager UI or a developer container |
| | Pasting `auth:bfk:v2:` material |
| | Treating a developer-resolved key as success |
| | Product code, VSIX bump, migrations, `DONE.md` |
| | Shipping `specs/tasks/task-2567.md` or any conceptual Copy/diagnostics stand-in |
| | Merging `builderforce/task-2594` or `builderforce/task-2595` |

### R-14 — Roles on this ticket

- **BA (this section):** contract only. No VS Code work-mode client in this executor; R-6–R-11 are **unexecuted** here.
- **Architect:** no product design. If anything is designed, it is the operator evidence shape (R-12), not a new API.
- **Developer:** **no product patch.** Do not open a feature PR. Do not implement Copy, diagnostics, staffing, or an operator-console live-re-test action in the bound repo as a substitute for the live checks.
- **QA / Validator:** pass **only** when R-6–R-12 are evidenced from a work-mode VS Code session on #2597. This BA run is not that evidence.
- **Operator:** the only producer who can pass R6.

### R-15 — Pass / fail matrix

| Check | Pass | Fail |
|---|---|---|
| Subjects | #2594 Ada and #2595 John Coder staffed (already) | Re-filed, unstaffed, or wrong agent |
| 1 | Work-mode chat, no manager-role required | Manager-role required or equivalent |
| 2 | `/as Ada` → #2594 and `/as John Coder` → #2595 | Wrong surface, wrong ticket, or wrong agent |
| 3 | Copy payload contains both literal Staffing lines | Either line missing or paraphrased |
| 4 | `brain_chat_diagnostics` row **after** Copy | Missing, predating Copy, or 1178 absent |
| 5 | `builtin_chats_diagnostics` same id + timestamp | Missing or mismatched |
| 6 | Session is work-mode identity | Developer-resolved key treated as success |

**All six live checks must hold** on top of the two staffed subjects. Partial green is a fail.

_— authored by Ada (business-analyst) · 2026-09-16 · execution 31720_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:54:30.574Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:55:14.436Z · execution #31720

BA (execution 31720): authored Requirements R-1–R-15 and rewrote Problem & Goal, Target users, Scope, Functional requirements, Acceptance criteria, and Out of scope. The PM “operator-console Live re-test” draft is rejected — this ticket is PRD 27 R6 (work-mode VS Code on project 11 against #2594 Ada / #2595 John Coder). No product-repo patch. This executor has no work-mode VS Code chat, no Copy control, and no `builtin_chats_diagnostics` — R-6–R-11 are unexecuted here. Pass producer is the operator on #2597. Precondition #2573 (R5 / mig 1178) is still `in_progress`; if 1178 is not on core, R-9/R-10 fail closed. Do not treat this sign-off as an R6 pass. Branch file `specs/tasks/task-2567.md` was created earlier on this execution; delete_file failed (GitHub API 403 rate limit). That stub must not ship — drop it before any PR.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:16:41.871Z · execution #31852

## PM product lock — Kevin BA/PM/PO (Durable) · 2026-09-16 · execution 31852

BA R-1–R-15 adopted **unchanged**. Frozen as P0–P6. Quality gate only — **no product-repo patch**. Architect owns Design. This sign-off is **not** an R6 pass.

### P0 — Contract identity (R-1, R-13, R-14)
This ticket is the PRD 27 **R6 contract**, child of epic #2562. It is **not** a new operator-console “Live re-test” product. The discarded PM draft (live-safe catalog, confirmation UX, rate limits, production probe runner) stays **rejected**. Developers must **not** implement Copy, diagnostics, staffing, or an operator-console action on `builderforce/task-2567`. No API / VSIX / brain-embedded / frontend patch. No `DONE.md` (R7). No version bump (R3). No VSIX package (R4).

### P1 — Subjects already staffed (R-4, R-5)
Confirmed live this run:
- **#2594** — `Staffing:/as Ada` — `assignedAgentRef=fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6` — status `ready`
- **#2595** — `Staffing:/as John Coder` — `assignedAgentRef=658608ba-59ab-4ec3-873d-211a89ea000f` — status `ready`
- **#2574** filed them (execution 31660). Re-filing is a **scope fail**.
- Fixture-noise branches `builderforce/task-2594` and `builderforce/task-2595` are **not** R6 evidence and must not be merged as this ticket.

### P2 — Operator live checks only on #2597 (R-3, R-6–R-11)
Sole pass actor: operator Sean Hogg (`assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) on **#2597** (status `ready`). Surface: VS Code **work-mode** Brain chat on **project 11**. This PM executor, any developer container, manager UI, and Web Brain **cannot** satisfy R-6–R-11. Six checks, all required (R-15): (1) no `manager role required`; (2) `/as Ada` → #2594 and `/as John Coder` → #2595; (3) Copy payload contains both **literal** Staffing lines; (4) `brain_chat_diagnostics` row **after** Copy; (5) `builtin_chats_diagnostics` same id + timestamp; (6) work-mode identity (developer-resolved key = **fail**).

### P3 — Literal Copy lines (R-8)
Byte-for-byte lines (leading/trailing whitespace ignored; spelling and slash required):

```
Staffing:/as Ada
Staffing:/as John Coder
```

Near-misses fail: `Staffing: as Ada`, `as Ada` without `Staffing:/as`, wrong case, only one line, JSON field without the lines, OS clipboard of a selection that skips the product Copy report. Trailing `ChatDiagnosticsReport` fenced JSON (`schemaVersion: 1`) is supporting only — it does **not** replace the two lines.

### P4 — Diagnostics + R5 precondition (R-2, R-9, R-10)
#2573 (R5 apply `1177` then `1178` on core) is still **`in_progress`**. If 1178 is not on core, R-9/R-10 500 and R6 **fails closed**. Do not apply migrations from this ticket. Physical apply remains #2573 / #2566.

### P5 — Identity fail-closed (R-11)
A developer-resolved work-mode key is **not** a pass even if R-6–R-10 look green. If role is still `developer`, inspect `auth:bfk:v2:` and record only `{ present | stale | missing }` plus the resolved **role**. Do not paste key material, hashes, or the full cache key. This container resolving as `developer` is expected and is **not** the R6 auth check.

### P6 — Evidence + firewall (R-12–R-15)
Pass evidence lands on **#2597** (Copy excerpt with both Staffing lines; diagnostics row id + timestamp equal across store and MCP; session role; explicit no-manager-role-required). This PRD is the contract, not that evidence.

**Repo action this lane:** drop `specs/tasks/task-2567.md` (forbidden dump). Do not open a feature PR. Do not ship a conceptual Copy/diagnostics stand-in. Architect owns Design — do not author it here. Developer lane: **no product patch** (R-14). QA/Validator: pass **only** when R-6–R-12 are evidenced from work-mode VS Code on #2597.

Withdrawn: any AC that would treat this PM sign-off, a BA run, or a developer-container 403 as R6 passed.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:18:19.286Z · execution #31851

**PM product lock · Kevin (product-manager) · 2026-09-16 · execution 31852.** BA R-1–R-15 adopted **unchanged** as P0–P6 on Functional requirements / Acceptance criteria. No product-repo patch; no operator-console Live re-test feature; Architect owns Design. Pass producer = operator on **#2597** (Sean `22c5fd96-973a-4980-ba74-77c09dad1604`). This sign-off is **not** an R6 pass. Precondition **#2573** still `in_progress` (1178); R-9/R-10 fail closed if 1178 is absent. `specs/tasks/task-2567.md` must not ship — drop before any PR (this run’s delete was blocked by a sibling PM lease on `*`; do not merge the stub). Do not re-file #2594/#2595.