> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2597
> _Each agent that updates this PRD signs its change below._

# PRD 27 R6 remainder — VS Code work-mode Copy + diagnostics (operator)

**Ticket:** operator remainder of #2574 (R6)  
**Project:** 11  
**Mode:** operator-only (work-mode VS Code chat)  
**Status:** WIP  

## Problem & Goal

The developer container filed and staffed the two subject tickets under #2574 but cannot drive VS Code work-mode chat, Copy, or inspect `auth:bfk:v2:`. R6 therefore remains incomplete until an operator proves work-mode identity, slash-staffing, Copy payload fidelity, and post-Copy diagnostics in the real VS Code work-mode surface.

**Goal:** From work-mode VS Code chat (not manager UI), staff both subjects, Copy a payload that contains the literal staffing lines, confirm matching diagnostics after Copy, and only then pass. A session key that still resolves as `developer` is not a pass.

## Target users / ICP roles

- **Operator** — sole actor for this remainder; drives work-mode VS Code chat, Copy, and optional auth inspection.
- **Ada** (`assignedAgentRef=fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6`) — staffed subject #2594.
- **John Coder** (`assignedAgentRef=658608ba-59ab-4ec3-873d-211a89ea000f`) — staffed subject #2595.
- **Developer container** — out of this remainder; already filed/staffed tickets and cannot complete the six checks.

## Scope

In-scope work is exclusively the six operator checks below, executed in work-mode VS Code chat against project 11 subjects #2594 and #2595 (parent #2574), plus evidence recorded on this ticket.

## Functional requirements

1. Open work-mode VS Code chat and confirm the response is **not** manager-role required (or equivalent).
2. Invoke `/as Ada` against #2594 and `/as John Coder` against #2595.
3. Copy the chat/report. The copied payload **must** include the literal lines `Staffing:/as Ada` and `Staffing:/as John Coder`.
4. Confirm a `brain_chat_diagnostics` row exists with timestamp **after** the Copy action.
5. Confirm `builtin_chats_diagnostics` returns that same row (matching identifiers and timestamps).
6. If the session key still resolves as `developer`, inspect `auth:bfk:v2:` and record whether work-mode identity is present, stale, or missing. Do not paste key material. Do not treat a developer-resolved key as a pass.

## Acceptance criteria

Pass **only if all six hold**. Evidence on this ticket must include:

- Copy excerpt containing the literal lines `Staffing:/as Ada` and `Staffing:/as John Coder`.
- Diagnostics row id and timestamp (post-Copy), shown to match between `brain_chat_diagnostics` and `builtin_chats_diagnostics`.
- Auth prefix/role if the session key was checked (no key material).

Any of the following is a fail: manager-role required (or equivalent) in work-mode chat; Copy payload missing either Staffing line; diagnostics row missing, predating Copy, or mismatched across the two diagnostics surfaces; developer-resolved session key treated as success.

## Out of scope

- Re-filing or re-staffing #2594 / #2595 from the developer container or manager UI.
- Driving this remainder from manager UI.
- Pasting or logging `auth:bfk:v2:` key material.
- Treating a developer-resolved session key as a pass.
- Work beyond the six checks and the evidence record on this ticket.

## Requirements

This ticket is an **operator remainder**, not a product change. Requirements are the six live checks the operator must satisfy in **work-mode VS Code chat** against already-filed subjects #2594 and #2595. No API, VSIX, brain-embedded, or frontend patch belongs on `builderforce/task-2597`. A docs-only `specs/tasks/task-2597.md` is not evidence — drop it.

**Precondition (not this ticket):** PRD 27 **R5** (#2573) must have applied core migrations `1177_coordination_requires_manager.sql` and `1178_brain_chat_diagnostics.sql`. If 1178 is absent, R-6/R-7 will 500 and this remainder fails closed. Do not apply migrations from this ticket.

**Already done (not this ticket):** Developer container on #2574 filed and staffed the two subjects. Do not re-file or re-staff them from the manager UI or a developer container.

### R-1 — Actor and surface

| Item | Value |
|---|---|
| Sole actor | Operator (Sean Hogg, `assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) |
| Surface | VS Code **work-mode** Brain chat (installed VSIX; PRD 27 packaged `builderforce-ai-2026.9.75`) |
| Project | **11** only |
| Parent | #2574 (PRD 27 R6) |
| Not a valid surface | Manager UI, this cloud/developer-container executor, Web Brain, a session that still resolves as `developer` |

This BA run, any developer-container run, and any manager-UI action **cannot** satisfy R-3–R-8. They may only record that they could not.

### R-2 — Subjects (fixtures; no product work)

| Ticket | Staffing line (literal) | assignedAgentRef | Agent |
|---|---|---|---|
| **#2594** | `Staffing:/as Ada` | `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6` | Ada (Sr. Product Mgr) |
| **#2595** | `Staffing:/as John Coder` | `658608ba-59ab-4ec3-873d-211a89ea000f` | John Coder (Durable) |

Both tickets instruct dispatched agents: confirm staffing and **stop**. Branches `builderforce/task-2594` and `builderforce/task-2595` (fixture-noise BA commits) are **not** R6 evidence. Do not merge them as this remainder.

### R-3 — Work-mode chat is admitted (check 1)

**Given** the operator opens a **new** work-mode VS Code chat bound to project 11  
**When** they send any turn (including the `/as` invocations in R-4)  
**Then** the response is **not** `manager role required`, not HTTP 403/502 wrapping that error, and not an equivalent coordination-gate refusal.

Fail: any such refusal in work-mode chat. A developer-container 403 on kanban routes is **out of band** and does not pass or fail this check.

### R-4 — Slash-staff both subjects (check 2)

**Given** R-3 holds  
**When** the operator invokes, in that same work-mode chat:

1. `/as Ada` against **#2594**
2. `/as John Coder` against **#2595**

**Then** each invocation targets the named ticket and the named agent. Order is not significant. Re-filing the tickets is a fail of scope, not a substitute.

Fail: invocations from manager UI; `/as` against any other ticket; staffing a different agent.

### R-5 — Copy payload contains both literal Staffing lines (check 3)

**Given** R-4 completed in that chat  
**When** the operator uses the chat **Copy** control (the product Copy that POSTs diagnostics — not OS clipboard of a selection that skips the report)  
**Then** the copied payload contains **both** of these **literal** lines, byte-for-byte as a line (leading/trailing whitespace ignored, spelling and slash required):

```
Staffing:/as Ada
Staffing:/as John Coder
```

Near-misses that **fail**: `Staffing: as Ada`, `as Ada` without the `Staffing:/as` prefix, `Staffing:/as ada`, only one of the two lines, a staffing summary JSON field without the lines, or a copied fragment that is not the Copy report.

The Copy report may also carry a trailing `ChatDiagnosticsReport` fenced JSON block with `schemaVersion: 1` (PRD 27 §6.5). That block is supporting evidence; it does **not** replace the two Staffing lines.

### R-6 — Diagnostics row written after Copy (check 4)

**Given** the Copy in R-5  
**When** the operator inspects `brain_chat_diagnostics` for that chat  
**Then** a row exists whose `created_at` (or equivalent timestamp) is **strictly after** the Copy action time.

Record: **row id** and **timestamp**.

Fail: no row; row timestamp **before** Copy; a historical row from an earlier chat reused as evidence; 500 because 1178 is not on core (#2573 still open).

### R-7 — MCP diagnostics returns the same row (check 5)

**Given** the row from R-6  
**When** the operator calls `builtin_chats_diagnostics` for that chat  
**Then** the tool returns a row whose **id** and **timestamp** match R-6 (same chat, same Copy). Identifier and timestamp mismatch is a fail.

This BA executor **does not** have `builtin_chats_diagnostics` in catalog — only the operator’s work-mode session can run this check.

### R-8 — Work-mode identity; developer key is not a pass (check 6)

**Given** the work-mode session used for R-3–R-7  
**When** the operator resolves the session key’s role  

- If the role is the expected **work-mode / owner** identity (not `developer`): record the **role name only**. Auth-store inspect is optional.
- If the role is still `developer`: inspect VS Code secret `auth:bfk:v2:` and record **only** `{ present | stale | missing }` plus the resolved **role**. **Do not paste, log, or screenshot key material, hashes, or the full cache key.**

**Fail-closed:** a developer-resolved work-mode key is **not** a pass, even if R-3–R-7 look green. Documenting the inspect result is required and still a **fail** until identity is work-mode. Cache schema is `KEY_CACHE_SCHEMA.bfk = 2` (`auth:bfk:v2:<hash>`); a v1 leftover is `stale`.

### R-9 — Evidence that must be written on this ticket

Pass requires **all** of the following on **this** ticket (PRD Test Evidence / a dated operator note), not only in chat:

1. Copy excerpt that visibly includes the two literal lines from R-5.
2. Diagnostics **row id** + **timestamp**, shown equal between `brain_chat_diagnostics` and `builtin_chats_diagnostics`.
3. Session **role** (and `auth:bfk:v2:` present/stale/missing if the role was `developer`).
4. Explicit statement that work-mode chat did **not** return manager-role required.

Absence of any item = remainder not passed.

### R-10 — Scope firewall

| In | Out |
|---|---|
| Six operator checks in work-mode VS Code chat | Re-filing / re-staffing #2594 / #2595 |
| Evidence record on this ticket PRD | Driving the remainder from manager UI or a developer container |
| Auth prefix/role only if key was inspected | Pasting `auth:bfk:v2:` material |
| | Treating a developer-resolved key as success |
| | Product code, VSIX bump, migrations, `DONE.md` (that is R7 / #2574) |
| | Shipping `specs/tasks/task-2597.md` or any conceptual Copy/diagnostics stand-in |

### R-11 — Roles on this ticket

- **BA (this section):** contract only. No VS Code work-mode client in this executor; R-3–R-8 are **unexecuted** here.
- **Architect:** no product design. If anything is designed, it is the operator evidence shape (R-9), not a new API.
- **Developer:** **no product patch.** Do not open a feature PR. Do not implement Copy, diagnostics, or staffing in the bound repo as a substitute for the live checks.
- **QA / Validator:** pass **only** when R-3–R-9 are evidenced from a work-mode VS Code session. This BA run is not that evidence.
- **Operator:** the only producer who can pass the ticket.

### R-12 — Pass / fail matrix

| Check | Pass | Fail |
|---|---|---|
| 1 | Work-mode chat, no manager-role required | Manager-role required or equivalent |
| 2 | `/as Ada` → #2594 and `/as John Coder` → #2595 | Wrong surface, wrong ticket, or wrong agent |
| 3 | Copy payload contains both literal Staffing lines | Either line missing or paraphrased |
| 4 | `brain_chat_diagnostics` row **after** Copy | Missing, predating Copy, or 1178 absent |
| 5 | `builtin_chats_diagnostics` same id + timestamp | Missing or mismatched |
| 6 | Session is work-mode identity | Developer-resolved key treated as success |

**All six must hold.** Partial green is a fail.

_— authored by Ada (business-analyst) · 2026-09-16 · execution 31701_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:43:45.482Z_

## Design

This ticket has **no product architecture**. Copy, persisted diagnostics, slash-staffing, and work-mode identity already exist in PRD 27 (`specs/builderforce/27-prd-staffed-work-and-chat-diagnostics.md`). They must not be redesigned, reimplemented, or patched on `builderforce/task-2597`.

Shipped seams this remainder **observes** (read-only; not in scope to change):

| Seam | Existing contract |
|---|---|
| Product Copy | POSTs `ChatDiagnosticsReport` (`schemaVersion: 1`) to `/api/brain/chats/:id/diagnostics` (mig 1178, 512 KB clamp, 25 per chat) |
| Store | `brain_chat_diagnostics` |
| MCP readback | `chats.diagnostics` advertised as `builtin_chats_diagnostics` |
| Auth cache | `KEY_CACHE_SCHEMA.bfk = 2` → secret prefix `auth:bfk:v2:` (v1 leftover = stale) |
| Staffing lines | Copy report must contain the literal `Staffing:/as <agent>` lines from the work-mode chat |

The only design artifact is the **operator evidence contract** (R-9): a fill-in record the operator writes on **this ticket PRD** (Test Evidence section and/or a dated operator note). Chat-only evidence is insufficient. A repo dump (`specs/tasks/task-2597.md`) is not evidence and must not ship.

### 1. Surfaces (existing; not to be built)

| Checks | Surface | Invalid surface |
|---|---|---|
| 1–3 | VS Code **work-mode** Brain chat, project **11**, packaged VSIX `builderforce-ai-2026.9.75` | Manager UI, Web Brain, this cloud/developer-container executor, a session that still resolves as `developer` |
| 3 | Product **Copy** control (the control that POSTs diagnostics) | OS clipboard of a selection that skips the report |
| 4 | `brain_chat_diagnostics` row for **that** chat, timestamp strictly after Copy | A historical row from another chat; HTTP 500 because 1178 is absent (#2573) |
| 5 | `builtin_chats_diagnostics` in the **same** work-mode session | This architect/BA executor (tool not in catalog here) |
| 6 | Session **role** name; only if `developer`, inspect `auth:bfk:v2:` as `{present \| stale \| missing}` | Pasting key material, hashes, or the full cache key |

### 2. Evidence record shape (R-9)

Operator fills **all** blocks on this ticket. Absence of any block = remainder not passed. Partial green is a fail (R-12).

```
#2597 operator evidence — work-mode VS Code (project 11)

A. Surface admission (R-3 / check 1)
   chatId: <id>
   workMode: true
   managerRoleRequired: false
   statement: "Work-mode chat did not return manager-role required (or equivalent)."

B. Slash-staff (R-4 / check 2)
   /as Ada         → ticket #2594  assignedAgentRef=fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6
   /as John Coder  → ticket #2595  assignedAgentRef=658608ba-59ab-4ec3-873d-211a89ea000f

C. Copy payload excerpt (R-5 / check 3)
   copyAt: <ISO-8601>
   excerpt: <Copy report fragment that contains BOTH literal lines, each as its own line;
            leading/trailing whitespace ignored; spelling and slash required>
     Staffing:/as Ada
     Staffing:/as John Coder
   optional: trailing ChatDiagnosticsReport fenced JSON with schemaVersion: 1
             (supporting only; does not replace the two Staffing lines)

D. Diagnostics identity (R-6 / R-7 / checks 4–5)
   copyAt:                    <ISO-8601, same as C>
   brain_chat_diagnostics:    { id: <row id>, created_at: <ISO-8601> }
   builtin_chats_diagnostics: { id: <same row id>, timestamp: <same created_at> }
   match: true
   created_at > copyAt: true

E. Session identity (R-8 / check 6)
   role: <work-mode / owner role name — never paste key material>
   if role == developer:
     auth:bfk:v2: <present | stale | missing>
     result: FAIL   # developer-resolved key is not a pass even if A–D look green
```

**Check 3 near-misses (fail):** `Staffing: as Ada`; `as Ada` without the `Staffing:/as` prefix; `Staffing:/as ada`; only one of the two lines; a `staffing` JSON field without the lines; a copied fragment that is not the Copy report.

### 3. Fail-closed / no-build rules

- Precondition **#2573** (mig `1178_brain_chat_diagnostics.sql`) is still `in_progress` at Design time. If 1178 is not on core, R-6/R-7 500 and this remainder **fails closed**. Do not apply migrations from this ticket.
- **Developer:** no product patch, no feature PR, no Copy/diagnostics/staffing implementation in the bound repo as a substitute for the live checks.
- **Architect:** no new API, schema, VSIX bump, or `DONE.md` (R7 lives on #2574).
- Fixtures **#2594** / **#2595** are already filed and staffed. Do not re-file or re-staff. Branches `builderforce/task-2594` and `builderforce/task-2595` are not R6 evidence.
- This cloud/architect executor **cannot** execute R-3–R-8. Authoring this Design is **not** an R6 pass.

### 4. Downstream consumers

| Role | Uses this Design for |
|---|---|
| Operator (Sean Hogg, `assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) | Fill blocks A–E on this ticket after the six live checks |
| Developer | Confirm there is no product work; do not invent a stand-in |
| QA / Validator | Pass **only** when A–E are present and R-12 all-six hold; this Design is the rubric, not the evidence |

_— authored by CTO (architect) · 2026-09-16 · execution 31843_

_— rewritten by CTO · 2026-09-16T16:15:51.479Z_

## Implementation Notes

**No product patch. No feature PR. No Copy/diagnostics/staffing implementation.** This is the developer deliverable for #2597.

Ticket #2597 is an operator remainder of PRD 27 R6 (#2574). R-10 / R-11 / Design §3 forbid API, VSIX, brain-embedded, frontend, migrations, `DONE.md`, and `specs/tasks/task-2597.md`. Copy, persisted diagnostics, slash-staffing, and work-mode identity already exist in PRD 27; they must be **observed** in work-mode VS Code chat, not rebuilt on `builderforce/task-2597`.

### What this developer run did

| Action | Result |
|---|---|
| Product-repo change | **None.** Inventing a Copy/diagnostics/staffing stand-in would violate R-10 and the Architect lock (exec 31843). |
| Feature PR | **Not opened.** R-11: “Developer: no product patch. Do not open a feature PR.” |
| `specs/tasks/task-2597.md` | **Not shipped.** Architect deleted it in execution 31843. This run did not recreate it. |
| Bound repo this run | **None checked out** (`git_sync_latest` / `run_command` / `list_files` all returned “no repository bound”). Even if a tree were bound, no product edit is in scope. |
| R-3–R-8 (six live checks) | **Unexecuted.** This cloud/developer-container executor has no work-mode VS Code chat, no product Copy control, and no `builtin_chats_diagnostics` in catalog. Session role here is `developer` — R-8 fail-closed; documenting that is required and is still not a pass. |
| Fixtures #2594 / #2595 | **Untouched.** Do not re-file or re-staff. Branches `builderforce/task-2594` and `builderforce/task-2595` are not R6 evidence. |

### Operator evidence still required (R-9 blocks A–E)

This Implementation Notes section is **not** Test Evidence and **not** an R6 pass. Operator Sean Hogg (`assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) must fill A–E on this ticket after the six live checks in work-mode VS Code chat (project 11, packaged VSIX `builderforce-ai-2026.9.75`):

1. Work-mode chat admitted (not manager-role required).
2. `/as Ada` → #2594; `/as John Coder` → #2595.
3. Product Copy payload contains both literal lines `Staffing:/as Ada` and `Staffing:/as John Coder`.
4. `brain_chat_diagnostics` row **after** Copy (id + timestamp).
5. `builtin_chats_diagnostics` same id + timestamp.
6. Session is work-mode / owner identity — a developer-resolved key is not a pass.

### Precondition

#2573 (PRD 27 R5 / mig `1178_brain_chat_diagnostics.sql`) remains a fail-closed gate for R-6/R-7. Do not apply migrations from this ticket.

_— authored by Bob Developer (V2 Container) · 2026-09-16 · execution 31907_

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T16:25:34.477Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:44:10.561Z · execution #31701

BA (execution 31701): authored Requirements R-1–R-12 on this ticket PRD. This executor has no work-mode VS Code chat, no Copy control, and no `builtin_chats_diagnostics` tool — R-3–R-8 are unexecuted here and remain operator-only. Deleted stray `specs/tasks/task-2597.md` (docs-only stub; not evidence). No product-repo patch. Precondition: #2573 (R5 / mig 1178) is still `in_progress`; if 1178 is not on core, R-6/R-7 fail closed. Do not treat this sign-off as an R6 pass.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T15:51:33.961Z · execution #31733

**PM scope lock (todo lane) — Kevin BA/PM/PO · 2026-09-16.** Product contract is R-1–R-12 as authored. This ticket is operator-only: no API/VSIX/brain-embedded/frontend patch, no feature PR, no `specs/tasks/task-2597.md`. Downstream architect/developer slots must not invent product work; Design (if authored) is the R-9 evidence shape only.

This cloud/developer-container executor **cannot** satisfy R-3–R-8 (no work-mode VS Code chat, no Copy control, no `builtin_chats_diagnostics`). Session here is not the operator surface. R-3–R-9 remain **unexecuted**; this note is **not** an R6 pass.

Fixtures unchanged: #2594 Ada (`fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6`), #2595 John Coder (`658608ba-59ab-4ec3-873d-211a89ea000f`). Do not re-file or re-staff. Precondition #2573 (mig 1178) is still `in_progress` — R-6/R-7 fail closed if 1178 is absent from core.

Sole producer who can pass: operator Sean Hogg (`assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) in work-mode VS Code chat on project 11. GitHub API was rate-limited this run (403); `search_code` for `task-2597` under `specs` returned 0 matches (stub already dropped by BA).

---

### Update — Product Manager · 2026-09-16T16:01:33.976Z · execution #31778

**PO ready-lane freeze — Product Owner · 2026-09-16 · execution #31778.** Scope + acceptance criteria approved: R-1–R-12 are the product contract. Operator-only six checks in work-mode VS Code chat; no product-repo patch, no feature PR, no `specs/tasks/task-2597.md`. Fixtures #2594 (Ada) and #2595 (John Coder) confirmed staffed as specified. This sign-off is **not** an R6 pass — R-3–R-9 stay unexecuted until operator Sean Hogg (`assignedUserId=22c5fd96-973a-4980-ba74-77c09dad1604`) records Copy excerpt, post-Copy diagnostics id+timestamp (both surfaces), session role, and no manager-role required. #2573 (mig 1178) still `in_progress`; R-6/R-7 fail closed if 1178 is absent. Downstream architect/developer must not invent Copy/diagnostics/staffing product work.

---

### Update — CTO · 2026-09-16T16:16:15.510Z · execution #31843

**Architect ready-lane lock — CTO · 2026-09-16 · execution #31843.** Design authored: operator evidence shape (R-9) only — blocks A–E (admission, slash-staff #2594/#2595, Copy excerpt with both literal Staffing lines, diagnostics id+timestamp match across `brain_chat_diagnostics` and `builtin_chats_diagnostics`, session role / `auth:bfk:v2:` present|stale|missing). No product API, no VSIX, no migrations, no feature PR. Deleted stray `specs/tasks/task-2597.md` from `builderforce/task-2597` (not evidence; R-10). This sign-off is **not** an R6 pass — R-3–R-9 stay unexecuted until operator Sean Hogg fills A–E in work-mode VS Code chat. #2573 (mig 1178) still `in_progress`; R-6/R-7 fail closed if 1178 is absent. Developer must not invent Copy/diagnostics/staffing product work.