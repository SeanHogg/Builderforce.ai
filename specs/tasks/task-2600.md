> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2600
> _Each agent that updates this PRD signs its change below._

# PRD: Bind a readable checkout for ticket 2554

**Status:** WIP  
**Ticket:** #2554  
**Blocked work:** PRD 27 survey loop B0–B5  
**Repo:** SeanHogg/Builderforce.ai  
**Branch:** `builderforce/task-2554`

## Problem & Goal

The developer container for #2554 can `write_file` onto `seanhogg/builderforce.ai` on `builderforce/task-2554`, but `list_files`, `read_file`, `git_*`, and `run_command` fail with “no repository bound” / “no repository checked out”.

B0–B5 cannot be implemented without reading live sources (`CONNECTED_CANVAS_ACTIONS`, the form surface registry, formObject publish, `summarizeForm`, `CanvasFormSurface`, `peopleObjects`, `growthApi.createCampaign`, `CAMPAIGN_BLOCKERS`, `marketing_audience_members`, and the five next-intl catalogs). Blind overwrites already destroyed production code (`0267ffd` probed `canvasChange.ts`).

**Goal:** Bind a full checkout of SeanHogg/Builderforce.ai as branch `builderforce/task-2554` so the next developer run can search, edit in place, run `pnpm run check` in `frontend`, and open a real PR. This PRD unblocks that checkout only.

## Target users / ICP roles

- Downstream developer agents executing #2554 / PRD 27 B0–B5
- Human reviewers who must inspect a real branch and CI, not blind writes

No end-customer surface is in scope.

## Scope

In scope:

- Bind SeanHogg/Builderforce.ai to the developer container as a complete, readable working tree
- Check out (or create from current default/base) branch `builderforce/task-2554`
- Enable `list_files`, `read_file`, search, in-place edit, `git_*`, and `run_command` against that working tree
- Preserve existing production files; no speculative overwrites
- Leave enough workspace for a later run to execute `pnpm run check` in `frontend` and open a PR from `builderforce/task-2554`

Out of this ticket’s product surface, but required as environment preconditions:

- Auth/credentials sufficient to clone/fetch the private repo and push the task branch
- Working tree includes the files B0–B5 must read (listed in Acceptance criteria)

## Functional requirements

1. **Repository bind.** The container has a single bound git repository: SeanHogg/Builderforce.ai (same remote as `seanhogg/builderforce.ai`).
2. **Branch checkout.** HEAD is `builderforce/task-2554`. If the branch exists remotely, check it out and fast-forward or reset only as needed to match remote without discarding unpublished task work. If it does not exist, create it from the current integration base and track origin.
3. **Full working tree.** The checkout is a complete clone (or equivalent sparse-disabled checkout), not write-only stubs. `list_files` at repo root returns the real tree.
4. **Read path.** `read_file` succeeds for any path present on the branch, including TypeScript/TSX sources, registries, and the five next-intl catalogs.
5. **Write path.** `write_file` / in-place edit updates the bound working tree (not an unbound shadow). Writes are git-visible (`git status`, `git diff`).
6. **Git operations.** `git status`, `git log`, `git diff`, `git add`, `git commit`, and `git push` of `builderforce/task-2554` succeed against the bound repo.
7. **Command execution.** `run_command` runs in the checkout. From `frontend`, `pnpm run check` is invokable (install/tooling failures are environment bugs to report, not “no repository checked out”).
8. **No blind mutation.** Binding and checkout must not rewrite application sources. In particular, do not touch `PHASE_SURFACES` and do not re-probe or overwrite `canvasChange.ts` or other production files as a side effect of binding.
9. **Handoff.** After bind, a subsequent developer run can search the tree, edit the B0–B5 files in place, run frontend check, and open a PR — without another bind step.

## Acceptance criteria

- [ ] Container reports a bound repository; “no repository bound” / “no repository checked out” no longer appear on `list_files`, `read_file`, `git_*`, or `run_command`.
- [ ] `git rev-parse --abbrev-ref HEAD` is `builderforce/task-2554`.
- [ ] `git remote -v` points at SeanHogg/Builderforce.ai.
- [ ] `list_files` at repo root shows the real project layout (not an empty or write-only mount).
- [ ] `read_file` succeeds for all of:
  - `CONNECTED_CANVAS_ACTIONS`
  - form surface registry
  - formObject publish
  - `summarizeForm`
  - `CanvasFormSurface`
  - `peopleObjects`
  - `growthApi.createCampaign`
  - `CAMPAIGN_BLOCKERS`
  - `marketing_audience_members`
  - the five next-intl catalogs
- [ ] A no-op `run_command` (e.g. `pwd` / `git status`) runs in the working tree and shows a clean or expected status — not a bind error.
- [ ] `pnpm run check` can be launched from `frontend` (command starts; bind is not the failure mode).
- [ ] Working tree matches the remote branch content; production files are intact vs. origin (no new destructive writes from this bind).
- [ ] `PHASE_SURFACES` is byte-identical to the checked-out branch tip; this ticket does not modify it.
- [ ] Next developer run can search, edit in place, commit, push `builderforce/task-2554`, and open a real PR.

## Out of scope

- Implementing PRD 27 survey loop B0–B5 (wiring, copy, or behavior)
- Conceptual survey wiring dumps, design notes, or speculative patches
- Any change to `PHASE_SURFACES`
- Blind `write_file` of canvas/form/campaign/i18n sources to “unblock” reads
- Repairing or rewriting `canvasChange.ts` / commit `0267ffd` beyond not repeating that overwrite
- Frontend feature work, catalog edits, campaign API changes, or form-surface registry changes
- CI config, secrets rotation, or repo permission redesign except as required to complete the bind
- Opening the B0–B5 product PR in this ticket (handoff only: environment must make that PR possible)

## Requirements

This ticket is an **environment unblock**, not a product change. Requirements are the bind contract the architect and developer must satisfy so ticket **#2554** (PRD 27 survey loop B0–B5) can search, edit in place, run `pnpm run check` in `frontend`, and open a real PR. No survey wiring, form-surface registry change, campaign API change, catalog edit, or `PHASE_SURFACES` mutation is in scope here.

### R-1 — Target identity

- **Repository:** `SeanHogg/Builderforce.ai` (same origin as this workspace’s `seanhogg/builderforce.ai`).
- **Branch the next #2554 developer run must be checked out on:** `builderforce/task-2554`.
- **Ticket whose developer container must be readable:** #2554 (not #2600, not #2601).
- **Do not** bind a different repo, a different branch, or dump survey / canvas / campaign / i18n source onto this ticket’s PR.

Ticket #2554 already records `gitBranch = builderforce/task-2554` (4 commits ahead of `main`, no pull request). Ticket **#2600** records `gitBranch = builderforce/task-2600`. A run on #2600 may therefore see a readable working tree on **this ticket’s branch** — enough to inventory main and author this contract, **not** the success condition. Success is a developer-container run **on #2554**.

Project 11 (`BuilderForce.AI`) still has `githubRepoUrl = null`, `githubRepoOwner = null`, `githubRepoName = null`, `sourceControlRepoUrl = null`. That is the platform-level reason container tools fail with `no repository bound` / `no repository checked out`. The same blocker is recorded on sibling unblocks (#2588 → #2527, #2545, #2532, #2555, #2573, #2575). Binding for #2554 is an operator/container action against that ticket’s `gitBranch`; it is not a product-repo commit on #2600.

### R-2 — Bound working tree

The #2554 developer container must have a bound working copy of that repo at that branch such that:

1. `list_files` enumerates the real tree at repo root (`frontend/`, `api/`, `packages/`, `specs/`, …) — not `no repository bound` and not an empty or write-only mount.
2. `read_file` returns existing file contents (not `no repository bound`).
3. `search_code` can locate symbols in that tree.
4. `git_*` (`status`, `diff`, `log`, `rev-parse`, `add`, `commit`, `push` as already authorized for the task) succeed (not `no repository checked out` / `credential delegation absent`). `git rev-parse --abbrev-ref HEAD` is `builderforce/task-2554`. `git remote -v` points at `SeanHogg/Builderforce.ai`.
5. `write_file` / in-place edit updates the **same** bound tree; writes must appear in `git status` / `git diff` on `builderforce/task-2554`.
6. `run_command` runs in that checkout. A no-op (`pwd` / `git status`) must not fail as a bind error. From `frontend`, `pnpm run check` is invokable (install/tooling failures are environment bugs to report, not “no repository checked out”).

### R-3 — B0–B5 sources that must be readable (do not overwrite)

These paths exist on **this BA checkout of `seanhogg/builderforce.ai`** as of 2026-09-16 (`list_files` / `read_file` succeeded before a later GitHub API 403). A #2554 run must be able to **read** them before any edit. Blind `write_file` of these paths is forbidden on this ticket and on #2554 until the file has been read on the bound tree.

| Concern | Path (repo-relative) | BA inventory 2026-09-16 |
|---|---|---|
| `CONNECTED_CANVAS_ACTIONS` (also `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `canInvokeCreationObjectAction`) | `frontend/src/domains/canvas/domain/canvasChange.ts` | Present; production module (not a stub). **Do not overwrite.** Nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` is the 0267ffd two-line `export {}` probe — restore is **#2601**, not this ticket. |
| formObject publish (one mapping from form card → published form) | `frontend/src/lib/formObject.ts` | Present |
| `summarizeForm` client | `frontend/src/lib/founderOpsApi.ts` | Present |
| `summarizeForm` / form publish server | `api/src/application/collection/formPublishing.ts` | Present |
| `CanvasFormSurface` | `frontend/src/components/creation-canvas/CanvasFormSurface.tsx` | Present |
| Form surface registry | `frontend/src/components/creation-canvas/creationObjectSurfaces.ts` | Present |
| Canvas surfaces | `frontend/src/lib/canvasSurfaces.ts` | Present |
| `PHASE_SURFACES` / canvas phases | `frontend/src/lib/canvasPhases.ts` | Present. **This ticket and #2554 must not modify it.** §11.7 Ideas visibility is already shipped. |
| `peopleObjects` (B0 adds `close`) | `frontend/src/lib/peopleObjects.ts` | Present |
| People contract | `packages/creation-canvas-contract/src/people.ts` | Present |
| `growthApi.createCampaign` + `send` + `addMembersFromCrm` (B4 client; route `/api/growth/audiences/:id/members/from-crm`) | `frontend/src/lib/growthApi.ts` | Present (`createCampaign` posts to `/api/growth/campaigns`) |
| Public form `/f/<slug>` | `frontend/src/app/f/[slug]/page.tsx` | Present; unauthenticated; `?t=` recipient token |
| i18n locale source of truth | `frontend/src/i18n/config.ts` | `LOCALES = ['en', 'zh', 'es', 'fr', 'de']` |
| Five next-intl catalogs | `frontend/src/i18n/messages/{en,de,es,fr,zh}.json` | Locale set confirmed on this checkout; catalog directory matches the sibling #2588 main inventory |
| Idea log / evidence kinds (B1) | `frontend/src/lib/ideaLog.ts` | Present |
| Founder objects | `frontend/src/lib/founderObjects.ts` | Present |
| PRD 27 (wiring-only spec) | `specs/builderforce/27-prd-idea-validation-surveys.md` | Present |

**Not path-confirmed this run (GitHub `search_code` 403 after the inventory reads):** `CAMPAIGN_BLOCKERS` and the `marketing_audience_members` table/migration. After bind, the #2554 developer **must** `search_code` those symbols on the bound tree and read the hits **before** editing. Do not invent paths. B3 uses existing `growthApi.createCampaign` + `send` with `CAMPAIGN_BLOCKERS` first (no second send path). B4 is CRM → `marketing_audience_members` with `source='crm'` only, de-dupe `(audience_id, email)`, dedicated from-crm route — do not reuse POST members’ hardcoded `source:'import'`.

**Constraint:** Blind overwrite already happened once (`0267ffd` nested probe of `canvasChange.ts`). Binding and checkout must not rewrite application sources. In particular do not touch `PHASE_SURFACES` and do not re-probe `canvasChange.ts`.

### R-4 — Durability

The bind must survive the next developer-container run on **#2554** without a second manual bind: same repo, same branch, credential delegation still present. A readable #2600 checkout of main / `builderforce/task-2600` is necessary inventory, **not** sufficient proof.

### R-5 — Non-destructive

Binding must not rewrite, empty, force-push, or truncate production files. No force-push to `main`. No commit on this ticket that changes canvas, forms, campaigns, people objects, or i18n.

This ticket’s product-repo footprint must stay **empty of survey/canvas source**. The only allowed change on `builderforce/task-2600` is removing dead environment-ops docs (`specs/tasks/task-2600.md`). Do not open the B0–B5 product PR from #2600.

### R-6 — Scope firewall (this ticket)

| In | Out |
|---|---|
| Bind / re-bind checkout + restore git credential delegation for **#2554** | PRD 27 B0–B5 wiring, copy, or behavior |
| Confirm `list_files` / `read_file` / `search_code` / `git_*` / `run_command` / in-place `write_file` on `builderforce/task-2554` | New survey kind, second answer store, fifth contact store, `/surveys` dest, second send path |
| Operator notes in **this ticket PRD** only | Conceptual survey wiring dumps, design notes, or speculative patches |
| Reconcile / drop conceptual dumps **on #2554’s branch after bind** (not here) | Any change to `PHASE_SURFACES` |
| Leave enough workspace to run `pnpm run check` in `frontend` on #2554 | Repairing `canvasChange.ts` / `0267ffd` (that is **#2601**) |
| | CI config, secrets rotation, or repo-permission redesign except as required to complete the bind |

### R-7 — Acceptance (testable)

A developer-container run **assigned to #2554** can:

1. `list_files` at repo root and see the real layout (`frontend/`, `api/`, `packages/`, `specs/`).
2. `read_file` `frontend/src/domains/canvas/domain/canvasChange.ts` and receive the existing production source (`CONNECTED_CANVAS_ACTIONS` present; not a 2-line stub).
3. `read_file` each R-3 path in the table above (formObject, `summarizeForm` client/server, `CanvasFormSurface`, form surface registry, `peopleObjects`, `growthApi.ts`, `/f/[slug]`, i18n config + five catalogs, `canvasPhases.ts`).
4. `search_code` for `CONNECTED_CANVAS_ACTIONS`, `CAMPAIGN_BLOCKERS`, and `marketing_audience_members` and get hits on the bound tree (not `no repository bound`).
5. Run `git rev-parse --abbrev-ref HEAD` → `builderforce/task-2554` and `git remote -v` → `SeanHogg/Builderforce.ai`, without “no repository bound” / “no repository checked out”.
6. Run a no-op `run_command` (`pwd` / `git status`) in the working tree; bind is not the failure mode. `pnpm run check` can be **launched** from `frontend`.
7. Make a trivial in-place edit via `write_file` that shows in `git diff` on that branch, then revert it — proving write and git share one tree.
8. Open a real PR from `builderforce/task-2554` for **#2554 product work** (that PR is #2554’s, not this ticket’s).

This ticket is done when those eight are true for #2554’s container, **or** when an operator records that #2554’s container already has a readable checkout (a readable #2600 checkout of main / `builderforce/task-2600` is necessary but not sufficient proof).

### R-8 — Roles

- **Business Analyst (#2600, this section):** Bind contract, R-3 inventory, post-bind product constraints for #2554. No product files.
- **Architect (#2600):** Design how the container bind + credential delegation is restored (ticket `gitBranch`, repo binding, secret delegation, project 11 `githubRepoUrl=null`). No product design.
- **Developer (#2600):** Execute the bind / restore delegation. Do not author B0–B5 product files. Do not commit to `builderforce/task-2600` except to keep it empty of dumps.
- **QA / Validator:** Prove R-7 against a **#2554** developer run, not against this ticket’s checkout alone.

### R-9 — Post-bind product constraints (for #2554, not this ticket)

Recorded here so the first readable #2554 run does not dump a second product. **Do not implement these on #2600.**

1. **Wiring only.** Do not add a survey kind, second answer store, fifth contact store, `/surveys` dest, or second send path. Do not pick PRD 27 §11.4 / `sales_contacts` as user-owned canonical. Do not implement §11.1–11.6/11.8 unless needed for the wire.
2. **`PHASE_SURFACES` stays untouched.** §11.7 Ideas visibility is already shipped.
3. **B0:** form actions + `CONNECTED_CANVAS_ACTIONS`; object-scoped form surface (registry unique order, `AUTHORING_SURFACE`, host mount); formObject publish body + `/f/` URL; `summarizeForm` client; `CanvasFormSurface`; `peopleObjects` add `close`; i18n five catalogs. Kill: `/f/<slug>` unopenable **or** form surface not in registry.
4. **B1:** Ask people on Ideas (disabled-not-hidden); `IDEA_EVIDENCE_KINDS` includes form; `idea.tested` catalog.
5. **B2:** connect `idea.test` (draft questions from problem/segment/riskiestAssumptions onto linked form). Fix `canvas_publish_form` hint. Drop or connect `idea.explore`.
6. **B3:** Send via existing `growthApi.createCampaign` + `send`. `CAMPAIGN_BLOCKERS` first. No second send path.
7. **B4:** CRM → `marketing_audience_members` `source='crm'` only; de-dupe `(audience_id,email)`; own `sales_contacts` only (`owner_user_id`). Dedicated from-crm route — do not reuse POST members’ hardcoded `source:'import'`.
8. **B5:** collect folds `summarizeForm` counts onto the card. Founder sets validated/dropped **manually** — never auto.
9. **FR-10:** next-intl 5 locales; theme tokens; `/f/<slug>` both themes; ~360px.
10. **House:** `pnpm run check` in `frontend`. Isolate from unrelated dirty tree.
11. **#2554 branch already has dumps** (4 commits ahead of main, no PR): `specs/tasks/task-2554.md` and nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (0267ffd-class probe). First action after a readable checkout: **delete those dumps** (and let #2601 revert 0267ffd so the nested path is absent on main). Then read the R-3 files. Then wire B0–B5 against the real tree. Do not write the real `canvasChange.ts` module into the nested `Builderforce.ai/` prefix. Do not invent `CanvasChangeKind` / `applyObjectChange`.

### R-10 — Operator bind recipe (architect/developer execute; BA does not)

1. Bind project 11 (or the #2554 container) to `SeanHogg/Builderforce.ai`.
2. Check out **#2554** on `builderforce/task-2554` (not `builderforce/task-2600`, not `builderforce/task-2601`).
3. Restore git credential delegation so `list_files` / `read_file` / `search_code` / `git_*` / `run_command` work in that container.
4. Do not force-push. Do not merge #2554’s dump commits as the product PR. Do not copy survey/canvas source onto #2600.
5. Prove R-7 on a **#2554** run, then leave this ticket.

_— authored by Ada (Sr. Product Mgr) as Business Analyst · 2026-09-16 · execution #31708_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:52:15.592Z_

## Design

This ticket is an **environment unblock**, not a product design. The architect deliverable is this bind + credential-delegation contract so ticket **#2554** (PRD 27 survey loop B0–B5) can search, edit in place, run `pnpm run check` in `frontend`, and open a real PR. No survey wiring, form-surface registry change, campaign API change, catalog edit, or `PHASE_SURFACES` mutation is designed or shipped here.

### D-0 — Success identity (do not retarget)

| Item | Value |
|---|---|
| Repo to bind | `SeanHogg/Builderforce.ai` (`https://github.com/SeanHogg/Builderforce.ai`) |
| Branch the next **#2554** developer run must be checked out on | `builderforce/task-2554` |
| Ticket whose **developer container** must become readable | **#2554** (not #2600, not #2601) |
| This ticket’s own `gitBranch` | `builderforce/task-2600` — a readable tree **here** is necessary to author this contract, **not** R-7 |
| Project 11 source-control row (measured 2026-09-16, exec 31898) | `githubRepoUrl = null`, `githubRepoOwner = null`, `githubRepoName = null`, `sourceControlRepoUrl = null`, `sourceControlIntegrationId = null` |

Agent `projects.update` **cannot** set those GitHub fields. Layer A is an operator / project-settings action, not a product-repo commit. Same measured nulls as sibling unblocks (#2588 → #2527, #2590 → #2545, #2591 → #2555, #2639 → #2562). Connecting project 11 once is the **shared** Layer A step. This ticket’s success remains specifically **#2554 Layers B+C**. Do not retarget #2600 to bind every sibling.

### D-1 — Topology: three surfaces, not one

Measured this run (exec 31898) against live project/ticket/execution state:

| Surface | Who | What tools do today |
|---|---|---|
| **S1 — Durable executor** (this run, BA, PM, PO) | Cloud/durable agent on `seanhogg/builderforce.ai` | `list_files` / `read_file` succeed on **main** (R-3 inventory reconfirmed in D-8). `search_code` still 403s on GitHub API rate-limit **independently of bind**. A readable S1 is **not** R-7. |
| **S2 — #2554 developer container** (Bob, executor=`container`) | Ticket #2554, `gitBranch=builderforce/task-2554` | `write_file` succeeds as **disconnected blobs**. `list_files` / `read_file` / `git_*` / `run_command` fail with `no repository bound` / `no repository checked out`. Live container run **exec 31656** is still `running` (dispatched 2026-09-16T15:28Z; reaper-requeued after `container-op ask_human → 500`). It already dumped product-shaped files without a working tree (D-9). |
| **S3 — Project 11 source-control connection** | BuilderForce.AI project record | All GitHub URL/owner/name fields **null**. This is the platform-level reason container tools fail. |

**Ghost bind:** ticket metadata can name `seanhogg/builderforce.ai` @ `builderforce/task-2554` (`gitRepo` / `gitBranch` set; 4 commits ahead of `main`, no PR) while S2 tools still disagree. Treat tool results as truth, not the ticket’s labels.

**Write-without-read:** `write_file` talks to a blob/PR path that does **not** share a working tree with `list_files` / `read_file` / `git_*` / `run_command`. Proven by #2554 dumps (D-9) and by the earlier `0267ffd` nested probe of `canvasChange.ts` (restore is **#2601**, not this ticket). Binding is restoring **one** working tree, not enabling more blind writes.

### D-2 — Root-cause layers (restore in order)

| Layer | Failure string | What must be true |
|---|---|---|
| **A — Project GitHub connection** | `no repository bound` at project scope | Project 11 connected to `SeanHogg/Builderforce.ai`. After connect, `githubRepoUrl` / `githubRepoOwner` / `githubRepoName` are non-null and agree. |
| **B — Container working-tree checkout** | `no repository bound` / `no repository checked out` inside the **#2554** container | That container has a local working copy of the repo **on `builderforce/task-2554`**. `list_files` enumerates the real tree (`frontend/`, `api/`, `packages/`, `specs/`); `read_file` returns contents; `search_code` hits the same tree; `write_file` edits **that** tree; `run_command` runs in that checkout. |
| **C — Git credential delegation** | `credential delegation absent` / git ops fail as unbound | `git_*` (`status`, `diff`, `log`, `rev-parse`, `add`, `commit`, `push` as already authorized for the task) succeed **in the same container** against the same checkout. `git rev-parse --abbrev-ref HEAD` is `builderforce/task-2554`. `git remote -v` points at `SeanHogg/Builderforce.ai`. |

Layer A is **shared** with the sibling class. Layers B+C are **per-container**. A readable #2600 checkout does not restore #2554’s working tree.

### D-3 — Fail-closed constraints

1. **No product files on `builderforce/task-2600`.** Do not add survey wiring, canvas/form/campaign/i18n source, `PHASE_SURFACES` edits, a nested `Builderforce.ai/` prefix, `CanvasChangeKind` / `applyObjectChange`, or a substitute tree. Operator notes live **only** in this PRD. The only allowed git change on this branch is removing dead environment-ops docs (`specs/tasks/task-2600.md`).
2. **Do not force-push.** No force-push to `main`. No merge of #2554’s dump commits from this ticket. Do not open the B0–B5 product PR from #2600.
3. **Do not re-dispatch #2554** until R-7 is proven on S2. Premature dispatch burns the producer and adds more dumps. Exec **31656 is already live and dumping** — do not treat it as an R-7 proof run; do not start a second producer on top of it.
4. **Do not invent an in-repo bind subsystem** (scripts, `.builderforce` config, dummy git remotes, “developer clones into this ticket’s empty tree”). Binding is a platform/container action.
5. **Do not bind a different repo or branch.** Not `builderforce/task-2600`, not `builderforce/task-2601`, not another GitHub origin.
6. **Do not truncate or re-probe R-3 files** as a side effect of binding. In particular: do not touch `PHASE_SURFACES` (`frontend/src/lib/canvasPhases.ts`); do not overwrite `frontend/src/domains/canvas/domain/canvasChange.ts` (138-line production module); do not write the real module into nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts`. Restore of commit `0267ffd` is **#2601**.
7. If Layer A cannot be set by the assigned developer (no project-settings permission), **escalate to a human operator**. Do not substitute dumps. Fail closed.

### D-4 — Operator bind sequence (developer executes; architect does not)

1. **Layer A.** In project 11 settings, connect GitHub to `SeanHogg/Builderforce.ai` (`https://github.com/SeanHogg/Builderforce.ai`). Confirm owner/name agree. This also unblocks Layer A for the sibling class; it does **not** by itself satisfy R-7.
2. **Layer B.** On ticket **#2554** (not this ticket), ensure the developer container checks out `builderforce/task-2554` as a **local working copy** of that origin — complete clone (sparse-disabled), not write-only stubs. Re-bind if the container reports the repo bound but `list_files` still fails (ghost bind).
3. **Layer C.** Restore git credential delegation into that same container so `git status` / `git diff` / `git log` / `git rev-parse` (and add/commit/push as already authorized) do not fail with `no repository checked out` / `credential delegation absent`.
4. **Command path.** Confirm `run_command` executes in that checkout. A no-op (`pwd` / `git status`) must not fail as a bind error. From `frontend`, `pnpm run check` must be **launchable** (install/tooling failures are environment bugs to report, not “no repository checked out”).
5. **Durability (R-4).** The bind must survive the **next** #2554 developer-container run without a second manual bind: same repo, same branch, credentials still present. A readable #2600 / main checkout is **not** that proof.
6. **Prove R-7 on a #2554 run** (D-5). Then leave this ticket. Do not open a product PR from #2600. Do not implement B0–B5 here.

### D-5 — Proof matrix (R-7) — must run on S2

A developer-container run **assigned to #2554** can:

| # | Check | Pass |
|---|---|---|
| 1 | `list_files` at repo root | Sees real layout `frontend/`, `api/`, `packages/`, `specs/` (not `no repository bound`, not empty/write-only) |
| 2 | `read_file` `frontend/src/domains/canvas/domain/canvasChange.ts` | Existing production source (138 lines); `CONNECTED_CANVAS_ACTIONS` + `canInvokeCreationObjectAction` present — **not** a 2-line `export {}` stub |
| 3 | `read_file` each D-8 / R-3 path | formObject, `summarizeForm` client/server, `CanvasFormSurface`, form surface registry, `peopleObjects`, `growthApi.ts`, `/f/[slug]`, i18n config + five catalogs, `canvasPhases.ts`, people contract, idea log, founder objects, PRD 27 spec |
| 4 | `search_code` for `CONNECTED_CANVAS_ACTIONS`, `CAMPAIGN_BLOCKERS`, `marketing_audience_members` | Hits on the bound tree (not `no repository bound`). Do **not** invent paths for the two unconfirmed symbols; read the hits before any edit |
| 5 | `git rev-parse --abbrev-ref HEAD` and `git remote -v` | HEAD is `builderforce/task-2554`; remote is `SeanHogg/Builderforce.ai`; no unbound / no-checkout error |
| 6 | No-op `run_command` (`pwd` / `git status`); `pnpm run check` from `frontend` | Bind is not the failure mode. Check **starts**; install/tooling failures are reported as env bugs, not bind |
| 7 | Trivial in-place `write_file` then revert | Change appears in `git diff` on **that** branch — write and git share one tree |
| 8 | Open a real PR from `builderforce/task-2554` | That PR is **#2554 product work** (B0–B5 after dumps are deleted), not this ticket’s |

This ticket is done when those eight are true for **#2554’s container**, **or** when an operator records that #2554’s container already has a readable checkout. A readable #2600 / main checkout (S1) is **not** sufficient.

**QA / Validator:** prove R-7 against a **#2554** developer run, not against this ticket’s checkout alone. Distinguish GitHub **403 rate-limit** (API throttled; local `list_files`/`read_file` may still work — observed this run on S1) from unbound / missing delegation. Exec 31656 (write-without-read dumps) is **not** a passing proof.

### D-6 — Failure-mode dictionary

| Observation | Layer | Action |
|---|---|---|
| `no repository bound` on `list_files` / `read_file` in #2554 container | A and/or B | Connect project 11; checkout `builderforce/task-2554` in that container |
| `no repository checked out` on `git_*` / `run_command` | B and/or C | Restore working tree + git credentials into the same container; do not “fix” by skipping git |
| `credential delegation absent` on `git_*` | C | Restore git credentials into the same container |
| `write_file` works, `list_files` fails | Ghost bind / write-without-read | Re-bind working tree; **stop writing** until read works |
| `search_code` 403 / GitHub rate limit while `list_files` works | Not unbound | Wait / retry; do not treat as bind failure; do not dump files; do not invent `CAMPAIGN_BLOCKERS` / `marketing_audience_members` paths |
| Ticket `gitRepo` / `gitBranch` set but tools fail | Ghost bind | Tool results win |
| `pnpm run check` fails to **start** with bind error | B | Re-bind; not a frontend bug |
| `pnpm run check` starts then install/typecheck fails | Not bind | Report as env/tooling; out of this ticket except to note it |
| #2600 checkout readable | S1 only | Irrelevant to R-7 |
| Nested `Builderforce.ai/…/canvasChange.ts` 2-line stub | Write-without-read / `0267ffd` | Do not overwrite the real 138-line module. Restore is **#2601**. After bind, #2554 **deletes** the nested dump |

### D-7 — Role split (this ticket)

| Role | Owns | Does not |
|---|---|---|
| **Architect (this section)** | Bind topology, layers A/B/C, fail-closed, proof matrix, post-bind first actions for #2554 | Product design; executing the bind; a product PR; B0–B5 wiring |
| **Developer (#2600)** | Execute D-4 if they have operator/container access; otherwise escalate; keep `builderforce/task-2600` **empty of dumps** | B0–B5 product files; committing to this branch except to delete dead ops docs; opening the survey PR |
| **QA / Validator** | Prove D-5 / R-7 on a **#2554** run | Treating S1 as proof; treating exec 31656 dumps as pass |
| **Product Manager / BA / PO** | Scope firewall R-1–R-10 / P0–P6 (already locked) | Bind mechanics |

### D-8 — R-3 inventory reconfirmed on S1 (exec 31898, `ref: main`)

These paths exist on main and **must be readable on S2 before any #2554 edit**. Blind `write_file` of these paths is forbidden until the file has been read. This ticket must not truncate them.

| Concern | Path | This run |
|---|---|---|
| `CONNECTED_CANVAS_ACTIONS` + `ProposedCanvasChange` + `canvasChangesCanAutoApply` + `canInvokeCreationObjectAction` | `frontend/src/domains/canvas/domain/canvasChange.ts` | Present; **138 lines**; production module. `form: ['publish', 'collect', 'close']` and `idea: ['explore', 'test', 'reveal']` already listed. **Do not overwrite.** Nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` is the 0267ffd two-line probe — restore is **#2601**. |
| formObject publish (one mapping card → published form) | `frontend/src/lib/formObject.ts` | Present (178 lines) |
| `summarizeForm` client | `frontend/src/lib/founderOpsApi.ts` | Present (1077 lines); `CanvasFormSurface` already imports `summarizeForm` / `publishForm` / `closeForm` |
| `summarizeForm` / form publish server | `api/src/application/collection/formPublishing.ts` | Present (703 lines) |
| `CanvasFormSurface` | `frontend/src/components/creation-canvas/CanvasFormSurface.tsx` | Present (273 lines); already documents CRM → `source: crm` through existing audience-member store |
| Form surface registry | `frontend/src/components/creation-canvas/creationObjectSurfaces.ts` | Present (72 lines) |
| Canvas surfaces | `frontend/src/lib/canvasSurfaces.ts` | Present (297 lines) |
| `PHASE_SURFACES` / canvas phases | `frontend/src/lib/canvasPhases.ts` | Present (133 lines). **This ticket and #2554 must not modify it.** §11.7 Ideas visibility is already shipped. |
| `peopleObjects` (B0 adds `close`) | `frontend/src/lib/peopleObjects.ts` | Present (515 lines) |
| People contract | `packages/creation-canvas-contract/src/people.ts` | Present (515 lines) |
| `growthApi.createCampaign` + `send` + `addMembersFromCrm` | `frontend/src/lib/growthApi.ts` | Present (414 lines). `createCampaign` posts to `/api/growth/campaigns`. `addMembersFromCrm` posts `/api/growth/audiences/:id/members/from-crm`. B3 uses existing create+send; B4 uses the from-crm route — **no second send path, do not reuse POST members’ `source:'import'`**. |
| Public form `/f/<slug>` | `frontend/src/app/f/[slug]/page.tsx` | Present (32 lines); unauthenticated; `?t=` recipient token (BA) |
| i18n locale source of truth | `frontend/src/i18n/config.ts` | `LOCALES = ['en', 'zh', 'es', 'fr', 'de']` |
| Five next-intl catalogs | `frontend/src/i18n/messages/{en,de,es,fr,zh}.json` | All five present on this checkout |
| Idea log / evidence kinds (B1) | `frontend/src/lib/ideaLog.ts` | Present (158 lines) |
| PRD 27 (wiring-only spec) | `specs/builderforce/27-prd-idea-validation-surveys.md` | Present (343 lines) |

**Not path-confirmed this run** (`search_code` GitHub 403, same as BA): `CAMPAIGN_BLOCKERS` and the `marketing_audience_members` table/migration. After bind, the #2554 developer **must** `search_code` those symbols on the bound tree and read the hits **before** editing. Do not invent paths.

### D-9 — First actions on #2554 after S2 is readable (not this ticket)

#2554 is **4 commits ahead of main, no PR**. Container file-change log (exec 31656; not to be copied here):

- `specs/tasks/task-2554.md` (created/modified under write-without-read)
- `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (**nested `Builderforce.ai/` prefix** — 0267ffd-class probe, not the real module)

**First action after bind (developer of #2554):**

1. **Delete those dumps.** Do not merge them. Do not write the real `canvasChange.ts` into the nested prefix. Let #2601 revert `0267ffd` so the nested path is absent on main.
2. **Read** every R-3 path (D-8) before editing. `search_code` `CAMPAIGN_BLOCKERS` and `marketing_audience_members`; read the hits; do not invent paths.
3. Wire B0–B5 against the **real** tree per R-9 (wiring only; no new survey kind, second answer store, fifth contact store, `/surveys` dest, or second send path). `PHASE_SURFACES` stays untouched.
4. **Do not** invent `CanvasChangeKind` / `applyObjectChange`. **Do not** re-implement `formObject`, `CanvasFormSurface`, `growthApi.createCampaign` / `send` / `addMembersFromCrm`, or `/f/<slug>`.
5. House: `pnpm run check` in `frontend`. Isolate from unrelated dirty tree.

**Do not implement any of D-9 on #2600.**

### D-10 — This ticket’s product-repo footprint

#2600 file-change log is **only** `specs/tasks/task-2600.md` (created by BA, deleted by PO commit `843a60377af5877f4cae723ba223af7145b989b6`, re-deleted this run if it had reappeared). Keep it that way. Do not open a product PR from #2600. The only allowed git change on `builderforce/task-2600` is removing dead environment-ops docs so the branch stays empty of survey/canvas source (R-5 / P0 / P3).

### D-11 — What this architect run cannot do (and must not fake)

- Cannot set project 11 `githubRepoUrl` via agent tools (reconfirmed null this run).
- Cannot operate the #2554 developer container from this durable executor.
- Cannot satisfy R-7 from S1.
- Cannot stop exec 31656 from this slot; Design forbids treating it as proof and forbids a second dispatch until R-7.
- Will **not** write product files, bind scripts, or conceptual dumps onto `builderforce/task-2600` as a stand-in.

The architect record for lane `ready` is **this Design**. Developer executes D-4. QA proves D-5 on **#2554**.

_— authored by Architect / CTO · 2026-09-16 · execution #31898_

_— rewritten by CTO · 2026-09-16T16:27:56.257Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:53:02.331Z · execution #31708

**Business Analyst (backlog) — 2026-09-16 · execution #31708.** Scope locked as an environment unblock for **#2554**, not a product change.

This run’s checkout of `seanhogg/builderforce.ai` is readable for `list_files` / `read_file` (later GitHub API calls 403’d). That does **not** satisfy R-7: success is a #2554 developer-container run on `builderforce/task-2554`. #2600 has `gitBranch=builderforce/task-2600`. Project 11 `githubRepoUrl` is still `null` (same blocker as #2588/#2545/#2532).

R-3 paths inventoried on this checkout: `CONNECTED_CANVAS_ACTIONS` lives in `frontend/src/domains/canvas/domain/canvasChange.ts` (production module; nested `Builderforce.ai/` prefix is the 0267ffd probe — restore is **#2601**). formObject, `summarizeForm` (founderOpsApi + formPublishing), `CanvasFormSurface`, form surface registry, `peopleObjects`, `growthApi.createCampaign`, `/f/[slug]`, i18n `en/zh/es/fr/de`, `canvasPhases.ts` (`PHASE_SURFACES` — do not touch), PRD 27 spec. `CAMPAIGN_BLOCKERS` and `marketing_audience_members` were not path-confirmed this run (search 403); #2554 must search them after bind.

**This ticket’s repo footprint:** Requirements live in this PRD only. Ada created `specs/tasks/task-2600.md` earlier in this execution; `delete_file` then 403’d (GitHub rate limit). Developer must drop that docs-only file so `builderforce/task-2600` stays empty of dumps (R-5). Do not ship survey/canvas source on #2600.

**#2554 dumps to drop after bind (not here):** branch is 4 commits ahead of main with no PR — `specs/tasks/task-2554.md` and nested `Builderforce.ai/…/canvasChange.ts`. Reconcile by deleting, then read R-3, then wire B0–B5 against the real files.

Architect owns Design (bind + credential delegation). Developer executes the bind. QA proves R-7 on a **#2554** run.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:00:25.543Z · execution #31771

**Product Manager lock (todo) — 2026-09-16 · execution #31771.** Ada (Sr. Product Mgr) / Kevin BA/PM/PO.

R-1–R-10 are adopted as the product contract **without change**. This ticket remains an **environment unblock**, not a product change. PM sign-off is the scope lock, **not** an R-7 bind pass.

**P0 — No product-repo patch.** Bound `seanhogg/builderforce.ai` on this run is empty (workspace 0 files; `list_files` GitHub 403; `search_code` for `task-2600.md` = 0 matches). That emptiness does **not** authorize dumping survey / canvas / campaign / i18n source onto `builderforce/task-2600`. Do not invent a conceptual stand-in. Do not ship `specs/tasks/task-2600.md`. Keep this branch empty of dumps (R-5).

**P1 — Success condition.** A developer-container run **assigned to #2554** can `list_files` / `read_file` / `search_code` / `git_*` / `run_command` / in-place `write_file` on `SeanHogg/Builderforce.ai` @ `builderforce/task-2554` (R-7). A readable #2600 checkout of `main` or `builderforce/task-2600` is necessary inventory, **not** sufficient proof (R-1, R-4).

**P2 — Platform blocker (verified this run).** Project 11 still has `githubRepoUrl = null`, `githubRepoOwner = null`, `githubRepoName = null`, `sourceControlRepoUrl = null` (`projects.get` 2026-09-16). Agent tools cannot set `githubRepoUrl`. Same class as #2590 → #2545, #2588 → #2527, #2532, #2555, #2573, #2575. Architect Design **must** name the operator/platform action that sets project 11’s GitHub binding and restores credential delegation for the **#2554** container. Do not design a fake “developer clones into this ticket’s empty tree” path.

**P3 — Scope firewall (locked).** Out: PRD 27 B0–B5 wiring/copy/behavior; any change to `PHASE_SURFACES`; overwrite or re-probe of `canvasChange.ts`; inventing `CanvasChangeKind` / `applyObjectChange`; writing the real module into nested `Builderforce.ai/`; opening the B0–B5 product PR from #2600. Restore of commit `0267ffd` (nested 2-line `export {}` probe) is **#2601**, not this ticket.

**P4 — #2554 dumps (not here).** After bind, the first readable #2554 run deletes `specs/tasks/task-2554.md` and nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts`, then reads R-3 paths, then wires B0–B5 against the real tree. Do not copy those dumps onto #2600.

**P5 — Handoff.** Architect owns Design (bind + credential delegation + who sets `githubRepoUrl`). Developer executes the bind / restore; do not author B0–B5 product files. QA / Validator prove R-7 against a **#2554** developer run. Sibling #2590 is the same operator bind for #2545 (human assignee); do not merge those product scopes.

**P6 — Schedule.** `startDate = 2026-09-16`, `dueDate = 2026-09-17` (urgent unblock of parent #2554, due 2026-09-23).

R-3 inventory and R-9 post-bind product constraints stand. `CONNECTED_CANVAS_ACTIONS` lives in `frontend/src/domains/canvas/domain/canvasChange.ts` (production module). `CAMPAIGN_BLOCKERS` and `marketing_audience_members` remain unconfirmed until the #2554 tree is searchable — do not invent paths.

---

### Update — Product Manager · 2026-09-16T16:10:44.236Z · execution #31811

**Product Owner ready-lane lock — 2026-09-16 · execution #31811.**

Scope + acceptance **approved**. Contract = BA R-1–R-10 + PM P0–P6, unchanged.

This ticket remains an **environment unblock**, not a product change. Success is a developer-container run **assigned to #2554** that can `list_files` / `read_file` / `search_code` / `git_*` / `run_command` / in-place `write_file` on `SeanHogg/Builderforce.ai` @ `builderforce/task-2554` (R-7). A readable #2600 checkout of `main` / `builderforce/task-2600` is necessary inventory, **not** sufficient proof (R-1, R-4). This PO run did read production `frontend/src/domains/canvas/domain/canvasChange.ts` (138 lines; `CONNECTED_CANVAS_ACTIONS` + `canInvokeCreationObjectAction` present — not a stub), `frontend/src/lib/peopleObjects.ts` (515 lines), and `frontend/src/lib/canvasPhases.ts` (133 lines, unread beyond header — **do not modify `PHASE_SURFACES`**). That inventory does **not** close R-7.

**P0 — No product-repo patch from PO.** Project 11 still has `githubRepoUrl = null`, `githubRepoOwner = null`, `githubRepoName = null`, `sourceControlRepoUrl = null` (`projects.get` this run). Architect Design **must** name the operator/platform action that sets that binding and restores credential delegation for the **#2554** container. Do not design a fake clone-into-#2600 path.

**P3/R-5 — Dump removed.** Deleted `specs/tasks/task-2600.md` from `builderforce/task-2600` (commit `843a60377af5877f4cae723ba223af7145b989b6`). That is the only allowed product-repo change on this ticket. Do not ship survey/canvas/campaign/i18n source here. Do not open the B0–B5 product PR from #2600.

**Out (locked):** PRD 27 B0–B5 wiring/copy/behavior; any change to `PHASE_SURFACES`; overwrite or re-probe of `canvasChange.ts`; inventing `CanvasChangeKind` / `applyObjectChange`; writing the real module into nested `Builderforce.ai/`; 0267ffd restore (**#2601**).

**Handoff:** Architect owns Design (bind + credential delegation + who sets `githubRepoUrl`). Developer executes the bind / restore; do not author B0–B5 product files. QA / Validator prove R-7 against a **#2554** developer run.

_— Product Owner · 2026-09-16 · execution #31811_

---

### Update — CTO · 2026-09-16T16:28:33.772Z · execution #31898

**Architect (ready) — 2026-09-16 · execution #31898.** Design authored (D-0–D-11). Environment unblock only: Layers A/B/C, fail-closed, R-7 proof matrix targeting **#2554** @ `builderforce/task-2554`, not this ticket. Project 11 `githubRepoUrl` still null (cannot set via agent tools). S1 this run can `list_files`/`read_file` on main (R-3 reconfirmed: `canvasChange.ts` 138 lines with `CONNECTED_CANVAS_ACTIONS`; `form: ['publish','collect','close']` already listed). `search_code` 403 is GitHub rate-limit, not unbound. #2554 container exec 31656 is live and dumping (`specs/tasks/task-2554.md`, nested `Builderforce.ai/…/canvasChange.ts`) — fail-closed: do not treat as R-7; do not re-dispatch until bind. Removed dead `specs/tasks/task-2600.md` if it had reappeared (R-5). No product design, no B0–B5, no `PHASE_SURFACES` change. Developer executes D-4. QA proves D-5 on a **#2554** run.