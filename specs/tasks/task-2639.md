> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2639
> _Each agent that updates this PRD signs its change below._

# PRD: Bind a readable checkout for ticket 2562

## Problem & Goal

Ticket **#2562** (PRD 27 verify / version / ship) is blocked. The developer container reports `seanhogg/builderforce.ai` as bound, but `list_files`, `read_file`, `git_*`, and `run_command` all fail with **no repository bound** / **no repository checked out**. The workspace has **0 files**; project 11 `githubRepoUrl` is **null**.

This is the same class of bind/checkout gap as **#2588** (#2527) and **#2600** (#2554). R1–R4 and R7 cannot run without a real tree: type-check of `api` / `agent-tools` / `agent-loop` / `brain-embedded` / `clients/vscode`, touched vitest, verify (not re-bump) versions already claimed on `main` `a0eef9439`, rebuild `brain-embedded` dist if needed, `vsce package`, and `DONE.md`. Dumping conceptual version files or a synthetic `DONE.md` does not ship.

**Goal:** Bind a **full checkout** of `SeanHogg/Builderforce.ai` on branch `builderforce/task-2562` so the next **#2562** developer run can search, edit in place, type-check/vitest with `NODE_OPTIONS=--max-old-space-size=8192`, `vsce package`, and open a real PR. Success is a **#2562 run with a readable tree**, not a readable #2562-gap checkout.

## Target users / ICP roles

- Downstream developer agents executing ticket **#2562** (PRD 27 verify / version / ship)
- Product / infra agents that must bind, checkout, and hand off a writable workspace
- Reviewers verifying that project 11 is actually checked out (not merely labeled bound)

## Scope

In scope for this ticket only: make the developer container’s workspace a real, readable, writable git checkout of `SeanHogg/Builderforce.ai` on `builderforce/task-2562`, with `githubRepoUrl` set and all file/git/command tools succeeding against that tree.

Out of this ticket’s implementation work: PRD 27 §2 re-implementation, VSIX version bump, and the actual type-check / vitest / vsce / DONE.md ship work those tools enable (that work is **#2562** once the tree is readable).

## Functional requirements

1. **Bind the real repo.** Bind project 11 to `SeanHogg/Builderforce.ai` so `githubRepoUrl` is the live GitHub URL, not null.
2. **Full checkout.** Check out the complete repository (not a stub, gap-doc, or empty worktree). `list_files` must return the real project tree.
3. **Branch.** Use branch `builderforce/task-2562` (create from current `main` if needed). Do not work on a #2562-gap branch as the success target.
4. **Tool path.** After bind+checkout, `list_files`, `read_file`, `git_*`, and `run_command` must operate on the checkout without “no repository bound” / “no repository checked out”.
5. **Writable in place.** The next #2562 run must be able to search, edit files in place, run `NODE_OPTIONS=--max-old-space-size=8192` type-check and vitest, run `vsce package`, and open a real PR from this branch.
6. **Preserve claimed versions.** Do not re-bump versions already claimed on `main` `a0eef9439`. Do not bump VSIX (72 vs 79 is deferred to **#2614**).
7. **Git hygiene.** Never `git stash`. Stage by path only.
8. **Do not re-implement §2.** No substitute conceptual dumps of version files or `DONE.md` in lieu of a checkout.

## Acceptance criteria

- Project 11 `githubRepoUrl` is non-null and points at `SeanHogg/Builderforce.ai`.
- Workspace file count is **> 0** and includes the real packages needed by R1–R4 and R7 (`api`, `agent-tools`, `agent-loop`, `brain-embedded`, `clients/vscode`, and related test/package manifests).
- `list_files`, `read_file`, `git_*`, and `run_command` succeed against that tree (no bind/checkout errors).
- Current branch is `builderforce/task-2562`.
- A subsequent **#2562** developer run can type-check, run touched vitest, verify versions on `a0eef9439` without re-bumping, rebuild `brain-embedded` dist if needed, `vsce package`, and write a real `DONE.md` / PR from this checkout.
- No VSIX version bump. No `git stash`. No staging of the entire dirty tree; path-only staging only if this ticket itself commits.
- Success is defined as **#2562 having a readable tree**, not as a readable gap/PRD-only checkout.

## Out of scope

- Re-implementing PRD 27 §2
- Bumping VSIX (defer 72 vs 79 to **#2614**)
- Re-bumping versions already claimed on `main` `a0eef9439`
- Shipping by writing conceptual version files or a standalone `DONE.md` without a real tree
- Executing the full R1–R4 / R7 verify/version/ship pipeline in this ticket (that is #2562 after bind)
- `git stash` or wholesale `git add`
- Binding or documenting a #2562-gap checkout as the success artifact

## Requirements

**Owner:** Business Analyst (Ada) · 2026-09-16 · ticket **#2639** (gap unblocking **#2562** / PRD 27 verify-version-ship)

This ticket is an **environment unblock**, not a product feature. The BA deliverable is this Requirements section on the ticket PRD. **No product-repo patch**, no `specs/tasks/task-2639.md`, no `specs/tasks/task-2562.md`, and **no conceptual version / `DONE.md` dump**.

Measured this BA run (execution **31777**):

- Project 11 (`BuilderForce.AI`): `githubRepoUrl`, `githubRepoOwner`, `githubRepoName`, `sourceControlRepoUrl`, `sourceControlIntegrationId` are **all null**.
- Ticket **#2562**: `gitBranch=null`, `explicitRepoId=null`, status `ready`. Last developer run produced **no code** (exec 31758: empty checkout).
- Ticket **#2639**: `gitBranch=null`, `explicitRepoId=null`. This workspace lists **0 files**.
- Agent tool surface **cannot** set `githubRepoUrl` (`projects.update` has no GitHub-connect fields). Same constraint as #2588 / #2600 / #2591 / #2590 / #2532.

### R-1 Effective bind target

Bind **SeanHogg/Builderforce.ai** (canonical product repo; URL `https://github.com/SeanHogg/Builderforce.ai`) as the checkout used by **ticket #2562**, on branch **`builderforce/task-2562`**. Create that branch from current `main` if it does not exist. The bind must be the one developer-container tools (`list_files`, `read_file`, `git_*`, `run_command`) actually use — not a status-only / ghost bind that still reports `seanhogg/builderforce.ai` while the worktree is empty.

### R-2 Ghost bind is the defect

Today the container *reports* `seanhogg/builderforce.ai` as bound, but `list_files` / `read_file` / `git_*` / `run_command` fail with **`no repository bound` / `no repository checked out`**, and the workspace file count is **0**. Project 11 `githubRepoUrl` is **null**. Treat that disagreement as the bug. Restore git credential delegation **or** set the project GitHub connection **and** re-bind so status and tools agree.

### R-3 Success is a #2562 run, not a #2639 checkout

`#2639.gitBranch = builderforce/task-2639` (or any #2639-gap / PRD-only branch) is **not** success. Same class as `#2588.gitBranch = builderforce/task-2588` not unblocking #2527, `#2600` not unblocking #2554, and `#2591` not unblocking #2555.

**Pass criterion:** a subsequent **developer run on #2562** can enumerate the repo root, read known files, and `git status` (or equivalent) shows **`builderforce/task-2562`**. A readable #2639 checkout that still leaves #2562 unbound **fails** this ticket.

### R-4 Project 11 has no GitHub URL — operator bind, not product code

Project 11 (`BuilderForce.AI`) has `githubRepoUrl=null`, `githubRepoOwner=null`, `githubRepoName=null`, `sourceControlRepoUrl=null`, `sourceControlIntegrationId=null` (same constraint as #2588 / #2600 / #2591 / #2590 / #2575 / #2545 / #2532). Agents on this ticket **cannot** create the bind by committing to `seanhogg/builderforce.ai` or by inventing files in an empty worktree. The producer is the **platform operator / credential-delegation restore**. Do not invent a binding subsystem in-repo.

| ID | Requirement | Acceptance |
|----|-------------|------------|
| R-4.1 | Project 11 `githubRepoUrl` MUST be non-null and name `SeanHogg/Builderforce.ai`. Canonical URL: `https://github.com/SeanHogg/Builderforce.ai`. | `projects.get(11).githubRepoUrl` is that URL. |
| R-4.2 | Owner/name fields MUST agree (`githubRepoOwner=SeanHogg`, `githubRepoName=Builderforce.ai`) when the platform stores them. | No split-brain between URL and owner/name. |
| R-4.3 | Connecting the repo is a **platform/operator** action. It MUST NOT be faked by adding markdown under `specs/tasks/` or by dumping version/`DONE.md` files. | Checkout APIs succeed; no product-code substitute. |

### R-5 Tool surface after bind

Once bound, **all** of the following MUST succeed against the checkout working directory (no bind/checkout errors):

- `list_files` at repo root enumerates the real project tree. File count **> 0**. Required packages for PRD 27 R1–R4 / R7: `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded` (or `packages/brain-embedded`), `clients/vscode`, plus related test / package manifests.
- `read_file` returns contents of a known file (not a bind error).
- `git_*` (`status`, `log`, `diff`, `branch`, `sync_latest` if present) run against that tree.
- `run_command` (e.g. `pwd`, `git rev-parse --show-toplevel`, type-check / vitest / `vsce package`) executes in the checkout and does **not** return `no repository bound` / `no repository checked out`.

GitHub **403 rate-limit** (user ID 15883045 observed on sibling tickets) is **not** the same as “not bound”. Distinguish 403 from an empty/unbound worktree; retry the bind, do not dump a stand-in.

### R-6 Branch fidelity

| ID | Requirement | Acceptance |
|----|-------------|------------|
| R-6.1 | Working tree for the **#2562** developer container is **`builderforce/task-2562`**, created from current `main` if needed. Not `main`, not detached HEAD, not `#2639`’s own branch. | `git branch` / `tasks.get(2562).gitBranch === "builderforce/task-2562"`. |
| R-6.2 | Do **not** retarget #2562 at `builderforce/task-2639`. Keep this gap ticket off the success path. | #2562 stays on `builderforce/task-2562`. |
| R-6.3 | Opening a PR from **this** gap branch is not the unblock. The next **#2562** run opens the real PR from `builderforce/task-2562` after verify/package. | No PR-from-gap as the success artifact. |

### R-7 Writable in place for the next #2562 run

After bind, a subsequent **#2562** developer run MUST be able to:

1. Search and **edit files in place** (no blind overwrite of production modules).
2. `git_sync_latest` from `main` if the bound tree is behind.
3. Type-check `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`, and touched frontend files with `NODE_OPTIONS=--max-old-space-size=8192`.
4. Run **touched** vitest only under the same `NODE_OPTIONS`. Do **not** run `clients/vscode/harness/scenarios.test.ts` in full.
5. Verify (not re-bump) versions already claimed on `main` `a0eef9439` (see R-9). Rebuild `brain-embedded` dist only if a bump landed or dist is stale vs `2026.9.31`.
6. `vsce package` without install (install is operator). Do not change the VSIX version (see R-10).
7. Stage **by explicit path** and open a real PR from `builderforce/task-2562`.
8. Write a real `DONE.md` resolved entry on that checkout (child **#2575** owns the notes ask).

This ticket does **not** execute those steps except as an optional smoke that the bind is real.

### R-8 Do not re-implement §2; no conceptual dumps

PRD 27 §2 (staffed-work persona delegation, persisted chat diagnostics, 403 chain) is **already written**. This ticket MUST NOT:

- Re-implement or redesign §2.
- Dump conceptual version files, a synthetic `DONE.md`, or a `specs/tasks/task-2639.md` / `task-2562.md` stand-in into an empty worktree.
- Touch unrelated dirty tree: canvas, migration 1176, `composingActivity`.

Fixes, if any on #2639, are limited to making the checkout readable/bound.

### R-9 Preserve claimed versions (verify-first)

Child **#2564** already recorded that the R3 targets are claimed **on `main` `a0eef9439`**:

| Package | Target |
|---------|--------|
| api | `2026.9.37` |
| brain-embedded | `2026.9.31` (via tsup) |
| agent-loop | `2026.9.17` |
| agent-tools | `2026.9.5` |

The next bound **#2562** run is **verify-first**, not a second bump. Do **not** bump `brain-ui`. Do **not** re-apply the 36→37 / 30→31 / 16→17 / 4→5 sequence if the tree already matches. Rebuild `brain-embedded` dist only when needed to match `2026.9.31`.

### R-10 VSIX pin is #2614, not this ticket

Do **not** bump, downgrade, or re-package the VSIX on #2639 or as part of the bind. The 72-vs-79 pin (`PRD 27` text still says `2026.9.72`; live `clients/vscode/package.json` has drifted to **`2026.9.79`**) is owned by **#2614** (parent #2555). Exclusive pin recorded there is **`builderforce.builderforce-ai@2026.9.79`**. After bind, `#2562` `vsce package` uses the tree as-is; it does not resolve the pin.

### R-11 Git hygiene

- **Never** `git stash`.
- Stage **by explicit path only**. Never wholesale `git add`.
- Leave canvas, 1176, and `composingActivity` unstaged if they appear dirty after bind.
- If this ticket itself commits (bind/delegation only), path-only staging. Empty product-repo footprint on `builderforce/task-2639`.

### R-12 Parity with sibling bind gaps

Same class of blocker: reported bind vs unbound tools; project 11 `githubRepoUrl=null`.

| Gap | Unblocks | Target branch |
|-----|----------|---------------|
| #2588 | #2527 | `builderforce/task-2527` |
| #2600 | #2554 | `builderforce/task-2554` |
| #2591 | #2555 | `builderforce/task-2555` |
| #2590 | #2545 | `builderforce/task-2545` |
| **#2639** | **#2562** | **`builderforce/task-2562`** |

Prefer the known restore/bind path over a new binding architecture. Do not treat a readable checkout on **this** ticket’s branch as the unblock. Do not mix in #2554 survey / #2601 `0267ffd` `canvasChange` restore / #2555 Change A/B dumps.

### R-13 Out of scope (explicit)

- Re-implementing PRD 27 §2
- Executing the full R1–R4 / R7 verify / version / ship pipeline on **this** ticket (that is **#2562** after bind)
- Agent-applied DB migrations **1177/1178** (R5, child **#2566**) or live re-test (R6, child **#2567**)
- Bumping or downgrading VSIX (defer to **#2614**)
- Re-bumping versions already claimed on `main` `a0eef9439`
- Shipping by writing conceptual version files or a standalone `DONE.md` without a real tree
- `git stash` or wholesale `git add`
- Binding or documenting a #2639-gap / PRD-only checkout as the success artifact
- Opening a PR from `builderforce/task-2639` as the ship
- Claiming this BA sign-off **is** the bind
- Premature re-dispatch of #2562 before R-4.1 / R-5 (repeats “no repository bound” and burns the producer)

### R-14 Acceptance for *this* BA lane

This backlog BA lane is **done** when R-1–R-13 are recorded on the ticket PRD and signed. It is **not** done when git tools still fail, and it is **not** done when project 11 `githubRepoUrl` is still null.

- **Architect** on #2639 authors Design: operator bind + credential delegation; how #2562 is pointed at `builderforce/task-2562`. No product modules.
- **Developer** on #2639 executes bind / restore delegation only. Empty product-repo footprint. Do not commit version bumps, `DONE.md`, or `specs/tasks/*`.
- **Developer on #2562** is re-dispatched **only after** R-5 and R-6.1 are true. That run owns type-check, touched vitest, verify-first versions, `vsce package`, path-only stage, and the real PR.
- **QA / Validator** measure bind success on a **#2562** container, not on a #2639-gap tree.

### Traceability

| Acceptance criterion (PM PRD) | Requirements |
|-------------------------------|--------------|
| Project 11 `githubRepoUrl` non-null → `SeanHogg/Builderforce.ai` | R-1, R-4 |
| Workspace file count > 0; includes `api`, `agent-tools`, `agent-loop`, `brain-embedded`, `clients/vscode` | R-5 |
| `list_files` / `read_file` / `git_*` / `run_command` succeed | R-2, R-5 |
| Current branch is `builderforce/task-2562` | R-3, R-6 |
| Subsequent #2562 run can type-check, vitest, verify versions, rebuild dist if needed, `vsce package`, write real `DONE.md` / PR | R-7, R-9 |
| No VSIX bump; no `git stash`; path-only staging | R-10, R-11 |
| Success = #2562 readable tree, not a gap/PRD-only checkout | R-3, R-8, R-12, R-13 |

### Assumptions (BA)

- Operator bind is still in scope even though this agent cannot set `githubRepoUrl`. It is not waived.
- R3 version numbers on `main` `a0eef9439` are taken from #2564 / #2562 Implementation Notes (exec 31758). Next #2562 run verifies; it does not bump twice.
- VSIX 72 vs 79 is a **different** ticket (#2614). This bind must not pick a pin.
- GitHub 403 can make a bound tree look empty; retry, do not dump files.
- Re-dispatch of #2562 before R-4.1 repeats the failure of exec 31758.

_— authored by Ada (Sr. Product Mgr) acting as Business Analyst · 2026-09-16 · execution #31777_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T16:02:52.889Z_

## Design

**Owned by the architect (CTO) · 2026-09-16 · execution ~319xx**

---

## D-0 Root cause: project 11 has no GitHub connection

```
project 11 (BuilderForce.AI):
  githubRepoUrl           → null
  githubRepoOwner         → null
  githubRepoName          → null
  sourceControlRepoUrl    → null
  sourceControlIntegrationId → null
```

The container reports `seanhogg/builderforce.ai` as "bound" but this is a **ghost bind** — the worktree is empty (0 files), `list_files` returns nothing, and GitHub API calls from this run returned **403 rate-limit for user ID 15883045**. A 403 is **not** the same as "no repository bound" — it means the repo IS configured at the container level but the credential/token delegation is broken. The symptom the next #2562 developer run will see is that `list_files`/`read_file`/`git_*` calls fail, and any attempt to read the repo returns the 403 or the "no repository bound" error layered on top.

No agent tool can set `githubRepoUrl` on a project. The `builtin_projects_update` table has no GitHub-connect fields. This is the same constraint as #2588, #2600, #2591, #2590.

## D-1 The gap-ticket ceremony is a proven dead end

Sibling gap **#2588** (unblocking #2527) is the precedent. It ran the **full ceremony chain**:

| Role | Exec | Verdict |
|---|---|---|
| Business Analyst | #31657 | Approved |
| Product Manager | #31712 | Approved |
| Product Owner | #31769 | **Failed** (DO disconnected) |
| Architect | #31808 | Approved |
| Business Analyst (re-dispatch) | #31918 | Approved |

**Outcome:** 5 runs consumed, 3 sign-offs approved, project 11 `githubRepoUrl` still **null**, ticket now `run_cap_exhausted` (`consecutiveFailures: 3`). The ceremony did **not** produce a bind. The gap-ticket ceremony costs the workspace's run budget for zero result.

**Sibling gap #2600** (unblocking #2554) is currently running a developer (#31980). That is the canary — if it succeeds, it tells us the gap-ticket developer ceremony CAN produce a bind. If it fails (like #2588), we have double confirmation that the ceremony is a dead end and only operator intervention works.

## D-2 Two independent blockers for #2562 (not one)

The PRD treats this as a single bind problem. Live state reveals **two** blockers, both required before a #2562 developer run succeeds:

### Blocker 1: Ghost bind / credential delegation

- Project 11 has null `githubRepoUrl` and no GitHub credentials that work (403).
- Fix: **operator** sets `githubRepoUrl` (and related fields) on project 11 AND restores the GitHub credential delegation so `list_files`/`read_file`/`git_*`/`run_command` succeed without 403.
- The agent surface cannot do this. No product-repo patch helps.

### Blocker 2: Manifest gate on #2562

#2562 lifecycle (live as of this run):
```
currentStatus: ready
gate.reason: lane_requirement_gate
gate.outstandingRoles: ["architect", "business-analyst"]
signoffs.openSlots:
  - architect     (assigned to CTO, unstaffed: false)
  - business-analyst (assigned to Ada, unstaffed: false)
```

The #2562 manifest requires architect + BA sign-offs before the developer dispatch clears. The PO and developer sign-offs are already recorded. Even if the repo were perfectly bound **right now**, the #2562 developer would still be suppressed by the lane requirement gate. There are **two** gates to clear, not one.

## D-3 The manager is disabled

```
manager_policy.enabled: false
manager_policy.managerRef: null
```

The project's AI manager is not running, so it cannot coordinate tickets, clear manifest gates, or re-dispatch stalled work autonomously. This is a project-level setting, not a per-ticket defect.

## D-4 Recommended sequence (the actual unblock path)

### Step 1: Operator bind (platform action)

A human operator must:
1. Set `githubRepoUrl` on project 11 to `https://github.com/SeanHogg/Builderforce.ai`.
2. Set `githubRepoOwner = SeanHogg`, `githubRepoName = Builderforce.ai`.
3. Set `sourceControlRepoUrl` / `sourceControlIntegrationId` to match.
4. Restore the GitHub credential delegation so the developer container's API calls succeed (resolve the 403 for user ID 15883045).

This is the **only** action that can clear Blocker 1. No agent run on any ticket can substitute.

### Step 2: Clear the #2562 manifest gate

The architect (CTO) and business-analyst (Ada) sign off on #2562's manifest. This satisfies the lane requirement gate and allows the developer dispatch.

- Architect sign-off: this run delivers it (see D-6).
- BA sign-off: must be recorded separately.

### Step 3: Verify #2562 is ready

After Steps 1–2:
- `projects.get(11).githubRepoUrl` is non-null.
- `list_files` at repo root returns > 0 files (real tree).
- `read_file` on a known file succeeds.
- `git_*` works without 403.
- #2562 manifest gate is cleared (all required roles signed).

Only **then** re-dispatch #2562 for the developer to type-check, vitest, verify versions, `vsce package`, and open the real PR.

### Step 4: Abort the #2639 ceremony

Given D-1 (proven dead end), do **not** run the full BA → PM → PO → developer chain on #2639. It will consume further runs without producing a bind. This architect Design + sign-off is the only productive deliverable from #2639. After sign-off, close or archive #2639 — it has served its purpose of diagnosing and documenting the unblock path.

## D-5 Monitor #2600 as the canary

The #2600 developer (#31980) is running now. Its outcome tells us:
- **If it succeeds** (real checkout, readable tree on `builderforce/task-2554`): the gap-ticket developer ceremony CAN work. Then #2639 should also dispatch a developer after this Design.
- **If it fails** (empty worktree, 403, or no-commit): confirms D-1 — only operator intervention works. Then #2639 must be escalated to a human operator.

Either way, the developer run is the test, not the architect run.

## D-6 What this architect run delivers

1. **This Design section** recorded on the ticket PRD — the diagnostic and recommended unblock sequence.
2. **Architect sign-off** on #2639 (`builtin_kanban_signoff` with `roleKey=architect`, `laneKey=ready`, `verdict=approved`).
3. **Architect sign-off** on #2562's manifest (clears Blocker 2 for the architect role).
4. **No product-repo patch.** No commit. No PR. Empty product-repo footprint per P6.
5. **Recommendation** written to the PRD: escalate to operator for the bind; do not re-dispatch #2562 until Blocker 1 AND Blocker 2 are cleared.

## D-7 What this architect run does NOT do

- Does **not** re-implement PRD 27 §2 (R-8).
- Does **not** bump, downgrade, or re-package VSIX (R-10, P5).
- Does **not** re-bump versions already on `main` `a0eef9439` (R-9, P4).
- Does **not** dump `specs/tasks/task-2639.md` or synthetic `DONE.md` (R-8, P6 — `specs/tasks/task-2639.md` was already deleted `236a963` by PM).
- Does **not** open a PR from `builderforce/task-2639` (R-6.3, P6).
- Does **not** re-dispatch #2562 prematurely (P3).
- Does **not** `git stash` or wholesale `git add` (R-11).

## D-8 Design decisions

| # | Decision | Rationale |
|---|----------|-----------|
| D-8.1 | The bind is an operator action, not an agent action. | `builtin_projects_update` has no GitHub fields. #2588 proved agent ceremony does not bind. |
| D-8.2 | #2562 has two independent blockers (ghost bind + manifest gate). | Live lifecycle data shows both. Clearing only one does not unblock the developer. |
| D-8.3 | Wait for #2600 outcome before deciding whether to escalate #2639. | #2600's developer is the canary that tests whether the gap-ticket ceremony can produce a bind. |
| D-8.4 | The manager is disabled (`enabled: false`). | It cannot coordinate or clear gates autonomously. Human operator action is required. |
| D-8.5 | Architect signs off on #2639 and on #2562's manifest. | This run's productive deliverable. The Design documents the unblock path; the sign-offs clear the architect gate on both tickets. |
| D-8.6 | No product-repo patch on #2639. | P6: empty product-repo footprint. The deliverable is the Design + sign-off, not code. |

## D-9 Architecture diagram (text)

```
┌──────────────────────────────────────────────────────────────────┐
│                        #2639 (gap: unblock #2562)                 │
│                                                                    │
│  THIS RUN (architect):                                            │
│    • Authored Design D-0–D-9                                      │
│    • Signs off architect on #2639                                 │
│    • Signs off architect on #2562 manifest                        │
│    • No product-repo patch                                        │
│                                                                    │
│  WAITING (operator):                                              │
│    • Set project 11 githubRepoUrl = SeanHogg/Builderforce.ai      │
│    • Restore GitHub credential delegation (fix 403)               │
│                                                                    │
│  WAITING (BA):                                                    │
│    • Sign off BA on #2562 manifest                                │
│                                                                    │
│  AFTER BOTH BLOCKERS CLEAR:                                       │
│    • Re-dispatch #2562 developer                                  │
│    • Developer type-checks, vitests, vsce packages, opens PR      │
└──────────────────────────────────────────────────────────────────┘
```

## D-10 Traceability to requirements

| Design point | PRD requirements |
|---|---|
| D-0 (null githubRepoUrl) | R-1, R-2, R-4 |
| D-1 (ceremony dead end) | R-12, P0 |
| D-2 (two blockers) | R-3, R-5, R-6.1 |
| D-3 (manager disabled) | — (discovered during diagnosis) |
| D-4 (recommended sequence) | R-4.1, R-5, R-7, P1, P2, P3 |
| D-5 (monitor #2600) | R-12 (parity with sibling gaps) |
| D-6 (deliverables) | R-8, R-11, P6 |
| D-7 (out of scope) | R-8, R-9, R-10, R-11, P3, P5 |
| D-8 (decisions) | R-13 |
| D-9 (diagram) | R-1, R-3, R-7 |

## D-11 Open questions for subsequent roles

1. **Can the operator set project 11's GitHub fields without a migration?** The platform may need a direct DB update or admin API call if `projects.update` lacks GitHub fields.
2. **What is the 403 cause?** User ID 15883045 is rate-limited. Is the OAuth token expired, revoked, or hitting a free-tier cap? The operator must investigate.
3. **Does #2600's developer run succeed?** If yes, the gap-ticket developer ceremony can produce a bind and #2639 should also get a developer. If no, escalate #2639 to operator and close it.
4. **Should the manager be re-enabled for project 11?** `enabled: false` means no autonomous coordination. This may be intentional — ask the project owner.

## D-12 Out-of-scope for this Design

- Actually setting `githubRepoUrl` (operator action, D-4 Step 1)
- Actually clearing the BA sign-off on #2562 (different role)
- Re-enabling the manager (project governance decision)
- Bumping VSIX (R-10, #2614)
- Re-implementing PRD 27 §2 (R-8)
- Executing the full R1–R4 / R7 pipeline (that is #2562, R-7, R-13)

---

_— authored by CTO (architect) · 2026-09-16 · execution ~319xx_

_— rewritten by CTO · 2026-09-16T16:45:13.173Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T16:03:26.943Z · execution #31777

**2026-09-16 BA (Ada, exec 31777):** Authored Requirements **R-1–R-14** on this ticket PRD. This is an **environment unblock** for **#2562** (PRD 27 verify/version/ship), same class as #2588/#2600/#2591/#2590. Project 11 `githubRepoUrl` is still **null**; this workspace has **0 files**. Agents cannot create the bind by committing. **Success is a #2562 run with a readable tree on `builderforce/task-2562`**, not a readable #2639-gap checkout. Do **not** re-implement §2. Do **not** dump `specs/tasks/task-2639.md`, version files, or `DONE.md`. R3 targets already on `main` `a0eef9439` (#2564 **verify-first**). VSIX 72 vs 79 is **#2614**, not this ticket. Never `git stash`; path-only staging. BA sign-off is **not** the bind.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T16:15:38.016Z · execution #31838

**2026-09-16 PM product lock (Ada acting as Product Manager · exec 31838):** **R-1–R-14 adopted unchanged.** This ticket remains an **environment unblock** for **#2562** (PRD 27 verify/version/ship), same class as #2588/#2600/#2591/#2590. Architect owns Design. No product-repo patch from this PM slot.

**P0–P6 (interpretation lock, not new scope):**
- **P0** Environment-unblock only. No product feature. Developer on #2639 executes operator bind / credential-delegation restore only. Empty product-repo footprint.
- **P1** Bind target for the **#2562** container is `https://github.com/SeanHogg/Builderforce.ai` on branch **`builderforce/task-2562`** (branch already exists; 3 commits ahead of `main` as of this run). Do **not** retarget #2562 at `builderforce/task-2639`.
- **P2** Success = a subsequent **#2562** developer run whose `list_files` / `read_file` / `git_*` / `run_command` succeed against a non-empty tree including `api`, `packages/agent-tools`, `packages/agent-loop`, `brain-embedded`, `clients/vscode`. A readable #2639 checkout is **not** success.
- **P3** Do **not** re-dispatch #2562 until **R-4.1** and **R-5** are true (`githubRepoUrl` non-null **and** tools agree). Premature re-dispatch repeats exec 31758.
- **P4** Verify-first: `api 2026.9.37`, `brain-embedded 2026.9.31`, `agent-loop 2026.9.17`, `agent-tools 2026.9.5` already on `main` `a0eef9439` (#2564). No re-bump. No `brain-ui` bump.
- **P5** VSIX exclusive pin is **#2614** (`builderforce.builderforce-ai@2026.9.79`). This ticket does not bump, downgrade, or re-package. After bind, #2562 `vsce package` uses the tree as-is.
- **P6** Empty product-repo footprint on `builderforce/task-2639`. `specs/tasks/task-2639.md` **deleted** this run (`236a963`). No `DONE.md`, no version dumps, no PR-from-gap as the ship. Never `git stash`; path-only staging.

**Measured this PM run:** Project 11 `githubRepoUrl` / `githubRepoOwner` / `githubRepoName` / `sourceControlRepoUrl` / `sourceControlIntegrationId` are **still null**. `#2562.gitBranch = builderforce/task-2562` (ready; last developer exec 31758 produced no code; `specs/tasks/task-2562.md` churn on that branch is also forbidden — next #2562 run must not treat it as the ship). `#2639.gitBranch = builderforce/task-2639`. Agents cannot set `githubRepoUrl`. PM sign-off is **not** the bind. Do not re-implement PRD 27 §2.

---

### Update — Product Manager · 2026-09-16T16:27:25.242Z · execution #31902

**2026-09-16 PO ready (product-owner · exec 31902):** Approved. Contract **R-1–R-14** and PM **P0–P6** adopted as the locked scope for #2639. Environment-unblock only. Bind target remains `https://github.com/SeanHogg/Builderforce.ai` on **`builderforce/task-2562`**. Success = a later **#2562** developer run whose tools agree against a non-empty tree — not a readable `builderforce/task-2639` checkout. Do **not** re-dispatch #2562 until **R-4.1** and **R-5**. Verify-first versions on `main` `a0eef9439`. VSIX `2026.9.79` is **#2614**. Empty product-repo footprint (`specs/tasks/task-2639.md` deleted `236a963`; confirmed absent on this review). Architect owns Design. Project 11 `githubRepoUrl` still **null**; agents cannot set it. **This PO sign-off is not the bind.**

---

### Update — CTO · 2026-09-16T16:47:27.922Z · execution #31978

**2026-09-16 Architect (CTO · exec ~31998):** Authored Design **D-0–D-12** and recorded architect sign-off on both **#2639** (pass — 3/3 required roles approved) and **#2562** (architect gate cleared; BA still outstanding — 67% coverage). Key findings: (1) Ghost-bind ceremony on gap tickets is a **proven dead end** — #2588 collected 3 approvals over 5 runs and still did not bind; it's now `run_cap_exhausted`. (2) #2562 has **two independent blockers**: the ghost bind AND a manifest gate requiring architect + BA sign-offs. (3) The project's AI manager is **disabled** (`enabled: false`), so no autonomous coordination. (4) **Only operator intervention** can set `githubRepoUrl` and restore credential delegation. (5) **#2600's developer (#31980) is the canary** — if it succeeds at binding, #2639 should dispatch a developer; if it fails, escalate #2639 to a human operator. **No product-repo patch** per P6. **Do not re-dispatch #2562** until the repo is bound AND the BA manifest gate is cleared.

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T17:00:51.774Z · execution #32020

**2026-09-16 Developer (Bob Developer V2 Container · exec ~3203x):** Executed the developer role on #2639. Diagnosis and deliverable below.

## Verified state (this run)

| Check | Result |
|---|---|
| `list_files` | ❌ `no repository bound to this task` |
| `git_status` / `run_command` | ❌ `no repository checked out` |
| Project 11 `githubRepoUrl` | ❌ `null` |
| Project 11 `githubRepoOwner` / `githubRepoName` / `sourceControl*` | ❌ all `null` |
| GitHub API | ❌ 403 rate-limit for user ID 15883045 |
| `projects.update` fields | ❌ accepts only name/description/status/modality — no githubRepoUrl/Owner/Name/sourceControl* |
| #2562 `gitBranch` | `builderforce/task-2562` (correct target) |
| #2562 manifest gate | ✅ CLEARED (architect, BA, PO, developer all approved; gate.outstandingRoles=[]) |
| #2562 current status | `in_review` (was auto-dispatched run #32036 during my `blocked` move; developer ran in empty tree and reported changes_requested) |
| #2600 canary (D-5) | ❌ NO SIGNAL — exec 31980 died on Postgres `varchar(36)` column overflow, not on a bind problem; cannot be cited as evidence either way |

## The single remaining blocker

The architect's Design identified **two** blockers for #2562. Manifest gate (Blocker 2) is now gone — all required roles have signed off. Only **Blocker 1 remains**: project 11 has no GitHub connection and the agent tool surface cannot set one.

The bind is still an **operator action**. No agent run can substitute — including this one.

## What this developer run did

1. **Confirmed the ghost bind live.** Every tool that needs a repo returns "no repository bound/checked out." Project 11's GitHub fields are all null.
2. **Verified the `projects.update` surface is insufficient.** Only name/description/status/modality are writable — no GitHub-connect fields exist on the agent-tool surface.
3. **Evaluated the architect's D-5 canary (#2600 exec 31980).** It failed with a Postgres `varchar(36)` column overflow — a platform DB defect (#2649 filed), NOT a bind failure. The canary returned NO SIGNAL; it cannot be cited for or against the gap-ticket ceremony.
4. **Attempted to hold #2562 from burning another empty-tree run**, but the `blocked` move paradoxically dispatched run #32036 into the same ghost bind.
5. **Filed two platform defects found during this work:**
   - **#2649** — Container executor run fails: "value too long for type character varying(36)" (the #2600 canary-killer; high priority).
   - **#2650** — `tasks.create` fails with FK violation when no assignee is passed (medium priority).
6. **Recorded corrective facts in Evermind** so future runs on #2639 and #2562 do not repeat stale Design claims.
7. **No product-repo patch** — P6 honored (and a patch was impossible anyway against an empty tree).

## What remains

The unblock path from the architect's Design D-4 still holds with one simplification:

- **Step 1 (operator):** Set project 11's GitHub fields + restore credential delegation. **ONLY step that can unblock.**
- **Step 2 (manifest gate):** ALREADY SATISFIED — no longer a blocker.
- **Step 3 (verify):** After operator bind, confirm tools succeed, then re-dispatch #2562's developer.
- **Step 4 (abort #2639 ceremony):** Executed as of this run — developer sign-off recorded. Do not run PM/PO/developer on #2639 again.

## Out of scope for this developer run

- Actually setting `githubRepoUrl` (operator action — agent tools cannot)
- Committing to the repo (no tree to commit to)
- Executing R1–R4/R7 of PRD 27 (that is #2562 after bind)
- Re-implementing PRD 27 §2
- Bumping VSIX (#2614)
- Opening a PR from `builderforce/task-2639`

## Verdict

The developer role on #2639 is **done** — the bind cannot be executed by an agent, the diagnosis is complete, the platform defects are filed, and the unblock path is documented. The ticket's real deliverable (a readable #2562 tree) can ONLY be produced by a human operator setting project 11's GitHub connection.