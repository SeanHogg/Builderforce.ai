> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2574
> _Each agent that updates this PRD signs its change below._

# PRD: Live re-test — file and staff two tasks on project 11 (R6)

## Problem & Goal

Work-mode VS Code chat can incorrectly refuse with a manager-role error, even when the operator is acting in a valid work session. Copied chat reports and diagnostics can omit staffing attribution, which blocks verification that an agent was actually staffed. Auth keys may still resolve as `developer` instead of the expected work-mode identity.

**Goal:** On project 11, file two tasks, staff both, and prove R6 end-to-end: work-mode VS Code chat does not return manager-role required; the copied report includes `Staffing:/as <agent>`; a `brain_chat_diagnostics` row exists after Copy; `builtin_chats_diagnostics` returns that row. If the key still resolves as developer, inspect `auth:bfk:v2:`.

## Target users / ICP roles

- **Operator / work-mode user** in VS Code chat (not manager UI).
- **Staffed agent** invoked via `/as <agent>` on a filed task.
- **Downstream verification agents** that consume this PRD and the copied report / diagnostics.

Manager-console-only flows are not the primary path for this re-test.

## Scope

In-scope for project **11** only:

1. File **two** tasks.
2. Staff both tasks (explicit `/as <agent>` staffing).
3. Drive the interaction from **work-mode VS Code chat**.
4. Copy the chat/report after staffing and confirm the copy payload.
5. Confirm diagnostics persistence and queryability:
   - `brain_chat_diagnostics` row written **after Copy**
   - `builtin_chats_diagnostics` returns that row
6. If identity still resolves as `developer`, check `auth:bfk:v2:` and record the resolved role/key prefix (no key material in the report).

## Functional requirements

1. **Task filing:** Create two distinct tasks on project 11 with enough context to staff an agent.
2. **Staffing:** Staff each task as a named agent. Staffing must be visible as `Staffing:/as <agent>` in the copied report (literal prefix plus the agent identifier used).
3. **Work-mode chat:** VS Code work-mode chat must accept the session. It must **not** return a manager-role-required error (or equivalent refusal).
4. **Copy report:** After staffing and chat, Copy must produce a report that includes the Staffing line for each staffed task.
5. **Diagnostics write:** Performing Copy must create a `brain_chat_diagnostics` row (timestamp after the Copy action).
6. **Diagnostics read:** `builtin_chats_diagnostics` must return that same row (matching identifiers / timestamps from the Copy).
7. **Auth fallback:** If the session key still resolves as `developer`, inspect `auth:bfk:v2:` and note whether work-mode identity is present, stale, or missing. Do not treat a developer-resolved key as a pass.

## Acceptance criteria

All of the following must hold on a live run against project 11:

- Two tasks exist on project 11 and both are staffed.
- Work-mode VS Code chat completes the flow with **no** “manager role required” (or equivalent) response.
- Copied report contains `Staffing:/as <agent>` for the staffed agent(s).
- A `brain_chat_diagnostics` row exists with a timestamp **after** Copy.
- `builtin_chats_diagnostics` returns that row.
- Fail the re-test if the auth key still resolves as `developer` unless `auth:bfk:v2:` is checked and the mismatch is documented as the blocking cause.

## Out of scope

- Filing or staffing tasks on any project other than 11.
- Changing product auth, role mapping, or `auth:bfk:v2:` implementation (inspect only).
- Manager-role UI, manager-only chat, or granting manager privileges.
- Unrelated chat diagnostics, historical rows from before this Copy, or non-work-mode clients.
- Exposing, copying, or rotating raw auth keys.

## Requirements

_Owned by the business-analyst. Signed 2026-09-16. This ticket is an **operational live re-test** of PRD 27 **R6** on **project 11**. It is **not** a product-code change. Do **not** ship `specs/tasks/task-2574.md`. Do **not** treat filing+staffing, a container developer identity, or a fixture-branch commit as an R6 pass._

Parent epic is **#2562** (PRD 27 — verify, version, and ship staffed work + chat diagnostics). Sibling **#2567** owns the same R6 **contract** (Requirements-only; not a second live run). This ticket owns the **live execution** of that contract: two staffed subjects plus proof that work-mode VS Code chat, Copy, diagnostics write/read, and work-mode identity all hold.

### R-1 — Project, parent, and non-product nature

- In-scope **only** on **project 11**. Do not file, staff, or re-test on any other project.
- Parent remains **#2562**. Do not re-parent the subjects or this ticket.
- **No product-repo patch** on this ticket: no API / VS Code / brain-ui / agent-loop change, no auth or `auth:bfk:v2:` implementation change (inspect only), no manager-role grant, no migration, no VSIX bump, no `DONE.md` / release-note work (those are other PRD 27 children).
- Requirements live on **this ticket PRD**. Do **not** add or merge `specs/tasks/task-2574.md` (or any docs-only dump). If that path exists on `builderforce/task-2574`, **drop it** — it must not ship. GitHub 403 this BA run could not re-read the branch; `main` does not contain the file.

### R-2 — Two subject tasks (file + staff) — already satisfied

Exactly **two** distinct subject tickets on project 11, both children of **#2574**. They **already exist** from developer execution **31660**. **Do not file replacements.** Do not add a third subject.

| Ticket | Title | Staffing line (literal) | assignedAgentRef |
| --- | --- | --- | --- |
| **#2594** | R6 re-test subject A — staff Ada for work-mode VS Code chat | `Staffing:/as Ada` | `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6` (Ada, Sr. Product Mgr) |
| **#2595** | R6 re-test subject B — staff John Coder for work-mode VS Code chat | `Staffing:/as John Coder` | `658608ba-59ab-4ec3-873d-211a89ea000f` (John Coder, Durable) |

Pass for this requirement = both tickets exist on project 11 **and** each `assignedAgentRef` matches the table. Filing+staffing **alone** is **not** an R6 pass (see R-16).

### R-3 — Subject tickets are fixtures, not product work

#2594 and #2595 exist **only** so work-mode VS Code chat can invoke `/as <agent>` and so the Copy report can include the Staffing line.

- Descriptions already instruct any dispatched agent: confirm staffing and **stop**.
- Do **not** write files, open a PR, change status, or invent scope on #2594 / #2595.
- Auto-run that opened `builderforce/task-2594` and `builderforce/task-2595` (BA commits, no PR) is **fixture noise**. **Ignore** those branches. **Do not merge** them. They are **not** R6 evidence.

### R-4 — Staffing attribution (literal Copy contract)

Staffing must be visible in the **copied report** as the **literal** prefix plus the agent identifier used:

- For #2594: `Staffing:/as Ada`
- For #2595: `Staffing:/as John Coder`

Rules:

- Prefix is `Staffing:/as ` (capital S, no space before the colon, one space after `/as`).
- Agent identifier is the **display name** used at `/as` time (`Ada`, `John Coder`), not the UUID, not a role key (`business-analyst`, `developer`), and not a coordinator auto-run agent.
- Both lines must appear in the **same** Copy payload. One of two is a fail.
- A board `assignedAgentRef` without the Copy line is **not** sufficient.

### R-5 — Client and session: work-mode VS Code chat only

The live session **must** be **work-mode VS Code chat**.

- **Not** manager-console chat, manager-only UI, a developer-container run, a Brain web chat, or any other client.
- The session must **accept** the operator. It must **not** return “manager role required”, “manager-role required”, or any equivalent refusal that the operator is not allowed to chat because they lack manager.
- Completing the flow from a container (execution 31660 or this BA run) does **not** satisfy R-5. Those runs are not work-mode VS Code.

### R-6 — `/as` invocation against the subjects

In that work-mode session the operator must invoke:

1. `/as Ada` against **#2594**
2. `/as John Coder` against **#2595**

- Both invocations are required.
- Invoking `/as` on a different ticket, a different agent, or only one of the two is a fail.
- Auto-run that dispatched Ada as **coordinator** on #2595 (assignee remains John Coder) is **not** an `/as John Coder` invocation.

### R-7 — Copy action and report contents

After staffing and chat, the operator must **Copy** the chat/report.

- Copy is the product Copy action in work-mode VS Code (the action that is specified to persist diagnostics). A manual paste into a ticket comment is evidence **only if** it is that Copy payload; it is **not** a substitute for the diagnostics write in R-8.
- The copied report **must** include both literal Staffing lines from R-4.
- Attach a **Copy excerpt** (the Staffing lines plus enough surrounding text to show it is the Copy payload) on **#2597** and on **#2574**. Do not attach raw auth keys.

### R-8 — `brain_chat_diagnostics` row after Copy

Performing Copy **must** create a `brain_chat_diagnostics` row whose timestamp is **strictly after** the Copy action.

- Historical rows from before this Copy **do not count**.
- Rows written by a non-Copy path, a different client, or a different chat **do not count**.
- Evidence: row **id** and **timestamp**, recorded on #2597 and #2574.

### R-9 — `builtin_chats_diagnostics` returns that row

`builtin_chats_diagnostics` **must** return the **same** row as R-8 (matching identifiers / timestamps from that Copy).

- A 0-row result, a different row, or a row whose timestamp is before Copy is a fail.
- This BA / developer-container tool list does **not** include `builtin_chats_diagnostics`. Agents on those slots **cannot** query it. The operator on #2597 must.

### R-10 — Auth fail-closed (`developer` is not a pass)

If the **work-mode VS Code** session key still resolves as `developer`:

1. Inspect `auth:bfk:v2:` in VS Code secret storage.
2. Record whether work-mode identity is **present**, **stale**, or **missing**.
3. Record **role** and **key prefix only**. **Never** paste, copy, or rotate raw key material.

Fail closed:

- A developer-resolved work-mode key **without** the `auth:bfk:v2:` inspection note is a **fail**.
- This container / BA run resolving as `developer` is **expected** for those slots and is **not** the R6 auth check (R-12).
- Do not treat a developer-resolved key as a pass even if Copy and diagnostics succeed.

### R-11 — Operator remainder is #2597 (human)

The live steps in R-5–R-10 are **operator-only**. They are captured as gap **#2597** (assigned to Sean Hogg `22c5fd96-973a-4980-ba74-77c09dad1604`).

- **Do not** re-dispatch the developer container, this BA slot, Ada, or John Coder as a substitute for #2597.
- #2597 may still auto-dispatch Ada as coordinator despite the human assignee — the **operator must complete the VS Code steps themselves**.
- Do not file a duplicate remainder ticket.

### R-12 — Container / BA identity is not the auth check

- Execution **31660** (Bob Developer container) and this BA execution are **not** work-mode VS Code sessions.
- They cannot: drive work-mode chat, perform Copy, write the post-Copy diagnostics row, call `builtin_chats_diagnostics`, or read `auth:bfk:v2:`.
- Do **not** record those runs as R-5–R-10 evidence.

### R-13 — Evidence that must be attached before #2574 can pass

Attach **all** of the following on **#2597** and copy the same facts onto **#2574** (PRD append or ticket comment) before ready-lane PO re-approval:

1. Confirmation that work-mode VS Code chat did **not** return manager-role required (R-5).
2. Copy excerpt containing both `Staffing:/as Ada` and `Staffing:/as John Coder` (R-4, R-7).
3. `brain_chat_diagnostics` **row id** and **timestamp after Copy** (R-8).
4. Confirmation that `builtin_chats_diagnostics` returned that row (R-9).
5. If the work-mode key still resolved as `developer`: `auth:bfk:v2:` **prefix + role + present/stale/missing** (R-10). No key material.

Until that packet exists, R6 is **not passed** and ready-lane PO must not approve.

### R-14 — Relationship to #2567, #2562, and other PRD 27 children

- **#2567** = R6 **contract** ticket (BA Requirements only; same AC; **not** a second live re-test; do not file another pair of subjects).
- **#2574** (this ticket) = live **file + staff + prove** vehicle. Subjects #2594 / #2595. Remainder #2597.
- **#2562** = parent epic. R1–R4 / R7 (type-check, versions, VSIX, DONE.md, notes) are **out of this ticket**.
- **#2566** (R5 migrations 1177/1178) is **out of this ticket**.
- **#2614** (VSIX 72 vs 79) is **out of this ticket**.
- Do not move OKRs from this BA slot. Filing+staffing and a Requirements freeze are not Extraction Rate / Tasks-completed movement.

### R-15 — Out of scope (do not)

- Filing or staffing on any project other than 11.
- Changing product auth, role mapping, or `auth:bfk:v2:` implementation.
- Manager-role UI, manager-only chat, or granting manager privileges.
- Unrelated / historical `brain_chat_diagnostics` rows, non-work-mode clients.
- Exposing, copying, or rotating raw auth keys.
- Merging `builderforce/task-2594` or `builderforce/task-2595`.
- Shipping `specs/tasks/task-2574.md`.
- Re-implementing PRD 27 §2 (staffed work / diagnostics) or any verify/version/package child.
- Treating coordinator auto-run as `/as` staffing.

### R-16 — Pass / fail (fail-closed)

**R6 passes on #2574 only when every item holds on a live project-11 run:**

1. #2594 and #2595 exist and are staffed as R-2.
2. Work-mode VS Code chat completes with **no** manager-role-required (R-5).
3. `/as Ada` on #2594 and `/as John Coder` on #2595 (R-6).
4. Copy report contains both literal Staffing lines (R-4, R-7).
5. A `brain_chat_diagnostics` row exists with timestamp **after** that Copy (R-8).
6. `builtin_chats_diagnostics` returns that row (R-9).
7. Work-mode identity is not an unchecked `developer` (R-10).
8. The R-13 evidence packet is attached to #2597 and #2574.

**Any missing item = fail.** Filing+staffing (R-2) without R-5–R-10 is a **partial** and must stay fail-closed.

### Assumptions (BA, 2026-09-16)

- File+staff (R-2) is already true from execution 31660; this BA pass does not re-file.
- `specs/tasks/task-2574.md` is **absent on `main`**. File-change history shows it was created on this ticket’s branch; it must not merge. This BA run did not add it.
- GitHub API is 403 rate-limited (user 15883045) this run; branch contents of `builderforce/task-2574` could not be re-read.
- Project 11 `githubRepoUrl` may still be null; that does **not** block R6 (R6 is live VS Code, not a checkout).
- BA lane delivers this Requirements section only. Live AC remain on #2597.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T16:00:00Z_

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T16:02:29.390Z_

## Design

_Owned by the architect — to be authored._

## Implementation Notes

Filed and staffed two R6 subject tickets on project 11 from this developer-container run (execution 31660). No product code change and no PR — this ticket is an operational live re-test, and the bound repo (seanhogg/builderforce.ai) is not required for filing/staffing.

### Subjects filed (project 11, parent #2574)

| Ticket | Title | Staffing line | assignedAgentRef | Auto-run |
| --- | --- | --- | --- | --- |
| **#2594** | R6 re-test subject A — staff Ada for work-mode VS Code chat | `Staffing:/as Ada` | `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6` (Ada, Sr. Product Mgr) | dispatched (`will_run`). A business-analyst run already opened `builderforce/task-2594` (1 commit ahead of main, no PR). That branch is **not** R6 evidence — ignore it. |
| **#2595** | R6 re-test subject B — staff John Coder for work-mode VS Code chat | `Staffing:/as John Coder` | `658608ba-59ab-4ec3-873d-211a89ea000f` (John Coder, Durable) | dispatched (`will_run`; autoRun.agentRef reported Ada as coordinator — **assignee remains John Coder**). A business-analyst run already opened `builderforce/task-2595` (1 commit ahead of main, no PR). Ignore that branch. |

Both descriptions instruct any dispatched agent: confirm staffing and **stop**. No files, no PR, no invented scope. Auto-run still fired process-template BA slots; those commits are fixture noise.

### What this container cannot do (operator remainder)

This run is Bob Developer (V2 Container) on the #2574 producer slot. It is **not** a work-mode VS Code chat session.

- No VS Code work-mode client, so the manager-role-required check was **not** executed.
- No Copy action, so no `brain_chat_diagnostics` row was written by this run.
- `builtin_chats_diagnostics` is **not** in this agent's tool list; the row cannot be queried here.
- `auth:bfk:v2:` lives in VS Code secret storage; this container cannot inspect it. This run resolving as `developer` is expected for the producer slot and is **not** an R6 pass.

Captured as gap **#2597** (assigned to Sean Hogg `22c5fd96-973a-4980-ba74-77c09dad1604`): work-mode chat → `/as Ada` on #2594 / `/as John Coder` on #2595 → Copy → diagnostics write/read → auth:bfk:v2: if still developer. Note: #2597 still auto-dispatched Ada as coordinator despite the human assignee — operator must complete the VS Code steps themselves.

### Auth note

Do not treat this container's developer identity as the R6 auth check. R6 fails closed if the **work-mode VS Code** key still resolves as `developer` unless `auth:bfk:v2:` is inspected and the mismatch is documented.

### Sign-off evidence

- executionId `31660`
- tasks `#2594`, `#2595` (staffed), `#2597` (operator remainder)
- no `prUrl` / `diffFiles` (no product code change)

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:39:43.054Z_

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:39:47.112Z · execution #31660

Developer (execution 31660): filed+staffed #2594 (Ada, Staffing:/as Ada) and #2595 (John Coder, Staffing:/as John Coder) on project 11. Operator remainder (VS Code work-mode, Copy, brain_chat_diagnostics, builtin_chats_diagnostics, auth:bfk:v2:) captured as #2597 assigned to Sean Hogg. R6 is NOT passed from this run — container cannot drive work-mode chat. Auto-run also spawned BA commits on builderforce/task-2594 and task-2595; those are fixture noise, not R6 evidence.

---

### Update — Product Manager · 2026-09-16T15:50:21.240Z · execution #31725

**PO ready-lane verdict (execution 31725): changes_requested.** Inspected execution 31660, tickets #2594/#2595/#2597, accountability, and file-change list (GitHub 403 blocked reading `specs/tasks/task-2574.md`). Filing+staffing on project 11 is real; R6 live AC are not. Do not treat container developer identity as the auth check. Operator must complete #2597 and attach Copy excerpt + post-Copy diagnostics row id/timestamp (and auth:bfk:v2: prefix/role if still developer) before this ticket can be approved. Delete `specs/tasks/task-2574.md` — it must not ship. No OKR movement.

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T16:03:10.179Z · execution #31773

**BA ready-lane (Ada, execution 31773, 2026-09-16):** Requirements **R-1–R-16** authored on this ticket PRD. Contract: project-11 live R6 only; subjects **#2594** (`Staffing:/as Ada`, `fdbbd9af-80eb-483e-a5d0-557dbfdd2cc6`) and **#2595** (`Staffing:/as John Coder`, `658608ba-59ab-4ec3-873d-211a89ea000f`) already filed+staffed (exec 31660) — do not re-file; work-mode VS Code chat must not return manager-role required; Copy must contain both literal Staffing lines; `brain_chat_diagnostics` row **after** Copy; `builtin_chats_diagnostics` returns that row; work-mode `developer` key fail-closed unless `auth:bfk:v2:` prefix/role/present|stale|missing is documented (no key material). Remainder **#2597** (Sean Hogg) — do not re-dispatch a container as substitute. Dropped `specs/tasks/task-2574.md` from `builderforce/task-2574` (commit `182687ab0ab4034a36121be778226403668c637f`). Ignore fixture branches task-2594/task-2595. Sibling **#2567** is the contract ticket, not a second live run. R6 is **not passed** until the R-13 evidence packet is attached. No OKR movement. No product-behavior patch.