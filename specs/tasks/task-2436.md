> **PRD** — drafted by Bob Developer (V2 (Container)) · task #2436
> _Each agent that updates this PRD signs its change below._

# PRD: Chat list progress percentage (VSIX)

## Problem & Goal

Chat lists in the VS Code extension show titles and timestamps but not ticket completion. Users scanning the **Chats** sidebar or the in-chat switcher cannot tell which conversations are done vs still in progress without opening each chat.

**Goal:** Surface a per-chat ticket progress percentage on every chat list row (sidebar tree and in-chat dropdown), computed once on the server and returned with `GET /api/brain/chats`, with list UI refreshing when ticket health changes.

## Target users / ICP roles

- Developers using the Brain VS Code extension who juggle multiple ticket-backed chats
- Anyone triaging or switching chats from the sidebar or chat switcher

## Scope

1. Extend `GET /api/brain/chats` to include per-chat `progressPct`, batched via `ChatTicketService` (no N+1).
2. Add optional `progressPct?: number` to `BfBrainChat` in `bfApi.ts`.
3. Render the percentage in `sessionsTree.ts` on `chat item.description`.
4. Render the same percentage in the brain-ui chat switcher / dropdown.
5. Refresh list/switcher display when ticket health changes.

## Functional requirements

- **List payload:** Each chat in `GET /api/brain/chats` includes `progressPct` when ticket progress is known. Computation is batched server-side through `ChatTicketService`.
- **Client type:** `BfBrainChat` exposes `progressPct?: number`. Clients treat a missing value as “no progress to show.”
- **Sidebar (Chats):** Each chat row shows progress next to title/time, e.g. `· 85%`.
- **Chat switcher:** Dropdown items show the same percentage, same source field.
- **Refresh:** When ticket health updates, sidebar rows and the switcher re-read/re-render `progressPct` without a separate per-chat progress fetch.
- **Performance:** Listing chats must not issue per-chat progress requests.

## Acceptance criteria

- Chat rows in the VSIX sidebar show `· 85%` (or equivalent) next to title/time when `progressPct` is present.
- The chat switcher dropdown shows the same percentage for the same chats.
- Progress is computed server-side on the list endpoint; no N+1 API calls for progress.
- Chats without ticket progress omit the percentage rather than showing a misleading `0%` or placeholder.
- After a ticket health change, both the sidebar list and the switcher reflect the updated percentage.

## Out of scope

- Changing how ticket progress itself is calculated inside `ChatTicketService`
- Progress UI on surfaces other than the Chats sidebar tree and the in-chat switcher/dropdown
- Filtering, sorting, or grouping chats by progress
- Web/non-VSIX chat lists
- Historical progress charts, tooltips with ticket breakdown, or click-through from `%` to ticket details
- New endpoints dedicated to progress; progress rides only on the existing chats list response

## Requirements

_Owned by the business-analyst — to be authored._

## Design

_Owned by the architect — to be authored._

## Implementation Notes

This is an **epic composition contract**. Production code ships on children **#2437–#2444**, not on `builderforce/task-2436`. Do **not** dump VSIX, `packages/brain-ui`, or API source onto the epic branch. A docs-only `specs/tasks/task-2436.md` (or the same dump on a child branch) is **not** the implementation and must not ship.

**Bind:** project 11 `githubRepoUrl=null`. Developer containers fail `list_files` / `read_file` / `git_*` / `run_command` with “no repository bound”. Bind **SeanHogg/Builderforce.ai** as the child ticket’s `gitBranch` before any code PR. Until then, children stay type/API/UI tickets — do not invent a conceptual stand-in.

### Child map (do not re-scope)

| Child | Owns | Must not do |
| --- | --- | --- |
| **#2437** | `GET /api/brain/chats` attaches per-chat `progressPct` in **one** `ChatTicketService` batch | N+1 per chat; change how pct is calculated inside the service |
| **#2438** | `progressPct?: number` on `BfBrainChat` in `clients/vscode/src/bfApi.ts` | Rename/remove `ticketProgressPct`; UI; API |
| **#2439** | Sidebar Chats tree: append ` · {n}%` on `sessionsTree.ts` **item.description** | Rewrite title; prefix `%` on the label |
| **#2440** | brain-ui chat switcher/dropdown: same suffix, same field | Title-encoding / U+0025 bugs; new endpoints |
| **#2441** | Refresh tree + switcher from the **list** payload when ticket health changes | New progress endpoint; per-chat fetch |
| **#2442** | AC: sidebar shows `· 85%` (or equivalent) when present | — |
| **#2443** | AC: switcher shows the **same** percentage for the same chats | — |
| **#2444** | AC: no N+1 — progress computed server-side on the list request | — |

### Field contract (resolves #2438 open point)

Epic goal is **ticket completion on chat list rows**, not streaming/generation progress.

Existing list fields stay:

- `ticketCount?: number`
- `ticketProgressPct?: number \| null` — `null` / omitted = no linked tickets

Add:

- `progressPct?: number` — optional **number** (not `number \| null`). Absent = unknown / nothing to show.

**Populate `progressPct` from the same ChatTicketService rollup as `ticketProgressPct`:**

- No linked tickets / no summary → **omit** `progressPct`. Keep `ticketProgressPct: null` (or omit).
- Linked tickets with a known pct, **including 0** → set `progressPct` to that integer 0–100 **and** set `ticketProgressPct` to the same number.
- When both are present they **must match**.

**Forbid** `progressPct: summary?.pct ?? 0` when there is no summary. That is a misleading `0%` and violates epic AC (“chats without ticket progress omit the percentage”). Child #2437 BA “never null / default 0” is **overruled** by this epic AC.

Numeric **0 shows** (` · 0%`) — that is a real incomplete ticket. `null` / `undefined` **omits**. Same rule as project fact `ticket-2440-progresspct-not-encoding`.

UI reads `progressPct` first; if absent, may fall back to `ticketProgressPct` **only when it is a finite number** (not `null`). That keeps #2551’s existing list fields visible if #2437 has not landed yet.

Do **not** treat `progressPct` as in-flight generation. Token/stream progress is out of scope.

### Overlap with bug #2551

#2551 owns ChatTicketService ceremony totals (`total: 1` for incomplete spec/roadmap/retro), `aggregateTicketHealth` treating `total: 0` as weight 1, and attaching `ticketCount` + `ticketProgressPct`. This epic **must not** re-implement that math (epic out of scope: “changing how ticket progress itself is calculated”).

#2551 display sketch was a **title prefix** `67% · title`. This epic’s AC is a **meta suffix** next to title/time: ` · 85%`.

**Bind display here:** keep the chat **title** unchanged. Put ` · {n}%` on the **description / secondary** slot (`TreeItem.description` in `sessionsTree.ts`; equivalent secondary text in the brain-ui switcher). Do not also prefix the title. If #2551 later prefixes the title, drop one of the two — do not show `%` twice.

### Server (#2437, #2444)

1. In the `GET /api/brain/chats` handler, after the page of chats is loaded, call the existing batch (`ChatTicketService.summarizeChatsTicketHealth` / `attachChatTicketSummaries` / `rollupChatTicketHealth` — use the helper already on main, do not add a parallel rollup).
2. One batch per list request, keyed by the chat ids on that page. No per-chat HTTP and no per-chat progress queries.
3. Fail-soft: if the batch throws, still return the chat list; omit `progressPct` (and leave `ticketProgressPct` as today).
4. No new endpoints. Progress rides only on this list response.
5. Test: spy that the batch is invoked **once** per list request, not once per chat.

### Client type (#2438)

File: `clients/vscode/src/bfApi.ts`, `export interface BfBrainChat`.

Add `progressPct?: number` with a one-line JSDoc: ticket rollup 0–100 from `GET /api/brain/chats`; absent on older servers / chats with no ticket health; distinct from `ticketProgressPct` (`null` = no linked tickets).

Preserve every existing field. `BrainChat` in `brain-embedded` is a different type — only touch it if the list payload is mapped through it; do not invent a second semantic.

### Sidebar (#2439, #2442)

File: `clients/vscode/src/sessionsTree.ts`.

Shared helper (same module or a tiny util used by tree + switcher):

```ts
function formatChatListProgressPct(
  chat: { progressPct?: number; ticketProgressPct?: number | null },
): string {
  const raw =
    typeof chat.progressPct === "number" && Number.isFinite(chat.progressPct)
      ? chat.progressPct
      : typeof chat.ticketProgressPct === "number" && Number.isFinite(chat.ticketProgressPct)
        ? chat.ticketProgressPct
        : undefined;
  if (raw === undefined) return "";
  return ` · ${Math.round(raw)}%`;
}
```

Append the return value to **`item.description`** (existing time/meta). Do not replace `item.label`. Empty string when unknown — never `" · 0%"` from a missing field, never a placeholder.

`conversationTreeLabel` already reads `ticketProgressPct`. Do not duplicate the percentage onto the label in this epic.

### Switcher (#2440, #2443)

brain-ui chat switcher / dropdown (and the VSIX webview that hosts it). Same helper, same source field, same ` · {n}%` suffix on secondary/meta text. Numeric 0 shows; null/undefined omits. Not a title-encoding bug.

### Refresh (#2441)

When ticket health changes, **re-read** `GET /api/brain/chats` on the existing list-invalidation path (the same refresh that already reloads the Chats tree). Re-render sidebar rows and the switcher from the new `progressPct`. No dedicated progress fetch. No client-side recompute of ticket %.

### Out of scope (repeat)

- ChatTicketService pct formula / ceremony `total` (that is #2551)
- Web/non-VSIX chat lists
- Filter/sort/group by progress
- Charts, tooltips, click-through from `%` to the ticket
- New endpoints
- Dumping implementation onto this epic branch while unbound

_— rewritten by Bob Developer (V2 (Container)) · 2026-09-16T15:46:50.958Z_

## Review

### Verdict: **changes_requested** (code-reviewer · in_review · execution #31880)

This is a **composition epic**. Developer Implementation Notes (exec #31674) correctly bind production code to children **#2437–#2444**, not `builderforce/task-2436`. That contract is sound. The GitHub PR and the feature acceptance criteria are **not** met.

### Evidence inspected
- PR [#830](https://github.com/SeanHogg/Builderforce.ai/pull/830) (`builderforce/task-2436`): was a docs-only dump of `specs/tasks/task-2436.md`. Deleted in this review ([43a4f13](https://github.com/SeanHogg/Builderforce.ai/commit/43a4f13f3f1921eb904034c4acb05654d36dd86c)). Diff is now empty (`files: 0`, `codeChanged: false`).
- Ticket PRD Implementation Notes + PO ready verdict (exec #31765).
- Children **#2437–#2444**: all `ready`/`todo`; **0/8 done**; `codeDelivered: false`; **no GitHub PRs**; child branches have unmerged commits with “no pull request was ever opened”.
- Main (SeanHogg/Builderforce.ai): `BfBrainChat` has `ticketCount` / `ticketProgressPct` only — **no** `progressPct`. `conversationTreeLabel` and `chatSwitcherLabel` still **prefix** `{n}% · title` (#2551 sketch), which is not this epic’s suffix-on-description AC.

### Blockers
1. **Forbidden dump.** `specs/tasks/task-2436.md` must not ship on this epic. Removed. Do **not** re-add it. A docs dump is not the implementation.
2. **Acceptance criteria unmet on main.** Sidebar ` · {n}%` on `item.description`, switcher same field/same chats, batched `progressPct` on `GET /api/brain/chats` (no N+1) are not in product code. Children have not opened GitHub PRs.
3. **Do not merge PR #830 as the feature.** An empty or docs-only epic PR is not shipped VSIX progress. Merging it would close the epic while the UI/API work is still on children.

### High
4. Main still **prefixes** the chat title. Epic AC is a **meta suffix** ` · {n}%` on `sessionsTree` `item.description` and the brain-ui switcher secondary/meta; **title stays title-only** (one `%` — do not also keep the prefix).
5. `#2438` (`progressPct?: number` on `BfBrainChat` in `clients/vscode/src/bfApi.ts`) is not landed.

### Medium
6. Child **#2437** description still says `progressPct: summary?.pct ?? 0`. Epic AC **overrules** that: omit when unknown; never default 0; numeric 0 shows only when it is a real incomplete ticket.
7. Child **#2440** description still says paint via `chatSwitcherLabel` title encoding. Epic binds suffix on secondary/meta, not title-encoding.

### Advisory
8. Ready-lane **BA Requirements** and **Architect Design** sections remain unauthored (unsigned required slots). That does not substitute for child implementation and is not this review’s producing-role fix list.

### Fixes for the producing role
- Leave `builderforce/task-2436` **without** `specs/tasks/task-2436.md` and **without** VSIX / `packages/brain-ui` / API dumps.
- Do **not** treat PR #830 as complete or mergeable as the feature.
- Land children **#2437–#2444** (bind SeanHogg/Builderforce.ai; open GitHub PRs) per the child map. Re-request in_review of this epic only after those ACs are in product code.

_— Code Reviewer (validator-t1) · 2026-09-16_

_— rewritten by Validator · 2026-09-16T16:22:38.249Z_

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Bob Developer (V2 (Container)) · 2026-09-16T15:46:55.742Z · execution #31674

**2026-09-16 — Bob Developer (V2, execution #31674):** Authored Implementation Notes as the epic composition contract. Code is **not** on `builderforce/task-2436` (githubRepoUrl=null; repo tools fail “no repository bound”). Children: #2437 API batch `progressPct`, #2438 `BfBrainChat` type, #2439 sessionsTree `description`, #2440 brain-ui switcher, #2441 list refresh, #2442–#2444 ACs. Decisions: (1) `progressPct` is ticket rollup, not streaming; (2) omit when unknown — do **not** `?? 0`; numeric 0 shows; (3) keep `ticketProgressPct`; UI suffix ` · {n}%` on description/meta, not a title prefix (avoids double-% with #2551); (4) do not re-implement ChatTicketService math (#2551 owns ceremony totals / aggregateTicketHealth). Drop any docs-only `specs/tasks/task-2436.md`. Bind SeanHogg/Builderforce.ai on each child branch before coding.

---

### Update — Product Manager · 2026-09-16T15:58:16.253Z · execution #31765

**2026-09-16 — Product Owner (ready-lane review, execution #31765):** Approved epic **scope + acceptance criteria**. This ticket is a composition epic: production code ships on children **#2437–#2444**, not on `builderforce/task-2436`. Binding (product, not implementation): (1) `progressPct` is the ChatTicketService **ticket** rollup on `GET /api/brain/chats` (one batch; no N+1; no new endpoints); (2) **omit** when unknown — never `summary?.pct ?? 0`; numeric **0 shows** (` · 0%`); `null`/`undefined` omit; (3) UI is a **meta suffix** ` · {n}%` on `sessionsTree` `item.description` and the brain-ui switcher secondary/meta — **do not** prefix or rewrite the chat title (avoids double-% with #2551); (4) keep `ticketProgressPct`; when both present they must match; UI may fall back to a finite `ticketProgressPct` only if `progressPct` is absent; (5) refresh by re-reading the **list** payload on the existing invalidation path. Child **#2437** BA “never null / default 0” remains **overruled** by this AC. Distinct from #2608 `ctx {n}%`. BA Requirements and Architect Design stay those roles’ ready-lane slots; this verdict does not author them. Do not dump VSIX/API source or `specs/tasks/task-2436.md` onto the epic branch.

---

### Update — Product Manager · 2026-09-16T15:59:41.374Z · execution #31765

**2026-09-16 — Product Owner (ready, follow-up binding):** Grounded against main: `BfBrainChat` already has `ticketCount` / `ticketProgressPct` and does **not** yet have `progressPct` (#2438). `ChatTicketService.summarizeChatsTicketHealth` already batches `{ ticketCount, ticketProgressPct }` (#2437 attaches `progressPct` from that same rollup). `conversationTreeLabel` and `chatSwitcherLabel` currently **prefix** `{n}% · title` — that is #2551's sketch, not this epic's AC. **One `%` only:** when the suffix ships, title stays **title-only** (stop the prefix — already in #2442's BA text). Do not leave prefix + `description` suffix both showing the same number. Switcher (#2440/#2443) uses the same suffix-on-secondary rule, not `chatSwitcherLabel`'s current title encoding.