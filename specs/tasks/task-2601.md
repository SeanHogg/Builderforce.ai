> **PRD** — drafted by Ada (Sr. Product Mgr) · task #2601
> _Each agent that updates this PRD signs its change below._

# PRD: Restore `canvasChange.ts` after probe overwrite (revert 0267ffd)

## Problem & Goal

Commit `0267ffd360e524dbd6ae8a93d1e71df8202d4dee` on `builderforce/task-2554` did **not** overwrite the real canvas domain module. It **added** a nested probe file at the wrong prefix:

`Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts`

Content at the tip of `builderforce/task-2554` (verified 2026-09-16):

```ts
// probe — will be overwritten; write_file capability check
export {};
```

The developer container had no usable git checkout, so the write landed as a **created** path relative to a sparse/wrong bind of main (repo root treated as if it were already under `Builderforce.ai/`). Diff of `0267ffd` is `+2 / −0`.

The **real** module is intact on both `origin/main` and `builderforce/task-2554`:

`frontend/src/domains/canvas/domain/canvasChange.ts` (138 lines)

Public surface: `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction`. It does **not** export `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE`.

**Risk if `builderforce/task-2554` is merged as-is:** a junk `Builderforce.ai/` prefix tree ships on main. The live canvas-change API would keep working (the real file is untouched), but the probe must not merge.

**Goal:** Revert `0267ffd` (or delete the nested probe path) so `builderforce/task-2554` matches `origin/main` at that path — meaning the nested file is **absent**. Do not merge `builderforce/task-2554` until that is true. Do not rewrite the real 138-line module.

**Reference:** `SeanHogg/Builderforce.ai@0267ffd`

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:07.065Z_

## Target users / ICP roles

- Downstream agents executing this restore (primary consumers of this PRD)
- Frontend canvas domain maintainers
- Reviewers / operators gating merge of `builderforce/task-2554`

## Scope

In scope:

- Revert `0267ffd` on `builderforce/task-2554`, **or** delete `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (and empty `Builderforce.ai/` prefix dirs) so the nested path is absent — the same state as `origin/main`
- Confirm the **real** `frontend/src/domains/canvas/domain/canvasChange.ts` is unchanged vs main (138 lines; exports `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction`)
- Hold merge of `builderforce/task-2554` until the nested probe is gone
- No product/behaviour change beyond undoing the probe add

Out of this restore’s file set: PRD 27 survey wiring, `PHASE_SURFACES`, `CONNECTED_CANVAS_ACTIONS` edits, and any rewrite of canvas-change semantics.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:11.085Z_

## Functional requirements

1. **FR-1 Delete the nested probe, do not “restore” into it.** `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` must be **absent** on the tip of `builderforce/task-2554`, matching `origin/main` (that path does not exist on main). Writing the real 138-line module into the nested prefix is a **fail**.
2. **FR-2 Preferred restore path.** Prefer `git revert 0267ffd` (SHA `0267ffd360e524dbd6ae8a93d1e71df8202d4dee`). Equivalent delete of the nested blob is acceptable. Do not rewrite history unless revert cannot apply.
3. **FR-3 Real domain API stays the main blob.** `frontend/src/domains/canvas/domain/canvasChange.ts` must remain the 138-line module that exports `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, and `canInvokeCreationObjectAction`. It must **not** export `CANVAS_CHANGE_PROBE`, `CanvasChangeKind`, or `applyObjectChange`.
4. **FR-4 No stub ship.** Neither the 2-line `export {}` probe nor any `CANVAS_CHANGE_PROBE` / `"task-2554-repo-probe"` string may be present on the branch tip that would be merged.
5. **FR-5 Merge gate.** `builderforce/task-2554` must not be merged until FR-1–FR-4 hold.
6. **FR-6 Minimal blast radius.** Do not change the real canvas domain module, `canvasChange.test.ts`, PRD 27 survey work, `PHASE_SURFACES`, or unrelated files as part of this restore.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:14.961Z_

## Acceptance criteria

- [ ] Tip of `builderforce/task-2554` does not contain `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (path absent, as on `origin/main`).
- [ ] Tip does not contain `CANVAS_CHANGE_PROBE`, `"task-2554-repo-probe"`, or the 2-line write_file probe (`export {};` under that nested prefix).
- [ ] `git diff origin/main...builderforce/task-2554 -- frontend/src/domains/canvas/domain/canvasChange.ts` is empty.
- [ ] Real module still exports `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, and `canInvokeCreationObjectAction` (consumers / `canvasChange.test.ts` unchanged).
- [ ] Restore is attributable to revert of `0267ffd` (or an equivalent delete commit naming that SHA / #2601).
- [ ] `builderforce/task-2554` is not merged until the above are true.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:19.896Z_

## Out of scope

- Completing, debugging, or shipping PRD 27 survey work on `#2554` / `builderforce/task-2554` (B0–B5, `PHASE_SURFACES`, form/idea acts)
- Rewriting canvas-change semantics, types, `CONNECTED_CANVAS_ACTIONS`, or inventing `CanvasChangeKind` / `applyObjectChange` to match the original (incorrect) ticket prose
- Writing the real 138-line module into the nested `Builderforce.ai/frontend/...` path
- Adding a docs-only `specs/tasks/task-2601.md` (or similar) as a stand-in for the revert
- Fixing platform sparse-tree / “created vs main” recording
- Changing CI, merge queue, or branch protection beyond holding the #2554 merge
- Force-push / history rewrite unless revert cannot delete the nested file
- Any new probe, placeholder, or partial reimplementation of `canvasChange.ts`

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:23.980Z_

## Requirements

**Owner:** Business Analyst (Ada) · 2026-09-16  
**Kind:** defect restore / merge-gate — no product behaviour change.

### Verified facts (do not re-derive)

Inspected `SeanHogg/Builderforce.ai` on 2026-09-16:

| Claim in the original ticket / PM draft | Verified state |
|---|---|
| `0267ffd` overwrote the real canvas domain module | **False.** `0267ffd360e524dbd6ae8a93d1e71df8202d4dee` is an **add** (`+2 / −0`) of a **new nested path**. |
| Probe body is `export const CANVAS_CHANGE_PROBE = "task-2554-repo-probe"` | **False.** Tip of `builderforce/task-2554` at the nested path is the 2-line write_file probe: `// probe — will be overwritten; write_file capability check` + `export {};` |
| Real module was destroyed (~128 lines, `CanvasChangeKind` / `applyObjectChange`) | **False.** `frontend/src/domains/canvas/domain/canvasChange.ts` is **intact and identical** on `origin/main` and on `builderforce/task-2554` (138 lines). |
| Nested `Builderforce.ai/frontend/...` exists on main | **False.** That prefix tree is absent on `origin/main`. Matching main means the nested file **must not exist**. |

**Real module path (source of truth):** `frontend/src/domains/canvas/domain/canvasChange.ts`

**Spurious probe path (the only damage from `0267ffd`):** `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts`

**Real public surface** (also the imports in `frontend/src/domains/canvas/domain/canvasChange.test.ts`):

- `ProposedCanvasChange` (discriminated union of object/connection add/update/delete/layout/action)
- `canvasChangesCanAutoApply`
- `CONNECTED_CANVAS_ACTIONS`
- `canInvokeCreationObjectAction`

The module does **not** export `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE`. Restoring those names would be a **new** API, which is out of scope.

### BR-1 — Target of the restore

The code change lands on **`builderforce/task-2554`** (the branch that carries `0267ffd`), not as a rewrite of the real module on `main` / `builderforce/task-2601`. Ticket #2601 exists so this restore is scheduled, signed, and gated independently of PRD 27 survey work.

### BR-2 — Required action

Prefer `git revert 0267ffd` (full SHA `0267ffd360e524dbd6ae8a93d1e71df8202d4dee`). Equivalent: **delete** `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (and any now-empty `Builderforce.ai/` prefix directories) with a commit that cites revert of `0267ffd`. Do **not** force-push / rewrite history unless revert cannot apply.

### BR-3 — What must be true after the restore

1. Nested path `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` is **absent** from the tip of `builderforce/task-2554` (same as `origin/main`).
2. Real path `frontend/src/domains/canvas/domain/canvasChange.ts` is **byte-for-byte unchanged** vs `origin/main` (138-line domain module; no probe comments; no `CANVAS_CHANGE_PROBE`).
3. Existing consumers keep compiling against `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction`. `canvasChange.test.ts` is not rewritten as part of this ticket.
4. No `Builderforce.ai/` prefix tree is introduced onto `main` by merging #2554.

### BR-4 — What must not happen

- Do **not** write the 138-line module into the nested `Builderforce.ai/frontend/...` path. That would still ship junk if #2554 is merged.
- Do **not** modify `CONNECTED_CANVAS_ACTIONS` (including `form` / `idea` acts), `PHASE_SURFACES`, or any PRD 27 survey wiring. This ticket is not #2554 product work.
- Do **not** invent `CanvasChangeKind` / `applyObjectChange` to satisfy the original (incorrect) ticket prose.
- Do **not** add a docs-only `specs/tasks/task-2601.md` (or similar) as a stand-in for the revert.
- Do **not** merge `builderforce/task-2554` until BR-3 holds.

### BR-5 — Merge / ship gate

`builderforce/task-2554` stays unmerged until:

- `git show builderforce/task-2554:Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` fails (path gone), **and**
- `git diff origin/main...builderforce/task-2554 -- frontend/src/domains/canvas/domain/canvasChange.ts` is empty.

Passing this gate is the definition of done for #2601. Completing PRD 27 B0–B5 is **not**.

### BR-6 — Traceability

Restore commit message must name `0267ffd` / #2554 / #2601 so the revert is attributable. No other files except the nested probe (and empty prefix dirs) belong in that commit.

### Assumptions

- Platform “created vs main” recording of the nested path is a tooling defect; fixing that recorder is out of scope (see Out of scope).
- Later commits on `builderforce/task-2554` (`8ececde`, `b006b5d`) are PRD docs for #2554 and do not further overwrite the real module. If a future commit **does** overwrite `frontend/src/domains/canvas/domain/canvasChange.ts`, restore **that** path from `origin/main` as well — but that is not the current tip.
- This BA pass does not itself revert `0267ffd` on `task-2554`; developer on #2601 / #2554 executes BR-2 against the branch that carries the probe.

_— rewritten by Ada (Sr. Product Mgr) · 2026-09-16T15:46:03.232Z_

## Design

**Owner:** Architect (CTO) · 2026-09-16 · execution #31868
**Kind:** merge-gate restore. No new product surface. No canvas-domain behaviour change. Adopt BA BR-1–BR-6 and PM P0–P5 unchanged.

This ticket is not a rewrite of `canvasChange.ts`. It is a **delete of a nested junk path** so `builderforce/task-2554` can be merged without shipping a `Builderforce.ai/` prefix tree onto `main`.

### D-0 — Bind (fail-closed)

Developer executes against checkout **`builderforce/task-2554`** on `SeanHogg/Builderforce.ai` (the branch that carries `0267ffd360e524dbd6ae8a93d1e71df8202d4dee`).

This #2601 workspace bind is `builderforce/task-2601` / `main`. That bind:

- Has the **real** 138-line module at `frontend/src/domains/canvas/domain/canvasChange.ts` (identical to `origin/main`) — **do not open it**.
- Does **not** have the nested probe (that path is a 404 on `main`).
- Must **not** receive a product patch for this restore (P3 / BR-1).
- Must **not** ship `specs/tasks/task-2601.md` (P5 / BR-4). If that dump is still on `builderforce/task-2601`, delete it as hygiene on this branch only — it is not the restore.

If the developer container is bound to #2601 or `main`: **STOP**. Do not `write_file` the nested path. Do not copy the 138-line module into `Builderforce.ai/frontend/...`. Re-bind / check out `builderforce/task-2554` and proceed from D-2.

### D-1 — Topology (two paths, one real)

Independently re-verified 2026-09-16 against GitHub (raw + commit page + this bind), not re-derived from BA/PM notes:

| Path | Role | `origin/main` | `builderforce/task-2554` tip | After restore |
|---|---|---|---|---|
| `frontend/src/domains/canvas/domain/canvasChange.ts` | Live domain module | 138 lines, intact | identical 138 lines | **Unchanged** vs `origin/main` |
| `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` | Spurious probe from `0267ffd` | **absent** (HTTP 404) | present (HTTP 200); 2-line `export {}` probe | **Absent** |

`0267ffd` is an **add only** (`+2 / −0`). Commit title: *Add Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts — task #2554*. Body:

```ts
// probe — will be overwritten; write_file capability check
export {};
```

It is **not** `CANVAS_CHANGE_PROBE`. It did **not** overwrite the live module.

The nested `Builderforce.ai/` prefix is not a package, not a workspace member, and is not imported. TypeScript path aliases do not resolve it. Later commits on `task-2554` (`8ececde`, `b006b5d`) are PRD dumps for #2554 and do not touch either canvasChange path — `git revert 0267ffd` is expected to apply cleanly.

**Live public surface** (also the imports in `frontend/src/domains/canvas/domain/canvasChange.test.ts`):

- `ProposedCanvasChange` (discriminated union: object/connection add/update/delete/layout/action)
- `canvasChangesCanAutoApply`
- `CONNECTED_CANVAS_ACTIONS`
- `canInvokeCreationObjectAction`

The module does **not** export `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE`. Inventing those names is a new API and is out of scope (P2).

`CONNECTED_CANVAS_ACTIONS` already lists `form: ['publish', 'collect', 'close']` and `idea: ['explore', 'test', 'reveal']` on **main**. Those rows are #2554 product work, not this restore — leave them.

### D-2 — Apply sequence (developer, in_progress, on `builderforce/task-2554`)

Preferred:

```
git checkout builderforce/task-2554
git revert --no-edit 0267ffd360e524dbd6ae8a93d1e71df8202d4dee
```

If revert cannot apply (unexpected later touch of the nested path):

```
git rm -f -- Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts
# prune now-empty prefix dirs so they are not committed
git commit -m "Revert 0267ffd — delete nested probe canvasChange.ts (#2554, #2601)"
```

Commit message **must** name `0267ffd` / `#2554` / `#2601` (BR-6). File set of that commit: the nested probe blob (and empty `Builderforce.ai/` prefix dirs) **only**.

Do **not** `--force`. Do **not** rebase / rewrite `0267ffd` out of history unless revert cannot delete the nested file.

Do **not** open a PR from `builderforce/task-2601` that claims to restore canvasChange.ts. The restore commit lands on `builderforce/task-2554`. #2601 stays the scheduling / sign-off ticket.

### D-3 — Frozen files (do not open)

- `frontend/src/domains/canvas/domain/canvasChange.ts` (live 138-line module)
- `frontend/src/domains/canvas/domain/canvasChange.test.ts` (imports the live surface; 100 lines; no rewrite)
- `CONNECTED_CANVAS_ACTIONS` (including existing `form` / `idea` acts)
- `PHASE_SURFACES` and any PRD 27 survey wiring (B0–B5 on #2554)
- CI, merge-queue, branch-protection config

### D-4 — Fail-closed (these are design failures, not “helpful restores”)

1. Writing the 138-line module into `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (still ships a junk prefix tree if #2554 merges).
2. Inventing `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE` / `"task-2554-repo-probe"`.
3. Patching the live module on `builderforce/task-2601` or `main`.
4. Adding or re-adding `specs/tasks/task-2601.md` (or any docs dump) as a stand-in for the revert.
5. Merging `builderforce/task-2554` before D-5 holds.
6. Any new probe, placeholder, or partial reimplementation of `canvasChange.ts`.

### D-5 — Proof matrix (definition of done = BR-5)

| Check | Pass |
|---|---|
| `git show builderforce/task-2554:Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` | **fails** (path gone) |
| `git diff origin/main...builderforce/task-2554 -- frontend/src/domains/canvas/domain/canvasChange.ts` | **empty** |
| Nested tree `Builderforce.ai/` | absent (empty dirs not committed) |
| Live module exports | `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction` only |
| Strings `CANVAS_CHANGE_PROBE`, `task-2554-repo-probe` | absent on tip |
| 2-line write_file probe (`export {};` under the nested prefix) | absent |
| `builderforce/task-2554` merge | **held** until every row above passes |

No new tests. Existing `canvasChange.test.ts` is the regression net for the live API (FR-6). Completing PRD 27 B0–B5 is **not** done for #2601.

### D-6 — Layers

- **Layer A (product code on this ticket):** none. Restore is a git delete on another branch.
- **Layer B (apply):** developer on in_progress, checkout `builderforce/task-2554`, D-2 then D-5. Attributable revert commit naming `0267ffd` / #2554 / #2601.
- **Layer C (this #2601 branch):** hygiene only — delete leftover `specs/tasks/task-2601.md` if still present. Do not rewrite `canvasChange.ts` here.

This architect pass does **not** execute D-2 (wrong bind: `builderforce/task-2601`; P3 / BR-1). It locks Design so the developer has a fail-closed apply plan.

_— Architect (CTO) · ready · 2026-09-16 · execution #31868_

_— rewritten by CTO · 2026-09-16T16:20:50.707Z_

## Implementation Notes

_Owned by the developer — to be authored._

## Review

_Owned by the code-reviewer — to be authored._

## Test Evidence

_Owned by the qa-tester — to be authored._

## Acceptance

_Owned by the validator — to be authored._

---

### Update — Ada (Sr. Product Mgr) · 2026-09-16T15:46:55.074Z · execution #31711

**BA (Ada) · 2026-09-16:** Authored Requirements (BR-1–BR-6) and corrected Problem & Goal / Scope / Functional requirements / Acceptance criteria / Out of scope. Verified against SeanHogg/Builderforce.ai: `0267ffd` **adds** nested `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` (2-line `export {}` probe); real `frontend/src/domains/canvas/domain/canvasChange.ts` (138 lines, `ProposedCanvasChange` / `canvasChangesCanAutoApply` / `CONNECTED_CANVAS_ACTIONS` / `canInvokeCreationObjectAction`) is intact on main and on `builderforce/task-2554`. Done = revert/delete the nested path so it is absent (matches main). Do not write the real module into the nested prefix. Do not invent `CanvasChangeKind` / `applyObjectChange`. Developer executes the revert on `builderforce/task-2554`; this BA pass does not rewrite `canvasChange.ts`.

---

### Update — Kevin BA/PM/PO (Durable) · 2026-09-16T15:56:14.744Z · execution #31755

**PM (Kevin BA/PM/PO) · 2026-09-16 · execution #31755 — product decision lock (todo)**

Re-verified independently against public GitHub (raw + commit page), not re-derived from BA notes:

| Check | Result |
|---|---|
| `SeanHogg/Builderforce.ai@0267ffd` | **Add only** (`+2 / −0`). Commit title: *Add Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts — task #2554*. Body is the 2-line write_file probe (`// probe — will be overwritten; write_file capability check` + `export {};`). **Not** `CANVAS_CHANGE_PROBE`. |
| Nested path on `builderforce/task-2554` | **Still present** (HTTP 200). Must be **absent** after restore. |
| Nested path on `origin/main` | **Absent** (HTTP 404). Matching main means delete, not rewrite. |
| Real module on `main` and on `builderforce/task-2554` | **Intact and identical.** Exports `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction`. Does **not** export `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE`. |

**Product decisions (locked for architect / developer / reviewers):**

1. **P0 — No user-facing change.** This ticket is a merge-gate defect restore. Canvas behaviour, auto-apply rules, and `CONNECTED_CANVAS_ACTIONS` (including existing `form` / `idea` acts already on main) stay as they are. There is no UX, copy, or API change to ship.
2. **P1 — “Restore” means delete the nested probe.** Success is `Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` **gone** from the tip of `builderforce/task-2554`, matching `origin/main`. Writing the 138-line module into that prefix is a **fail** (still ships a junk `Builderforce.ai/` tree).
3. **P2 — Original ticket prose is superseded.** The board description that said `0267ffd` overwrote the real module with `CANVAS_CHANGE_PROBE` / that we must restore `CanvasChangeKind` / `applyObjectChange` at the nested path is **false**. This PRD + BR-1–BR-6 is the source of truth. Do not invent those names.
4. **P3 — Land the revert on `builderforce/task-2554`, not on `builderforce/task-2601`.** #2601 schedules and gates the restore independently of PRD 27 survey work (#2554). A patch on `task-2601` / `main` that rewrites the live 138-line blob is out of scope.
5. **P4 — Hold merge of #2554** until BR-5: `git show builderforce/task-2554:Builderforce.ai/frontend/src/domains/canvas/domain/canvasChange.ts` fails, and `git diff origin/main...builderforce/task-2554 -- frontend/src/domains/canvas/domain/canvasChange.ts` is empty.
6. **P5 — Do not ship `specs/tasks/task-2601.md`.** A docs-only dump of this PRD already exists on `builderforce/task-2601` (HTTP 200). BR-4 forbids it as a stand-in for the revert. Developer/operator: delete that path if it remains; do not add another.

**This PM pass does not execute `git revert 0267ffd`.** Bound workspace for #2601 is empty (GitHub API 403 rate limit on list/search/write). The probe lives on `builderforce/task-2554`. Developer on in_progress executes BR-2 there.

**Definition of product-done:** nested probe absent; real canvas-change API unchanged; #2554 unmerged until that is true. Completing PRD 27 B0–B5 is not this ticket.

_— Kevin BA/PM/PO (Durable) · product-manager · todo · 2026-09-16_

---

### Update — Product Manager · 2026-09-16T16:05:44.495Z · execution #31788

**PO (ready) · 2026-09-16 · execution #31788 — scope + acceptance approved**

Independently re-verified against GitHub (not re-derived from BA/PM notes):

| Check | Result |
|---|---|
| `SeanHogg/Builderforce.ai@0267ffd` | Add only (`+2 / −0`). Nested 2-line write_file probe (`// probe — will be overwritten; write_file capability check` + `export {};`). |
| Nested path on `builderforce/task-2554` | Still present (HTTP 200). Restore **not yet executed** — expected at this lane. |
| Nested path on `origin/main` | Absent (HTTP 404). |
| Real `frontend/src/domains/canvas/domain/canvasChange.ts` | Intact and identical on main and task-2554 (138 lines). Exports `ProposedCanvasChange`, `canvasChangesCanAutoApply`, `CONNECTED_CANVAS_ACTIONS`, `canInvokeCreationObjectAction`. Does **not** export `CanvasChangeKind`, `applyObjectChange`, or `CANVAS_CHANGE_PROBE`. `canvasChange.test.ts` imports that same surface. |
| `builderforce/task-2554` merge | Unmerged (4 commits ahead, no PR) — hold until BR-5. |
| `specs/tasks/task-2601.md` on `builderforce/task-2601` | Still HTTP 200 (docs dump). P5/BR-4 forbid it. Not a substitute for the revert. |

**PO verdict on ready-lane contract:** **approved.**

- Problem, Scope, FR-1–FR-6, BR-1–BR-6, Acceptance criteria, and Out of scope are the source of truth. Original board prose that claimed overwrite / `CANVAS_CHANGE_PROBE` / `CanvasChangeKind` / `applyObjectChange` is superseded (P2).
- Adopt PM P0–P5 unchanged. No user-facing product change. Restore = **delete** the nested probe on `builderforce/task-2554`, not rewrite the live 138-line module, and not write that module into the nested prefix.
- Architect still owns Design on this same ready lane. Developer on in_progress executes `git revert 0267ffd` (or equivalent delete) **on `builderforce/task-2554`**, commit naming `0267ffd` / #2554 / #2601.
- This PO pass does **not** execute the revert (wrong branch bind + BR-1/P3). Definition of product-done remains BR-5: nested path gone **and** empty diff vs main on the real module; #2554 unmerged until then.

_— Product Owner · ready · 2026-09-16_