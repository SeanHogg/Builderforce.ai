{
  "name": "@webdit/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types for WebDiT runtime + converter (manifest format, architecture ids, quantization modes).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck:native": "tsgo --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.6",
    "@typescript/native-preview": "7.0.0-dev.20260627.2"
  }
}
