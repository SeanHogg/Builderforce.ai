# Tables using a (kind, id) pair with no FOREIGN KEY to `objects` (PRD 20 §2). The registry exists; each entry clears when every target it can name is registered.
# Regenerate: node scripts/check-polymorphic-fk.mjs --update
activity_log.(target_type, target_id)
creation_session_objects.(resource_type, resource_id)
proposal_evaluations.(subject_type, subject_id)
