# Tables whose name matches a kernel primitive shape (PRD 20 §0). A listed entry has been looked at; it is not necessarily wrong.
# Regenerate: node scripts/check-shape-lint.mjs --update
agent_host_sync_history
agent_inference_logs
alert_events
artifact_likes
audit_report_runs
board_connections
brain_chat_messages
calendar_connections
catalog_adoption_events
ceremony_participants
chat_members
chat_messages
coaching_notes
connector_call_logs
connector_connections
coordination_notes
creation_outcome_events
creation_session_comments
creation_session_snapshots
deployment_events
dev_team_members
drive_connections
email_preferences
error_collector_integrations
# feedback_collector_integrations — ADJUDICATED, same verdict as its twin above.
#   Not a `connection`: the kernel connection models a WORKSPACE's account with a
#   vendor (an OAuth identity, tokens, a refresh cycle). This row is a child of ONE
#   feedback collector holding ONE inbound signing secret, with no identity, no
#   token lifecycle and no outbound calls. It cascades with its collector, which a
#   workspace-level connection row could not.
feedback_collector_integrations
execution_messages
freelancer_conversations
freelancer_messages
freelancer_notifications
ide_training_jobs
import_runs
incident_events
integration_sync_logs
knowledge_document_tags
knowledge_document_versions
knowledge_documents
legal_document_versions
mailbox_connections
marketing_assets
marketing_campaign_sends
marketing_tool_runs
marketplace_skill_likes
meeting_attendees
monitor_events
newsletter_events
on_call_members
personality_events
poker_votes
pr_reconciliation_runs
project_insight_events
project_manager_configs
prompt_library_versions
qa_routing_settings
qa_runs
rd_tax_credit_config
repo_analysis_runs
sales_associate_settings
sales_coaching_notes
source_control_integrations
spec_versions
team_members
tenant_members
tenant_openrouter_connections
tool_audit_events
tool_runs
# feedback_webhook_deliveries — ADJUDICATED: the name matches, the shape does not.
#   The `delivery` primitive models something WE SEND (a dispatch, its attempts,
#   its outcome). This is the mirror image: an index of what a provider sent US,
#   existing only so a retry of an already-handled delivery cannot open a second
#   ticket. It has no recipient, no payload, no attempt count and no terminal
#   state — a unique index is its entire behaviour.
feedback_webhook_deliveries
webhook_deliveries
