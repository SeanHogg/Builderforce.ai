{
  "id": "conflict-detection",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "windowThresholdDays": {
        "type": "integer",
        "minimum": 1,
        "maximum": 30,
        "default": 7,
        "description": "Fallback review-window size in days, used when requests carry no explicit review window or priority version."
      }
    }
  }
}
